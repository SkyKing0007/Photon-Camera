26528 / 0.9726528 — optical handoff UI-thread lifecycle repair

Observed on-device regression in 26527:
- transition curtain appears at an optical boundary;
- process disappears/restarts before the new lens session becomes stable;
- target physical cameras subsequently open normally after app relaunch;
- app-local log contains no Java FATAL line because the process dies before its file logger flushes the system exception.

Exact 26527 code diagnosis:
- 26526 IrisZoomController called CaptureController.restartCamera() directly from ScaleGestureDetector / UI thread.
- 26527 restartCameraForIrisHandoff() added the curtain, then dispatched restartCamera() via processExecutor.
- restartCamera() directly invokes cameraEventsListener.onCameraRestarted() and configureTransform()/GLPreview state, which are UI-owned operations.
- therefore 26527 changed the established thread contract at every optical handoff.

26528 correction:
- only CaptureController.java changes;
- curtain remains;
- requested/displayed zoom split remains;
- sequential optical-anchor transitions remain;
- immutable session-route proof remains;
- adaptive direct/logical+physical routing remains;
- 1.6 s curtain fail-safe remains;
- actual restartCamera() is always marshalled to Activity.runOnUiThread after the curtain closes, including chained handoffs originating from CameraBackground.

Frozen 26527 image pipeline:
- active Iris 26521 Spatial/MGC final rejection/alignment parity unchanged;
- DNG SubIFD preview unchanged;
- 26525 DefaultCrop unchanged;
- RawVideo unchanged;
- no exposure, merge, denoise, tone, sharpening, or temporal math changes.

Successful-26527 runtime authority expected:
- handoff HEAD: ef5f868802ced149f6700d524ff0ef6f08fbb675
- candidate tar SHA-256: f0240428386a8e14d3926a0a322736dd68d4d41b01250fd073465ea03c7b94a5
- candidate manifest SHA-256: 61e552b9e602477119ffe383623d28111460ed2695bcba83db4058a8d13f466e

Runtime changed-owner allowlist: exactly 1
app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java

must point exactly at ef5f868802ced149f6700d524ff0ef6f08fbb675.
