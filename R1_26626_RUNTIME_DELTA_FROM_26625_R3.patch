diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index 1018e555bff945538de346ddb139b6fba18b32e1..99c534688f71b799f38c8880c03d122ebed3fc15 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -1,23 +1,135 @@
 precision highp float;
+precision highp int;
 precision mediump sampler2D;
 
 uniform sampler2D GlobalMappedLog;
+uniform sampler2D SourceLinear;
+uniform sampler2D SourceGuideLog;
+uniform sampler2D LocalBand;
+uniform sampler2D CoarseReconstruction;
+uniform sampler2D CurrentToneLog;
+uniform sampler2D DeltaLog;
 uniform float referenceLog;
+uniform float sourceReferenceLog;
+uniform float mappedReferenceLog;
 uniform float sigmaEv;
 uniform float edgeSlope;
+uniform float iris26626PreservationStrength;
+uniform int iris26626Mode;
 out float Output;
 
 /* IRIS_26621_TRUE_LOCAL_LAPLACIAN_REMAP
- * Fast Local-Laplacian intensity slice. alpha=1 exactly preserves small/detail
- * log-luminance differences. Only deviations outside sigmaEv use the gentler
- * edge slope. The remap is continuous, monotonic and symmetric around referenceLog. */
-float irisRemap(float x){
-    float d=x-referenceLog;
+ * Mode 0 is the exact successful-26625 remap equation. Other modes are 26626-only helpers used
+ * inside the same Local-Laplacian owner; none changes the protected reduce/accumulate/reconstruct
+ * shaders or the final uniform-RGB render owner. */
+const vec3 IRIS_26626_LUMA=vec3(0.22897456,0.69173852,0.07928691);
+const float IRIS_26626_LOG_FLOOR=0.000244140625; /* 2^-12 */
+
+float irisRemapDelta(float x,float center){
+    float d=x-center;
     float a=abs(d);
     float mapped=a<=sigmaEv ? a : sigmaEv+edgeSlope*(a-sigmaEv);
-    return referenceLog+(d<0.0?-mapped:mapped);
+    return d<0.0 ? -mapped : mapped;
+}
+float iris26626SourceGuide(vec3 rgb){
+    rgb=max(rgb,vec3(0.0));
+    float y=dot(rgb,IRIS_26626_LUMA);
+    float peak=max(rgb.r,max(rgb.g,rgb.b));
+    return max(y,peak);
+}
+float irisAt(sampler2D tex,ivec2 p){
+    ivec2 sz=textureSize(tex,0);
+    return texelFetch(tex,clamp(p,ivec2(0),sz-ivec2(1)),0).r;
+}
+void irisAxis(int fineCoord,out int base,out float wm,out float wc,out float wp){
+    base=fineCoord/2;
+    if((fineCoord&1)==0){wm=0.125;wc=0.750;wp=0.125;}
+    else{wm=0.0;wc=0.5;wp=0.5;}
+}
+float irisExpand(sampler2D tex,ivec2 p){
+    int bx,by;float wxm,wxc,wxp,wym,wyc,wyp;
+    irisAxis(p.x,bx,wxm,wxc,wxp);irisAxis(p.y,by,wym,wyc,wyp);
+    float sum=0.0;
+    sum+=irisAt(tex,ivec2(bx-1,by-1))*(wxm*wym);
+    sum+=irisAt(tex,ivec2(bx  ,by-1))*(wxc*wym);
+    sum+=irisAt(tex,ivec2(bx+1,by-1))*(wxp*wym);
+    sum+=irisAt(tex,ivec2(bx-1,by  ))*(wxm*wyc);
+    sum+=irisAt(tex,ivec2(bx  ,by  ))*(wxc*wyc);
+    sum+=irisAt(tex,ivec2(bx+1,by  ))*(wxp*wyc);
+    sum+=irisAt(tex,ivec2(bx-1,by+1))*(wxm*wyp);
+    sum+=irisAt(tex,ivec2(bx  ,by+1))*(wxc*wyp);
+    sum+=irisAt(tex,ivec2(bx+1,by+1))*(wxp*wyp);
+    return sum;
 }
+
 void main(){
     ivec2 p=ivec2(gl_FragCoord.xy);
-    Output=irisRemap(texelFetch(GlobalMappedLog,p,0).r);
+
+    if(iris26626Mode==1){
+        float sourceGuide=iris26626SourceGuide(texelFetch(SourceLinear,p,0).rgb);
+        Output=log2(max(sourceGuide,IRIS_26626_LOG_FLOOR));
+        return;
+    }
+    if(iris26626Mode==2){
+        float sourceGuide=iris26626SourceGuide(texelFetch(SourceLinear,p,0).rgb);
+        float sourceLog=log2(max(sourceGuide,IRIS_26626_LOG_FLOOR));
+        Output=mappedReferenceLog+irisRemapDelta(sourceLog,sourceReferenceLog);
+        return;
+    }
+    if(iris26626Mode==3){
+        Output=exp2(texelFetch(SourceGuideLog,p,0).r);
+        return;
+    }
+    if(iris26626Mode==4){
+        float sourceReconstruction=texelFetch(LocalBand,p,0).r+irisExpand(CoarseReconstruction,p);
+        Output=sourceReconstruction-texelFetch(CurrentToneLog,p,0).r;
+        return;
+    }
+    if(iris26626Mode==5){
+        float sourceGuide=iris26626SourceGuide(texelFetch(SourceLinear,p,0).rgb);
+        float sourceLog=log2(max(sourceGuide,IRIS_26626_LOG_FLOOR));
+        float upperGate=smoothstep(0.65,0.72,sourceGuide)
+                *(1.0-smoothstep(0.98,1.05,sourceGuide));
+        float current=texelFetch(CurrentToneLog,p,0).r;
+        float delta=texelFetch(DeltaLog,p,0).r;
+
+        /* IRIS_26626_FAIL_CLOSED_SOURCE_STRUCTURE_GATE
+         * The source-domain candidate may only preserve structure that is locally present in the
+         * source itself.  A one-sided material step has zero balance; a flat bright plateau beside
+         * a distant edge has zero curvature.  Symmetric ridges/valleys and smooth curved fields may
+         * contribute, but the correction is capped by their measured source-log curvature so it
+         * cannot invent or over-amplify contrast.  Radii are tied to existing Local-Laplacian scales. */
+        float allowed=0.0;
+        const int IRIS_26626_R0=4;
+        const int IRIS_26626_R1=16;
+        const int IRIS_26626_R2=32;
+        for(int k=0;k<3;k++){
+            int r=k==0?IRIS_26626_R0:(k==1?IRIS_26626_R1:IRIS_26626_R2);
+            ivec2 dx=ivec2(r,0);
+            ivec2 dy=ivec2(0,r);
+            float lx=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,clamp(p-dx,ivec2(0),textureSize(SourceLinear,0)-ivec2(1)),0).rgb),IRIS_26626_LOG_FLOOR));
+            float rx=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,clamp(p+dx,ivec2(0),textureSize(SourceLinear,0)-ivec2(1)),0).rgb),IRIS_26626_LOG_FLOOR));
+            float uy=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,clamp(p-dy,ivec2(0),textureSize(SourceLinear,0)-ivec2(1)),0).rgb),IRIS_26626_LOG_FLOOR));
+            float dyv=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,clamp(p+dy,ivec2(0),textureSize(SourceLinear,0)-ivec2(1)),0).rgb),IRIS_26626_LOG_FLOOR));
+
+            float ax0=abs(sourceLog-lx), ax1=abs(sourceLog-rx);
+            float ay0=abs(sourceLog-uy), ay1=abs(sourceLog-dyv);
+            float axMax=max(ax0,ax1), ayMax=max(ay0,ay1);
+            float bx=axMax>0.003 ? min(ax0,ax1)/axMax : 0.0;
+            float by=ayMax>0.003 ? min(ay0,ay1)/ayMax : 0.0;
+            float cx=sourceLog-0.5*(lx+rx);
+            float cy=sourceLog-0.5*(uy+dyv);
+            float sx=(cx*delta)>0.0 ? bx*abs(cx) : 0.0;
+            float sy=(cy*delta)>0.0 ? by*abs(cy) : 0.0;
+            allowed=max(allowed,max(sx,sy));
+        }
+        allowed=min(allowed,0.20); /* pre-strength EV cap; max final correction <=0.06 EV */
+        float correction=sign(delta)*min(abs(delta),allowed);
+        Output=current+upperGate*clamp(iris26626PreservationStrength,0.0,0.30)*correction;
+        return;
+    }
+
+    /* Mode 0: byte-equivalent successful-26625 scalar equation. */
+    float x=texelFetch(GlobalMappedLog,p,0).r;
+    Output=referenceLog+irisRemapDelta(x,referenceLog);
 }
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index e587919ad3aa6d92ec763fe3b885e129f2ee1611..a42886410be9978004e11dc3b37a07ef64f28f60 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -1037,6 +1037,116 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 
+    /* IRIS_26626_SHORT_COMPONENT_DIAGNOSTICS_ONLY
+     * Read-only coverage-grid proof. Existing 26607/26611 component membership, seed, trust and
+     * compatibleFlow equations remain byte-for-byte owners. This shader only classifies the final
+     * state so device logs can distinguish missing component membership from a CFA-safe flow
+     * barrier. RGBA = membership, untrusted membership, hard-barrier frontier, open frontier. */
+    val shortComponentDiagnostics26626 = """
+        #version 300 es
+        precision highp float;
+        precision highp int;
+        uniform sampler2D uAnchor;
+        uniform sampler2D uTrust;
+        uniform ivec2 uGridSize;
+        uniform ivec2 uRawSize;
+        layout(location = 0) out vec4 oDiagnostics;
+
+        ivec2 clampGrid(ivec2 p) { return clamp(p, ivec2(0), uGridSize - ivec2(1)); }
+        float trustAt(ivec2 p) { return clamp(texelFetch(uTrust, clampGrid(p), 0).r, 0.0, 1.0); }
+        vec2 rawFlowAtAnchor(ivec2 p) {
+            return texelFetch(uAnchor, clampGrid(p), 0).xy * vec2(uRawSize);
+        }
+        float compatibleFlow(ivec2 a, ivec2 b) {
+            float delta = length(rawFlowAtAnchor(a) - rawFlowAtAnchor(b));
+            return 1.0 - smoothstep(0.50, 1.25, delta);
+        }
+        void main() {
+            ivec2 p = clampGrid(ivec2(gl_FragCoord.xy));
+            vec4 anchor = texelFetch(uAnchor, p, 0);
+            float component = anchor.z > 0.0 ? 1.0 : 0.0;
+            float trust = trustAt(p);
+            float untrusted = component > 0.0 && trust <= 0.0 ? 1.0 : 0.0;
+            float hardBarrier = 0.0;
+            float openFrontier = 0.0;
+            if (untrusted > 0.0) {
+                for (int i = 0; i < 4; ++i) {
+                    ivec2 offset = i == 0 ? ivec2(-1, 0) :
+                                  i == 1 ? ivec2( 1, 0) :
+                                  i == 2 ? ivec2( 0,-1) : ivec2(0, 1);
+                    ivec2 q = clampGrid(p + offset);
+                    if (trustAt(q) > 0.0) {
+                        float compatibility = compatibleFlow(p, q);
+                        if (compatibility <= (1.0 / 255.0)) hardBarrier = 1.0;
+                        else openFrontier = 1.0;
+                    }
+                }
+            }
+            oDiagnostics = vec4(component, untrusted, hardBarrier, openFrontier);
+        }
+    """.trimIndent()
+
+    /* IRIS_26626_SHORT_COMMON_ACCUMULATOR_PROXY
+     * Diagnostic-only normalized sample of the exact common Sabre color/weight accumulator at the
+     * existing quarter-resolution coverage grid. It creates no alternate image or Resolve owner. */
+    val shortAccumulatorProxy26626 = """
+        #version 300 es
+        precision highp float;
+        precision highp int;
+        uniform sampler2D uAccumulatedColor;
+        uniform sampler2D uAccumulatedWeightsGb;
+        uniform ivec2 uProbeSize;
+        layout(location = 0) out vec4 oProxy;
+        void main() {
+            vec2 uv = gl_FragCoord.xy / vec2(uProbeSize);
+            vec4 color = texture(uAccumulatedColor, uv);
+            vec2 weightsGb = texture(uAccumulatedWeightsGb, uv).rg;
+            vec3 weights = max(vec3(color.a, weightsGb.r, weightsGb.g), vec3(1.0e-6));
+            vec3 normalized = color.rgb / weights;
+            oProxy = vec4(normalized, (weights.r + weights.g + weights.b) / 3.0);
+        }
+    """.trimIndent()
+
+    /* IRIS_26626_SHORT_COMMON_ACCUMULATOR_DELTA
+     * Compare the same normalized common-accumulator proxy immediately after the unchanged final
+     * SHORT merge. The copied target mask restricts proof to the exact pre-existing rescue domain.
+     * RGBA8 = target cell, changed normalized color, relative color delta, relative weight delta. */
+    val shortAccumulatorDelta26626 = """
+        #version 300 es
+        precision highp float;
+        precision highp int;
+        uniform sampler2D uBeforeProxy;
+        uniform sampler2D uAccumulatedColor;
+        uniform sampler2D uAccumulatedWeightsGb;
+        uniform sampler2D uTargetMask;
+        uniform ivec2 uProbeSize;
+        layout(location = 0) out vec4 oDelta;
+        void main() {
+            ivec2 p = clamp(ivec2(gl_FragCoord.xy), ivec2(0), uProbeSize - ivec2(1));
+            float target = texelFetch(uTargetMask, p, 0).r > 0.0 ? 1.0 : 0.0;
+            if (target <= 0.0) {
+                oDelta = vec4(0.0);
+                return;
+            }
+            vec2 uv = gl_FragCoord.xy / vec2(uProbeSize);
+            vec4 before = texelFetch(uBeforeProxy, p, 0);
+            vec4 color = texture(uAccumulatedColor, uv);
+            vec2 weightsGb = texture(uAccumulatedWeightsGb, uv).rg;
+            vec3 weights = max(vec3(color.a, weightsGb.r, weightsGb.g), vec3(1.0e-6));
+            vec3 after = color.rgb / weights;
+            vec3 denom = max(max(abs(before.rgb), abs(after)), vec3(1.0));
+            float relativeColor = max(max(abs(after.r - before.r) / denom.r,
+                                         abs(after.g - before.g) / denom.g),
+                                      abs(after.b - before.b) / denom.b);
+            float afterWeight = (weights.r + weights.g + weights.b) / 3.0;
+            float relativeWeight = abs(afterWeight - before.a) / max(max(abs(before.a), abs(afterWeight)), 1.0);
+            float changed = max(relativeColor, relativeWeight) > 1.0e-5 ? 1.0 : 0.0;
+            oDelta = vec4(target, changed,
+                          clamp(relativeColor * 64.0, 0.0, 1.0),
+                          clamp(relativeWeight * 64.0, 0.0, 1.0));
+        }
+    """.trimIndent()
+
     /* IRIS_26607_UNIVERSAL_EFFECTIVE_HIGHLIGHT_LOSS_WEIGHT
      * Rescue is triggered by physical NORMAL information loss, not by semantic scene type and not
      * only by literal sensor code saturation. For each spatial CFA phase, exposure-normalized
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index c08204ff555d2cdb3eea25a873b19038738fdf8e..82c3070e4731d31e27f0797850e1a81ff3401158 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -575,6 +575,9 @@ internal class GlesMgcRawSpatialStacker(
     private var sabreShortComponentAnchorProgram26607 = 0
     private var sabreShortComponentPropagateProgram26607 = 0
     private var sabreShortComponentEffectiveFlowProgram26625 = 0
+    private var sabreShortComponentDiagnosticsProgram26626 = 0
+    private var sabreShortAccumulatorProxyProgram26626 = 0
+    private var sabreShortAccumulatorDeltaProgram26626 = 0
     private var sabreShortRescueWeightProgram26607 = 0
     private var sabreRgbChromaPostprocessor: GlesIris26529SpatialRgbChromaPostprocessor? = null
     private var sabreConvertAlignmentSparseProgram = 0
@@ -2469,6 +2472,13 @@ internal class GlesMgcRawSpatialStacker(
                 }
                 var mergeFlow = flow
                 var shortFallbackTrustedCells26625 = 0
+                var shortComponentMembershipCells26626 = 0
+                var shortUntrustedComponentCells26626 = 0
+                var shortHardBarrierFrontierCells26626 = 0
+                var shortOpenFrontierCells26626 = 0
+                var shortContributionMask26626 = 0
+                var shortAccumulatorBefore26626 = 0
+                var shortAccumulatorContribution26626: ShortAccumulatorContribution26626? = null
                 val unblockerWidth = ceilDiv(width, UNBLOCKER_FULLRES_TILE_SIZE * 2)
                 val unblockerHeight = ceilDiv(height, UNBLOCKER_FULLRES_TILE_SIZE * 2)
                 val unblocker = createTexture(
@@ -2556,6 +2566,10 @@ internal class GlesMgcRawSpatialStacker(
                     )
                     mergeFlow = component.effectiveFlow
                     shortFallbackTrustedCells26625 = component.fallbackTrustedCells
+                    shortComponentMembershipCells26626 = component.componentMembershipCells26626
+                    shortUntrustedComponentCells26626 = component.untrustedComponentCells26626
+                    shortHardBarrierFrontierCells26626 = component.hardBarrierFrontierCells26626
+                    shortOpenFrontierCells26626 = component.openFrontierCells26626
                     highlightShortBoundarySeedCells26607 = component.seedCells
                     highlightShortBoundaryFinalCells26607 = component.finalCells
                     val rescuedWeight = createTexture(
@@ -2676,6 +2690,23 @@ internal class GlesMgcRawSpatialStacker(
                     )
                     highlightShortTargetAccumulatorEligibleCells26607 = rescueStats.nonzeroCells
                     highlightShortTargetEligibleCellsAbi26607 = rescueStats.nonzeroCells
+                    /* IRIS_26626_SHORT_ACCUMULATOR_PROOF_SNAPSHOT
+                     * Copy only the small target-rescue mask and normalized common accumulator proxy.
+                     * Existing rescue/effective coverage release timing remains unchanged below. */
+                    shortContributionMask26626 = createTexture(
+                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_NEAREST,
+                    )
+                    copySabreR8Mask26626(
+                        rescueEffectiveCoverage, shortContributionMask26626,
+                        coverageWidth, coverageHeight,
+                    )
+                    shortAccumulatorBefore26626 = createTexture(
+                        coverageWidth, coverageHeight, GLES30.GL_RGBA16F, GLES30.GL_NEAREST,
+                    )
+                    renderSabreShortAccumulatorProxy26626(
+                        accumulatedColor, accumulatedWeightsGb, shortAccumulatorBefore26626,
+                        coverageWidth, coverageHeight,
+                    )
                     renderSabreCoverage(
                         effectiveCoverage, accumulatedCoverage, coverageWidth, coverageHeight,
                         accumulatedWeightScale,
@@ -2724,6 +2755,23 @@ internal class GlesMgcRawSpatialStacker(
                     accumulatedValidity26614 = accumulatedValidity26614,
                     validityWeightScale26614 = validityWeightScale26614,
                 )
+                if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT &&
+                    shortContributionMask26626 != 0 && shortAccumulatorBefore26626 != 0) {
+                    shortAccumulatorContribution26626 =
+                        measureSabreShortAccumulatorContribution26626(
+                            shortAccumulatorBefore26626, shortContributionMask26626,
+                            accumulatedColor, accumulatedWeightsGb,
+                            coverageWidth, coverageHeight,
+                        )
+                    releaseOwnedTexture(
+                        shortAccumulatorBefore26626, "26626 SHORT pre-merge accumulator proxy",
+                    )
+                    shortAccumulatorBefore26626 = 0
+                    releaseOwnedTexture(
+                        shortContributionMask26626, "26626 SHORT target contribution mask",
+                    )
+                    shortContributionMask26626 = 0
+                }
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     require(index == frames.lastIndex) {
                         "26604 HIGHLIGHT_SHORT must remain the final scheduled auxiliary frame"
@@ -2747,6 +2795,10 @@ internal class GlesMgcRawSpatialStacker(
                             "fallbackGeometryCannotAuthorizeAlone=true " +
                             "fallbackActualSampling=true " +
                             "fallbackTrustedCells=$shortFallbackTrustedCells26625 " +
+                            "componentMembershipCells=$shortComponentMembershipCells26626 " +
+                            "untrustedComponentCells=$shortUntrustedComponentCells26626 " +
+                            "hardBarrierFrontierCells=$shortHardBarrierFrontierCells26626 " +
+                            "openFrontierCells=$shortOpenFrontierCells26626 " +
                             "literalCoreSelfSeed=false effectiveCoreSelfSeed=false " +
                             "componentMembership=LITERAL_PLUS_BOUNDARY_PROVEN_EFFECTIVE_LOSS " +
                             "effectiveLossPhotometricBypass=BOUNDARY_PROVEN_COMPONENT_ONLY_ADDITIVE " +
@@ -2758,6 +2810,9 @@ internal class GlesMgcRawSpatialStacker(
                             "componentWeightedTargetCells=$highlightShortComponentWeightedCells26607 " +
                             "targetAccumulatorEligibleCells=$highlightShortTargetAccumulatorEligibleCells26607 " +
                             "totalShortAccumulatorEligibleCells=$highlightShortTotalAccumulatorEligibleCells26607 " +
+                            "commonAccumulatorTargetProbeCells=${shortAccumulatorContribution26626?.targetCells ?: 0} " +
+                            "commonAccumulatorChangedProbeCells=${shortAccumulatorContribution26626?.changedCells ?: 0} " +
+                            "commonAccumulatorInputChanged=${(shortAccumulatorContribution26626?.changedCells ?: 0) > 0} " +
                             "resolveEffective=NOT_DIRECTLY_MEASURED outputPreserved=DEVICE_IMAGE_REQUIRED " +
                             "sourceClip3x3Continuous=true bayerQuadWholeRgb=true " +
                             "dng=false srDetailEvidence=false"
@@ -3509,9 +3564,77 @@ internal class GlesMgcRawSpatialStacker(
         val seedCells: Int,
         val finalCells: Int,
         val fallbackTrustedCells: Int,
+        val componentMembershipCells26626: Int,
+        val untrustedComponentCells26626: Int,
+        val hardBarrierFrontierCells26626: Int,
+        val openFrontierCells26626: Int,
         val passes: Int,
     )
 
+    private data class ShortComponentDiagnostics26626(
+        val componentMembershipCells: Int,
+        val untrustedComponentCells: Int,
+        val hardBarrierFrontierCells: Int,
+        val openFrontierCells: Int,
+    )
+
+    /* IRIS_26626_SHORT_COMPONENT_DIAGNOSTICS_ONLY
+     * Read the dedicated RGBA8 classification texture. This never feeds trust, flow, weight, merge
+     * or Resolve; it only proves which already-existing 26607/26611 condition stopped expansion. */
+    private fun measureSabreShortComponentDiagnostics26626(
+        anchorTexture: Int,
+        trustTexture: Int,
+        gridWidth: Int,
+        gridHeight: Int,
+    ): ShortComponentDiagnostics26626 {
+        val diagnostic = createTexture(
+            gridWidth, gridHeight, GLES30.GL_RGBA8, GLES30.GL_NEAREST,
+        )
+        try {
+            val program = sabreShortComponentDiagnosticsProgram26626
+            check(program != 0) { "26626 SHORT component diagnostic program missing" }
+            GLES30.glUseProgram(program)
+            bindTexture(program, "uAnchor", 0, anchorTexture)
+            bindTexture(program, "uTrust", 1, trustTexture)
+            uniform2i(program, "uGridSize", gridWidth, gridHeight)
+            uniform2i(program, "uRawSize", width, height)
+            draw(program, gridWidth, gridHeight, intArrayOf(diagnostic))
+
+            val byteCount = Math.multiplyExact(Math.multiplyExact(gridWidth, gridHeight), 4)
+            val buffer = ByteBuffer.allocateDirect(byteCount)
+            bindRenderTargets(intArrayOf(diagnostic), "26626 SHORT component diagnostic readback")
+            GLES30.glBindBuffer(GLES30.GL_PIXEL_PACK_BUFFER, 0)
+            GLES30.glPixelStorei(GLES30.GL_PACK_ALIGNMENT, 1)
+            GLES30.glReadPixels(
+                0, 0, gridWidth, gridHeight,
+                GLES30.GL_RGBA, GLES30.GL_UNSIGNED_BYTE, buffer,
+            )
+            GLES30.glBindFramebuffer(GLES30.GL_FRAMEBUFFER, 0)
+            checkGlError("26626 SHORT component diagnostic readback")
+            buffer.rewind()
+            var membership = 0
+            var untrusted = 0
+            var hardBarrier = 0
+            var openFrontier = 0
+            repeat(gridWidth * gridHeight) {
+                if ((buffer.get().toInt() and 0xff) != 0) membership++
+                if ((buffer.get().toInt() and 0xff) != 0) untrusted++
+                if ((buffer.get().toInt() and 0xff) != 0) hardBarrier++
+                if ((buffer.get().toInt() and 0xff) != 0) openFrontier++
+            }
+            val details = "membershipCells=$membership untrustedMembershipCells=$untrusted " +
+                "hardBarrierFrontierCells=$hardBarrier openFrontierCells=$openFrontier " +
+                "barrierRawPx=0.50..1.25 equationsUnchanged=true readOnly=true"
+            PLog.i(SABRE_TAG, "IRIS_26626_SHORT_COMPONENT_PROPAGATION_PROOF $details")
+            MotionTrace.processingState("IRIS_26626_SHORT_COMPONENT_PROPAGATION_PROOF", details)
+            return ShortComponentDiagnostics26626(
+                membership, untrusted, hardBarrier, openFrontier,
+            )
+        } finally {
+            releaseOwnedTexture(diagnostic, "26626 SHORT component diagnostics")
+        }
+    }
+
     /* IRIS_26625_RADIOMETRICALLY_VALIDATED_FALLBACK_GEOMETRY
      * The direct-sun 26624 device trace proved a distinct false-closed case: SHORT was captured
      * with 2.5 EV headroom, but only ~1% of sparse cells retained sub-pixel local-affine residuals,
@@ -3638,6 +3761,9 @@ internal class GlesMgcRawSpatialStacker(
             val finalCells = countSabreTrustCells26606(
                 current, gridWidth, gridHeight, "26607 SHORT component final probe",
             )
+            val diagnostics26626 = measureSabreShortComponentDiagnostics26626(
+                anchorTexture, current, gridWidth, gridHeight,
+            )
             val effectiveFlowTexture = createTexture(
                 gridWidth, gridHeight, GLES30.GL_RGBA16F, GLES30.GL_LINEAR,
             )
@@ -3666,7 +3792,12 @@ internal class GlesMgcRawSpatialStacker(
                 effectiveFlowTexture, flow.scaleX, flow.scaleY, flow.offsetX, flow.offsetY,
             )
             return ShortComponentTrust26607(
-                current, effectiveFlow, seedCells, finalCells, fallbackTrustedCells, passes,
+                current, effectiveFlow, seedCells, finalCells, fallbackTrustedCells,
+                diagnostics26626.componentMembershipCells,
+                diagnostics26626.untrustedComponentCells,
+                diagnostics26626.hardBarrierFrontierCells,
+                diagnostics26626.openFrontierCells,
+                passes,
             )
         } finally {
             releaseOwnedTexture(seedProbe, "26607 SHORT component seed probe")
@@ -3880,6 +4011,129 @@ internal class GlesMgcRawSpatialStacker(
         checkGlError(label)
     }
 
+    private data class ShortAccumulatorContribution26626(
+        val targetCells: Int,
+        val changedCells: Int,
+        val maximumRelativeColorDelta: Float,
+        val meanRelativeColorDelta: Float,
+        val maximumRelativeWeightDelta: Float,
+        val meanRelativeWeightDelta: Float,
+    )
+
+    private fun copySabreR8Mask26626(
+        source: Int,
+        output: Int,
+        copyWidth: Int,
+        copyHeight: Int,
+    ) {
+        val program = sabreCopyMaskProgram
+        check(program != 0) { "26626 SHORT mask-copy program missing" }
+        GLES30.glUseProgram(program)
+        bindTexture(program, "uRejection", 0, source)
+        uniform1f(program, "uAccumulatedWeightScale", 1f)
+        draw(program, copyWidth, copyHeight, intArrayOf(output))
+    }
+
+    private fun renderSabreShortAccumulatorProxy26626(
+        accumulatedColor: Int,
+        accumulatedWeightsGb: Int,
+        output: Int,
+        probeWidth: Int,
+        probeHeight: Int,
+    ) {
+        val program = sabreShortAccumulatorProxyProgram26626
+        check(program != 0) { "26626 SHORT accumulator proxy program missing" }
+        GLES30.glUseProgram(program)
+        bindTexture(program, "uAccumulatedColor", 0, accumulatedColor)
+        bindTexture(program, "uAccumulatedWeightsGb", 1, accumulatedWeightsGb)
+        uniform2i(program, "uProbeSize", probeWidth, probeHeight)
+        draw(program, probeWidth, probeHeight, intArrayOf(output))
+    }
+
+    /* IRIS_26626_SHORT_COMMON_ACCUMULATOR_CONTRIBUTION_PROOF
+     * Compare a small quarter-resolution normalized proxy before/after the unchanged final SHORT
+     * merge. This proves whether the common Resolve input actually changed; it does not claim that
+     * Resolve or final presentation preserved that change. */
+    private fun measureSabreShortAccumulatorContribution26626(
+        beforeProxy: Int,
+        targetMask: Int,
+        accumulatedColor: Int,
+        accumulatedWeightsGb: Int,
+        probeWidth: Int,
+        probeHeight: Int,
+    ): ShortAccumulatorContribution26626 {
+        val delta = createTexture(
+            probeWidth, probeHeight, GLES30.GL_RGBA8, GLES30.GL_NEAREST,
+        )
+        try {
+            val program = sabreShortAccumulatorDeltaProgram26626
+            check(program != 0) { "26626 SHORT accumulator-delta program missing" }
+            GLES30.glUseProgram(program)
+            bindTexture(program, "uBeforeProxy", 0, beforeProxy)
+            bindTexture(program, "uAccumulatedColor", 1, accumulatedColor)
+            bindTexture(program, "uAccumulatedWeightsGb", 2, accumulatedWeightsGb)
+            bindTexture(program, "uTargetMask", 3, targetMask)
+            uniform2i(program, "uProbeSize", probeWidth, probeHeight)
+            draw(program, probeWidth, probeHeight, intArrayOf(delta))
+
+            val byteCount = Math.multiplyExact(Math.multiplyExact(probeWidth, probeHeight), 4)
+            val buffer = ByteBuffer.allocateDirect(byteCount)
+            bindRenderTargets(intArrayOf(delta), "26626 SHORT accumulator-delta readback")
+            GLES30.glBindBuffer(GLES30.GL_PIXEL_PACK_BUFFER, 0)
+            GLES30.glPixelStorei(GLES30.GL_PACK_ALIGNMENT, 1)
+            GLES30.glReadPixels(
+                0, 0, probeWidth, probeHeight,
+                GLES30.GL_RGBA, GLES30.GL_UNSIGNED_BYTE, buffer,
+            )
+            GLES30.glBindFramebuffer(GLES30.GL_FRAMEBUFFER, 0)
+            checkGlError("26626 SHORT accumulator-delta readback")
+            buffer.rewind()
+            var targetCells = 0
+            var changedCells = 0
+            var maxColor = 0f
+            var maxWeight = 0f
+            var sumColor = 0.0
+            var sumWeight = 0.0
+            repeat(probeWidth * probeHeight) {
+                val target = buffer.get().toInt() and 0xff
+                val changed = buffer.get().toInt() and 0xff
+                val colorScaled = (buffer.get().toInt() and 0xff) / 255f
+                val weightScaled = (buffer.get().toInt() and 0xff) / 255f
+                if (target != 0) {
+                    targetCells++
+                    if (changed != 0) changedCells++
+                    val relativeColor = colorScaled / 64f
+                    val relativeWeight = weightScaled / 64f
+                    maxColor = maxOf(maxColor, relativeColor)
+                    maxWeight = maxOf(maxWeight, relativeWeight)
+                    sumColor += relativeColor.toDouble()
+                    sumWeight += relativeWeight.toDouble()
+                }
+            }
+            val denom = targetCells.coerceAtLeast(1)
+            val result = ShortAccumulatorContribution26626(
+                targetCells = targetCells,
+                changedCells = changedCells,
+                maximumRelativeColorDelta = maxColor,
+                meanRelativeColorDelta = (sumColor / denom).toFloat(),
+                maximumRelativeWeightDelta = maxWeight,
+                meanRelativeWeightDelta = (sumWeight / denom).toFloat(),
+            )
+            val details = "targetProbeCells=${result.targetCells} changedProbeCells=${result.changedCells} " +
+                "maxRelativeColorDelta=${result.maximumRelativeColorDelta} " +
+                "meanRelativeColorDelta=${result.meanRelativeColorDelta} " +
+                "maxRelativeWeightDelta=${result.maximumRelativeWeightDelta} " +
+                "meanRelativeWeightDelta=${result.meanRelativeWeightDelta} " +
+                "commonAccumulatorInputChanged=${result.changedCells > 0} " +
+                "resolveEffective=NOT_DIRECTLY_MEASURED outputPreserved=DEVICE_IMAGE_REQUIRED"
+            PLog.i(SABRE_TAG, "IRIS_26626_SHORT_ACCUMULATOR_CONTRIBUTION $details")
+            MotionTrace.processingState("IRIS_26626_SHORT_ACCUMULATOR_CONTRIBUTION", details)
+            return result
+        } finally {
+            releaseOwnedTexture(delta, "26626 SHORT accumulator-delta")
+        }
+    }
+
     private data class ShortEffectiveCoverageStats26606(
         val nonzeroCells: Int,
         val strongCells: Int,
@@ -5614,6 +5868,18 @@ internal class GlesMgcRawSpatialStacker(
             GlesMgcRawSabreShaders.shortComponentEffectiveFlow26625,
             "iris_26625_short_component_effective_flow",
         )
+        sabreShortComponentDiagnosticsProgram26626 = linkProgram(
+            GlesMgcRawSabreShaders.shortComponentDiagnostics26626,
+            "iris_26626_short_component_diagnostics",
+        )
+        sabreShortAccumulatorProxyProgram26626 = linkProgram(
+            GlesMgcRawSabreShaders.shortAccumulatorProxy26626,
+            "iris_26626_short_accumulator_proxy",
+        )
+        sabreShortAccumulatorDeltaProgram26626 = linkProgram(
+            GlesMgcRawSabreShaders.shortAccumulatorDelta26626,
+            "iris_26626_short_accumulator_delta",
+        )
         sabreShortRescueWeightProgram26607 = linkProgram(
             GlesMgcRawSabreShaders.shortRescueWeight26607,
             "iris_26607_short_rescue_weight",
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index edf4dbc3993bb5eabdfaf685cf34cc22ca9e65c1..602cd84ccbadc14a915081f1d696e7a907afa6dc 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -111,6 +111,7 @@ public final class MotionV2Render extends Node {
     public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.885f;
     public static final float IRIS_26623_SPARSE_WHITE_SLOPE = 0.270f;
     public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.200f;
+    private static final float IRIS_26626_SOURCE_PRESERVATION_MAX = 0.30f;
 
     static float iris26621MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
         float x = Math.max(sourceGuide, 0.0f);
@@ -381,6 +382,37 @@ public final class MotionV2Render extends Node {
                 basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
     }
 
+    /* IRIS_26626_BOUNDED_SOURCE_DOMAIN_LOCAL_LAPLACIAN_REFERENCE
+     * 26625 remains the canonical baseline. A second transient Local-Laplacian pass uses the
+     * original source-domain guide and source references, but maps every reference center through
+     * the unchanged 26623 tone owner. The final per-pixel blend is capped by the exact pre-code
+     * monotonicity/halo sweep; no scene semantic detector or alternate RGB owner is introduced. */
+    private float iris26626SourcePreservationStrength() {
+        final float requested = Math.max(basePipeline.mParameters.motionV2DisplayGain, 1.0e-6f)
+                * OUTPUT_EXPOSURE_SCALE;
+        final float adaptiveEnable = iris26623Smoothstep(1.05f, 1.25f, requested);
+        final float pressure = iris26623HighlightPressure(
+                basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
+                basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
+                basePipeline.mParameters.motionV2ToneBaseSceneWhite,
+                basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
+        return iris26582Clamp(
+                IRIS_26626_SOURCE_PRESERVATION_MAX * adaptiveEnable * pressure,
+                0.0f, IRIS_26626_SOURCE_PRESERVATION_MAX);
+    }
+
+    private float iris26626MappedReferenceLog(float sourceReferenceLog) {
+        final float sourceGuide = (float)Math.pow(2.0, sourceReferenceLog);
+        final float mappedGuide = iris26623MapMotionSdrFinalGuide(
+                sourceGuide,
+                basePipeline.mParameters.motionV2DisplayGain,
+                basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
+                basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
+                basePipeline.mParameters.motionV2ToneBaseSceneWhite,
+                basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
+        return (float)(Math.log(Math.max(mappedGuide, 0.000244140625f)) / Math.log(2.0));
+    }
+
     private void iris26621ClearLocalToneState() {
         basePipeline.mParameters.motionV2LocalToneLogMap = null;
         basePipeline.mParameters.motionV2LocalToneLogWidth = 0;
@@ -389,6 +421,186 @@ public final class MotionV2Render extends Node {
         basePipeline.mParameters.motionV2LocalToneLogSourceHeight = 0;
     }
 
+    private GLTexture iris26626ApplyBoundedSourceDomainPreservation(
+            GLTexture source, GLTexture currentTone, GLFormat scalar, float preservationStrength) {
+        if (preservationStrength <= 1.0e-7f) return currentTone;
+
+        final GLTexture[] sourceGuide = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS];
+        final GLTexture[] sourceBands = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS - 1];
+        GLTexture coarsestLinear = null;
+        GLTexture reconstruction = null;
+        GLTexture delta = null;
+        GLTexture blended = null;
+        boolean keepBlended = false;
+        try {
+            sourceGuide[0] = new GLTexture(
+                    new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
+            glProg.setTexture("SourceLinear", source);
+            glProg.setVar("iris26626Mode", 1);
+            glProg.drawBlocks(sourceGuide[0]);
+
+            for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
+                Point previous = sourceGuide[level - 1].mSize;
+                Point nextSize = new Point(
+                        Math.max(1, (previous.x + 1) / 2),
+                        Math.max(1, (previous.y + 1) / 2));
+                sourceGuide[level] = new GLTexture(
+                        nextSize, scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+                glProg.useAssetProgram("motionv2/local_laplacian_downsample_26621");
+                glProg.setTexture("InputBuffer", sourceGuide[level - 1]);
+                glProg.drawBlocks(sourceGuide[level]);
+            }
+
+            final float referenceStep =
+                    (IRIS_26621_REFERENCE_MAX_LOG - IRIS_26621_REFERENCE_MIN_LOG)
+                            / (IRIS_26621_REFERENCE_COUNT - 1.0f);
+            for (int reference = 0; reference < IRIS_26621_REFERENCE_COUNT; reference++) {
+                final float sourceReferenceLog =
+                        IRIS_26621_REFERENCE_MIN_LOG + reference * referenceStep;
+                final float mappedReferenceLog = iris26626MappedReferenceLog(sourceReferenceLog);
+                final GLTexture[] remap = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS];
+                try {
+                    remap[0] = new GLTexture(
+                            new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+                    glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
+                    glProg.setTexture("SourceLinear", source);
+                    glProg.setVar("sourceReferenceLog", sourceReferenceLog);
+                    glProg.setVar("mappedReferenceLog", mappedReferenceLog);
+                    glProg.setVar("sigmaEv", IRIS_26621_DETAIL_SIGMA_EV);
+                    glProg.setVar("edgeSlope", IRIS_26621_EDGE_SLOPE);
+                    glProg.setVar("iris26626Mode", 2);
+                    glProg.drawBlocks(remap[0]);
+
+                    for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
+                        remap[level] = new GLTexture(
+                                new Point(sourceGuide[level].mSize), scalar, null,
+                                GL_LINEAR, GL_CLAMP_TO_EDGE);
+                        glProg.useAssetProgram("motionv2/local_laplacian_downsample_26621");
+                        glProg.setTexture("InputBuffer", remap[level - 1]);
+                        glProg.drawBlocks(remap[level]);
+                    }
+
+                    for (int level = 0; level < IRIS_26621_LAPLACIAN_LEVELS - 1; level++) {
+                        GLTexture next = new GLTexture(
+                                new Point(sourceGuide[level].mSize), scalar, null,
+                                GL_LINEAR, GL_CLAMP_TO_EDGE);
+                        glProg.useAssetProgram("motionv2/local_laplacian_accumulate_26621");
+                        glProg.setTexture("Accumulator",
+                                sourceBands[level] != null ? sourceBands[level] : remap[level]);
+                        glProg.setTexture("RemapFine", remap[level]);
+                        glProg.setTexture("RemapCoarse", remap[level + 1]);
+                        glProg.setTexture("GuideLevel", sourceGuide[level]);
+                        glProg.setVar("referenceLog", sourceReferenceLog);
+                        glProg.setVar("referenceStep", referenceStep);
+                        glProg.setVar("referenceMin", IRIS_26621_REFERENCE_MIN_LOG);
+                        glProg.setVar("referenceMax", IRIS_26621_REFERENCE_MAX_LOG);
+                        glProg.setVar("firstReference", reference == 0 ? 1 : 0);
+                        glProg.drawBlocks(next);
+                        if (sourceBands[level] != null) {
+                            try { sourceBands[level].close(); } catch (Throwable ignored) {}
+                        }
+                        sourceBands[level] = next;
+                    }
+                } finally {
+                    for (GLTexture texture : remap) {
+                        if (texture != null) {
+                            try { texture.close(); } catch (Throwable ignored) {}
+                        }
+                    }
+                }
+            }
+
+            coarsestLinear = new GLTexture(
+                    new Point(sourceGuide[IRIS_26621_LAPLACIAN_LEVELS - 1].mSize),
+                    scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
+            glProg.setTexture("SourceGuideLog", sourceGuide[IRIS_26621_LAPLACIAN_LEVELS - 1]);
+            glProg.setVar("iris26626Mode", 3);
+            glProg.drawBlocks(coarsestLinear);
+
+            reconstruction = new GLTexture(
+                    new Point(coarsestLinear.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_global_log_26621");
+            glProg.setTexture("InputBuffer", coarsestLinear);
+            glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
+            glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
+            iris26623SetAdaptiveUpperToneUniforms();
+            glProg.drawBlocks(reconstruction);
+            try { coarsestLinear.close(); } catch (Throwable ignored) {}
+            coarsestLinear = null;
+
+            for (int level = IRIS_26621_LAPLACIAN_LEVELS - 2; level >= 1; level--) {
+                GLTexture next = new GLTexture(
+                        new Point(sourceGuide[level].mSize), scalar, null,
+                        GL_LINEAR, GL_CLAMP_TO_EDGE);
+                glProg.useAssetProgram("motionv2/local_laplacian_reconstruct_26621");
+                glProg.setTexture("LocalBand", sourceBands[level]);
+                glProg.setTexture("CoarseReconstruction", reconstruction);
+                glProg.drawBlocks(next);
+                try { reconstruction.close(); } catch (Throwable ignored) {}
+                reconstruction = next;
+            }
+
+            delta = new GLTexture(
+                    new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
+            glProg.setTexture("LocalBand", sourceBands[0]);
+            glProg.setTexture("CoarseReconstruction", reconstruction);
+            glProg.setTexture("CurrentToneLog", currentTone);
+            glProg.setVar("iris26626Mode", 4);
+            glProg.drawBlocks(delta);
+            try { reconstruction.close(); } catch (Throwable ignored) {}
+            reconstruction = null;
+
+            blended = new GLTexture(
+                    new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
+            glProg.setTexture("SourceLinear", source);
+            glProg.setTexture("CurrentToneLog", currentTone);
+            glProg.setTexture("DeltaLog", delta);
+            glProg.setVar("iris26626PreservationStrength", preservationStrength);
+            glProg.setVar("iris26626Mode", 5);
+            glProg.drawBlocks(blended);
+
+            Log.i(Name, "IRIS_26626_BOUNDED_SOURCE_DOMAIN_LOCAL_LAPLACIAN"
+                    + " active=true"
+                    + " preservationStrength=" + preservationStrength
+                    + " preservationMax=" + IRIS_26626_SOURCE_PRESERVATION_MAX
+                    + " lowerBodyGate=" + IRIS_26623_UPPER_TONE_START + "..0.72"
+                    + " baseline=EXACT_26625_RECONSTRUCTION"
+                    + " sourceReferences=" + IRIS_26621_REFERENCE_COUNT
+                    + " sourceLevels=" + IRIS_26621_LAPLACIAN_LEVELS
+                    + " true2xSharedFinalMap="
+                    + basePipeline.mParameters.motionV2SuperResOutputEnabled);
+            keepBlended = true;
+            return blended;
+        } finally {
+            if (coarsestLinear != null) {
+                try { coarsestLinear.close(); } catch (Throwable ignored) {}
+            }
+            if (reconstruction != null) {
+                try { reconstruction.close(); } catch (Throwable ignored) {}
+            }
+            if (delta != null) {
+                try { delta.close(); } catch (Throwable ignored) {}
+            }
+            for (GLTexture texture : sourceBands) {
+                if (texture != null) {
+                    try { texture.close(); } catch (Throwable ignored) {}
+                }
+            }
+            for (GLTexture texture : sourceGuide) {
+                if (texture != null) {
+                    try { texture.close(); } catch (Throwable ignored) {}
+                }
+            }
+            if (!keepBlended && blended != null) {
+                try { blended.close(); } catch (Throwable ignored) {}
+            }
+        }
+    }
+
     private GLTexture iris26621BuildLocalLaplacianTone(GLTexture source) {
         iris26621ClearLocalToneState();
         if (!basePipeline.mParameters.motionV2Active) return null;
@@ -433,6 +645,7 @@ public final class MotionV2Render extends Node {
                     glProg.setVar("referenceLog", referenceLog);
                     glProg.setVar("sigmaEv", IRIS_26621_DETAIL_SIGMA_EV);
                     glProg.setVar("edgeSlope", IRIS_26621_EDGE_SLOPE);
+                    glProg.setVar("iris26626Mode", 0);
                     glProg.drawBlocks(remap[0]);
 
                     for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
@@ -510,6 +723,16 @@ public final class MotionV2Render extends Node {
                 }
             }
 
+            final float iris26626PreservationStrength = iris26626SourcePreservationStrength();
+            if (iris26626PreservationStrength > 1.0e-7f) {
+                GLTexture preservedTone = iris26626ApplyBoundedSourceDomainPreservation(
+                        source, finalTone, scalar, iris26626PreservationStrength);
+                if (preservedTone != finalTone) {
+                    try { finalTone.close(); } catch (Throwable ignored) {}
+                    finalTone = preservedTone;
+                }
+            }
+
             if (basePipeline.mParameters.motionV2SuperResOutputEnabled) {
                 finalTone.BufferLoad();
                 ByteBuffer map = finalTone.textureBuffer(scalar, true);
@@ -534,6 +757,11 @@ public final class MotionV2Render extends Node {
                     + " sigmaEv=" + IRIS_26621_DETAIL_SIGMA_EV
                     + " edgeSlope=" + IRIS_26621_EDGE_SLOPE
                     + " detailAlpha=1.0"
+                    + " iris26626BoundedSourceDomain=true"
+                    + " iris26626PreservationStrength=" + iris26626PreservationStrength
+                    + " iris26626PreservationMax=" + IRIS_26626_SOURCE_PRESERVATION_MAX
+                    + " iris26626LowerBodyGate=" + IRIS_26623_UPPER_TONE_START + "..0.72"
+                    + " iris26626Baseline=EXACT_26625_RECONSTRUCTION"
                     + " globalExposureOwner=motionV2DisplayGain"
                     + " outputExposureScaleConsumedOnce=" + OUTPUT_EXPOSURE_SCALE
                     + " absoluteFullResolutionToneMap=true"
diff --git a/app/version.properties b/app/version.properties
index c267c7a0b688eb85106f7d5ed00ec27f841fa52a..8d90f51731635bc5b0784c38936bb70364beda4d 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726625
-VERSION_BUILD=26625
+VERSION_NAME=0.9726626
+VERSION_BUILD=26626
