diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2HighlightChromaReliability.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2HighlightChromaReliability.java
index 41ec74baa9015186b1b921a69665b9302de827fe..5a94f75cfc2024a97b68da2f9550d20506a37f73 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2HighlightChromaReliability.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2HighlightChromaReliability.java
@@ -26,12 +26,12 @@ public final class MotionV2HighlightChromaReliability extends Node {
     @Override public void Run() {
         if (!(basePipeline.mParameters.motionV2Active || basePipeline.mParameters.irisNightActive))
             throw new IllegalStateException("Highlight reconstruction outside Iris Spatial RGB/Night");
-        if (basePipeline.mParameters.motionV2Active
-                && basePipeline.mParameters.motionV2ReconstructionOwner
+        if (basePipeline.mParameters.motionV2ReconstructionOwner
                 != com.particlesdevs.photoncamera.processing.render.Parameters.MOTION_V2_RECONSTRUCTION_SPATIAL_RGB) {
             throw new IllegalStateException(
-                    "26545 V1.2 Spatial highlight node reached non-Spatial Motion owner="
-                            + basePipeline.mParameters.motionV2ReconstructionOwner);
+                    "26548 V1.2 Spatial highlight node reached non-Spatial Iris owner="
+                            + basePipeline.mParameters.motionV2ReconstructionOwner
+                            + " night=" + basePipeline.mParameters.irisNightActive);
         }
         float[] reliability = basePipeline.mParameters.motionV2SpatialReliability;
         int rw = basePipeline.mParameters.motionV2SpatialReliabilityWidth;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2MgcSourceExposure.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2MgcSourceExposure.java
index 0acc163133882bb36a6e20bcfb6fb2fbbd5d98a5..7aec67c4dc5850f1b52f572e981b5c5f97c81776 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2MgcSourceExposure.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2MgcSourceExposure.java
@@ -8,8 +8,8 @@ import com.particlesdevs.photoncamera.util.Log;
  *
  * Spatial/Bento Accepted-Short MGC BaselineExposure describes the darker SOURCE DOMAIN used
  * while Spatial reconstruction runs. Restore that domain before camera-white/profile color.
- * Sabre admits NORMAL frames only and must never execute this node. Night retains its Spatial
- * source-domain contract. This is not scene exposure and never writes Camera2.
+ * Sabre must never execute this node in either Motion or Night. This is not scene exposure and
+ * never writes Camera2.
  */
 public final class MotionV2MgcSourceExposure extends Node {
     public MotionV2MgcSourceExposure() {
@@ -23,12 +23,12 @@ public final class MotionV2MgcSourceExposure extends Node {
         if (!(basePipeline.mParameters.motionV2Active || basePipeline.mParameters.irisNightActive)) {
             throw new IllegalStateException("MotionV2MgcSourceExposure outside Iris Spatial RGB/Night");
         }
-        if (basePipeline.mParameters.motionV2Active
-                && basePipeline.mParameters.motionV2ReconstructionOwner
+        if (basePipeline.mParameters.motionV2ReconstructionOwner
                 != com.particlesdevs.photoncamera.processing.render.Parameters.MOTION_V2_RECONSTRUCTION_SPATIAL_RGB) {
             throw new IllegalStateException(
-                    "26545 V1.2 Spatial source-restore node reached non-Spatial Motion owner="
-                            + basePipeline.mParameters.motionV2ReconstructionOwner);
+                    "26548 V1.2 Spatial source-restore node reached non-Spatial Iris owner="
+                            + basePipeline.mParameters.motionV2ReconstructionOwner
+                            + " night=" + basePipeline.mParameters.irisNightActive);
         }
         float sourceGain = basePipeline.mParameters.motionV2MgcSourceExposureGain;
         if (!Float.isFinite(sourceGain) || sourceGain <= 0.0f) {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index c122d9d02df75a2c95947c57cdeb5ece844db7e3..68db8350b770f663a8176970b09f64346012da06 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -76,26 +76,36 @@ public class PostPipeline extends GLBasePipeline {
         return "NONE_OR_INVALID(" + owner + ")";
     }
 
-    private void validateMotionReconstructionOwnership() {
-        if (!mParameters.motionV2Active) return;
+    /* IRIS_26548_V1_2_SHARED_RECONSTRUCTION_OWNER_CONTRACT
+     * Motion and Night share the same durable reconstruction-owner field. Validate that owner
+     * before either post graph is scheduled so an owner-specific Spatial node can never consume
+     * Sabre output (or vice versa). This is routing validation only; reconstruction math is unchanged.
+     */
+    private void validateIrisReconstructionOwnership() {
+        if (!(mParameters.motionV2Active || mParameters.irisNightActive)) return;
         final int owner = mParameters.motionV2ReconstructionOwner;
+        final String pipeline = mParameters.irisNightActive ? "Night" : "Motion";
         if (owner != Parameters.MOTION_V2_RECONSTRUCTION_SPATIAL_RGB
                 && owner != Parameters.MOTION_V2_RECONSTRUCTION_SABRE) {
-            throw new IllegalStateException("26545 V1.2 missing/invalid Motion reconstruction owner=" + owner);
+            throw new IllegalStateException("26548 V1.2 missing/invalid " + pipeline
+                    + " reconstruction owner=" + owner);
         }
         if (owner == Parameters.MOTION_V2_RECONSTRUCTION_SABRE) {
             if (mParameters.motionV2SpatialReliability != null
                     || mParameters.motionV2SpatialReliabilityWidth != 0
                     || mParameters.motionV2SpatialReliabilityHeight != 0) {
-                throw new IllegalStateException("26545 V1.2 Sabre received Spatial reliability payload");
+                throw new IllegalStateException("26548 V1.2 " + pipeline
+                        + " Sabre received Spatial reliability payload");
             }
             if (Math.abs(mParameters.motionV2ReconstructionZoom - 1.0f) > 1.0e-5f
                     || Math.abs(mParameters.motionV2SpatialReconstructionZoom - 1.0f) > 1.0e-5f
                     || Math.abs(mParameters.motionV2SuperResOutputScale - 1.0f) > 1.0e-5f) {
-                throw new IllegalStateException("26545 V1.2 Sabre inherited Spatial SR geometry");
+                throw new IllegalStateException("26548 V1.2 " + pipeline
+                        + " Sabre inherited Spatial SR geometry");
             }
             if (Math.abs(mParameters.motionV2MgcSourceExposureGain - 1.0f) > 1.0e-5f) {
-                throw new IllegalStateException("26545 V1.2 Sabre inherited Spatial/Bento source exposure");
+                throw new IllegalStateException("26548 V1.2 " + pipeline
+                        + " Sabre inherited Spatial/Bento source exposure");
             }
         } else {
             float[] reliability = mParameters.motionV2SpatialReliability;
@@ -104,9 +114,17 @@ public class PostPipeline extends GLBasePipeline {
                     || mParameters.motionV2SpatialReliabilityHeight <= 0
                     || reliability.length != mParameters.motionV2SpatialReliabilityWidth
                             * mParameters.motionV2SpatialReliabilityHeight) {
-                throw new IllegalStateException("26545 V1.2 Spatial owner missing native reliability payload");
+                throw new IllegalStateException("26548 V1.2 " + pipeline
+                        + " Spatial owner missing native reliability payload");
             }
         }
+        Log.i("PostPipeline", "IRIS_26548_V1_2_RECONSTRUCTION_OWNER_VALID"
+                + " pipeline=" + pipeline
+                + " owner=" + motionReconstructionOwnerName(owner)
+                + " spatialReliability=" + (mParameters.motionV2SpatialReliability != null)
+                + " spatialSourceGain=" + mParameters.motionV2MgcSourceExposureGain
+                + " reconstructionZoom=" + mParameters.motionV2ReconstructionZoom
+                + " superResScale=" + mParameters.motionV2SuperResOutputScale);
     }
 
     public int getRotation() {
@@ -398,17 +416,33 @@ public class PostPipeline extends GLBasePipeline {
     }
 
     private void BuildDefaultPipeline() {
-        /* IRIS_26535_NIGHT_SHARED_SPATIAL_RGB_POST_GRAPH
-         * Night shares Motion's proven MGC Spatial-RGB reconstruction carrier but retains its own
-         * capture policy, neutral presentation gain and Jin finishing. No Bayer/RCD consumer exists.
+        /* IRIS_26548_V1_2_NIGHT_OWNER_AWARE_POST_GRAPH
+         * Night reconstruction is currently Sabre, but post routing follows the durable owner
+         * rather than assuming an old Spatial-RGB contract. Spatial-only source restoration and
+         * reliability highlight reconstruction are scheduled only for a proven Spatial owner.
+         * Common camera-RGB color/presentation/render nodes remain unchanged.
          */
         if(mParameters.irisNightActive){
+            validateIrisReconstructionOwnership();
+            final int reconstructionOwner = mParameters.motionV2ReconstructionOwner;
             add(new IrisNightRgbInput());
-            add(new StageTelemetry("IRIS_NIGHT_SPATIAL_RGB_INPUT"));
-            add(new MotionV2MgcSourceExposure());
-            add(new StageTelemetry("IRIS_NIGHT_MGC_SOURCE_RESTORE"));
-            add(new MotionV2HighlightChromaReliability());
-            add(new StageTelemetry("IRIS_NIGHT_HIGHLIGHT_CHROMA_RELIABILITY"));
+            add(new StageTelemetry("IRIS_NIGHT_RGB_INPUT"));
+            if (reconstructionOwner == Parameters.MOTION_V2_RECONSTRUCTION_SPATIAL_RGB) {
+                add(new MotionV2MgcSourceExposure());
+                add(new StageTelemetry("IRIS_NIGHT_SPATIAL_MGC_SOURCE_RESTORE"));
+                add(new MotionV2HighlightChromaReliability());
+                add(new StageTelemetry("IRIS_NIGHT_SPATIAL_HIGHLIGHT_CHROMA_RELIABILITY"));
+            } else if (reconstructionOwner == Parameters.MOTION_V2_RECONSTRUCTION_SABRE) {
+                com.particlesdevs.photoncamera.util.MotionTrace.processingState(
+                        "IRIS_26548_V1_2_NIGHT_SABRE_POST_OWNERSHIP",
+                        "owner=SABRE carrier=RESOLVE_SABRE_LINEAR_RGB"
+                                + " spatialSourceRestore=false spatialHighlightReliability=false"
+                                + " spatialReliabilityPayload=false rcd=false demosaic=false"
+                                + " commonIrisFinishingBegins=MotionV2ColorTransform");
+            } else {
+                throw new IllegalStateException("26548 V1.2 unhandled Night reconstruction owner="
+                        + reconstructionOwner);
+            }
             add(new MotionV2ColorTransform());
             add(new StageTelemetry("IRIS_NIGHT_PROFILE_COLOR"));
             add(new MotionV2DisplayExposure());
@@ -491,7 +525,7 @@ public class PostPipeline extends GLBasePipeline {
              * from the shutter-time viewfinder only after color; the user's Iris controls remain
              * later and additive.
              */
-            validateMotionReconstructionOwnership();
+            validateIrisReconstructionOwnership();
             final int reconstructionOwner = mParameters.motionV2ReconstructionOwner;
             if (reconstructionOwner == Parameters.MOTION_V2_RECONSTRUCTION_SPATIAL_RGB) {
                 add(new MotionV2MgcSourceExposure());
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
index 0109819918d20f73aad41db12b82cf61005e29a0..31341685d9bdef4c61bad5721a04c5b9135ffd00 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
@@ -122,6 +122,27 @@ public final class IrisNightProcessor {
             if (r == null || r.raw == null) {
                 throw new IllegalStateException("26547 Night Sabre linear-RGB carrier missing");
             }
+            if (p.motionV2ReconstructionOwner != Parameters.MOTION_V2_RECONSTRUCTION_SABRE) {
+                throw new IllegalStateException("26548 V1.2 Night reconstruction owner is not Sabre owner="
+                        + p.motionV2ReconstructionOwner);
+            }
+            if (Math.abs(p.motionV2ReconstructionZoom - 1.0f) > 1.0e-5f
+                    || Math.abs(p.motionV2SpatialReconstructionZoom - 1.0f) > 1.0e-5f
+                    || Math.abs(p.motionV2SuperResOutputScale - 1.0f) > 1.0e-5f) {
+                throw new IllegalStateException("26548 V1.2 Night Sabre must remain native-grid"
+                        + " reconstructionZoom=" + p.motionV2ReconstructionZoom
+                        + " spatialZoom=" + p.motionV2SpatialReconstructionZoom
+                        + " outputScale=" + p.motionV2SuperResOutputScale);
+            }
+            final boolean iris26548NightSuperResRequested = batch.superResEnabled;
+            if (p.motionV2SuperResOutputEnabled) {
+                // Sabre is intentionally native-grid in this build. Publish effective state rather
+                // than allowing EXIF/DNG routing to claim a 2x Spatial output that Sabre did not make.
+                p.motionV2SuperResOutputEnabled = false;
+                Log.i(TAG, "IRIS_26548_V1_2_NIGHT_SABRE_SR_EFFECTIVE"
+                        + " requested=" + iris26548NightSuperResRequested
+                        + " effective=false owner=SABRE nativeGrid=true");
+            }
             if (r.inputFrames < images.size()) {
                 throw new IllegalStateException("26540 Night MGC scheduled-frame mismatch scheduled="
                         + r.inputFrames + " expectedAtLeast=" + images.size());
@@ -202,8 +223,9 @@ public final class IrisNightProcessor {
                     + " pecanProfileSnr=referencePreMerge"
                     + " denoiseMagnitude=propagatedResidual readShotCorrelationStrength=true"
                     + " isoDenoiseAuthority=false darknessDenoiseAuthority=false"
-                    + " autoLowLightLumaFloor=false ultraHdr=false superRes="
-                    + p.motionV2SuperResOutputEnabled;
+                    + " autoLowLightLumaFloor=false ultraHdr=false"
+                    + " superResRequested=" + iris26548NightSuperResRequested
+                    + " superResEffective=" + p.motionV2SuperResOutputEnabled;
 
             final PostPipeline pipeline = new PostPipeline(true);
             final long postStartNs = System.nanoTime();
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index a85c04a7bda37252be7d88e23fe4f880f518d879..5b8e286e9848d7dbb62304b14fc8746374a34859 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -669,15 +669,14 @@ object PhotonMotionMgc1271Bridge {
                     "DNG metadata produced without request")
             }
 
-            if (!parameters.irisNightActive) {
-                PLog.i(TAG, "IRIS_26545_V1_2_RECONSTRUCTION_OWNERSHIP " +
-                    "owner=${if (sabreSelected) "SABRE" else "SPATIAL_RGB"} " +
-                    "spatialReliability=${parameters.motionV2SpatialReliability != null} " +
-                    "spatialSourceRestoreAllowed=${!sabreSelected} " +
-                    "spatialHighlightNodeAllowed=${!sabreSelected} " +
-                    "residualDenoise=$denoisePass " +
-                    "carrier=${if (sabreSelected) "RESOLVE_SABRE_LINEAR_RGB" else "SPATIAL_LINEAR_RGB"}")
-            }
+            PLog.i(TAG, "IRIS_26548_V1_2_RECONSTRUCTION_OWNERSHIP " +
+                "pipeline=${if (parameters.irisNightActive) "NIGHT" else "MOTION"} " +
+                "owner=${if (sabreSelected) "SABRE" else "SPATIAL_RGB"} " +
+                "spatialReliability=${parameters.motionV2SpatialReliability != null} " +
+                "spatialSourceRestoreAllowed=${!sabreSelected} " +
+                "spatialHighlightNodeAllowed=${!sabreSelected} " +
+                "residualDenoise=$denoisePass " +
+                "carrier=${if (sabreSelected) "RESOLVE_SABRE_LINEAR_RGB" else "SPATIAL_LINEAR_RGB"}")
             PLog.i(TAG, "IRIS_26545_RECONSTRUCTION_TIMING " +
                 "method=${if (sabreSelected) "SABRE" else "SPATIAL_RGB"} " +
                 "srEnabled=${fixedOutputScale > 1f} fusionMs=$iris26535FusionMs " +
