diff --git a/app/src/main/assets/shaders/motionv2/color_transform.glsl b/app/src/main/assets/shaders/motionv2/color_transform.glsl
index 10ae929049633e6b431c6267007ff5a47baf66eb..678170ce0c4842de590aa08c8d57875c31472b29 100644
--- a/app/src/main/assets/shaders/motionv2/color_transform.glsl
+++ b/app/src/main/assets/shaders/motionv2/color_transform.glsl
@@ -5,6 +5,10 @@ uniform sampler2D InputBuffer;
 uniform sampler2D ProfileHueSatMap;
 uniform sampler2D ProfileLookMap;
 uniform sampler2D Acr3Curve;
+uniform sampler2D PhotonCurveOff;
+uniform sampler2D PhotonCurveOn;
+uniform float photonAdaptiveWhitePoint;
+uniform vec3 photonCameraNeutral;
 uniform int acr3CurveSize;
 uniform int acr3Enabled;
 uniform int profileHueDivisions;
@@ -27,6 +31,9 @@ out vec3 Output;
 #ifndef USE_PROFILE_LOOK
 #define USE_PROFILE_LOOK 0
 #endif
+#ifndef USE_PHOTON_HIGHLIGHT_COMPRESSION
+#define USE_PHOTON_HIGHLIGHT_COMPRESSION 0
+#endif
 
 /* IRIS_26628_DNG_HSV_PROFILE_MAP
  * Independently written equivalent of the DNG HSV table mechanics used by bjzhou.  SensorSpecifics
@@ -149,6 +156,20 @@ vec3 iris26638ApplyAcr3(vec3 color){
 void main(){
     ivec2 xy=ivec2(gl_FragCoord.xy);
     vec3 cameraRgb=max(texelFetch(InputBuffer,xy,0).rgb,vec3(0.0));
+#if USE_PHOTON_HIGHLIGHT_COMPRESSION == 1
+    /* IRIS_26650_EXACT_PHOTON_TOGGLE_DIFFERENTIAL
+     * Photon applies the same common adaptive-white-point/exposure/Reinhard curve in both OFF and
+     * ON states; the toggle changes only the final shoulder. Sample both exact curves at the same
+     * pre-color, white-balanced luminance and apply ON/OFF as one scalar to Iris camera RGB.
+     * This preserves Iris chromaticity and makes OFF the successful 26648 pixel path. */
+    vec3 photonRgb=(cameraRgb/max(photonCameraNeutral,vec3(1.0e-6)))
+            /max(photonAdaptiveWhitePoint,1.0e-6);
+    float photonLuma=max(dot(photonRgb,vec3(0.299,0.587,0.114)),0.0);
+    float photonOff=texture(PhotonCurveOff,vec2(photonLuma,0.5)).r;
+    float photonOn=texture(PhotonCurveOn,vec2(photonLuma,0.5)).r;
+    float photonToggleRatio=photonOff>1.0e-7?photonOn/photonOff:1.0;
+    cameraRgb*=clamp(photonToggleRatio,0.0,1.0);
+#endif
     vec3 profileRgb=vec3(dot(sensorToProfileRow0,cameraRgb),dot(sensorToProfileRow1,cameraRgb),dot(sensorToProfileRow2,cameraRgb));
 #if USE_PROFILE_HUESAT == 1
     profileRgb=iris26628ApplyMap(profileRgb,ProfileHueSatMap,profileHueDivisions,profileSatDivisions,profileValueDivisions);
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index c2776ec8cfe0e969220e888d6963675556448eb0..ca6c52c963e45000a2731a9335ee0564b448fbc0 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -17,6 +17,7 @@ uniform float edgeSlope;
 uniform float iris26626PreservationStrength;
 uniform float iris26639ShadowBodyStrength;
 uniform float iris26639ShadowBodyEnd;
+uniform int iris26650HighlightCompressionEnabled;
 uniform int iris26626Mode;
 out float Output;
 
@@ -203,22 +204,21 @@ void main(){
         float baseLinear=exp2(clamp(baseLog,-12.0,0.0));
         float residual=currentLinear-baseLinear;
 
-        /* IRIS_26635_SPATIALLY_COHERENT_HIGHLIGHT_ROLLOFF
-         * B is the already-mapped low-frequency illumination owner.  Give B a small continuous
-         * asymptotic lift toward white (no target-white plateau), then carry the structural
-         * residual R with a bounded highlight weight. Small residuals are gently suppressed in
-         * bright fields; strong slats/folds/specular geometry retain unit weight. */
+        /* IRIS_26650_SINGLE_HIGHLIGHT_COMPRESSION_AUTHORITY
+         * OFF must remain the exact successful-26648 presentation path. ON neutralizes only the
+         * later 26635 smooth-base lift that would otherwise push Photon-compressed highlights back
+         * toward white. This is keyed from the capture-frozen PostPipeline owner, not a second UI
+         * read, so the whole capture uses one immutable toggle state. */
+        float iris26650Hc=iris26650HighlightCompressionEnabled!=0 ? 1.0 : 0.0;
         float upperGate=smoothstep(0.65,0.985,baseLinear);
         float shoulderGate=smoothstep(0.70,0.985,baseLinear);
         vec3 iris26646Radiance=iris26646SourceRadianceSurvival(p);
         float sourceStructureGate=max(max(iris26644SourceStructure(p),iris26645SourceTexture(p)),iris26646Radiance.z);
         float smoothShoulder=1.0-pow(max(1.0-baseLinear,0.0),1.12);
-        /* IRIS_26645_STRUCTURE_AWARE_HIGHLIGHT_BASE
-         * Smooth bright illumination keeps the successful 26635 shoulder. Real source texture
-         * retains more base headroom so grass/pavement/fabric is not lifted into one pale plateau. */
         float structureShoulderScale=1.0-0.72*sourceStructureGate;
-        float baseOut=mix(baseLinear,smoothShoulder,
+        float legacyBaseOut=mix(baseLinear,smoothShoulder,
             0.55*shoulderGate*structureShoulderScale);
+        float baseOut=mix(legacyBaseOut,baseLinear,iris26650Hc);
 
         /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BASE
          * Correct only the low-frequency base B. The 0.05 true-floor endpoint and the scene-derived
@@ -232,13 +232,15 @@ void main(){
         float bodyBump=16.0*bodyT*bodyT*(1.0-bodyT)*(1.0-bodyT);
         float bodyScale=exp2(-0.25*bodyStrength*bodyBump);
         float shadowBase=baseOut*bodyScale;
+        /* IRIS_26650_UNIVERSAL_HIGHLIGHT_RESIDUAL_PRESERVATION
+         * OFF keeps the exact 26648 smooth-bright residual policy. ON preserves the completed
+         * residual at unit weight so the newly created highlight headroom is not immediately
+         * collapsed again. */
         float structureGate=smoothstep(0.012,0.045,abs(residual));
         float smallResidualWeight=mix(1.0,0.78,upperGate);
-        /* IRIS_26644_VISUAL_HIGHLIGHT_STRUCTURE_PRESERVATION
-         * Keep 26635 suppression only for genuinely smooth bright-field residuals. If the
-         * pre-tone source independently proves real local structure, preserve that residual. */
-        float residualWeight=mix(
+        float legacyResidualWeight=mix(
                 smallResidualWeight,1.0,max(structureGate,sourceStructureGate));
+        float residualWeight=mix(legacyResidualWeight,1.0,iris26650Hc);
         float weightedResidual=residualWeight*residual;
         float candidateLinear=shadowBase+weightedResidual;
         /* IRIS_26645_STRUCTURAL_HEADROOM_RESERVATION
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ColorTransform.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ColorTransform.java
index 13a6f6ecbdda80c46aae26d70bb6c7b880841fe9..6995c2208c33c69c75a247492735e5609698d12d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ColorTransform.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ColorTransform.java
@@ -34,6 +34,17 @@ public final class MotionV2ColorTransform extends Node {
         profileHueSatTexture = null;
         profileLookTexture = null;
         acr3Texture = null;
+        if (basePipeline instanceof PostPipeline) {
+            PostPipeline pipeline = (PostPipeline) basePipeline;
+            if (pipeline.motionV2PhotonCurveOff != null) {
+                try { pipeline.motionV2PhotonCurveOff.close(); } catch (Throwable ignored) {}
+                pipeline.motionV2PhotonCurveOff = null;
+            }
+            if (pipeline.motionV2PhotonCurveOn != null) {
+                try { pipeline.motionV2PhotonCurveOn.close(); } catch (Throwable ignored) {}
+                pipeline.motionV2PhotonCurveOn = null;
+            }
+        }
     }
 
     @Override
@@ -65,8 +76,22 @@ public final class MotionV2ColorTransform extends Node {
                 basePipeline.mParameters.irisJpegLookMapSize);
         if (hasHueSat) glProg.setDefine("USE_PROFILE_HUESAT", 1);
         if (hasLook) glProg.setDefine("USE_PROFILE_LOOK", 1);
+        final PostPipeline pipeline = (PostPipeline) basePipeline;
+        final boolean iris26650HighlightCompression =
+                basePipeline.mParameters.motionV2Active
+                        && pipeline.motionV2PhotonHighlightCompressionEnabled;
+        if (iris26650HighlightCompression) glProg.setDefine("USE_PHOTON_HIGHLIGHT_COMPRESSION", 1);
         glProg.useAssetProgram("motionv2/color_transform");
         glProg.setTexture("InputBuffer", previousNode.WorkingTexture);
+        if (iris26650HighlightCompression) {
+            if (pipeline.motionV2PhotonCurveOff == null || pipeline.motionV2PhotonCurveOn == null) {
+                throw new IllegalStateException("26650 enabled Highlight Compression missing Photon curves");
+            }
+            glProg.setTexture("PhotonCurveOff", pipeline.motionV2PhotonCurveOff);
+            glProg.setTexture("PhotonCurveOn", pipeline.motionV2PhotonCurveOn);
+            glProg.setVar("photonAdaptiveWhitePoint", pipeline.motionV2PhotonAdaptiveWhitePoint);
+            glProg.setVar("photonCameraNeutral", cameraNeutral);
+        }
 
         /* IRIS_26638_ACR3_SINGLE_TABLE_OWNER
          * The exact bjzhou default ACR3 samples are the sole photographic RGB-rendering curve
@@ -120,6 +145,11 @@ public final class MotionV2ColorTransform extends Node {
                 + " negativeProfileExcursionMapBypass=true"
                 + " neutralAxisNegativeGamutFit=true"
                 + " legacyPhotonColorAffected=false"
+                + " photonHighlightCompression=" + iris26650HighlightCompression
+                + " photonHighlightKnee=" + pipeline.motionV2PhotonHighlightKnee
+                + " photonClippedFraction=" + pipeline.motionV2PhotonHighlightClippedFraction
+                + " photonAdaptiveWhitePoint=" + pipeline.motionV2PhotonAdaptiveWhitePoint
+                + " photonToggleDifferentialPreColor=true"
                 + " dngAffected=false");
     }
 
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java
new file mode 100644
index 0000000000000000000000000000000000000000..4776da1d7b135ea7f423901931511c93776ed3e5
--- /dev/null
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java
@@ -0,0 +1,367 @@
+package com.particlesdevs.photoncamera.processing.opengl.postpipeline;
+
+import android.graphics.Point;
+
+import com.particlesdevs.photoncamera.processing.opengl.GLFormat;
+import com.particlesdevs.photoncamera.processing.opengl.GLTexture;
+import com.particlesdevs.photoncamera.processing.opengl.nodes.Node;
+import com.particlesdevs.photoncamera.processing.opengl.scripts.GLHistogram;
+import com.particlesdevs.photoncamera.util.BufferUtils;
+import com.particlesdevs.photoncamera.util.Log;
+import com.particlesdevs.photoncamera.util.MotionTrace;
+
+import static android.opengl.GLES20.GL_CLAMP_TO_EDGE;
+import static android.opengl.GLES20.GL_LINEAR;
+
+/**
+ * IRIS_26650_PHOTON_HIGHLIGHT_COMPRESSION_ANALYSIS_OWNER
+ *
+ * Motion-only analysis owner for the user-visible Settings -> Photo Settings -> Highlight
+ * Compression switch.  The camera RGB carrier remains Iris-owned and is never replaced by the
+ * Photon tone pipeline.  Instead this node reconstructs Photon's complete common
+ * AutoExposureCurve analysis on the exact pre-color Sabre camera-linear RGB domain, builds both
+ * the Highlight Compression OFF and ON curves, and publishes those two curves to
+ * MotionV2ColorTransform.  That downstream owner applies only ON/OFF, so OFF is byte-for-pixel
+ * equivalent to the successful 26648 presentation path while ON reproduces the Photon toggle's
+ * exact differential without replacing Iris color, exposure, SHORT fusion, VGN, or UHDR owners.
+ *
+ * Domain bridge proof: Photon Bayer2Float's AutoExposureCurve input is cameraRGB/whitePoint and
+ * GLHistogram multiplies each channel by whitePoint (1/extent).  The products cancel exactly,
+ * therefore histogramming Iris' pre-WB cameraRGB with exposure=1 produces the same bin ownership;
+ * mapped bin values are still expanded by extent=1/whitePoint exactly as Photon does.
+ */
+public final class MotionV2PhotonHighlightCompression extends Node {
+    private static final int HIST_SIZE = 256;
+    private static final int CURVE_SIZE = 1024;
+
+    private static final float TARGET = 128.0f;
+    private static final float NOISE_MAX = 0.05f;
+    private static final float GAIN_MAX = 9.0f;
+    private static final float WHITE_APPLY = 0.8f;
+    private static final float FILL_COEFFICIENT = 0.99f;
+    private static final float APPLY_GAMMA_MIX = 0.05f;
+    private static final float KNEE_MAX = 0.90f;
+    private static final float KNEE_MIN = 0.55f;
+    private static final float KNEE_REF = 0.10f;
+    private static final float CLIP_TOLERANCE = 0.03f;
+
+    private final boolean enabled;
+
+    public MotionV2PhotonHighlightCompression(boolean enabled) {
+        super("", "MotionV2PhotonHighlightCompression");
+        this.enabled = enabled;
+    }
+
+    @Override public void Compile() {}
+    @Override public void AfterRun() {}
+
+    @Override
+    public void Run() {
+        if (!basePipeline.mParameters.motionV2Active || basePipeline.mParameters.irisNightActive) {
+            throw new IllegalStateException("26650 Highlight Compression owner is Motion-only");
+        }
+        if (previousNode == null || previousNode.WorkingTexture == null) {
+            throw new IllegalStateException("26650 missing pre-color Sabre RGB input");
+        }
+
+        final PostPipeline pipeline = (PostPipeline) basePipeline;
+        clearPublishedCurves(pipeline);
+
+        final float[] wp = basePipeline.mParameters.whitePoint;
+        requireWhitePoint(wp);
+        final float noiseS = basePipeline.mParameters.motionV2WronskiNoiseS;
+        final float noiseO = basePipeline.mParameters.motionV2WronskiNoiseO;
+        if (!Float.isFinite(noiseS) || !Float.isFinite(noiseO) || noiseS <= 0.0f || noiseO < 0.0f) {
+            throw new IllegalStateException("26650 invalid Motion noise authority S=" + noiseS + " O=" + noiseO);
+        }
+
+        final float[] extent = new float[3];
+        for (int c = 0; c < 3; ++c) extent[c] = 1.0f / wp[c];
+        final float gCeiling = (float)Math.pow(
+                (Math.cbrt(extent[0]) + Math.cbrt(extent[2])) * 0.5, 3.0);
+        if (gCeiling > extent[1]) extent[1] = gCeiling;
+
+        final int[][] hist;
+        GLHistogram histogram = new GLHistogram(glProg, HIST_SIZE);
+        try {
+            histogram.Rc = true;
+            histogram.Gc = true;
+            histogram.Bc = true;
+            histogram.Ac = false;
+            histogram.resize = 3;
+            // Exact domain bridge described in the class comment: Photon WB * histogram exposure
+            // cancels to the un-WB cameraRGB carrier that Iris has here.
+            histogram.exposure[0] = 1.0f;
+            histogram.exposure[1] = 1.0f;
+            histogram.exposure[2] = 1.0f;
+            hist = histogram.Compute(previousNode.WorkingTexture);
+        } finally {
+            histogram.close();
+        }
+
+        final int normR = sum(hist[0]);
+        final int normG = sum(hist[1]);
+        final int normB = sum(hist[2]);
+        if (normR <= 0 || normG <= 0 || normB <= 0) {
+            throw new IllegalStateException("26650 empty RGB histogram");
+        }
+
+        final float[][] mapped = new float[3][HIST_SIZE];
+        remapHistogram(mapped, extent, 1.0f);
+
+        float avgPass1 = estimateAvg(hist, mapped, normR, normG, normB);
+        GainClamp gain1 = clampGain(TARGET / Math.max(avgPass1, 1.0e-4f), noiseS, noiseO);
+        float mpyPass1 = gain1.value;
+        float sceneWhitePass1 = searchWhite(hist, mapped, normR, normG, normB, mpyPass1);
+
+        float adaptiveWhitePoint = 1.0f;
+        float avgFinal = avgPass1;
+        float mpyPreNorm = mpyPass1;
+        float sceneWhiteFinal = sceneWhitePass1;
+        GainClamp gain2 = gain1;
+        if (sceneWhitePass1 > 1.0f) {
+            adaptiveWhitePoint = srgbDecodeExtended(sceneWhitePass1);
+            remapHistogram(mapped, extent, adaptiveWhitePoint);
+            avgFinal = estimateAvg(hist, mapped, normR, normG, normB);
+            gain2 = clampGain(TARGET / Math.max(avgFinal, 1.0e-4f), noiseS, noiseO);
+            mpyPreNorm = gain2.value;
+            sceneWhiteFinal = searchWhite(hist, mapped, normR, normG, normB, mpyPreNorm);
+        }
+
+        float normL = 0.0f;
+        float normRr = 0.0f;
+        for (int i = 0; i < HIST_SIZE; ++i) {
+            float val = (i / (HIST_SIZE - 1.0f)) * mpyPreNorm;
+            normL += Math.min(val, 1.0f);
+            normRr += (val * (1.0f + val / (mpyPreNorm * mpyPreNorm))) / (1.0f + val);
+        }
+        float mpy = mpyPreNorm * normL / Math.max(normRr, 1.0e-8f);
+        float whiteMax = sceneWhiteFinal * mpy;
+        float whiteEff = mix(mpy, whiteMax, WHITE_APPLY);
+
+        long clipped = 0L;
+        long total = (long)normR + normG + normB;
+        for (int i = 0; i < HIST_SIZE; ++i) {
+            for (int c = 0; c < 3; ++c) {
+                float x = mapped[c][i] / (HIST_SIZE - 1.0f);
+                float g = mix(x, (float)Math.sqrt(Math.max(x, 0.0f)), APPLY_GAMMA_MIX);
+                float v = g * mpy;
+                float r = v * (1.0f + v / (whiteEff * whiteEff)) / (1.0f + v);
+                if (r > 1.0f + CLIP_TOLERANCE) clipped += (long)hist[c][i];
+            }
+        }
+        float clippedFraction = total > 0L ? clipped / (float)total : 0.0f;
+        float kneeLo = Math.min(KNEE_MIN, KNEE_MAX);
+        float knee = mix(KNEE_MAX, kneeLo,
+                Math.min(clippedFraction / Math.max(KNEE_REF, 1.0e-4f), 1.0f));
+
+        float[] curveOff = new float[CURVE_SIZE];
+        float[] curveOn = new float[CURVE_SIZE];
+        float maxDelta = 0.0f;
+        int activeSamples = 0;
+        for (int i = 0; i < CURVE_SIZE; ++i) {
+            float x = i / (CURVE_SIZE - 1.0f);
+            float g = mix(x, (float)Math.sqrt(x), APPLY_GAMMA_MIX);
+            float r = g * mpy;
+            r = r * (1.0f + r / (whiteEff * whiteEff)) / (1.0f + r);
+            float off = mix(r, r * r, APPLY_GAMMA_MIX);
+            float on = softShoulder(off, knee);
+            off = clamp01(off);
+            on = clamp01(on);
+            curveOff[i] = off;
+            curveOn[i] = on;
+            float d = off - on;
+            if (d > 1.0e-7f) ++activeSamples;
+            maxDelta = Math.max(maxDelta, d);
+        }
+
+        pipeline.motionV2PhotonHighlightCompressionEnabled = enabled;
+        pipeline.motionV2PhotonAdaptiveWhitePoint = adaptiveWhitePoint;
+        pipeline.motionV2PhotonHighlightKnee = enabled ? knee : 1.0f;
+        pipeline.motionV2PhotonHighlightClippedFraction = clippedFraction;
+
+        if (enabled) {
+            pipeline.motionV2PhotonCurveOff = new GLTexture(
+                    new Point(CURVE_SIZE, 1), new GLFormat(GLFormat.DataType.FLOAT_32, 1),
+                    BufferUtils.getFrom(curveOff), GL_LINEAR, GL_CLAMP_TO_EDGE);
+            pipeline.motionV2PhotonCurveOn = new GLTexture(
+                    new Point(CURVE_SIZE, 1), new GLFormat(GLFormat.DataType.FLOAT_32, 1),
+                    BufferUtils.getFrom(curveOn), GL_LINEAR, GL_CLAMP_TO_EDGE);
+        }
+
+        final String line = "IRIS_26650_PHOTON_HIGHLIGHT_COMPRESSION"
+                + " enabled=" + enabled
+                + " owner=preColorSabreCameraRgb"
+                + " toggleApplication=exactOnOverOffCurveRatio"
+                + " photonCommonAnalysis=true"
+                + " histSize=" + HIST_SIZE
+                + " histogramResize=3"
+                + " domainBridge=unbalancedCameraRgbEquivalent"
+                + " extent=" + extent[0] + "," + extent[1] + "," + extent[2]
+                + " avgPass1=" + avgPass1
+                + " mpyPass1=" + mpyPass1
+                + " sceneWhitePass1=" + sceneWhitePass1
+                + " adaptiveWhitePoint=" + adaptiveWhitePoint
+                + " avgFinal=" + avgFinal
+                + " mpyPreNorm=" + mpyPreNorm
+                + " reinhardNormL=" + normL
+                + " reinhardNormR=" + normRr
+                + " mpyFinal=" + mpy
+                + " sceneWhiteFinal=" + sceneWhiteFinal
+                + " whiteMax=" + whiteMax
+                + " whiteEff=" + whiteEff
+                + " clippedFraction=" + clippedFraction
+                + " appliedKnee=" + (enabled ? knee : 1.0f)
+                + " potentialKnee=" + knee
+                + " clipTolerance=" + CLIP_TOLERANCE
+                + " kneeMax=" + KNEE_MAX
+                + " kneeMin=" + KNEE_MIN
+                + " kneeRef=" + KNEE_REF
+                + " applyGammaMix=" + APPLY_GAMMA_MIX
+                + " whiteApply=" + WHITE_APPLY
+                + " fillCoefficient=" + FILL_COEFFICIENT
+                + " noiseSource=motionV2WronskiCamera2"
+                + " noiseS=" + noiseS
+                + " noiseO=" + noiseO
+                + " gainNoiseMaxPass1=" + gain1.noiseLimit
+                + " gainNoiseClampedPass1=" + gain1.noiseClamped
+                + " gainMaxClampedPass1=" + gain1.maxClamped
+                + " gainNoiseMaxFinal=" + gain2.noiseLimit
+                + " gainNoiseClampedFinal=" + gain2.noiseClamped
+                + " gainMaxClampedFinal=" + gain2.maxClamped
+                + " curveOffEnd=" + curveOff[CURVE_SIZE - 1]
+                + " curveOnEnd=" + curveOn[CURVE_SIZE - 1]
+                + " curveActiveSamples=" + activeSamples
+                + " curveMaxDelta=" + maxDelta
+                + " shortFusionUnchanged=true"
+                + " irisColorUnchanged=true"
+                + " heicUhHdrUnchanged=true";
+        Log.i(Name, line);
+        try { MotionTrace.processingState("IRIS_26650_PHOTON_HIGHLIGHT_COMPRESSION", line); }
+        catch (Throwable ignored) {}
+
+        WorkingTexture = previousNode.WorkingTexture;
+        glProg.closed = true;
+    }
+
+    private static void clearPublishedCurves(PostPipeline p) {
+        if (p.motionV2PhotonCurveOff != null) try { p.motionV2PhotonCurveOff.close(); } catch (Throwable ignored) {}
+        if (p.motionV2PhotonCurveOn != null) try { p.motionV2PhotonCurveOn.close(); } catch (Throwable ignored) {}
+        p.motionV2PhotonCurveOff = null;
+        p.motionV2PhotonCurveOn = null;
+        p.motionV2PhotonHighlightCompressionEnabled = false;
+        p.motionV2PhotonAdaptiveWhitePoint = 1.0f;
+        p.motionV2PhotonHighlightKnee = 1.0f;
+        p.motionV2PhotonHighlightClippedFraction = 0.0f;
+    }
+
+    private static void requireWhitePoint(float[] wp) {
+        if (wp == null || wp.length < 3) throw new IllegalStateException("26650 missing camera neutral");
+        for (int i = 0; i < 3; ++i) {
+            if (!Float.isFinite(wp[i]) || wp[i] <= 0.0f || wp[i] > 1.0f) {
+                throw new IllegalStateException("26650 invalid camera neutral[" + i + "]=" + wp[i]);
+            }
+        }
+    }
+
+    private static int sum(int[] h) {
+        long s = 0L;
+        for (int v : h) s += v;
+        if (s > Integer.MAX_VALUE) throw new IllegalStateException("26650 histogram count overflow");
+        return (int)s;
+    }
+
+    private static void remapHistogram(float[][] mapped, float[] extent, float adaptiveWhitePoint) {
+        float awp = Math.max(adaptiveWhitePoint, 1.0e-8f);
+        for (int c = 0; c < 3; ++c) {
+            for (int i = 0; i < HIST_SIZE; ++i) {
+                float linear = (i + 0.5f) / (HIST_SIZE - 1.0f) * extent[c] / awp;
+                mapped[c][i] = srgbEncodeExtended(linear) * (HIST_SIZE - 1.0f);
+            }
+        }
+    }
+
+    private static float estimateAvg(int[][] result, float[][] mapped,
+                                     int histNormR, int histNormG, int histNormB) {
+        float cap = HIST_SIZE - 1.0f;
+        float sum = 0.0f;
+        int cnt = 0;
+        int total = histNormR + histNormG + histNormB;
+        for (int i = 0; i < HIST_SIZE - 1; ++i) {
+            if (cnt > total * FILL_COEFFICIENT) break;
+            sum += result[0][i] * Math.min(mapped[0][i], cap)
+                    + result[1][i] * Math.min(mapped[1][i], cap)
+                    + result[2][i] * Math.min(mapped[2][i], cap);
+            cnt += result[0][i] + result[1][i] + result[2][i];
+        }
+        return cnt > 0 ? sum / cnt : TARGET;
+    }
+
+    private static float searchWhite(int[][] result, float[][] mapped,
+                                     int histNormR, int histNormG, int histNormB, float mpy) {
+        float white = Float.MAX_VALUE;
+        for (int c = 0; c < 3; ++c) {
+            int histNorm = c == 0 ? histNormR : (c == 1 ? histNormG : histNormB);
+            float sum = 0.0f;
+            int cnt = 0;
+            double lower = Math.max(HIST_SIZE * 2.0 / 3.0, HIST_SIZE / (mpy + 0.001));
+            for (int i = HIST_SIZE - 1; i > lower; --i) {
+                sum += result[c][i] * mapped[c][i];
+                cnt += result[c][i];
+                if (cnt > histNorm * 0.005f) break;
+            }
+            if (cnt == 0) {
+                sum = HIST_SIZE - 1.0f;
+                cnt = 1;
+            }
+            white = Math.min(white, (sum / cnt) / HIST_SIZE);
+        }
+        return white;
+    }
+
+    private static GainClamp clampGain(float requested, float noiseS, float noiseO) {
+        float mpy = requested;
+        float gainNoiseMax = (float)(NOISE_MAX / Math.sqrt(noiseS * 0.5 + noiseO));
+        gainNoiseMax = Math.max(gainNoiseMax, 1.0f);
+        boolean noiseClamped = false;
+        boolean maxClamped = false;
+        if (mpy > gainNoiseMax) { mpy = gainNoiseMax; noiseClamped = true; }
+        if (mpy > GAIN_MAX) { mpy = GAIN_MAX; maxClamped = true; }
+        return new GainClamp(mpy, gainNoiseMax, noiseClamped, maxClamped);
+    }
+
+    private static float srgbEncodeExtended(float v) {
+        if (v <= 0.0f) return 0.0f;
+        return v <= 0.0031308f ? v * 12.92f
+                : 1.055f * (float)Math.pow(v, 1.0f / 2.4f) - 0.055f;
+    }
+
+    private static float srgbDecodeExtended(float v) {
+        return v <= 0.0031308f ? v / 12.92f
+                : (float)Math.pow((v + 0.055f) / 1.055f, 2.4f);
+    }
+
+    private static float softShoulder(float x, float knee) {
+        if (x <= knee) return x;
+        float t = x - knee;
+        float s = 1.0f - knee;
+        float g = t / s;
+        return knee + s * g * g;
+    }
+
+    private static float clamp01(float v) { return Math.min(Math.max(v, 0.0f), 1.0f); }
+    private static float mix(float a, float b, float t) { return a + (b - a) * t; }
+
+    private static final class GainClamp {
+        final float value;
+        final float noiseLimit;
+        final boolean noiseClamped;
+        final boolean maxClamped;
+        GainClamp(float value, float noiseLimit, boolean noiseClamped, boolean maxClamped) {
+            this.value = value;
+            this.noiseLimit = noiseLimit;
+            this.noiseClamped = noiseClamped;
+            this.maxClamped = maxClamped;
+        }
+    }
+}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 05d590eb1765c0282899bf8cb842accdd3971df4..1e8758eaede516798591bc48ba01cc48414a8e4d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -742,6 +742,8 @@ public final class MotionV2Render extends Node {
                     basePipeline.mParameters.motionV2ShadowBodyStrength);
             glProg.setVar("iris26639ShadowBodyEnd",
                     basePipeline.mParameters.motionV2ShadowBodyEnd);
+            glProg.setVar("iris26650HighlightCompressionEnabled",
+                    ((PostPipeline) basePipeline).motionV2PhotonHighlightCompressionEnabled ? 1 : 0);
             glProg.setVar("iris26626Mode", 6);
             glProg.drawBlocks(coherentHighlightTone);
             try { finalTone.close(); } catch (Throwable ignored) {}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index 3c5d57bee199b58faa980f732058d052841c7d10..c062589d66b09003bd3199a04401bacc9021ac10 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -45,6 +45,16 @@ public class PostPipeline extends GLBasePipeline {
     public GLTexture irisMotionDirectRgbTexture;
     public GLTexture motionV2HighlightProvenanceTexture;
 
+    /* IRIS_26650_PHOTON_HIGHLIGHT_COMPRESSION_HANDOFF
+     * Analysis curves are created from the pre-color Sabre camera RGB and consumed exactly once
+     * by MotionV2ColorTransform. OFF publishes no textures and is the 26648 pixel path. */
+    public GLTexture motionV2PhotonCurveOff;
+    public GLTexture motionV2PhotonCurveOn;
+    public boolean motionV2PhotonHighlightCompressionEnabled = false;
+    public float motionV2PhotonAdaptiveWhitePoint = 1.0f;
+    public float motionV2PhotonHighlightKnee = 1.0f;
+    public float motionV2PhotonHighlightClippedFraction = 0.0f;
+
     /* IRIS_26432_TRUE_V2_GAINMAP_HANDOFF */
     public Bitmap motionV2GainMapBitmap;
     public float motionV2GainMapMaxRatio = 1.0f;
@@ -534,6 +544,10 @@ public class PostPipeline extends GLBasePipeline {
              * shutter-time viewfinder. User Iris controls remain later and additive.
              */
             validateIrisReconstructionOwnership();
+            final boolean iris26650HighlightCompression =
+                    com.particlesdevs.photoncamera.settings.PreferenceKeys.isHighlightCompressionOn();
+            add(new MotionV2PhotonHighlightCompression(iris26650HighlightCompression));
+            add(new StageTelemetry("IRIS_26650_PHOTON_HIGHLIGHT_COMPRESSION_ANALYSIS"));
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
                     "IRIS_26560_MOTION_SABRE_POST_OWNERSHIP",
                     "owner=SABRE carrier=RESOLVE_SABRE_LINEAR_RGB"
@@ -571,7 +585,7 @@ public class PostPipeline extends GLBasePipeline {
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
                     "IRIS_26410_MOTION_V2_POST_GRAPH",
                     "owner=" + motionReconstructionOwnerName(mParameters.motionV2ReconstructionOwner)
-                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,OwnerSpecificPreColor,MotionV2ColorTransform[ACR3],MotionV2ViewfinderExposureMatcher,MotionV2DisplayExposure,IrisManualOptional,MotionV2Render,RotateWatermark"
+                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,PhotonHighlightCompressionAnalysis,OwnerSpecificPreColor,MotionV2ColorTransform[ACR3+PhotonToggleDifferential],MotionV2ViewfinderExposureMatcher,MotionV2DisplayExposure,IrisManualOptional,MotionV2Render,RotateWatermark"
                             + " spatialHighlightNode=false"
                             + " directMultiframeRgb=" + directRgbCarrier
                             + " strictSabreAuthority26560=true"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
index 0e62c7da9db048132927754ad940d14b7068d2a9..5a5fbef92dfad284958729f28c6d1bb83cd9a6b8 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
@@ -51,6 +51,8 @@ public class PreferenceKeys {
         COMMON_KEYS.add(Key.KEY_IRIS_SUPER_RES.mValue);
         // IRIS_26636_HEIC_GLOBAL_PUBLICATION_TOGGLE: independent from RAW/DNG ownership.
         COMMON_KEYS.add(Key.KEY_IRIS_HEIC_OUTPUT.mValue);
+        // IRIS_26650_HIGHLIGHT_COMPRESSION_GLOBAL_TOGGLE: one universal photo presentation choice.
+        COMMON_KEYS.add(Key.KEY_HIGHLIGHT_COMPRESSION.mValue);
     }
 
     private final SettingsManager settingsManager;
@@ -77,6 +79,7 @@ public class PreferenceKeys {
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_BRACKETING_MODE, 0); // Default to disable bracketing
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_IRIS_SUPER_RES, false);
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_IRIS_HEIC_OUTPUT, false);
+        settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_HIGHLIGHT_COMPRESSION, true);
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_PHOTO_PREFIX_NAME, "IMG_");
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_VIDEO_RESOLUTION, resources.getString(R.string.pref_video_resolution_default));
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_RAWVIDEO_DOWNSCALE_4X, false);
@@ -300,6 +303,10 @@ public class PreferenceKeys {
         return preferenceKeys.settingsManager.getBoolean(SCOPE_GLOBAL, Key.KEY_IRIS_HEIC_OUTPUT);
     }
 
+    public static boolean isHighlightCompressionOn() {
+        return preferenceKeys.settingsManager.getBoolean(SCOPE_GLOBAL, Key.KEY_HIGHLIGHT_COMPRESSION);
+    }
+
     public static void setIrisHeicOutput(boolean value) {
         if (value && isIrisSuperResOn()) value = false;
         preferenceKeys.settingsManager.set(SCOPE_GLOBAL, Key.KEY_IRIS_HEIC_OUTPUT, value);
@@ -559,6 +566,7 @@ public class PreferenceKeys {
         KEY_BRACKETING_MODE(R.string.pref_bracketing_key),
         KEY_IRIS_SUPER_RES(R.string.pref_iris_super_res_key),
         KEY_IRIS_HEIC_OUTPUT(R.string.pref_iris_heic_output_key),
+        KEY_HIGHLIGHT_COMPRESSION(R.string.pref_highlight_compression_key),
         KEY_COUNTDOWN_TIMER(R.string.pref_countdown_timer_key),
         /**
          * Enhanced settings keys
diff --git a/app/src/main/res/values/default_prefs.xml b/app/src/main/res/values/default_prefs.xml
index 52d1de1143dc5c1681bb6e7deaeb83f23c875711..f1469ac1e85cf5e8311dbb054877a891dab222bf 100644
--- a/app/src/main/res/values/default_prefs.xml
+++ b/app/src/main/res/values/default_prefs.xml
@@ -13,6 +13,7 @@
     <bool name="pref_system_nr_default">false</bool>
     <bool name="pref_disable_align_default">false</bool>
     <bool name="pref_hdrx_nr_default">true</bool>
+    <bool name="pref_highlight_compression_default">true</bool>
     <string name="pref_iris_luma_denoise_default" translatable="false">0.0</string>
     <string name="pref_iris_chroma_denoise_default" translatable="false">1.0</string>
     <string name="pref_iris_saturation_default" translatable="false">1.0</string>
diff --git a/app/src/main/res/values/preference_keys.xml b/app/src/main/res/values/preference_keys.xml
index 2ed6f213ec69ae9d6ce01b713e9091095dbbcf60..569c7147200d20443f43ce5b35a30b61b0888c78 100644
--- a/app/src/main/res/values/preference_keys.xml
+++ b/app/src/main/res/values/preference_keys.xml
@@ -38,6 +38,7 @@
     <string name="pref_gain_seekbar_key" translatable="false">pref_gain_seekbar_key</string>
     <string name="pref_shadows_seekbar_key" translatable="false">pref_shadows_seekbar_key</string>
     <string name="pref_frame_count_key" translatable="false">pref_frame_count_key</string>
+    <string name="pref_highlight_compression_key" translatable="false">pref_highlight_compression</string>
     <string name="pref_contrast_seekbar_key" translatable="false">pref_contrast_seekbar_key</string>
     <string name="pref_sharpness_seekbar_key" translatable="false">pref_sharpness_seekbar_key</string>
     <string name="pref_saturation_seekbar_key" translatable="false">pref_saturation_seekbar_key</string>
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index 6bb7755bdea11771a386830fc8d8647e72d69cd0..105c3c9cdf608b4d0cbece5b48e0ebdaca0a6ae2 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -175,6 +175,8 @@
     <string name="rawvideo_write_zip">Write ZIP</string>
     <string name="rawvideo_crop_169">Crop to 16:9</string>
     <string name="photo_settings">Photo Settings</string>
+    <string name="highlight_compression">Highlight Compression</string>
+    <string name="highlight_compression_summary">Preserve gradation in bright highlights</string>
     <string name="video_resolution">Video Resolution</string>
     <string name="pref_video_resolution_key" translatable="false">pref_video_resolution_key</string>
     <string name="pref_video_resolution_default" translatable="false">1920x1080</string>
diff --git a/app/src/main/res/xml/preferences.xml b/app/src/main/res/xml/preferences.xml
index 0aa47896ff2532a2edce6bc0f12567bef93efdc2..43356dccdf6d780c10b1759fb4937fcf97e435f2 100644
--- a/app/src/main/res/xml/preferences.xml
+++ b/app/src/main/res/xml/preferences.xml
@@ -14,6 +14,7 @@
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_photo_key" ns0:title="@string/photo_settings">
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="@string/pref_frame_count_key" ns0:layout="@layout/preference_seekbar" ns0:title="@string/frame_count" ns0:defaultValue="@string/pref_framecount_default" ns1:maxValue="50" ns1:minValue="1" ns0:icon="@drawable/ic_burst_mode_black_24dp" />
+        <com.particlesdevs.photoncamera.ui.settings.custompreferences.ManagedSwitchPreference ns0:key="@string/pref_highlight_compression_key" ns0:defaultValue="@bool/pref_highlight_compression_default" ns0:layout="@layout/preference_with_margin" ns0:title="@string/highlight_compression" ns0:summary="@string/highlight_compression_summary" ns0:icon="@drawable/ic_tune_black_24dp" />
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_video_key" ns0:title="@string/video_settings">
         <ListPreference ns0:layout="@layout/preference_with_margin" ns0:key="@string/pref_video_resolution_key" ns0:title="@string/video_resolution" ns1:useSimpleSummaryProvider="true" ns0:icon="@drawable/ic_saturation" ns0:entries="@array/video_resolution_entries" ns0:entryValues="@array/video_resolution_entryvalues" ns0:defaultValue="@string/pref_video_resolution_default" />
diff --git a/app/version.properties b/app/version.properties
index a06ef3066ec0e2be3dfc6bf68962387e0b9ef98e..3b68343f2fb1223a717d70778b34fe27b2d85aee 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726648
-VERSION_BUILD=26648
+VERSION_NAME=0.9726650
+VERSION_BUILD=26650
