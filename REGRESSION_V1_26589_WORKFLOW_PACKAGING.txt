WORKFLOW / PACKAGING REGRESSION CONTRACT

app/build/** and app/.cxx/** never count as runtime-source changes.
Repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
Unexpected app/src/** changes remain visible and fail.
The live repository app container is never compared directly to compiled-candidate authority.
Runtime source is written only after deterministic candidate construction and packaged pre-build gates.
The exact successful 26588 compiler/build order is preserved unchanged.
No backup branch is created for this narrow runtime assertion correction.
