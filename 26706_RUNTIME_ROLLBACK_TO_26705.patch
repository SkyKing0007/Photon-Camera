diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index a7aec906907756153024d076f879b5ad261cb58b..92cd088363b74a7e3e0554050c13aa3b07941912 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -86,15 +86,8 @@ float iris26640MapMotionSdrGuide(float sourceGuide){
     const float upperStart=0.65;
     if(adaptiveEnable<=1.0e-7 || x<=upperStart)return legacy;
     float pressure=iris26640HighlightPressure();
-    /* IRIS_26706_VISUAL_HIGHLIGHT_SPACING
-     * 26705 proved that broad visually flattened highlights can cluster at nominal source white
-     * even when hard sensor clipping is not the deciding condition. Preserve the exact mapping
-     * through source guide 0.65, but for broad highlight pressure reserve more SDR code space above
-     * nominal white and increase (rather than decrease) the C1 source-white slope. This remains one
-     * global pointwise monotone scalar: no spatial mask, no semantic scene class, no SHORT ownership
-     * change, and no channel-dependent operation. */
-    float targetWhite=mix(0.945,0.895,pressure);
-    float targetSlope=mix(0.360,0.620,pressure);
+    float targetWhite=mix(0.945,0.925,pressure);
+    float targetSlope=mix(0.360,0.300,pressure);
     float startValue=0.0; float startSlope=0.0;
     iris26640LegacyBody(upperStart,requested,startValue,startSlope);
     float width=1.0-upperStart;
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 8127aa6217f6947602fe7ac483cabd03e918172f..3aa2888c2142151c9b2c7a3b4e1432e42108fbbf 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -174,15 +174,8 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
     if(adaptiveEnable<=1.0e-7 || x<=upperStart) return legacy;
 
     float pressure=iris26623HighlightPressure();
-    /* IRIS_26706_VISUAL_HIGHLIGHT_SPACING
-     * 26705 proved that broad visually flattened highlights can cluster at nominal source white
-     * even when hard sensor clipping is not the deciding condition. Preserve the exact mapping
-     * through source guide 0.65, but for broad highlight pressure reserve more SDR code space above
-     * nominal white and increase (rather than decrease) the C1 source-white slope. This remains one
-     * global pointwise monotone scalar: no spatial mask, no semantic scene class, no SHORT ownership
-     * change, and no channel-dependent operation. */
-    float targetWhite=mix(0.945,0.895,pressure);
-    float targetSlope=mix(0.360,0.620,pressure);
+    float targetWhite=mix(0.945,0.925,pressure);
+    float targetSlope=mix(0.360,0.300,pressure);
     float startValue=0.0;
     float startSlope=0.0;
     iris26623LegacyBody(upperStart,requested,startValue,startSlope);
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index c64e4032c4df268e4bcbb3e3c8fc136afc4495d5..127667c49d4158967e314a3d7e0793dbca8db7e0 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -3272,14 +3272,11 @@ internal object GlesMgcRawSabreShaders {
     """.trimIndent()
 
     /* IRIS_26651_FUSION_RADIANCE_WEIGHT_TELEMETRY
-     * IRIS_26706_SAMPLED_FUSION_DECISION_TELEMETRY
-     * Tiny 64x48 diagnostic only: recover SHORT luminance algebraically from the actual NORMAL/fused
-     * production values and final scalar radiance authority, and carry the packed decision code for
-     * sampled admission/rejection counts. It never feeds production reconstruction and replaces no
-     * full-resolution image path. */
+     * Tiny diagnostic only: recover SHORT luminance algebraically from the actual NORMAL/fused
+     * production values and final scalar radiance authority. Chromaticity authority is carried
+     * separately and never changes this luminance equation. It never feeds production reconstruction. */
     val universalFusionTelemetry26651 = """
         #version 300 es
-        /* IRIS_26706_SAMPLED_FUSION_DECISION_TELEMETRY */
         precision highp float;
         uniform sampler2D uNormalMean;
         uniform sampler2D uFusedMean;
@@ -3299,7 +3296,7 @@ internal object GlesMgcRawSabreShaders {
             float shortRadiance = authority > 0.005
                 ? max((fusedRadiance - (1.0 - authority) * normalRadiance) / authority, 0.0)
                 : normalRadiance;
-            oTelemetry = vec4(normalRadiance, shortRadiance, authority, float(packed) / 255.0);
+            oTelemetry = vec4(normalRadiance, shortRadiance, authority, fusedRadiance);
         }
     """.trimIndent()
 
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index d69dea8f56737d2655b332d4bc1fdc1157d41548..779e482e5afd598aa4d2a9424e9e51efdf9eb479 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -25,7 +25,6 @@ import java.nio.ByteOrder
 import kotlin.math.ceil
 import kotlin.math.exp
 import kotlin.math.max
-import kotlin.math.roundToInt
 import kotlin.math.sqrt
 
 internal enum class MgcRawProcessorPipeline {
@@ -2813,14 +2812,6 @@ internal class GlesMgcRawSpatialStacker(
                 )
                 PLog.i(SABRE_TAG, "IRIS_26699_FUSION_DECISION_BARRIER_REMOVED productionFusionUnchanged=true")
                 PLog.i(SABRE_TAG, "IRIS_26699_FUSION_RADIANCE_BARRIER_REMOVED productionFusionUnchanged=true")
-                /* IRIS_26706_SAMPLED_FUSION_DECISION_TELEMETRY
-                 * One 64x48 probe restores decision/radiance observability without reviving the old
-                 * full-resolution R8 readback or changing production fusion. */
-                logSabreUniversalFusionRadiance26651(
-                    normalMean = resolveAccumulatedColor26604,
-                    fusedMean = fused,
-                    decision = fusionDecision26651,
-                )
                 renderSabreShortColorValidityAugment26651(
                     shortColorValidity = shortColorValidity26651,
                     normalWeightR = sabreRSupport26613,
@@ -3077,8 +3068,8 @@ internal class GlesMgcRawSpatialStacker(
                         "shortInTemporalAccumulator=false scalarRadianceAuthority=true " +
                         "normalChromaMaster=true shortChromaRequiresAllNormalColorSupportLost=true " +
                         "edgeCorrespondence=true spatialFill=false dilation=false borderExtrapolation=false " +
-                        "fusionDecisionMeasured=IRIS_26706_FUSION_DECISION_SAMPLE " +
-                        "radianceWeightsMeasured=IRIS_26706_FUSION_DECISION_SAMPLE " +
+                        "fusionDecisionMeasured=IRIS_26651_NORMAL_MASTER_SHORT_FUSION " +
+                        "radianceWeightsMeasured=IRIS_26651_FUSION_RADIANCE_WEIGHTS " +
                         "postVgnMeasured=IRIS_26605_HDR_DOMAIN_STATS " +
                         "true2xGuide=${enableSabreSuperRes} true2xDetailEvidenceShort=false " +
                         "uhdrSource=COMMON_POST_VGN_HDR_MASTER"
@@ -4470,39 +4461,17 @@ internal class GlesMgcRawSpatialStacker(
             var fusedSum = 0.0
             var shortWeightSum = 0.0
             var allCount = 0
-            var validCount = 0
             var activeCount = 0
-            var normalOwned = 0
-            var blended = 0
-            var shortOwned = 0
-            var geometryRejected = 0
-            var sourceRejected = 0
-            var snrRejected = 0
             for (index in 0 until probeWidth * probeHeight) {
                 val normal = Half.toFloat(half.get(index * 4)).toDouble()
                 val short = Half.toFloat(half.get(index * 4 + 1)).toDouble()
-                val shortWeight = Half.toFloat(half.get(index * 4 + 2)).toDouble().coerceIn(0.0, 1.0)
-                val decisionNorm = Half.toFloat(half.get(index * 4 + 3)).toDouble()
-                if (!normal.isFinite() || !short.isFinite() || !shortWeight.isFinite() || !decisionNorm.isFinite()) continue
-                val packed = (decisionNorm * 255.0).roundToInt().coerceIn(0, 255)
+                val shortWeight = Half.toFloat(half.get(index * 4 + 2)).toDouble()
+                val fused = Half.toFloat(half.get(index * 4 + 3)).toDouble()
+                if (!normal.isFinite() || !short.isFinite() || !shortWeight.isFinite() || !fused.isFinite()) continue
                 normalSum += normal
-                shortWeightSum += shortWeight
-                fusedSum += (1.0 - shortWeight) * normal + shortWeight * short
+                fusedSum += fused
+                shortWeightSum += shortWeight.coerceIn(0.0, 1.0)
                 allCount++
-                when {
-                    packed <= 191 -> {
-                        validCount++
-                        val radianceCode = packed % 48
-                        when {
-                            radianceCode <= 1 -> normalOwned++
-                            radianceCode >= 46 -> shortOwned++
-                            else -> blended++
-                        }
-                    }
-                    packed < 216 -> geometryRejected++
-                    packed < 233 -> sourceRejected++
-                    else -> snrRejected++
-                }
                 if (shortWeight > 0.005) {
                     shortSum += short
                     activeCount++
@@ -4510,18 +4479,15 @@ internal class GlesMgcRawSpatialStacker(
             }
             val meanShortWeight = if (allCount > 0) shortWeightSum / allCount else Double.NaN
             val details =
-                "samples=$allCount sampleGrid=${probeWidth}x${probeHeight} fullFrameReadback=false " +
-                    "sampledValid=$validCount sampledNormalOwned=$normalOwned sampledBlended=$blended sampledShortOwned=$shortOwned " +
-                    "sampledGeometryRejected=$geometryRejected sampledSourceRejected=$sourceRejected sampledSnrRejected=$snrRejected " +
-                    "activeShortSamples=$activeCount " +
+                "samples=$allCount activeShortSamples=$activeCount " +
                     "normalRadianceMean=${if (allCount > 0) normalSum / allCount else Double.NaN} " +
                     "normalizedShortRadianceMean=${if (activeCount > 0) shortSum / activeCount else Double.NaN} " +
                     "fusedRadianceMean=${if (allCount > 0) fusedSum / allCount else Double.NaN} " +
                     "normalFinalWeightMean=${if (meanShortWeight.isFinite()) 1.0 - meanShortWeight else Double.NaN} " +
-                    "shortFinalWeightMean=$meanShortWeight productionFusionUnchanged=true " +
+                    "shortFinalWeightMean=$meanShortWeight " +
                     "confidenceSemantics=FINAL_PHYSICAL_RADIOMETRIC_WEIGHT"
-            PLog.i(SABRE_TAG, "IRIS_26706_FUSION_DECISION_SAMPLE $details")
-            MotionTrace.processingState("IRIS_26706_FUSION_DECISION_SAMPLE", details)
+            PLog.i(SABRE_TAG, "IRIS_26651_FUSION_RADIANCE_WEIGHTS $details")
+            MotionTrace.processingState("IRIS_26651_FUSION_RADIANCE_WEIGHTS", details)
         } finally {
             releaseOwnedTexture(probe, "26651 fusion radiance/weight telemetry")
         }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 44c4fd6451041bd0ee27b137d227cf8cf65f4b0c..93be7f066d4958d67328f1bd73aa78640011cec4 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -122,13 +122,9 @@ public final class MotionV2Render extends Node {
     public static final float IRIS_26621_SDR_WHITE_ANCHOR = 0.95f;
     public static final float IRIS_26623_UPPER_TONE_START = 0.65f;
     public static final float IRIS_26623_SPARSE_WHITE_ANCHOR = 0.945f;
-    /* IRIS_26706_VISUAL_HIGHLIGHT_SPACING
-     * Keep sparse behavior and the <=0.65 body mapping unchanged. Under broad visual highlight
-     * pressure, reserve more display range above nominal white and increase the C1 white slope so
-     * near-white ordering is not crowded into the same few SDR codes. */
-    public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.895f;
+    public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.925f;
     public static final float IRIS_26623_SPARSE_WHITE_SLOPE = 0.360f;
-    public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.620f;
+    public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.300f;
     /* IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE constants. OFF remains exact 26652. */
     private static final float IRIS_26653_HC_WEAK_WHITE_ANCHOR = 0.915f;
     private static final float IRIS_26653_HC_STRONG_WHITE_ANCHOR = 0.890f;
@@ -1521,52 +1517,6 @@ public final class MotionV2Render extends Node {
                     + " gainMapSharedMap=true"
                     + " preColorDifferential=false");
 
-            final float iris26706At090 = iris26623MapMotionSdrFinalGuide(0.90f,
-                    basePipeline.mParameters.motionV2DisplayGain,
-                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
-                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-            final float iris26706At095 = iris26623MapMotionSdrFinalGuide(0.95f,
-                    basePipeline.mParameters.motionV2DisplayGain,
-                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
-                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-            final float iris26706At100 = iris26623MapMotionSdrFinalGuide(1.00f,
-                    basePipeline.mParameters.motionV2DisplayGain,
-                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
-                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-            final float iris26706At101 = iris26623MapMotionSdrFinalGuide(1.01f,
-                    basePipeline.mParameters.motionV2DisplayGain,
-                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
-                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-            final float iris26706At125 = iris26623MapMotionSdrFinalGuide(1.25f,
-                    basePipeline.mParameters.motionV2DisplayGain,
-                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
-                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
-                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-            Log.i(Name, "IRIS_26706_VISUAL_HIGHLIGHT_SPACING"
-                    + " owner=IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE"
-                    + " visualFlatteningCountsEvenBelowHardSensorClip=true"
-                    + " lowerToneExactThrough=" + IRIS_26623_UPPER_TONE_START
-                    + " sparseWhiteAnchor=" + IRIS_26623_SPARSE_WHITE_ANCHOR
-                    + " broadWhiteAnchor=" + IRIS_26623_BROAD_WHITE_ANCHOR
-                    + " sparseWhiteSlope=" + IRIS_26623_SPARSE_WHITE_SLOPE
-                    + " broadWhiteSlope=" + IRIS_26623_BROAD_WHITE_SLOPE
-                    + " pressure=" + iris26623Pressure
-                    + " map090=" + iris26706At090
-                    + " map095=" + iris26706At095
-                    + " map100=" + iris26706At100
-                    + " map101=" + iris26706At101
-                    + " map125=" + iris26706At125
-                    + " normalAeUnchanged=true shortExposureUnchanged=true shortFusionProductionUnchanged=true");
-
             Log.i(Name, "IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE"
                     + " master=extendedLinearPreTone"
                     + " brightnessTargetGain=" + basePipeline.mParameters.motionV2DisplayGain
diff --git a/app/version.properties b/app/version.properties
index e5c6c5b2a4abd6d91fc5c48ea6572d7685f80cc1..c68c0956cc2b4de15446a1f72c1b3efe44107238 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726706
-VERSION_BUILD=26706
+VERSION_NAME=0.9726705
+VERSION_BUILD=26705
