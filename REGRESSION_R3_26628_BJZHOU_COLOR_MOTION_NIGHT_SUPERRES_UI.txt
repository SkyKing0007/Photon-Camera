26628 R3 permanent applicable regressions

1. Failed 26628 R1 NDK compile must never recur: motionv2_jpeg444_jni.cpp may not redeclare the int y pixel-coordinate parameter as a local float y. Exact failed token `float y=luma(center)` is forbidden; exactly two corrected `float lum=luma(center)` declarations are required.
2. Upstream Sabre/SHORT/CFA/alignment/merge/denoise and established pink/cyan edge protections stay byte-identical to successful 26627 unless intentionally reopened later.
3. Motion/Night/SR default color may not reintroduce generic >1 chroma restoration; 0.95 presentation stays contraction-only with common-axis negative handling.
4. Equal ForwardMatrix anchors remain valid; no Xiaomi/OnePlus/manufacturer/model color heuristic.
5. Full 3D DCP HueSat/Look ordering remains value-hue-saturation in both GL and true2x CPU publication.
6. Lens text geometry must not rely on platform Button internal padding/minimum/baseline behavior. Lens text is explicitly centered with zero padding and disabled font padding.
7. Lens row may prefer a 3dp upward lift only while preserving the measured >=10dp live-viewfinder and >=8dp chevron hard clearances. No device/model UI special case.
8. app/build/** and app/.cxx/** never count as runtime-source changes; repository-only scaffolding absent from compiled authority never counts as runtime change; unexpected app/src/** remains visible and fails.

R3 exact failed-R2 Java regression:
- AuxButtonsLayout lens children are TextView end-to-end.
- No standalone Button type/import/cast may remain in AuxButtonsLayout.
- Actual revised AuxButtonsLayout.java must pass supplemental javac compilation against minimal Android/app API stubs before packaging.
- Real project :app:compileDebugJavaWithJavac remains mandatory and authoritative in Actions.
