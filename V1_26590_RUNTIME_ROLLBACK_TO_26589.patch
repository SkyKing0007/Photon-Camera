diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 95f9d3bd93dc28b7baf6265b3b6fa247b8c2f7fb..7882b0708973b1fbc950f5bee2511c4d375ae440 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -392,14 +392,12 @@ internal object GlesMgcRawSabreShaders {
     """.trimIndent()
 
 
-    /* IRIS_26590_NEAR_CLIP_FAIL_CLOSED_SHORT_RESTORE_MASK
-     * One scalar confidence controls the whole RGB replacement. Match the physical 0.98
-     * black-subtracted near-clip concept that triggers SHORT capture: at least two CFA sites in
-     * the center NORMAL 2x2 quad must be near saturation. SHORT must retain strong headroom in its
-     * aligned 3x3 source neighborhood. Alignment/rejection/coverage remain owned by the exact
-     * Sabre reference-domain flow; there is no second correspondence search. Radiometric agreement
-     * is still checked only on nearby sensor-unclipped RAW quads after black subtraction and the
-     * exact exposure ratio, so low confidence remains exact NORMAL RGB.
+    /* IRIS_26587_FAIL_CLOSED_SHORT_RESTORE_MASK
+     * One scalar confidence controls the whole RGB replacement. NORMAL clipping is proven in
+     * unnormalized sensor codes across at least two CFA sites in the center 2x2 quad. SHORT must
+     * remain unclipped in its aligned 3x3 neighborhood. Alignment/rejection/coverage come from
+     * the existing Sabre reference-domain owner. Radiometric agreement is checked only on nearby
+     * unclipped RAW quads after black subtraction and the exact exposure ratio.
      */
     val shortRestoreMask26587 = """
         #version 300 es
@@ -418,8 +416,6 @@ internal object GlesMgcRawSabreShaders {
         uniform vec4 uShortBlackByPhase;
         uniform float uWhiteLevel;
         uniform float uExposureRatio;
-        uniform float uReferenceNearClipThreshold;
-        uniform float uShortHeadroomThreshold;
         uniform float uNormalCoverageThreshold;
         uniform float uFlowVariationThreshold;
         uniform float uShortWeightThreshold;
@@ -434,7 +430,7 @@ internal object GlesMgcRawSabreShaders {
         float normalizedRaw(float raw, float black) {
             return max(raw - black, 0.0) / max(uWhiteLevel - black, 1.0);
         }
-        int sensorClippedQuad(highp usampler2D t, ivec2 p) {
+        int clippedQuad(highp usampler2D t, ivec2 p) {
             ivec2 q = (clampRaw(p) / 2) * 2;
             float clipCode = uWhiteLevel - 0.5;
             int count = 0;
@@ -442,28 +438,10 @@ internal object GlesMgcRawSabreShaders {
                 count += rawAt(t, q + ivec2(x, y)) >= clipCode ? 1 : 0;
             return count;
         }
-        int referenceNearClipQuad(ivec2 p) {
-            ivec2 q = (clampRaw(p) / 2) * 2;
-            int count = 0;
-            for (int y = 0; y < 2; ++y) for (int x = 0; x < 2; ++x) {
-                ivec2 s = q + ivec2(x, y);
-                float signal = normalizedRaw(
-                    rawAt(uReferenceRaw, s),
-                    blackAt(uReferenceBlackByPhase, phaseAt(s))
-                );
-                count += signal >= uReferenceNearClipThreshold ? 1 : 0;
-            }
-            return count;
-        }
-        bool shortNeighborhoodHasHeadroom(ivec2 p) {
-            for (int y = -1; y <= 1; ++y) for (int x = -1; x <= 1; ++x) {
-                ivec2 s = clampRaw(p + ivec2(x, y));
-                float signal = normalizedRaw(
-                    rawAt(uShortRaw, s),
-                    blackAt(uShortBlackByPhase, phaseAt(s))
-                );
-                if (signal >= uShortHeadroomThreshold) return false;
-            }
+        bool shortNeighborhoodUnclipped(ivec2 p) {
+            float clipCode = uWhiteLevel - 0.5;
+            for (int y = -1; y <= 1; ++y) for (int x = -1; x <= 1; ++x)
+                if (rawAt(uShortRaw, p + ivec2(x, y)) >= clipCode) return false;
             return true;
         }
         float quadMean(highp usampler2D t, ivec2 p, vec4 blackByPhase) {
@@ -484,7 +462,7 @@ internal object GlesMgcRawSabreShaders {
             ivec2 referenceP = ivec2(gl_FragCoord.xy);
             ivec2 shortP = clampRaw(ivec2(floor(shortUv * vec2(uRawSize))));
 
-            if (referenceNearClipQuad(referenceP) < 2 || !shortNeighborhoodHasHeadroom(shortP)) {
+            if (clippedQuad(uReferenceRaw, referenceP) < 2 || !shortNeighborhoodUnclipped(shortP)) {
                 oMask = 0.0; return;
             }
             float shortWeight = texture(uShortWeight, referenceUv).r;
@@ -503,7 +481,7 @@ internal object GlesMgcRawSabreShaders {
                 for (int qx = -1; qx <= 1; ++qx) {
                     ivec2 rp = referenceQuad + ivec2(qx * 2, qy * 2);
                     ivec2 sp = shortQuad + ivec2(qx * 2, qy * 2);
-                    if (sensorClippedQuad(uReferenceRaw, rp) != 0 || sensorClippedQuad(uShortRaw, sp) != 0) continue;
+                    if (clippedQuad(uReferenceRaw, rp) != 0 || clippedQuad(uShortRaw, sp) != 0) continue;
                     float nr = quadMean(uReferenceRaw, rp, uReferenceBlackByPhase);
                     float ns = quadMean(uShortRaw, sp, uShortBlackByPhase) * uExposureRatio;
                     if (nr < 0.025 || ns < 0.025) continue;
@@ -525,23 +503,6 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 
-    /* IRIS_26590_SHORT_MASK_EFFECT_PROBE
-     * Read-only sparse diagnostic. Downsample the already-finalized native-resolution mask to a
-     * tiny probe texture so device logs can distinguish an empty mask from real SHORT admission
-     * without a full-frame CPU readback. This shader never feeds reconstruction or rendering.
-     */
-    val shortRestoreMaskProbe26590 = """
-        #version 300 es
-        precision highp float;
-        uniform sampler2D uMask;
-        uniform ivec2 uProbeSize;
-        layout(location = 0) out float oMask;
-        void main() {
-            vec2 uv = gl_FragCoord.xy / vec2(uProbeSize);
-            oMask = texture(uMask, uv).r;
-        }
-    """.trimIndent()
-
     /* IRIS_26587_RGBA16F_WHOLE_RGB_SHORT_RESTORE
      * SHORT remains native-exposure through reconstruction/VGN. The exposure ratio is applied only
      * here, in RGBA16F, and one scalar mask replaces RGB together. Alpha stays NORMAL-owned.
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 995068fec8944a6cfea95bd259fa8601a91c2ae5..7a30deed2b4a9f3571e391c3cb69acd44bae8571 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -553,7 +553,6 @@ internal class GlesMgcRawSpatialStacker(
     private var sabreOutputTransformProgram = 0
     private var sabreRgb16ToFloatProgram = 0
     private var sabreShortRestoreMaskProgram26587 = 0
-    private var sabreShortRestoreMaskProbeProgram26590 = 0
     private var sabreShortRestoreRgba16fProgram26587 = 0
     private var sabreRgbChromaPostprocessor: GlesIris26529SpatialRgbChromaPostprocessor? = null
     private var sabreConvertAlignmentSparseProgram = 0
@@ -2446,7 +2445,7 @@ internal class GlesMgcRawSpatialStacker(
                         resolveParameters = sabreResolveParameters,
                     )
                     highlightShortMask26587 = createTexture(
-                        width, height, GLES30.GL_R8, GLES30.GL_NEAREST,
+                        width, height, GLES30.GL_R8, GLES30.GL_LINEAR,
                     )
                     val requiredNormalAux = minOf(6, normalFrameCount - 1)
                     val coverageThreshold = if (requiredNormalAux >= 2) {
@@ -2467,17 +2466,6 @@ internal class GlesMgcRawSpatialStacker(
                         normalCoverageThreshold = coverageThreshold,
                         output = highlightShortMask26587,
                     )
-                    val shortMaskProbe26590 = probeSabreShortRestoreMask26590(highlightShortMask26587)
-                    PLog.i(
-                        SABRE_TAG,
-                        "IRIS_26590_SHORT_MASK_EFFECT probe=${shortMaskProbe26590.width}x${shortMaskProbe26590.height} " +
-                            "active=${shortMaskProbe26590.activePixels}/${shortMaskProbe26590.totalPixels} " +
-                            "strong=${shortMaskProbe26590.strongPixels}/${shortMaskProbe26590.totalPixels} " +
-                            "maxConfidence=${shortMaskProbe26590.maxConfidence} " +
-                            "meanConfidence=${shortMaskProbe26590.meanConfidence} " +
-                            "referenceNearClipThreshold=0.98 shortHeadroomThreshold=0.90 " +
-                            "sameSabreFlow=true wholeRgb=true maskFilter=NEAREST",
-                    )
                     highlightShortAdmitted26587 = true
                     highlightShortMaskGenerated26587 = true
                     PLog.i(
@@ -3995,8 +3983,6 @@ internal class GlesMgcRawSpatialStacker(
         uniform4fv(program, "uShortBlackByPhase", positionalBlackLevels26587(shortCalibration))
         uniform1f(program, "uWhiteLevel", sensorWhiteLevel)
         uniform1f(program, "uExposureRatio", exposureRatio)
-        uniform1f(program, "uReferenceNearClipThreshold", 0.98f)
-        uniform1f(program, "uShortHeadroomThreshold", 0.90f)
         uniform1f(program, "uNormalCoverageThreshold", normalCoverageThreshold)
         val flowThreshold = MgcSabreRejectionTuning.flowVariationThresholds(ceilDiv(width, 2)).unblockerReduction
         uniform1f(program, "uFlowVariationThreshold", flowThreshold)
@@ -4006,59 +3992,6 @@ internal class GlesMgcRawSpatialStacker(
         draw(program, width, height, intArrayOf(output))
     }
 
-    private data class ShortMaskProbe26590(
-        val width: Int,
-        val height: Int,
-        val activePixels: Int,
-        val strongPixels: Int,
-        val totalPixels: Int,
-        val maxConfidence: Float,
-        val meanConfidence: Float,
-    )
-
-    private fun probeSabreShortRestoreMask26590(mask: Int): ShortMaskProbe26590 {
-        val probeWidth = 64
-        val probeHeight = 48
-        val probe = createTexture(probeWidth, probeHeight, GLES30.GL_R8, GLES30.GL_NEAREST)
-        try {
-            val program = sabreShortRestoreMaskProbeProgram26590
-            check(program != 0)
-            GLES30.glUseProgram(program)
-            bindTexture(program, "uMask", 0, mask)
-            uniform2i(program, "uProbeSize", probeWidth, probeHeight)
-            draw(program, probeWidth, probeHeight, intArrayOf(probe))
-            val bytes = readR8Mask(
-                probe,
-                "26590 SHORT restore mask effect probe",
-                maskWidth = probeWidth,
-                maskHeight = probeHeight,
-            )
-            var active = 0
-            var strong = 0
-            var maximum = 0
-            var sum = 0L
-            bytes.forEach { raw ->
-                val value = raw.toInt() and 0xff
-                if (value != 0) active++
-                if (value >= 128) strong++
-                maximum = maxOf(maximum, value)
-                sum += value.toLong()
-            }
-            val total = bytes.size.coerceAtLeast(1)
-            return ShortMaskProbe26590(
-                width = probeWidth,
-                height = probeHeight,
-                activePixels = active,
-                strongPixels = strong,
-                totalPixels = bytes.size,
-                maxConfidence = maximum / 255f,
-                meanConfidence = sum.toFloat() / (255f * total.toFloat()),
-            )
-        } finally {
-            releaseOwnedTexture(probe, "26590 SHORT mask effect probe")
-        }
-    }
-
     private fun renderSabreShortRestoreRgba16f26587(
         normalRgb: Int,
         shortRgb: Int,
@@ -4342,10 +4275,6 @@ internal class GlesMgcRawSpatialStacker(
             GlesMgcRawSabreShaders.shortRestoreMask26587,
             "iris_26587_sabre_short_restore_mask",
         )
-        sabreShortRestoreMaskProbeProgram26590 = linkProgram(
-            GlesMgcRawSabreShaders.shortRestoreMaskProbe26590,
-            "iris_26590_sabre_short_restore_mask_probe",
-        )
         sabreShortRestoreRgba16fProgram26587 = linkProgram(
             GlesMgcRawSabreShaders.shortRestoreRgba16f26587,
             "iris_26587_sabre_short_restore_rgba16f",
diff --git a/app/version.properties b/app/version.properties
index 80468789134914ce47a6c726df956b37f239508f..204e98004a4ad692130222abcc8558ee9ac42c6e 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726590
-VERSION_BUILD=26590
+VERSION_NAME=0.9726589
+VERSION_BUILD=26589
