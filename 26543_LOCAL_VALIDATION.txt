LOCAL 26543 V1.1 VALIDATION COMPLETED BEFORE PACKAGING
- V1 failure from Actions run 32922062185 was isolated to one GLSL reserved identifier: `vec3 precision` in active embedded mergeRgb. All earlier lineage/ownership/memory/patch gates passed before that compiler failure.
- V1.1 changes only that shader identifier to `precisionCoeffs`; Figure-7 equations and runtime architecture are unchanged.
- Exact 26542 base candidate reconstructed: 967 files.
- Seven-file candidate hashes exactly match validator pins.
- Live ownership validation PASS: both Night (`IrisNightProcessor`) and Motion (`HdrxProcessor`) -> PhotonMotionMgc1271Bridge -> GlesMgcRawFusion(SPATIAL_RGB) -> GlesIris26521SpatialRgbStacker -> embedded covariance/mergeRgb.
- Dormant MotionV2CfaReconstruction is required to have zero production callers.
- Dormant IrisNightMgc1271Bridge caller count required to be zero.
- Figure-7 RAW/2 coordinate/halo self-test PASS.
- Figure-7 active embedded shader/host binding contract PASS.
- GLSL reserved-identifier preflight now deliberately rejects the exact V1 pattern `vec3 precision`; self-test PASS.
- Night bounded-memory contract PASS: persistent native RAW copies=1, Camera2 RAW slots<=3.
- Low-memory banded covariance contract PASS: retained temporal full RAW/2 covariance textures=0.
- Java/Kotlin contract preflight PASS.
- Changed Java/Kotlin lexical/balance + embedded GLSL contract preflight PASS.
- Packaged two-stage transformer from exact 26542 reproduces all 967 audited V1.1 candidate files byte-for-byte.
- Forward binary patch applies with fuzz=0 and reproduces all 967 audited candidate files byte-for-byte.
- Rollback binary patch applies with fuzz=0 and restores all 967 exact 26542 files byte-for-byte.
- Python scripts compile; packaged self-tests pass.
- bash -n build script PASS.
- IMPORTANT: this local container still has no Khronos glslang executable and cannot download binaries directly. The workflow bootstraps SHA-verified Khronos glslang 16.5.0 and compiles the active embedded covariance + mergeRgb before PRE-BUILD SAFETY PROOF. V1.1 is not described as locally glslang-compiled.

V1.2 proof-determinism correction:
- GitHub V1.1 failure was not runtime drift: regenerated patch differed only because runner emitted 7-char git index blob IDs while packaged patch used 40-char IDs.
- Runtime payload/candidate remains byte-identical to V1.1.
- Forward and rollback patch generation is now forced with --full-index on both packaging and runner replay.
- V1.2 workflow explicitly guards both --full-index commands.
- Additional V1.2 hardening: rollback no longer uses git diff -R. Candidate is committed as the rollback baseline and exact 26542 runtime files are copied back before a normal full-index diff, guaranteeing canonical a/ -> b/ paths.
