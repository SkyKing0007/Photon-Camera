26607 V1 Permanent Universal Highlight Reconstruction Regressions
================================================================

DEVICE FAILURE MADE PERMANENT — 26606 FLOW-CELL BOUNDARY UNDERSAMPLING
The successful 26606 device bulb test proved that SHORT genuinely reaches the production path but target rescue remained extremely sparse. The sparse flow grid covered roughly 62x46 cells over a 4096x3072 RAW, so one cell represented about 66x67 RAW pixels, while the 26606 boundary proof sampled only a small approximately 6x6 RAW-pixel center patch. A real highlight boundary could therefore cross a flow cell without ever entering the proof sample. 26607 must sample across the actual flow-cell footprint.

DEVICE FAILURE MADE PERMANENT — PREMATURE SHORT HEADROOM VETO
26606 boundary-stage validity began rejecting useful SHORT evidence near 90% of sensor saturation even though the production common-RBF 3x3 source-CFA guard retains a continuous headroom ramp until the final approximately 0.75% before clipping. A valid 95% or 98% SHORT sample must not be discarded by a stricter early owner. The production physical source-headroom contract remains final authority. True saturation still fails closed.

EFFECTIVE-HIGHLIGHT-LOSS REGRESSION
Literal sensor-code clipping is not the only lost-information state. When exposure-normalized SHORT remains physically valid and demonstrates that a near-saturated NORMAL region has already lost trustworthy radiometric ordering, that effective loss may enter the same universal target-loss owner. Healthy bright overlap must not be misclassified merely because it is bright.

UNIVERSALITY REGRESSION
No bulb, filament, chandelier, cloud, window, wall, sky, sign, metal, skin or other semantic-object detector may own reconstruction. The decision must derive from RAW headroom, exposure-normalized radiometric evidence, geometry/coherence, component connectivity and source validity.

COMPONENT / GEOMETRY REGRESSIONS
- measurable NORMAL<->SHORT overlap establishes absolute radiometric correspondence.
- local affine residual alone is insufficient to prove absolute registration.
- connected component trust uses bottleneck/min propagation rather than repeated confidence multiplication.
- incompatible-flow regions remain barriers; trust may not jump a strong displacement discontinuity.
- valid component propagation must not decay toward zero merely because the recoverable clipped core is large.

OWNERSHIP REGRESSIONS
- measurable NORMAL ordinary rejection remains separate from censored/effective-loss SHORT reconstruction.
- common Sabre/RBF accumulator remains the only production fusion tunnel.
- common 3x3 CFA source-headroom protection remains the final physical clipping veto.
- one scalar confidence controls the complete RGB observation.
- old private SHORT masks/means/accumulators/fuses/late compositors remain dormant.
- DNG and true-2x high-frequency detail remain NORMAL-only.
- normal Motion LONG and Night ownership remain unchanged.
- tone/Resolve/VGN ownership remains unchanged in 26607.

SEMANTIC-STATE REGRESSION
Captured, scheduled, confidence-field-generated, accumulator-eligible, accumulator-contributed, Resolve-effective, target-recovered and output-preserved are distinct states. An earlier state never proves a later state.

26607 directly measures target accumulator eligibility after the applicable active rescue/source-validity/final-weight prerequisites. It must not describe that measurement as a directly observed accumulator delta. Direct accumulator contribution and Resolve effectiveness remain NOT_DIRECTLY_MEASURED unless a future implementation actually measures them. Final output preservation requires device-image validation.
