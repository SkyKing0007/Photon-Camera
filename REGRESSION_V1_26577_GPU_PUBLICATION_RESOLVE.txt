26577 GPU PUBLICATION RESOLVE PERMANENT REGRESSION CONTRACT

Authority failure reproduced on successful 26576 runtime:
- shot 2 Super Res shutter/processing frozen true, outputScale=2.0.
- true2x reconstruction succeeded on GPU at 8192x6144 in 9984 ms.
- phaseMean=2.9809031, phaseP10=2.0, genuine 26573 cross-frame proof active.
- publication reported gpuEligible=1 gpuUsed=0 gpuAttemptMs=475.116 gpuFallbackReason=gpu_failed.
- CPU publication then consumed streamMs=27405.605 and save/publish 28671 ms.
- final JPEG dimensions were 6144x8192 after rotation.

Permanent applicable requirements:
1. The successful 26571 two-PBO explicit-fence GPU publication path remains first choice.
2. A failure of only the explicit fence/resolve transport MUST try a GPU map-synchronized PBO compatibility tier before CPU rendering.
3. The compatibility tier MUST use the identical 26571 publication compute/pixel math, same bounded 256-row bands, same two PBO allocations, same Display-P3 4:4:4 base JPEG, and same 1:1 gain map.
4. glMapBufferRange may be the synchronization point in the compatibility tier, matching the already-proven Sabre PBO behavior.
5. glUnmapBuffer result MUST be checked; map/unmap/bind/unbind/fence/dispatch failures MUST have a specific reason.
6. Generic gpu_failed is forbidden as a publication fallback reason.
7. CPU publication remains the exact successful 26570 final correctness fallback only after both GPU tiers fail.
8. No Super Res reconstruction, alignment, max8/top2 evidence, 26573 proof, 26574 refinement, Sabre/VGN RGB/chroma ownership, DNG, Night, or UHDR ownership changes are permitted by this correction.
9. app/build/** and app/.cxx/** never count as runtime-source changes; repository-only module scaffolding absent from compiled-candidate authority never counts; unexpected app/src/** remains fatal.
