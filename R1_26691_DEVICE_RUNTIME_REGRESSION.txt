PHOTON 26691 R1 — DEVICE RUNTIME CAPTURE/HISTOGRAM REGRESSION

Observed on successful Actions build 26690 after clean install on Xiaomi 15 Ultra:
- ordinary Photo mode starts and behaves normally;
- Spektra mode enters/exits normally and its Vulkan RAW viewfinder renders correctly;
- Spektra shutter reaches the Iris/Spektra delegate;
- controller remains preview=STREAMING and capture=IDLE;
- no STILL_CONFIGURING/STILL_CAPTURING transition, RAW still delivery, processing, or saved JPEG occurs.

Exact capture blocker in successful 26690 compiled candidate:
- RawVulkanPreviewController.captureStill() snapshots preview state before Camera2 still submission;
- it used requireNotNull(latestLensShadingMap), rejecting the capture when no valid preview LSC had arrived;
- downstream RawColorShaderParameters/VulkanRenderer capture path already accepts nullable LSC;
- Camera2RawSession already capability-checks STATISTICS_INFO_AVAILABLE_LENS_SHADING_MAP_MODES and requests ON only when advertised.

Permanent 26691 capture contract:
1. absence of cached preview LSC must never reject the shutter before Camera2 still submission;
2. still-frame metadata LSC is preferred when valid;
3. a valid same-route cached preview LSC may be used as fallback;
4. if neither exists, capture continues with null LSC;
5. route/session changes retain existing cache invalidation, preventing cross-lens/cross-route reuse;
6. critical markers cover capture acceptance, still RAW arrival + LSC source, RCD success, JPEG save, and capture errors.

Spektra histogram regression:
- existing Iris live histogram reads the normal Iris GL preview, which is deliberately hidden while Spektra's dedicated Vulkan viewfinder is presented;
- therefore the shared Iris histogram must not be repurposed or modified for Spektra;
- the exact hosted Unspektrawesome renderer already exposes VulkanRenderer.histogramPixels(scale), backed by the native ViewfinderHistogram path and returning the native 128x128 rendered histogram image.

Permanent 26691 histogram contract:
1. Spektra uses only the active Spektra renderer/native histogram path;
2. histogram is off by default on Spektra entry and requires the Spektra-only top control to enable it, matching the supplied Unspektrawesome reference behavior;
3. the Spektra histogram worker runs only while Spektra is active + enabled and never queues overlapping polls;
4. only exact 128x128 / 16384-pixel native results are displayed;
5. leaving Spektra stops the worker, resets the Spektra toggle off, and restores the unchanged Iris histogram presentation path;
6. IrisLiveHistogramView and its normal-mode rendering behavior remain byte-identical to successful 26690.

Protected successful-26690 behavior:
- startup JNI containment;
- normal Photo mode;
- Spektra entry/exit and current viewfinder rendering;
- exact Unspektrawesome 1.1.2 native library;
- native/CMake/shader universe;
- non-Spektra Iris histogram behavior.
