diff --git a/app/src/main/assets/shaders/preview/main_fs.glsl b/app/src/main/assets/shaders/preview/main_fs.glsl
index 1259b675ca350a82520a33b6028d82c563dd6cf4..5468b55256930573b310636e1e7a5c66e0ca9816 100644
--- a/app/src/main/assets/shaders/preview/main_fs.glsl
+++ b/app/src/main/assets/shaders/preview/main_fs.glsl
@@ -34,20 +34,25 @@ void main() {
     if(enablePeak)
         color = color + dc*32.0*diff*w;
 
-    /* IRIS_26712_FULL_INVERSE_HAL_PREVIEW_PRESENTATION
-     * 26708 remains the visible authority. Iris may physically move the shared sensor exposure
-     * only while a composition is stationary, but that capture-domain offset must be invisible.
-     * Apply the exact frame-owned inverse exposure as a true linear multiplier, then encode/clamp.
-     * This intentionally allows the viewfinder highlights to clip exactly as the HAL-looking
-     * baseline would; protected RAW headroom remains capture data and is not advertised visually.
+    /* IRIS_26662_FRAME_MATCHED_PREVIEW_PRESENTATION
+     * Preserve exact 26660 preview bytes when no RAW protection is active. When protection is
+     * active, restore body brightness with a white-anchored rational exposure map instead of the
+     * 26661 full linear multiplier. The map is monotone, maps display white to display white, and
+     * rescales RGB uniformly, so it cannot create the blown preview highlights seen in 26661.
      */
-    float iris26662Gain = clamp(iris26662ReferencePreviewGain, 0.3535534, 2.8284271);
-    if (abs(iris26662Gain - 1.0) > 0.0001) {
+    float iris26662Gain = max(iris26662ReferencePreviewGain, 1.0);
+    if (iris26662Gain > 1.0001) {
         vec3 encoded = clamp(color.rgb, vec3(0.0), vec3(1.0));
         vec3 lowLinear = encoded / 12.92;
         vec3 highLinear = pow((encoded + 0.055) / 1.055, vec3(2.4));
         vec3 linearRgb = mix(lowLinear, highLinear, step(vec3(0.04045), encoded));
-        linearRgb = max(linearRgb, vec3(0.0)) * iris26662Gain;
+        float y = dot(linearRgb, vec3(0.2126, 0.7152, 0.0722));
+        float guide = max(y, max(linearRgb.r, max(linearRgb.g, linearRgb.b)));
+        if (guide > 1.0e-6) {
+            float mappedGuide = iris26662Gain * guide
+                    / (1.0 + (iris26662Gain - 1.0) * guide);
+            linearRgb *= mappedGuide / guide;
+        }
         vec3 lowEncoded = 12.92 * linearRgb;
         vec3 highEncoded = 1.055 * pow(max(linearRgb, vec3(0.0)), vec3(1.0 / 2.4)) - 0.055;
         color.rgb = clamp(mix(lowEncoded, highEncoded, step(vec3(0.0031308), linearRgb)),
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index 1ba2811eec174bd3a27c926eb1f118f975bdd167..8de1aeee6440e5eaa8af70b454e4b0ae8f664f1a 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -1823,16 +1823,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
      * not evidence for zero protection: return NaN so the renderer holds the last confirmed actual
      * result until an exact timestamp is available. Capture exposure ownership is unchanged. */
     public float getMotion26663ReferencePreviewProtectionEv(long sensorTimestampNs) {
-        if (!isZslMode()) return 0.0f;
-        if (sensorTimestampNs <= 0L) return Float.NaN;
-        synchronized (mMotion26662PreviewResultLock) {
-            for (int i = 0; i < MOTION_26662_PREVIEW_RESULT_HISTORY; i++) {
-                if (mMotion26662PreviewResultTimestampNs[i] == sensorTimestampNs) {
-                    return mMotion26662PreviewResultProtectionEv[i];
-                }
-            }
-        }
-        return Float.NaN;
+        /* IRIS_26713_PREVIEW_EXPOSURE_ISOLATION
+         * Iris capture exposure is forbidden from affecting the visible viewfinder. 26708-style
+         * HAL/System-AE presentation is therefore hard unity regardless of capture recipe state. */
+        return 0.0f;
     }
 
     /* IRIS_26496_SPATIALLY_PERSISTENT_HIGHLIGHT_TRIGGER
@@ -1868,12 +1862,108 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private volatile long mMotion26708PublishedTimestampNs = 0L, mMotion26708PublishedGeneration = -1L;
     private volatile double mMotion26708PublishedExposureEnergy = Double.NaN;
 
-    /* IRIS_26712_ONE_SHOT_HAL_REFERENCE_OWNER
-     * HAL/System AE owns visible navigation and the untouched scene baseline. After one complete
-     * RAW meter cycle on a stable composition, Iris computes exactly one signed capture target,
-     * switches the shared RAW+preview request directly to that manual shutter/ISO pair, and hides
-     * the physical offset through frame-exact presentation. No adjusted RAW can vote again.
-     * Intentional navigation releases back to HAL while movement is underway. */
+    private void motion26713CancelRecipe(@NonNull String reason) {
+        mMotion26713ObservationPending = false;
+        mMotion26713ObservationStartMs = 0L;
+        mMotion26713RecipeReady = false;
+        mMotion26713RecipeExposureNs = 0L;
+        mMotion26713RecipeIso = 0;
+        mMotion26713RecipeEnergy = Double.NaN;
+        mMotion26713RecipeOffsetEv = 0.0f;
+        mMotion26713RecipeBaselineEnergy = Double.NaN;
+        mMotion26713RecipeBaselineTimestampNs = 0L;
+        mMotion26713RecipeGeneration = -1L;
+        mMotion26713RecipeMotionFrames = 0;
+        mMotion26709HalBaselineReady = false;
+        mMotion26709ReferenceSettled = false;
+        mMotion26710DecisionIssued = false;
+        mMotion26710ManualReferenceActive = false;
+        mMotion26710TargetExposureNs = 0L;
+        mMotion26710TargetIso = 0;
+        mMotion26710TargetEnergy = Double.NaN;
+        mMotion26710TargetProtectionEv = 0.0f;
+        mMotion26712TargetPhysicalOffsetEv = 0.0f;
+        mMotion26709FrozenCaptureProtectionEv = 0.0f;
+        mMotion26709HalBaselineEnergy = Double.NaN;
+        mMotion26709HalBaselineExposureNs = 0L;
+        mMotion26709HalBaselineIso = 0;
+        mMotion26709LastConsumedMeterTimestampNs = 0L;
+        mMotion26710HalBaselineHighlightFraction = Float.NaN;
+        mMotion26710HalBaselineFloorFraction = Float.NaN;
+        mMotion26710HalBaselineShadowFraction = Float.NaN;
+        mMotion26710HalBaselineMeanSignal = Float.NaN;
+        mMotion26680StableFrames = 0;
+        mMotion26680StationaryFrames = 0;
+        clearMotion26708HighlightClassifier("26713-" + reason);
+        Log.i(TAG, "IRIS_26713_CAPTURE_RECIPE_INVALIDATED reason=" + reason
+                + " previewMutation=false halAeContinuous=true");
+    }
+
+    private boolean motion26713ManualSensorAvailable() {
+        if (mCameraCharacteristics == null) return false;
+        int[] caps = mCameraCharacteristics.get(
+                CameraCharacteristics.REQUEST_AVAILABLE_CAPABILITIES);
+        if (caps == null) return false;
+        for (int c : caps) {
+            if (c == CameraCharacteristics.REQUEST_AVAILABLE_CAPABILITIES_MANUAL_SENSOR) return true;
+        }
+        return false;
+    }
+
+    private boolean motion26713ResultMatchesTarget(@NonNull TotalCaptureResult result,
+            long targetExposureNs, int targetIso) {
+        Long exp = result.get(CaptureResult.SENSOR_EXPOSURE_TIME);
+        Integer iso = result.get(CaptureResult.SENSOR_SENSITIVITY);
+        if (exp == null || exp <= 0L || iso == null || iso <= 0
+                || targetExposureNs <= 0L || targetIso <= 0) return false;
+        double actual = (double) exp * (double) iso;
+        double target = (double) targetExposureNs * (double) targetIso;
+        return Math.abs(Math.log(actual / target) / Math.log(2.0))
+                <= MOTION_26486_EXPOSURE_HALF_WINDOW_EV;
+    }
+
+    private void motion26713ClearRecipeAfterPhysicalBatch() {
+        mMotion26713ObservationPending = false;
+        mMotion26713ObservationStartMs = 0L;
+        mMotion26713RecipeReady = false;
+        mMotion26713RecipeExposureNs = 0L;
+        mMotion26713RecipeIso = 0;
+        mMotion26713RecipeEnergy = Double.NaN;
+        mMotion26713RecipeOffsetEv = 0.0f;
+        mMotion26713RecipeBaselineEnergy = Double.NaN;
+        mMotion26713RecipeBaselineTimestampNs = 0L;
+        mMotion26713RecipeGeneration = -1L;
+        mMotion26713RecipeMotionFrames = 0;
+        mMotion26709HalBaselineReady = false;
+        mMotion26709ReferenceSettled = false;
+        mMotion26710DecisionIssued = false;
+        mMotion26710ManualReferenceActive = false;
+        mMotion26710TargetExposureNs = 0L;
+        mMotion26710TargetIso = 0;
+        mMotion26710TargetEnergy = Double.NaN;
+        mMotion26710TargetProtectionEv = 0.0f;
+        mMotion26712TargetPhysicalOffsetEv = 0.0f;
+        mMotion26709FrozenCaptureProtectionEv = 0.0f;
+        mMotion26709HalBaselineEnergy = Double.NaN;
+        mMotion26709HalBaselineExposureNs = 0L;
+        mMotion26709HalBaselineIso = 0;
+        mMotion26709LastConsumedMeterTimestampNs = 0L;
+        mMotion26710HalBaselineHighlightFraction = Float.NaN;
+        mMotion26710HalBaselineFloorFraction = Float.NaN;
+        mMotion26710HalBaselineShadowFraction = Float.NaN;
+        mMotion26710HalBaselineMeanSignal = Float.NaN;
+        mMotion26680StableFrames = 0;
+        mMotion26680StationaryFrames = 0;
+        clearMotion26708HighlightClassifier("26713-physical-batch-complete");
+        Log.i(TAG, "IRIS_26713_PHYSICAL_CAPTURE_COMPLETE"
+                + " previewWasNeverTakenOver=true halAeContinuous=true"
+                + " processingIndependent=true");
+    }
+
+    /* IRIS_26713_CAPTURE_DOMAIN_ONLY_EXPOSURE_OWNER
+     * HAL/System AE owns the repeating preview continuously. A stable HAL scene produces one cached
+     * signed NORMAL recipe; Iris never applies that recipe to repeating preview. The recipe is
+     * consumed only by RAW-only still-capture requests after shutter. */
     private static final float MOTION_26710_REFERENCE_HEADROOM_TARGET = 0.72f;
     private static final float MOTION_26710_HDR_MIN_PROTECTION_EV = 0.35f;
     private static final float MOTION_26710_CLIP_MIN_PROTECTION_EV = 0.70f;
@@ -1971,6 +2061,26 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private float mMotion26712LastMeanTileEv = 0.0f;
     private float mMotion26712TargetPhysicalOffsetEv = 0.0f;
 
+    /* IRIS_26713_CAPTURE_DOMAIN_ONLY_EXPOSURE_OWNER
+     * Live repeating preview is permanently HAL/System-AE owned. Iris only observes a stable HAL
+     * scene and caches a capture recipe. The recipe may never write AE/ISO/shutter or preview gain.
+     * At shutter, corrected NORMAL/LONG exposures are RAW-only still-capture transactions. */
+    private static final long MOTION_26713_OBSERVATION_TIMEOUT_MS = 1200L;
+    private static final float MOTION_26713_OBSERVATION_DRIFT_EV = 0.12f;
+    private static final float MOTION_26713_RECIPE_INVALIDATE_EV = 0.22f;
+    private static final int MOTION_26713_RECIPE_MOTION_FRAMES = 2;
+    private boolean mMotion26713ObservationPending = false;
+    private long mMotion26713ObservationStartMs = 0L;
+    private boolean mMotion26713RecipeReady = false;
+    private long mMotion26713RecipeExposureNs = 0L;
+    private int mMotion26713RecipeIso = 0;
+    private double mMotion26713RecipeEnergy = Double.NaN;
+    private float mMotion26713RecipeOffsetEv = 0.0f;
+    private double mMotion26713RecipeBaselineEnergy = Double.NaN;
+    private long mMotion26713RecipeBaselineTimestampNs = 0L;
+    private long mMotion26713RecipeGeneration = -1L;
+    private int mMotion26713RecipeMotionFrames = 0;
+
     private static final int MOTION_26496_MIN_HIGHLIGHT_SAMPLES = 2;
     private static final long MOTION_26496_HIGHLIGHT_EVIDENCE_HOLD_NS = 400_000_000L;
 
@@ -2865,6 +2975,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mMotion26712LastChangedTiles = 0;
         mMotion26712LastMeanTileEv = 0.0f;
         mMotion26712TargetPhysicalOffsetEv = 0.0f;
+        mMotion26713ObservationPending = false;
+        mMotion26713ObservationStartMs = 0L;
+        mMotion26713RecipeReady = false;
+        mMotion26713RecipeExposureNs = 0L;
+        mMotion26713RecipeIso = 0;
+        mMotion26713RecipeEnergy = Double.NaN;
+        mMotion26713RecipeOffsetEv = 0.0f;
+        mMotion26713RecipeBaselineEnergy = Double.NaN;
+        mMotion26713RecipeBaselineTimestampNs = 0L;
+        mMotion26713RecipeGeneration = -1L;
+        mMotion26713RecipeMotionFrames = 0;
         /* Post-capture HAL restore is a separate live-session transaction and is not cleared here. */
         /* IRIS_26705_NORMAL_AE_SHORT_HEADROOM_OWNER
          * No 26701 transaction survives: NORMAL compensation is never changed by HDR logic. */
@@ -3243,29 +3364,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
      * Adjusted RAWs are capture evidence only and can never rewrite NORMAL. Local subject/TV/flicker
      * motion cannot release the epoch; semantic navigation releases to HAL during movement. */
     private void updateMotion26680StablePreviewAuthority(@NonNull TotalCaptureResult result) {
-        /* IRIS_26711_POST_CAPTURE_HAL_RESTORE_CALLBACK
-         * Processing may already be running; HAL restoration belongs to live preview and must not
-         * wait for image processing to finish. */
-        if (isZslMode() && motion26711HandlePostCaptureHalRestore(result)) return;
-        /* IRIS_26712_PROCESSING_PREVIEW_STAYS_HAL
-         * Once the immutable physical batch exists, processing has zero live-camera ownership.
-         * Keep publishing zero presentation offset for HAL preview results even while the image
-         * pipeline is busy, so the renderer cannot retain the prior hidden capture compensation. */
-        if (isZslMode() && isProcessing && !mMotion26710ManualReferenceActive) {
-            Long processingPreviewTs = result.get(CaptureResult.SENSOR_TIMESTAMP);
-            Integer processingPreviewAeMode = result.get(CaptureResult.CONTROL_AE_MODE);
-            if (processingPreviewTs != null && processingPreviewTs > 0L
-                    && processingPreviewAeMode != null
-                    && processingPreviewAeMode != CaptureResult.CONTROL_AE_MODE_OFF) {
-                recordMotion26662PreviewProtection(processingPreviewTs, 0.0f);
-            }
-            return;
-        }
-        if (!isZslMode() || mZslCapturing || burst || isProcessing
+        /* IRIS_26713_CAPTURE_RECIPE_OBSERVER
+         * The repeating preview request is never modified here. HAL/System AE/AWB remains the
+         * continuous visible owner exactly as in 26708. Iris only caches a RAW still-capture recipe. */
+        if (!isZslMode() || mZslCapturing || burst
                 || mPreviewRequestBuilder == null || mCaptureSession == null
-                || mCameraCharacteristics == null) {
-            return;
-        }
+                || mCameraCharacteristics == null) return;
         if (mMotion26680LatchGeneration != mIris26548CameraHealthGeneration) {
             resetMotion26680StablePreviewAuthority("generation-change");
         }
@@ -3276,16 +3380,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         Long exposureNs = result.get(CaptureResult.SENSOR_EXPOSURE_TIME);
         Integer iso = result.get(CaptureResult.SENSOR_SENSITIVITY);
         Long sensorTs = result.get(CaptureResult.SENSOR_TIMESTAMP);
-        final boolean iris26710ManualResult = mMotion26710ManualReferenceActive
-                && aeMode != null && aeMode == CaptureResult.CONTROL_AE_MODE_OFF;
-        if (aeMode == null || (aeMode == CaptureResult.CONTROL_AE_MODE_OFF && !iris26710ManualResult)
+        if (aeMode == null || aeMode == CaptureResult.CONTROL_AE_MODE_OFF
                 || exposureNs == null || exposureNs <= 0L || iso == null || iso <= 0
-                || sensorTs == null || sensorTs <= 0L) {
-            return;
-        }
+                || sensorTs == null || sensorTs <= 0L) return;
+
+        /* Hard preview isolation: no capture-domain exposure ever reaches presentation. */
+        recordMotion26662PreviewProtection(sensorTs, 0.0f);
+        if (isProcessing) return;
+
         final double exposureEnergy = (double) exposureNs * (double) iso;
+        final double priorEnergy = mMotion26680LastExposureEnergy;
         mMotion26680LastExposureEnergy = exposureEnergy;
-
         int shakiness = 0;
         try {
             if (PhotonCamera.getGyro() != null) {
@@ -3293,251 +3398,59 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             }
         } catch (Exception ignored) {}
 
-        android.util.Range<Integer> aeRange = mCameraCharacteristics.get(
-                CameraCharacteristics.CONTROL_AE_COMPENSATION_RANGE);
-        android.util.Rational aeStep = mCameraCharacteristics.get(
-                CameraCharacteristics.CONTROL_AE_COMPENSATION_STEP);
-        final boolean adaptiveAeAvailable = aeRange != null && aeStep != null
-                && aeStep.floatValue() > 0.0f;
-        Integer resultCompObj = result.get(CaptureResult.CONTROL_AE_EXPOSURE_COMPENSATION);
-        Integer builderCompObj = mPreviewRequestBuilder.get(
-                CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION);
-        int resultCompSteps = resultCompObj != null ? resultCompObj
-                : (builderCompObj != null ? builderCompObj : 0);
-
-        /* Preserve 26709's proven timestamp-exact presentation compensation, but it now follows
-         * one immutable physical target instead of a feedback search. */
-        if (mMotion26709ReleasePending) {
-            recordMotion26662PreviewProtection(sensorTs, 0.0f);
-        } else if (mMotion26709HalBaselineReady && Double.isFinite(mMotion26709HalBaselineEnergy)
-                && mMotion26709HalBaselineEnergy > 0.0 && exposureEnergy > 0.0) {
-            float physicalOffsetEv = (float)(Math.log(
-                    exposureEnergy / mMotion26709HalBaselineEnergy) / Math.log(2.0));
-            recordMotion26662PreviewProtection(sensorTs, -physicalOffsetEv);
-        } else {
-            recordMotion26662PreviewProtection(sensorTs, 0.0f);
-        }
-
-        /* A physical reframe returned AE to HAL. Ignore trailing manual results and begin a fresh
-         * untouched HAL epoch only after the new repeating result reports AE ON again. */
-        if (mMotion26709ReleasePending) {
-            if (aeMode == CaptureResult.CONTROL_AE_MODE_OFF
-                    || resultCompSteps != mMotion26709HalBaseCompSteps) return;
-            mMotion26709ReleasePending = false;
-            mMotion26709HalBaselineReady = false;
-            mMotion26709ReferenceSettled = false;
-            mMotion26709AeStepEv = 0.0f;
-            mMotion26709HalBaselineEnergy = Double.NaN;
-            mMotion26709HalBaselineExposureNs = 0L;
-            mMotion26709HalBaselineIso = 0;
-            mMotion26709LastConsumedMeterTimestampNs = 0L;
-            mMotion26710DecisionIssued = false;
-            mMotion26710ManualReferenceActive = false;
-            mMotion26710TargetExposureNs = 0L;
-            mMotion26710TargetIso = 0;
-            mMotion26710TargetEnergy = Double.NaN;
-            mMotion26710TargetProtectionEv = 0.0f;
-            mMotion26710TargetObserveFrames = 0;
-            mMotion26680StableFrames = 0;
-            mMotion26680StationaryFrames = 0;
-            mMotion26711PreviewTransitionHold = false;
-            Log.i(TAG, "IRIS_26711_REFERENCE_RELEASE_CONFIRMED compSteps=" + resultCompSteps
-                    + " aeMode=" + aeMode
-                    + " newUntouchedHalEpoch=true exposureFeedbackForbidden=true");
+        if (mMotion26713RecipeReady) {
+            if (shakiness > MOTION_26680_REFRAME_GYRO_THRESHOLD) {
+                mMotion26713RecipeMotionFrames++;
+            } else {
+                mMotion26713RecipeMotionFrames = 0;
+            }
+            double baselineDriftEv = Double.isFinite(mMotion26713RecipeBaselineEnergy)
+                    && mMotion26713RecipeBaselineEnergy > 0.0
+                    ? Math.abs(Math.log(exposureEnergy / mMotion26713RecipeBaselineEnergy)
+                        / Math.log(2.0)) : Double.POSITIVE_INFINITY;
+            if (mMotion26713RecipeMotionFrames >= MOTION_26713_RECIPE_MOTION_FRAMES
+                    || baselineDriftEv > MOTION_26713_RECIPE_INVALIDATE_EV) {
+                motion26713CancelRecipe(mMotion26713RecipeMotionFrames
+                        >= MOTION_26713_RECIPE_MOTION_FRAMES
+                        ? "camera-navigation" : "hal-scene-energy-changed");
+            }
             return;
         }
 
-        if (mMotion26680PreviewLatched) {
-            final boolean iris26712Navigation = motion26712NavigationConfirmed();
-            if (iris26712Navigation) {
-                try {
-                    /* IRIS_26712_HAL_NAVIGATION_RELEASE
-                     * The viewfinder must behave like 26708 while the user navigates. Release the
-                     * hidden capture exposure while the composition is moving, not after it settles. */
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_MODE,
-                            CaptureRequest.CONTROL_AE_MODE_ON);
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION, 0);
-                    if (mMotion26680AwbLockApplied) {
-                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
-                    }
-                    mMotion26710ManualReferenceActive = false;
-                    mMotion26680PreviewLatched = false;
-                    mMotion26680AeLockApplied = false;
-                    mMotion26680AwbLockApplied = false;
-                    mMotion26680StableFrames = 0;
-                    mMotion26680StationaryFrames = 0;
-                    mMotion26680ReframeMotionFrames = 0;
-                    mMotion26709HalBaselineReady = false;
-                    mMotion26709ReferenceSettled = false;
-                    mMotion26709ReleasePending = false;
-                    mMotion26709AeStepEv = 0.0f;
-                    mMotion26709HalBaselineEnergy = Double.NaN;
-                    mMotion26709HalBaselineExposureNs = 0L;
-                    mMotion26709HalBaselineIso = 0;
-                    mMotion26709LastConsumedMeterTimestampNs = 0L;
-                    mMotion26710DecisionIssued = false;
-                    mMotion26710TargetExposureNs = 0L;
-                    mMotion26710TargetIso = 0;
-                    mMotion26710TargetEnergy = Double.NaN;
-                    mMotion26710TargetProtectionEv = 0.0f;
-                    mMotion26712TargetPhysicalOffsetEv = 0.0f;
-                    mMotion26710TargetObserveFrames = 0;
-                    mMotion26711LockPoseValid = false;
-                    mMotion26711LockedSceneSignature = null;
-                    mMotion26711ReframeSettledFrames = 0;
-                    mMotion26711SceneChangeFrames = 0;
-                    mMotion26712NavigationFrames = 0;
-                    mMotion26712NavigationStartMs = 0L;
-                    mMotion26712WholeFrameSamples = 0;
-                    mMotion26711PreviewTransitionHold = false;
-                    clearMotion26708HighlightClassifier("26712-navigation-release");
-                    rebuildPreviewBuilder();
-                    Log.i(TAG, "IRIS_26712_HAL_NAVIGATION_RELEASE generation="
-                            + mIris26548CameraHealthGeneration
-                            + " restoreHalAeNow=true compensationSteps=0"
-                            + " waitForNewSceneSettle=false previewHold=false"
-                            + " changedTiles=" + mMotion26712LastChangedTiles
-                            + " meanTileEv=" + mMotion26712LastMeanTileEv
-                            + " handShakeCannotUnlock=true returningHandJerkCannotUnlock=true"
-                            + " subjectMotionCannotUnlock=true tvContentCannotUnlock=true"
-                            + " viewfinderAuthority=HAL_26708");
-                } catch (IllegalArgumentException | IllegalStateException e) {
-                    Log.w(TAG, "IRIS_26712 navigation release skipped: "
-                            + e.getClass().getSimpleName());
-                }
-                return;
-            }
-
-            /* User/UI EV is a new HAL baseline request. It may terminate the epoch, but RAW/image
-             * content never can. */
-            if (builderCompObj != null && builderCompObj != mMotion26709HalBaseCompSteps) {
-                try {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_MODE,
-                            CaptureRequest.CONTROL_AE_MODE_ON);
-                    if (mMotion26680AwbLockApplied) {
-                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
-                    }
-                    mMotion26710ManualReferenceActive = false;
-                    mMotion26680PreviewLatched = false;
-                    mMotion26680AeLockApplied = false;
-                    mMotion26680AwbLockApplied = false;
-                    mMotion26680StableFrames = 0;
-                    mMotion26680StationaryFrames = 0;
-                    mMotion26709HalBaselineReady = false;
-                    mMotion26709ReferenceSettled = false;
-                    mMotion26710DecisionIssued = false;
-                    mMotion26712TargetPhysicalOffsetEv = 0.0f;
-                    mMotion26709LastConsumedMeterTimestampNs = 0L;
-                    clearMotion26708HighlightClassifier("26710-user-ev-change");
-                    rebuildPreviewBuilder();
-                    Log.i(TAG, "IRIS_26710_USER_EV_REBASE requestedSteps=" + builderCompObj
-                            + " priorHalBaseSteps=" + mMotion26709HalBaseCompSteps
-                            + " userAuthority=true");
-                } catch (IllegalArgumentException | IllegalStateException ignored) {}
-                return;
-            }
-
-            /* Once the one-shot target has been issued, no RAW statistic is allowed to write
-             * exposure again. We only observe whether the exact manual target has reached sensor. */
-            if (mMotion26710DecisionIssued) {
-                if (mMotion26710ManualReferenceActive) {
-                    mMotion26710TargetObserveFrames++;
-                    double mismatchEv = Double.isFinite(mMotion26710TargetEnergy)
-                            && mMotion26710TargetEnergy > 0.0 && exposureEnergy > 0.0
-                            ? Math.abs(Math.log(exposureEnergy / mMotion26710TargetEnergy)
-                                / Math.log(2.0)) : Double.POSITIVE_INFINITY;
-                    boolean builderExact = motion26711LockedManualRequestMatchesBuilder();
-                    boolean resultExact = iris26710ManualResult
-                            && mismatchEv <= MOTION_26710_TARGET_OBSERVED_TOLERANCE_EV;
-                    if (!mMotion26709ReferenceSettled) {
-                        if (resultExact) {
-                            mMotion26709ReferenceSettled = true;
-                            mMotion26711PreviewTransitionHold = false;
-                            motion26711CaptureLockPoseAndScene();
-                            float observedPhysicalOffsetEv = (float)Math.max(
-                                    -MOTION_26709_MAX_RELATIVE_EV,
-                                    Math.min(MOTION_26712_BRIGHTEN_MAX_EV,
-                                        Math.log(exposureEnergy / mMotion26709HalBaselineEnergy)
-                                            / Math.log(2.0)));
-                            float observedProtectionEv = Math.max(0.0f,
-                                    -observedPhysicalOffsetEv);
-                            Log.i(TAG, "IRIS_26712_REFERENCE_LOCKED"
-                                    + " exposureNs=" + exposureNs + " iso=" + iso
-                                    + " targetExposureNs=" + mMotion26710TargetExposureNs
-                                    + " targetIso=" + mMotion26710TargetIso
-                                    + " targetPhysicalOffsetEv=" + mMotion26712TargetPhysicalOffsetEv
-                                    + " targetProtectionEv=" + mMotion26710TargetProtectionEv
-                                    + " observedPhysicalOffsetEv=" + observedPhysicalOffsetEv
-                                    + " observedProtectionEv=" + observedProtectionEv
-                                    + " mismatchEv=" + mismatchEv
-                                    + " manualAeOff=true oneDecisionPerEpoch=true"
-                                    + " continuousEnforcement=true transitionHidden=true"
-                                    + " viewfinderAuthority=HAL_26708");
-                        } else if (mMotion26710TargetObserveFrames
-                                >= MOTION_26710_TARGET_OBSERVE_MAX_FRAMES || !builderExact) {
-                            mMotion26710TargetObserveFrames = 0;
-                            motion26711ReassertLockedManualReference(
-                                    builderExact ? "TARGET_NOT_OBSERVED" : "BUILDER_OWNER_LOST");
-                        }
-                    } else {
-                        if (!resultExact || !builderExact) {
-                            mMotion26711LockedResultMismatchFrames++;
-                            if (mMotion26711LockedResultMismatchFrames
-                                    >= MOTION_26711_LOCK_REASSERT_FRAMES) {
-                                mMotion26711LockedResultMismatchFrames = 0;
-                                motion26711ReassertLockedManualReference(
-                                        builderExact ? "RESULT_DRIFT" : "BUILDER_OWNER_LOST");
-                            }
-                        } else {
-                            mMotion26711LockedResultMismatchFrames = 0;
-                            if (mMotion26711PreviewTransitionHold) {
-                                mMotion26711PreviewTransitionHold = false;
-                                Log.i(TAG, "IRIS_26711_LOCK_REASSERT_CONFIRMED"
-                                        + " exposureNs=" + exposureNs + " iso=" + iso
-                                        + " sameFrozenTarget=true previewReleased=true");
-                            }
-                        }
-                    }
-                } else if (mMotion26709ReferenceSettled) {
-                    /* A zero-protection epoch is still immutable: keep HAL AE locked until a
-                     * proven scene epoch change or physical capture completion. */
-                    Boolean resultAeLock = result.get(CaptureResult.CONTROL_AE_LOCK);
-                    Boolean builderAeLock = mPreviewRequestBuilder.get(CaptureRequest.CONTROL_AE_LOCK);
-                    if (!Boolean.TRUE.equals(resultAeLock) || !Boolean.TRUE.equals(builderAeLock)) {
-                        try {
-                            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-                            rebuildPreviewBuilder();
-                            Log.w(TAG, "IRIS_26711_HAL_BASELINE_LOCK_REASSERT"
-                                    + " exposureRecompute=false sameHalEpoch=true");
-                        } catch (IllegalArgumentException | IllegalStateException ignored) {}
-                    }
-                }
+        if (mMotion26713ObservationPending) {
+            final long nowMs = android.os.SystemClock.elapsedRealtime();
+            final long ageMs = nowMs - mMotion26713ObservationStartMs;
+            double baselineDriftEv = Double.isFinite(mMotion26709HalBaselineEnergy)
+                    && mMotion26709HalBaselineEnergy > 0.0
+                    ? Math.abs(Math.log(exposureEnergy / mMotion26709HalBaselineEnergy)
+                        / Math.log(2.0)) : Double.POSITIVE_INFINITY;
+            if (ageMs > MOTION_26713_OBSERVATION_TIMEOUT_MS
+                    || shakiness > MOTION_26680_REFRAME_GYRO_THRESHOLD
+                    || baselineDriftEv > MOTION_26713_OBSERVATION_DRIFT_EV) {
+                motion26713CancelRecipe(ageMs > MOTION_26713_OBSERVATION_TIMEOUT_MS
+                        ? "observation-timeout" : (shakiness > MOTION_26680_REFRAME_GYRO_THRESHOLD
+                        ? "observation-camera-motion" : "observation-hal-drift"));
                 return;
             }
-
             if (mMotion26708PublishedTimestampNs <= 0L
-                    || mMotion26708PublishedGeneration != mIris26548CameraHealthGeneration) return;
-            long meterAgeNs = Math.abs(sensorTs - mMotion26708PublishedTimestampNs);
-            if (meterAgeNs > MOTION_26709_METER_FRESH_NS
+                    || mMotion26708PublishedGeneration != mIris26548CameraHealthGeneration
                     || !Float.isFinite(mMotion26709PublishedP995)
                     || mMotion26709PublishedP995 <= 0.0f) return;
-            if (Double.isFinite(mMotion26708PublishedExposureEnergy)
-                    && mMotion26708PublishedExposureEnergy > 0.0) {
-                double meterEvMismatch = Math.abs(Math.log(
-                        mMotion26709HalBaselineEnergy / mMotion26708PublishedExposureEnergy)
-                        / Math.log(2.0));
-                if (meterEvMismatch > 0.08) return;
-            }
+            long meterAgeNs = Math.abs(sensorTs - mMotion26708PublishedTimestampNs);
+            if (meterAgeNs > MOTION_26709_METER_FRESH_NS
+                    || !Double.isFinite(mMotion26708PublishedExposureEnergy)
+                    || mMotion26708PublishedExposureEnergy <= 0.0) return;
+            double meterEvMismatch = Math.abs(Math.log(
+                    mMotion26709HalBaselineEnergy / mMotion26708PublishedExposureEnergy)
+                    / Math.log(2.0));
+            if (meterEvMismatch > 0.08) return;
 
-            /* Freeze every scene statistic used by NORMAL and LONG from the untouched HAL epoch.
-             * These values never update the locked exposure after this point. */
             mMotion26709LastConsumedMeterTimestampNs = mMotion26708PublishedTimestampNs;
             mMotion26710HalBaselineHighlightFraction = mMotion26380RawHighlightFraction;
             mMotion26710HalBaselineFloorFraction = mMotion26709PublishedFloorFraction;
             mMotion26710HalBaselineShadowFraction = mMotion26709PublishedShadowFraction;
             mMotion26710HalBaselineMeanSignal = mMotion26709PublishedMeanSignal;
+
             final boolean broadOrMixedHdr = mMotion26709PublishedBroadVotes > 0
                     || mMotion26709PublishedMixedVotes > 0;
             final boolean coherentHighlight = mMotion26709PublishedCoherentCells > 0;
@@ -3576,10 +3489,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 requestedProtectionEv = 0.0f;
             }
 
-            /* IRIS_26712_ONE_SHOT_SIGNED_SCENE_CORRECTION
-             * Still one immutable vote from the untouched HAL epoch. Highlight pressure may move
-             * NORMAL darker. Only a clearly dark, highlight-safe scene may move NORMAL brighter;
-             * no post-adjustment RAW is allowed to vote again. */
             float requestedPhysicalOffsetEv = -requestedProtectionEv;
             if (requestedProtectionEv <= 0.0f
                     && Float.isFinite(mMotion26710HalBaselineFloorFraction)
@@ -3602,186 +3511,101 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     requestedPhysicalOffsetEv = requestedBrightenEv;
                 }
             }
-            mMotion26712TargetPhysicalOffsetEv = requestedPhysicalOffsetEv;
-            mMotion26710TargetProtectionEv = Math.max(0.0f, -requestedPhysicalOffsetEv);
-            mMotion26710DecisionIssued = true;
-
-            if (Math.abs(requestedPhysicalOffsetEv) < MOTION_26710_DECISION_DEADBAND_EV) {
-                try {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-                    mMotion26680AeLockApplied = true;
-                    mMotion26709ReferenceSettled = true;
-                    motion26711CaptureLockPoseAndScene();
-                    rebuildPreviewBuilder();
-                    Log.i(TAG, "IRIS_26712_REFERENCE_LOCKED"
-                            + " exposureNs=" + exposureNs + " iso=" + iso
-                            + " targetPhysicalOffsetEv=0.0 decision=HAL_BASELINE"
-                            + " oneDecisionPerEpoch=true postAdjustmentRawCannotRewriteExposure=true"
-                            + " navigationReleaseOnly=true viewfinderAuthority=HAL_26708");
-                } catch (IllegalArgumentException | IllegalStateException e) {
-                    Log.w(TAG, "IRIS_26712 HAL baseline lock skipped: "
-                            + e.getClass().getSimpleName());
-                }
-                return;
-            }
 
-            boolean manualSensor = false;
-            int[] caps = mCameraCharacteristics.get(
-                    CameraCharacteristics.REQUEST_AVAILABLE_CAPABILITIES);
-            if (caps != null) for (int c : caps) {
-                if (c == CameraCharacteristics.REQUEST_AVAILABLE_CAPABILITIES_MANUAL_SENSOR) {
-                    manualSensor = true;
-                    break;
-                }
+            if (!motion26713ManualSensorAvailable()) {
+                requestedPhysicalOffsetEv = 0.0f;
             }
             android.util.Range<Long> exposureRange = mCameraCharacteristics.get(
                     CameraCharacteristics.SENSOR_INFO_EXPOSURE_TIME_RANGE);
             android.util.Range<Integer> sensitivityRange = mCameraCharacteristics.get(
                     CameraCharacteristics.SENSOR_INFO_SENSITIVITY_RANGE);
-            if (!manualSensor || exposureRange == null || sensitivityRange == null) {
-                /* Zero-pump invariant wins over speculative AE-compensation fallback. */
-                try {
-                    mMotion26710TargetProtectionEv = 0.0f;
-                    mMotion26712TargetPhysicalOffsetEv = 0.0f;
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-                    mMotion26680AeLockApplied = true;
-                    mMotion26709ReferenceSettled = true;
-                    rebuildPreviewBuilder();
-                    Log.w(TAG, "IRIS_26710_MANUAL_REFERENCE_UNAVAILABLE"
-                            + " fallback=LOCK_HAL_BASELINE zeroPumpInvariant=true"
-                            + " requestedPhysicalOffsetEv=" + requestedPhysicalOffsetEv);
-                } catch (IllegalArgumentException | IllegalStateException ignored) {}
-                return;
+            if (exposureRange == null || sensitivityRange == null) {
+                requestedPhysicalOffsetEv = 0.0f;
             }
 
-            final double targetEnergy = mMotion26709HalBaselineEnergy
-                    * Math.pow(2.0, requestedPhysicalOffsetEv);
-            long targetExposureNs;
-            int targetIso;
-            boolean iris26711HalShutterPreserved;
-            if (requestedPhysicalOffsetEv < 0.0f) {
-                /* Preserve HAL anti-banding shutter and remove exposure through ISO first. */
-                targetExposureNs = Math.max(exposureRange.getLower(),
-                        Math.min(exposureRange.getUpper(), mMotion26709HalBaselineExposureNs));
-                targetIso = (int)Math.round(targetEnergy / Math.max(1.0, targetExposureNs));
-                targetIso = Math.max(sensitivityRange.getLower(),
-                        Math.min(Math.min(sensitivityRange.getUpper(), mMotion26709HalBaselineIso),
-                                targetIso));
-                double preliminaryEnergy = (double)targetExposureNs * (double)targetIso;
-                iris26711HalShutterPreserved = targetIso > sensitivityRange.getLower()
-                        || preliminaryEnergy <= targetEnergy * 1.015;
-                if (preliminaryEnergy > targetEnergy * 1.015) {
-                    targetIso = sensitivityRange.getLower();
-                    targetExposureNs = Math.round(targetEnergy / Math.max(1, targetIso));
+            long targetExposureNs = mMotion26709HalBaselineExposureNs;
+            int targetIso = mMotion26709HalBaselineIso;
+            if (Math.abs(requestedPhysicalOffsetEv) >= MOTION_26710_DECISION_DEADBAND_EV
+                    && exposureRange != null && sensitivityRange != null) {
+                final double targetEnergy = mMotion26709HalBaselineEnergy
+                        * Math.pow(2.0, requestedPhysicalOffsetEv);
+                if (requestedPhysicalOffsetEv < 0.0f) {
                     targetExposureNs = Math.max(exposureRange.getLower(),
-                            Math.min(Math.min(exposureRange.getUpper(),
-                                    mMotion26709HalBaselineExposureNs), targetExposureNs));
-                    iris26711HalShutterPreserved =
-                            targetExposureNs == mMotion26709HalBaselineExposureNs;
+                            Math.min(exposureRange.getUpper(), mMotion26709HalBaselineExposureNs));
+                    targetIso = (int)Math.round(targetEnergy / Math.max(1.0, targetExposureNs));
+                    targetIso = Math.max(sensitivityRange.getLower(),
+                            Math.min(Math.min(sensitivityRange.getUpper(), mMotion26709HalBaselineIso),
+                                    targetIso));
+                    double preliminaryEnergy = (double)targetExposureNs * (double)targetIso;
+                    if (preliminaryEnergy > targetEnergy * 1.015) {
+                        targetIso = sensitivityRange.getLower();
+                        targetExposureNs = Math.round(targetEnergy / Math.max(1, targetIso));
+                        targetExposureNs = Math.max(exposureRange.getLower(),
+                                Math.min(Math.min(exposureRange.getUpper(),
+                                        mMotion26709HalBaselineExposureNs), targetExposureNs));
+                    }
+                } else {
+                    long shutterCeilingNs = Math.max(mMotion26709HalBaselineExposureNs,
+                            iris26381MotionOpportunityCeilingNs(result));
+                    shutterCeilingNs = Math.max(exposureRange.getLower(),
+                            Math.min(exposureRange.getUpper(), shutterCeilingNs));
+                    targetIso = Math.max(sensitivityRange.getLower(),
+                            Math.min(sensitivityRange.getUpper(), mMotion26709HalBaselineIso));
+                    targetExposureNs = Math.round(targetEnergy / Math.max(1, targetIso));
+                    targetExposureNs = Math.max(mMotion26709HalBaselineExposureNs,
+                            Math.min(shutterCeilingNs, targetExposureNs));
+                    double preliminaryEnergy = (double)targetExposureNs * (double)targetIso;
+                    if (preliminaryEnergy < targetEnergy * 0.985) {
+                        targetIso = (int)Math.ceil(targetEnergy / Math.max(1.0, targetExposureNs));
+                        targetIso = Math.max(mMotion26709HalBaselineIso,
+                                Math.min(sensitivityRange.getUpper(), targetIso));
+                    }
                 }
-            } else {
-                /* A rare HAL-underexposed scene needs photons, not a fake display lift. Spend
-                 * available motion-safe shutter first, then ISO only if the shutter opportunity
-                 * cannot reach the single predicted target. */
-                long shutterCeilingNs = Math.max(mMotion26709HalBaselineExposureNs,
-                        iris26381MotionOpportunityCeilingNs(result));
-                shutterCeilingNs = Math.max(exposureRange.getLower(),
-                        Math.min(exposureRange.getUpper(), shutterCeilingNs));
-                targetIso = Math.max(sensitivityRange.getLower(),
-                        Math.min(sensitivityRange.getUpper(), mMotion26709HalBaselineIso));
-                targetExposureNs = Math.round(targetEnergy / Math.max(1, targetIso));
-                targetExposureNs = Math.max(mMotion26709HalBaselineExposureNs,
-                        Math.min(shutterCeilingNs, targetExposureNs));
-                double preliminaryEnergy = (double)targetExposureNs * (double)targetIso;
-                if (preliminaryEnergy < targetEnergy * 0.985) {
-                    targetIso = (int)Math.ceil(targetEnergy / Math.max(1.0, targetExposureNs));
-                    targetIso = Math.max(mMotion26709HalBaselineIso,
-                            Math.min(sensitivityRange.getUpper(), targetIso));
-                }
-                iris26711HalShutterPreserved =
-                        targetExposureNs == mMotion26709HalBaselineExposureNs;
-            }
-            double quantizedEnergy = (double)targetExposureNs * (double)targetIso;
-            final float quantizedPhysicalOffsetEv = (float)Math.max(-MOTION_26709_MAX_RELATIVE_EV,
-                    Math.min(MOTION_26712_BRIGHTEN_MAX_EV,
-                        Math.log(quantizedEnergy / mMotion26709HalBaselineEnergy) / Math.log(2.0)));
-            if (!(quantizedEnergy > 0.0)
-                    || (requestedPhysicalOffsetEv < 0.0f && quantizedPhysicalOffsetEv >= -0.03f)
-                    || (requestedPhysicalOffsetEv > 0.0f && quantizedPhysicalOffsetEv <= 0.03f)) {
-                mMotion26710TargetProtectionEv = 0.0f;
-                mMotion26712TargetPhysicalOffsetEv = 0.0f;
-                try {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-                    mMotion26680AeLockApplied = true;
-                    mMotion26709ReferenceSettled = true;
-                    motion26711CaptureLockPoseAndScene();
-                    rebuildPreviewBuilder();
-                } catch (IllegalArgumentException | IllegalStateException ignored) {}
-                Log.w(TAG, "IRIS_26712_TARGET_CLAMPED_TO_HAL zeroPumpInvariant=true");
-                return;
             }
-            final float quantizedProtectionEv = Math.max(0.0f, -quantizedPhysicalOffsetEv);
+
+            final double quantizedEnergy = (double)targetExposureNs * (double)targetIso;
+            float quantizedOffsetEv = (float)(Math.log(
+                    quantizedEnergy / mMotion26709HalBaselineEnergy) / Math.log(2.0));
+            quantizedOffsetEv = Math.max(-MOTION_26709_MAX_RELATIVE_EV,
+                    Math.min(MOTION_26712_BRIGHTEN_MAX_EV, quantizedOffsetEv));
+            if (Math.abs(quantizedOffsetEv) < 0.03f) quantizedOffsetEv = 0.0f;
+
             mMotion26710TargetExposureNs = targetExposureNs;
             mMotion26710TargetIso = targetIso;
             mMotion26710TargetEnergy = quantizedEnergy;
-            mMotion26710TargetProtectionEv = quantizedProtectionEv;
-            mMotion26712TargetPhysicalOffsetEv = quantizedPhysicalOffsetEv;
-            mMotion26710TargetObserveFrames = 0;
-            try {
-                mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
-                mMotion26680AeLockApplied = false;
-                mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_MODE,
-                        CaptureRequest.CONTROL_AE_MODE_OFF);
-                mPreviewRequestBuilder.set(CaptureRequest.SENSOR_EXPOSURE_TIME, targetExposureNs);
-                mPreviewRequestBuilder.set(CaptureRequest.SENSOR_SENSITIVITY, targetIso);
-                mMotion26710ManualReferenceActive = true;
-                mMotion26711PreviewTransitionHold = true;
-                rebuildPreviewBuilder();
-                Log.i(TAG, "IRIS_26712_ONE_SHOT_REFERENCE_COMMIT"
-                        + " halExposureNs=" + mMotion26709HalBaselineExposureNs
-                        + " halIso=" + mMotion26709HalBaselineIso
-                        + " targetExposureNs=" + targetExposureNs
-                        + " targetIso=" + targetIso
-                        + " requestedPhysicalOffsetEv=" + requestedPhysicalOffsetEv
-                        + " quantizedPhysicalOffsetEv=" + quantizedPhysicalOffsetEv
-                        + " quantizedProtectionEv=" + quantizedProtectionEv
-                        + " rawP995=" + mMotion26709PublishedP995
-                        + " rawHighlightFraction=" + mMotion26710HalBaselineHighlightFraction
-                        + " broadVotes=" + mMotion26709PublishedBroadVotes
-                        + " mixedVotes=" + mMotion26709PublishedMixedVotes
-                        + " coherentCells=" + mMotion26709PublishedCoherentCells
-                        + " halShutterPreservedForAntibanding=" + iris26711HalShutterPreserved
-                        + " isoFloor=" + sensitivityRange.getLower()
-                        + " directManualJump=true intermediateTargetsForbidden=true"
-                        + " signedOneShot=true viewfinderAuthority=HAL_26708"
-                        + " postAdjustmentRawCannotRewriteExposure=true");
-            } catch (IllegalArgumentException | IllegalStateException e) {
-                mMotion26710ManualReferenceActive = false;
-                mMotion26711PreviewTransitionHold = false;
-                mMotion26710TargetProtectionEv = 0.0f;
-                mMotion26712TargetPhysicalOffsetEv = 0.0f;
-                try {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_MODE,
-                            CaptureRequest.CONTROL_AE_MODE_ON);
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-                    mMotion26680AeLockApplied = true;
-                    mMotion26709ReferenceSettled = true;
-                    rebuildPreviewBuilder();
-                } catch (IllegalArgumentException | IllegalStateException ignored) {}
-                Log.w(TAG, "IRIS_26710 one-shot manual target skipped: "
-                        + e.getClass().getSimpleName() + " fallback=HAL_LOCK zeroPumpInvariant=true");
-            }
+            mMotion26710TargetProtectionEv = Math.max(0.0f, -quantizedOffsetEv);
+            mMotion26712TargetPhysicalOffsetEv = quantizedOffsetEv;
+            mMotion26710DecisionIssued = true;
+            mMotion26709ReferenceSettled = true;
+            mMotion26713ObservationPending = false;
+            mMotion26713ObservationStartMs = 0L;
+            mMotion26713RecipeReady = true;
+            mMotion26713RecipeExposureNs = targetExposureNs;
+            mMotion26713RecipeIso = targetIso;
+            mMotion26713RecipeEnergy = quantizedEnergy;
+            mMotion26713RecipeOffsetEv = quantizedOffsetEv;
+            mMotion26713RecipeBaselineEnergy = mMotion26709HalBaselineEnergy;
+            mMotion26713RecipeBaselineTimestampNs = sensorTs;
+            mMotion26713RecipeGeneration = mIris26548CameraHealthGeneration;
+            mMotion26713RecipeMotionFrames = 0;
+            Log.i(TAG, "IRIS_26713_CAPTURE_RECIPE_READY"
+                    + " halExposureNs=" + mMotion26709HalBaselineExposureNs
+                    + " halIso=" + mMotion26709HalBaselineIso
+                    + " targetExposureNs=" + targetExposureNs
+                    + " targetIso=" + targetIso
+                    + " targetPhysicalOffsetEv=" + quantizedOffsetEv
+                    + " targetProtectionEv=" + mMotion26710TargetProtectionEv
+                    + " rawP995=" + mMotion26709PublishedP995
+                    + " previewMutation=false repeatingAeMode=HAL_ON"
+                    + " captureDomainOnly=true adjustedRawCannotVoteAgain=true");
             return;
         }
 
-        /* Before a scene is latched, raw gyro shakiness may delay HAL-baseline qualification,
-         * but after lock only net pose/whole-frame proof above can release exposure. */
         if (shakiness > MOTION_26680_REFRAME_GYRO_THRESHOLD) {
             mMotion26680StableFrames = 0;
             mMotion26680StationaryFrames = 0;
             return;
         }
-
         mMotion26680StationaryFrames++;
         boolean aeSettled = aeState != null
                 && (aeState == CaptureResult.CONTROL_AE_STATE_CONVERGED
@@ -3790,74 +3614,35 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         boolean awbSettled = awbState == null
                 || awbState == CaptureResult.CONTROL_AWB_STATE_CONVERGED
                 || awbState == CaptureResult.CONTROL_AWB_STATE_LOCKED;
-        boolean energyStable = true;
-        if (Double.isFinite(mMotion26709HalBaselineEnergy)
-                && mMotion26709HalBaselineEnergy > 0.0 && exposureEnergy > 0.0) {
-            double evDelta = Math.abs(Math.log(exposureEnergy / mMotion26709HalBaselineEnergy)
-                    / Math.log(2.0));
-            energyStable = evDelta <= MOTION_26680_STABLE_ENERGY_EV;
-        }
-        mMotion26709HalBaselineEnergy = exposureEnergy;
-        if (aeSettled && awbSettled && energyStable) {
-            mMotion26680StableFrames++;
-        } else {
-            mMotion26680StableFrames = 0;
-        }
-
+        boolean energyStable = !Double.isFinite(priorEnergy) || priorEnergy <= 0.0
+                || Math.abs(Math.log(exposureEnergy / priorEnergy) / Math.log(2.0))
+                    <= MOTION_26680_STABLE_ENERGY_EV;
+        if (aeSettled && awbSettled && energyStable) mMotion26680StableFrames++;
+        else mMotion26680StableFrames = 0;
         if (mMotion26680StableFrames < MOTION_26680_STABLE_CONFIRM_FRAMES) return;
 
-        Boolean aeLockAvailable = mCameraCharacteristics.get(
-                CameraCharacteristics.CONTROL_AE_LOCK_AVAILABLE);
-        Boolean awbLockAvailable = mCameraCharacteristics.get(
-                CameraCharacteristics.CONTROL_AWB_LOCK_AVAILABLE);
-        if (!Boolean.TRUE.equals(aeLockAvailable)) {
-            Log.w(TAG, "IRIS_26710_REFERENCE_UNAVAILABLE aeLock=false fallback=HAL");
-            return;
-        }
-        try {
-            mMotion26709HalBaselineReady = true;
-            mMotion26709ReferenceSettled = false;
-            mMotion26709ReleasePending = false;
-            mMotion26709HalBaseCompSteps = resultCompSteps;
-            mMotion26709TargetCompSteps = resultCompSteps;
-            mMotion26709AeStepEv = adaptiveAeAvailable ? aeStep.floatValue() : 0.0f;
-            mMotion26709HalBaselineEnergy = exposureEnergy;
-            mMotion26709HalBaselineExposureNs = exposureNs;
-            mMotion26709HalBaselineIso = iso;
-            mMotion26709LastConsumedMeterTimestampNs = 0L;
-            mMotion26710DecisionIssued = false;
-            mMotion26710ManualReferenceActive = false;
-            mMotion26710TargetExposureNs = 0L;
-            mMotion26710TargetIso = 0;
-            mMotion26710TargetEnergy = Double.NaN;
-            mMotion26710TargetProtectionEv = 0.0f;
-            mMotion26712TargetPhysicalOffsetEv = 0.0f;
-            mMotion26710TargetObserveFrames = 0;
-            clearMotion26708HighlightClassifier("26712-hal-baseline-start");
-            if (Boolean.TRUE.equals(awbLockAvailable)) {
-                mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, true);
-                mMotion26680AwbLockApplied = true;
-            }
-            mMotion26680PreviewLatched = true;
-            mMotion26680AeLockApplied = false;
-            mMotion26680ReframeMotionFrames = 0;
-            rebuildPreviewBuilder();
-            Log.i(TAG, "IRIS_26710_HAL_OBSERVATION_BASELINE generation="
-                    + mIris26548CameraHealthGeneration
-                    + " exposureNs=" + exposureNs + " iso=" + iso
-                    + " compSteps=" + resultCompSteps
-                    + " stableFrames=" + mMotion26680StableFrames
-                    + " stationaryFrames=" + mMotion26680StationaryFrames
-                    + " shakiness=" + shakiness
-                    + " aeUntouched=true awbLocked=" + mMotion26680AwbLockApplied
-                    + " oneShotDecisionPending=true viewfinder26708Behavior=true");
-        } catch (IllegalArgumentException | IllegalStateException e) {
-            mMotion26680PreviewLatched = false;
-            mMotion26680AeLockApplied = false;
-            mMotion26680AwbLockApplied = false;
-            mMotion26709HalBaselineReady = false;
-            Log.w(TAG, "IRIS_26710 baseline start skipped: " + e.getClass().getSimpleName());
-        }
+        mMotion26709HalBaselineReady = true;
+        mMotion26709ReferenceSettled = false;
+        mMotion26709HalBaselineEnergy = exposureEnergy;
+        mMotion26709HalBaselineExposureNs = exposureNs;
+        mMotion26709HalBaselineIso = iso;
+        mMotion26709LastConsumedMeterTimestampNs = 0L;
+        mMotion26710DecisionIssued = false;
+        mMotion26710ManualReferenceActive = false;
+        mMotion26710TargetExposureNs = 0L;
+        mMotion26710TargetIso = 0;
+        mMotion26710TargetEnergy = Double.NaN;
+        mMotion26710TargetProtectionEv = 0.0f;
+        mMotion26712TargetPhysicalOffsetEv = 0.0f;
+        mMotion26713ObservationPending = true;
+        mMotion26713ObservationStartMs = android.os.SystemClock.elapsedRealtime();
+        clearMotion26708HighlightClassifier("26713-hal-observation-start");
+        Log.i(TAG, "IRIS_26713_HAL_OBSERVATION_START"
+                + " exposureNs=" + exposureNs + " iso=" + iso
+                + " stableFrames=" + mMotion26680StableFrames
+                + " stationaryFrames=" + mMotion26680StationaryFrames
+                + " previewMutation=false aeLock=false awbLock=false"
+                + " systemAeContinuous=true captureRecipePending=true");
     }
 
     /*
@@ -8354,8 +8139,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         final boolean longExpected;
         final Motion26486ShortTicket shortTicket;
         final Motion26505LongTicket longTicket;
-        final TotalCaptureResult normalReferenceResult;
-        final long normalReferenceTimestampNs;
+        volatile TotalCaptureResult normalReferenceResult;
+        volatile long normalReferenceTimestampNs;
+        final long normalTargetExposureNs;
+        final int normalTargetIso;
+        final double normalTargetEnergy;
+        final float normalTargetOffsetEv;
         final long startElapsedMs;
         final long completionTimeoutMs;
         final int normalFramesAtPress;
@@ -8373,6 +8162,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 boolean shortExpected, boolean longExpected,
                 Motion26486ShortTicket shortTicket, Motion26505LongTicket longTicket,
                 TotalCaptureResult normalReferenceResult, long normalReferenceTimestampNs,
+                long normalTargetExposureNs, int normalTargetIso, float normalTargetOffsetEv,
                 long startElapsedMs, long completionTimeoutMs,
                 Motion26598PreShutterNormals preShutter,
                 long cameraHealthGeneration, String physicalCameraId) {
@@ -8384,6 +8174,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             this.longTicket = longTicket;
             this.normalReferenceResult = normalReferenceResult;
             this.normalReferenceTimestampNs = normalReferenceTimestampNs;
+            this.normalTargetExposureNs = normalTargetExposureNs;
+            this.normalTargetIso = normalTargetIso;
+            this.normalTargetEnergy = (double) normalTargetExposureNs * (double) normalTargetIso;
+            this.normalTargetOffsetEv = normalTargetOffsetEv;
             this.startElapsedMs = startElapsedMs;
             this.completionTimeoutMs = completionTimeoutMs;
             this.normalFramesAtPress = preShutter == null ? 0 : preShutter.images.size();
@@ -8422,7 +8216,13 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 TotalCaptureResult result) {
             if (ticket == null || result == null || !normalTopUpTickets.contains(ticket)) return;
             Long ts = result.get(CaptureResult.SENSOR_TIMESTAMP);
-            if (ts != null && ts > 0L) ownedNormalResults.put(ts, result);
+            if (ts != null && ts > 0L) {
+                ownedNormalResults.put(ts, result);
+                if (normalReferenceResult == null) {
+                    normalReferenceResult = result;
+                    normalReferenceTimestampNs = ts;
+                }
+            }
         }
         synchronized int normalReadyCount() {
             int count = preShutterNormalImages.size();
@@ -8820,10 +8620,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             @NonNull Motion26593CapturePlan plan, int missingNormals) {
         if (missingNormals <= 0) return true;
         if (mCameraDevice == null || mCaptureSession == null || mImageReaderRaw == null
-                || plan.normalReferenceResult == null || mBackgroundHandler == null) return false;
-        Long expObj = plan.normalReferenceResult.get(CaptureResult.SENSOR_EXPOSURE_TIME);
-        Integer isoObj = plan.normalReferenceResult.get(CaptureResult.SENSOR_SENSITIVITY);
-        if (expObj == null || expObj <= 0L || isoObj == null || isoObj <= 0) return false;
+                || mBackgroundHandler == null || plan.normalTargetExposureNs <= 0L
+                || plan.normalTargetIso <= 0) return false;
+        final long expObj = plan.normalTargetExposureNs;
+        final int isoObj = plan.normalTargetIso;
 
         boolean manual = false;
         int[] caps = mCameraCharacteristics == null ? null
@@ -8920,8 +8720,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                 + " result=" + ts);
                         return;
                     }
-                    ticket.exposureAccepted = motionExposurePairMatches(
-                            result, plan.normalReferenceResult);
+                    ticket.exposureAccepted = motion26713ResultMatchesTarget(
+                            result, plan.normalTargetExposureNs, plan.normalTargetIso);
                     if (!ticket.exposureAccepted) {
                         ticket.failed = true;
                         ticket.closeRaw();
@@ -8969,6 +8769,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     + " mode=" + (manual ? "MANUAL_FROZEN_EXPOSURE" : "CAPTURE_LOCAL_AE_LOCK")
                     + " exposureNs=" + expObj + " iso=" + isoObj
                     + " previewRepeatingRequestMutated=false rawOnlyTarget=true"
+                    + " captureDomainOnly26713=true"
                     + " generationOwnedTickets=true");
             return true;
         } catch (CameraAccessException | IllegalArgumentException | IllegalStateException e) {
@@ -9004,6 +8805,15 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
         try {
             iris26524ApplyZoomToPreviewBuilder();
+            /* IRIS_26713_CLEAN_HAL_REPEATING_RESTORE
+             * Capture-local manual SENSOR_* values never enter this builder. Explicitly return the
+             * canonical AUTO preview to HAL AE/0EV before rebuilding repeating ownership. */
+            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
+            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_MODE,
+                    CaptureRequest.CONTROL_AE_MODE_ON);
+            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION, 0);
+            mPreviewRequestBuilder.set(CaptureRequest.SENSOR_EXPOSURE_TIME, null);
+            mPreviewRequestBuilder.set(CaptureRequest.SENSOR_SENSITIVITY, null);
             final CaptureRequest liveRequest = mPreviewRequestBuilder.build();
             final Integer aeMode = liveRequest.get(CaptureRequest.CONTROL_AE_MODE);
             final Long sensorExposure = liveRequest.get(CaptureRequest.SENSOR_EXPOSURE_TIME);
@@ -9144,7 +8954,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
      * user presses during a transition, wait only for a result belonging to the new absolute
      * target; the existing exact-exposure post-shutter NORMAL top-up fills any missing ring slots. */
     private boolean motion26676ProtectionIncreasePendingForShutter() {
-        return isZslMode() && (!mMotion26709HalBaselineReady || !mMotion26709ReferenceSettled);
+        /* IRIS_26713_RECIPE_ONLY_SHUTTER_GUARD
+         * Waiting never changes preview exposure; HAL continues normally while Iris waits briefly
+         * for the stable-scene capture recipe. Timeout still captures a full HAL-exposure burst. */
+        return isZslMode() && !mMotion26713RecipeReady;
     }
 
     private boolean motion26676DeferShutterUntilProtectedGeneration() {
@@ -9207,17 +9020,27 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && iris26593ReferenceTs != null
                 && iris26593ReferenceExp != null && iris26593ReferenceExp > 0L
                 && iris26593ReferenceIso != null && iris26593ReferenceIso > 0;
-        if (iris26593SessionReady && mMotion26709HalBaselineReady
-                && Double.isFinite(mMotion26709HalBaselineEnergy)
-                && mMotion26709HalBaselineEnergy > 0.0) {
-            double refEnergy = (double) iris26593ReferenceExp * (double) iris26593ReferenceIso;
-            double protect = refEnergy > 0.0
-                    ? Math.log(mMotion26709HalBaselineEnergy / refEnergy) / Math.log(2.0) : 0.0;
-            mMotion26709FrozenCaptureProtectionEv = (float)Math.max(0.0,
-                    Math.min(MOTION_26709_MAX_RELATIVE_EV, protect));
-        } else {
-            mMotion26709FrozenCaptureProtectionEv = 0.0f;
-        }
+        final double iris26713CurrentHalEnergy = iris26593SessionReady
+                ? (double) iris26593ReferenceExp * (double) iris26593ReferenceIso : Double.NaN;
+        final boolean iris26713RecipeGenerationValid = mMotion26713RecipeReady
+                && mMotion26713RecipeGeneration == mIris26548CameraHealthGeneration
+                && mMotion26713RecipeExposureNs > 0L && mMotion26713RecipeIso > 0
+                && Double.isFinite(mMotion26713RecipeBaselineEnergy)
+                && mMotion26713RecipeBaselineEnergy > 0.0;
+        final double iris26713RecipeBaselineMismatchEv = iris26713RecipeGenerationValid
+                && Double.isFinite(iris26713CurrentHalEnergy) && iris26713CurrentHalEnergy > 0.0
+                ? Math.abs(Math.log(iris26713CurrentHalEnergy / mMotion26713RecipeBaselineEnergy)
+                    / Math.log(2.0)) : Double.POSITIVE_INFINITY;
+        final boolean iris26713RecipeValidAtShutter = iris26713RecipeGenerationValid
+                && iris26713RecipeBaselineMismatchEv <= MOTION_26713_RECIPE_INVALIDATE_EV;
+        final long iris26713NormalTargetExp = iris26713RecipeValidAtShutter
+                ? mMotion26713RecipeExposureNs : (iris26593ReferenceExp == null ? 0L : iris26593ReferenceExp);
+        final int iris26713NormalTargetIso = iris26713RecipeValidAtShutter
+                ? mMotion26713RecipeIso : (iris26593ReferenceIso == null ? 0 : iris26593ReferenceIso);
+        final float iris26713NormalTargetOffsetEv = iris26713RecipeValidAtShutter
+                ? mMotion26713RecipeOffsetEv : 0.0f;
+        mMotion26709FrozenCaptureProtectionEv = Math.max(0.0f, -iris26713NormalTargetOffsetEv);
+        mMotion26712TargetPhysicalOffsetEv = iris26713NormalTargetOffsetEv;
         if (!iris26593SessionReady) {
             Log.w(TAG, "IRIS_26593_MOTION_NOT_READY sessionReady=false"
                     + " cameraDevice=" + (mCameraDevice != null)
@@ -9252,6 +9075,20 @@ private long mMotionUnifiedLastUpdateMs = 0L;
 
         final Motion26598PreShutterNormals iris26598PreShutterNormals =
                 freezeMotion26598PreShutterNormals(iris26593NormalReference, iris26593TotalTarget);
+        final boolean iris26713TargetMatchesHal = iris26713NormalTargetExp > 0L
+                && iris26713NormalTargetIso > 0
+                && Math.abs(Math.log(((double) iris26713NormalTargetExp * iris26713NormalTargetIso)
+                    / iris26713CurrentHalEnergy) / Math.log(2.0))
+                    <= MOTION_26486_EXPOSURE_HALF_WINDOW_EV;
+        if (!iris26713TargetMatchesHal) {
+            iris26598PreShutterNormals.closeAll();
+            Log.i(TAG, "IRIS_26713_HAL_ZSL_EVIDENCE_EXCLUDED_FROM_CORRECTED_NORMAL"
+                    + " ringAtPress=" + iris26598PreShutterNormals.ringCountAtPress
+                    + " targetExposureNs=" + iris26713NormalTargetExp
+                    + " targetIso=" + iris26713NormalTargetIso
+                    + " targetOffsetEv=" + iris26713NormalTargetOffsetEv
+                    + " previewRemainsHal=true");
+        }
 
         mMotion26505LongRequested = false;
         Motion26505LongTicket iris26505PreviousLongTicket = mMotion26505CaptureLongTicket;
@@ -9260,11 +9097,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             clearMotion26505LongTicket(iris26505PreviousLongTicket, "new_generation");
         }
 
-        /* IRIS_26710_ONE_SHOT_REFERENCE_PLUS_HAL_LONG_CAPTURE_PLAN
-         * NORMAL itself is now the highlight-safe equal-exposure reference burst. The old darker
-         * HIGHLIGHT_SHORT transaction is therefore retired. One RAW-only SHADOW_LONG remains the
-         * auxiliary body/shadow exposure and is chosen independently from the frozen untouched HAL
-         * baseline shadow/SNR evidence; NORMAL underexposure cannot drag LONG down. */
+        /* IRIS_26713_CAPTURE_DOMAIN_NORMAL_PLUS_HAL_LONG_PLAN
+         * Preview remains HAL-owned. A prepared Iris NORMAL recipe is consumed only by RAW-only
+         * still requests after shutter; HAL prebuffer RAWs remain analysis evidence unless their
+         * exposure already equals the recipe. LONG remains independently HAL-baselined. */
         final Motion26486ShortTicket iris26486ShortTicket = new Motion26486ShortTicket();
         mMotion26486CaptureShortTicket = iris26486ShortTicket;
         final boolean iris26593ShortSubmitted = false;
@@ -9284,7 +9120,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
 
         final int iris26593AuxCount = iris26505LongRequested ? 1 : 0;
-        Log.i(TAG, "IRIS_26710_ONE_SHOT_REFERENCE_CAPTURE_PLAN"
+        Log.i(TAG, "IRIS_26713_CAPTURE_DOMAIN_REFERENCE_PLAN"
                 + " liveViewfinderOwner=26708_STABLE_HAL_APPEARANCE"
                 + " oneShotReferenceSettled=" + mMotion26709ReferenceSettled
                 + " referenceProtectionEv=" + mMotion26709FrozenCaptureProtectionEv
@@ -9292,7 +9128,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 + " shadowLongRequested=" + iris26505LongRequested
                 + " halBaselinedLong=true totalTarget=" + iris26593TotalTarget
                 + " normalTarget=" + (iris26593TotalTarget - iris26593AuxCount)
-                + " equalExposureNormalReference=true previewPhysicalOffsetHidden=true");
+                + " equalExposureNormalReference=true captureDomainOnly=true"
+                + " previewRepeatingNeverMutated=true recipeValidAtShutter="
+                + iris26713RecipeValidAtShutter);
         final int iris26593NormalTarget = iris26593TotalTarget - iris26593AuxCount;
         if (iris26593NormalTarget < 1) {
             iris26598PreShutterNormals.closeAll();
@@ -9308,14 +9146,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         final int iris26593MissingNormals = Math.max(0,
                 iris26593NormalTarget - iris26593NormalAtPress);
         final long iris26593TimeoutMs = motion26593CompletionTimeoutMs(
-                iris26593MissingNormals, iris26593ReferenceExp,
+                iris26593MissingNormals, iris26713NormalTargetExp,
                 iris26480ShortHighlightRequested || iris26505LongRequested);
         final long iris26593StartMs = android.os.SystemClock.elapsedRealtime();
         final Motion26593CapturePlan iris26593Plan = new Motion26593CapturePlan(
                 iris26593TotalTarget, iris26593NormalTarget,
                 iris26480ShortHighlightRequested, iris26505LongRequested,
-                iris26486ShortTicket, iris26505LongTicket, iris26593NormalReference,
-                iris26593ReferenceTs, iris26593StartMs, iris26593TimeoutMs,
+                iris26486ShortTicket, iris26505LongTicket,
+                iris26713TargetMatchesHal ? iris26593NormalReference : null,
+                iris26713TargetMatchesHal && iris26593ReferenceTs != null ? iris26593ReferenceTs : 0L,
+                iris26713NormalTargetExp, iris26713NormalTargetIso, iris26713NormalTargetOffsetEv,
+                iris26593StartMs, iris26593TimeoutMs,
                 iris26598PreShutterNormals, mIris26548CameraHealthGeneration, physicalID);
         mMotion26593CapturePlan = iris26593Plan;
 
@@ -9345,8 +9186,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                         + " bufferedRaw=" + iris26593BufferedRawFrames
                         + " timeoutMs=" + iris26593TimeoutMs
                         + " postShutterNormalAdmission=" + (iris26593MissingNormals > 0)
-                        + " normalReferenceExposureNs=" + iris26593ReferenceExp
-                        + " normalReferenceIso=" + iris26593ReferenceIso
+                        + " normalReferenceExposureNs=" + iris26713NormalTargetExp
+                        + " normalReferenceIso=" + iris26713NormalTargetIso
+                        + " captureDomainOnly=true previewRepeatingMutated=false"
                         + " preShutterNormalsGenerationOwned=true"
                         + " postShutterNormalsTicketOwned=true"
                         + " superResCaptureBudgetOwner=false"
@@ -10348,10 +10190,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mZslCapturing = false;
         burst = false;
         mState = STATE_PREVIEW;
-        /* IRIS_26711_CAPTURE_EXPOSURE_OWNER_ENDS_AT_PHYSICAL_BATCH
-         * All NORMAL/LONG pixels and metadata are immutable above. Live preview now returns to
-         * ordinary HAL AE at 0 EV immediately; Sabre/render/JPEG/UHDR processing is independent. */
-        motion26711ReleaseToHalAfterPhysicalCapture();
+        /* IRIS_26713_CAPTURE_RECIPE_OWNER_ENDS_AT_PHYSICAL_BATCH
+         * The visible repeating request never left HAL. Only the cached capture recipe ends here;
+         * processing has zero live-camera ownership. */
+        motion26713ClearRecipeAfterPhysicalBatch();
         processExecutor.execute(() -> {
             try {
                 mBackgroundHandler.post(this::unlockFocus);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
index a05274d91ce7ad9c8e27de167841dd9c8d4bda31..670bd5a9a7406fb501ec177ca4921bd428c580d6 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
@@ -69,15 +69,8 @@ public class MainRenderer implements GLSurfaceView.Renderer, SurfaceTexture.OnFr
         //GLES20.glClear(GLES20.GL_COLOR_BUFFER_BIT);
 
         long iris26553FrameTimestamp = -1L;
-        CaptureController iris26711Controller = PhotonCamera.getCaptureController();
-        /* IRIS_26711_HIDDEN_PHYSICAL_EXPOSURE_TRANSITION
-         * Do not consume sensor frames while HAL->manual, lock-reassert or post-capture HAL
-         * restoration is physically transitioning. The OES texture therefore keeps the last
-         * stable displayed frame; SurfaceTexture will deliver the newest frame when the hold ends. */
-        boolean iris26711HoldExposureTransition = iris26711Controller != null
-                && iris26711Controller.shouldHoldMotion26711PreviewExposureTransition();
         synchronized (this) {
-            if (mUpdateST && !iris26711HoldExposureTransition) {
+            if (mUpdateST) {
                 mSTexture.updateTexImage();
                 iris26553FrameTimestamp = mSTexture.getTimestamp();
                 mUpdateST = false;
@@ -87,20 +80,20 @@ public class MainRenderer implements GLSurfaceView.Renderer, SurfaceTexture.OnFr
         GLES20.glUniform1i(enablePeak, PhotonCamera.getSettings().focusPeak);
         GLES20.glUniform1i(mirror, mMirrorPreview ? 1 : 0);
         GLES20.glUniform1f(irisSoftwareZoom, mSoftwareZoom);
-        /* IRIS_26709_SIGNED_FRAME_EXACT_PREVIEW_PRESENTATION
+        /* IRIS_26667_FRAME_EXACT_PREVIEW_PRESENTATION
          * Preserve 26662 timestamp matching and 26663 hold-on-miss, with no additional display
          * slew. Compensation changes only when an exact Camera2 result for the displayed frame
          * proves the protection EV. */
-        CaptureController iris26663Controller = iris26711Controller;
+        CaptureController iris26663Controller = PhotonCamera.getCaptureController();
         if (iris26663Controller != null && iris26553FrameTimestamp > 0L) {
             float exactEv = iris26663Controller.getMotion26663ReferencePreviewProtectionEv(
                     iris26553FrameTimestamp);
             if (Float.isFinite(exactEv)) {
-                mIris26663LastConfirmedProtectionEv = Math.max(-1.50f, Math.min(1.50f, exactEv));
+                mIris26663LastConfirmedProtectionEv = Math.max(0.0f, exactEv);
             }
         }
         float iris26667PreviewGain = (float) Math.pow(2.0,
-                Math.max(-1.50f, Math.min(1.50f, mIris26663LastConfirmedProtectionEv)));
+                Math.max(0.0f, mIris26663LastConfirmedProtectionEv));
         GLES20.glUniform1f(iris26662ReferencePreviewGain, iris26667PreviewGain);
         mIris26678PreviewFlicker[0] = 0.0f;
         mIris26678PreviewFlicker[1] = 0.0f;
diff --git a/app/version.properties b/app/version.properties
index ca48333a352e87625ed877d78d2bcbcff7351b2d..bc0cc5ce96ffcea194297ab2e374fc78babd2f79 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726712
-VERSION_BUILD=26712
+VERSION_NAME=0.9726713
+VERSION_BUILD=26713
