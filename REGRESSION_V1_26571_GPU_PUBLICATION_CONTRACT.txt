26571 V1 TRUE2X GPU PUBLICATION CONTRACT

Runtime authority:
- exact successful 26570 V1.2 compiled candidate from commit 0c05139fab0240bed159e4250a05bba38c5463dd
- Actions run 33435528495
- artifact 9774331069 photon-26570-v1-surface-performance
- artifact SHA-256 5295e7899ff0c672681c44746656794d2bae9d52994847d40f74c9c669f6b70f
- deterministic candidate tar SHA-256 bc43d4bda1bdd42fa31b4e6988e81b0a76188939a118bf97f9554a6089bdde12

Scope:
- app/src/main/cpp/motionv2_jpeg444_jni.cpp
- app/src/main/cpp/CMakeLists.txt
- app/version.properties
No backup by user direction.

IRIS_26571_TRUE2X_GPU_PUBLICATION:
- operates only after the successful true2x carrier already exists;
- does not alter Sabre/VGN reconstruction, frame selection, exposure, alignment, DNG, or UHDR/JPEG-R ownership;
- duplicates the successful 26570 final profile/adaptive/tone/headroom/gain equations in a GLES 3.1 compute shader;
- RGB output is the same final SDR publication domain; alpha carries the 1:1 gain-map code;
- output texture height is bounded to the existing publication bandRows (256 Motion fast path);
- exactly two GL_PIXEL_PACK_BUFFER slots + fences provide asynchronous bounded readback;
- CPU libjpeg encodes prior bands concurrently while the GPU producer renders/readbacks later bands;
- requested base quality, 4:4:4 sampling, Display-P3 ICC, grayscale gain quality 95 and 1:1 gain geometry are unchanged.

Fallback:
- GPU eligible only when Jin is disabled and watermark is disabled in V1.
- Any unsupported EGL/GLES <3.1, texture-size limit, shader compile/link, allocation, dispatch, fence, mapping/readback, or GPU-temp JPEG failure routes to the exact successful 26570 CPU publisher from the unchanged true2x carrier.
- GPU partial outputs use temporary files and are deleted before CPU replay.
- There is no resolution fallback and no 12MP publication path introduced.

Telemetry:
- MotionTrace receives IRIS_26571_TRUE2X_GPU_PUBLICATION with sourceReadMs, uploadDispatchMs, readbackWaitMs, deinterleaveMs, producerMs, baseJpegMs, gainJpegMs, totalMs.
- MotionTrace receives IRIS_26571_TRUE2X_GPU_FALLBACK on fallback and IRIS_26571_TRUE2X_CPU_FALLBACK for exact CPU timing.

26570 invariance:
- successful 26570 VGN surface-continuity/highlight/chroma bytes are protected and unchanged;
- successful 26570 CPU profileColor/adaptiveAppearance/tone/renderHeadroom/renderBaseCached/Jin/streaming helper functions are byte-identical;
- current CPU worker cap remains unchanged;
- no lower-resolution gain map in V1.

Pre-handoff reachability regression:
- renderReadback must NOT require a non-null Iris26571ReadyBand output because GPU output materialization occurs later in resolve().
- the producer intentionally calls renderReadback(..., nullptr, ...) and the guard must accept that deferred-output contract.
- this prevents an otherwise compile-clean build from silently routing every eligible Super Res publication through the 26570 CPU fallback.

26571 V1 LOCALIZED EDGE-COLOR / VIBRANCY CORRECTION CONTRACT
-------------------------------------------------------------
Observed 26570 comparison target:
- Iris 26570 clean open sky color is the reference and must not be shifted toward Photon Camera's cyan-biased sky.
- 26570 surface-continuity cleanup fixed clumping/pepper/seam behavior and must remain illumination-independent.
- 26570 one-sided luma authority fixed thin-structure luma fill and must remain byte-identical.
- prior ceiling-light pink/magenta false-color suppression must not be weakened.
- remaining defect: real foliage/subject chroma can be weakened to grey at sky/background boundaries, and moderate edges can still inherit opposite-side chroma.

Required runtime behavior:
- IRIS_26571_COHERENT_CHROMA_PRESERVATION keeps the 26570 target-chroma cap: output cleanup never invents chroma magnitude beyond the center sample.
- IRIS_26571_SAME_SIDE_MATERIAL_BOUNDARY protects a one-sided luma transition only when chroma disagreement proves a material/color boundary; smooth illumination with coherent chroma retains 26570 continuity cleanup.
- IRIS_26571_CROSS_EDGE_CHROMA_OWNERSHIP prevents local-median chroma from crossing moderate sky/subject or subject/background material boundaries while preserving center luma.
- IRIS_26571_DIRECTIONAL_EDGE_CHROMA_FLOOR preserves the exact 26570 lower-chroma decision in interiors; only coherent non-highlight directional edges receive an 80% center-chroma floor, capped at the original center magnitude.
- IRIS_26571_IIR_MATERIAL_EDGE_STATE_RESET retains the 26570 >0.55 strong-luma reset and adds a non-highlight moderate-edge reset only when chroma jump independently proves a material boundary.
- highlightPreservePermission disables all new chroma-preservation escape paths near highlights, preserving prior pink/magenta false-color cleanup.

Permanent regressions:
- uniform clean sky must be exact pass-through in the universal cleanup fixture;
- uniform coherent foliage/subject color must be exact pass-through;
- isolated unsupported false chroma on a neutral same-luma surface must remain suppressible;
- near-highlight isolated pink chroma must remain eligible for cleanup;
- a moderate one-sided foliage/sky color boundary must be protected from consensus blending;
- same-material illumination change with coherent chroma must not be reclassified as a material boundary;
- directional edge protection may restore toward the original center magnitude but must never exceed it;
- no global saturation/vibrance multiplier is introduced anywhere in the 26571 correction.
