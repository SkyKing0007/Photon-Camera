26570 V1 INFRASTRUCTURE DIFF AUDIT AGAINST SUCCESSFUL 26569 V1.1

Verification-mechanics authority:
  26569 V1.1 success run 33410892866, artifact 9765167937.

Infrastructure behavior intentionally unchanged:
  authority artifact -> exact candidate tar -> deterministic candidate reconstruction
  -> semantic/ownership validation -> modified runtime-expanded GLSL reserved scan
  -> pinned real glslang -> authority-seeded live candidate equality
  -> real Kotlin + Java compilers -> real NDK both ABIs -> patch proof
  -> PRE-BUILD SAFETY PROOF -> full assembleDebug -> exactly one APK
  -> authority-seeded post-build invariance -> deterministic candidate export.

Necessary 26570 infrastructure deltas only:
1. Authority pins advance from successful 26568 to successful 26569 V1.1.
2. Runtime allowlist changes to exactly 3 paths.
3. Shader verifier expands the four modified VGN embedded compute shaders and compiles those exact runtime sources.
4. Semantic regressions add illumination-independent surface continuity, one-sided edge, chroma exclusion, luma-envelope, IIR reset, and bounded concurrent encoder contracts.
5. Native protected manifest excludes only the intentionally changed motionv2_jpeg444_jni.cpp; all other native/vendor files remain byte-identical.

No verification order is weakened, removed, reordered, or substituted.
The real 26569 compiled candidate is used for local replay of every changed mechanic before packaging.
