26601 V1 — Common Sabre SHORT ownership + restored presentation

Base/runtime authority: successful 26600 V1 commit d12358acac0249eb37b5bbea137de107bd96612f, Actions run 33967121035, job 101309093227, artifact 9969833503, artifact SHA-256 519c3a9aa1554a9839117a8590f8e5bc3c9ce830f3ba1d9df9d25718ad64bfe4.

Verification-mechanics authority: exact successful 26600 implementation, preserving the successful 26593 real compiler/NDK/full-assemble ordering and pins. No build-step redesign, reordering, or simplification is intended.

Upload/replace every file in this ZIP at repository root on experimental-clean-photon-rebuild, commit, and push. Do not place payload files directly into live app/src; the guarded Actions build reconstructs exact successful 26600 compiled-candidate authority and overlays only the eight sealed runtime files.

User explicitly requested no new backup for 26601. Deterministic full-index forward/rollback patches are packaged and verified.

26601 corrects two device-visible regressions without reopening successful 26600 capture or geometry:
1. SHORT no longer produces a second post-VGN RGB image that is blended across material/highlight edges. Exact 26600 SHORT geometry/mask remains the trust authority, but accepted SHORT is exposure-normalized and switches the homogeneous Sabre color/R-weight/Gb-weight evidence before the single common dehomogenize -> Resolve -> VGN chain. LONG remains in its already-common guarded Sabre path. DNG and true2x high-frequency evidence remain NORMAL-only.
2. The coordinated five-file presentation set is restored byte-for-byte from successful 26598. This removes the 26599-added mid/high presentation-knee stage that produced the washed/hazy SDR base, while leaving common MotionV2Render sceneWhite/headroom mapping unchanged. The intent is to restore the prior good brightness/contrast rendition, not darken the image.

This handoff is upload-ready only until GitHub Actions runs the pinned real GLSL 16.5.0 compiler, real Kotlin/Java compilers, both NDK ABIs, full :app:assembleDebug, exactly-one-APK, and post-build invariance gates.
