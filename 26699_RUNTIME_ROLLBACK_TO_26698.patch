diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 779e482e5afd598aa4d2a9424e9e51efdf9eb479..7ccdb63cd4b4b6a3d7363565cdc8ac6d47a1e4a6 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2546,12 +2546,14 @@ internal class GlesMgcRawSpatialStacker(
                     GLES30.GL_LINEAR,
                 )
                 renderDilation(reverseWeight, frameWeight)
-                /* IRIS_26699_LONG_PROOF_REDUNDANT_BARRIER_REMOVED
-                 * The disabled isolated proof target never feeds the production accumulator.
-                 * Same-context GPU ordering is sufficient; do not drain the GPU here.
+                /* IRIS_26698_LONG_FINAL_WEIGHT_PROOF_DISABLED
+                 * 26658 proved this isolated target never feeds the production accumulator.
+                 * Retain only an explicit completion boundary at the former proof location.
                  */
                 if (frame.role == RawBurstFrameRole.SHADOW_LONG) {
-                    PLog.i(SABRE_TAG, "IRIS_26699_LONG_PROOF_REDUNDANT_BARRIER_REMOVED productionAccumulatorUnchanged=true")
+                    GLES30.glFinish()
+                    checkGlError("26698 LONG proof-only work disabled completion")
+                    PLog.i(SABRE_TAG, "IRIS_26698_LONG_FINAL_WEIGHT_PROOF_DISABLED productionAccumulatorUnchanged=true")
                 }
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     check(shortPhysicalReverseWeight26610 != 0) {
@@ -2810,8 +2812,12 @@ internal class GlesMgcRawSpatialStacker(
                         "inferredLossAllowed=${referenceProtectionEv < 0.25f} " +
                         "protectedNormalRequiresMeasuredSourceLoss=${referenceProtectionEv >= 0.25f}",
                 )
-                PLog.i(SABRE_TAG, "IRIS_26699_FUSION_DECISION_BARRIER_REMOVED productionFusionUnchanged=true")
-                PLog.i(SABRE_TAG, "IRIS_26699_FUSION_RADIANCE_BARRIER_REMOVED productionFusionUnchanged=true")
+                GLES30.glFinish()
+                checkGlError("26698 fusion decision telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_FUSION_DECISION_READBACK_DISABLED productionFusionUnchanged=true")
+                GLES30.glFinish()
+                checkGlError("26698 fusion radiance telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_FUSION_RADIANCE_PROBE_DISABLED productionFusionUnchanged=true")
                 renderSabreShortColorValidityAugment26651(
                     shortColorValidity = shortColorValidity26651,
                     normalWeightR = sabreRSupport26613,
@@ -2914,7 +2920,9 @@ internal class GlesMgcRawSpatialStacker(
                     output = physicalHdr26605,
                     program = sabreOutputTransformFloatProgram,
                 )
-                PLog.i(SABRE_TAG, "IRIS_26699_HDR_PROBE_BARRIER_REMOVED stage=FLOAT_HDR_HANDOFF_PRE_VGN productionHdrUnchanged=true")
+                GLES30.glFinish()
+                checkGlError("26698 FLOAT_HDR_HANDOFF_PRE_VGN telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_HDR_PROBE_DISABLED stage=FLOAT_HDR_HANDOFF_PRE_VGN productionHdrUnchanged=true")
             }
             renderSabreOutputTransform(
                 resolvedRgbPlanes = nativeResolvedPlanes,
@@ -2995,7 +3003,9 @@ internal class GlesMgcRawSpatialStacker(
                     }
                 }
                 if (preserveExtendedHdrThroughVgn) {
-                    PLog.i(SABRE_TAG, "IRIS_26699_HDR_PROBE_BARRIER_REMOVED stage=POST_VGN_HDR_MASTER productionHdrUnchanged=true")
+                    GLES30.glFinish()
+                    checkGlError("26698 POST_VGN_HDR_MASTER telemetry disabled completion")
+                    PLog.i(SABRE_TAG, "IRIS_26698_HDR_PROBE_DISABLED stage=POST_VGN_HDR_MASTER productionHdrUnchanged=true")
                 }
             }
             /* IRIS_26601_SHARED_NATIVE_HDR_AUTHORITY
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/control/Swipe.java b/app/src/main/java/com/particlesdevs/photoncamera/control/Swipe.java
index 50a906176b90667d74da9dc3e923314911e1baac..adc570277bcb91bfe56cccaaaa999e63c3b0980e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/control/Swipe.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/control/Swipe.java
@@ -14,7 +14,6 @@ import androidx.constraintlayout.widget.ConstraintLayout;
 import com.particlesdevs.photoncamera.circularbarlib.api.ManualModeConsole;
 import com.particlesdevs.photoncamera.circularbarlib.control.ManualParamModel;
 import com.particlesdevs.photoncamera.R;
-import com.particlesdevs.photoncamera.api.CameraMode;
 import com.particlesdevs.photoncamera.capture.CaptureController;
 import com.particlesdevs.photoncamera.ui.camera.CameraFragment;
 import com.particlesdevs.photoncamera.ui.camera.viewmodel.CameraFragmentViewModel;
@@ -180,26 +179,6 @@ public class Swipe {
         float x = event.getRawX() - location[0];
         float y = event.getRawY() - location[1];
         if (x < 0f || y < 0f || x >= width || y >= height) return null;
-
-        /* IRIS_26699_VIDEO_BOTTOM_TOUCH_FOCUS_EXCLUSION
-         * Video previews intentionally extend under the translucent bottom controls. That visual
-         * preview area is not a focus target: shutter/gallery/switcher keep their own touch owners,
-         * while tap/long-press focus remains available everywhere above the actual bottom bar. */
-        CameraMode mode = captureController.getAuthoritativeCameraMode();
-        if (mode == CameraMode.VIDEO || mode == CameraMode.RAWVIDEO) {
-            View bottomControls = cameraFragment.findViewById(R.id.layout_bottombar);
-            if (bottomControls != null && bottomControls.isShown()) {
-                int[] bottomLocation = new int[2];
-                bottomControls.getLocationOnScreen(bottomLocation);
-                float rawY = event.getRawY();
-                if (rawY >= bottomLocation[1]
-                        && rawY < bottomLocation[1] + bottomControls.getHeight()) {
-                    Log.d(TAG, "IRIS_26699_VIDEO_BOTTOM_FOCUS_REJECT mode=" + mode
-                            + " rawY=" + rawY + " bottomTop=" + bottomLocation[1]);
-                    return null;
-                }
-            }
-        }
         return new float[]{x, y};
     }
 
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
index 1e76602421ca0f8bd8c319689bba230a394bdab7..78c1bfa9dd74a52add4f2befb9dae39190007dc7 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
@@ -433,7 +433,6 @@ public class ImageSaver {
                         && parameters.motionV2SuperResOutputEnabled
                         && true2xRenderRgbPath != null
                         && true2xWidth > 0 && true2xHeight > 0;
-                final long iris26699EncoderStartNs = System.nanoTime();
                 boolean saved;
                 if (useSuperRes) {
                     saved = MotionV2Jpeg444Encoder.writeTrue2x(
@@ -450,19 +449,11 @@ public class ImageSaver {
                     saved = MotionV2Jpeg444Encoder.write(fileToSave, img, jpgQuality);
                 }
                 if (!saved) return false;
-                final long iris26699EncoderMs =
-                        (System.nanoTime() - iris26699EncoderStartNs) / 1_000_000L;
 
-                final long iris26699ExifStartNs = System.nanoTime();
                 img.recycle();
                 ExifInterface inter = ParseExif.setAllAttributes(
                         fileToSave.toFile(), exifData);
                 inter.saveAttributes();
-                final long iris26699ExifMs =
-                        (System.nanoTime() - iris26699ExifStartNs) / 1_000_000L;
-                Log.i(TAG, "IRIS_26699_JPEGR_PUBLICATION_TIMING encoderMs=" + iris26699EncoderMs
-                        + " exifMs=" + iris26699ExifMs + " superRes=" + useSuperRes
-                        + " extraFileProof=false");
                 iris26642ScheduleUltraHdrDecodeProof(fileToSave, "JPEG");
                 return true;
             } catch (IOException e) {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2CfaInput.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2CfaInput.java
index 90b8439ba5fb81d59d3fbcd9d34cfa054c06c60c..ccf724a272ae814ceadd15d2dcdb7ea386e99b81 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2CfaInput.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2CfaInput.java
@@ -1,14 +1,12 @@
 package com.particlesdevs.photoncamera.processing.opengl.postpipeline;
 
 import android.graphics.Point;
-import android.opengl.GLES30;
 
 import com.particlesdevs.photoncamera.processing.opengl.GLDrawParams;
 import com.particlesdevs.photoncamera.processing.opengl.GLFormat;
 import com.particlesdevs.photoncamera.processing.opengl.GLTexture;
 import com.particlesdevs.photoncamera.processing.opengl.nodes.Node;
 import com.particlesdevs.photoncamera.util.Log;
-import com.particlesdevs.photoncamera.util.Allocator;
 
 import java.nio.ByteBuffer;
 
@@ -71,59 +69,12 @@ public final class MotionV2CfaInput extends Node {
                         && raw.x > 0 && raw.y > 0
                         && (raw.x % 2) == 0 && (raw.y % 2) == 0;
         boolean directRgbCarrier = directBayer && pipeline.motionV2DirectRgbCarrier;
-        long directHalfBytes = (long) raw.x * (long) raw.y * 4L * 2L;
-        long directFloatBytes = (long) raw.x * (long) raw.y * 4L * 4L;
-        boolean directHalfRgbCarrier = directRgbCarrier && source.capacity() == directHalfBytes;
-        if (directRgbCarrier
-                && source.capacity() != directHalfBytes
-                && source.capacity() != directFloatBytes) {
-            throw new IllegalStateException("26699 Motion direct RGB carrier byte contract invalid bytes="
-                    + source.capacity() + " halfExpected=" + directHalfBytes
-                    + " floatExpected=" + directFloatBytes);
-        }
-
-        try {
-            if (directHalfRgbCarrier) {
-                /* IRIS_26699_MOTION_DIRECT_RGBA16F_UPLOAD
-                 * GLTexture FLOAT_16 historically uses GL_FLOAT as its generic client type. The
-                 * Sabre carrier physically contains IEEE half values, so allocate the same RGBA16F
-                 * internal texture but upload these bytes explicitly as GL_HALF_FLOAT. */
-                WorkingTexture = new GLTexture(
-                        raw,
-                        new GLFormat(GLFormat.DataType.FLOAT_16, 4),
-                        GL_NEAREST,
-                        GL_CLAMP_TO_EDGE);
-                int[] previousBinding = new int[1];
-                GLES30.glGetIntegerv(GLES30.GL_TEXTURE_BINDING_2D, previousBinding, 0);
-                try {
-                    GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, WorkingTexture.mTextureID);
-                    GLES30.glPixelStorei(GLES30.GL_UNPACK_ALIGNMENT, 8);
-                    view.position(0);
-                    GLES30.glTexSubImage2D(
-                            GLES30.GL_TEXTURE_2D, 0, 0, 0, raw.x, raw.y,
-                            GLES30.GL_RGBA, GLES30.GL_HALF_FLOAT, view);
-                    com.particlesdevs.photoncamera.processing.opengl.GLCoreBlockProcessing
-                            .checkEglError("26699 Motion RGBA16F direct client upload");
-                } finally {
-                    GLES30.glPixelStorei(GLES30.GL_UNPACK_ALIGNMENT, 1);
-                    GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, previousBinding[0]);
-                }
-            } else {
-                WorkingTexture = new GLTexture(
-                        directRgbCarrier ? raw : half,
-                        new GLFormat(GLFormat.DataType.FLOAT_32, 4),
-                        view,
-                        GL_NEAREST,
-                        GL_CLAMP_TO_EDGE);
-            }
-        } finally {
-            /* glTexSubImage2D has consumed client memory before returning. Match Night's proven
-             * ownership contract and release the native carrier before ping-pong allocation. */
-            pipeline.motionV2FloatCfa = null;
-            Allocator.free(source);
-            source = null;
-            view = null;
-        }
+        WorkingTexture = new GLTexture(
+                directRgbCarrier ? raw : half,
+                new GLFormat(GLFormat.DataType.FLOAT_32, 4),
+                view,
+                GL_NEAREST,
+                GL_CLAMP_TO_EDGE);
 
         if (directBayer && !directRgbCarrier) {
             throw new IllegalStateException(
@@ -170,11 +121,10 @@ public final class MotionV2CfaInput extends Node {
                 GL_CLAMP_TO_EDGE);
         basePipeline.texnum = 0;
 
-        Log.d(Name, "IRIS_26699_V2_RGB_INPUT"
+        Log.d(Name, "IRIS_26501_V2_FLOAT32_RGB_INPUT"
                 + " carrier=" + (directRgbCarrier ? "properPerFrameCameraRgbFullRes" : "packedCfaHalfRes")
-                + " transferFormat=" + (directHalfRgbCarrier ? "rgba16f_direct" : "rgba32f")
-                + " bytesPerChannel=" + (directHalfRgbCarrier ? 2 : 4)
-                + " cpuCarrierReleasedAfterUpload=true"
+                + " transferFormat=rgba32f"
+                + " bytesPerChannel=4"
                 + " rgbPingPong=" + raw.x + "x" + raw.y
                 + " main1=true main2=true main3=true"
                 + " texnumReset=0"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
index 3a08d25265145fe01cd01a278f5fe17095f9488d..d51cc93373b3d00e8d38ebff0b6555fe685ea096 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
@@ -141,14 +141,14 @@ public class StageTelemetry extends Node {
     public void Run() {
         GLTexture source = previousNode == null ? null : previousNode.WorkingTexture;
         WorkingTexture = source;
-        // IRIS_26699_STAGE_TELEMETRY_REDUNDANT_BARRIER_REMOVED
-        // The node is a pure pass-through. Its former proof readbacks are gone and all following
-        // GPU work is ordered in this same context, so an explicit full-GPU drain is redundant.
+        // IRIS_26698_PRODUCTION_TELEMETRY_IMAGE_WORK_DISABLED
+        // Preserve the exact pass-through node and a conservative GPU completion boundary.
         if (!basePipeline.mParameters.motionV2Active || source == null) return;
-        String message = "IRIS_26699_STAGE_TELEMETRY_REDUNDANT_BARRIER_REMOVED"
+        android.opengl.GLES20.glFinish();
+        String message = "IRIS_26698_STAGE_TELEMETRY_IMAGE_WORK_DISABLED"
                 + " stage=" + stage
                 + " histogramReadback=false chromaProbeReadback=false"
-                + " explicitCompletionBoundary=false sameTexturePassThrough=true";
+                + " explicitCompletionBoundary=true sameTexturePassThrough=true";
         Log.i("IRIS26698", message);
         try { com.particlesdevs.photoncamera.util.MotionTrace.processingState("STAGE_TELEMETRY_26698", message); }
         catch (Throwable ignored) {}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index 935cab6f60a9c2cc4f2d0ac9e51add14d0808a54..9d99412505f32760401ab73d36199ebe51810d4d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -636,22 +636,16 @@ object PhotonMotionMgc1271Bridge {
                 }
             }
             forceOpaqueHalfAlpha(denoiseBuffer, size.x, size.y)
-            /* IRIS_26699_MOTION_DIRECT_RGBA16F_HANDOFF
-             * Keep the exact accepted CPU RGBA16F residual-denoise result. Standard Motion now
-             * transfers those half-float bytes directly to PostPipeline, avoiding the old
-             * half->GPU->float32 CPU conversion. Night deliberately retains its proven RGBA32F
-             * parity bridge because its dedicated input contract is separate.
+            /* IRIS_26548_NIGHT_RGBA32F_MOTION_PARITY_HANDOFF
+             * 26547 proved that the Night-only native-half CPU handoff can reach a native status-11
+             * crash at the first PostPipeline input. GLTexture's FLOAT_16 internal format uses a
+             * GL_FLOAT client upload contract, while that Night ByteBuffer physically contains
+             * 16-bit half values. Reuse Motion's already-proven cross-context contract instead:
+             * convert the finished MGC RGBA16F texture to an RGBA32F CPU carrier inside the MGC
+             * context, then let the PostPipeline upload the exact same representation Motion uses.
+             * Reconstruction/Sabre/VGN math is unchanged.
              */
-            val directHalfMotionCarrier = !parameters.irisNightActive && cfa in 0..3
-            val output: ByteBuffer = if (directHalfMotionCarrier) {
-                denoiseBuffer
-            } else {
-                convertHalfRgbaToFloatRgba(denoiseBuffer, size.x, size.y)
-            }
-            if (directHalfMotionCarrier) {
-                PLog.i(TAG, "IRIS_26699_MOTION_DIRECT_RGBA16F_HANDOFF bytes=${output.capacity()} " +
-                    "denoiseUnchanged=true conversionRoundTrip=false clientType=GL_HALF_FLOAT")
-            }
+            val output: ByteBuffer = convertHalfRgbaToFloatRgba(denoiseBuffer, size.x, size.y)
             GLES30.glDeleteTextures(1, intArrayOf(resultTexture), 0)
             resultTexture = 0
             checkGl("delete exported MGC RGB")
@@ -762,7 +756,7 @@ object PhotonMotionMgc1271Bridge {
                 "denoise=SABRE_RESOLVE_PLUS_SABRE_DEFAULT_RESIDUAL " +
                 "baselineEv=${stacked.baselineExposureEv} " +
                 "displayGain=${parameters.motionV2DisplayGain} " +
-                "crossContext=${if (parameters.irisNightActive) "float32_rgba_cpu_night_motion_parity" else if (directHalfMotionCarrier) "half16_rgba_cpu_motion_direct" else "float32_rgba_cpu_motion"} alphaSupport=disabled")
+                "crossContext=${if (parameters.irisNightActive) "float32_rgba_cpu_night_motion_parity" else "float32_rgba_cpu_motion"} alphaSupport=disabled")
 
             return MotionV2Merger.Result(
                 output,
@@ -792,9 +786,6 @@ object PhotonMotionMgc1271Bridge {
                 stacked.true2xReconstructionMs,
             ).also { result ->
                 result.sabreSelected = true
-                /* Successful Motion Result now owns the native RGBA16F carrier until the
-                 * PostPipeline input uploads and frees it. Keep Night's float32 ownership unchanged. */
-                if (directHalfMotionCarrier) halfBuffer = null
                 true2xOutputHandedOff = resultTrue2xRenderRgbPath != null &&
                     (!produceNormalStackedDng || resultTrue2xLinearRawPath != null)
             }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index 04cf72a7910b2e205dab854a0e267eddc31beaa7..8f033db7d84def68137fd3e0b11a00fcca5eed18 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -78,30 +78,13 @@ public final class MotionV2Jpeg444Encoder {
             base=output.resolveSibling("."+output.getFileName()+".26507.base.jpg");
             gain=output.resolveSibling("."+output.getFileName()+".26507.gain.jpg");
             Files.deleteIfExists(base);Files.deleteIfExists(gain);Files.deleteIfExists(output);
-            final long timingStartNs=System.nanoTime();
-            long phaseStartNs=System.nanoTime();
-            boolean baseOk=writeNative(bitmap,base.toString(),Math.max(1,Math.min(100,quality)),isDisplayP3Bitmap(bitmap));
-            long baseEncodeMs=(System.nanoTime()-phaseStartNs)/1_000_000L;
-            if(!baseOk)return false;
+            if(!writeNative(bitmap,base.toString(),Math.max(1,Math.min(100,quality)),isDisplayP3Bitmap(bitmap)))return false;
             Bitmap contents=gm.getGainmapContents();
-            if(contents==null||contents.isRecycled())return false;
-            phaseStartNs=System.nanoTime();
-            boolean gainOk=encodeGainmapNative(contents,gain.toString(),GAINMAP_QUALITY);
-            long gainEncodeMs=(System.nanoTime()-phaseStartNs)/1_000_000L;
-            if(!gainOk)return false;
-            phaseStartNs=System.nanoTime();
-            boolean packageOk=packageJpegRNative(base.toString(),gain.toString(),output.toString(),1,
+            if(contents==null||contents.isRecycled()||!encodeGainmapNative(contents,gain.toString(),GAINMAP_QUALITY))return false;
+            boolean ok=packageJpegRNative(base.toString(),gain.toString(),output.toString(),1,
                     gm.getRatioMin(),gm.getRatioMax(),gm.getGamma(),gm.getEpsilonSdr(),gm.getEpsilonHdr(),
-                    gm.getMinDisplayRatioForHdrTransition(),gm.getDisplayRatioForFullHdr(),true);
-            long packageMs=(System.nanoTime()-phaseStartNs)/1_000_000L;
-            phaseStartNs=System.nanoTime();
-            boolean validationOk=packageOk&&isJpegRNative(output.toString());
-            long validationMs=(System.nanoTime()-phaseStartNs)/1_000_000L;
-            boolean ok=packageOk&&validationOk;
-            Log.i(TAG,"IRIS_26699_JPEGR_STAGE_TIMING baseEncodeMs="+baseEncodeMs
-                    +" gainEncodeMs="+gainEncodeMs+" packageMs="+packageMs
-                    +" validationMs="+validationMs
-                    +" totalMs="+((System.nanoTime()-timingStartNs)/1_000_000L));
+                    gm.getMinDisplayRatioForHdrTransition(),gm.getDisplayRatioForFullHdr(),true)
+                    && isJpegRNative(output.toString());
             Log.i(TAG,"IRIS_26565_DISPLAY_P3_JPEG444 encoded="+ok+" gainmap=true subsampling=444 jpegR="+ok+" gamut=DISPLAY_P3");
             return ok;
         }catch(Throwable t){Log.e(TAG,"IRIS_26507_JPEG444_FAILED",t);return false;}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
index 8c28c69c0048708d457ed8262740cb852fdb10d0..4b1ac9a89cd6ba17c20c5bd688c630f0065c98df 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
@@ -198,10 +198,6 @@ public class CameraFragment extends Fragment implements BaseActivity.BackPressed
         return captureController;
     }
 
-    void setVideoRecordingInfoVisible(boolean visible) {
-        if (mCameraUIView != null) mCameraUIView.setVideoRecordingInfoVisible(visible);
-    }
-
     public IrisZoomController getIrisZoomController() {
         return irisZoomController;
     }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIController.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIController.java
index 136916065b5027e01141e4fa6a6674db46bd2024..3d87741a6333459fa9276770d0ec9c9567f3a630 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIController.java
@@ -99,11 +99,9 @@ final class CameraUIController implements CameraUIEventsListener,
                     case VIDEO:
                         if (!cameraFragment.captureController.mIsRecordingVideo) {
                             cameraFragment.captureController.VideoStart();
-                            cameraFragment.setVideoRecordingInfoVisible(true);
                             view.setActivated(false);
                         } else {
                             cameraFragment.captureController.VideoEnd();
-                            cameraFragment.setVideoRecordingInfoVisible(false);
                             view.setActivated(true);
                             restoreVideoIdleScaleAfterStop(view);
                         }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
index 2e8017aab4c51f2a6bbaaaacc87af3b8d3499c39..51f2834e5bef3588e4ab906c8b40da3236299a3b 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
@@ -3,7 +3,6 @@ package com.particlesdevs.photoncamera.ui.camera;
 import com.particlesdevs.photoncamera.capture.CaptureController;
 
 import android.os.Bundle;
-import android.os.SystemClock;
 import android.widget.TextView;
 
 import com.particlesdevs.photoncamera.app.PhotonCamera;
@@ -78,8 +77,6 @@ public class CameraUIViewImpl implements CameraUIView {
     private final ProgressBar mProcessingProgressBar;
     private final LiquidModePicker mModePicker;
     private final TextView mVideoRecordingInfo;
-    private long iris26699VideoStartElapsedMs = -1L;
-    private Runnable iris26699VideoRecordingTicker;
     private View formatSelectorPill;
     private View formatExpandedPanel;
     private View formatJpgButton;
@@ -149,31 +146,6 @@ public class CameraUIViewImpl implements CameraUIView {
         this.mShutterButton = bottombuttons.shutterButton;
         this.mModePicker = cameraFragment.cameraFragmentBinding.layoutBottombar.modeSwitcher.modePickerView;
         this.mVideoRecordingInfo = cameraFragment.cameraFragmentBinding.getRoot().findViewById(R.id.video_recording_info);
-        this.iris26699VideoRecordingTicker = () -> {
-            boolean active = displayedMode == CameraMode.VIDEO
-                    && cameraFragment.captureController != null
-                    && cameraFragment.captureController.mIsRecordingVideo;
-            if (!active || mVideoRecordingInfo == null) {
-                iris26699VideoStartElapsedMs = -1L;
-                if (mVideoRecordingInfo != null && displayedMode != CameraMode.RAWVIDEO) {
-                    mVideoRecordingInfo.setText("");
-                    mVideoRecordingInfo.setAlpha(0.0f);
-                    mVideoRecordingInfo.setVisibility(View.GONE);
-                }
-                return;
-            }
-            if (iris26699VideoStartElapsedMs < 0L) {
-                iris26699VideoStartElapsedMs = SystemClock.elapsedRealtime();
-            }
-            long elapsedMs = Math.max(0L, SystemClock.elapsedRealtime() - iris26699VideoStartElapsedMs);
-            long totalSeconds = elapsedMs / 1000L;
-            mVideoRecordingInfo.setText(String.format(java.util.Locale.US, "%02d:%02d",
-                    totalSeconds / 60L, totalSeconds % 60L));
-            mVideoRecordingInfo.setAlpha(1.0f);
-            mVideoRecordingInfo.setVisibility(View.VISIBLE);
-            long delayMs = Math.max(100L, 1000L - (elapsedMs % 1000L));
-            mVideoRecordingInfo.postDelayed(iris26699VideoRecordingTicker, delayMs);
-        };
         this.initListeners();
         this.initModeSwitcher();
         this.initLiquidUi();
@@ -1617,21 +1589,6 @@ public class CameraUIViewImpl implements CameraUIView {
     public void setVideoRecordingInfoVisible(boolean visible) {
         if (mVideoRecordingInfo != null) {
             mVideoRecordingInfo.post(() -> {
-                if (displayedMode == CameraMode.VIDEO) {
-                    mVideoRecordingInfo.removeCallbacks(iris26699VideoRecordingTicker);
-                    if (visible
-                            && cameraFragment.captureController != null
-                            && cameraFragment.captureController.mIsRecordingVideo) {
-                        iris26699VideoStartElapsedMs = SystemClock.elapsedRealtime();
-                        iris26699VideoRecordingTicker.run();
-                    } else {
-                        iris26699VideoStartElapsedMs = -1L;
-                        mVideoRecordingInfo.setText("");
-                        mVideoRecordingInfo.setAlpha(0.0f);
-                        mVideoRecordingInfo.setVisibility(View.GONE);
-                    }
-                    return;
-                }
                 boolean allowVisible =
                         visible
                                 && displayedMode == CameraMode.RAWVIDEO
@@ -1649,14 +1606,12 @@ public class CameraUIViewImpl implements CameraUIView {
                 }
             });
         }
+
+
     }
 
     @Override
     public void destroy() {
-        if (mVideoRecordingInfo != null && iris26699VideoRecordingTicker != null) {
-            mVideoRecordingInfo.removeCallbacks(iris26699VideoRecordingTicker);
-        }
-        iris26699VideoStartElapsedMs = -1L;
         topbar = null;
         bottombuttons = null;
     }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/SurfaceViewOverViewfinder.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/SurfaceViewOverViewfinder.java
index 119973430a263c8ec3843778c4731a77a2abe67b..58649995d0f0df970d8c57e343422c9b7683cf44 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/SurfaceViewOverViewfinder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/SurfaceViewOverViewfinder.java
@@ -37,8 +37,7 @@ public class SurfaceViewOverViewfinder extends SurfaceView {
 
     private void initPaints() {
         whitePaint.setColor(Color.WHITE);
-        whitePaint.setAlpha(153); // IRIS_26699_GRID_VISIBILITY: 60% visible / 40% transparent.
-        whitePaint.setStrokeWidth(1.0f);
+        whitePaint.setStrokeWidth(1.5f);
 
         textPaint.setColor(Color.WHITE);
         textPaint.setTextSize(25);
diff --git a/app/src/main/res/layout/camera_fragment.xml b/app/src/main/res/layout/camera_fragment.xml
index 15677bba1265df48f845034af56f6b9cd769e0d1..c9f1b32dfb4df17ac4450ed6ca0a8a3be0ec7a4c 100644
--- a/app/src/main/res/layout/camera_fragment.xml
+++ b/app/src/main/res/layout/camera_fragment.xml
@@ -315,8 +315,8 @@
                 android:id="@+id/video_recording_info"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
-                android:layout_marginTop="4dp"
-                android:layout_marginEnd="8dp"
+                android:layout_marginTop="12dp"
+                android:layout_marginEnd="12dp"
                 android:textColor="@android:color/white"
                 android:textSize="13sp"
                 android:fontFamily="monospace"
@@ -326,7 +326,7 @@
                 android:paddingTop="4dp"
                 android:paddingBottom="4dp"
                 android:visibility="gone"
-                app:layout_constraintTop_toBottomOf="@id/iris_live_histogram"
+                app:layout_constraintTop_toBottomOf="@id/top_black_shell"
                 app:layout_constraintEnd_toEndOf="parent" />
 
     </androidx.constraintlayout.widget.ConstraintLayout>
diff --git a/app/version.properties b/app/version.properties
index f58ca58803c1c859e7423dddac377202fa49cb5e..281069d0ec69ff24379d5c3634fc61685ff973b6 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726699
-VERSION_BUILD=26699
+VERSION_NAME=0.9726698
+VERSION_BUILD=26698
