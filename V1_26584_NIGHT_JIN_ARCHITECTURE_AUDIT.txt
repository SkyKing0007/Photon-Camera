26584 V1 Night/Jin high-risk architecture audit

MATH / DOMAIN
- Input to Jin remains the completed Night SDR render converted to sRGB when necessary and resized to 512x512.
- Pinned Jin ONNX input normalization [-1,1], output denormalization and dense RGB output-minus-input residual are unchanged.
- New adapter operates only on Jin's interleaved 512x512x3 RGB residual; it does not alter Sabre/VGN linear RGB, DNG, gain-map source, or the common Iris render shader.
- Global per-channel proposal mean is removed fail-closed: if centering would reverse a component sign, component becomes zero; centered magnitude can never exceed the model proposal.
- Residual low/high spatial split is 3x3 at Jin guide resolution only. Low/high luma and chroma are separately capped, then guide-detail gating can only reduce the proposal further.
- Highlight gates only relax cleanup caps; they cannot amplify beyond the already-centered Jin proposal.

OWNERSHIP / ROUTING
- Iris common finishing remains photograph owner: MotionV2ColorTransform -> ViewfinderExposureMatcher -> AdaptiveColorAppearance -> DisplayExposure -> MotionV2Render.
- Night +0.30..0.40 EV visibility advantage is unchanged.
- Jin remains enabled after completed Iris render, but owns cleanup residual only; broad global exposure/white-balance/color style is explicitly rejected.
- Normal mode never calls this Night enhancer and the entire successful 26583 tone owner set remains byte-identical.
- Night true2x receives the same constrained residual produced by the one Jin inference; no second model inference or alternate style owner is introduced.

DETAIL / COLOR / HIGHLIGHT INVARIANTS
- Four-neighbor 512 guide luma/RGB structure produces detailGate; stronger structure reduces Jin residual.
- Existing native full-resolution Sabre-guided transfer remains byte-identical and can further suppress cross-edge transfer.
- Smooth neutral shadows retain only bounded chroma-cleanup latitude.
- Supported highlights retain bounded extra cleanup latitude for highlight recovery.
- No independent tone-map channel owner, no saturation owner, no hue reconstruction and no per-channel global gain are introduced.
- Existing 26581 false-color/background-gap/SR material-edge protections remain byte-identical.

LIFECYCLE / FAILURE
- OrtEnvironment/session creation, model prewarm, pinned CPU threading and model-file lifecycle are unchanged.
- Any setup/inference/adapter/native failure returns false and leaves the already-rendered multiframe base unchanged.
- Existing Bitmap cleanup/recycle paths remain unchanged.

MEMORY / PERFORMANCE
- Existing input float buffer: 3*512*512 floats (~3 MiB), unchanged.
- Existing dense residual: 3*512*512 floats (~3 MiB), unchanged.
- New temporary low-pass buffer: 3*512*512 floats (~3 MiB).
- Small constant/scratch arrays are method-local and bounded; CLEANUP_DX/DY are static to avoid per-pixel neighbor-array allocation.
- No new full-resolution Java bitmap/float carrier, native allocation, PBO, GL texture, SSBO or GPU fence is added.
- Added compute is O(512*512*9) low-pass plus O(512*512*4) guide-edge/cap work; no native-resolution CPU scan is added.

NATIVE / GPU / PUBLICATION
- motionv2_jpeg444_jni.cpp remains byte-identical SHA-256 d73cd24452280958f089e9124545a81461939e8fa0c9c95272b2825942e4b43d.
- No JNI signature changes.
- Device-proven GPU publication / ASYNC_FENCE mechanics remain unchanged.
- TurboJPEG 4:4:4 Night/Normal publication path remains unchanged.

CONCLUSION
- 26584 intentionally changes only Jin residual ownership/strength at 512x512 ; successful 26583 projected tone is inherited byte-identical.
- Night brightness/capture/Sabre/model/native transfer/publication remain frozen.
