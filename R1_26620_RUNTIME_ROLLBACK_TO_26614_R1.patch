diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl
deleted file mode 100644
index d133eef73d84c34b55b3af8677800fdd181d408c..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl
+++ /dev/null
@@ -1,102 +0,0 @@
-precision highp float;
-precision mediump sampler2D;
-
-uniform sampler2D Pyramid0;
-uniform sampler2D Pyramid1;
-uniform sampler2D Pyramid2;
-uniform sampler2D Pyramid3;
-uniform sampler2D Pyramid4;
-uniform sampler2D Pyramid5;
-uniform sampler2D Pyramid6;
-uniform sampler2D Pyramid7;
-out float Output;
-
-/* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_PRESENTATION
- * Work only in the scalar log-intensity guide. The coarsest illumination base
- * remains the exact 26614 global-tone result. Finer Laplacian bands may move
- * continuously toward their source contrast only when the global map compressed
- * them, only while their amplitude is in the detail/medium-structure regime,
- * and never beyond source contrast. This mirrors the Local-Laplacian principle
- * alpha=1 (detail preservation) while leaving large edges to the global tone map.
- * There is no semantic classifier, tile atlas, fine-structure amplification, or
- * per-channel color operation. */
-const float IRIS_SIGMA_DETAIL_EV = 1.32192809489; /* log2(2.5), paper-style range scale */
-
-vec2 levelSampleAligned(sampler2D tex,vec2 level0Coord,float scale){
-    vec2 sz=vec2(textureSize(tex,0));
-    vec2 coord=level0Coord*scale;
-    vec2 uv=(coord+vec2(0.5))/max(sz,vec2(1.0));
-    vec2 lo=vec2(0.5)/max(sz,vec2(1.0));
-    return texture(tex,clamp(uv,lo,vec2(1.0)-lo)).rg;
-}
-
-float preserveCompressedBand(float mappedBand,float sourceBand,float scaleWeight){
-    float am=abs(mappedBand);
-    float as=abs(sourceBand);
-    float compression=max(as-am,0.0);
-    float denom=max(am*as,1.0e-6);
-    float signAgreement=(mappedBand*sourceBand)/denom;
-    float signGate=smoothstep(0.75,0.98,signAgreement);
-    float compressionGate=smoothstep(0.02,0.15,compression);
-    float detailGate=1.0-smoothstep(0.75*IRIS_SIGMA_DETAIL_EV,
-                                    1.25*IRIS_SIGMA_DETAIL_EV,as);
-    float blend=clamp(scaleWeight*signGate*compressionGate*detailGate,0.0,1.0);
-    return mix(mappedBand,sourceBand,blend);
-}
-
-float softLimit(float x,float limitValue){
-    float a=abs(x);
-    return x/(1.0+a/max(limitValue,1.0e-6));
-}
-
-void main(){
-    /* Every reduce pass is phase-anchored at input index 2*q. Reconstruct all
-     * coarser levels at that same level-0 sample location; using one normalized
-     * UV for unequal/ceil-sized levels would introduce a half-pixel pyramid drift. */
-    vec2 level0Coord=gl_FragCoord.xy-vec2(0.5);
-    vec2 p0=levelSampleAligned(Pyramid0,level0Coord,1.0);
-    vec2 p1=levelSampleAligned(Pyramid1,level0Coord,0.5);
-    vec2 p2=levelSampleAligned(Pyramid2,level0Coord,0.25);
-    vec2 p3=levelSampleAligned(Pyramid3,level0Coord,0.125);
-    vec2 p4=levelSampleAligned(Pyramid4,level0Coord,0.0625);
-    vec2 p5=levelSampleAligned(Pyramid5,level0Coord,0.03125);
-    vec2 p6=levelSampleAligned(Pyramid6,level0Coord,0.015625);
-    vec2 p7=levelSampleAligned(Pyramid7,level0Coord,0.0078125);
-
-    float source0=p0.r;
-    float mapped0=p0.g;
-    float outLog=p7.g;
-
-    /* Approximate source-pixel bands: 128-256, 64-128, 32-64, 16-32,
-     * 8-16, 4-8 and 2-4. The finest band stays exactly global-tone mapped;
-     * the coarsest >~256 px illumination remains the global exposure anchor. */
-    outLog+=preserveCompressedBand(p6.g-p7.g,p6.r-p7.r,0.84);
-    outLog+=preserveCompressedBand(p5.g-p6.g,p5.r-p6.r,0.78);
-    outLog+=preserveCompressedBand(p4.g-p5.g,p4.r-p5.r,0.68);
-    outLog+=preserveCompressedBand(p3.g-p4.g,p3.r-p4.r,0.54);
-    outLog+=preserveCompressedBand(p2.g-p3.g,p2.r-p3.r,0.38);
-    outLog+=preserveCompressedBand(p1.g-p2.g,p1.r-p2.r,0.18);
-    outLog+=preserveCompressedBand(p0.g-p1.g,p0.r-p1.r,0.00);
-
-    float correctionEv=outLog-mapped0;
-    /* Deep shadows remain on the exact 26614 global map. The transition is
-     * smooth in rendered luminance and introduces no black offset. */
-    float mappedGuide=exp2(mapped0);
-    float shadowPreserve=smoothstep(0.020,0.080,mappedGuide);
-    correctionEv*=shadowPreserve;
-    /* IRIS_26620_BLACK_CRUSH_PROTECTION
-     * Negative local contrast restoration is introduced more conservatively through
-     * the lower rendered body so the multiscale stage cannot deepen 26614 shadows
-     * abruptly. Positive recovery keeps the original symmetric floor gate. */
-    if(correctionEv<0.0){
-        correctionEv*=smoothstep(0.080,0.200,mappedGuide);
-    }
-    /* Symmetric smooth bound; zero stays exactly zero and no hard threshold
-     * exists in source brightness or highlight position. */
-    correctionEv=softLimit(correctionEv,0.55);
-    if(!isnan(source0) && !isinf(source0) && !isnan(correctionEv) && !isinf(correctionEv)){
-        Output=correctionEv;
-    }else{
-        Output=0.0;
-    }
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl
deleted file mode 100644
index 1892f6e07d728b4791d4a170623807e3892198a2..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl
+++ /dev/null
@@ -1,35 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D InputPyramid;
-out vec2 Output;
-
-/* IRIS_26620_LOCAL_LAPLACIAN_GAUSSIAN_DOWNSAMPLE
- * Centered 5x5 binomial Gaussian reduction before every 2:1 pyramid step.
- * R/G remain source/global log-guide pairs at identical spatial phases. */
-vec2 pairAt(ivec2 p){
-    ivec2 sz=textureSize(InputPyramid,0);
-    return texelFetch(InputPyramid,clamp(p,ivec2(0),sz-ivec2(1)),0).rg;
-}
-
-float kernel5(int o){
-    int a=abs(o);
-    if(a==0)return 6.0;
-    if(a==1)return 4.0;
-    return 1.0;
-}
-
-void main(){
-    ivec2 q=ivec2(gl_FragCoord.xy);
-    ivec2 center=q*2;
-    vec2 sum=vec2(0.0);
-    for(int oy=-2;oy<=2;oy++){
-        float wy=kernel5(oy);
-        for(int ox=-2;ox<=2;ox++){
-            float wx=kernel5(ox);
-            sum+=pairAt(center+ivec2(ox,oy))*(wx*wy);
-        }
-    }
-    Output=sum*(1.0/256.0);
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl
deleted file mode 100644
index 17c33173765c4a1f1dd3e0b7b7079c03e96a0329..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl
+++ /dev/null
@@ -1,68 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D InputBuffer;
-uniform float displayGain;
-uniform float outputExposureScale;
-out vec2 Output;
-
-/* IRIS_26620_LOCAL_LAPLACIAN_SEED
- * First Gaussian level at half resolution. R stores log2(source guide), G stores
- * log2(the exact 26614 globally mapped guide). The centered 5x5 binomial kernel
- * is a standard Gaussian-pyramid reduce filter; there is no tile or coarse-map owner. */
-const vec3 LUMA = vec3(0.22897456,0.69173852,0.07928691);
-const float LOG_FLOOR = 0.000244140625; /* 2^-12 */
-
-float lumaOf(vec3 c){return dot(c,LUMA);}
-float max3(vec3 c){return max(c.r,max(c.g,c.b));}
-float guideOf(vec3 c){c=max(c,vec3(0.0));return max(lumaOf(c),max3(c));}
-
-float iris26614MapMotionSdrFinalGuide(float sourceGuide){
-    float x=max(sourceGuide,0.0);
-    float requestedFinalGain=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
-    float whiteAnchor=min(0.95,requestedFinalGain);
-    float bodyGain=requestedFinalGain;
-    if(requestedFinalGain>whiteAnchor){
-        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
-    }
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-    }
-    float reserve=1.0-whiteAnchor;
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(whiteAnchor,1.0e-6);
-    float excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
-}
-
-vec2 logPairAt(ivec2 p){
-    ivec2 sz=textureSize(InputBuffer,0);
-    p=clamp(p,ivec2(0),sz-ivec2(1));
-    float sourceGuide=guideOf(texelFetch(InputBuffer,p,0).rgb);
-    float mappedGuide=iris26614MapMotionSdrFinalGuide(sourceGuide);
-    return vec2(log2(max(sourceGuide,LOG_FLOOR)),
-                log2(max(mappedGuide,LOG_FLOOR)));
-}
-
-float kernel5(int o){
-    int a=abs(o);
-    if(a==0)return 6.0;
-    if(a==1)return 4.0;
-    return 1.0;
-}
-
-void main(){
-    ivec2 q=ivec2(gl_FragCoord.xy);
-    ivec2 center=q*2;
-    vec2 sum=vec2(0.0);
-    for(int oy=-2;oy<=2;oy++){
-        float wy=kernel5(oy);
-        for(int ox=-2;ox<=2;ox++){
-            float wx=kernel5(ox);
-            sum+=logPairAt(center+ivec2(ox,oy))*(wx*wy);
-        }
-    }
-    Output=sum*(1.0/256.0);
-}
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 028a5adc874095494b706aa29d7b3374ac37865f..b9578215a51538ebcfcbdf5ca01f8631135b1053 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -8,8 +8,6 @@ uniform float irisOutputZoom;
 uniform int iris26592MotionHdrHandoff;
 uniform float displayGain;
 uniform float iris26604MotionSdrKneeFinal;
-uniform sampler2D iris26620LocalLaplacianCorrection;
-uniform int iris26620LocalLaplacianEnabled;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
 
@@ -197,7 +195,6 @@ void main() {
     ivec2 xy=ivec2(gl_FragCoord.xy);
     ivec2 sourceSize=textureSize(InputBuffer,0);
     ivec2 sourceXY=xy;
-    vec2 sourcePixel=vec2(sourceXY);
     vec3 linearSrgb;
     float zoom=max(irisOutputZoom,1.0);
     if(zoom<=1.00001) {
@@ -205,11 +202,10 @@ void main() {
          * Exact tested 26523 1x path remains unchanged.
          */
         if(sourceXY.x==0 && sourceSize.x>1) sourceXY.x=1;
-        sourcePixel=vec2(sourceXY);
         linearSrgb=max(texelFetch(InputBuffer,sourceXY,0).rgb,vec3(0.0));
     } else {
         vec2 center=(vec2(sourceSize)-vec2(1.0))*0.5;
-        sourcePixel=center+(vec2(xy)-center)/zoom;
+        vec2 sourcePixel=center+(vec2(xy)-center)/zoom;
         linearSrgb=max(iris26524BilinearInput(sourcePixel),vec3(0.0));
         sourceXY=ivec2(clamp(floor(sourcePixel+vec2(0.5)),
                             vec2(0.0),vec2(sourceSize-ivec2(1))));
@@ -221,30 +217,6 @@ void main() {
      */
     linearSrgb=mapExtendedLinearHeadroom(linearSrgb);
 
-    /* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_FINAL_OWNER
-     * The exact 26614 global tone map above consumes motionV2DisplayGain/outputExposureScale once.
-     * A half-resolution correction field reconstructed from seven Gaussian/Laplacian scales then
-     * restores only contrast that the global map compressed. Apply one RGB scalar after global
-     * mapping so hue/channel ratios are unchanged and no second exposure owner is introduced. */
-    if(iris26620LocalLaplacianEnabled!=0){
-        vec2 correctionSize=vec2(textureSize(iris26620LocalLaplacianCorrection,0));
-        /* Pyramid0 is decimated at source index 2*q, so sample in that exact
-         * phase instead of mapping by normalized source dimensions. */
-        vec2 correctionCoord=sourcePixel*0.5;
-        vec2 uv=(correctionCoord+vec2(0.5))/max(correctionSize,vec2(1.0));
-        vec2 halfTexel=vec2(0.5)/max(correctionSize,vec2(1.0));
-        uv=clamp(uv,halfTexel,vec2(1.0)-halfTexel);
-        float correctionEv=clamp(texture(iris26620LocalLaplacianCorrection,uv).r,-0.55,0.55);
-        /* Local presentation may restore compressed contrast, but it may not
-         * create a new display-gamut clip that was absent after the 26614 map. */
-        float mappedPeak=max3(linearSrgb);
-        if(correctionEv>0.0){
-            float availableUpEv=mappedPeak>1.0e-7 ? max(log2(1.0/mappedPeak),0.0) : 0.0;
-            correctionEv=min(correctionEv,availableUpEv);
-        }
-        linearSrgb*=exp2(correctionEv);
-    }
-
     /* IRIS_26604_FINAL_DOMAIN_TONE
      * outputExposureScale and Motion brightness target are already consumed by mapFinalSdrGuide;
      * there is no second image multiplier after the canonical tone owner.
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index fef09b9a3783099ed226a2752525c1c9d225fc0a..1c4812c0f6f278177a1dc88c5b780747737e1dce 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -504,35 +504,9 @@ struct Params {
     bool mirror=false;
     bool motionHdrHandoff=false;
     float residualZoom=1.f,displayGain=1.f,exposureEv=0.f,shadows=0.f,contrast=0.f,sceneWhite=1.f;
-    const uint16_t* localLaplacianMap=nullptr;
-    int localLaplacianW=0,localLaplacianH=0,localLaplacianSourceW=0,localLaplacianSourceH=0;
     float sensorToProfile[9]{};
     float profileToDisplay[9]{};
 };
-/* IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN_MAP
- * Sample the exact half-resolution R16F correction field produced by 1x MotionV2Render.
- * Coordinates are scene/source aligned before crop/rotation, so 1x and true-2x share one
- * spatial presentation decision while native SR detail remains independently reconstructed. */
-inline float localLaplacianEv(const Params&p,float trueX,float trueY){
-    if(!p.motionHdrHandoff||!p.localLaplacianMap||p.localLaplacianW<=0||p.localLaplacianH<=0||p.localLaplacianSourceW<=0||p.localLaplacianSourceH<=0)return 0.f;
-    float oneX=(trueX+0.5f)*(float)p.localLaplacianSourceW/(float)std::max(p.trueW,1)-0.5f;
-    float oneY=(trueY+0.5f)*(float)p.localLaplacianSourceH/(float)std::max(p.trueH,1)-0.5f;
-    /* Pyramid0 is generated by the exact 2:1 phase q -> source index 2*q. */
-    float mx=oneX*0.5f;
-    float my=oneY*0.5f;
-    mx=clampf(mx,0.f,(float)std::max(0,p.localLaplacianW-1));
-    my=clampf(my,0.f,(float)std::max(0,p.localLaplacianH-1));
-    int x0=(int)std::floor(mx),y0=(int)std::floor(my);
-    int x1=std::min(x0+1,p.localLaplacianW-1),y1=std::min(y0+1,p.localLaplacianH-1);
-    float fx=mx-x0,fy=my-y0;
-    auto at=[&](int x,int y){
-        float v=iris26564::halfToFloat(p.localLaplacianMap[(size_t)y*p.localLaplacianW+x]);
-        return std::isfinite(v)?clampf(v,-0.55f,0.55f):0.f;
-    };
-    float a=at(x0,y0)+(at(x1,y0)-at(x0,y0))*fx;
-    float b=at(x0,y1)+(at(x1,y1)-at(x0,y1))*fx;
-    return a+(b-a)*fy;
-}
 inline Vec3 mat(const float*m,Vec3 v){
     return {m[0]*v.r+m[1]*v.g+m[2]*v.b,
             m[3]*v.r+m[4]*v.g+m[5]*v.b,
@@ -632,20 +606,12 @@ inline float iris26614MapMotionSdrFinalGuide(float sourceGuide,float displayGain
     float tailScale=reserve/std::max(whiteAnchor,1.0e-6f),excess=x-1.f;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float localCorrectionEv){
+inline Vec3 renderHeadroom(Vec3 rgb,const Params&p){
     rgb=clampNonnegative(rgb);float y=std::max(luma(rgb),0.f),pk=peak(rgb),guide=std::max(y,pk);
     /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_CPU */
     if(p.motionHdrHandoff){
         float mappedFinal=iris26614MapMotionSdrFinalGuide(guide,p.displayGain);
         if(guide>1.0e-7f)rgb=mul(rgb,mappedFinal/guide);
-        /* IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_AFTER_GLOBAL */
-        float correctionEv=clampf(localCorrectionEv,-0.55f,0.55f);
-        if(correctionEv>0.f){
-            float mappedPeak=peak(rgb);
-            float availableUpEv=mappedPeak>1.0e-7f?std::max(std::log2(1.f/mappedPeak),0.f):0.f;
-            correctionEv=std::min(correctionEv,availableUpEv);
-        }
-        rgb=mul(rgb,std::exp2(correctionEv));
     }else{
         const float start=0.50f;
         if(guide>1.0e-7f&&guide>start){float white=std::max(p.sceneWhite,start+0.05f);float u=std::max((guide-start)/std::max(white-start,1.0e-6f),0.f);float x=clampf(u,0.f,1.f);float shaped=std::log(1.f+6.f*x)/std::log(7.f);float mapped=start+(1.25f-start)*shaped;rgb=mul(rgb,mapped/guide);}
@@ -701,13 +667,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
     crx*=tw/th;crx/=((float)w.w/(float)w.h);crx*=1.025f;crx*=15.f;cry=(cry+1.f/15.f)*15.f;if(crx<0.f||cry<0.f)return false;*u=crx;*v=cry;return true;
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localEv=localLaplacianEv(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localEv);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localEv=localLaplacianEv(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localEv);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
@@ -789,7 +755,6 @@ precision highp usampler2D;
 /* IRIS_26571_TRUE2X_PUBLICATION_COMPUTE */
 layout(local_size_x=16,local_size_y=8,local_size_z=1) in;
 layout(binding=0) uniform highp sampler2D uSource;
-uniform highp sampler2D uLocalLaplacianMap;
 layout(rgba8ui,binding=0) writeonly uniform highp uimage2D uOutput;
 layout(std430,binding=1) readonly buffer IrisGammaLut { float gammaLut[]; };
 
@@ -811,9 +776,6 @@ uniform float uShadows;
 uniform float uContrast;
 uniform float uSceneWhite;
 uniform int uMotionHdrHandoff;
-uniform int uLocalLaplacianEnabled;
-uniform ivec2 uLocalLaplacianSize;
-uniform ivec2 uLocalLaplacianSourceSize;
 uniform float uGainMax;
 uniform float uGainContentMax;
 uniform vec3 uSensorRow0;
@@ -930,16 +892,6 @@ vec2 irisOutputToSource(ivec2 op){
     float sx=cx+(ux-cx)/uResidualZoom,sy=cy+(uy-cy)/uResidualZoom;
     return clamp(2.0*(vec2(sx,sy)+vec2(0.5))-vec2(0.5),vec2(0.0),vec2(uTrueSize-ivec2(1)));
 }
-/* IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_GPU_SHARED_FIELD */
-float irisLocalLaplacianEv(vec2 trueSource){
-    if(uLocalLaplacianEnabled==0)return 0.0;
-    vec2 oneSource=(trueSource+vec2(0.5))*vec2(uLocalLaplacianSourceSize)/max(vec2(uTrueSize),vec2(1.0))-vec2(0.5);
-    vec2 mapCoord=oneSource*0.5;
-    vec2 uv=(mapCoord+vec2(0.5))/max(vec2(uLocalLaplacianSize),vec2(1.0));
-    vec2 texel=vec2(0.5)/max(vec2(uLocalLaplacianSize),vec2(1.0));
-    uv=clamp(uv,texel,vec2(1.0)-texel);
-    return clamp(texture(uLocalLaplacianMap,uv).r,-0.55,0.55);
-}
 float iris26614MapMotionSdrFinalGuide(float sourceGuide){
     const float scale=0.80;
     float x=max(sourceGuide,0.0);
@@ -952,20 +904,13 @@ float iris26614MapMotionSdrFinalGuide(float sourceGuide){
     float tailScale=reserve/max(whiteAnchor,1.0e-6),excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-vec3 irisHeadroom(vec3 rgb,float localCorrectionEv){
+vec3 irisHeadroom(vec3 rgb){
     rgb=irisClampNonnegative(rgb);
     float y=max(irisLuma(rgb),0.0),pk=irisPeak(rgb),guide=max(y,pk);
     /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_GPU */
     if(uMotionHdrHandoff!=0){
         float mappedFinal=iris26614MapMotionSdrFinalGuide(guide);
         if(guide>1.0e-7)rgb*=mappedFinal/guide;
-        float correctionEv=clamp(localCorrectionEv,-0.55,0.55);
-        if(correctionEv>0.0){
-            float mappedPeak=irisPeak(rgb);
-            float availableUpEv=mappedPeak>1.0e-7?max(log2(1.0/mappedPeak),0.0):0.0;
-            correctionEv=min(correctionEv,availableUpEv);
-        }
-        rgb*=exp2(correctionEv);
     }else{
         float start=0.50;
         if(guide>1.0e-7&&guide>start){float white=max(uSceneWhite,start+0.05);float u=max((guide-start)/max(white-start,1.0e-6),0.0);float x=clamp(u,0.0,1.0);float shaped=log(1.0+6.0*x)/log(7.0);float mapped=start+(1.25-start)*shaped;rgb*=mapped/guide;}
@@ -983,10 +928,8 @@ void main(){
     ivec2 local=ivec2(gl_GlobalInvocationID.xy);
     if(local.x>=uOutputSize.x||local.y>=uBandH)return;
     ivec2 op=ivec2(local.x,uBandTop+local.y);
-    vec2 trueSource=irisOutputToSource(op);
-    vec3 hdr=irisSamplePreRender(trueSource);
-    float localEv=irisLocalLaplacianEv(trueSource);
-    vec3 sdr=irisHeadroom(hdr,localEv);
+    vec3 hdr=irisSamplePreRender(irisOutputToSource(op));
+    vec3 sdr=irisHeadroom(hdr);
     uint gainCode=0u;
     if(uGenerateGain!=0){
         const float off=0.015625;float safeMax=max(uGainMax,1.001);
@@ -1036,20 +979,12 @@ public:
         if(maxTex<std::max(outWidth,std::max(p.trueW,p.trueH))){set(reason,"GL_MAX_TEXTURE_SIZE");return false;}
         std::string compileError;program=iris26571Program(&compileError);if(!program){set(reason,("compute:"+compileError).c_str());return false;}
         glGenTextures(1,&sourceTex);glBindTexture(GL_TEXTURE_2D,sourceTex);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);
-        if(p.motionHdrHandoff&&p.localLaplacianMap&&p.localLaplacianW>0&&p.localLaplacianH>0){
-            glGenTextures(1,&localLaplacianTex);glActiveTexture(GL_TEXTURE1);glBindTexture(GL_TEXTURE_2D,localLaplacianTex);
-            glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_LINEAR);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_LINEAR);
-            glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);
-            glTexStorage2D(GL_TEXTURE_2D,1,GL_R16F,p.localLaplacianW,p.localLaplacianH);
-            glTexSubImage2D(GL_TEXTURE_2D,0,0,0,p.localLaplacianW,p.localLaplacianH,GL_RED,GL_HALF_FLOAT,p.localLaplacianMap);
-            glActiveTexture(GL_TEXTURE0);
-        }
         glGenTextures(1,&outputTex);glBindTexture(GL_TEXTURE_2D,outputTex);glTexStorage2D(GL_TEXTURE_2D,1,GL_RGBA8UI,outWidth,maxBandRows);
         glGenFramebuffers(1,&fbo);glBindFramebuffer(GL_FRAMEBUFFER,fbo);glFramebufferTexture2D(GL_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_TEXTURE_2D,outputTex,0);if(glCheckFramebufferStatus(GL_FRAMEBUFFER)!=GL_FRAMEBUFFER_COMPLETE){set(reason,"RGBA8UI framebuffer");return false;}
         glGenBuffers(1,&lutSsbo);glBindBuffer(GL_SHADER_STORAGE_BUFFER,lutSsbo);glBufferData(GL_SHADER_STORAGE_BUFFER,(GLsizeiptr)(lut.v.size()*sizeof(float)),lut.v.data(),GL_STATIC_DRAW);
         const size_t maxBytes=(size_t)outWidth*maxBandRows*4;glGenBuffers(2,pbo);for(int i=0;i<2;i++){glBindBuffer(GL_PIXEL_PACK_BUFFER,pbo[i]);glBufferData(GL_PIXEL_PACK_BUFFER,(GLsizeiptr)maxBytes,nullptr,GL_STREAM_READ);}
         glBindBuffer(GL_PIXEL_PACK_BUFFER,0);GLenum err=glGetError();if(err!=GL_NO_ERROR){set(reason,"GL allocation");return false;}
-        glUseProgram(program);glUniform1i(loc("uSource"),0);glUniform1i(loc("uLocalLaplacianMap"),1);setParams();return true;
+        glUseProgram(program);glUniform1i(loc("uSource"),0);setParams();return true;
     }
     bool renderReadback(int fd,float gainMax,float gainContentMax,bool generateGain,int top,int h,int slot,Iris26571ReadyBand*out,Iris26571GpuTiming*timing,std::string*reason){
         if(!params||slot<0||slot>1){set(reason,"render_invalid_state");return false;}
@@ -1075,20 +1010,20 @@ public:
     }
 private:
     EGLDisplay display=EGL_NO_DISPLAY;EGLConfig config=nullptr;EGLSurface surface=EGL_NO_SURFACE;EGLContext context=EGL_NO_CONTEXT;
-    GLuint program=0,sourceTex=0,localLaplacianTex=0,outputTex=0,fbo=0,lutSsbo=0,pbo[2]{0,0};GLsync fence[2]{0,0};int pendingTop[2]{0,0},pendingH[2]{0,0};int sourceAllocW=0,sourceAllocH=0,outWidth=0,outHeight=0,maxBandRows=0;bool explicitFenceSync=true;const Params*params=nullptr;
+    GLuint program=0,sourceTex=0,outputTex=0,fbo=0,lutSsbo=0,pbo[2]{0,0};GLsync fence[2]{0,0};int pendingTop[2]{0,0},pendingH[2]{0,0};int sourceAllocW=0,sourceAllocH=0,outWidth=0,outHeight=0,maxBandRows=0;bool explicitFenceSync=true;const Params*params=nullptr;
     GLint loc(const char*n){return glGetUniformLocation(program,n);}
     static void set(std::string*r,const char*m){if(r)*r=m?m:"unknown";}
     static void setGl(std::string*r,const char*stage,GLenum err){if(!r)return;char b[96];std::snprintf(b,sizeof(b),"%s_gl_error_0x%04x",stage?stage:"gl",(unsigned int)err);*r=b;}
     void setParams(){
-        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);glUniform1i(loc("uLocalLaplacianEnabled"),(params->motionHdrHandoff&&params->localLaplacianMap&&params->localLaplacianW>0&&params->localLaplacianH>0&&params->localLaplacianSourceW>0&&params->localLaplacianSourceH>0)?1:0);glUniform2i(loc("uLocalLaplacianSize"),params->localLaplacianW,params->localLaplacianH);glUniform2i(loc("uLocalLaplacianSourceSize"),params->localLaplacianSourceW,params->localLaplacianSourceH);
+        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);
         glUniform3f(loc("uSensorRow0"),params->sensorToProfile[0],params->sensorToProfile[1],params->sensorToProfile[2]);glUniform3f(loc("uSensorRow1"),params->sensorToProfile[3],params->sensorToProfile[4],params->sensorToProfile[5]);glUniform3f(loc("uSensorRow2"),params->sensorToProfile[6],params->sensorToProfile[7],params->sensorToProfile[8]);
         glUniform3f(loc("uDisplayRow0"),params->profileToDisplay[0],params->profileToDisplay[1],params->profileToDisplay[2]);glUniform3f(loc("uDisplayRow1"),params->profileToDisplay[3],params->profileToDisplay[4],params->profileToDisplay[5]);glUniform3f(loc("uDisplayRow2"),params->profileToDisplay[6],params->profileToDisplay[7],params->profileToDisplay[8]);
     }
     void destroy(){
         if(display!=EGL_NO_DISPLAY&&context!=EGL_NO_CONTEXT&&surface!=EGL_NO_SURFACE)eglMakeCurrent(display,surface,surface,context);
-        for(int i=0;i<2;i++)if(fence[i]){glDeleteSync(fence[i]);fence[i]=0;}if(pbo[0]||pbo[1])glDeleteBuffers(2,pbo);if(lutSsbo)glDeleteBuffers(1,&lutSsbo);if(fbo)glDeleteFramebuffers(1,&fbo);if(outputTex)glDeleteTextures(1,&outputTex);if(localLaplacianTex)glDeleteTextures(1,&localLaplacianTex);if(sourceTex)glDeleteTextures(1,&sourceTex);if(program)glDeleteProgram(program);
+        for(int i=0;i<2;i++)if(fence[i]){glDeleteSync(fence[i]);fence[i]=0;}if(pbo[0]||pbo[1])glDeleteBuffers(2,pbo);if(lutSsbo)glDeleteBuffers(1,&lutSsbo);if(fbo)glDeleteFramebuffers(1,&fbo);if(outputTex)glDeleteTextures(1,&outputTex);if(sourceTex)glDeleteTextures(1,&sourceTex);if(program)glDeleteProgram(program);
         if(display!=EGL_NO_DISPLAY){eglMakeCurrent(display,EGL_NO_SURFACE,EGL_NO_SURFACE,EGL_NO_CONTEXT);if(context!=EGL_NO_CONTEXT)eglDestroyContext(display,context);if(surface!=EGL_NO_SURFACE)eglDestroySurface(display,surface);eglTerminate(display);}
-        display=EGL_NO_DISPLAY;surface=EGL_NO_SURFACE;context=EGL_NO_CONTEXT;program=sourceTex=localLaplacianTex=outputTex=fbo=lutSsbo=0;pbo[0]=pbo[1]=0;
+        display=EGL_NO_DISPLAY;surface=EGL_NO_SURFACE;context=EGL_NO_CONTEXT;program=sourceTex=outputTex=fbo=lutSsbo=0;pbo[0]=pbo[1]=0;
     }
 };
 
@@ -1169,19 +1104,11 @@ extern "C" JNIEXPORT jstring JNICALL Java_com_particlesdevs_photoncamera_process
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeTrue2xNative(
         JNIEnv*e,jclass,jobject bitmap,jstring renderPath,jint trueW,jint trueH,jint rawW,jint rawH,jint cropW,jint cropH,jint rotation,jboolean mirror,jfloat residualZoom,
         jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat sceneWhite,jboolean motionHdrHandoff,
-        jobject localLaplacianBuffer,jint localLaplacianW,jint localLaplacianH,jint localLaplacianSourceW,jint localLaplacianSourceH,
         jobject watermarkBitmap,
         jfloatArray jinResidual,jintArray jinReference,jint jinW,jint jinH,jstring gainPath,jfloat gainMaxRatio,jfloat gainContentMaxRatio,jstring path,jint quality){
     using namespace iris26564render;gIris26576PublicationTelemetry="backend=UNSET callStarted=true";AndroidBitmapInfo info{};if(!bitmap||!renderPath||!path||trueW<=0||trueH<=0||rawW<=0||rawH<=0||cropW<=0||cropH<=0||trueW!=rawW*2||trueH!=rawH*2||cropW>rawW||cropH>rawH||AndroidBitmap_getInfo(e,bitmap,&info)!=ANDROID_BITMAP_RESULT_SUCCESS||info.format!=ANDROID_BITMAP_FORMAT_RGBA_8888){iris26576SetPublicationTelemetry("backend=NONE validationFailed=true");return JNI_FALSE;}
     int expectedNativeW=(rotation==90||rotation==270)?cropH:rawW,expectedNativeH=(rotation==90||rotation==270)?rawW:cropH;if((int)info.width!=expectedNativeW||(int)info.height!=expectedNativeH)return JNI_FALSE;
     Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
-    if(p.motionHdrHandoff){
-        if(!localLaplacianBuffer||localLaplacianW<=0||localLaplacianH<=0||localLaplacianSourceW<=0||localLaplacianSourceH<=0)return JNI_FALSE;
-        void*mapAddress=e->GetDirectBufferAddress(localLaplacianBuffer);jlong mapCapacity=e->GetDirectBufferCapacity(localLaplacianBuffer);
-        const jlong mapBytes=(jlong)localLaplacianW*(jlong)localLaplacianH*2ll;
-        if(!mapAddress||mapCapacity<mapBytes)return JNI_FALSE;
-        p.localLaplacianMap=(const uint16_t*)mapAddress;p.localLaplacianW=localLaplacianW;p.localLaplacianH=localLaplacianH;p.localLaplacianSourceW=localLaplacianSourceW;p.localLaplacianSourceH=localLaplacianSourceH;
-    }
     if(!sensorToProfile||!profileToDisplay||e->GetArrayLength(sensorToProfile)!=9||e->GetArrayLength(profileToDisplay)!=9)return JNI_FALSE;e->GetFloatArrayRegion(sensorToProfile,0,9,p.sensorToProfile);e->GetFloatArrayRegion(profileToDisplay,0,9,p.profileToDisplay);if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.sensorToProfile)if(!std::isfinite(v))return JNI_FALSE;for(float v:p.profileToDisplay)if(!std::isfinite(v))return JNI_FALSE;if(!std::isfinite(p.displayGain)||p.displayGain<=0.f||!std::isfinite(p.exposureEv)||!std::isfinite(p.shadows)||!std::isfinite(p.contrast)||!std::isfinite(p.residualZoom)||!std::isfinite(p.sceneWhite)||p.sceneWhite<=0.f)return JNI_FALSE;
     U rp(e,renderPath),gp(e,gainPath),op(e,path);if(!rp.c||!op.c)return JNI_FALSE;const bool generateGain=gp.c!=nullptr;if(generateGain&&(!std::isfinite((float)gainMaxRatio)||gainMaxRatio<=1.f||!std::isfinite((float)gainContentMaxRatio)||gainContentMaxRatio<1.f))return JNI_FALSE;const float gainContentMax=generateGain?clampf((float)gainContentMaxRatio,1.f,(float)gainMaxRatio):1.f;int sourceFd=open(rp.c,O_RDONLY);if(sourceFd<0)return JNI_FALSE;struct stat st{};uint64_t expected=(uint64_t)trueW*(uint64_t)trueH*6ull;if(fstat(sourceFd,&st)!=0||(uint64_t)st.st_size!=expected){close(sourceFd);return JNI_FALSE;}
     Watermark water{};if(watermarkBitmap){AndroidBitmapInfo wi{};if(AndroidBitmap_getInfo(e,watermarkBitmap,&wi)!=ANDROID_BITMAP_RESULT_SUCCESS||wi.format!=ANDROID_BITMAP_FORMAT_RGBA_8888||wi.width==0||wi.height==0){close(sourceFd);return JNI_FALSE;}void*wp=nullptr;if(AndroidBitmap_lockPixels(e,watermarkBitmap,&wp)!=ANDROID_BITMAP_RESULT_SUCCESS||!wp){close(sourceFd);return JNI_FALSE;}water.w=(int)wi.width;water.h=(int)wi.height;water.rgba.resize((size_t)water.w*water.h*4);for(int y=0;y<water.h;y++)std::memcpy(water.rgba.data()+(size_t)y*water.w*4,(const uint8_t*)wp+(size_t)y*wi.stride,(size_t)water.w*4);AndroidBitmap_unlockPixels(e,watermarkBitmap);}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index c89a661cd592bc83e779cb37c166299260c21c5f..28611650a88ad5dc878cdad4988bf65e25264700 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -95,13 +95,6 @@ public final class MotionV2Render extends Node {
     private static final float HDR_EXPOSURE_SCALE = OUTPUT_EXPOSURE_SCALE;
     private static final int GAINMAP_DOWNSAMPLE = 1;
 
-    /* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_OWNER
-     * Eight Gaussian levels begin at half resolution. The coarsest mapped level remains the
-     * exact 26614 global appearance base; finer levels can only restore contrast that 26614
-     * compressed, never exceed source contrast. The final correction is a single RGB scalar. */
-    private static final int IRIS_26620_LAPLACIAN_LEVELS = 8;
-    private static final float IRIS_26620_LOCAL_CORRECTION_LIMIT_EV = 0.55f;
-
     /* IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_APPEARANCE
      * 26613 solved viewfinder brightness in the dark/body range but then applied that full gain
      * before a display-domain 0.80 knee. Device ceiling, bathroom and curtain pairs prove that
@@ -290,99 +283,6 @@ public final class MotionV2Render extends Node {
         return Math.max(lo, Math.min(hi, x));
     }
 
-    private void iris26620ClearLocalLaplacianState() {
-        basePipeline.mParameters.motionV2LocalLaplacianMap = null;
-        basePipeline.mParameters.motionV2LocalLaplacianWidth = 0;
-        basePipeline.mParameters.motionV2LocalLaplacianHeight = 0;
-        basePipeline.mParameters.motionV2LocalLaplacianSourceWidth = 0;
-        basePipeline.mParameters.motionV2LocalLaplacianSourceHeight = 0;
-    }
-
-    private GLTexture iris26620BuildLocalLaplacianCorrection(GLTexture source) {
-        iris26620ClearLocalLaplacianState();
-        if (!basePipeline.mParameters.motionV2Active) return null;
-        if (source == null || source.mSize == null || source.mSize.x <= 0 || source.mSize.y <= 0) {
-            throw new IllegalStateException("IRIS_26620 invalid Local Laplacian source");
-        }
-
-        final GLTexture[] pyramid = new GLTexture[IRIS_26620_LAPLACIAN_LEVELS];
-        GLTexture correction = null;
-        boolean keepCorrection = false;
-        try {
-            Point levelSize = new Point(
-                    Math.max(1, (source.mSize.x + 1) / 2),
-                    Math.max(1, (source.mSize.y + 1) / 2));
-            final GLFormat pairFormat = new GLFormat(GLFormat.DataType.FLOAT_16, 2);
-            pyramid[0] = new GLTexture(levelSize, pairFormat, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-            glProg.useAssetProgram("motionv2/local_laplacian_seed_26620");
-            glProg.setTexture("InputBuffer", source);
-            glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
-            glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            glProg.drawBlocks(pyramid[0]);
-
-            for (int i = 1; i < IRIS_26620_LAPLACIAN_LEVELS; i++) {
-                Point previous = pyramid[i - 1].mSize;
-                levelSize = new Point(
-                        Math.max(1, (previous.x + 1) / 2),
-                        Math.max(1, (previous.y + 1) / 2));
-                pyramid[i] = new GLTexture(levelSize, pairFormat, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                glProg.useAssetProgram("motionv2/local_laplacian_downsample_26620");
-                glProg.setTexture("InputPyramid", pyramid[i - 1]);
-                glProg.drawBlocks(pyramid[i]);
-            }
-
-            correction = new GLTexture(
-                    pyramid[0].mSize,
-                    new GLFormat(GLFormat.DataType.FLOAT_16, 1),
-                    null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-            glProg.useAssetProgram("motionv2/local_laplacian_correction_26620");
-            for (int i = 0; i < IRIS_26620_LAPLACIAN_LEVELS; i++) {
-                glProg.setTexture("Pyramid" + i, pyramid[i]);
-            }
-            glProg.drawBlocks(correction);
-
-            if (basePipeline.mParameters.motionV2SuperResOutputEnabled) {
-                correction.BufferLoad();
-                ByteBuffer map = correction.textureBuffer(
-                        new GLFormat(GLFormat.DataType.FLOAT_16, 1), true);
-                map.position(0);
-                basePipeline.mParameters.motionV2LocalLaplacianMap = map;
-                basePipeline.mParameters.motionV2LocalLaplacianWidth = correction.mSize.x;
-                basePipeline.mParameters.motionV2LocalLaplacianHeight = correction.mSize.y;
-                basePipeline.mParameters.motionV2LocalLaplacianSourceWidth = source.mSize.x;
-                basePipeline.mParameters.motionV2LocalLaplacianSourceHeight = source.mSize.y;
-            }
-
-            Log.i(Name, "IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN"
-                    + " active=true"
-                    + " levels=" + IRIS_26620_LAPLACIAN_LEVELS
-                    + " source=" + source.mSize.x + "x" + source.mSize.y
-                    + " correction=" + correction.mSize.x + "x" + correction.mSize.y
-                    + " mapSource=" + source.mSize.x + "x" + source.mSize.y
-                    + " coarsest=" + pyramid[IRIS_26620_LAPLACIAN_LEVELS - 1].mSize.x
-                        + "x" + pyramid[IRIS_26620_LAPLACIAN_LEVELS - 1].mSize.y
-                    + " globalExposureOwner=motionV2DisplayGain"
-                    + " outputExposureScaleConsumedOnce=" + OUTPUT_EXPOSURE_SCALE
-                    + " coarsestBaseOwner=IRIS_26614_GLOBAL"
-                    + " restoreOnlyCompressedBands=true"
-                    + " sourceContrastUpperBound=true"
-                    + " rgbScalarOnly=true"
-                    + " correctionLimitEv=" + IRIS_26620_LOCAL_CORRECTION_LIMIT_EV
-                    + " true2xSharedMap=" + basePipeline.mParameters.motionV2SuperResOutputEnabled);
-            keepCorrection = true;
-            return correction;
-        } finally {
-            for (GLTexture texture : pyramid) {
-                if (texture != null) {
-                    try { texture.close(); } catch (Throwable ignored) {}
-                }
-            }
-            if (!keepCorrection && correction != null) {
-                try { correction.close(); } catch (Throwable ignored) {}
-            }
-        }
-    }
-
     @Override
     public void Run() {
         if (!(basePipeline.mParameters.motionV2Active || basePipeline.mParameters.irisNightActive)) {
@@ -425,28 +325,18 @@ public final class MotionV2Render extends Node {
                 + " reconstructionZoom=" + reconstructionZoom
                 + " renderResidual=" + irisOutputZoom
                 + " product=" + (reconstructionZoom * irisOutputZoom));
-        GLTexture iris26620Correction = iris26620BuildLocalLaplacianCorrection(extendedLinearHdr);
-        try {
-            glProg.useAssetProgram("motionv2/render");
-            glProg.setTexture("InputBuffer", extendedLinearHdr);
-            glProg.setVar("sceneWhite", sceneWhite);
-            glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
-            glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
-            glProg.setVar("iris26604MotionSdrKneeFinal", IRIS_26604_MOTION_SDR_KNEE_FINAL);
-            glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            glProg.setVar("irisOutputZoom", irisOutputZoom);
-            glProg.setVar("iris26620LocalLaplacianEnabled", iris26620Correction != null ? 1 : 0);
-            if (iris26620Correction != null) {
-                glProg.setTexture("iris26620LocalLaplacianCorrection", iris26620Correction);
-            }
-
-            WorkingTexture = basePipeline.getMain();
-            glProg.drawBlocks(WorkingTexture);
-        } finally {
-            if (iris26620Correction != null) {
-                try { iris26620Correction.close(); } catch (Throwable ignored) {}
-            }
-        }
+                glProg.useAssetProgram("motionv2/render");
+        glProg.setTexture("InputBuffer", extendedLinearHdr);
+        glProg.setVar("sceneWhite", sceneWhite);
+        glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
+        glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
+        glProg.setVar("iris26604MotionSdrKneeFinal", IRIS_26604_MOTION_SDR_KNEE_FINAL);
+        
+                glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
+        glProg.setVar("irisOutputZoom", irisOutputZoom);
+
+        WorkingTexture = basePipeline.getMain();
+        glProg.drawBlocks(WorkingTexture);
 
         PostPipeline pipeline = (PostPipeline) basePipeline;
         pipeline.motionV2GainMapBitmap = null;
@@ -695,9 +585,7 @@ public final class MotionV2Render extends Node {
                 + " iris26614DisplayDomainKneeAuthority=false"
                 + " iris26614CanonicalAppearanceGainAuthority=true"
                 + " IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_UHDR_SR_PARITY=true"
-                + " localTone=" + basePipeline.mParameters.motionV2Active
-                + " localToneOwner=IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN"
-                + " globalToneOwner=IRIS_26614_CANONICAL_SOURCE_DOMAIN"
+                + " localTone=false"
                 + " sharpening=false");
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index 862e5807b2773bb163a4537619b2add2ebf3249a..fb10528bc1d9526c29bfe1a9c1d38d60108a39ec 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -28,7 +28,6 @@ import com.particlesdevs.photoncamera.util.Allocator;
 
 import java.io.File;
 import java.io.FileNotFoundException;
-import java.nio.ByteBuffer;
 import java.util.Arrays;
 import java.util.Scanner;
 
@@ -125,15 +124,6 @@ public class Parameters {
     public float motionV2RenderResidualZoom = 1.0f;
     public boolean motionV2SuperResOutputEnabled = false;
     public float motionV2SuperResOutputScale = 1.0f;
-    /* IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN_MAP
-     * Half-resolution R16F correction field produced by the 1x MotionV2Render multiscale owner.
-     * It is presentation-only, direct-buffer backed, and consumed synchronously by true-2x
-     * publication so SR cannot solve an independent local tone field. */
-    public ByteBuffer motionV2LocalLaplacianMap = null;
-    public int motionV2LocalLaplacianWidth = 0;
-    public int motionV2LocalLaplacianHeight = 0;
-    public int motionV2LocalLaplacianSourceWidth = 0;
-    public int motionV2LocalLaplacianSourceHeight = 0;
     public float motionV2HardwareZoom = 1.0f;
     public float motionV2ResidualSoftwareZoom = 1.0f;
     public int realWL = -1;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index ce36fb537c3e35b7bead084e78a27ea5c471b2b8..e4c71385582dbae3b14a46cad3f8010f2e1b475a 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -15,7 +15,6 @@ import com.particlesdevs.photoncamera.processing.render.Parameters;
 import com.particlesdevs.photoncamera.processing.processor.IrisMotionSettings;
 import com.particlesdevs.photoncamera.processing.processor.IrisNightNeuralEnhancer;
 import com.particlesdevs.photoncamera.processing.opengl.postpipeline.MotionV2Render;
-import java.nio.ByteBuffer;
 import java.nio.file.Path;
 import java.nio.file.Files;
 import java.io.File;
@@ -156,27 +155,6 @@ public final class MotionV2Jpeg444Encoder {
                     ? attachedGainmap.getDisplayRatioForFullHdr() : 1.0f;
             final float publicationSceneWhite =
                     MotionV2Render.iris26598PublicationSceneWhite(parameters);
-            final ByteBuffer localLaplacianMap = parameters.motionV2LocalLaplacianMap;
-            final int localLaplacianWidth = parameters.motionV2LocalLaplacianWidth;
-            final int localLaplacianHeight = parameters.motionV2LocalLaplacianHeight;
-            final int localLaplacianSourceWidth = parameters.motionV2LocalLaplacianSourceWidth;
-            final int localLaplacianSourceHeight = parameters.motionV2LocalLaplacianSourceHeight;
-            if (parameters.motionV2Active) {
-                long expectedBytes = 2L * localLaplacianWidth * localLaplacianHeight;
-                if (localLaplacianMap == null || !localLaplacianMap.isDirect()
-                        || localLaplacianWidth <= 0 || localLaplacianHeight <= 0
-                        || localLaplacianSourceWidth <= 0 || localLaplacianSourceHeight <= 0
-                        || expectedBytes <= 0L || localLaplacianMap.capacity() < expectedBytes) {
-                    Log.e(TAG, "IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_MAP_MISSING"
-                            + " map=" + (localLaplacianMap != null)
-                            + " direct=" + (localLaplacianMap != null && localLaplacianMap.isDirect())
-                            + " size=" + localLaplacianWidth + "x" + localLaplacianHeight
-                            + " source=" + localLaplacianSourceWidth + "x" + localLaplacianSourceHeight
-                            + " expectedBytes=" + expectedBytes
-                            + " capacity=" + (localLaplacianMap != null ? localLaplacianMap.capacity() : 0));
-                    return false;
-                }
-            }
             final long renderStartNs = System.nanoTime();
             final boolean baseOk = writeTrue2xNative(
                     bitmap, renderRgb16fPath, trueWidth, trueHeight,
@@ -185,8 +163,6 @@ public final class MotionV2Jpeg444Encoder {
                     sensorToProPhoto, profileToDisplay,
                     parameters.motionV2DisplayGain, exposureEv, shadows, contrast,
                     publicationSceneWhite, parameters.motionV2Active,
-                    localLaplacianMap, localLaplacianWidth, localLaplacianHeight,
-                    localLaplacianSourceWidth, localLaplacianSourceHeight,
                     watermark,
                     useJin ? jinResidual.residual : null,
                     useJin ? jinResidual.referenceRgb : null,
@@ -207,9 +183,6 @@ public final class MotionV2Jpeg444Encoder {
                     + " iris26614DisplayDomainKneeAuthority=false"
                     + " iris26614CanonicalAppearanceGainAuthority=true"
                     + " IRIS_26614_TRUE2X_SOURCE_DOMAIN_PRESENTATION=true"
-                    + " IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN=" + parameters.motionV2Active
-                    + " localLaplacianSize=" + localLaplacianWidth + "x" + localLaplacianHeight
-                    + " localLaplacianSource=" + localLaplacianSourceWidth + "x" + localLaplacianSourceHeight
                     + " adaptiveSceneWhite=" + parameters.motionV2ToneAdaptiveSceneWhite
                     + " publicationSceneWhiteSource="
                         + (parameters.motionV2Active ? "BASE_26598_MOTION" : "ADAPTIVE_26591_NIGHT")
@@ -290,13 +263,6 @@ public final class MotionV2Jpeg444Encoder {
             Log.e(TAG, "IRIS_26564_TRUE2X_STREAMING_JPEG_FAILED", t);
             return false;
         } finally {
-            if (parameters != null && parameters.motionV2Active) {
-                parameters.motionV2LocalLaplacianMap = null;
-                parameters.motionV2LocalLaplacianWidth = 0;
-                parameters.motionV2LocalLaplacianHeight = 0;
-                parameters.motionV2LocalLaplacianSourceWidth = 0;
-                parameters.motionV2LocalLaplacianSourceHeight = 0;
-            }
             if (watermark != null && !watermark.isRecycled()) watermark.recycle();
             try { if (base != null) Files.deleteIfExists(base); } catch (Throwable ignored) {}
             try { if (gain != null) Files.deleteIfExists(gain); } catch (Throwable ignored) {}
@@ -350,8 +316,6 @@ public final class MotionV2Jpeg444Encoder {
             int rotation, boolean mirror, float renderResidualZoom,
             float[] sensorToProPhoto, float[] proPhotoToSrgb, float displayGain,
             float exposureEv, float shadows, float contrast, float sceneWhite, boolean motionHdrHandoff,
-            ByteBuffer localLaplacianMap, int localLaplacianWidth, int localLaplacianHeight,
-            int localLaplacianSourceWidth, int localLaplacianSourceHeight,
             Bitmap watermark,
             float[] jinResidual, int[] jinReferenceRgb, int jinWidth, int jinHeight,
             String true2xGainmapPath, float true2xGainmapMaxRatio,
diff --git a/app/version.properties b/app/version.properties
index aae15f2c8d43a9ec15c664d8e9bf6079fcc615f3..1529e77152446c36d7bbc901c2ec2c0602fc9fdf 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726620
-VERSION_BUILD=26620
+VERSION_NAME=0.9726614
+VERSION_BUILD=26614
