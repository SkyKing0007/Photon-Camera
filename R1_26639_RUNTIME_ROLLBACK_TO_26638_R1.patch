diff --git a/app/src/main/assets/shaders/motionv2/color_transform.glsl b/app/src/main/assets/shaders/motionv2/color_transform.glsl
index 10ae929049633e6b431c6267007ff5a47baf66eb..062444d296a8a621cc3852a0fe9d20a8ddaf1379 100644
--- a/app/src/main/assets/shaders/motionv2/color_transform.glsl
+++ b/app/src/main/assets/shaders/motionv2/color_transform.glsl
@@ -168,23 +168,6 @@ void main(){
         float renderedY=dot(renderedDisplay,displayP3Luma);
         if(targetY>=0.0 && renderedY>1.0e-8){
             linearDisplay=renderedDisplay*(targetY/renderedY);
-            /* IRIS_26639_ACR3_CALIBRATED_CHROMA_FLOOR
-             * ACR3 may reshape/enhance the calibrated color, but saturation=1.0 must never lose
-             * channel separation that the DNG color solution already established. Restore only the
-             * missing span around the same luminance-neutral axis; common-axis gamut safety remains
-             * the sole negative-excursion owner below. */
-            float baselineSpan=max(linearDisplayOriginal.r,max(linearDisplayOriginal.g,linearDisplayOriginal.b))
-                    -min(linearDisplayOriginal.r,min(linearDisplayOriginal.g,linearDisplayOriginal.b));
-            float renderedSpan=max(linearDisplay.r,max(linearDisplay.g,linearDisplay.b))
-                    -min(linearDisplay.r,min(linearDisplay.g,linearDisplay.b));
-            if(renderedSpan+1.0e-8<baselineSpan){
-                if(renderedSpan>1.0e-8){
-                    vec3 chroma=linearDisplay-vec3(targetY);
-                    linearDisplay=vec3(targetY)+chroma*(baselineSpan/renderedSpan);
-                }else{
-                    linearDisplay=linearDisplayOriginal;
-                }
-            }
         }
     }
     /* Frozen pink/cyan-edge invariant: translate all channels together, never clip one channel. */
diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index 091c5af68a4971dd91580c5c3d8c1e87754a2f7f..473702043d9312ee0ea0a4ca7dd34514f1e942f3 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -21,30 +21,6 @@ out float Output;
  */
 const float UHDR_OFFSET = 0.015625;
 float luminance(vec3 c){return dot(c,vec3(0.22897456,0.69173852,0.07928691));}
-float max3(vec3 c){return max(c.r,max(c.g,c.b));}
-float iris26639BaseGlobalSdrGuide(float sourceGuide){
-    const float outputScale=0.80;
-    float x=max(sourceGuide,0.0);
-    float requested=max(displayGain,1.0e-6)*outputScale;
-    float whiteAnchor=min(0.95,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor)bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
-    float safeWhite=max(whiteAnchor,1.0e-6);
-    float shoulderRatio=max(bodyGain/safeWhite-1.0,0.0);
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        float cubic=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rational=bodyGain*x/(1.0+shoulderRatio*x);
-        return 0.5*(cubic+rational);
-    }
-    float rationalSlope=bodyGain/((1.0+shoulderRatio)*(1.0+shoulderRatio));
-    float bodySlope=0.5*(whiteAnchor+rationalSlope);
-    float reserve=max(1.0-whiteAnchor,0.0);
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(bodySlope,1.0e-6);
-    float excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
-}
 float srgbDecode(float x){x=clamp(x,0.0,1.0);return x<=0.04045?x/12.92:pow((x+0.055)/1.055,2.4);}
 vec3 srgbDecode(vec3 c){return vec3(srgbDecode(c.r),srgbDecode(c.g),srgbDecode(c.b));}
 
@@ -100,28 +76,17 @@ void main(){
     float safeMax=max(maxGainRatio,1.001);
     float ratio;
     if(motionHdrHandoff!=0){
-        /* IRIS_26639_SELECTIVE_RECOVERABLE_HEADROOM_UHDR
-         * Ordinary SDR body is exact unity. Eligibility is pointwise/source-aligned and is never
-         * spatially dilated, so a chandelier cannot lend gain to a darker neighbor. Desired gain is
-         * derived from the same pre-tone max-channel/luma guide and the frozen 0.80 brightness target.
-         * The actual SDR sample is used only as a one-sided ceiling: local tone/detail can REDUCE gain
-         * to prevent an overshoot rim, but can never create extra gain or an HDR island. */
-        const float hdrEntry=0.65;
-        const float hdrFullEntry=0.85;
+        /* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION
+         * The completed SDR remains exact appearance authority. The post-VGN master contributes
+         * only a registered scalar luminance guide. A smooth log-domain display expansion builds
+         * the HDR rendition as SDR * gain(sourceY), so local SDR tone/detail can never create an
+         * accidental unity island. Gain is exactly 1 at black, continuous everywhere, approaches
+         * maxGainRatio asymptotically, and never changes RGB chromaticity or spatial geometry. */
+        const float gainKnee=1.50;
         float sourceY=max(luminance(hdrPositive),0.0);
-        float sourceGuide=max(sourceY,max3(hdrPositive));
-        float eligibility=smoothstep(hdrEntry,hdrFullEntry,sourceGuide);
-        if(sourceGuide<=hdrEntry || eligibility<=1.0e-7){
-            ratio=1.0;
-        }else{
-            float globalSdr=max(iris26639BaseGlobalSdrGuide(sourceGuide),0.0);
-            float requested=max(displayGain,1.0e-6)*hdrExposureScale;
-            float recoverable=max(globalSdr,sourceY*requested);
-            float desired=clamp((recoverable+UHDR_OFFSET)/(globalSdr+UHDR_OFFSET),1.0,safeMax);
-            float pixelCeiling=clamp((recoverable+UHDR_OFFSET)/(sdr+UHDR_OFFSET),1.0,safeMax);
-            float eligibleRatio=min(desired,pixelCeiling);
-            ratio=clamp(exp2(log2(max(eligibleRatio,1.0))*eligibility),1.0,safeMax);
-        }
+        float weight=sourceY/(sourceY+gainKnee);
+        float logGain=log2(safeMax)*weight;
+        ratio=clamp(exp2(logGain),1.0,safeMax);
     }else{
         float hdrTargetScale=hdrExposureScale;
         float hdr=max(luminance(hdrPositive*hdrTargetScale),0.0);
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index 8d3e55dbe75a4e2334dfa516c01df29f9b32bfe0..b1feb643bf9ac851a06f9962450e18f20cf56a62 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -15,8 +15,6 @@ uniform float mappedReferenceLog;
 uniform float sigmaEv;
 uniform float edgeSlope;
 uniform float iris26626PreservationStrength;
-uniform float iris26639ShadowBodyStrength;
-uniform float iris26639ShadowBodyEnd;
 uniform int iris26626Mode;
 out float Output;
 
@@ -107,23 +105,10 @@ void main(){
         float shoulderGate=smoothstep(0.70,0.985,baseLinear);
         float smoothShoulder=1.0-pow(max(1.0-baseLinear,0.0),1.12);
         float baseOut=mix(baseLinear,smoothShoulder,0.55*shoulderGate);
-
-        /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BASE
-         * Correct only the low-frequency base B. The 0.05 true-floor endpoint and the scene-derived
-         * upper body endpoint are exact identity with zero derivative change. A quartic C1 bump
-         * reaches at most 0.25 EV at its center; the structural residual R is added afterward and is
-         * never multiplied by this correction. */
-        const float shadowFloorEnd=0.050;
-        float bodyEnd=clamp(iris26639ShadowBodyEnd,0.18,0.42);
-        float bodyStrength=clamp(iris26639ShadowBodyStrength,0.0,1.0);
-        float bodyT=clamp((baseOut-shadowFloorEnd)/max(bodyEnd-shadowFloorEnd,1.0e-6),0.0,1.0);
-        float bodyBump=16.0*bodyT*bodyT*(1.0-bodyT)*(1.0-bodyT);
-        float bodyScale=exp2(-0.25*bodyStrength*bodyBump);
-        float shadowBase=baseOut*bodyScale;
         float structureGate=smoothstep(0.012,0.045,abs(residual));
         float smallResidualWeight=mix(1.0,0.78,upperGate);
         float residualWeight=mix(smallResidualWeight,1.0,structureGate);
-        float outLinear=clamp(shadowBase+residualWeight*residual,IRIS_26626_LOG_FLOOR,1.0);
+        float outLinear=clamp(baseOut+residualWeight*residual,IRIS_26626_LOG_FLOOR,1.0);
         Output=log2(outLinear);
         return;
     }
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 69e79c692f129b10e7ef40b1cb62103b64fb110c..9962628ae08c747e1201235f4dbf2586c577b396 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -623,16 +623,7 @@ inline bool profileColor(const SourceRegion&s,const Params&p,int x,int y,Vec3*ou
     float profileFloor=std::min(profile.r,std::min(profile.g,profile.b));float originalFloor=std::min(original.r,std::min(original.g,original.b));
     if(p.acr3Enabled&&profileFloor>=0.f&&originalFloor>=0.f){
         Vec3 rendered=mat(p.profileToDisplay,iris26638ApplyAcr3(p,profile));float targetY=luma(original),renderedY=luma(rendered);
-        if(targetY>=0.f&&renderedY>1.0e-8f){
-            linear=mul(rendered,targetY/renderedY);
-            /* IRIS_26639_ACR3_CALIBRATED_CHROMA_FLOOR_TRUE2X_CPU */
-            float baselineSpan=peak(original)-std::min(original.r,std::min(original.g,original.b));
-            float renderedSpan=peak(linear)-std::min(linear.r,std::min(linear.g,linear.b));
-            if(renderedSpan+1.0e-8f<baselineSpan){
-                if(renderedSpan>1.0e-8f){Vec3 neutral{targetY,targetY,targetY};Vec3 c=sub(linear,neutral);linear=add(neutral,mul(c,baselineSpan/renderedSpan));}
-                else linear=original;
-            }
-        }
+        if(targetY>=0.f&&renderedY>1.0e-8f)linear=mul(rendered,targetY/renderedY);
     }
     float floor=std::min(linear.r,std::min(linear.g,linear.b));if(floor<0.f){linear.r-=floor;linear.g-=floor;linear.b-=floor;}*out=linear;
     return std::isfinite(out->r)&&std::isfinite(out->g)&&std::isfinite(out->b);
@@ -783,29 +774,15 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
     switch(p.rotation){case 270:x+=(th-ch);crx=(x-(th-ch))/tw;cry=(y-cw)/th;break;case 180:crx=x/tw;cry=(y-ch)/th;break;case 90:crx=x/tw;cry=(y-tw)/th;break;default:y+=(th-ch);crx=x/tw;cry=(y-th)/th;break;}
     crx*=tw/th;crx/=((float)w.w/(float)w.h);crx*=1.025f;crx*=15.f;cry=(cry+1.f/15.f)*15.f;if(crx<0.f||cry<0.f)return false;*u=crx;*v=cry;return true;
 }
-inline float iris26639SelectiveGainRatio(Vec3 hdr,Vec3 sdr,const Params&p,float gainMax,float gainContentMax){
-    const float off=0.015625f,hdrEntry=0.65f,hdrFullEntry=0.85f;
-    float safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);
-    Vec3 hp=clampNonnegative(hdr);float sourceY=std::max(luma(hp),0.f);float sourceGuide=std::max(sourceY,peak(hp));
-    float eligibility=smoothstep(hdrEntry,hdrFullEntry,sourceGuide);if(sourceGuide<=hdrEntry||eligibility<=1.0e-7f)return 1.f;
-    float globalSdr=std::max(iris26621MapMotionSdrFinalGuide(sourceGuide,p.displayGain),0.f);
-    float requested=std::max(p.displayGain,1.0e-6f)*0.80f;
-    float recoverable=std::max(globalSdr,sourceY*requested);
-    float desired=clampf((recoverable+off)/(globalSdr+off),1.f,contentMax);
-    float sdrY=std::max(luma(clampNonnegative(sdr)),0.f);
-    float pixelCeiling=clampf((recoverable+off)/(sdrY+off),1.f,contentMax);
-    float eligible=std::min(desired,pixelCeiling);
-    return clampf(std::exp2(std::log2(std::max(eligible,1.f))*eligibility),1.f,contentMax);
-}
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26639_SELECTIVE_RECOVERABLE_HEADROOM_TRUE2X_CPU */ratio=iris26639SelectiveGainRatio(hdr,sdr,p,safeMax,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_CPU */const float gainKnee=1.50f;float sourceY=std::max(luma(clampNonnegative(hdr)),0.f);float weight=sourceY/(sourceY+gainKnee);float logGain=std::log2(contentMax)*weight;ratio=clampf(std::exp2(logGain),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26639_SELECTIVE_RECOVERABLE_HEADROOM_TRUE2X_CPU */ratio=iris26639SelectiveGainRatio(hdr,sdr,p,safeMax,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_CPU */const float gainKnee=1.50f;float sourceY=std::max(luma(clampNonnegative(hdr)),0.f);float weight=sourceY/(sourceY+gainKnee);float logGain=std::log2(contentMax)*weight;ratio=clampf(std::exp2(logGain),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
@@ -962,7 +939,7 @@ vec3 iris26638ApplyAcr3(vec3 color){
 }
 vec3 irisProfileColor(ivec2 p){
     vec3 camera=irisCameraAt(p),profile=irisMatRows(uSensorRow0,uSensorRow1,uSensorRow2,camera);vec3 original=irisMatRows(uDisplayRow0,uDisplayRow1,uDisplayRow2,profile),linear=original;
-    if(uAcr3Enabled!=0&&min(profile.r,min(profile.g,profile.b))>=0.0&&min(original.r,min(original.g,original.b))>=0.0){vec3 rendered=irisMatRows(uDisplayRow0,uDisplayRow1,uDisplayRow2,iris26638ApplyAcr3(profile));float targetY=irisLuma(original),renderedY=irisLuma(rendered);if(targetY>=0.0&&renderedY>1.0e-8){linear=rendered*(targetY/renderedY);float baselineSpan=irisPeak(original)-min(original.r,min(original.g,original.b));float renderedSpan=irisPeak(linear)-min(linear.r,min(linear.g,linear.b));if(renderedSpan+1.0e-8<baselineSpan){if(renderedSpan>1.0e-8){vec3 c=linear-vec3(targetY);linear=vec3(targetY)+c*(baselineSpan/renderedSpan);}else linear=original;}}}
+    if(uAcr3Enabled!=0&&min(profile.r,min(profile.g,profile.b))>=0.0&&min(original.r,min(original.g,original.b))>=0.0){vec3 rendered=irisMatRows(uDisplayRow0,uDisplayRow1,uDisplayRow2,iris26638ApplyAcr3(profile));float targetY=irisLuma(original),renderedY=irisLuma(rendered);if(targetY>=0.0&&renderedY>1.0e-8)linear=rendered*(targetY/renderedY);}
     float floorV=min(linear.r,min(linear.g,linear.b));if(floorV<0.0)linear-=vec3(floorV);return irisClampNonnegative(linear);
 }
 vec3 irisTone(vec3 rgb){
@@ -1092,12 +1069,12 @@ void main(){
         float contentMax=clamp(uGainContentMax,1.0,safeMax);
         float ratio;
         if(uMotionHdrHandoff!=0){
-            /* IRIS_26639_SELECTIVE_RECOVERABLE_HEADROOM_TRUE2X_GPU */
-            const float hdrEntry=0.65,hdrFullEntry=0.85;
-            vec3 hp=irisClampNonnegative(hdr);float sourceY=max(irisLuma(hp),0.0);float sourceGuide=max(sourceY,irisPeak(hp));
-            float eligibility=smoothstep(hdrEntry,hdrFullEntry,sourceGuide);
-            if(sourceGuide<=hdrEntry||eligibility<=1.0e-7){ratio=1.0;}
-            else{float globalSdr=max(iris26621MapMotionSdrFinalGuide(sourceGuide),0.0);float requested=max(uDisplayGain,1.0e-6)*0.80;float recoverable=max(globalSdr,sourceY*requested);float desired=clamp((recoverable+off)/(globalSdr+off),1.0,contentMax);float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);float pixelCeiling=clamp((recoverable+off)/(sdrY+off),1.0,contentMax);float eligible=min(desired,pixelCeiling);ratio=clamp(exp2(log2(max(eligible,1.0))*eligibility),1.0,contentMax);}
+            /* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_GPU */
+            const float gainKnee=1.50;
+            float sourceY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
+            float weight=sourceY/(sourceY+gainKnee);
+            float logGain=log2(contentMax)*weight;
+            ratio=clamp(exp2(logGain),1.0,contentMax);
         }else{
             float hdrY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
             float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 1a05185c1fd20df15b3678e73e0df01e630214af..1b1fa6d23e7bdec24273f058b9aa95cd55b27efd 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -82,6 +82,7 @@ public final class MotionV2Render extends Node {
      * Motion UHDR keeps the completed SDR as exact appearance authority. The post-VGN master
      * supplies only the spatial luminance guide for a continuous output-referred HDR expansion.
      * No scene-linear/SDR quotient, no single match anchor, no hard threshold, and no RGB gain. */
+    private static final float IRIS_26632_HDR_GAIN_KNEE = 1.50f;
     /* IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS
      * Preserve the tested 26505 SDR primary exactly. Ultra HDR is a reversible
      * rendition relationship: the gain map should recover the wanted HDR signal
@@ -238,21 +239,6 @@ public final class MotionV2Render extends Node {
         return hdrBase;
     }
 
-    /* IRIS_26639_SCENE_ADAPTIVE_UHDR_CAPACITY
-     * Capacity follows useful recoverable upper-tail headroom from the already-sampled P99.8 guide.
-     * It is never a denoise/exposure/capture authority. The per-pixel shader remains the stricter
-     * owner and may encode exact unity everywhere even when metadata advertises this bounded range. */
-    private static float iris26639MotionGainCapacity(Parameters p) {
-        float guide = p.motionV2ToneP998Guide;
-        if (!Float.isFinite(guide) || guide <= 0.0f) guide = p.motionV2ToneP99Guide;
-        if (!Float.isFinite(guide) || guide <= 0.0f) return 2.0f;
-        float globalSdr = iris26621MapMotionSdrFinalGuide(guide, p.motionV2DisplayGain);
-        float requested = Math.max(p.motionV2DisplayGain, 1.0e-6f) * OUTPUT_EXPOSURE_SCALE;
-        float recoverable = Math.max(globalSdr, guide * requested);
-        float ratio = (recoverable + 0.015625f) / Math.max(globalSdr + 0.015625f, 1.0e-6f);
-        return iris26582Clamp(ratio, 1.25f, IRIS_26592_MOTION_UHDR_MAX_RATIO);
-    }
-
     public MotionV2Render() { super("", "MotionV2Render"); }
     @Override public void Compile() {}
 
@@ -738,10 +724,6 @@ public final class MotionV2Render extends Node {
             glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
             glProg.setTexture("CurrentToneLog", finalTone);
             glProg.setTexture("SourceGuideLog", guide[IRIS_26635_HIGHLIGHT_BASE_LEVEL]);
-            glProg.setVar("iris26639ShadowBodyStrength",
-                    basePipeline.mParameters.motionV2ShadowBodyStrength);
-            glProg.setVar("iris26639ShadowBodyEnd",
-                    basePipeline.mParameters.motionV2ShadowBodyEnd);
             glProg.setVar("iris26626Mode", 6);
             glProg.drawBlocks(coherentHighlightTone);
             try { finalTone.close(); } catch (Throwable ignored) {}
@@ -811,10 +793,6 @@ public final class MotionV2Render extends Node {
                     + " iris26626PreservationMax=" + IRIS_26626_SOURCE_PRESERVATION_MAX
                     + " iris26626LowerBodyGate=" + IRIS_26623_UPPER_TONE_START + "..0.72"
                     + " iris26626Baseline=EXACT_26625_RECONSTRUCTION"
-                    + " iris26639ShadowBodyStrength=" + basePipeline.mParameters.motionV2ShadowBodyStrength
-                    + " iris26639ShadowBodyEnd=" + basePipeline.mParameters.motionV2ShadowBodyEnd
-                    + " iris26639ShadowMaxCorrectionEv=0.25"
-                    + " iris26639ShadowStructureResidualPreserved=true"
                     + " globalExposureOwner=motionV2DisplayGain"
                     + " outputExposureScaleConsumedOnce=" + OUTPUT_EXPOSURE_SCALE
                     + " absoluteFullResolutionToneMap=true"
@@ -939,7 +917,7 @@ public final class MotionV2Render extends Node {
             float maxGainRatio = iris26550Night
                     ? Math.max(2.0f, Math.min(2.5f, HDR_EXPOSURE_SCALE * postDisplaySensorWhite
                             * mgcSourceExposureGain))
-                    : iris26639MotionGainCapacity(basePipeline.mParameters);
+                    : IRIS_26592_MOTION_UHDR_MAX_RATIO;
 
             GLTexture gainTexture = null;
             try {
@@ -1047,10 +1025,9 @@ public final class MotionV2Render extends Node {
                                     + " actualPeakContentRatio=" + actualPeakContentRatio
                                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
                                     + " capacityMatchesGainMapMax=true actualPeakContentRatio=" + actualPeakContentRatio
-                                    + " bodyGainOwner=IRIS_26639_SELECTIVE_RECOVERABLE_HEADROOM"
-                                    + " hdrGainSource=POST_VGN_MAX_GUIDE_UPPER_TONE_RECOVERY"
-                                    + " hdrGainEntry=" + IRIS_26623_UPPER_TONE_START
-                                    + " hdrGainFullEntry=0.85"
+                                    + " bodyGainOwner=CONTINUOUS_OUTPUT_REFERRED_SCALAR_EXPANSION"
+                                    + " hdrGainSource=POST_VGN_LUMINANCE_GUIDED_SDR_DISPLAY_EXPANSION"
+                                    + " hdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
                                     + " singleMatchAnchor=false fixedBodyGain=false nominalWhiteDiscontinuity=false"
                                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                                     + " hdrExposureScale=" + HDR_EXPOSURE_SCALE);
@@ -1088,13 +1065,11 @@ public final class MotionV2Render extends Node {
                         + " postAliasSpikeRepair=false"
                         + " sdrBaseDetailAuthority=true colorAuthority=SDR_BASE_ONLY"
                         + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
-                        + " motionHdrGainEntry=" + IRIS_26623_UPPER_TONE_START
-                        + " motionHdrGainFullEntry=0.85"
+                        + " motionHdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
                         + " motionHdrGainIsScalarLuminanceOnly=true"
-                        + " motionHdrGainEquation=IRIS_26639_SELECTIVE_PER_PIXEL_RECOVERABLE_HEADROOM"
-                        + " motionHdrEligibilityDilation=false motionHdrPerPixelCeiling=true"
-                        + " motionHdrSingleMatchAnchor=false motionHdrFixedBodyGain=false"
-                        + " IRIS_26639_SELECTIVE_UHDR=true");
+                        + " motionHdrGainEquation=OUTPUT_REFERRED_SDR_TIMES_SOURCE_GUIDED_SCALAR_GAIN"
+                        + " motionHdrSingleMatchAnchor=false motionHdrFixedBodyGain=false motionHdrNominalWhiteGate=false"
+                        + " IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION=true");
             } finally {
                 if (gainTexture != null) {
                     try { gainTexture.close(); } catch (Throwable ignored) {}
@@ -1180,9 +1155,8 @@ public final class MotionV2Render extends Node {
                 + " outputExposureEv=-0.321928"
                 + " hdrTargetUsesSameScale=true"
                 + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
-                + " hdrGainEntry=" + IRIS_26623_UPPER_TONE_START
-                + " hdrGainFullEntry=0.85"
-                + " uhdrLuminanceOnly=true uhdrSelectiveRecoverableHeadroom=true"
+                + " hdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
+                + " uhdrLuminanceOnly=true uhdrOutputReferredContinuousExpansion=true"
                 + " IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS=true"
                 + " syntheticBitmapGainMap=false"
                 + " irisOutputZoom=" + irisOutputZoom
@@ -1192,7 +1166,7 @@ public final class MotionV2Render extends Node {
                 + " iris26614DisplayDomainKneeAuthority=false"
                 + " iris26614CanonicalAppearanceGainAuthority=false"
                 + " IRIS_26623_ADAPTIVE_UPPER_TONE_SDR_UHDR_SR_PARITY=true"
-                + " iris26630AdaptiveColorV5=false iris26639Acr3CalibratedChromaFloor=true iris26630Saturation=" + iris26630MotionSaturation
+                + " iris26630AdaptiveColorV5=true iris26630Saturation=" + iris26630MotionSaturation
                 + " iris26630SaturationOwner=" + (basePipeline.mParameters.motionV2Active ? "MOTION_PER_LENS" : "NIGHT_NEUTRAL_1_0")
                 + " localTone=" + basePipeline.mParameters.motionV2Active
                 + " localToneOwner=IRIS_26621_FAST_LOCAL_LAPLACIAN"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index 32ea365fcb943659dce1b6b3c753dd2d0914632b..06b8f8f03ad456f9b9366444f3e06d39eaf434e1 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -63,8 +63,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
     private static final float NIGHT_FULL_MATCH_LUMA = 0.22f;
     private static final float NIGHT_DARK_LUMA = 0.06f;
     private static final float NIGHT_HEADROOM_GUIDE_LIMIT = 5.0f;
-    private float iris26639CandidateP25Guide = Float.NaN;
-    private float iris26639CandidateP50Guide = Float.NaN;
     private float iris26550CandidateP99Guide = Float.NaN;
     private float iris26582CandidateP95Guide = Float.NaN;
     private float iris26583CandidateP995Guide = Float.NaN;
@@ -99,8 +97,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
         }
         WorkingTexture = previousNode.WorkingTexture;
         iris26582CandidateGuides.clear();
-        iris26639CandidateP25Guide = Float.NaN;
-        iris26639CandidateP50Guide = Float.NaN;
         iris26582CandidateP95Guide = Float.NaN;
         iris26550CandidateP99Guide = Float.NaN;
         iris26583CandidateP995Guide = Float.NaN;
@@ -108,12 +104,8 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
         iris26584CandidateGuideGrid = null;
         iris26584CandidateGridWidth = 0;
         iris26584CandidateGridHeight = 0;
-        basePipeline.mParameters.motionV2ToneP25Guide = Float.NaN;
-        basePipeline.mParameters.motionV2ToneP50Guide = Float.NaN;
         basePipeline.mParameters.motionV2ToneP95Guide = Float.NaN;
         basePipeline.mParameters.motionV2ToneP99Guide = Float.NaN;
-        basePipeline.mParameters.motionV2ShadowBodyStrength = 0.0f;
-        basePipeline.mParameters.motionV2ShadowBodyEnd = 0.30f;
         basePipeline.mParameters.motionV2ToneP995Guide = Float.NaN;
         basePipeline.mParameters.motionV2ToneP998Guide = Float.NaN;
         basePipeline.mParameters.motionV2TonePredictedClipFraction = 0.0f;
@@ -208,8 +200,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             }
             basePipeline.mParameters.motionV2DisplayGain = gain;
             ToneDecision iris26583Tone = iris26583ToneDecision(gain);
-            basePipeline.mParameters.motionV2ToneP25Guide = iris26639CandidateP25Guide;
-            basePipeline.mParameters.motionV2ToneP50Guide = iris26639CandidateP50Guide;
             basePipeline.mParameters.motionV2ToneP95Guide = iris26582CandidateP95Guide;
             basePipeline.mParameters.motionV2ToneP99Guide = iris26550CandidateP99Guide;
             basePipeline.mParameters.motionV2ToneP995Guide = iris26583CandidateP995Guide;
@@ -223,45 +213,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             basePipeline.mParameters.motionV2ToneBaseSceneWhite = iris26583Tone.baseSceneWhite;
             basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite = iris26583Tone.adaptiveSceneWhite;
             basePipeline.mParameters.motionV2ToneAdaptiveStrength = iris26583Tone.adaptiveStrength;
-
-            /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BODY_DECISION
-             * Reuse the already-frozen linear candidate sample set. The decision is driven only by
-             * actual mapped body/highlight distribution: no ISO, shutter, exposure metadata or
-             * effectiveSupport participates. Dark scenes with genuinely separated lower tails stay
-             * open; high-DR scenes whose P25 crowds toward P50 receive at most the renderer's 0.25 EV
-             * low-frequency body-depth correction. Night is intentionally frozen. */
-            float mappedP25 = MotionV2Render.iris26623MapMotionSdrFinalGuide(
-                    iris26639CandidateP25Guide, gain,
-                    iris26583Tone.projectedBroadNearFraction, iris26583Tone.projectedHardFraction,
-                    iris26583Tone.baseSceneWhite, iris26583Tone.adaptiveSceneWhite);
-            float mappedP50 = MotionV2Render.iris26623MapMotionSdrFinalGuide(
-                    iris26639CandidateP50Guide, gain,
-                    iris26583Tone.projectedBroadNearFraction, iris26583Tone.projectedHardFraction,
-                    iris26583Tone.baseSceneWhite, iris26583Tone.adaptiveSceneWhite);
-            float mappedP95 = MotionV2Render.iris26623MapMotionSdrFinalGuide(
-                    iris26582CandidateP95Guide, gain,
-                    iris26583Tone.projectedBroadNearFraction, iris26583Tone.projectedHardFraction,
-                    iris26583Tone.baseSceneWhite, iris26583Tone.adaptiveSceneWhite);
-            float bodyDrEv26639 = log2((mappedP95 + 0.015625f) / Math.max(mappedP50 + 0.015625f, 1.0e-6f));
-            float lowerCrowding26639 = mappedP25 / Math.max(mappedP50, 1.0e-6f);
-            float drGate26639 = smoothstep(0.80f, 1.55f, bodyDrEv26639);
-            float crowdGate26639 = smoothstep(0.50f, 0.72f, lowerCrowding26639);
-            float shadowStrength26639 = iris26550Night ? 0.0f : clamp(drGate26639 * crowdGate26639, 0.0f, 1.0f);
-            float shadowBodyEnd26639 = clamp(mappedP50 * 1.05f, 0.18f, 0.42f);
-            basePipeline.mParameters.motionV2ShadowBodyStrength = shadowStrength26639;
-            basePipeline.mParameters.motionV2ShadowBodyEnd = shadowBodyEnd26639;
-            Log.i(Name, "IRIS_26639_SIGNAL_DRIVEN_SHADOW_BODY"
-                    + " p25Guide=" + iris26639CandidateP25Guide
-                    + " p50Guide=" + iris26639CandidateP50Guide
-                    + " p95Guide=" + iris26582CandidateP95Guide
-                    + " mappedP25=" + mappedP25
-                    + " mappedP50=" + mappedP50
-                    + " mappedP95=" + mappedP95
-                    + " broadDrEv=" + bodyDrEv26639
-                    + " lowerCrowding=" + lowerCrowding26639
-                    + " strength=" + shadowStrength26639
-                    + " bodyEnd=" + shadowBodyEnd26639
-                    + " maxCorrectionEv=0.25 signalDriven=true metadataDriven=false effectiveSupportDriven=false");
             valid = true;
 
             Log.i(Name, "IRIS_26583_PROJECTED_HIGHLIGHT_TAIL_DECISION"
@@ -440,10 +391,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                     iris26582CandidateGuides.add(Math.max(s.r, Math.max(s.g, s.b)));
                 }
                 Collections.sort(iris26582CandidateGuides);
-                int p25 = Math.max(0, Math.min(iris26582CandidateGuides.size() - 1,
-                        Math.round((iris26582CandidateGuides.size() - 1) * 0.25f)));
-                int p50 = Math.max(0, Math.min(iris26582CandidateGuides.size() - 1,
-                        Math.round((iris26582CandidateGuides.size() - 1) * 0.50f)));
                 int p95 = Math.max(0, Math.min(iris26582CandidateGuides.size() - 1,
                         Math.round((iris26582CandidateGuides.size() - 1) * 0.95f)));
                 int p99 = Math.max(0, Math.min(iris26582CandidateGuides.size() - 1,
@@ -452,8 +399,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                         Math.round((iris26582CandidateGuides.size() - 1) * 0.995f)));
                 int p998 = Math.max(0, Math.min(iris26582CandidateGuides.size() - 1,
                         Math.round((iris26582CandidateGuides.size() - 1) * 0.998f)));
-                iris26639CandidateP25Guide = iris26582CandidateGuides.get(p25);
-                iris26639CandidateP50Guide = iris26582CandidateGuides.get(p50);
                 iris26582CandidateP95Guide = iris26582CandidateGuides.get(p95);
                 iris26550CandidateP99Guide = iris26582CandidateGuides.get(p99);
                 iris26583CandidateP995Guide = iris26582CandidateGuides.get(p995);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index f864177ba15eb061925feb07e8fb5e17639af8c9..08079ca6d4178dba38c75ed2fc7b8673cdac5523 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -514,15 +514,21 @@ object PhotonMotionMgc1271Bridge {
                 (1f / checkNotNull(sabreNoiseScale)).coerceIn(1f, stacked.mergedFrameCount.toFloat())
             requireParity(noiseEquivalentSupport.isFinite() && noiseEquivalentSupport >= 1f,
                 "missing/malformed noise-equivalent temporal support")
-            /* IRIS_26639_NO_SUPPORT_DERIVED_GLOBAL_LUMA
-             * noiseEquivalentSupport is diagnostic/merge evidence, not captured-frame count and not
-             * an image-wide noise authority. 26638 proved that a bright 13-NORMAL capture can report
-             * support ~= 1 and falsely drive the completed linear carrier to 0.35 luma smoothing.
-             * Automatic luma therefore returns to zero: only the explicit luma control may request
-             * full-resolution residual luma cleanup. ISO, shutter and exposure are not substitutes.
-             * Sabre reconstruction/rejection, the 26634 local support field and chroma cleanup remain
-             * unchanged. */
-            val automaticLumaScale26639 = 0.0f
+            /* IRIS_26633_LOW_SUPPORT_RESIDUAL_LUMA_FLOOR
+             * Worm-shaped residual luma is exposed when Sabre's measured noise-equivalent support
+             * remains low even after a large nominal frame count. Keep the existing MGC/Pecan
+             * residual denoise owner and measured noise model; add only a bounded support-derived
+             * luma floor. Pecan's frequency/outlier/revert tuning remains the structure authority.
+             * No ISO/scene-brightness heuristic and no post-render blur is introduced.
+             * IRIS_26634_LOCAL_RESIDUAL_NOISE_SUPPORT then only reduces this existing owner in
+             * better-supported 4x4 regions; it never raises the 26633 luma/chroma ceiling. */
+            val supportDeficit26633 =
+                ((3.0f - noiseEquivalentSupport) / 2.0f).coerceIn(0f, 1f)
+            val supportGate26633 = supportDeficit26633 * supportDeficit26633 *
+                (3.0f - 2.0f * supportDeficit26633)
+            val automaticLumaScale26633 = if (irisSettings.noiseReductionEnabled) {
+                0.35f * supportGate26633
+            } else 0f
             /* IRIS_26545_SHARED_RESIDUAL_DENOISE_CONTROLS
              * The reconstruction owns temporal noise reduction. These two user controls act only
              * on the completed linear-RGB carrier, before PostPipeline/tone/UHDR. They never alter
@@ -530,7 +536,7 @@ object PhotonMotionMgc1271Bridge {
              * Luma uses a new preference key and defaults to zero to preserve the proven 26544
              * zero-luma behavior until the user deliberately opts in. Chroma keeps its prior value.
              */
-            val lumaScale = maxOf(requestedLumaScale, automaticLumaScale26639).coerceIn(0f, 2f)
+            val lumaScale = maxOf(requestedLumaScale, automaticLumaScale26633).coerceIn(0f, 2f)
             val chromaScale = irisSettings.chromaDenoise.coerceIn(0f, 2f)
             val denoisePass = MgcFullResolutionDenoise.Pass.SABRE_DEFAULT
             val runFullResolutionDenoise = irisSettings.noiseReductionEnabled &&
@@ -614,7 +620,7 @@ object PhotonMotionMgc1271Bridge {
 
             PLog.i(TAG, "IRIS_26545_DENOISE_NOISE_AUTHORITY " +
                 "stage=PRE_POSTPIPELINE_LINEAR_RGB master=${irisSettings.noiseReductionEnabled} " +
-                "luma=$lumaScale chroma=$chromaScale automaticLuma26639=$automaticLumaScale26639 " +
+                "luma=$lumaScale chroma=$chromaScale automaticLuma26633=$automaticLumaScale26633 " +
                 "noiseEquivalentSupport=$noiseEquivalentSupport executed=$runFullResolutionDenoise " +
                 "pass=$denoisePass noiseSource=$noiseAuthority mgcBase=${mgcBase.timestamp} " +
                 "referenceSnr=$referenceSnr outputTuningSnr=$tuningSnr " +
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index b5c0064e0e13c4b9bd504a08d54ecae6cf5d4a78..94daa7dd3726ee856b35de23fbb57ba98ef27334 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -60,15 +60,8 @@ public class Parameters {
      * Decision-only presentation state produced by MotionV2ViewfinderExposureMatcher from its
      * existing low-resolution candidate probe. No RAW/capture/DNG owner consumes these fields.
      */
-    /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BODY_STATE
-     * Lower/body percentiles come from the same fixed-resolution linear Motion probe that already
-     * owns P95/P99. They are presentation evidence only: no capture/AE/noise owner consumes them. */
-    public float motionV2ToneP25Guide = Float.NaN;
-    public float motionV2ToneP50Guide = Float.NaN;
     public float motionV2ToneP95Guide = Float.NaN;
     public float motionV2ToneP99Guide = Float.NaN;
-    public float motionV2ShadowBodyStrength = 0.0f;
-    public float motionV2ShadowBodyEnd = 0.30f;
     /* IRIS_26583_PROJECTED_COMPACT_HIGHLIGHT_TAIL_STATE
      * P99.5/P99.8 max-channel evidence plus projected baseline-tone ceiling occupancy.
      * Decision-only presentation metadata; reconstruction/chroma/DNG/capture remain untouched.
diff --git a/app/version.properties b/app/version.properties
index 7576a6cf3065e1881f04f88d541bd391949572f4..771b54600b6359cf1a439f514a03d87d1a963dad 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726639
-VERSION_BUILD=26639
+VERSION_NAME=0.9726638
+VERSION_BUILD=26638
