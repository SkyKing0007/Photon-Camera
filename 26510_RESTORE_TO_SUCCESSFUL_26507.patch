diff -ruN candidate/app/src/main/assets/shaders/motionv2/mfsr_bjzhou_rejection_base.glsl base/app/src/main/assets/shaders/motionv2/mfsr_bjzhou_rejection_base.glsl
--- candidate/app/src/main/assets/shaders/motionv2/mfsr_bjzhou_rejection_base.glsl	2026-08-19 16:59:21.547100609 +0000
+++ base/app/src/main/assets/shaders/motionv2/mfsr_bjzhou_rejection_base.glsl	2026-08-18 00:00:00.000000000 +0000
@@ -20,26 +20,14 @@
  * a RAW/2 guide while Iris actually allocated RAW/4. Host now owns the exact
  * geometry-derived threshold.
  */
-/* IRIS_26510_PHYSICAL_MGC_WARP_BOUNDARY
- * MGC rejection validates the same physical sensor evidence final Spatial RGB can consume.
- */
-vec4 sampleBiquadraticAbsolute(sampler2D image,vec2 uv){
-    vec2 ts=1.0/vec2(guideSize);vec2 f=fract(uv*vec2(guideSize));vec2 c=f*f-f+0.5;
-    vec2 lo=0.5*ts,hi=vec2(1.0)-lo;
-    vec2 w0=clamp(uv-c*ts,lo,hi),w1=clamp(uv+c*ts,lo,hi);
-    vec4 s=abs(texture(image,vec2(w0.x,w0.y)))+abs(texture(image,vec2(w0.x,w1.y)))+abs(texture(image,vec2(w1.x,w1.y)))+abs(texture(image,vec2(w1.x,w0.y)));
-    s.w/=1024.0;return 0.25*s;
-}
+vec2 mirrorUv(vec2 uv){uv=mod(uv,2.0);return mix(uv,2.0-uv,greaterThan(uv,vec2(1.0)));}
+vec4 sampleBiquadraticAbsolute(sampler2D image,vec2 uv){vec2 ts=1.0/vec2(guideSize);vec2 f=fract(uv*vec2(guideSize));vec2 c=f*f-f+0.5;vec2 w0=uv-c*ts,w1=uv+c*ts;vec4 s=abs(texture(image,vec2(w0.x,w0.y)))+abs(texture(image,vec2(w0.x,w1.y)))+abs(texture(image,vec2(w1.x,w1.y)))+abs(texture(image,vec2(w1.x,w0.y)));s.w/=1024.0;return 0.25*s;}
 vec3 nvar(vec3 shot,vec3 read,float signal){return max(shot*max(signal,0.0)+read,vec3(1.0e-10));}
 float q8(float v){return round(255.0*clamp(v,0.0,1.0))/255.0;}
 /* IRIS_26487_BJZHOU_REJECTION_EXACT_EQUATION */
 void main(){
     ivec2 p=ivec2(gl_GlobalInvocationID.xy);if(any(greaterThanEqual(p,rawHalf)))return;
-    vec2 uv=(vec2(p)+0.5)/vec2(rawHalf);vec4 flow=texture(flowTexture,uv);
-    vec2 warped=uv+flow.xy/vec2(rawHalf);
-    if(any(lessThan(warped,vec2(0.0)))||any(greaterThanEqual(warped,vec2(1.0)))){
-        imageStore(outReverseWeight,p,vec4(0.0));imageStore(outPixelDifference,p,vec4(0.0));return;
-    }
+    vec2 uv=(vec2(p)+0.5)/vec2(rawHalf);vec4 flow=texture(flowTexture,uv);vec2 warped=mirrorUv(uv+flow.xy/vec2(rawHalf));
     float unblocker=texture(unblockerTexture,uv).r;if(flow.z<flowVariationThreshold)unblocker=0.0;bool motionPrior=flow.z>flowVariationThreshold;
     vec4 reference=texture(referenceGuide,uv);bool greenOnly=reference.w<0.0;reference.w=abs(reference.w)/1024.0;
     vec4 current=sampleBiquadraticAbsolute(currentGuide,warped);
diff -ruN candidate/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_contribute_26501.glsl base/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_contribute_26501.glsl
--- candidate/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_contribute_26501.glsl	2026-08-19 16:59:21.546890758 +0000
+++ base/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_contribute_26501.glsl	2026-08-18 00:00:00.000000000 +0000
@@ -48,12 +48,18 @@
 }
 float wbForColor(int c){return c==0?wbR:(c==2?wbB:1.0);}
 bool inBounds(ivec2 p){return all(greaterThanEqual(p,ivec2(0)))&&all(lessThan(p,rawSize));}
-/* IRIS_26510_PHYSICAL_BORDER_EVIDENCE_OWNER
- * Outside the physical RAW rectangle there is no color evidence. Reference and
- * auxiliary frames obey the same rule; no phase-clamped synthetic CFA support.
- */
+int clampPhaseCoordinate(int v,int extent){
+    int phase=v&1;
+    if(phase>=extent)return extent-1;
+    int last=phase+2*((extent-1-phase)/2);
+    return clamp(v,phase,last);
+}
+ivec2 phaseClamp(ivec2 p){
+    return ivec2(clampPhaseCoordinate(p.x,rawSize.x),clampPhaseCoordinate(p.y,rawSize.y));
+}
 bool ownedSample(ivec2 requested,out ivec2 ownedPixel){
     if(inBounds(requested)){ownedPixel=requested;return true;}
+    if(referenceFrame!=0){ownedPixel=phaseClamp(requested);return true;}
     ownedPixel=ivec2(0);return false;
 }
 float physicalSensor(ivec2 p){
diff -ruN candidate/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_vgn_chroma_26510.glsl base/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_vgn_chroma_26510.glsl
--- candidate/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_vgn_chroma_26510.glsl	2026-08-19 17:01:35.540471108 +0000
+++ base/app/src/main/assets/shaders/motionv2/mfsr_spatial_rgb_vgn_chroma_26510.glsl	1970-01-01 00:00:00.000000000 +0000
@@ -1,53 +0,0 @@
-precision highp float;
-precision highp int;
-uniform highp sampler2D cameraRgb;
-uniform ivec2 rawSize;
-uniform float wbR;
-uniform float wbB;
-out vec4 Output;
-
-/* IRIS_26510_VGN_CHROMA_ONLY_OWNER
- * Full-resolution, post-fusion, camera-RGB chroma-only repair. It preserves the
- * 26507 luma and frame-equivalent alpha exactly and changes only isolated R-G/B-G
- * opponent outliers using the most luma-consistent opposite-neighbour direction.
- */
-bool inside(ivec2 p){return all(greaterThanEqual(p,ivec2(0)))&&all(lessThan(p,rawSize));}
-vec4 px(ivec2 p){return texelFetch(cameraRgb,clamp(p,ivec2(0),rawSize-ivec2(1)),0);}
-float lum(vec3 c){return dot(c,vec3(0.25,0.5,0.25));}
-vec2 opp(vec3 c){return c.rb-vec2(c.g);}
-vec3 toCalculation(vec3 camera){return camera*max(vec3(wbR,1.0,wbB),vec3(1.0e-6));}
-vec3 toCamera(vec3 calculation){return calculation/max(vec3(wbR,1.0,wbB),vec3(1.0e-6));}
-void axisCandidate(ivec2 p,ivec2 d,float yc,out vec2 cand,out float cost,out float consensus,out float valid){
-    ivec2 pa=p-d,pb=p+d;
-    if(!inside(pa)||!inside(pb)){cand=vec2(0.0);cost=1.0e20;consensus=0.0;valid=0.0;return;}
-    vec3 a=toCalculation(max(px(pa).rgb,vec3(0.0))),b=toCalculation(max(px(pb).rgb,vec3(0.0)));
-    float ya=lum(a),yb=lum(b);vec2 oa=opp(a),ob=opp(b);
-    float sigma=max(0.006,0.045*max(yc,0.04));float wa=exp(-abs(ya-yc)/sigma),wb=exp(-abs(yb-yc)/sigma);
-    cand=(oa*wa+ob*wb)/max(wa+wb,1.0e-7);
-    float pair=length(oa-ob),ct=max(0.010,0.055*max(yc,0.05));
-    consensus=1.0-smoothstep(ct,3.0*ct,pair);
-    cost=abs(ya-yc)+abs(yb-yc)+0.35*abs(ya-yb)+0.20*pair;valid=1.0;
-}
-float supportAt(ivec2 p){return max(px(p).a,1.0);}
-float supportJump(ivec2 p,float s){
-    float j=0.0;const ivec2 d[8]=ivec2[8](ivec2(-1,0),ivec2(1,0),ivec2(0,-1),ivec2(0,1),ivec2(-1,-1),ivec2(1,-1),ivec2(-1,1),ivec2(1,1));
-    for(int i=0;i<8;++i)if(inside(p+d[i]))j=max(j,abs(supportAt(p+d[i])-s));return j;
-}
-void main(){
-    ivec2 p=ivec2(gl_FragCoord.xy);if(!inside(p)){Output=vec4(0.0);return;}
-    vec4 center=px(p);vec3 camera=max(center.rgb,vec3(0.0));vec3 rgb=toCalculation(camera);float y=lum(rgb);vec2 centerOpp=opp(rgb);
-    vec2 c0,c1,c2,c3;float k0,k1,k2,k3,n0,n1,n2,n3,v0,v1,v2,v3;
-    axisCandidate(p,ivec2(1,0),y,c0,k0,n0,v0);axisCandidate(p,ivec2(0,1),y,c1,k1,n1,v1);
-    axisCandidate(p,ivec2(1,1),y,c2,k2,n2,v2);axisCandidate(p,ivec2(1,-1),y,c3,k3,n3,v3);
-    vec2 cand=c0;float best=k0,consensus=n0,valid=v0;
-    if(k1<best){cand=c1;best=k1;consensus=n1;valid=v1;}if(k2<best){cand=c2;best=k2;consensus=n2;valid=v2;}if(k3<best){cand=c3;best=k3;consensus=n3;valid=v3;}
-    if(valid<0.5){Output=center;return;}
-    float diff=length(centerOpp-cand),thr=max(0.012,0.040*max(y,0.05));
-    float outlier=smoothstep(thr,2.75*thr,diff)*consensus;float highlightRisk=smoothstep(0.16,0.58,y);
-    float support=max(center.a,1.0),jump=supportJump(p,support);float supportRisk=smoothstep(1.25,4.0,jump)*(1.0-smoothstep(10.0,14.0,support));
-    float edgeDist=float(min(min(p.x,p.y),min(rawSize.x-1-p.x,rawSize.y-1-p.y)));float borderRisk=1.0-smoothstep(2.0,9.0,edgeDist);
-    float authority=max(0.72*highlightRisk,max(0.82*supportRisk,0.78*borderRisk));float blend=clamp(outlier*authority,0.0,0.82);
-    vec2 fo=mix(centerOpp,cand,blend);float g=y-0.25*(fo.x+fo.y);vec3 filtered=vec3(g+fo.x,g,g+fo.y);
-    if(any(lessThan(filtered,vec3(0.0))))filtered=rgb;
-    Output=vec4(toCamera(filtered),center.a);
-}
diff -ruN candidate/app/src/main/cpp/motionv2_jpeg444_jni.cpp base/app/src/main/cpp/motionv2_jpeg444_jni.cpp
--- candidate/app/src/main/cpp/motionv2_jpeg444_jni.cpp	2026-08-19 16:59:21.549602679 +0000
+++ base/app/src/main/cpp/motionv2_jpeg444_jni.cpp	2026-08-18 00:00:00.000000000 +0000
@@ -8,11 +8,10 @@
 #include <turbojpeg.h>
 #include "motionv2_jpeg_r_encoder.h"
 #define TAG "MotionV2Jpeg444"
-/* IRIS_26510_JPEG_HUFFMAN_SPEED_OWNER: entropy optimization off; quantization/quality/444 pixels unchanged. */
 namespace { struct U{JNIEnv*e;jstring s;const char*c;U(JNIEnv*e,jstring s):e(e),s(s),c(s?e->GetStringUTFChars(s,nullptr):nullptr){}~U(){if(c)e->ReleaseStringUTFChars(s,c);}};
 bool write(const char*p,const unsigned char*d,size_t n){FILE*f=fopen(p,"wb");if(!f)return false;size_t w=fwrite(d,1,n,f);int a=fflush(f),b=fclose(f);return w==n&&a==0&&b==0;}
 bool f3(JNIEnv*e,jfloatArray a,std::array<float,3>*o){if(!a||e->GetArrayLength(a)!=3)return false;e->GetFloatArrayRegion(a,0,3,o->data());return !e->ExceptionCheck();}}
-extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeNative(JNIEnv*e,jclass,jobject bitmap,jstring path,jint quality){AndroidBitmapInfo i{};if(!bitmap||!path||AndroidBitmap_getInfo(e,bitmap,&i)!=ANDROID_BITMAP_RESULT_SUCCESS||i.format!=ANDROID_BITMAP_FORMAT_RGBA_8888)return JNI_FALSE;void*p=nullptr;if(AndroidBitmap_lockPixels(e,bitmap,&p)!=ANDROID_BITMAP_RESULT_SUCCESS||!p)return JNI_FALSE;tjhandle h=tj3Init(TJINIT_COMPRESS);if(!h){AndroidBitmap_unlockPixels(e,bitmap);return JNI_FALSE;}int q=std::clamp((int)quality,1,100);bool cfg=tj3Set(h,TJPARAM_QUALITY,q)>=0&&tj3Set(h,TJPARAM_SUBSAMP,TJSAMP_444)>=0&&tj3Set(h,TJPARAM_OPTIMIZE,0)>=0;unsigned char*out=nullptr;size_t n=0;int rc=cfg?tj3Compress8(h,(const unsigned char*)p,(int)i.width,(int)i.stride,(int)i.height,TJPF_RGBA,&out,&n):-1;AndroidBitmap_unlockPixels(e,bitmap);U u(e,path);bool ok=rc>=0&&out&&n&&u.c&&write(u.c,out,n);if(out)tj3Free(out);tj3Destroy(h);return ok?JNI_TRUE:JNI_FALSE;}
-extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_encodeGainmapNative(JNIEnv*e,jclass,jobject bitmap,jstring path,jint quality){AndroidBitmapInfo i{};if(!bitmap||!path||AndroidBitmap_getInfo(e,bitmap,&i)!=ANDROID_BITMAP_RESULT_SUCCESS||(i.format!=ANDROID_BITMAP_FORMAT_A_8&&i.format!=ANDROID_BITMAP_FORMAT_RGBA_8888))return JNI_FALSE;void*p=nullptr;if(AndroidBitmap_lockPixels(e,bitmap,&p)!=ANDROID_BITMAP_RESULT_SUCCESS||!p)return JNI_FALSE;tjhandle h=tj3Init(TJINIT_COMPRESS);int pf=i.format==ANDROID_BITMAP_FORMAT_A_8?TJPF_GRAY:TJPF_RGBA;int ss=i.format==ANDROID_BITMAP_FORMAT_A_8?TJSAMP_GRAY:TJSAMP_444;int q=std::clamp((int)quality,1,100);bool cfg=h&&tj3Set(h,TJPARAM_QUALITY,q)>=0&&tj3Set(h,TJPARAM_SUBSAMP,ss)>=0&&tj3Set(h,TJPARAM_OPTIMIZE,0)>=0;unsigned char*out=nullptr;size_t n=0;int rc=cfg?tj3Compress8(h,(const unsigned char*)p,(int)i.width,(int)i.stride,(int)i.height,pf,&out,&n):-1;AndroidBitmap_unlockPixels(e,bitmap);U u(e,path);bool ok=rc>=0&&out&&n&&u.c&&write(u.c,out,n);if(out)tj3Free(out);if(h)tj3Destroy(h);return ok?JNI_TRUE:JNI_FALSE;}
+extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeNative(JNIEnv*e,jclass,jobject bitmap,jstring path,jint quality){AndroidBitmapInfo i{};if(!bitmap||!path||AndroidBitmap_getInfo(e,bitmap,&i)!=ANDROID_BITMAP_RESULT_SUCCESS||i.format!=ANDROID_BITMAP_FORMAT_RGBA_8888)return JNI_FALSE;void*p=nullptr;if(AndroidBitmap_lockPixels(e,bitmap,&p)!=ANDROID_BITMAP_RESULT_SUCCESS||!p)return JNI_FALSE;tjhandle h=tj3Init(TJINIT_COMPRESS);if(!h){AndroidBitmap_unlockPixels(e,bitmap);return JNI_FALSE;}int q=std::clamp((int)quality,1,100);bool cfg=tj3Set(h,TJPARAM_QUALITY,q)>=0&&tj3Set(h,TJPARAM_SUBSAMP,TJSAMP_444)>=0&&tj3Set(h,TJPARAM_OPTIMIZE,1)>=0;unsigned char*out=nullptr;size_t n=0;int rc=cfg?tj3Compress8(h,(const unsigned char*)p,(int)i.width,(int)i.stride,(int)i.height,TJPF_RGBA,&out,&n):-1;AndroidBitmap_unlockPixels(e,bitmap);U u(e,path);bool ok=rc>=0&&out&&n&&u.c&&write(u.c,out,n);if(out)tj3Free(out);tj3Destroy(h);return ok?JNI_TRUE:JNI_FALSE;}
+extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_encodeGainmapNative(JNIEnv*e,jclass,jobject bitmap,jstring path,jint quality){AndroidBitmapInfo i{};if(!bitmap||!path||AndroidBitmap_getInfo(e,bitmap,&i)!=ANDROID_BITMAP_RESULT_SUCCESS||(i.format!=ANDROID_BITMAP_FORMAT_A_8&&i.format!=ANDROID_BITMAP_FORMAT_RGBA_8888))return JNI_FALSE;void*p=nullptr;if(AndroidBitmap_lockPixels(e,bitmap,&p)!=ANDROID_BITMAP_RESULT_SUCCESS||!p)return JNI_FALSE;tjhandle h=tj3Init(TJINIT_COMPRESS);int pf=i.format==ANDROID_BITMAP_FORMAT_A_8?TJPF_GRAY:TJPF_RGBA;int ss=i.format==ANDROID_BITMAP_FORMAT_A_8?TJSAMP_GRAY:TJSAMP_444;int q=std::clamp((int)quality,1,100);bool cfg=h&&tj3Set(h,TJPARAM_QUALITY,q)>=0&&tj3Set(h,TJPARAM_SUBSAMP,ss)>=0&&tj3Set(h,TJPARAM_OPTIMIZE,1)>=0;unsigned char*out=nullptr;size_t n=0;int rc=cfg?tj3Compress8(h,(const unsigned char*)p,(int)i.width,(int)i.stride,(int)i.height,pf,&out,&n):-1;AndroidBitmap_unlockPixels(e,bitmap);U u(e,path);bool ok=rc>=0&&out&&n&&u.c&&write(u.c,out,n);if(out)tj3Free(out);if(h)tj3Destroy(h);return ok?JNI_TRUE:JNI_FALSE;}
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_packageJpegRNative(JNIEnv*e,jclass,jstring b,jstring g,jstring o,jint gamut,jfloatArray rmin,jfloatArray rmax,jfloatArray gamma,jfloatArray es,jfloatArray eh,jfloat ds,jfloat dh,jboolean use){U ub(e,b),ug(e,g),uo(e,o);if(!ub.c||!ug.c||!uo.c)return JNI_FALSE;iris26507::GainmapMetadata m;if(!f3(e,rmin,&m.ratioMin)||!f3(e,rmax,&m.ratioMax)||!f3(e,gamma,&m.gamma)||!f3(e,es,&m.epsilonSdr)||!f3(e,eh,&m.epsilonHdr))return JNI_FALSE;m.displaySdr=ds;m.displayHdr=dh;m.useBaseColorSpace=use==JNI_TRUE;std::string err;return iris26507::packageJpegR(ub.c,ug.c,uo.c,gamut,m,&err)?JNI_TRUE:JNI_FALSE;}
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_isJpegRNative(JNIEnv*e,jclass,jstring p){U u(e,p);return u.c&&iris26507::isJpegR(u.c)?JNI_TRUE:JNI_FALSE;}
diff -ruN candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java base/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
--- candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java	2026-08-19 16:59:21.549486836 +0000
+++ base/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java	2026-08-18 00:00:00.000000000 +0000
@@ -698,20 +698,6 @@
             Log.d(TAG,"Error in processingEventsListener.onProcessingFinished:"+Log.getStackTraceString(e));
         }
         imageFile = Paths.get(imageFile.toAbsolutePath() + ".jpg");
-        /* IRIS_26510_ASYNC_JPEGR_OUTPUT_OWNER: immutable output transfers to serialized worker. */
-        if(cameraMode==CameraMode.MOTION){
-            final Path outPath=imageFile;final Bitmap outBitmap=img;final ParseExif.ExifData outExif=exifData;
-            final ByteBuffer outDng=iris26480DeferredDng;final Path outDngPath=dngFile;final Parameters outDngParams=processingParameters;
-            iris26480DeferredDng=null;final long queuedNs=System.nanoTime();
-            MOTION_26480_OUTPUT_EXECUTOR.execute(()->{Integer old=null;boolean saved=false;try{
-                int tid=android.os.Process.myTid();old=android.os.Process.getThreadPriority(tid);android.os.Process.setThreadPriority(tid,android.os.Process.THREAD_PRIORITY_BACKGROUND);
-                saved=ImageSaver.Util.saveBitmapAsJPGMotionV2(outPath,outBitmap,ImageSaver.JPG_QUALITY,outExif);processingEventsListener.notifyImageSavedStatus(saved,outPath);
-                if(outDng!=null){outDng.position(0);boolean ds=ImageSaver.Util.saveStackedRaw(outDngPath,outDng,outDngParams);processingEventsListener.notifyImageSavedStatus(ds,outDngPath);}
-                Log.i(TAG,"IRIS_26510_ASYNC_JPEGR_OUTPUT_COMPLETE saved="+saved+" queuedToSavedMs="+((System.nanoTime()-queuedNs)/1000000L));
-            }catch(Exception e){Log.e(TAG,"IRIS_26510_ASYNC_JPEGR_OUTPUT_FAILED "+Log.getStackTraceString(e));try{processingEventsListener.notifyImageSavedStatus(false,outPath);}catch(Throwable ignored){}
-            }finally{if(outBitmap!=null&&!outBitmap.isRecycled())try{outBitmap.recycle();}catch(Throwable ignored){}if(outDng!=null)try{Allocator.free(outDng);}catch(Throwable ignored){}if(old!=null)try{android.os.Process.setThreadPriority(android.os.Process.myTid(),old);}catch(Throwable ignored){}}});
-            pipeline.close();Allocator.getMemoryCount();Log.i(TAG,"IRIS_26510_CAPTURE_RELEASE_BEFORE_JPEGR_SAVE captureUnlockedAfterImmutableOutputTransfer=true");callback.onFinished();return;
-        }
         //Saves the final bitmap
         final boolean imageSaved;
         if (cameraMode == CameraMode.MOTION) {
diff -ruN candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/MotionV2CfaReconstruction.java base/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/MotionV2CfaReconstruction.java
--- candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/MotionV2CfaReconstruction.java	2026-08-19 17:01:35.542522403 +0000
+++ base/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/MotionV2CfaReconstruction.java	2026-08-18 00:00:00.000000000 +0000
@@ -594,7 +594,6 @@
         GLTexture iris26501ChromaGuideScratch = null;
         GLTexture iris26501RgbCovScratch = null;
         GLTexture iris26501RgbOutput = null;
-        GLTexture iris26510VgnChromaOutput = null;
         GLTexture iris26501LensShading = null;
         int iris26501RgbFramebuffer = 0;
         int iris26501SemanticContributedFrames = 0;
@@ -2130,15 +2129,8 @@
                     iris26501RgbOutput.BufferLoad();
                     glProg.drawBlocks(raw.x, raw.y);
                     android.opengl.GLES30.glBindFramebuffer(android.opengl.GLES30.GL_FRAMEBUFFER, 0);
-                    /* IRIS_26510_VGN_CHROMA_ONLY_HOST: luma/support-preserving full-frame pass. */
-                    iris26510VgnChromaOutput = new GLTexture(raw,new GLFormat(GLFormat.DataType.FLOAT_16,4),null,GL_NEAREST,GL_CLAMP_TO_EDGE);
-                    glProg.useAssetProgram("motionv2/mfsr_spatial_rgb_vgn_chroma_26510");
-                    glProg.setVar("rawSize",raw);glProg.setVar("wbR",wronskiGlobalWbR);glProg.setVar("wbB",wronskiGlobalWbB);glProg.setTexture("cameraRgb",iris26501RgbOutput);
-                    iris26510VgnChromaOutput.BufferLoad();glProg.drawBlocks(raw.x,raw.y);
-                    android.opengl.GLES30.glBindFramebuffer(android.opengl.GLES30.GL_FRAMEBUFFER,0);
                     iris26505LowSupportReference.close();
-                    iris26480ReadbackOutput = iris26510VgnChromaOutput;
-                    Log.i(TAG,"IRIS_26510_VGN_CHROMA_ONLY_APPLIED fullResolution="+raw.x+"x"+raw.y+" lumaAuthority=26507 supportAlphaAuthority=26507 tileLookup=false temporalResampling=false");
+                    iris26480ReadbackOutput = iris26501RgbOutput;
                     Log.i(TAG, "IRIS_26502_STACK_AWARE_RGB_OUTPUT"
                             + " semanticFrames=" + iris26501SemanticContributedFrames
                             + " normalizeCount=1"
@@ -2685,9 +2677,7 @@
             }
             iris26488ReleaseReadbackFramebuffer(iris26501ChromaGuideScratch);
             iris26488ReleaseReadbackFramebuffer(iris26501RgbOutput);
-            iris26488ReleaseReadbackFramebuffer(iris26510VgnChromaOutput);
             if (iris26501LensShading != null) iris26501LensShading.close();
-            if (iris26510VgnChromaOutput != null) iris26510VgnChromaOutput.close();
             if (iris26501RgbOutput != null) iris26501RgbOutput.close();
             if (iris26501ChromaGuideScratch != null) iris26501ChromaGuideScratch.close();
             if (iris26501RgbCovScratch != null) iris26501RgbCovScratch.close();
diff -ruN candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java base/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
--- candidate/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java	2026-08-19 16:59:21.549676904 +0000
+++ base/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java	2026-08-18 00:00:00.000000000 +0000
@@ -26,18 +26,13 @@
             base=output.resolveSibling("."+output.getFileName()+".26507.base.jpg");
             gain=output.resolveSibling("."+output.getFileName()+".26507.gain.jpg");
             Files.deleteIfExists(base);Files.deleteIfExists(gain);Files.deleteIfExists(output);
-            long t0=System.nanoTime();
             if(!writeNative(bitmap,base.toString(),Math.max(1,Math.min(100,quality))))return false;
-            long tBase=System.nanoTime();
             Bitmap contents=gm.getGainmapContents();
             if(contents==null||contents.isRecycled()||!encodeGainmapNative(contents,gain.toString(),GAINMAP_QUALITY))return false;
-            long tGain=System.nanoTime();
             boolean ok=packageJpegRNative(base.toString(),gain.toString(),output.toString(),0,
                     gm.getRatioMin(),gm.getRatioMax(),gm.getGamma(),gm.getEpsilonSdr(),gm.getEpsilonHdr(),
                     gm.getMinDisplayRatioForHdrTransition(),gm.getDisplayRatioForFullHdr(),true)
                     && isJpegRNative(output.toString());
-            long tEnd=System.nanoTime();
-            Log.i(TAG,"IRIS_26510_JPEG444_TIMING baseMs="+((tBase-t0)/1000000L)+" gainMs="+((tGain-tBase)/1000000L)+" packageValidateMs="+((tEnd-tGain)/1000000L)+" optimizeHuffman=false quality="+quality+" gainQuality="+GAINMAP_QUALITY);
             Log.i(TAG,"IRIS_26507_JPEG444 encoded="+ok+" gainmap=true subsampling=444 jpegR="+ok);
             return ok;
         }catch(Throwable t){Log.e(TAG,"IRIS_26507_JPEG444_FAILED",t);return false;}
