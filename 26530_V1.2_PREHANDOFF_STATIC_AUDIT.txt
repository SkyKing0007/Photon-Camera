26530 V1.2 PRE-HANDOFF STATIC AUDIT
==================================
Purpose: build-procedure correction after V1.1 GLSL extractor failure.
Target remains: 0.9726530 / 26530
Branch: experimental-clean-photon-rebuild
Exact successful predecessor HEAD: dae32a760a31a7f9f8d80c612e8b58be4519e637
Routine backup branch: not required/current procedure.

V1.1 first real failure reproduced from Actions output:
- extracted legacy_merge.frag had a formatting newline before #version;
- glslang 16.5.0 rejected #version on line 2;
- Gradle never ran.

V1.2 correction:
- preflight extractor now matches successful 26529's canonicalization (.strip()+newline);
- explicit invariant asserts #version is the first emitted bytes;
- runtime forward patch unchanged;
- runtime rollback patch unchanged;
- apply transformer unchanged;
- independent runtime validator unchanged.

Strict 26529 procedure restored in build wrapper:
- exact predecessor HEAD + exact workflow run selection;
- exact tar SHA + manifest SHA + 950-file manifest;
- no committed runtime/build-infrastructure drift after predecessor;
- forward/rollback generated before candidate writes and compared byte-for-byte;
- patch replay and rollback exactness;
- new 26530 SR shader compile;
- exact 26529 embedded shader compile;
- inherited active shader compile;
- native syntax preflight;
- DNG SubIFD regression test;
- all reproducible proofs rerun post-Gradle;
- bjzhou native vendor manifest checked pre/post;
- cpp/deps exact pre/post contents;
- immutable pre/post-Gradle audited runtime manifest;
- exactly one APK;
- deterministic 950-file next candidate.

Local reproducible checks required before packaging:
- production Python py_compile;
- transformer self-test;
- exact successful-26529 candidate transform;
- independent runtime validator;
- generated patch == certified patch;
- generated rollback == certified rollback;
- forward patch replay == transformed candidate;
- rollback == exact successful 26529;
- corrected shader extractor first-byte #version proof;
- build bash -n;
- workflow YAML parse;
- handoff SHA manifest;
- stale branch and branch-creation command scan;
- deterministic ZIP content/hash validation.

Real Khronos glslang 16.5.0 remains authoritative in GitHub Actions. The handoff intentionally
fails before version bump/Gradle if any shader or inherited regression proof fails.
