26620 R1 PERMANENT PRESENTATION REGRESSIONS

1. Exact successful 26614 global tone/exposure owner remains unchanged. motionV2DisplayGain is consumed once with OUTPUT_EXPOSURE_SCALE=0.80.
2. Uniform spatial fields produce exactly zero Local-Laplacian correction; no global exposure drift is permitted.
3. Exact black receives no new offset. Negative correction is suppressed through deep/lower-shadow guards to prevent a new black-crush owner.
4. Local correction is one common RGB scalar only; channel ratios/hue are not independently remapped.
5. Positive local restoration cannot create a new preexisting-display-gamut clip.
6. Fine-detail band restoration weight is zero; no sharpening/microcontrast owner is reintroduced.
7. Centered 5x5 binomial Gaussian reductions use exact 2:1 phase. 1x and true2x correction sampling follow explicit source geometry and the inherited x=0->1 source workaround.
8. Local field uses continuous GL_LINEAR sampling. Fixed coarse long-edge maps/tile atlases and the 26618/26619 guided-map mechanisms are forbidden.
9. 26618 pre-render local RGB attenuation and near-white gain release are forbidden.
10. 26619 coarse guided-base reconstruction is forbidden.
11. Adaptive Wronski presentation is forbidden in this 26614 restart.
12. The exact 26619 native helper failure class remains guarded: new 26620 native code cannot use an unresolved unqualified smooth01 helper; Local-Laplacian half conversion is explicitly iris26564::halfToFloat.
13. True2x Java/JNI Local-Laplacian argument order is structurally pinned; CPU/GPU consume the same retained correction field rather than solving an independent spatial tone map.
14. SHORT/CFA/Sabre/alignment/denoise/DNG/adaptive-color/gainmap owners stay byte-identical to successful 26614 where outside the exact 9-path allowlist.
15. Generated app/build/** and app/.cxx/** are never runtime-source changes; repository-only scaffolding absent from compiled-candidate authority is excluded; unexpected app/src/** changes still fail strict allowlist equality.
