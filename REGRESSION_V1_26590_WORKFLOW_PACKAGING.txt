26590 carried packaging/handoff regressions

- app/build/** and app/.cxx/** never count as runtime-source changes.
- repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
- unexpected app/src/** changes remain visible and fail.
- strict runtime allowlist equality is never weakened.
- exact successful compiled-candidate artifact is runtime authority; live repository app/src is never runtime authority.
- candidate-first transform, deterministic full-index forward/rollback patches, frozen compiler-tested candidate, PRE-BUILD proof, exactly one APK and post-build invariance remain mandatory.
