Permanent JPEG color-ownership / DNG-invariance regression

26565 implemented the shared Display-P3 JPEG publication boundary but did not implement the requested
Iris-owned Camera2/DNG JPEG color-solver rewrite. JPEG renderers still consumed legacy Parameters /
Converter color ownership. Device comparison remained visibly desaturated.

26566 conditions:
- new IrisJpegColorSolver is JPEG-only and independently implements DNG color-spec behavior;
- solve scene white from SENSOR_NEUTRAL_COLOR_POINT and Camera2 ColorMatrix/CalibrationMatrix;
- dual-illuminant interpolation uses reciprocal correlated-color-temperature with Robertson reference;
- valid ForwardMatrix path maps individual camera RGB through calibration into D50 PCS;
- Bradford D50/scene-white fallback is used only when ForwardMatrix is unavailable;
- legacy wrongCalibration, colorMethod and customCCT heuristics never own the default Iris solver;
- Motion, Night, Super Res and normal JPEG consume the new JPEG-only matrix fields;
- existing Display-P3 publication remains downstream and unchanged;
- dedicated DNG writers and DNG color-shadow owner remain byte-identical;
- existing Parameters lines remain an exact ordered subsequence: only parallel JPEG fields/calls are added;
- all GLSL shaders remain exact successful-26565 bytes.
