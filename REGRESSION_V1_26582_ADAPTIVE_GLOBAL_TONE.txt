26582 V1 ADAPTIVE GLOBAL TONE REGRESSIONS

The visual reference is the user-provided matched Iris/Photon JPEG scene set only. No external Photon implementation is an authority for 26582 code.

Permanent behavior requirements:
- no-HDR-tail scene must preserve exact 26581 base scene-white decision;
- body/midtone response at and below linear guide 0.50 is invariant regardless of adaptive sceneWhite;
- broad window/sky HDR tail must preserve monotonic highlight ordering instead of flattening recoverable clouds to display white;
- robust P99 of a fully engaged HDR tail targets approximately 0.97 SDR, leaving the final display interval for rarer peaks;
- approximately 2.3% predicted clipping must engage strongly; isolated speculars below 0.2% must not globally alter the scene;
- adaptation is global only: no local tone mapping, local contrast or sharpening is introduced;
- highlight compression remains one uniform scalar applied to RGB; no per-channel clipping, hue repair, neutralization or white-mixing is introduced;
- stale 26581 viewfinder-solver neutralMix/overflow-to-white approximation is forbidden; solver and final renderer use the same scalar headroom model;
- 26581 micro-color, false-chroma, foliage/sky, gap-owner and SR material-edge-envelope owners remain byte-identical;
- motionv2/render.glsl and motionv2/gainmap.glsl remain byte-identical to successful 26581;
- Motion/Sabre/VGN/SR/DNG/Night/capture/exposure-frame policy remain unchanged except presentation tone-state fields;
- device-proven GPU publication remains byte-identical.

Required device trace for the actual fix build:
IRIS_26582_SCENE_ADAPTIVE_TONE_DECISION must report p95Guide, p99Guide, displayGain,
predictedClipFraction, baseSceneWhite, adaptiveSceneWhite, adaptiveStrength and highlightTarget,
with localTone=false uniformRgbScalar=true chromaOwnerUnchanged=true.
