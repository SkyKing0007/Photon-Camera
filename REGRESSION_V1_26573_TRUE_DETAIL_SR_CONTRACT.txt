26573 V1 CROSS-FRAME TRUE-DETAIL SUPER RESOLUTION CONTRACT

Runtime authority:
- exact successful 26572 V1 compiled candidate, not repository app/src.
- commit 2349eae1ec3479656171d26ae42fb3d229d3dd09.
- Actions run 33464122028 / job 99720312831.
- artifact 9784306626 photon-26572-v1-true-detail-sr.
- artifact ZIP SHA-256 8863884dacb7fc09dd65d39855c16e2f4892db836ff0273810fde047af88e097.
- deterministic candidate tar SHA-256 afe5b96a032f8571f512af2af031a23cc6de367e6aad35d73ac6db0f984887fe.
- no new backup for 26573; backup-26571-pre-true-detail-sr remains exact pre-redesign architectural rollback.

Problem being corrected:
- 26572 admitted zero-DC 2x2 direct-CFA luminance modulation based on phase and safety gates, but did not prove each 2x sample was repeatable across independent aligned RAW observations.
- thin wires, stems, curved metallic edges and bright/dark transitions could therefore turn phase/alignment instability into extra 50MP edge zippering without adding substantial genuine resolution.
- 26573 must admit high-resolution detail only when cross-frame evidence is both sufficiently supported and temporally stable.

IRIS_26573_CROSS_FRAME_TRUE_DETAIL_LUMA_OWNER:
- Sabre Resolve + VGN remains sole RGB/chroma/highlight and low-frequency identity owner.
- direct-CFA true2x contributes only high-resolution luminance microstructure.
- every retained aligned RAW contributes weighted first/second per-output-pixel luma moments plus sum(w) and sum(w^2).
- temporal effective support is Neff=(sumW^2)/sumW2.
- support gate uses smooth01((Neff-1.50)/1.50).
- relative luma sigma is sqrt(weightedVariance)/max(abs(meanY),0.030).
- stability gate is 1-smooth01((relativeSigma-0.060)/0.120).
- temporal agreement is supportGate*stabilityGate, clamped 0..1.
- the 2x2 block uses the minimum temporal agreement of all four subpixels; one unstable subpixel rejects the whole zero-DC residual.
- final confidence is phaseGate * temporalGate * weakest existing safety gate.
- accepted residual remains zero-DC inside its exact 2x2 cell, bounded to 0.42 relative microstructure before final scalar factor bound 0.68..1.47.
- output RGB is always trusted Sabre/VGN guide RGB multiplied by one scalar; direct-CFA chromaticity is never published.

Evidence/safety:
- phase support remains 4=>1.00, 3=>0.85, 2=>0.50, <2=>0.
- a single observation has zero temporal SR authority; two perfectly stable observations remain deliberately weak; stable independent observations can reach full authority.
- signal, highlight, block-mean chroma/material disagreement and low-frequency direct/guide agreement gates are preserved from successful 26572.
- protected highlight => exact guide when highlight gate closes; the earlier pink/magenta highlight protections remain authoritative.
- unstable alternating/misaligned evidence must return exact guide RGB.
- no unsharp mask, Laplacian, edge-enhancement or broad sharpening kernel is introduced.

Hard runtime proof:
- GPU diagnostic alpha packs exact phaseCount*8+reasonClass into the already-required RGBA16F publication readback; no second full-resolution readback is allowed.
- reason classes are fallback-other, active, strong, temporal-reject, highlight-reject, material/agreement-reject, phase-reject and signal-reject.
- GPU phase count, reason count and 32x24 grid total must each equal exact width*height before successful return.
- CPU fallback packs phase count plus quantized temporal agreement into the existing one-byte-per-pixel phase sidecar; no new full-resolution sidecar is added.
- CPU proof counts only non-overlapping published tile interiors; overlapping denoise halos are excluded from authoritative counts.
- IRIS_26573_SR_PROOF is mandatory through MotionTrace and reports backend, total, active/strong, rejection reasons, phase distribution where available, and compact 32x24 active/strong maps.
- a proof total mismatch is fail-closed rather than silently publishing unproven SR statistics.

Preserved successful behavior:
- GlesIris26529SpatialRgbChromaPostprocessor.kt is byte-identical to successful 26572/26571: clean-sky, no-clump, one-sided luma halo, coherent edge-color, cross-edge chroma exclusion and pink/magenta safety are unchanged.
- motionv2_jpeg444_jni.cpp from IRIS_26571_TRUE2X_GPU_PUBLICATION onward is byte-identical: GLES late publication, bounded bands, two PBOs/fences, concurrent CPU JPEG, exact CPU publication fallback, Display-P3, 4:4:4 base, 1:1 gain and q95 remain unchanged.
- CMakeLists.txt is byte-identical.
- DNG PRE_VGN boundary, capture/frame/exposure/alignment/Sabre/VGN/UHDR ownership remain unchanged.
- renderReadback must NOT require a non-null Iris26571ReadyBand output; deferred two-PBO output remains intentionally reachable exactly as in successful 26571 publication mechanics.

Permanent synthetic regressions:
- stable 8-observation temporal fixture reaches full temporal authority.
- single-observation fixture has zero temporal authority.
- two-observation fixture remains weak rather than full confidence.
- mild stable variation remains accepted.
- alternating 0.15/0.25 luma instability is rejected.
- one unstable subpixel rejects the complete 2x2 direct residual.
- accepted 2x2 direct residual sum remains zero before safety scaling.
- <2 block phases => exact guide.
- protected highlight => exact guide.
- final scalar factor remains within 0.68..1.47.
- accepted output chromaticity remains trusted guide chromaticity.
