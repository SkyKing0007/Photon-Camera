26631 PERMANENT REGRESSION — UHDR GAIN CLIFF / EDGE-MASK FAILURE

Real device failure introduced by 26629/26630:
  sourceGuide <= 1.0 + 1e-4 ? 1.0 : max(sourceGuide, 1.25)
This forces an abrupt 1.00 -> 1.25 luminance gain step and logarithmic code ~27 around nominal white,
which rendered as a bright contour around chandelier/ceiling highlight boundaries while SDR remained clean.

Permanent requirements:
- Motion gain is scalar luminance only.
- Completed SDR primary remains unchanged and exact at SDR display capacity.
- HDR/SDR comparison is performed in matched linear-light domains.
- No fixed 1.25 body-gain floor.
- No nominal-white epsilon switch.
- Gain varies continuously through nominal white.
- Normal body remains unity until HDR rendition intent exceeds final SDR luminance.
- Upper highlight/ceiling energy may gain continuously even when source radiance is below 1.0 if the SDR rendition compressed it.
- 1x and true2x CPU/GPU publication must use the same equation.
- Night's existing non-Motion quotient path remains unchanged.
- Full-resolution one-gain-sample-per-primary-pixel R8 geometry remains unchanged; no smoothing/downsampling is added.
- ratioMax=8 and JPEG-R packaging/capacity metadata remain unchanged.
- SDR tone/local-Laplacian/adaptive-color bytes remain frozen for this correction.

Regression probes include 0.6499/0.65/0.6501 and 0.9999/1.0/1.0001/1.001/1.01 plus upper-range samples,
across several display gains and highlight pressures. There must be no discontinuous jump at nominal white.
