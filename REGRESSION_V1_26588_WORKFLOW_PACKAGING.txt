26587 V1 WORKFLOW / PACKAGING REGRESSION

Verification mechanics inherit successful 26586 without reordering or weakening:
verify_package -> verify_scope -> obtain_authority -> make_candidate -> verify_shaders -> authority-seeded live candidate -> real Kotlin/Java -> real NDK -> deterministic patch proof -> PRE-BUILD SAFETY PROOF -> full assembleDebug -> exactly one APK -> authority-seeded post-build invariance -> deterministic candidate export.

Permanent packaging rules:
- exact successful 26586 Actions artifact/candidate is runtime authority, never repository app/src.
- canonical candidate snapshots are authority-seeded and overlay only live app/src, app/build.gradle and app/version.properties after candidate freeze.
- app/build/** and app/.cxx/** never count as runtime-source changes.
- repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
- unexpected files or bytes inside actual app/src/** remain visible and fail the exact allowlist.
- local-prebuild never claims real GLSL/Kotlin/Java/NDK/full assemble success.
- no backup branch is created for this localized build.
- workflow/build infrastructure may change only for 26586 authority pins, 26587 naming/version, six-file allowlist/counts, the two new shader variants and their exact regressions; no compiler/build gate is weakened.
