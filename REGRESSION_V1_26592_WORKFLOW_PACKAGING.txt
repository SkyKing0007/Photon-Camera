REGRESSION V1 26592 — WORKFLOW/PACKAGING

app/build/** and app/.cxx/** never count as runtime-source changes.
Repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
Strict runtime allowlist equality is never weakened.
Unexpected app/src/** changes remain visible and fail.
Authority-seeded canonical snapshots start from the exact prior successful compiled-candidate file universe and overlay only the intended live runtime source domain.
The frozen compiler-tested candidate must remain byte-identical through assemble and final export.
Deterministic full-index forward/rollback patches must pass at core.abbrev 7/12/40 with fuzz=0 and exact byte equality.

ACTIONS FAILURE 33826394252 PERMANENT REGRESSION:
The sealed workflow previously grepped for literal `Exact 26591 direct-sun fixture`, which was absent from REGRESSION_V1_26592_HDR_RADIOMETRIC_HANDOFF.txt, so Actions failed before any real compiler ran.
The packaged local verify_package gate must now prove that the exact workflow HDR-regression grep literal exists in the sealed HDR regression file before Actions. Workflow/regression literal drift must fail locally.
