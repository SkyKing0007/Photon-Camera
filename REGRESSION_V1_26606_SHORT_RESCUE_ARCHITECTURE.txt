26606 V1 Permanent SHORT Rescue Regressions
===========================================

DEVICE FAILURE MADE PERMANENT
Successful 26605 V1.1 device telemetry showed a valid approximately -2.5 EV SHORT but the active gate treated flow.w as absolute 3x3 flow-range error. In the chandelier capture 2504/2852 sampled cells were >=2 RAW pixels; in the close-filament capture 2839/2852 were >=2 RAW pixels. Under 26605, >=2 meant zero clipped-reference flow confidence. This exact semantic class must never return.

SIMULATION FAILURE MADE PERMANENT
- A first-order affine flow field with large local range must retain near-full local geometry confidence when its center is consistent with the surrounding field.
- An isolated bad center flow must fail closed.
- A coherently biased wrong flow can have zero local residual, so local residual alone is never sufficient. SHORT rescue must also be anchored by measurable NORMAL<->SHORT boundary radiometry.
- Trust through a connected valid clipped core must use bottleneck min/max propagation; repeated confidence multiplication that decays valid trust toward zero is forbidden.
- A low-confidence/discontinuous region remains a propagation barrier.

OWNERSHIP REGRESSIONS
- flow.z remains ordinary Sabre normalized local-variation ownership.
- flow.w is SHORT-rescue local affine residual in RAW pixels only.
- ordinary measurable-NORMAL rejection/unblocker/dilation cannot be the final veto once NORMAL is physically censored and SHORT has passed the dedicated rescue proof.
- only HIGHLIGHT_SHORT invokes the rescue owner.
- old private SHORT masks/means/protected accumulators/fuses/late compositors remain disabled.
- NORMAL Motion LONG remains excluded; Night LONG behavior is unchanged.
- DNG and high-frequency true-2x SR evidence remain NORMAL-only.

ARTIFACT-PROTECTION REGRESSIONS
- exact common 3x3 source-CFA footprint headroom in the RBF merge remains the final physical veto.
- one scalar weight controls all RGB channels; no independent R/G/B rescue.
- SHORT source saturation/headroom, local flow discontinuity, and boundary radiometric disagreement fail closed.
- no localReliableHue-style hue/chroma patch or detail-paste authority is introduced.

CONTRIBUTION-SEMMANTICS REGRESSION
"captured", "scheduled", "confidence generated", "source-clipped SHORT contribution", and "clipped-reference rescue contribution" are distinct states. The bridge may prove scheduling only. Actual highlight rescue is owned by IRIS_26606_SHORT_EFFECTIVE_PROOF and must report a nonzero source-clipped rescue population. Ordinary-region SHORT participation cannot masquerade as highlight recovery.
