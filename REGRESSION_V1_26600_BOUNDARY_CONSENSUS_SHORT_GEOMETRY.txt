26600 V1 permanent regression — boundary-consensus SHORT geometry

Real 26599 device failure: plant candidate=20 -> flow=0 and chandelier candidate=9 -> flow=0; fullActive=0 in both. The old active gate rejected every highlight candidate using one 3x3 max-range flow metric.

Permanent requirements:
- do not require the textureless blown core to pass the old max-range flow.w gate;
- same 2 RAW-pixel geometry tolerance remains;
- local geometry seed needs strict 5/9 majority and rejects ambiguous competing flow clusters;
- non-literal effective-loss recovery must originate from measurable NORMAL/SHORT boundary radiometry and propagate only along coherent flow through highlight cells;
- conflicting motion/occlusion remains reference-only and must leave an untrusted seam rather than crossing;
- literal sensor-code clipping keeps direct trusted-geometry ownership;
- SHORT restoration remains whole-RGB; SHORT is not DNG or SR detail evidence.
