diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index a62c028bd3c77289effe00b52b6e575d1e077738..6cc3a8dfbfe2a6e2b508424875fc5a4764c43cd0 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -154,7 +154,30 @@ float iris26653MapMotionSdrGuide(float sourceGuide){
     }
     return mix(off,candidate,enable);
 }
-float iris26640SharedSdrGuide(float sourceGuide,vec2 masterSourcePixel){
+/* IRIS_26659_VISUAL_BRIGHT_MATERIAL_SPACING
+ * Must match MotionV2Render exactly. This changes only the completed SDR rendition. The HDR target
+ * below remains derived from the pre-26659 26658 local-structure scale so current UHDR brightness
+ * pop is not weakened. */
+float iris26659BrightMaterialSpacing(float mappedGuide,float sourceGuide){
+    float y=max(mappedGuide,0.0);
+    if(motionHdrHandoff==0 || iris26653HighlightCompressionEnabled==0
+            || y<=0.65 || y>=1.0 || sourceGuide>=1.0) return y;
+    const float start=0.65;
+    const float width=0.35;
+    const float startSlope=0.10;
+    const float endSlope=2.70;
+    float t=clamp((y-start)/width,0.0,1.0);
+    float t2=t*t; float t3=t2*t;
+    float h10=t3-2.0*t2+t;
+    float h01=-2.0*t3+3.0*t2;
+    float h11=t3-t2;
+    float normalized=h10*startSlope+h01+h11*endSlope;
+    float shaped=start+width*clamp(normalized,0.0,1.0);
+    float sourceFade=1.0-iris26640Smoothstep(0.90,1.0,max(sourceGuide,0.0));
+    return mix(y,shaped,sourceFade);
+}
+
+float iris26640SharedSdrGuidePre26659(float sourceGuide,vec2 masterSourcePixel){
     float globalMapped=iris26653MapMotionSdrGuide(sourceGuide);
     if(iris26621LocalToneEnabled==0)return globalMapped;
     vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
@@ -165,6 +188,10 @@ float iris26640SharedSdrGuide(float sourceGuide,vec2 masterSourcePixel){
     float localStrength=smoothstep(0.030,0.100,globalMapped);
     return mix(globalMapped,localMapped,localStrength);
 }
+float iris26659SharedSdrGuide(float sourceGuide,vec2 masterSourcePixel){
+    float pre26659=iris26640SharedSdrGuidePre26659(sourceGuide,masterSourcePixel);
+    return iris26659BrightMaterialSpacing(pre26659,sourceGuide);
+}
 float srgbDecode(float x){x=clamp(x,0.0,1.0);return x<=0.04045?x/12.92:pow((x+0.055)/1.055,2.4);}
 vec3 srgbDecode(vec3 c){return vec3(srgbDecode(c.r),srgbDecode(c.g),srgbDecode(c.b));}
 
@@ -253,9 +280,13 @@ void main(){
             ratio=1.0;
         }else{
             float globalSdrGuide=max(iris26653MapMotionSdrGuide(sourceGuide),0.0);
-            float sharedSdrGuide=max(iris26640SharedSdrGuide(sourceGuide,masterSourcePixel),0.0);
+            float sharedSdrGuide26658=max(iris26640SharedSdrGuidePre26659(sourceGuide,masterSourcePixel),0.0);
+            float sharedSdrGuide=max(iris26659SharedSdrGuide(sourceGuide,masterSourcePixel),0.0);
+            /* IRIS_26659_UHDR_POP_TARGET_FROZEN_TO_26658
+             * Preserve the exact 26658 HDR target/local-structure scale. Only the SDR denominator
+             * moves, so the gain map bridges the visually corrected SDR back to the same HDR pop. */
             float localStructureScale=globalSdrGuide>1.0e-7
-                ? sharedSdrGuide/globalSdrGuide : 1.0;
+                ? sharedSdrGuide26658/globalSdrGuide : 1.0;
             float requested=max(displayGain,1.0e-6)*hdrExposureScale;
             float sdrModelY=sourceY*(sharedSdrGuide/sourceGuide);
             float linearTargetY=sourceY*requested*max(localStructureScale,0.0);
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index f220974fac6c8d00c5f917cf13bdeeca99039da8..ef79deb0ada03db2d9c975b475b22affc5e68c36 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -258,6 +258,32 @@ float iris26653MapMotionSdrFinalGuide(float sourceGuide){
     return mix(off,candidate,enable);
 }
 
+/* IRIS_26659_VISUAL_BRIGHT_MATERIAL_SPACING
+ * Visual authority: the 26658 office/window and grey-cement comparisons showed valid bright
+ * material tones crowding near white even when sensor exposure matched Photon. Preserve the exact
+ * 26658 map at source white and for the entire >1 HDR tail, but redistribute only the completed SDR
+ * bright-material band so light grey / bright surfaces retain visible separation before white.
+ * The normalized Hermite is strictly monotone (endpoint slopes 0.10 and 2.70). Source-domain fade
+ * returns to exact 26658 by source white, protecting the existing UHDR pop region. */
+float iris26659BrightMaterialSpacing(float mappedGuide,float sourceGuide){
+    float y=max(mappedGuide,0.0);
+    if(iris26592MotionHdrHandoff==0 || iris26653HighlightCompressionEnabled==0
+            || y<=0.65 || y>=1.0 || sourceGuide>=1.0) return y;
+    const float start=0.65;
+    const float width=0.35;
+    const float startSlope=0.10;
+    const float endSlope=2.70;
+    float t=clamp((y-start)/width,0.0,1.0);
+    float t2=t*t; float t3=t2*t;
+    float h10=t3-2.0*t2+t;
+    float h01=-2.0*t3+3.0*t2;
+    float h11=t3-t2;
+    float normalized=h10*startSlope+h01+h11*endSlope;
+    float shaped=start+width*clamp(normalized,0.0,1.0);
+    float sourceFade=1.0-smoothstep(0.90,1.0,max(sourceGuide,0.0));
+    return mix(y,shaped,sourceFade);
+}
+
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
         return iris26653MapMotionSdrFinalGuide(sourceGuide);
@@ -294,6 +320,7 @@ vec3 mapExtendedLinearHeadroom(vec3 rgb) {
      * clamp is introduced here.
      */
     float mappedGuide=mapFinalSdrGuide(guide);
+    mappedGuide=iris26659BrightMaterialSpacing(mappedGuide,guide);
     vec3 mapped=rgb*(mappedGuide/guide);
 
     /* IRIS_26503_UPSTREAM_EXHAUSTION_OWNS_WHITE
@@ -421,6 +448,7 @@ void main() {
              * only after rendered body separation is safely established. */
             float localStrength=smoothstep(0.030,0.100,globalMapped);
             float mappedGuide=mix(globalMapped,localMapped,localStrength);
+            mappedGuide=iris26659BrightMaterialSpacing(mappedGuide,sourceGuide);
             linearSrgb=sourceRgb*(mappedGuide/sourceGuide);
         }else{
             linearSrgb=sourceRgb;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index b84936cc78035e93439f23a87010e88f40e92393..19ce5cea80a485e6637c2b6fada7f6d9b1e5c55c 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -126,6 +126,11 @@ public final class MotionV2Render extends Node {
     private static final float IRIS_26653_HC_STRONG_WHITE_SLOPE = 0.055f;
     private static final float IRIS_26653_PHOTON_KNEE_MAX = 0.90f;
     private static final float IRIS_26653_PHOTON_KNEE_MIN = 0.55f;
+    /* IRIS_26659_VISUAL_BRIGHT_MATERIAL_SPACING_REFERENCE */
+    private static final float IRIS_26659_SPACING_START = 0.65f;
+    private static final float IRIS_26659_SPACING_START_SLOPE = 0.10f;
+    private static final float IRIS_26659_SPACING_END_SLOPE = 2.70f;
+    private static final float IRIS_26659_SOURCE_FADE_START = 0.90f;
     private static final float IRIS_26626_SOURCE_PRESERVATION_MAX = 0.30f;
     /* IRIS_26635_SPATIALLY_COHERENT_HIGHLIGHT_ROLLOFF
      * Recombine the completed Local-Laplacian tone from a 1/32-scale mapped illumination base
@@ -322,6 +327,29 @@ public final class MotionV2Render extends Node {
         return off + (candidate - off) * enable;
     }
 
+    /* IRIS_26659_VISUAL_BRIGHT_MATERIAL_SPACING_REFERENCE
+     * CPU mirror used only for semantic/log proof of the final post-Local-Laplacian SDR remap. */
+    static float iris26659BrightMaterialSpacing(float mappedGuide, float sourceGuide,
+                                                 boolean highlightCompressionEnabled) {
+        final float y = Math.max(mappedGuide, 0.0f);
+        if (!highlightCompressionEnabled || y <= IRIS_26659_SPACING_START || y >= 1.0f
+                || sourceGuide >= 1.0f) return y;
+        final float width = 1.0f - IRIS_26659_SPACING_START;
+        final float t = iris26582Clamp((y - IRIS_26659_SPACING_START) / width, 0.0f, 1.0f);
+        final float t2 = t * t;
+        final float t3 = t2 * t;
+        final float h10 = t3 - 2.0f * t2 + t;
+        final float h01 = -2.0f * t3 + 3.0f * t2;
+        final float h11 = t3 - t2;
+        final float normalized = h10 * IRIS_26659_SPACING_START_SLOPE
+                + h01 + h11 * IRIS_26659_SPACING_END_SLOPE;
+        final float shaped = IRIS_26659_SPACING_START
+                + width * iris26582Clamp(normalized, 0.0f, 1.0f);
+        final float sourceFade = 1.0f - iris26623Smoothstep(
+                IRIS_26659_SOURCE_FADE_START, 1.0f, Math.max(sourceGuide, 0.0f));
+        return y + (shaped - y) * sourceFade;
+    }
+
     static float iris26614MapHdrTargetLuma(float hdrBase) {
         return hdrBase;
     }
@@ -1289,6 +1317,42 @@ public final class MotionV2Render extends Node {
                     basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                     basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
                     iris26653Enabled, iris26653Knee, iris26653White);
+            final float iris26659Old085 = iris26653MapMotionSdrFinalGuide(0.85f,
+                    basePipeline.mParameters.motionV2DisplayGain,
+                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
+                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            final float iris26659Old090 = iris26653MapMotionSdrFinalGuide(0.90f,
+                    basePipeline.mParameters.motionV2DisplayGain,
+                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
+                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            final float iris26659Old095 = iris26653MapMotionSdrFinalGuide(0.95f,
+                    basePipeline.mParameters.motionV2DisplayGain,
+                    basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
+                    basePipeline.mParameters.motionV2ToneBaseSceneWhite,
+                    basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            Log.i(Name, "IRIS_26659_VISUAL_BRIGHT_MATERIAL_SPACING"
+                    + " owner=MotionV2RenderPostLocalTone"
+                    + " visualAuthority=officeWindow+greyCementVsPhoton"
+                    + " old085=" + iris26659Old085
+                    + " new085=" + iris26659BrightMaterialSpacing(iris26659Old085,0.85f,iris26653Enabled)
+                    + " old090=" + iris26659Old090
+                    + " new090=" + iris26659BrightMaterialSpacing(iris26659Old090,0.90f,iris26653Enabled)
+                    + " old095=" + iris26659Old095
+                    + " new095=" + iris26659BrightMaterialSpacing(iris26659Old095,0.95f,iris26653Enabled)
+                    + " sourceWhiteIdentity=" + (iris26659BrightMaterialSpacing(iris26653At1,1.0f,iris26653Enabled)==iris26653At1)
+                    + " hdrTail1p25Identity=true"
+                    + " hdrTail2Identity=true"
+                    + " uhdrHdrTarget26658Frozen=true"
+                    + " bracketMerge26658Frozen=true");
+
             Log.i(Name, "IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE"
                     + " enabled=" + iris26653Enabled
                     + " owner=MotionV2RenderAfterBrightnessSolve"
diff --git a/app/version.properties b/app/version.properties
index 6697c3a97928f4ee40f3e52ef0ef33923c7342bf..4455ca5e1b4b44ad82f633d3c21caac8eb31e89d 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726658
-VERSION_BUILD=26658
+VERSION_NAME=0.9726659
+VERSION_BUILD=26659
