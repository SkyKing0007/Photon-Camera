diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
index 75265cf..db719c1 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
@@ -2010,6 +2010,7 @@ internal class GlesIris26521SpatialRgbStacker(
                  * Denoise tuning follows Spatial's propagated post-rejection output NoiseModel.
                  * Sharpening remains disabled/frozen and retains the reference diagnostic only.
                  */
+                mgcReferenceSnr = bayerKernelTuning.referenceSnr,
                 mgcDenoiseTuningSnr = finishRawDenoiseSnr,
                 mgcSharpenTuningSnr = bayerKernelTuning.referenceSnr,
                 mgcSpatialReferenceOnlyDiagnostic = referenceOnly,
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt b/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
index 8ad0cb2..c8c41d7 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
@@ -46,6 +46,12 @@ data class RawStackResult(
     val mgcDenoiseShotNoise: FloatArray? = null,
     val mgcSpatialStrengthMap: MgcSpatialStrengthMap? = null,
     val mgcSabreNoiseModelScale: Float? = null,
+    /* IRIS_26537_REFERENCE_SNR_PROVENANCE
+     * Pre-merge/reference SNR is kept separate from propagated post-merge denoise SNR.
+     * It is diagnostic/activation evidence only; FinishRaw denoise remains tuned by the
+     * propagated output-noise model.
+     */
+    val mgcReferenceSnr: Float? = null,
     val mgcDenoiseTuningSnr: Float? = null,
     val mgcSharpenTuningSnr: Float? = null,
     val mgcSharpenAttenuationScale: Float? = null,
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
index 3afbb52..3fac57f 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
@@ -154,6 +154,35 @@ public class ImageSaver {
                 return false;
             }
         }
+        /** IRIS_26537_NIGHT_PLAIN_JPEG_PUBLICATION_FALLBACK
+         * Encoding-only fallback for an already-completed Iris Night bitmap. It deliberately does
+         * NOT call UltraHdrSaver and therefore cannot revive Photon's synthetic gain-map path.
+         * No reconstruction, denoise, tone, ADRC, or single-frame processing occurs here.
+         */
+        public static boolean saveBitmapAsJPGIrisNightPlain(
+                Path fileToSave, Bitmap img, int jpgQuality, ParseExif.ExifData exifData) {
+            if (fileToSave == null || img == null || img.isRecycled()) return false;
+            exifData.COMPRESSION = String.valueOf(jpgQuality);
+            try (OutputStream outputStream = Files.newOutputStream(fileToSave)) {
+                boolean saved = img.compress(Bitmap.CompressFormat.JPEG, jpgQuality, outputStream);
+                outputStream.flush();
+                if (!saved) return false;
+            } catch (Throwable t) {
+                Log.e(TAG, "IRIS_26537_NIGHT_PLAIN_JPEG_FAILED", t);
+                return false;
+            }
+            try {
+                img.recycle();
+                ExifInterface inter = ParseExif.setAllAttributes(fileToSave.toFile(), exifData);
+                inter.saveAttributes();
+                Log.i(TAG, "IRIS_26537_NIGHT_PLAIN_JPEG saved=true ultraHdr=false processingFallback=false");
+                return true;
+            } catch (Throwable t) {
+                Log.e(TAG, "IRIS_26537_NIGHT_PLAIN_JPEG_EXIF_FAILED", t);
+                return false;
+            }
+        }
+
         /** IRIS_26432_MOTION_V2_DIRECT_GAINMAP_JPEG */
         public static boolean saveBitmapAsJPGMotionV2(
                 Path fileToSave,
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 62d3abd..aefa387 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -88,7 +88,18 @@ public final class MotionV2Render extends Node {
         pipeline.motionV2GainMapBitmap = null;
         pipeline.motionV2GainMapMaxRatio = 1.0f;
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
+        /* IRIS_26537_NIGHT_DEFER_ULTRAHDR_UNTIL_AFTER_JIN
+         * Night must not allocate a full-resolution gain map before the 512x512 neural finish.
+         * Motion retains the proven UHDR path byte-for-byte. A later Night build may construct a
+         * Night-owned gain map from the FINAL Jin SDR image; 26537 deliberately publishes Night
+         * as normal JPEG first to prove the corrected lifecycle.
+         */
+        if (basePipeline.mParameters.irisNightActive) {
+            Log.i("MotionV2Render", "IRIS_26537_NIGHT_ULTRAHDR_DEFERRED"
+                    + " preJinGainMap=false motionUltraHdrUntouched=true");
+        }
+        if (basePipeline.mParameters.motionV2Active
+                && Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
             /* IRIS_26470_UHDR_RENDER_GEOMETRY_AUTHORITY */
             Point renderedSdrSize = new Point(WorkingTexture.mSize);
             Point gainSize = new Point(renderedSdrSize);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index 638b00d..0ec9590 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -221,7 +221,11 @@ public class PostPipeline extends GLBasePipeline {
          * IRIS_26432_TRUE_V2_ULTRAHDR_ATTACH
          * Gain map came from pre-tone extended-linear Motion V2 signal.
          */
-        if ((mParameters.motionV2Active || mParameters.irisNightActive) && motionV2GainMapBitmap != null) {
+        if (mParameters.irisNightActive && motionV2GainMapBitmap != null) {
+            throw new IllegalStateException(
+                    "26537 Night pre-Jin UltraHDR gain map is forbidden");
+        }
+        if (mParameters.motionV2Active && motionV2GainMapBitmap != null) {
             boolean attached = MotionV2UltraHdr.attach(
                     res,
                     motionV2GainMapBitmap,
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
index 42ff668..070116f 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
@@ -249,19 +249,60 @@ public class HdrxProcessor extends ProcessorBase {
         if(r.stackedDngRaw16!=null&&rgb==r.stackedDngRaw16)
             throw new IllegalStateException("26535 Night production RGB aliases DNG sidecar");
         p.motionV2EffectiveSupport=r.effectiveSupport;
-        Log.i(TAG,"IRIS_26535_NIGHT_SPATIAL_RGB_PRODUCTION_AUTHORITY jpegCarrier=MGC_SPATIAL_RGB_RGBA32F"
-                +" dngCarrier=normalized16_sidecar_separate rcd=false demosaic=false jin=optional_nnapi_cpu_retry"
-                +" srIntegratedSinglePass="+p.motionV2SuperResOutputEnabled);
+        Log.i(TAG,"IRIS_26537_NIGHT_SPATIAL_RGB_PRODUCTION_AUTHORITY jpegCarrier=MGC_SPATIAL_RGB_RGBA32F"
+                +" dngCarrier=normalized16_sidecar_separate rcd=false demosaic=false jin=optional_cpu_file_backed"
+                +" preJinUltraHdr=false srIntegratedSinglePass="+p.motionV2SuperResOutputEnabled);
+
+        /* IRIS_26537_NIGHT_RELEASE_DNG_BEFORE_JIN
+         * If RAW saving is enabled, serialize the independent normalized16/SR DNG sidecar now and
+         * release it before the native-resolution RGB bitmap and ORT session coexist. This changes
+         * only resource lifetime/save ordering, never the Night reconstruction carrier.
+         */
+        if(saveRAW>=1){
+            boolean rawSavedEarly=false;
+            try{
+                if(p.motionV2SuperResOutputEnabled&&r.superResLinearRawPath!=null
+                        &&r.superResLinearRawWidth>0&&r.superResLinearRawHeight>0){
+                    rawSavedEarly=com.particlesdevs.photoncamera.processing.IrisMotionSuperResDngWriter.write(
+                            dngFile,java.nio.file.Paths.get(r.superResLinearRawPath),
+                            r.superResLinearRawWidth,r.superResLinearRawHeight,p,
+                            r.dngNoiseProfile,r.dngStackFrames,r.dngSupportMin,r.dngSupportP01,
+                            r.dngSupportP10,r.dngSupportMedian,r.dngSupportMean,r.dngSupportMax,
+                            r.dngNoiseEquivalentSupport);
+                }else{
+                    if(r.stackedDngRaw16==null)throw new IllegalStateException("26537 Night normalized16 DNG sidecar missing");
+                    ByteBuffer dngBayer=r.stackedDngRaw16; dngBayer.position(0);
+                    rawSavedEarly=ImageSaver.Util.saveNormalized16StackedRaw(
+                            dngFile,dngBayer,p,r.dngNoiseProfile,r.dngStackFrames,
+                            r.dngSupportMin,r.dngSupportP01,r.dngSupportP10,r.dngSupportMedian,
+                            r.dngSupportMean,r.dngSupportMax,r.dngNoiseEquivalentSupport);
+                }
+            }catch(Throwable rawFailure){
+                Log.e(TAG,"IRIS_26537_NIGHT_DNG_SAVE_FAILED_CONTINUE_JPEG",rawFailure);
+                rawSavedEarly=false;
+            }finally{
+                if(r.stackedDngRaw16!=null)try{Allocator.free(r.stackedDngRaw16);}catch(Throwable ignored){}
+                if(r.superResLinearRawPath!=null)
+                    try{java.nio.file.Files.deleteIfExists(java.nio.file.Paths.get(r.superResLinearRawPath));}catch(Throwable ignored){}
+            }
+            processingEventsListener.notifyImageSavedStatus(rawSavedEarly,dngFile);
+            Log.i(TAG,"IRIS_26537_NIGHT_DNG_RELEASED_BEFORE_JIN saved="+rawSavedEarly
+                    +" dngFailureDoesNotBlockJpeg=true"
+                    +" dngNoiseEquivalentSupport="+r.dngNoiseEquivalentSupport);
+        }
 
         exifData.IMAGE_DESCRIPTION=p.toString()+"\n"+
                 com.particlesdevs.photoncamera.processing.parameters.IrisNightExposureSelector.diagnostics+
-                "\nIRIS_26536_NIGHT photonNight=false mgcSpatialRgb=true spatialBayer=false rcd=false neural=jin512_nnapi_cpu_retry_save_mgc_base superRes="+p.motionV2SuperResOutputEnabled;
+                "\nIRIS_26537_NIGHT photonNight=false mgcSpatialRgb=true spatialBayer=false rcd=false neural=jin512_cpu_file_backed_save_mgc_base preJinUltraHdr=false superRes="+p.motionV2SuperResOutputEnabled;
         com.particlesdevs.photoncamera.processing.opengl.postpipeline.PostPipeline pipeline=
                 new com.particlesdevs.photoncamera.processing.opengl.postpipeline.PostPipeline();
         final long iris26535PostStartNs=System.nanoTime();
         Bitmap img=pipeline.RunIrisNightRgb(rgb,p);
         try{Allocator.free(rgb);}catch(Throwable ignored){}
-        /* IRIS_26536_JIN_OPTIONAL_FINISH_ONLY: failure returns this completed MGC Night bitmap. */
+        Log.i(TAG,"IRIS_26537_NIGHT_RGB_CARRIER_RELEASED_BEFORE_JIN bytes="+expectedRgbBytes);
+        if(android.os.Build.VERSION.SDK_INT>=34 && img!=null && !img.isRecycled() && img.hasGainmap())
+            throw new IllegalStateException("26537 Night bitmap carried pre-Jin UltraHDR gain map");
+        /* IRIS_26537_JIN_OPTIONAL_FINISH_ONLY: failure returns this completed MGC Night bitmap. */
         img=IrisNightNeuralEnhancer.enhanceInPlace(img);
         final long iris26535PostMs=(System.nanoTime()-iris26535PostStartNs)/1_000_000L;
 
@@ -272,35 +313,17 @@ public class HdrxProcessor extends ProcessorBase {
         boolean imageSaved=ImageSaver.Util.saveBitmapAsJPGMotionV2(
                 imageFile,img,ImageSaver.JPG_QUALITY,exifData,
                 srDetailPath,srDetailWidth,srDetailHeight,p,pipeline.cropSize);
+        if(!imageSaved && img!=null && !img.isRecycled()){
+            Log.w(TAG,"IRIS_26537_NIGHT_PUBLICATION_FALLBACK encoder=android_jpeg"
+                    +" sameCompletedMgcOrJinBitmap=true processingFallback=false"
+                    +" photonNight=false adrc=false singleFrame=false");
+            imageSaved=ImageSaver.Util.saveBitmapAsJPGIrisNightPlain(
+                    imageFile,img,ImageSaver.JPG_QUALITY,exifData);
+        }
         processingEventsListener.notifyImageSavedStatus(imageSaved,imageFile);
         if(srDetailPath!=null)try{java.nio.file.Files.deleteIfExists(java.nio.file.Paths.get(srDetailPath));}catch(Throwable ignored){}
-
-        if(saveRAW>=1){
-            boolean rawSaved;
-            if(p.motionV2SuperResOutputEnabled&&r.superResLinearRawPath!=null
-                    &&r.superResLinearRawWidth>0&&r.superResLinearRawHeight>0){
-                rawSaved=com.particlesdevs.photoncamera.processing.IrisMotionSuperResDngWriter.write(
-                        dngFile,java.nio.file.Paths.get(r.superResLinearRawPath),
-                        r.superResLinearRawWidth,r.superResLinearRawHeight,p,
-                        r.dngNoiseProfile,r.dngStackFrames,r.dngSupportMin,r.dngSupportP01,
-                        r.dngSupportP10,r.dngSupportMedian,r.dngSupportMean,r.dngSupportMax,
-                        r.dngNoiseEquivalentSupport);
-                if(r.stackedDngRaw16!=null)try{Allocator.free(r.stackedDngRaw16);}catch(Throwable ignored){}
-            }else{
-                if(r.stackedDngRaw16==null)throw new IllegalStateException("26535 Night normalized16 DNG sidecar missing");
-                ByteBuffer dngBayer=r.stackedDngRaw16; dngBayer.position(0);
-                rawSaved=ImageSaver.Util.saveNormalized16StackedRaw(
-                        dngFile,dngBayer,p,r.dngNoiseProfile,r.dngStackFrames,
-                        r.dngSupportMin,r.dngSupportP01,r.dngSupportP10,r.dngSupportMedian,
-                        r.dngSupportMean,r.dngSupportMax,r.dngNoiseEquivalentSupport);
-                try{Allocator.free(dngBayer);}catch(Throwable ignored){}
-            }
-            processingEventsListener.notifyImageSavedStatus(rawSaved,dngFile);
-        }
-        if(r.superResLinearRawPath!=null)
-            try{java.nio.file.Files.deleteIfExists(java.nio.file.Paths.get(r.superResLinearRawPath));}catch(Throwable ignored){}
         final long iris26535SaveMs=(System.nanoTime()-iris26535SaveStartNs)/1_000_000L;
-        Log.i(TAG,"IRIS_26536_NIGHT_TIMING mergeMs="+iris26535MergeMs
+        Log.i(TAG,"IRIS_26537_NIGHT_TIMING mergeMs="+iris26535MergeMs
                 +" postPlusJinMs="+iris26535PostMs+" saveMs="+iris26535SaveMs
                 +" totalMs="+((System.nanoTime()-iris26535NightStartNs)/1_000_000L)
                 +" secondMgcPass=false");
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightNeuralEnhancer.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightNeuralEnhancer.java
index 90ff832..020379f 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightNeuralEnhancer.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightNeuralEnhancer.java
@@ -3,91 +3,109 @@ package com.particlesdevs.photoncamera.processing.processor;
 import android.graphics.Bitmap;
 import com.particlesdevs.photoncamera.app.PhotonCamera;
 import com.particlesdevs.photoncamera.util.Log;
-import java.io.ByteArrayOutputStream;
+import java.io.File;
+import java.io.FileOutputStream;
 import java.io.InputStream;
 import java.nio.FloatBuffer;
+import java.nio.file.Files;
+import java.nio.file.StandardCopyOption;
 import java.util.Collections;
 import ai.onnxruntime.OnnxTensor;
 import ai.onnxruntime.OnnxValue;
 import ai.onnxruntime.OrtEnvironment;
 import ai.onnxruntime.OrtSession;
 
-/** IRIS_26536_JIN_RESILIENT_NEURAL_OWNER
- * Runs the pinned Jin et al. LOL generator only at 512x512. The network result is reduced to
- * a 32x32 RGB gain field and applied in native code to the native-resolution base bitmap.
+/** IRIS_26537_JIN_FILE_BACKED_CPU_OWNER
+ * Jin et al. LOL remains an optional 512x512 RGB finishing stage AFTER completed multiframe MGC
+ * Spatial-RGB reconstruction. The pinned ONNX already includes Jin's residual Add + Tanh output.
  *
- * 26536 keeps NNAPI as the preferred provider but no longer lets an optional neural finishing
- * failure discard a completed multiframe MGC Spatial-RGB Night image. One CPU-only retry is
- * allowed; if both providers fail, the already-rendered MGC Night bitmap is returned unchanged.
- * This is not a Photon/ADRC/single-frame fallback: reconstruction has already completed.
+ * 26537 deliberately removes NNAPI-first startup and the 42.6 MB ByteArrayOutputStream + toByteArray
+ * duplication that coincided with the user's Night process death. The packaged model is streamed
+ * once to an app-private versioned file and ORT opens that path directly with CPU-only execution.
+ * CPU arena and fixed-shape memory-pattern preallocation are disabled to favor bounded/releasable
+ * memory over throughput for this one-image Night inference.
+ *
+ * Any Java/ORT failure returns the already-rendered multiframe MGC Night bitmap unchanged. This is
+ * never Photon Night, ADRC, ExposureFusion, PyramidMerging, or a single-frame reconstruction fallback.
  */
 public final class IrisNightNeuralEnhancer {
     private static final String TAG="IrisNightNeural";
     private static final int N=512, GRID=32;
+    private static final long MODEL_BYTES=42571162L;
+    private static final String MODEL_ASSET="models/iris_night_jin_lol_512.onnx";
+    private static final String MODEL_FILE="iris_night_jin_lol_512_bb7f911a.onnx";
     private static OrtEnvironment env;
-    private static OrtSession nnapiSession;
     private static OrtSession cpuSession;
-    private static String nnapiInputName;
     private static String cpuInputName;
-    private static byte[] modelBytes;
-    private static boolean nnapiDisabledAfterFailure;
+    private static File modelFile;
     static { System.loadLibrary("motionv2jpeg"); }
     private IrisNightNeuralEnhancer(){}
 
-    private static synchronized void ensureModel() throws Exception {
+    private static synchronized File ensureModelFile() throws Exception {
         if(env==null) env=OrtEnvironment.getEnvironment();
-        if(modelBytes!=null) return;
-        try(InputStream in=PhotonCamera.getAssetLoader().getInputStream("models/iris_night_jin_lol_512.onnx");
-            ByteArrayOutputStream o=new ByteArrayOutputStream()){
-            byte[] b=new byte[1<<16];
-            for(int r;(r=in.read(b))>0;) o.write(b,0,r);
-            modelBytes=o.toByteArray();
+        if(modelFile!=null && modelFile.isFile() && modelFile.length()==MODEL_BYTES) return modelFile;
+        File dir=new File(PhotonCamera.getApplicationContextStatic().getFilesDir(),"iris_models");
+        if(!dir.isDirectory() && !dir.mkdirs() && !dir.isDirectory())
+            throw new IllegalStateException("26537 cannot create Iris model directory");
+        File target=new File(dir,MODEL_FILE);
+        if(target.isFile() && target.length()==MODEL_BYTES){
+            modelFile=target;
+            Log.i(TAG,"IRIS_26537_JIN_MODEL_FILE_READY source=existing bytes="+target.length());
+            return target;
         }
-        Log.i(TAG,"IRIS_26536_JIN_MODEL_READY bytes="+modelBytes.length+" tensor=1x3x512x512");
-    }
-
-    private static synchronized OrtSession ensureNnapiSession() throws Exception {
-        ensureModel();
-        if(nnapiDisabledAfterFailure) return null;
-        if(nnapiSession!=null) return nnapiSession;
-        long t=System.nanoTime();
-        try(OrtSession.SessionOptions opts=new OrtSession.SessionOptions()){
-            opts.addNnapi();
-            nnapiSession=env.createSession(modelBytes,opts);
-            nnapiInputName=nnapiSession.getInputNames().iterator().next();
+        File tmp=new File(dir,MODEL_FILE+".tmp");
+        Files.deleteIfExists(tmp.toPath());
+        long copied=0L;
+        byte[] buffer=new byte[1<<16];
+        try(InputStream in=PhotonCamera.getAssetLoader().getInputStream(MODEL_ASSET);
+            FileOutputStream out=new FileOutputStream(tmp,false)){
+            for(int r;(r=in.read(buffer))>0;){ out.write(buffer,0,r); copied+=r; }
+            out.flush();
+            out.getFD().sync();
+        }
+        if(copied!=MODEL_BYTES || tmp.length()!=MODEL_BYTES){
+            Files.deleteIfExists(tmp.toPath());
+            throw new IllegalStateException("26537 Jin model copy size mismatch bytes="+copied);
         }
-        Log.i(TAG,"IRIS_26536_JIN_SESSION_CREATE provider=NNAPI ms="+msSince(t));
-        return nnapiSession;
+        Files.move(tmp.toPath(),target.toPath(),StandardCopyOption.REPLACE_EXISTING);
+        if(!target.isFile() || target.length()!=MODEL_BYTES)
+            throw new IllegalStateException("26537 Jin model file validation failed");
+        modelFile=target;
+        Log.i(TAG,"IRIS_26537_JIN_MODEL_FILE_READY source=asset_stream bytes="+target.length()
+                +" javaModelByteArray=false");
+        return target;
     }
 
     private static synchronized OrtSession ensureCpuSession() throws Exception {
-        ensureModel();
         if(cpuSession!=null) return cpuSession;
+        File model=ensureModelFile();
         long t=System.nanoTime();
+        Log.i(TAG,"IRIS_26537_JIN_SESSION_CREATE_BEGIN provider=CPU pathBacked=true"
+                +" cpuArena=false memoryPattern=false");
         try(OrtSession.SessionOptions opts=new OrtSession.SessionOptions()){
-            cpuSession=env.createSession(modelBytes,opts);
+            opts.setExecutionMode(OrtSession.SessionOptions.ExecutionMode.SEQUENTIAL);
+            opts.setInterOpNumThreads(1);
+            opts.setIntraOpNumThreads(2);
+            opts.setMemoryPatternOptimization(false);
+            opts.setCPUArenaAllocator(false);
+            cpuSession=env.createSession(model.getAbsolutePath(),opts);
             cpuInputName=cpuSession.getInputNames().iterator().next();
         }
-        Log.i(TAG,"IRIS_26536_JIN_SESSION_CREATE provider=CPU ms="+msSince(t));
+        Log.i(TAG,"IRIS_26537_JIN_SESSION_CREATE provider=CPU ms="+msSince(t)
+                +" pathBacked=true nnapi=false");
         return cpuSession;
     }
 
-    private static synchronized void disableNnapiAfterFailure(Throwable cause) {
-        nnapiDisabledAfterFailure=true;
-        if(nnapiSession!=null){
-            try{ nnapiSession.close(); }catch(Throwable ignored){}
-            nnapiSession=null;
-            nnapiInputName=null;
-        }
-        Log.e(TAG,"IRIS_26536_JIN_NNAPI_DISABLED_AFTER_FAILURE cause="+describe(cause),cause);
-    }
-
     public static Bitmap enhanceInPlace(Bitmap base){
         if(base==null||base.isRecycled())return base;
         final long totalStart=System.nanoTime();
+        Runtime rt=Runtime.getRuntime();
+        Log.i(TAG,"IRIS_26537_JIN_ENTRY bitmap="+base.getWidth()+"x"+base.getHeight()
+                +" javaUsedMiB="+((rt.totalMemory()-rt.freeMemory())/(1024L*1024L))
+                +" provider=CPU pathBacked=true nnapi=false");
         Bitmap small=null;
         try{
-            ensureModel();
+            ensureCpuSession();
             long prepStart=System.nanoTime();
             small=Bitmap.createScaledBitmap(base,N,N,true);
             int[] px=new int[N*N]; small.getPixels(px,0,N,0,0,N,N);
@@ -98,59 +116,36 @@ public final class IrisNightNeuralEnhancer {
                 f[plane+i]=(((c>>8)&255)/127.5f)-1f;
                 f[2*plane+i]=((c&255)/127.5f)-1f;
             }
-            Log.i(TAG,"IRIS_26536_JIN_PREP ms="+msSince(prepStart)+" tensor=1x3x512x512");
+            Log.i(TAG,"IRIS_26537_JIN_PREP ms="+msSince(prepStart)+" tensor=1x3x512x512 range=-1_to_1");
 
             float[] gains=new float[GRID*GRID*3];
-            boolean inferred=false;
-            Throwable nnapiFailure=null;
-            if(!nnapiDisabledAfterFailure){
-                try{
-                    OrtSession s=ensureNnapiSession();
-                    if(s!=null){
-                        long runStart=System.nanoTime();
-                        fillGainField(s,nnapiInputName,f,gains);
-                        Log.i(TAG,"IRIS_26536_JIN_RUN provider=NNAPI ms="+msSince(runStart)+" success=true");
-                        inferred=true;
-                    }
-                }catch(Throwable t){
-                    nnapiFailure=t;
-                    Log.e(TAG,"IRIS_26536_JIN_RUN provider=NNAPI success=false cause="+describe(t),t);
-                    disableNnapiAfterFailure(t);
-                }
-            }
-
-            if(!inferred){
-                try{
-                    long retryStart=System.nanoTime();
-                    OrtSession s=ensureCpuSession();
-                    fillGainField(s,cpuInputName,f,gains);
-                    Log.i(TAG,"IRIS_26536_JIN_CPU_RETRY ms="+msSince(retryStart)+" success=true"
-                            +" nnapiFailure="+(nnapiFailure==null?"none":describe(nnapiFailure)));
-                    inferred=true;
-                }catch(Throwable cpuFailure){
-                    Log.e(TAG,"IRIS_26536_JIN_CPU_RETRY success=false cause="+describe(cpuFailure),cpuFailure);
-                    Log.w(TAG,"IRIS_26536_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
-                            +" photonFallback=false adrcFallback=false singleFrameFallback=false"
-                            +" totalMs="+msSince(totalStart));
-                    return base;
-                }
+            try{
+                long runStart=System.nanoTime();
+                fillGainField(cpuSession,cpuInputName,f,gains);
+                Log.i(TAG,"IRIS_26537_JIN_RUN provider=CPU ms="+msSince(runStart)+" success=true");
+            }catch(Throwable inferenceFailure){
+                Log.e(TAG,"IRIS_26537_JIN_RUN provider=CPU success=false cause="+describe(inferenceFailure),inferenceFailure);
+                Log.w(TAG,"IRIS_26537_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
+                        +" photonFallback=false adrcFallback=false singleFrameFallback=false"
+                        +" totalMs="+msSince(totalStart));
+                return base;
             }
 
             long applyStart=System.nanoTime();
             if(!applyGainFieldNative(base,gains,GRID,GRID)){
-                Log.e(TAG,"IRIS_26536_JIN_GAIN_APPLY success=false; saving completed MGC Night base");
-                Log.w(TAG,"IRIS_26536_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
+                Log.e(TAG,"IRIS_26537_JIN_GAIN_APPLY success=false; saving completed MGC Night base");
+                Log.w(TAG,"IRIS_26537_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
                         +" reason=nativeGainApply photonFallback=false adrcFallback=false"
                         +" singleFrameFallback=false totalMs="+msSince(totalStart));
                 return base;
             }
-            Log.i(TAG,"IRIS_26536_JIN_GAIN_APPLY ms="+msSince(applyStart)+" success=true grid=32x32");
-            Log.i(TAG,"IRIS_26536_JIN_INFERENCE totalMs="+msSince(totalStart)
-                    +" tensor=512x512 fullResInference=false");
+            Log.i(TAG,"IRIS_26537_JIN_GAIN_APPLY ms="+msSince(applyStart)+" success=true grid=32x32");
+            Log.i(TAG,"IRIS_26537_JIN_INFERENCE totalMs="+msSince(totalStart)
+                    +" tensor=512x512 fullResInference=false provider=CPU");
             return base;
         }catch(Throwable setupFailure){
-            Log.e(TAG,"IRIS_26536_JIN_SETUP_FAILURE cause="+describe(setupFailure),setupFailure);
-            Log.w(TAG,"IRIS_26536_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
+            Log.e(TAG,"IRIS_26537_JIN_SETUP_FAILURE cause="+describe(setupFailure),setupFailure);
+            Log.w(TAG,"IRIS_26537_JIN_SKIPPED_SAVE_MGC_BASE reconstructedMultiframe=true"
                     +" reason=setup photonFallback=false adrcFallback=false singleFrameFallback=false"
                     +" totalMs="+msSince(totalStart));
             return base;
@@ -168,10 +163,10 @@ public final class IrisNightNeuralEnhancer {
             OnnxValue ov=rr.get(0);
             Object value=ov.getValue();
             if(!(value instanceof float[][][][]))
-                throw new IllegalStateException("26536 Jin ONNX output is not NCHW float");
+                throw new IllegalStateException("26537 Jin ONNX output is not NCHW float");
             float[][][][] y=(float[][][][])value;
             if(y.length!=1||y[0].length<3||y[0][0].length<N||y[0][0][0].length<N)
-                throw new IllegalStateException("26536 Jin ONNX output shape is smaller than 1x3x512x512");
+                throw new IllegalStateException("26537 Jin ONNX output shape is smaller than 1x3x512x512");
             for(int gy=0;gy<GRID;gy++)for(int gx=0;gx<GRID;gx++)for(int ch=0;ch<3;ch++){
                 double sum=0; int count=0;
                 for(int yy=gy*cell;yy<(gy+1)*cell;yy+=2)for(int xx=gx*cell;xx<(gx+1)*cell;xx+=2){
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index a791897..ae73d82 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -298,6 +298,7 @@ object PhotonMotionMgc1271Bridge {
             val readNoise = stacked.mgcDenoiseReadNoise
             val shotNoise = stacked.mgcDenoiseShotNoise
             val strength = stacked.mgcSpatialStrengthMap
+            val referenceSnr = stacked.mgcReferenceSnr
             val tuningSnr = stacked.mgcDenoiseTuningSnr
             requireParity(correlation?.size == 128 && correlation.all { it.isFinite() && it >= 0f },
                 "missing/malformed propagated correlation")
@@ -308,8 +309,10 @@ object PhotonMotionMgc1271Bridge {
             requireParity(strength != null && strength.width == (expectedOutputWidth + 3) / 4 &&
                     strength.height == (expectedOutputHeight + 3) / 4,
                 "missing/malformed Spatial strength map")
+            requireParity(referenceSnr != null && referenceSnr.isFinite() && referenceSnr >= 0f,
+                "missing/malformed MGC reference SNR")
             requireParity(tuningSnr != null && tuningSnr.isFinite() && tuningSnr >= 0f,
-                "missing/malformed MGC tuning SNR")
+                "missing/malformed propagated MGC tuning SNR")
             requireParity(lsc == null || stacked.lensShadingCorrectionApplied,
                 "LSC supplied but MGC output did not apply it")
             val iris26535SpatialReliability = publishSpatialReliability(parameters, checkNotNull(strength))
@@ -333,25 +336,29 @@ object PhotonMotionMgc1271Bridge {
                 channelNoiseProfile = if (irisSettings.customNoiseModelEnabled)
                     floatArrayOf() else reference.motionV2NoiseProfile.copyOf(),
             )
-            /* IRIS_26536_ADAPTIVE_MGC_LUMA_RECOVERY
-             * 26530 forced MGC luma to zero to protect fabric/foliage from the old heavy pass.
-             * That exposed correlated low-light luma residuals when later presentation needed a
-             * large lift. Restore only a conservative MGC/Pecan luma component. There is
-             * deliberately NO "flat area" classifier here: MGC's propagated read/shot model,
-             * correlation spectrum, five-scale outlier/revert tuning and native Spatial strength
-             * map remain the local structure/noise authority, so subtle cloth weave is not erased
-             * merely because its large-scale contrast is low.
-             *
-             * A small automatic floor exists only when propagated output SNR is genuinely low;
-             * otherwise a user value of zero remains zero. User-requested luma is deliberately
-             * scaled/capped so 1.0 cannot restore the former heavy-denoise behavior.
+            /* IRIS_26537_SOURCE_SNR_EFFECTIVE_SUPPORT_LUMA_AUTHORITY
+             * 26536 accidentally used finishRawDenoiseSnr here. That value is intentionally the
+             * propagated POST-merge output SNR and reached ~55 in the user's ISO-3210 bathroom
+             * stack, so low-light activation clamped to zero even though referenceSnr was ~4.6
+             * and frame-equivalent support was only ~5. Keep the two domains separate:
+             *   - reference/source SNR + real noise-equivalent support decide WHETHER the small
+             *     luma cleanup is needed;
+             *   - tuningSnr below remains the propagated output-noise authority used BY the
+             *     MGC/Pecan denoiser.
+             * There is still no flat-area classifier and no broad post-merge blur.
              */
             val requestedLumaScale = irisSettings.lumaDenoise.coerceIn(0f, 2f)
-            val lumaSnrRisk = ((32f - tuningSnr!!) / 22f).coerceIn(0f, 1f)
+            val noiseEquivalentSupport = stacked.normalStackedDngNoiseEquivalentSupport
+            requireParity(noiseEquivalentSupport.isFinite() && noiseEquivalentSupport >= 1f,
+                "missing/malformed noise-equivalent temporal support")
+            val lumaSnrRisk = ((24f - referenceSnr!!) / 20f).coerceIn(0f, 1f)
+            val lumaSupportRisk = ((7f - noiseEquivalentSupport) / 5f).coerceIn(0f, 1f)
             val lumaReliabilityRisk =
                 ((0.90f - parameters.motionV2SpatialReliabilityMean) / 0.45f).coerceIn(0f, 1f)
+            val supportWeight = 0.35f + 0.65f * lumaSupportRisk
             val adaptiveLowLightFloor =
-                (0.26f * lumaSnrRisk * (1f + 0.15f * lumaReliabilityRisk)).coerceAtMost(0.30f)
+                (0.30f * lumaSnrRisk * supportWeight *
+                    (1f + 0.10f * lumaReliabilityRisk)).coerceAtMost(0.30f)
             val userLumaContribution = (requestedLumaScale * 0.25f).coerceAtMost(0.45f)
             val lumaScale = maxOf(adaptiveLowLightFloor, userLumaContribution).coerceAtMost(0.45f)
             val chromaScale = irisSettings.chromaDenoise
@@ -424,6 +431,9 @@ object PhotonMotionMgc1271Bridge {
                 "master=${irisSettings.noiseReductionEnabled} " +
                 "requestedLuma=$requestedLumaScale effectiveMgcLuma=$lumaScale " +
                 "adaptiveLowLightFloor=$adaptiveLowLightFloor lumaSnrRisk=$lumaSnrRisk " +
+                "lumaSnrSource=referencePreMerge referenceSnr=$referenceSnr " +
+                "noiseEquivalentSupport=$noiseEquivalentSupport lumaSupportRisk=$lumaSupportRisk " +
+                "propagatedDenoiseTuningSnr=$tuningSnr " +
                 "lumaReliabilityRisk=$lumaReliabilityRisk textureClassifier=false " +
                 "structureAuthority=MGC_PECAN_OUTLIER_REVERT_SPATIAL_STRENGTH " +
                 "chroma=$chromaScale executed=$runFullResolutionDenoise " +
