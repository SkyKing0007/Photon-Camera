26608 V1 PERMANENT REGRESSION CONTRACT — UNIVERSAL HDR SHORT / PROTECTED BOUNDARIES / PREVIEW LIFECYCLE

1. AUTHORITY / SCOPE
- Base must be exact successful 26607 V1 compiled candidate, 1708 app files.
- Runtime delta must equal the exact 5-file allowlist.
- app/build/** and app/.cxx/** never count as runtime-source changes.
- repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
- unexpected actual app/src/** remains visible and fails.
- protected/native/vendor/DNG manifests must remain byte-identical outside the exact allowlist.

2. UNIVERSAL HDR ACQUISITION
- Existing 26380 AE/readiness sampler must remain byte-identical to successful 26607.
- Existing literal physical clipping trigger remains valid.
- New HDR conflict evidence must live in the proven read-only 3x3 dithered sampler and must not modify AE statistics.
- Production decision must contain no semantic object classifier for cloud, window, bulb, reflection, snow, curtain or similar scene labels.
- Broad dynamic-range fixture MUST trigger SHORT.
- Mixed useful dark + bright fixture MUST trigger SHORT.
- Compact coherent sub-clipping bright fixture MUST trigger SHORT.
- Ordinary evenly lit fixture MUST NOT trigger SHORT.
- Uniform bright-but-safe fixture MUST NOT trigger SHORT merely for being bright.
- Low-light fixture without bright conflict MUST NOT trigger SHORT.
- Isolated single-cell/hot-pixel-like bright impulse MUST NOT independently trigger SHORT.
- Dither sequence must cover spatial lattice holes over successive preview RAWs.

3. ONE-TUNNEL SHORT OWNERSHIP
- SHORT remains HIGHLIGHT_SHORT in common production Sabre reconstruction.
- No private SHORT accumulator or late RGB compositor may become active.
- Common Sabre RBF/Resolve/VGN/post-VGN extended HDR master remain inherited.
- NORMAL-only SR high-frequency detail and NORMAL-only DNG ownership remain unchanged.
- normal Motion LONG remains excluded; Night LONG remains common-Sabre Night evidence only.

4. PROTECTED SHORT BOUNDARY
- Measurable/high-gradient SHORT boundary rescue requires local residual proof.
- Neighborhood predictor alone cannot override a poor local residual at a measurable boundary.
- Deep two-phase censored core may bypass measurable-boundary local residual veto and retain successful 26607 component rescue.
- Do not globally cap rescue by ordinary NORMAL weight; clipped-reference veto must not kill real chandelier/highlight recovery.
- Common final 3x3 source clipping and final ~0.75% SHORT saturation fade remain unchanged.
- Repetitive coherent/high-contrast parallel microstructure is a universal structural regression, not a shutter-specific rule.

5. PREVIEW LIFECYCLE
- CaptureController must bind to the GLPreview owned by the exact current CameraFragment view.
- Detached `new GLPreview(activity)` fallback is forbidden.
- Successful 26607 GLPreview surface replay implementation remains byte-identical.
- Late listener registration after a valid surface exists must be able to replay that surface once per generation.
- Stale CameraFragment teardown must not clear a newer surviving CaptureController.
- No-current-fragment-view startup fails closed rather than opening camera against a detached view.

6. PUBLICATION / HDR TRANSPORT
- 26605 RGBA16F physical HDR carrier + normalized VGN proxy + post-VGN extended HDR restore remain inherited.
- MotionV2Render, gain map, UHDR attachment/publication, manual tone, VGN, Resolve and publication compute remain unchanged in 26608.
- 26608 is not permitted to hide missing SHORT acquisition by a new tone/UHDR workaround.

7. SEMANTIC STATE DISCIPLINE
CAPTURED -> SCHEDULED -> ELIGIBLE -> SOURCE_VALID -> GEOMETRY_VALIDATED -> FINAL_WEIGHT_NONZERO -> ACCUMULATOR_CONTRIBUTED -> RESOLVE_EFFECTIVE -> TARGET_RECOVERED -> OUTPUT_PRESERVED.
Earlier state never proves later state. Static/source telemetry must not overclaim device output preservation.

8. PATCH / FREEZE / BUILD
- Deterministic full-index forward/rollback patches must match at core.abbrev 7/12/40, fuzz=0, exact rollback and exact full-app equality.
- Frozen compiler candidate must remain byte-identical through real language compilers, NDK, full assemble and post-build invariance.
- Complete reserved-identifier scan and pinned real glslang 16.5.0 are mandatory for modified runtime-expanded GLSL.
- Real project Kotlin and Java compilers are mandatory.
- Full :app:assembleDebug including both native ABIs is mandatory.
- Exactly one intended APK is mandatory.
- Real failures become permanent applicable regressions; validators never substitute for compilers.
