26600 V1 — Boundary-consensus SHORT geometry ownership

Base/runtime authority: successful 26599 V1 commit 4c4962354fab1603db98d190cff6f9bba5ed46ba, Actions run 33946659672, job 101253882718, artifact 9963583434.

Upload/replace every file in this ZIP at repository root on experimental-clean-photon-rebuild, commit, and push. Do not place payload files directly into live app/src; the guarded Actions build reconstructs exact successful 26599 compiled-candidate authority and overlays only the three sealed runtime files.

User explicitly requested no new backup for 26600. Deterministic full-index forward/rollback patches are packaged and verified.

26599 device proof isolated the blocker: valid bright candidates all failed the SHORT flow gate before SHORT evidence was evaluated. 26600 replaces only that poisoned max-range criterion with strict majority boundary-derived geometry on the existing sparse Sabre grid, preserving the same 2 RAW-pixel tolerance and fail-closed motion boundaries.

This handoff is upload-ready only until GitHub Actions runs real GLSL/Kotlin/Java/NDK/full assemble.
