IRIS 26565 PERMANENT REGRESSION — V1.1 HOST C++ JCONFIG SCOPE

GitHub Actions failure: run 33322926514, rerun job 99288343214.
Failed handoff commit: 0d4c3f6e644af77f5b52d88c96e29f01fe30700a.

Observed failure:
The exact 26564 authority reconstruction, 551/551 semantic validation and deterministic
core.abbrev=7/12/40 forward/rollback patch gates all passed. The handoff then stopped before
the real Gradle/Android NDK compiler sequence in the optional host-only C++ syntax contract:

  app/src/main/cpp/third_party_26507/libjpeg-turbo/src/jpeglib.h:42:10:
  fatal error: 'jconfig.h' file not found

Root cause:
libjpeg-turbo's jconfig.h is a generated configuration header supplied by its configured build
tree. The V1.1 supplemental host clang++ gate pointed directly at raw pinned source headers
without reproducing the configured Android/libjpeg build tree. It therefore was not a hermetic
or authoritative reproduction of the Android NDK compile. A locally prepared environment had
allowed the supplemental check to pass, while the clean Actions runner correctly exposed the
missing generated header. That mismatch violates the strict same-input/same-environment gate
rule.

Permanent conditions for this and every applicable future handoff:
1. Never use this ad-hoc host-only clang++/fsyntax-only libjpeg contract as a blocking native gate.
2. Modified native runtime code must be compiled by the real project Android NDK through the
   packaged Gradle/full :app:assembleDebug sequence.
3. A supplemental native check may only be blocking if it reproduces the exact configured
   generated headers/toolchain/build inputs used by Actions; otherwise it must not substitute
   for or precede the authoritative NDK gate.
4. The sealed handoff must overwrite compile_26565_cpp_contract.py with an inert tombstone and must never invoke it as a compiler gate.
5. The packaged workflow/build script must retain the real Kotlin/Java compiler sequence and
   full :app:assembleDebug, and must fail unless that authoritative build succeeds.
6. Runtime candidate hashes, forward/rollback patches, DNG/protected manifests, version/build,
   and processing architecture remain byte-identical to 26565 V1/V1.1.
