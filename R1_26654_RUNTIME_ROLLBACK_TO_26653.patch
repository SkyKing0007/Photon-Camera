diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index dec1f293ba1150f685254843edab9922b957dd23..a62c028bd3c77289effe00b52b6e575d1e077738 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -15,8 +15,9 @@ uniform float iris26623ToneBroadNearFraction;
 uniform float iris26623ToneHardFraction;
 uniform float iris26623ToneBaseSceneWhite;
 uniform float iris26623ToneAdaptiveSceneWhite;
-uniform int iris26654HighlightCompressionEnabled;
-uniform float iris26654HighlightKnee;
+uniform int iris26653HighlightCompressionEnabled;
+uniform float iris26653HighlightKnee;
+uniform float iris26653AdaptiveWhitePoint;
 /* IRIS_26524_UHDR_ZOOM_GEOMETRY_PARITY */
 out float Output;
 
@@ -114,19 +115,17 @@ float iris26640MapMotionSdrGuide(float sourceGuide){
     return mix(legacy,candidate,adaptiveEnable);
 }
 
-vec3 iris26524BilinearHdr(vec2 sourcePixel);
-
-/* IRIS_26654_UHDR_SHARED_FINAL_SDR_TONE
+/* IRIS_26653_UHDR_SHARED_FINAL_SDR_TONE
  * Gain-map intent must model the exact same completed SDR global tone as MotionV2Render. */
-float iris26654MapMotionSdrGuide(float sourceGuide){
+float iris26653MapMotionSdrGuide(float sourceGuide){
     float x=max(sourceGuide,0.0);
     float off=iris26640MapMotionSdrGuide(x);
-    if(iris26654HighlightCompressionEnabled==0 || x<=0.65)return off;
+    if(iris26653HighlightCompressionEnabled==0 || x<=0.65)return off;
     float requested=max(displayGain,1.0e-6)*hdrExposureScale;
     float enable=iris26640Smoothstep(1.05,1.25,requested);
     if(enable<=1.0e-7)return off;
     float scenePressure=iris26640HighlightPressure();
-    float kneePressure=clamp((0.90-iris26654HighlightKnee)/(0.90-0.55),0.0,1.0);
+    float kneePressure=clamp((0.90-iris26653HighlightKnee)/(0.90-0.55),0.0,1.0);
     float pressure=max(scenePressure,kneePressure);
     float targetWhite=mix(0.915,0.890,pressure);
     float targetSlope=mix(0.090,0.055,pressure);
@@ -148,61 +147,23 @@ float iris26654MapMotionSdrGuide(float sourceGuide){
             +(t3-t2)*width*m1;
     }else{
         float reserve=max(1.0-targetWhite,0.0);
-        float baseWhite=max(iris26623ToneBaseSceneWhite,1.0);
-        float adaptiveWhite=max(iris26623ToneAdaptiveSceneWhite,baseWhite);
-        float whiteSpan=clamp(adaptiveWhite/baseWhite,1.0,3.0);
+        float whiteSpan=max(1.0,sqrt(max(iris26653AdaptiveWhitePoint,1.0)));
         float tailScale=reserve/max(m1,1.0e-6)*whiteSpan;
         float excess=x-1.0;
         candidate=targetWhite+reserve*excess/(excess+tailScale);
     }
     return mix(off,candidate,enable);
 }
-float iris26654HdrGuideAt(vec2 p){
-    vec3 rgb=max(iris26524BilinearHdr(p),vec3(0.0));
-    return max(luminance(rgb),max3(rgb));
-}
-float iris26654GainLocalToneAt(vec2 p){
+float iris26640SharedSdrGuide(float sourceGuide,vec2 masterSourcePixel){
+    float globalMapped=iris26653MapMotionSdrGuide(sourceGuide);
+    if(iris26621LocalToneEnabled==0)return globalMapped;
     vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
-    vec2 uv=(p+vec2(0.5))/max(toneSize,vec2(1.0));
+    vec2 uv=(masterSourcePixel+vec2(0.5))/max(toneSize,vec2(1.0));
     vec2 halfTexel=vec2(0.5)/max(toneSize,vec2(1.0));
     float localLog=texture(iris26621LocalToneLog,clamp(uv,halfTexel,vec2(1.0)-halfTexel)).r;
-    return exp2(clamp(localLog,-12.0,0.0));
-}
-float iris26654GainLocalDeltaAt(vec2 p){
-    return iris26654GainLocalToneAt(p)-iris26654MapMotionSdrGuide(iris26654HdrGuideAt(p));
-}
-float iris26654GainStructureConfidence(vec2 p){
-    float c=iris26654HdrGuideAt(p);
-    float l=iris26654HdrGuideAt(p+vec2(-1.0,0.0));
-    float r=iris26654HdrGuideAt(p+vec2( 1.0,0.0));
-    float u=iris26654HdrGuideAt(p+vec2(0.0,-1.0));
-    float d=iris26654HdrGuideAt(p+vec2(0.0, 1.0));
-    float norm=max(c,0.02);
-    float curvature=abs(4.0*c-l-r-u-d)/norm;
-    float gradient=max(abs(r-l),abs(d-u))/(2.0*norm);
-    return iris26640Smoothstep(0.010,0.050,max(curvature,0.35*gradient));
-}
-float iris26640SharedSdrGuide(float sourceGuide,vec2 masterSourcePixel){
-    float globalMapped=iris26654MapMotionSdrGuide(sourceGuide);
-    if(iris26621LocalToneEnabled==0)return globalMapped;
-    float localMapped=iris26654GainLocalToneAt(masterSourcePixel);
-    float localStrength=iris26640Smoothstep(0.030,0.100,globalMapped);
-    float legacyMapped=mix(globalMapped,localMapped,localStrength);
-    if(iris26654HighlightCompressionEnabled==0 || sourceGuide<=0.65)return legacyMapped;
-    const float radius=8.0;
-    float dc=0.25*(
-        iris26654GainLocalDeltaAt(masterSourcePixel+vec2(-radius,0.0))+
-        iris26654GainLocalDeltaAt(masterSourcePixel+vec2( radius,0.0))+
-        iris26654GainLocalDeltaAt(masterSourcePixel+vec2(0.0,-radius))+
-        iris26654GainLocalDeltaAt(masterSourcePixel+vec2(0.0, radius)));
-    float zeroDc=(localMapped-globalMapped)-dc;
-    float structure=iris26654GainStructureConfidence(masterSourcePixel);
-    float upper=iris26640Smoothstep(0.65,1.0,sourceGuide);
-    float maxAbs=mix(0.018,0.045,upper);
-    float detail=clamp(zeroDc,-maxAbs,maxAbs)*structure;
-    float ownedMapped=clamp(globalMapped+detail,0.0,1.0);
-    float finalOwner=iris26640Smoothstep(0.65,0.72,sourceGuide);
-    return mix(legacyMapped,ownedMapped,finalOwner);
+    float localMapped=exp2(clamp(localLog,-12.0,0.0));
+    float localStrength=smoothstep(0.030,0.100,globalMapped);
+    return mix(globalMapped,localMapped,localStrength);
 }
 float srgbDecode(float x){x=clamp(x,0.0,1.0);return x<=0.04045?x/12.92:pow((x+0.055)/1.055,2.4);}
 vec3 srgbDecode(vec3 c){return vec3(srgbDecode(c.r),srgbDecode(c.g),srgbDecode(c.b));}
@@ -291,7 +252,7 @@ void main(){
         if(sourceGuide<=1.0e-7 || sourceY<=1.0e-9){
             ratio=1.0;
         }else{
-            float globalSdrGuide=max(iris26654MapMotionSdrGuide(sourceGuide),0.0);
+            float globalSdrGuide=max(iris26653MapMotionSdrGuide(sourceGuide),0.0);
             float sharedSdrGuide=max(iris26640SharedSdrGuide(sourceGuide,masterSourcePixel),0.0);
             float localStructureScale=globalSdrGuide>1.0e-7
                 ? sharedSdrGuide/globalSdrGuide : 1.0;
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
index 9e54613cedebb80c2ba9d36400b3b00d2a7e2c9b..f0ce120b5c543dd2912e0b3cf085476100e4cf29 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
@@ -9,8 +9,9 @@ uniform float iris26623ToneBroadNearFraction;
 uniform float iris26623ToneHardFraction;
 uniform float iris26623ToneBaseSceneWhite;
 uniform float iris26623ToneAdaptiveSceneWhite;
-uniform int iris26654HighlightCompressionEnabled;
-uniform float iris26654HighlightKnee;
+uniform int iris26653HighlightCompressionEnabled;
+uniform float iris26653HighlightKnee;
+uniform float iris26653AdaptiveWhitePoint;
 out float Output;
 
 const vec3 IRIS_LUMA = vec3(0.22897456,0.69173852,0.07928691);
@@ -130,19 +131,19 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
     return mix(legacy,candidate,adaptiveEnable);
 }
 
-/* IRIS_26654_SINGLE_FINAL_HIGHLIGHT_TONE
+/* IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE
  * OFF is exact 26652. ON is applied only after brightnessTargetGain/displayGain is frozen.
  * Below source guide 0.65 the map is exactly OFF; above it one C1 monotone curve preserves
  * visible spacing of the real extended-linear >1.0 HDR master. */
-float iris26654MapMotionSdrFinalGuide(float sourceGuide){
+float iris26653MapMotionSdrFinalGuide(float sourceGuide){
     float x=max(sourceGuide,0.0);
     float off=iris26623MapMotionSdrFinalGuide(x);
-    if(iris26654HighlightCompressionEnabled==0 || x<=0.65)return off;
+    if(iris26653HighlightCompressionEnabled==0 || x<=0.65)return off;
     float requested=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
     float enable=smoothstep(1.05,1.25,requested);
     if(enable<=1.0e-7)return off;
     float scenePressure=iris26623HighlightPressure();
-    float kneePressure=clamp((0.90-iris26654HighlightKnee)/(0.90-0.55),0.0,1.0);
+    float kneePressure=clamp((0.90-iris26653HighlightKnee)/(0.90-0.55),0.0,1.0);
     float pressure=max(scenePressure,kneePressure);
     float targetWhite=mix(0.915,0.890,pressure);
     float targetSlope=mix(0.090,0.055,pressure);
@@ -167,9 +168,7 @@ float iris26654MapMotionSdrFinalGuide(float sourceGuide){
             +(t3-t2)*width*m1;
     }else{
         float reserve=max(1.0-targetWhite,0.0);
-        float baseWhite=max(iris26623ToneBaseSceneWhite,1.0);
-        float adaptiveWhite=max(iris26623ToneAdaptiveSceneWhite,baseWhite);
-        float whiteSpan=clamp(adaptiveWhite/baseWhite,1.0,3.0);
+        float whiteSpan=max(1.0,sqrt(max(iris26653AdaptiveWhitePoint,1.0)));
         float tailScale=reserve/max(m1,1.0e-6)*whiteSpan;
         float excess=x-1.0;
         candidate=targetWhite+reserve*excess/(excess+tailScale);
@@ -179,6 +178,6 @@ float iris26654MapMotionSdrFinalGuide(float sourceGuide){
 
 void main(){
     ivec2 p=ivec2(gl_FragCoord.xy);
-    float mapped=iris26654MapMotionSdrFinalGuide(irisGuide(texelFetch(InputBuffer,p,0).rgb));
+    float mapped=iris26653MapMotionSdrFinalGuide(irisGuide(texelFetch(InputBuffer,p,0).rgb));
     Output=log2(max(mapped,IRIS_LOG_FLOOR));
 }
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index 02d291d25f20505dd7b164fe7caff2125245f1ba..ca6c52c963e45000a2731a9335ee0564b448fbc0 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -17,6 +17,7 @@ uniform float edgeSlope;
 uniform float iris26626PreservationStrength;
 uniform float iris26639ShadowBodyStrength;
 uniform float iris26639ShadowBodyEnd;
+uniform int iris26650HighlightCompressionEnabled;
 uniform int iris26626Mode;
 out float Output;
 
@@ -203,11 +204,12 @@ void main(){
         float baseLinear=exp2(clamp(baseLog,-12.0,0.0));
         float residual=currentLinear-baseLinear;
 
-        /* IRIS_26654_LOCAL_LAPLACIAN_NO_HIGHLIGHT_TOGGLE_AUTHORITY
-         * Local Laplacian deliberately does not know whether Highlight Compression is enabled.
-         * It always reconstructs the inherited legacy local candidate. When HC is ON the final
-         * render/gainmap may consume only its bounded zero-DC source-structure residual; absolute
-         * upper-range brightness is owned by the post-brightness global 26654 tone. */
+        /* IRIS_26650_SINGLE_HIGHLIGHT_COMPRESSION_AUTHORITY
+         * OFF must remain the exact successful-26648 presentation path. ON neutralizes only the
+         * later 26635 smooth-base lift that would otherwise push Photon-compressed highlights back
+         * toward white. This is keyed from the capture-frozen PostPipeline owner, not a second UI
+         * read, so the whole capture uses one immutable toggle state. */
+        float iris26650Hc=iris26650HighlightCompressionEnabled!=0 ? 1.0 : 0.0;
         float upperGate=smoothstep(0.65,0.985,baseLinear);
         float shoulderGate=smoothstep(0.70,0.985,baseLinear);
         vec3 iris26646Radiance=iris26646SourceRadianceSurvival(p);
@@ -216,7 +218,7 @@ void main(){
         float structureShoulderScale=1.0-0.72*sourceStructureGate;
         float legacyBaseOut=mix(baseLinear,smoothShoulder,
             0.55*shoulderGate*structureShoulderScale);
-        float baseOut=legacyBaseOut;
+        float baseOut=mix(legacyBaseOut,baseLinear,iris26650Hc);
 
         /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BASE
          * Correct only the low-frequency base B. The 0.05 true-floor endpoint and the scene-derived
@@ -230,19 +232,18 @@ void main(){
         float bodyBump=16.0*bodyT*bodyT*(1.0-bodyT)*(1.0-bodyT);
         float bodyScale=exp2(-0.25*bodyStrength*bodyBump);
         float shadowBase=baseOut*bodyScale;
-        /* IRIS_26654_LOCAL_DETAIL_CANDIDATE_ONLY
-         * Keep the inherited structural measurements, but they no longer own absolute highlight
-         * brightness. 26635/26645/26646 may shape this local candidate only; the HC-ON consumer
-         * removes its local DC component and admits a bounded residual only where the source proves
-         * real structure. */
+        /* IRIS_26650_UNIVERSAL_HIGHLIGHT_RESIDUAL_PRESERVATION
+         * OFF keeps the exact 26648 smooth-bright residual policy. ON preserves the completed
+         * residual at unit weight so the newly created highlight headroom is not immediately
+         * collapsed again. */
         float structureGate=smoothstep(0.012,0.045,abs(residual));
         float smallResidualWeight=mix(1.0,0.78,upperGate);
         float legacyResidualWeight=mix(
                 smallResidualWeight,1.0,max(structureGate,sourceStructureGate));
-        float residualWeight=legacyResidualWeight;
+        float residualWeight=mix(legacyResidualWeight,1.0,iris26650Hc);
         float weightedResidual=residualWeight*residual;
         float candidateLinear=shadowBase+weightedResidual;
-        /* IRIS_26645_LEGACY_LOCAL_CANDIDATE_HEADROOM
+        /* IRIS_26645_STRUCTURAL_HEADROOM_RESERVATION
          * When real pre-tone structure would otherwise hit the display ceiling, lower only B by
          * exactly the proven positive overshoot. R remains unchanged, so the saved SDR retains its
          * local separation instead of becoming a clipped-looking sub-white plateau. */
@@ -250,7 +251,7 @@ void main(){
         float protectedBase=max(shadowBase-sourceStructureGate*positiveOvershoot,
             IRIS_26626_LOG_FLOOR);
         float outLinear=clamp(protectedBase+weightedResidual,IRIS_26626_LOG_FLOOR,1.0);
-        /* IRIS_26646_LEGACY_LOCAL_CANDIDATE_RADIANCE
+        /* IRIS_26646_EXTENDED_LINEAR_RADIANCE_SURVIVAL_OWNER
          * 26645 only used SourceLinear as a texture gate.  Carry the measured missing source
          * residual itself into the scalar tone result, while reserving bounded headroom in the
          * same structured region.  This is luminance-only presentation: final RGB continues to use
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index fdf0486330325d808121cdc18213e610c92a754f..f220974fac6c8d00c5f917cf13bdeeca99039da8 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -13,8 +13,9 @@ uniform float iris26623ToneBroadNearFraction;
 uniform float iris26623ToneHardFraction;
 uniform float iris26623ToneBaseSceneWhite;
 uniform float iris26623ToneAdaptiveSceneWhite;
-uniform int iris26654HighlightCompressionEnabled;
-uniform float iris26654HighlightKnee;
+uniform int iris26653HighlightCompressionEnabled;
+uniform float iris26653HighlightKnee;
+uniform float iris26653AdaptiveWhitePoint;
 uniform float iris26630MotionSaturation;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
@@ -212,19 +213,19 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
 }
 
 
-/* IRIS_26654_SINGLE_FINAL_HIGHLIGHT_TONE
+/* IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE
  * OFF is exact 26652. ON is applied only after brightnessTargetGain/displayGain is frozen.
  * Below source guide 0.65 the map is exactly OFF; above it one C1 monotone curve preserves
  * visible spacing of the real extended-linear >1.0 HDR master. */
-float iris26654MapMotionSdrFinalGuide(float sourceGuide){
+float iris26653MapMotionSdrFinalGuide(float sourceGuide){
     float x=max(sourceGuide,0.0);
     float off=iris26623MapMotionSdrFinalGuide(x);
-    if(iris26654HighlightCompressionEnabled==0 || x<=0.65)return off;
+    if(iris26653HighlightCompressionEnabled==0 || x<=0.65)return off;
     float requested=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
     float enable=smoothstep(1.05,1.25,requested);
     if(enable<=1.0e-7)return off;
     float scenePressure=iris26623HighlightPressure();
-    float kneePressure=clamp((0.90-iris26654HighlightKnee)/(0.90-0.55),0.0,1.0);
+    float kneePressure=clamp((0.90-iris26653HighlightKnee)/(0.90-0.55),0.0,1.0);
     float pressure=max(scenePressure,kneePressure);
     float targetWhite=mix(0.915,0.890,pressure);
     float targetSlope=mix(0.090,0.055,pressure);
@@ -249,9 +250,7 @@ float iris26654MapMotionSdrFinalGuide(float sourceGuide){
             +(t3-t2)*width*m1;
     }else{
         float reserve=max(1.0-targetWhite,0.0);
-        float baseWhite=max(iris26623ToneBaseSceneWhite,1.0);
-        float adaptiveWhite=max(iris26623ToneAdaptiveSceneWhite,baseWhite);
-        float whiteSpan=clamp(adaptiveWhite/baseWhite,1.0,3.0);
+        float whiteSpan=max(1.0,sqrt(max(iris26653AdaptiveWhitePoint,1.0)));
         float tailScale=reserve/max(m1,1.0e-6)*whiteSpan;
         float excess=x-1.0;
         candidate=targetWhite+reserve*excess/(excess+tailScale);
@@ -261,7 +260,7 @@ float iris26654MapMotionSdrFinalGuide(float sourceGuide){
 
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
-        return iris26654MapMotionSdrFinalGuide(sourceGuide);
+        return iris26653MapMotionSdrFinalGuide(sourceGuide);
     }
 
     float start=0.50;
@@ -379,54 +378,6 @@ vec3 iris26638UserSaturation(vec3 rgb,float userSaturation){
     return clamp(vec3(y)+chroma*min(requestedGain,limit),vec3(0.0),vec3(1.0));
 }
 
-
-/* IRIS_26654_BOUNDED_ZERO_DC_LOCAL_DETAIL
- * The 26653 global map is the only absolute highlight brightness owner after brightness solve.
- * Local Laplacian may contribute only a high-pass residual whose local DC component is removed,
- * whose amplitude is bounded, and whose source pixel proves real full-resolution structure.
- * Smooth ceilings/screens therefore cannot form a local bright island, while text/ribs/foliage
- * may retain bounded detail. HC-OFF preserves the inherited local presentation path. */
-float iris26654SourceGuideAt(vec2 p){
-    vec3 rgb=max(iris26524BilinearInput(p),vec3(0.0));
-    return max(luminance(rgb),max3(rgb));
-}
-float iris26654LocalToneAt(vec2 p){
-    vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
-    vec2 uv=(p+vec2(0.5))/max(toneSize,vec2(1.0));
-    vec2 halfTexel=vec2(0.5)/max(toneSize,vec2(1.0));
-    float localLog=texture(iris26621LocalToneLog,clamp(uv,halfTexel,vec2(1.0)-halfTexel)).r;
-    return exp2(clamp(localLog,-12.0,0.0));
-}
-float iris26654LocalDeltaAt(vec2 p){
-    float sourceGuide=iris26654SourceGuideAt(p);
-    return iris26654LocalToneAt(p)-iris26654MapMotionSdrFinalGuide(sourceGuide);
-}
-float iris26654SourceStructureConfidence(vec2 p){
-    float c=iris26654SourceGuideAt(p);
-    float l=iris26654SourceGuideAt(p+vec2(-1.0,0.0));
-    float r=iris26654SourceGuideAt(p+vec2( 1.0,0.0));
-    float u=iris26654SourceGuideAt(p+vec2(0.0,-1.0));
-    float d=iris26654SourceGuideAt(p+vec2(0.0, 1.0));
-    float norm=max(c,0.02);
-    float curvature=abs(4.0*c-l-r-u-d)/norm;
-    float gradient=max(abs(r-l),abs(d-u))/(2.0*norm);
-    float evidence=max(curvature,0.35*gradient);
-    return smoothstep(0.010,0.050,evidence);
-}
-float iris26654BoundedLocalDetail(vec2 p,float sourceGuide,float globalMapped,float localMapped){
-    const float radius=8.0;
-    float dc=0.25*(
-        iris26654LocalDeltaAt(p+vec2(-radius,0.0))+
-        iris26654LocalDeltaAt(p+vec2( radius,0.0))+
-        iris26654LocalDeltaAt(p+vec2(0.0,-radius))+
-        iris26654LocalDeltaAt(p+vec2(0.0, radius)));
-    float zeroDc=(localMapped-globalMapped)-dc;
-    float structure=iris26654SourceStructureConfidence(p);
-    float upper=smoothstep(0.65,1.0,sourceGuide);
-    float maxAbs=mix(0.018,0.045,upper);
-    return clamp(zeroDc,-maxAbs,maxAbs)*structure;
-}
-
 void main() {
     ivec2 xy=ivec2(gl_FragCoord.xy);
     ivec2 sourceSize=textureSize(InputBuffer,0);
@@ -459,25 +410,17 @@ void main() {
         float sourcePeak=max3(sourceRgb);
         float sourceGuide=max(sourceY,sourcePeak);
         if(sourceGuide>1.0e-7){
-            float globalMapped=iris26654MapMotionSdrFinalGuide(sourceGuide);
+            float globalMapped=iris26653MapMotionSdrFinalGuide(sourceGuide);
             vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
             vec2 uv=(sourcePixel+vec2(0.5))/max(toneSize,vec2(1.0));
             vec2 halfTexel=vec2(0.5)/max(toneSize,vec2(1.0));
             float localLog=texture(iris26621LocalToneLog,clamp(uv,halfTexel,vec2(1.0)-halfTexel)).r;
             float localMapped=exp2(clamp(localLog,-12.0,0.0));
-            /* IRIS_26654_SINGLE_ABSOLUTE_HIGHLIGHT_BASE
-             * HC OFF preserves the inherited presentation. HC ON fades stale local absolute-tone
-             * ownership out across the 0.65..0.72 entry band, then uses only bounded zero-DC
-             * source-structure detail around the global 26654 highlight base. */
+            /* IRIS_26621_BLACK_FLOOR_GLOBAL_AUTHORITY
+             * Exact black and deep shadows stay on the global curve; Local Laplacian fades in
+             * only after rendered body separation is safely established. */
             float localStrength=smoothstep(0.030,0.100,globalMapped);
-            float legacyMapped=mix(globalMapped,localMapped,localStrength);
-            float mappedGuide=legacyMapped;
-            if(iris26654HighlightCompressionEnabled!=0 && sourceGuide>0.65){
-                float detail=iris26654BoundedLocalDetail(sourcePixel,sourceGuide,globalMapped,localMapped);
-                float ownedMapped=clamp(globalMapped+detail,0.0,1.0);
-                float finalOwner=smoothstep(0.65,0.72,sourceGuide);
-                mappedGuide=mix(legacyMapped,ownedMapped,finalOwner);
-            }
+            float mappedGuide=mix(globalMapped,localMapped,localStrength);
             linearSrgb=sourceRgb*(mappedGuide/sourceGuide);
         }else{
             linearSrgb=sourceRgb;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java
index a9495f1966b2442a4e4a5254ffc049ded5e0cfb9..7d44501ccb0dc59d0f894a8ef02c155cb543e49d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2PhotonHighlightCompression.java
@@ -197,14 +197,14 @@ public final class MotionV2PhotonHighlightCompression extends Node {
         pipeline.motionV2PhotonHighlightKnee = enabled ? knee : 1.0f;
         pipeline.motionV2PhotonHighlightClippedFraction = clippedFraction;
 
-        /* IRIS_26654_ANALYSIS_ONLY_NO_TONE_WHITE_AUTHORITY
+        /* IRIS_26653_ANALYSIS_ONLY_NO_PRECOLOR_PIXEL_OWNER
          * Keep Photon's histogram/white/knee analysis exactly as measurement evidence, but never
          * publish a pre-color pixel curve. The user toggle is consumed only by MotionV2Render's
          * final extended-linear SDR tone authority after brightnessTargetGain is frozen. */
 
-        final String line = "IRIS_26654_PHOTON_HIGHLIGHT_ANALYSIS"
+        final String line = "IRIS_26653_PHOTON_HIGHLIGHT_ANALYSIS"
                 + " enabled=" + enabled
-                + " owner=analysisOnlyKneeConsumer"
+                + " owner=analysisOnlyFinalToneConsumer"
                 + " toggleApplication=finalExtendedLinearToneOnly"
                 + " photonCommonAnalysis=true"
                 + " histSize=" + HIST_SIZE
@@ -216,8 +216,8 @@ public final class MotionV2PhotonHighlightCompression extends Node {
                 + " sceneWhitePass1=" + sceneWhitePass1
                 + " whitePass1ValidChannels=" + whitePass1.validChannels
                 + " whitePass1Support=" + whitePass1.supportFraction
-                + " adaptiveWhitePointTelemetryOnly=" + adaptiveWhitePoint
-                + " adaptiveWhiteOutputAuthority=false irisSceneWhiteTailAuthority=true"
+                + " adaptiveWhitePoint=" + adaptiveWhitePoint
+                + " adaptiveWhiteSceneKeyLocked=true"
                 + " avgFinal=" + avgFinal
                 + " mpyPreNorm=" + mpyPreNorm
                 + " reinhardNormL=" + normL
@@ -261,7 +261,7 @@ public final class MotionV2PhotonHighlightCompression extends Node {
                 + " irisColorUnchanged=true"
                 + " heicUhHdrUnchanged=true";
         Log.i(Name, line);
-        try { MotionTrace.processingState("IRIS_26654_PHOTON_HIGHLIGHT_ANALYSIS", line); }
+        try { MotionTrace.processingState("IRIS_26653_PHOTON_HIGHLIGHT_ANALYSIS", line); }
         catch (Throwable ignored) {}
 
         WorkingTexture = previousNode.WorkingTexture;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index ddd27667906b219f209fbd843d47421ca0192628..b84936cc78035e93439f23a87010e88f40e92393 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -119,7 +119,7 @@ public final class MotionV2Render extends Node {
     public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.925f;
     public static final float IRIS_26623_SPARSE_WHITE_SLOPE = 0.360f;
     public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.300f;
-    /* IRIS_26654_SINGLE_FINAL_HIGHLIGHT_TONE constants. OFF remains exact 26652. */
+    /* IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE constants. OFF remains exact 26652. */
     private static final float IRIS_26653_HC_WEAK_WHITE_ANCHOR = 0.915f;
     private static final float IRIS_26653_HC_STRONG_WHITE_ANCHOR = 0.890f;
     private static final float IRIS_26653_HC_WEAK_WHITE_SLOPE = 0.090f;
@@ -244,15 +244,16 @@ public final class MotionV2Render extends Node {
         return legacy + (candidate - legacy) * adaptiveEnable;
     }
 
-    /* IRIS_26654_SINGLE_FINAL_HIGHLIGHT_TONE_REFERENCE
+    /* IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE_REFERENCE
      * OFF is bit-for-math identical to 26652. ON is applied only after brightnessTargetGain is
      * frozen. Below 0.65 it is exact identity to OFF; above that point one C1 monotone curve
      * reserves visible SDR spacing for the true extended-linear >1.0 master. */
-    static float iris26654MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain,
+    static float iris26653MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain,
                                                   float broadNearFraction, float hardFraction,
                                                   float baseSceneWhite, float adaptiveSceneWhite,
                                                   boolean highlightCompressionEnabled,
-                                                  float photonHighlightKnee) {
+                                                  float photonHighlightKnee,
+                                                  float photonAdaptiveWhitePoint) {
         final float x = Math.max(sourceGuide, 0.0f);
         final float off = iris26623MapMotionSdrFinalGuide(x, brightnessTargetGain,
                 broadNearFraction, hardFraction, baseSceneWhite, adaptiveSceneWhite);
@@ -312,14 +313,8 @@ public final class MotionV2Render extends Node {
                     + (t3 - t2) * width * m1;
         } else {
             final float reserve = Math.max(1.0f - targetWhite, 0.0f);
-            /* IRIS_26654_IRIS_SCENE_WHITE_TAIL_AUTHORITY
-             * The separate Photon per-channel white estimator is telemetry-only. The >1 tail
-             * consumes the already-solved Iris scene-white expansion so recovered HDR headroom
-             * cannot collapse merely because one Photon histogram channel lacked support. */
-            final float safeBaseWhite = Math.max(baseSceneWhite, 1.0f);
-            final float safeAdaptiveWhite = Math.max(adaptiveSceneWhite, safeBaseWhite);
-            final float whiteSpan = iris26582Clamp(
-                    safeAdaptiveWhite / safeBaseWhite, 1.0f, 3.0f);
+            final float whiteSpan = Math.max(1.0f,
+                    (float)Math.sqrt(Math.max(photonAdaptiveWhitePoint, 1.0f)));
             final float tailScale = reserve / Math.max(m1, 1.0e-6f) * whiteSpan;
             final float excess = x - 1.0f;
             candidate = targetWhite + reserve * excess / (excess + tailScale);
@@ -493,12 +488,13 @@ public final class MotionV2Render extends Node {
                 basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
     }
 
-    private void iris26654SetFinalToneUniforms() {
+    private void iris26653SetFinalToneUniforms() {
         iris26623SetAdaptiveUpperToneUniforms();
         PostPipeline p = (PostPipeline) basePipeline;
-        glProg.setVar("iris26654HighlightCompressionEnabled",
+        glProg.setVar("iris26653HighlightCompressionEnabled",
                 p.motionV2PhotonHighlightCompressionEnabled ? 1 : 0);
-        glProg.setVar("iris26654HighlightKnee", p.motionV2PhotonHighlightKnee);
+        glProg.setVar("iris26653HighlightKnee", p.motionV2PhotonHighlightKnee);
+        glProg.setVar("iris26653AdaptiveWhitePoint", p.motionV2PhotonAdaptiveWhitePoint);
     }
 
     /* IRIS_26626_BOUNDED_SOURCE_DOMAIN_LOCAL_LAPLACIAN_REFERENCE
@@ -523,7 +519,7 @@ public final class MotionV2Render extends Node {
     private float iris26626MappedReferenceLog(float sourceReferenceLog) {
         final float sourceGuide = (float)Math.pow(2.0, sourceReferenceLog);
         final PostPipeline p = (PostPipeline) basePipeline;
-        final float mappedGuide = iris26654MapMotionSdrFinalGuide(
+        final float mappedGuide = iris26653MapMotionSdrFinalGuide(
                 sourceGuide,
                 basePipeline.mParameters.motionV2DisplayGain,
                 basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
@@ -531,7 +527,8 @@ public final class MotionV2Render extends Node {
                 basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                 basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
                 p.motionV2PhotonHighlightCompressionEnabled,
-                p.motionV2PhotonHighlightKnee);
+                p.motionV2PhotonHighlightKnee,
+                p.motionV2PhotonAdaptiveWhitePoint);
         return (float)(Math.log(Math.max(mappedGuide, 0.000244140625f)) / Math.log(2.0));
     }
 
@@ -647,7 +644,7 @@ public final class MotionV2Render extends Node {
             glProg.setTexture("InputBuffer", coarsestLinear);
             glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
             glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            iris26654SetFinalToneUniforms();
+            iris26653SetFinalToneUniforms();
             glProg.drawBlocks(reconstruction);
             try { coarsestLinear.close(); } catch (Throwable ignored) {}
             coarsestLinear = null;
@@ -741,7 +738,7 @@ public final class MotionV2Render extends Node {
             glProg.setTexture("InputBuffer", source);
             glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
             glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            iris26654SetFinalToneUniforms();
+            iris26653SetFinalToneUniforms();
             glProg.drawBlocks(guide[0]);
 
             for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
@@ -843,6 +840,8 @@ public final class MotionV2Render extends Node {
                     basePipeline.mParameters.motionV2ShadowBodyStrength);
             glProg.setVar("iris26639ShadowBodyEnd",
                     basePipeline.mParameters.motionV2ShadowBodyEnd);
+            glProg.setVar("iris26650HighlightCompressionEnabled",
+                    ((PostPipeline) basePipeline).motionV2PhotonHighlightCompressionEnabled ? 1 : 0);
             glProg.setVar("iris26626Mode", 6);
             glProg.drawBlocks(coherentHighlightTone);
             try { finalTone.close(); } catch (Throwable ignored) {}
@@ -1003,7 +1002,7 @@ public final class MotionV2Render extends Node {
             glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
             glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
             glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            iris26654SetFinalToneUniforms();
+            iris26653SetFinalToneUniforms();
             glProg.setVar("irisOutputZoom", irisOutputZoom);
             glProg.setVar("iris26630MotionSaturation", iris26630MotionSaturation);
             glProg.setVar("iris26621LocalToneEnabled", iris26621LocalTone != null ? 1 : 0);
@@ -1074,7 +1073,7 @@ public final class MotionV2Render extends Node {
                 if (basePipeline.mParameters.motionV2Active && iris26621LocalTone != null) {
                     glProg.setTexture("iris26621LocalToneLog", iris26621LocalTone);
                 }
-                iris26654SetFinalToneUniforms();
+                iris26653SetFinalToneUniforms();
 
                 glProg.setVar("irisOutputZoom", irisOutputZoom);
                 glProg.drawBlocks(gainTexture);
@@ -1265,45 +1264,45 @@ public final class MotionV2Render extends Node {
                     + (IRIS_26623_BROAD_WHITE_ANCHOR - IRIS_26623_SPARSE_WHITE_ANCHOR) * iris26623Pressure;
             final float iris26623WhiteSlope = IRIS_26623_SPARSE_WHITE_SLOPE
                     + (IRIS_26623_BROAD_WHITE_SLOPE - IRIS_26623_SPARSE_WHITE_SLOPE) * iris26623Pressure;
-            final PostPipeline iris26654Pipeline = (PostPipeline) basePipeline;
-            final boolean iris26654Enabled = iris26654Pipeline.motionV2PhotonHighlightCompressionEnabled;
-            final float iris26654Knee = iris26654Pipeline.motionV2PhotonHighlightKnee;
-            final float iris26654WhiteTelemetry = iris26654Pipeline.motionV2PhotonAdaptiveWhitePoint;
-            final float iris26654At1 = iris26654MapMotionSdrFinalGuide(1.0f,
+            final PostPipeline iris26653Pipeline = (PostPipeline) basePipeline;
+            final boolean iris26653Enabled = iris26653Pipeline.motionV2PhotonHighlightCompressionEnabled;
+            final float iris26653Knee = iris26653Pipeline.motionV2PhotonHighlightKnee;
+            final float iris26653White = iris26653Pipeline.motionV2PhotonAdaptiveWhitePoint;
+            final float iris26653At1 = iris26653MapMotionSdrFinalGuide(1.0f,
                     basePipeline.mParameters.motionV2DisplayGain,
                     basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
                     basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
                     basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                     basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
-                    iris26654Enabled, iris26654Knee);
-            final float iris26654At125 = iris26654MapMotionSdrFinalGuide(1.25f,
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            final float iris26653At125 = iris26653MapMotionSdrFinalGuide(1.25f,
                     basePipeline.mParameters.motionV2DisplayGain,
                     basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
                     basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
                     basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                     basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
-                    iris26654Enabled, iris26654Knee);
-            final float iris26654At2 = iris26654MapMotionSdrFinalGuide(2.0f,
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            final float iris26653At2 = iris26653MapMotionSdrFinalGuide(2.0f,
                     basePipeline.mParameters.motionV2DisplayGain,
                     basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction,
                     basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
                     basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                     basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite,
-                    iris26654Enabled, iris26654Knee);
-            Log.i(Name, "IRIS_26654_SINGLE_FINAL_HIGHLIGHT_TONE"
-                    + " enabled=" + iris26654Enabled
+                    iris26653Enabled, iris26653Knee, iris26653White);
+            Log.i(Name, "IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE"
+                    + " enabled=" + iris26653Enabled
                     + " owner=MotionV2RenderAfterBrightnessSolve"
                     + " brightnessTargetGain=" + basePipeline.mParameters.motionV2DisplayGain
                     + " matcherUses26652OffMap=true"
                     + " lowerToneExactThrough=" + IRIS_26623_UPPER_TONE_START
-                    + " photonKnee=" + iris26654Knee
-                    + " photonAdaptiveWhiteTelemetryOnly=" + iris26654WhiteTelemetry
-                    + " map1=" + iris26654At1
-                    + " map1p25=" + iris26654At125
-                    + " map2=" + iris26654At2
-                    + " localLaplacianAbsoluteToneOwner=false localLaplacianBoundedZeroDcDetailOnly=true"
-                    + " gainMapSharedFinalToneAndResidualContract=true"
-                    + " preColorDifferential=false irisSceneWhiteTailAuthority=true");
+                    + " photonKnee=" + iris26653Knee
+                    + " photonAdaptiveWhitePoint=" + iris26653White
+                    + " map1=" + iris26653At1
+                    + " map1p25=" + iris26653At125
+                    + " map2=" + iris26653At2
+                    + " localLaplacianSharedMap=true"
+                    + " gainMapSharedMap=true"
+                    + " preColorDifferential=false");
 
             Log.i(Name, "IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE"
                     + " master=extendedLinearPreTone"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index ec01bc82db067b53076d5628f54e8c3a61527e26..28bd5807cdb0e7914ffbb39e0607c91956f35cea 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -545,7 +545,7 @@ public class PostPipeline extends GLBasePipeline {
             final boolean iris26650HighlightCompression =
                     com.particlesdevs.photoncamera.settings.PreferenceKeys.isHighlightCompressionOn();
             add(new MotionV2PhotonHighlightCompression(iris26650HighlightCompression));
-            add(new StageTelemetry("IRIS_26654_PHOTON_HIGHLIGHT_ANALYSIS_KNEE_ONLY"));
+            add(new StageTelemetry("IRIS_26653_PHOTON_HIGHLIGHT_ANALYSIS_FINAL_TONE_OWNER"));
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
                     "IRIS_26560_MOTION_SABRE_POST_OWNERSHIP",
                     "owner=SABRE carrier=RESOLVE_SABRE_LINEAR_RGB"
@@ -583,7 +583,7 @@ public class PostPipeline extends GLBasePipeline {
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
                     "IRIS_26410_MOTION_V2_POST_GRAPH",
                     "owner=" + motionReconstructionOwnerName(mParameters.motionV2ReconstructionOwner)
-                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,PhotonHighlightAnalysisOnly,OwnerSpecificPreColor,MotionV2ColorTransform[ACR3-no-highlight-tone],MotionV2ViewfinderExposureMatcher[26652-OFF-brightness-authority],MotionV2DisplayExposure,IrisManualOptional,MotionV2Render[26654-global-tone-plus-bounded-zero-dc-detail],RotateWatermark"
+                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,PhotonHighlightAnalysisOnly,OwnerSpecificPreColor,MotionV2ColorTransform[ACR3-no-highlight-tone],MotionV2ViewfinderExposureMatcher[26652-OFF-brightness-authority],MotionV2DisplayExposure,IrisManualOptional,MotionV2Render[26653-single-final-highlight-tone],RotateWatermark"
                             + " spatialHighlightNode=false"
                             + " directMultiframeRgb=" + directRgbCarrier
                             + " strictSabreAuthority26560=true"
diff --git a/app/version.properties b/app/version.properties
index 1bf13fbb2ce9635f76161f6993509e571fb9ce64..0813a9b5204b5477da933a144524c8d17126a88f 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726654
-VERSION_BUILD=26654
+VERSION_NAME=0.9726653
+VERSION_BUILD=26653
