Workflow/package regressions carried forward from successful 26590:
app/build/** and app/.cxx/** never count as runtime-source changes.
Repository-only module scaffolding absent from compiled-candidate authority never counts as runtime change.
Strict runtime allowlist equality is never weakened; unexpected app/src/** changes remain visible and fail.
Candidate snapshots are authority-seeded from the exact successful compiled-candidate file universe, then overlay only the intended live source domain.
The frozen compiler-tested candidate must remain byte-identical through assemble and final export.
