diff --git a/app/src/main/java/com/unspektrawesome/capture/FrameGeometrySnapshot.kt b/app/src/main/java/com/unspektrawesome/capture/FrameGeometrySnapshot.kt
index c854b5caec419b139f0e8ec00f06de9355777021..fe020f217e4d72a0f97b9118fda192a410a79a8d 100644
--- a/app/src/main/java/com/unspektrawesome/capture/FrameGeometrySnapshot.kt
+++ b/app/src/main/java/com/unspektrawesome/capture/FrameGeometrySnapshot.kt
@@ -88,6 +88,37 @@ data class FrameGeometrySnapshot(
     }
 
     companion object {
+        /* IRIS_26703_SPEKTRA_FULL_FRAME_STILL_GEOMETRY
+         * Saved still geometry is sensor-active-array owned. It must never inherit the
+         * viewfinder/UI viewport aspect, because CameraActivity can remain portrait-locked
+         * while the physical capture transform is landscape. */
+        fun createFullFrameStill(
+            sensor: SensorMetadata,
+            output: RawOutput,
+            transform: CaptureTransform,
+        ): FrameGeometrySnapshot {
+            val pixelArray = requireNotNull(sensor.pixelArray(output.maximumResolutionMode)) {
+                "Sensor pixel array is unavailable"
+            }
+            val activeArray = requireNotNull(sensor.activeArray(output.maximumResolutionMode)) {
+                "Sensor active array is unavailable"
+            }
+            validateActiveArray(activeArray, pixelArray)
+            val mappedActive = mapActiveArray(activeArray, pixelArray, output.size)
+            val normalizedActive = mappedActive.normalized(output.size)
+            val rawOutputSize = Size2d(mappedActive.width, mappedActive.height)
+            val orientedOutput = transform.outputSize(rawOutputSize)
+            return FrameGeometrySnapshot(
+                sourceSize = output.size,
+                activeArray = normalizedActive,
+                sourceCrop = normalizedActive,
+                cropPixels = mappedActive,
+                transform = transform,
+                targetViewport = orientedOutput,
+                outputSize = orientedOutput,
+            )
+        }
+
         fun create(
             sensor: SensorMetadata,
             output: RawOutput,
diff --git a/app/src/main/java/com/unspektrawesome/preview/RawVulkanPreviewController.kt b/app/src/main/java/com/unspektrawesome/preview/RawVulkanPreviewController.kt
index 557d47e8345876522360d823a3fa09157cef7557..6df63e890508c932887794e7b0eb0568cb14ef34 100644
--- a/app/src/main/java/com/unspektrawesome/preview/RawVulkanPreviewController.kt
+++ b/app/src/main/java/com/unspektrawesome/preview/RawVulkanPreviewController.kt
@@ -251,11 +251,20 @@ class RawVulkanPreviewController(
                 owner.camera.rawOutputs,
                 VulkanRawImportPolicy.candidateFormats,
             )
-            val geometry = FrameGeometrySnapshot.create(
+            val geometry = FrameGeometrySnapshot.createFullFrameStill(
                 owner.camera.sensorMetadata,
                 captureOutput,
                 transform,
-                requireNotNull(viewportSize) { "Viewfinder dimensions are unavailable" },
+            )
+            Log.i(
+                TAG,
+                "IRIS_26703_SPEKTRA_FULL_FRAME_STILL_GEOMETRY " +
+                    "raw=${captureOutput.size.width}x${captureOutput.size.height} " +
+                    "activeCrop=${geometry.cropPixels.width}x${geometry.cropPixels.height}" +
+                    "@${geometry.cropPixels.left},${geometry.cropPixels.top} " +
+                    "physicalRotation=${transform.orientationDegrees} " +
+                    "output=${geometry.outputSize.width}x${geometry.outputSize.height} " +
+                    "viewportIgnoredForStill=true",
             )
             PendingCapture(
                 owner,
diff --git a/app/version.properties b/app/version.properties
index 7520571e22143ea41ec337bed0bbff41b78cb129..c16052c9f04caf997c1d3723c3c6000a132d6bcc 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726702
-VERSION_BUILD=26702
+VERSION_NAME=0.9726703
+VERSION_BUILD=26703
