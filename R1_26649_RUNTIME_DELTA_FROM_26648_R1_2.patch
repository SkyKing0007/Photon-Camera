diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
index b0c4bb5b7c60c095a89d7329b6db755090463ed9..413c673899272805c79c2c36673e46e9ad65e511 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
@@ -9,6 +9,7 @@ uniform float iris26623ToneBroadNearFraction;
 uniform float iris26623ToneHardFraction;
 uniform float iris26623ToneBaseSceneWhite;
 uniform float iris26623ToneAdaptiveSceneWhite;
+uniform float iris26649PhotonHighlightKnee;
 out float Output;
 
 const vec3 IRIS_LUMA = vec3(0.22897456,0.69173852,0.07928691);
@@ -129,8 +130,22 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
 }
 
 
+/* IRIS_26649_PHOTON_HIGHLIGHT_COMPRESSION_OWNER
+ * Exact Photon Highlight Compression shoulder: identity below knee, quadratic sag above knee,
+ * and exact display white at x=1. Applied before Local Laplacian so all upper-range content
+ * receives the same headroom regardless of structure/scene semantics. */
+float iris26649PhotonSoftShoulder(float x){
+    float v=clamp(x,0.0,1.0);
+    float knee=clamp(iris26649PhotonHighlightKnee,0.0,1.0);
+    if(v<=knee || knee>=0.9999999) return v;
+    float span=max(1.0-knee,1.0e-7);
+    float g=(v-knee)/span;
+    return knee+span*g*g;
+}
+
 void main(){
     ivec2 p=ivec2(gl_FragCoord.xy);
     float mapped=iris26623MapMotionSdrFinalGuide(irisGuide(texelFetch(InputBuffer,p,0).rgb));
+    mapped=iris26649PhotonSoftShoulder(mapped);
     Output=log2(max(mapped,IRIS_LOG_FLOOR));
 }
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index c2776ec8cfe0e969220e888d6963675556448eb0..466dc698ecab58e5dd79b0d1eb2863c33452459b 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -203,22 +203,14 @@ void main(){
         float baseLinear=exp2(clamp(baseLog,-12.0,0.0));
         float residual=currentLinear-baseLinear;
 
-        /* IRIS_26635_SPATIALLY_COHERENT_HIGHLIGHT_ROLLOFF
-         * B is the already-mapped low-frequency illumination owner.  Give B a small continuous
-         * asymptotic lift toward white (no target-white plateau), then carry the structural
-         * residual R with a bounded highlight weight. Small residuals are gently suppressed in
-         * bright fields; strong slats/folds/specular geometry retain unit weight. */
+        /* IRIS_26649_PHOTON_HIGHLIGHT_COMPRESSION_HANDOFF
+         * GlobalMappedLog already contains the universal Photon quadratic Highlight Compression
+         * before Local Laplacian. Retire 26635's later smooth-base lift: it moved broad highlights
+         * back toward white and could undo the Photon separation. Keep the mapped base exactly. */
         float upperGate=smoothstep(0.65,0.985,baseLinear);
-        float shoulderGate=smoothstep(0.70,0.985,baseLinear);
         vec3 iris26646Radiance=iris26646SourceRadianceSurvival(p);
         float sourceStructureGate=max(max(iris26644SourceStructure(p),iris26645SourceTexture(p)),iris26646Radiance.z);
-        float smoothShoulder=1.0-pow(max(1.0-baseLinear,0.0),1.12);
-        /* IRIS_26645_STRUCTURE_AWARE_HIGHLIGHT_BASE
-         * Smooth bright illumination keeps the successful 26635 shoulder. Real source texture
-         * retains more base headroom so grass/pavement/fabric is not lifted into one pale plateau. */
-        float structureShoulderScale=1.0-0.72*sourceStructureGate;
-        float baseOut=mix(baseLinear,smoothShoulder,
-            0.55*shoulderGate*structureShoulderScale);
+        float baseOut=baseLinear;
 
         /* IRIS_26639_SIGNAL_DRIVEN_SHADOW_BASE
          * Correct only the low-frequency base B. The 0.05 true-floor endpoint and the scene-derived
@@ -232,14 +224,12 @@ void main(){
         float bodyBump=16.0*bodyT*bodyT*(1.0-bodyT)*(1.0-bodyT);
         float bodyScale=exp2(-0.25*bodyStrength*bodyBump);
         float shadowBase=baseOut*bodyScale;
-        float structureGate=smoothstep(0.012,0.045,abs(residual));
-        float smallResidualWeight=mix(1.0,0.78,upperGate);
-        /* IRIS_26644_VISUAL_HIGHLIGHT_STRUCTURE_PRESERVATION
-         * Keep 26635 suppression only for genuinely smooth bright-field residuals. If the
-         * pre-tone source independently proves real local structure, preserve that residual. */
-        float residualWeight=mix(
-                smallResidualWeight,1.0,max(structureGate,sourceStructureGate));
-        float weightedResidual=residualWeight*residual;
+        /* IRIS_26649_UNIVERSAL_HIGHLIGHT_RESIDUAL_PRESERVATION
+         * Photon Highlight Compression creates headroom for the entire upper tonal population,
+         * not only texture-classified pixels. Preserve the completed Local-Laplacian residual at
+         * unit weight; structure evidence remains only for the existing bounded headroom reserve. */
+        float residualWeight=1.0;
+        float weightedResidual=residual;
         float candidateLinear=shadowBase+weightedResidual;
         /* IRIS_26645_STRUCTURAL_HEADROOM_RESERVATION
          * When real pre-tone structure would otherwise hit the display ceiling, lower only B by
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 0a41823ba90c022ec19d840318d8173d2d9d438c..43acaf23dc979145809e66f19cd20d6d79ece623 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -13,6 +13,7 @@ uniform float iris26623ToneBroadNearFraction;
 uniform float iris26623ToneHardFraction;
 uniform float iris26623ToneBaseSceneWhite;
 uniform float iris26623ToneAdaptiveSceneWhite;
+uniform float iris26649PhotonHighlightKnee;
 uniform float iris26630MotionSaturation;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
@@ -210,9 +211,21 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
 }
 
 
+/* IRIS_26649_PHOTON_HIGHLIGHT_COMPRESSION_OWNER
+ * Exact Photon Highlight Compression shoulder. One scalar luminance response preserves RGB ratios;
+ * values below the adaptive knee are exact identity and display white remains exactly white. */
+float iris26649PhotonSoftShoulder(float x){
+    float v=clamp(x,0.0,1.0);
+    float knee=clamp(iris26649PhotonHighlightKnee,0.0,1.0);
+    if(v<=knee || knee>=0.9999999) return v;
+    float span=max(1.0-knee,1.0e-7);
+    float g=(v-knee)/span;
+    return knee+span*g*g;
+}
+
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
-        return iris26623MapMotionSdrFinalGuide(sourceGuide);
+        return iris26649PhotonSoftShoulder(iris26623MapMotionSdrFinalGuide(sourceGuide));
     }
 
     float start=0.50;
@@ -362,7 +375,8 @@ void main() {
         float sourcePeak=max3(sourceRgb);
         float sourceGuide=max(sourceY,sourcePeak);
         if(sourceGuide>1.0e-7){
-            float globalMapped=iris26623MapMotionSdrFinalGuide(sourceGuide);
+            float globalMapped=iris26649PhotonSoftShoulder(
+                    iris26623MapMotionSdrFinalGuide(sourceGuide));
             vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
             vec2 uv=(sourcePixel+vec2(0.5))/max(toneSize,vec2(1.0));
             vec2 halfTexel=vec2(0.5)/max(toneSize,vec2(1.0));
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 05d590eb1765c0282899bf8cb842accdd3971df4..c5177bc1d618f3a5d218a9d226e990571cbc9bc2 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -126,6 +126,37 @@ public final class MotionV2Render extends Node {
      * coherently, while real slat/fold/reflection structure remains in the residual. */
     private static final int IRIS_26635_HIGHLIGHT_BASE_LEVEL = 5;
 
+    /* IRIS_26649_PHOTON_HIGHLIGHT_COMPRESSION_OWNER
+     * Reproduce Photon AutoExposureCurve's Highlight Compression operator exactly while keeping
+     * Iris exposure/color ownership unchanged. Iris' already-frozen predicted clip population is
+     * the scene evidence; the Photon knee law and quadratic shoulder are exact: knee 0.90 -> 0.55
+     * as clipped fraction reaches 0.10, identity below the knee, and exact white 1.0 -> 1.0.
+     * This is a universal upper-range presentation operator, never a structure/scene-semantic gate. */
+    static final float IRIS_26649_PHOTON_KNEE_MAX = 0.90f;
+    static final float IRIS_26649_PHOTON_KNEE_MIN = 0.55f;
+    static final float IRIS_26649_PHOTON_KNEE_REF = 0.10f;
+
+    static float iris26649PhotonHighlightKnee(float clippedFraction) {
+        float f = iris26582Clamp(clippedFraction, 0.0f, 1.0f);
+        float pressure = Math.min(f / IRIS_26649_PHOTON_KNEE_REF, 1.0f);
+        return IRIS_26649_PHOTON_KNEE_MAX
+                + (IRIS_26649_PHOTON_KNEE_MIN - IRIS_26649_PHOTON_KNEE_MAX) * pressure;
+    }
+
+    static float iris26649PhotonSoftShoulder(float x, float knee) {
+        float v = iris26582Clamp(x, 0.0f, 1.0f);
+        float k = iris26582Clamp(knee, 0.0f, 1.0f);
+        if (v <= k || k >= 1.0f - 1.0e-7f) return v;
+        float span = Math.max(1.0f - k, 1.0e-7f);
+        float g = (v - k) / span;
+        return k + span * g * g;
+    }
+
+    private float iris26649PhotonHighlightKnee() {
+        return iris26649PhotonHighlightKnee(
+                basePipeline.mParameters.motionV2TonePredictedClipFraction);
+    }
+
     static float iris26621MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
         float x = Math.max(sourceGuide, 0.0f);
         float requestedFinalGain = Math.max(brightnessTargetGain, 1.0e-6f)
@@ -393,6 +424,7 @@ public final class MotionV2Render extends Node {
     }
 
     private void iris26623SetAdaptiveUpperToneUniforms() {
+        glProg.setVar("iris26649PhotonHighlightKnee", iris26649PhotonHighlightKnee());
         glProg.setVar("iris26623ToneBroadNearFraction",
                 basePipeline.mParameters.motionV2ToneProjectedBroadNearCeilingFraction);
         glProg.setVar("iris26623ToneHardFraction",
@@ -431,7 +463,9 @@ public final class MotionV2Render extends Node {
                 basePipeline.mParameters.motionV2ToneProjectedHardCeilingFraction,
                 basePipeline.mParameters.motionV2ToneBaseSceneWhite,
                 basePipeline.mParameters.motionV2ToneAdaptiveSceneWhite);
-        return (float)(Math.log(Math.max(mappedGuide, 0.000244140625f)) / Math.log(2.0));
+        final float photonCompressed = iris26649PhotonSoftShoulder(
+                mappedGuide, iris26649PhotonHighlightKnee());
+        return (float)(Math.log(Math.max(photonCompressed, 0.000244140625f)) / Math.log(2.0));
     }
 
     private void iris26621ClearLocalToneState() {
@@ -1164,6 +1198,16 @@ public final class MotionV2Render extends Node {
                     + (IRIS_26623_BROAD_WHITE_ANCHOR - IRIS_26623_SPARSE_WHITE_ANCHOR) * iris26623Pressure;
             final float iris26623WhiteSlope = IRIS_26623_SPARSE_WHITE_SLOPE
                     + (IRIS_26623_BROAD_WHITE_SLOPE - IRIS_26623_SPARSE_WHITE_SLOPE) * iris26623Pressure;
+            Log.i(Name, "IRIS_26649_PHOTON_HIGHLIGHT_COMPRESSION"
+                    + " enabled=true"
+                    + " clippedFraction=" + basePipeline.mParameters.motionV2TonePredictedClipFraction
+                    + " knee=" + iris26649PhotonHighlightKnee()
+                    + " kneeMax=" + IRIS_26649_PHOTON_KNEE_MAX
+                    + " kneeMin=" + IRIS_26649_PHOTON_KNEE_MIN
+                    + " kneeRef=" + IRIS_26649_PHOTON_KNEE_REF
+                    + " exactPhotonQuadraticShoulder=true"
+                    + " universalUpperRange=true sceneSemantic=false structureGate=false"
+                    + " colorsOwnerUnchanged=true shortFusionUnchanged=true heicUnchanged=true");
             Log.i(Name, "IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE"
                     + " master=extendedLinearPreTone"
                     + " brightnessTargetGain=" + basePipeline.mParameters.motionV2DisplayGain
diff --git a/app/version.properties b/app/version.properties
index a06ef3066ec0e2be3dfc6bf68962387e0b9ef98e..0adb0a5c91d583898340890e1ac12a93b4a9e0c4 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726648
-VERSION_BUILD=26648
+VERSION_NAME=0.9726649
+VERSION_BUILD=26649
