diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
index 49e44d4ad7462b9bd0016ddbfb1eda67046fbeb2..de11aced812698ab3beabcfe63ff38ed0e14510e 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
@@ -661,50 +661,26 @@ internal object Iris26529SpatialRgbChromaShaders {
         $common
         vec3 smoothAt(ivec2 p){uvec4 v=imageLoad(uSmooth,safePos(p));return vec3(decodeU16(v.r),float(signedChroma(v.g)),float(signedChroma(v.b)));}
         vec3 originalAt(ivec2 p){uvec4 v=imageLoad(uOriginal,safePos(p));return vec3(decodeU16(v.r),float(signedChroma(v.g)),float(signedChroma(v.b)));}
-        /* IRIS_26552_VGN_REAL_COLOR_GEOMETRY_SUPPORT
-         * Color identity never selects a branch. Real scene chroma is protected only when the
-         * same chroma vector has spatial support: coherent interiors, a supported one-sided object
-         * edge, or a thin/tangent structure with opposite-direction continuity. Isolated chroma
-         * retains full VGN correction authority. Strong near-clip boundaries disable this override.
+        /* IRIS_26549_VGN_TRUE_COLOR_CONFIDENCE
+         * Full-strength VGN remains the artifact authority, but coherent scene chroma is no
+         * longer treated as disposable merely because a smoother candidate has lower magnitude.
+         * A real-color region must have meaningful chroma and multi-neighbor hue/magnitude
+         * agreement in the pre-median local-clamp image. Isolated fringe/block chroma therefore
+         * receives the old full correction while stable object/light color is conserved.
          */
-        float chromaAgreement(vec2 c,vec2 n){
-            float cm=length(c),nm=length(n); if(cm<1.0||nm<1.0)return 0.0;
-            float vectorAgreement=dot(c,n)/max(cm*nm,1.0);
-            float ratio=min(cm,nm)/max(cm,nm);
-            return smoothstep(0.86,0.97,vectorAgreement)*smoothstep(0.32,0.72,ratio);
-        }
-        float highlightBoundary(ivec2 p){
-            float lo=originalAt(p).x,hi=lo;
+        float realColorConfidence(ivec2 p,vec2 c){
+            float mag=length(c); if(mag<256.0)return 0.0;
             const ivec2 d[8]=ivec2[8](ivec2(0,-1),ivec2(1,0),ivec2(0,1),ivec2(-1,0),ivec2(1,-1),ivec2(1,1),ivec2(-1,1),ivec2(-1,-1));
-            for(int i=0;i<8;++i){float y=originalAt(p+d[i]).x;lo=min(lo,y);hi=max(hi,y);}
-            float nearClip=smoothstep(58950.0,62900.0,hi);
-            float boundary=smoothstep(700.0,4200.0,hi-lo);
-            return nearClip*boundary;
-        }
-        float localLumaEdge(ivec2 p){
-            float lo=originalAt(p).x,hi=lo;
-            const ivec2 d[4]=ivec2[4](ivec2(-1,0),ivec2(1,0),ivec2(0,-1),ivec2(0,1));
-            for(int i=0;i<4;++i){float y=originalAt(p+d[i]).x;lo=min(lo,y);hi=max(hi,y);}
-            return smoothstep(300.0,2800.0,hi-lo);
-        }
-        float realColorConfidence(ivec2 p,vec2 c,int directionMask){
-            float mag=length(c); if(mag<192.0)return 0.0;
-            const ivec2 d[8]=ivec2[8](ivec2(0,-1),ivec2(1,0),ivec2(0,1),ivec2(-1,0),ivec2(1,-1),ivec2(1,1),ivec2(-1,1),ivec2(-1,-1));
-            float a[8];float total=0.0;
+            float support=0.0;
             for(int i=0;i<8;++i){
-                float v=chromaAgreement(c,originalAt(p+d[i]).yz);
-                if((directionMask&(1<<i))==0)v*=0.55;
-                a[i]=v;total+=v;
+                vec2 n=originalAt(p+d[i]).yz; float nm=length(n);
+                if(nm>128.0){
+                    float hue=dot(c,n)/max(mag*nm,1.0);
+                    float ratio=min(mag,nm)/max(mag,nm);
+                    support+=smoothstep(0.88,0.97,hue)*smoothstep(0.35,0.75,ratio);
+                }
             }
-            float coherent=smoothstep(2.4,5.2,total);
-            float tangent=max(max(min(a[0],a[2]),min(a[1],a[3])),max(min(a[4],a[6]),min(a[5],a[7])));
-            tangent=smoothstep(0.52,0.88,tangent);
-            float sideN=(a[0]+a[4]+a[7])/3.0,sideE=(a[1]+a[4]+a[5])/3.0;
-            float sideS=(a[2]+a[5]+a[6])/3.0,sideW=(a[3]+a[6]+a[7])/3.0;
-            float oneSided=smoothstep(0.45,0.80,max(max(sideN,sideS),max(sideE,sideW)));
-            float support=max(coherent,max(tangent,oneSided));
-            support*=smoothstep(192.0,1280.0,mag);
-            return support*(1.0-highlightBoundary(p));
+            return smoothstep(2.5,5.0,support)*smoothstep(256.0,1536.0,mag);
         }
         vec4 filter3(uvec4 encoded,ivec2 p){
             vec3 neg[4]; vec3 pos[4];
@@ -724,26 +700,18 @@ internal object Iris26529SpatialRgbChromaShaders {
             if(e.r!=0u&&e.a!=0u)f=filter3(e,p);
             uvec4 sm=imageLoad(uSmooth,p);ivec2 sc=ivec2(signedChroma(sm.g),signedChroma(sm.b));ivec2 fc=ivec2(f.yz);
             ivec2 originalChroma=ivec2(signedChroma(e.g),signedChroma(e.b));
-            int directionMask=int(e.a)&0xFF;
+            float preserve=realColorConfidence(p,vec2(originalChroma));
+            /* A coherent false-color patch must not be protected simply because it is spatially
+             * coherent. Require VGN's own directional candidate to agree with the original hue;
+             * disagreement or a near-neutral candidate keeps full artifact correction authority. */
             float originalMag=length(vec2(originalChroma));
             float filteredMag=length(vec2(fc));
-            float preserve=realColorConfidence(p,vec2(originalChroma),directionMask);
-            /* A coherent false-color patch must not be protected merely because it is spatially
-             * coherent. VGN's own directional candidate must still agree with the original vector. */
-            float candidateAgreement=chromaAgreement(vec2(originalChroma),vec2(fc));
-            preserve*=smoothstep(0.52,0.90,candidateAgreement)*smoothstep(96.0,384.0,filteredMag);
-            float highlight=highlightBoundary(p);
-            preserve*=1.0-highlight;
+            float hueAgreement=filteredMag>96.0?dot(vec2(originalChroma),vec2(fc))/max(originalMag*filteredMag,1.0):-1.0;
+            preserve*=smoothstep(0.84,0.97,hueAgreement)*smoothstep(96.0,384.0,filteredMag);
             float strength=clamp(uChromaStrength,0.0,1.0);
-            /* IRIS_26552_VGN_LOW_CHROMA_CROSS_EDGE_CONTAINMENT
-             * At a real luma edge, a near-neutral center must not inherit a larger chroma vector
-             * from the opposite side. This is containment only: it never boosts saturation.
-             * Strong near-clip boundaries deliberately bypass the override so VGN keeps authority. */
-            float lowOriginal=1.0-smoothstep(96.0,512.0,originalMag);
-            float expansion=smoothstep(96.0,768.0,filteredMag-originalMag);
-            float expansionGuard=lowOriginal*localLumaEdge(p)*expansion*(1.0-highlight);
-            fc=ivec2(mix(vec2(fc),vec2(originalChroma),clamp(expansionGuard,0.0,1.0)));
-            filteredMag=length(vec2(fc));
+            /* Preserve the old lower-saturation candidate only where the local chroma is not
+             * strongly supported as real scene color. Artifact regions therefore retain VGN 1.0
+             * behavior; coherent colors retain their pre-median chroma vector and magnitude. */
             if(abs(sc.x)+abs(sc.y)<abs(fc.x)+abs(fc.y)){
                 float lowSatWeight=strength*(1.0-preserve);
                 fc=ivec2(mix(vec2(fc),vec2(sc),lowSatWeight));
@@ -777,11 +745,6 @@ internal object Iris26529SpatialRgbChromaShaders {
         float apply(inout State s,float v,vec4 a,vec4 b,bool unsignedOut){float r=a[0]*v+a[1]*s.x0+a[2]*s.x1-b[1]*s.y0-b[2]*s.y1;s.x1=s.x0;s.y1=s.y0;s.x0=v;s.y0=unsignedOut?clamp(r,0.0,65504.0):r;return s.y0;}
         float steadyOutput(float v,vec4 a,vec4 b,bool u){float r=v*(a[0]+a[1]+a[2])/(1.0+b[1]+b[2]);return u?clamp(r,0.0,65504.0):r;}
         State steady(float x,float y){return State(x,x,y,y);} ivec2 pos(int inner,int outer){return uAxis==0?ivec2(inner,outer):ivec2(outer,inner);}
-        vec3 iirAt(ivec2 p){uvec4 v=imageLoad(uInput,safePos(p));return vec3(decodeU16(v.r),float(signedChroma(v.g)),float(signedChroma(v.b)));}
-        float iirAgreement(vec2 c,vec2 n){float cm=length(c),nm=length(n);if(cm<1.0||nm<1.0)return 0.0;return smoothstep(0.86,0.97,dot(c,n)/max(cm*nm,1.0))*smoothstep(0.32,0.72,min(cm,nm)/max(cm,nm));}
-        float iirHighlightBoundary(ivec2 p){float lo=iirAt(p).x,hi=lo;const ivec2 d[4]=ivec2[4](ivec2(-1,0),ivec2(1,0),ivec2(0,-1),ivec2(0,1));for(int i=0;i<4;++i){float y=iirAt(p+d[i]).x;lo=min(lo,y);hi=max(hi,y);}return smoothstep(58950.0,62900.0,hi)*smoothstep(700.0,4200.0,hi-lo);}
-        float iirLumaEdge(ivec2 p){float lo=iirAt(p).x,hi=lo;const ivec2 d[4]=ivec2[4](ivec2(-1,0),ivec2(1,0),ivec2(0,-1),ivec2(0,1));for(int i=0;i<4;++i){float y=iirAt(p+d[i]).x;lo=min(lo,y);hi=max(hi,y);}return smoothstep(300.0,2800.0,hi-lo);}
-        float iirRealColorSupport(ivec2 p,vec2 c){float mag=length(c);if(mag<192.0)return 0.0;vec2 l=iirAt(p+ivec2(-1,0)).yz,rn=iirAt(p+ivec2(1,0)).yz,u=iirAt(p+ivec2(0,-1)).yz,d=iirAt(p+ivec2(0,1)).yz;float al=iirAgreement(c,l),ar=iirAgreement(c,rn),au=iirAgreement(c,u),ad=iirAgreement(c,d);float tangent=max(min(al,ar),min(au,ad));float oneSide=max(max(al,ar),max(au,ad));float support=max(smoothstep(0.52,0.88,tangent),smoothstep(0.72,0.96,oneSide));return support*smoothstep(192.0,1280.0,mag)*(1.0-iirHighlightBoundary(p));}
         void main(){
             int outer=uAxis==0?int(gl_GlobalInvocationID.y):int(gl_GlobalInvocationID.x);int outerLimit=uAxis==0?uImageSize.y:uImageSize.x;int innerSize=uAxis==0?uImageSize.x:uImageSize.y;if(outer>=outerLimit)return;
             int start=uDirection==0?0:innerSize-1;int step=uDirection==0?1:-1;uvec4 b=imageLoad(uInput,pos(start,outer));float by=decodeU16(b.r),cr=float(signedChroma(b.g)),cb=float(signedChroma(b.b));
@@ -801,18 +764,7 @@ internal object Iris26529SpatialRgbChromaShaders {
                 if(uFilterLuma==0){
                     float errorSignal=decodeU16(px.a)*0.25;
                     float artifactConfidence=clamp((errorSignal-100.0)/200.0,0.0,1.0);
-                    vec2 inputChroma=vec2(inCr,inCb),filteredChroma=vec2(r,q);
-                    float inputMag=length(inputChroma),filteredMag=length(filteredChroma);
-                    float highlight=iirHighlightBoundary(p);
-                    float preserve=iirRealColorSupport(p,inputChroma);
-                    preserve*=smoothstep(0.52,0.90,iirAgreement(inputChroma,filteredChroma));
-                    preserve*=1.0-highlight;
-                    effectiveStrength*=artifactConfidence*(1.0-0.85*preserve);
-                    float lowOriginal=1.0-smoothstep(96.0,512.0,inputMag);
-                    float expansion=smoothstep(96.0,768.0,filteredMag-inputMag);
-                    float expansionGuard=lowOriginal*iirLumaEdge(p)*expansion*(1.0-highlight);
-                    r=mix(r,inCr,clamp(expansionGuard,0.0,1.0));
-                    q=mix(q,inCb,clamp(expansionGuard,0.0,1.0));
+                    effectiveStrength*=artifactConfidence;
                 }
                 if(effectiveStrength<0.9999){r=effectiveStrength<=0.0001?inCr:mix(inCr,r,effectiveStrength);q=effectiveStrength<=0.0001?inCb:mix(inCb,q,effectiveStrength);}
                 imageStore(uOutput,p,uvec4(uint(clamp(y,0.0,65504.0)),unsignedChroma(int(r)),unsignedChroma(int(q)),px.a));
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index a44680ccd4704e7064cf726ff08b1d438f9d9c9e..bb548e48680e14d4cfc7bfde40cb0af0be8ecf97 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -4450,8 +4450,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private boolean mIrisNight26540SequenceComplete = false;
     private boolean mIrisNight26540Dispatched = false;
     private int mIrisNight26540ExpectedFrames = 0;
-    // IRIS_26552_NIGHT_FROZEN_FRAME_PLAN_OWNER
-    private IrisNightFrameSelector.Plan mIrisNight26552FramePlan = null;
     private int mIrisNight26540SaveRaw = 0;
     private boolean mIrisNight26540SuperRes = false;
     private boolean mIrisNight26540Aspect169 = false;
@@ -4493,7 +4491,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 mIrisNight26540CaptureActive = false;
                 mIrisNight26540SequenceComplete = false;
                 mIrisNight26540ExpectedFrames = 0;
-                mIrisNight26552FramePlan = null;
                 mIrisNight26544ReferenceCopied = false;
                 mIrisNight26544SpoolFailure = null;
                 CaptureController.isProcessing = false;
@@ -4508,15 +4505,11 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         return aborted;
     }
 
-    private void prepareIrisNight26540CaptureState(IrisNightFrameSelector.Plan framePlan, int saveRaw,
+    private void prepareIrisNight26540CaptureState(int expectedFrames, int saveRaw,
             boolean superRes, boolean aspect169, boolean energySaving, boolean watermark,
             boolean binning, String cameraId, IrisMotionSettings.Snapshot irisSettings,
             CameraCharacteristics characteristics, int rotation) {
-        if (framePlan == null) throw new IllegalArgumentException("26552 Night frozen frame plan is null");
-        final int expectedFrames = framePlan.totalFrames;
-        if (expectedFrames < IrisNightFrameSelector.MIN_NIGHT_FRAMES
-                || expectedFrames > IrisNightFrameSelector.MAX_NIGHT_FRAMES)
-            throw new IllegalArgumentException("26552 Night invalid frozen frame count=" + expectedFrames);
+        if (expectedFrames < 2) throw new IllegalArgumentException("26540 Night requires >=2 frames");
         drainIrisNight26540ReaderBeforeCapture();
         cleanupIrisNight26544SpoolCache();
         mIrisNight26544ReferenceCopied = false;
@@ -4531,7 +4524,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             mIrisNight26540Results.clear();
             mIrisNight26540Requests.clear();
             mIrisNight26540ExpectedFrames = expectedFrames;
-            mIrisNight26552FramePlan = framePlan;
             mIrisNight26540SequenceComplete = false;
             mIrisNight26540Dispatched = false;
             mIrisNight26540SaveRaw = saveRaw;
@@ -4554,9 +4546,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         Log.processState(TAG, "NIGHT_CAPTURE_ARMED_" + expectedFrames + "_FRAMES");
         Log.critical(TAG, "IRIS_26544_NIGHT_CAPTURE_ARMED frames=" + expectedFrames
                 + " saveRaw=" + mIrisNight26540SaveRaw + " cameraId=" + mIrisNight26540CameraId);
-        Log.i(TAG, "IRIS_26552_NIGHT_CAPTURE_STATE_FROZEN frames=" + expectedFrames
-                + " shortRequested=" + framePlan.shortFrames
-                + " longRequested=" + framePlan.longFrames
+        Log.i(TAG, "IRIS_26540_NIGHT_CAPTURE_STATE_FROZEN frames=" + expectedFrames
                 + " saveRaw=" + mIrisNight26540SaveRaw
                 + " superRes=" + mIrisNight26540SuperRes
                 + " aspect169=" + mIrisNight26540Aspect169
@@ -4887,13 +4877,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                         + " inMemoryRawFrames=" + inMemoryRawFrames
                         + " diskBackedRawFrames=" + diskBackedRawFrames
                         + " persistentNativeCopies=1 asyncLiveCamera2Images=0");
-                final IrisNightFrameSelector.Plan frozenFramePlan = mIrisNight26552FramePlan;
-                if (frozenFramePlan == null)
-                    throw new IllegalStateException("26552 Night frozen frame plan missing at batch seal");
                 sealedBatch = new IrisNightBatch(matched, new ArrayList<>(BurstShakiness),
                         java.util.Collections.emptyMap(), mIrisNight26540Results, mIrisNight26540Requests,
                         mIrisNight26540Characteristics, RAW_FORMAT, mIrisNight26540Rotation,
-                        mIrisNight26540ExpectedFrames, frozenFramePlan.shortFrames, frozenFramePlan.longFrames,
+                        mIrisNight26540ExpectedFrames,
                         mIrisNight26540SaveRaw, mIrisNight26540SuperRes,
                         mIrisNight26540Aspect169, mIrisNight26540EnergySaving,
                         mIrisNight26540Watermark, mIrisNight26540Binning,
@@ -4903,7 +4890,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 mIrisNight26540Frames.clear();
                 mIrisNight26540Results.clear();
                 mIrisNight26540Requests.clear();
-                mIrisNight26552FramePlan = null;
             }
         } catch (Throwable sealFailure) {
             synchronized (mIrisNight26540Lock) {
@@ -4915,7 +4901,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 mIrisNight26540Requests.clear();
                 mIrisNight26540CaptureActive = false;
                 mIrisNight26540Dispatched = false;
-                mIrisNight26552FramePlan = null;
             }
             CaptureController.isProcessing = false;
             Log.e(TAG, "IRIS_26540_NIGHT_BATCH_SEAL_FAILED", sealFailure);
@@ -6731,9 +6716,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             final IrisMotionSettings.Snapshot iris26540IrisSettings =
                     iris26533CaptureMode == CameraMode.NIGHT
                             ? IrisMotionSettings.exactCamera2NightSnapshot() : null;
-            final IrisNightFrameSelector.Plan iris26552NightFramePlan =
-                    iris26533CaptureMode == CameraMode.NIGHT
-                            ? IrisNightFrameSelector.freezePlan(iris26540RequestedFrames) : null;
             final IrisNightExposureSelector.Plan iris26540NightPlan =
                     iris26533CaptureMode == CameraMode.NIGHT
                             ? IrisNightExposureSelector.freezePlan(
@@ -6741,16 +6723,14 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                     iris26540ExposureCompensation,
                                     PhotonCamera.getGyro() != null && PhotonCamera.getGyro().getTripod(),
                                     PhotonCamera.getGyro() == null ? 0 : PhotonCamera.getGyro().getFilteredShakiness(),
-                                    mPreviewCaptureResult, iris26552NightFramePlan.longFrames > 0)
+                                    mPreviewCaptureResult)
                             : null;
             if (iris26533CaptureMode == CameraMode.NIGHT) {
                 if (isZslMode()) throw new IllegalStateException("26541 Night must never enter Motion ZSL");
-                Log.i(TAG, "IRIS_26552_NIGHT_FRESH_CAPTURE_CONTRACT"
+                Log.i(TAG, "IRIS_26541_NIGHT_FRESH_CAPTURE_CONTRACT"
                         + " zsl=false motionRing=false preShutterRaw=false shutterTriggeredFreshRaw=true"
-                        + " requestedPreference=" + iris26540RequestedFrames
-                        + " totalRequested=" + iris26552NightFramePlan.totalFrames
-                        + " shortRequested=" + iris26552NightFramePlan.shortFrames
-                        + " longRequested=" + iris26552NightFramePlan.longFrames);
+                        + " shortRequested=" + IrisNightFrameSelector.SHORT_FRAMES
+                        + " longRequested=" + IrisNightFrameSelector.LONG_FRAMES);
             }
             // This is the CaptureRequest.Builder that we use to take a picture.
             final CaptureRequest.Builder captureBuilder;
@@ -6819,10 +6799,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             if (iris26533CaptureMode != CameraMode.NIGHT) SaverImplementation.IMAGE_BUFFER.clear();
 
             int frameCount = iris26533CaptureMode == CameraMode.NIGHT
-                    ? iris26552NightFramePlan.totalFrames
+                    ? IrisNightFrameSelector.getFrames(iris26540RequestedFrames)
                     : FrameNumberSelector.getFrames();
             if (iris26533CaptureMode == CameraMode.NIGHT) {
-                prepareIrisNight26540CaptureState(iris26552NightFramePlan, iris26540SaveRaw,
+                prepareIrisNight26540CaptureState(frameCount, iris26540SaveRaw,
                         iris26540SuperRes, iris26540Aspect169, iris26540EnergySaving,
                         iris26540Watermark, iris26540Binning, iris26540CameraId,
                         iris26540IrisSettings, mCameraCharacteristics, cameraRotation);
@@ -6896,10 +6876,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 long[] times = new long[frameCount];
                 for (int i = 0; i < frameCount; i++) {
                     if (iris26533CaptureMode == CameraMode.NIGHT) {
-                        if (frameCount != iris26552NightFramePlan.totalFrames)
-                            throw new IllegalStateException("26552 Night request count drift=" + frameCount
-                                    + " frozen=" + iris26552NightFramePlan.totalFrames);
-                        if (iris26552NightFramePlan.isShortFrame(i)) {
+                        if (frameCount != IrisNightFrameSelector.TOTAL_FRAMES)
+                            throw new IllegalStateException("26541 Night request count is not 12+3: " + frameCount);
+                        if (i < IrisNightFrameSelector.SHORT_FRAMES) {
                             IrisNightExposureSelector.applyShortPlan(captureBuilder, iris26540NightPlan);
                             captureBuilder.setTag(IRIS_26541_NIGHT_SHORT_TAG);
                             times[i] = iris26540NightPlan.shortExposureNs;
@@ -6917,10 +6896,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     mCaptureRequest = built;
                 }
                 if (iris26533CaptureMode == CameraMode.NIGHT) {
-                    Log.i(TAG, "IRIS_26552_NIGHT_DYNAMIC_REQUESTS"
-                            + " short=" + iris26552NightFramePlan.shortFrames
-                            + " long=" + iris26552NightFramePlan.longFrames
-                            + " total=" + captures.size()
+                    Log.i(TAG, "IRIS_26541_NIGHT_12_PLUS_3_REQUESTS"
+                            + " short=12 long=3 total=" + captures.size()
                             + " shortNs=" + iris26540NightPlan.shortExposureNs
                             + " longNs=" + iris26540NightPlan.longExposureNs
                             + " shortIso=" + iris26540NightPlan.shortIso
@@ -7070,9 +7047,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     if (iris26533CaptureMode == CameraMode.NIGHT) {
                         try { PhotonCamera.getGyro().CompleteSequence(); } catch (Throwable ignored) {}
                         synchronized (mIrisNight26540Lock) {
-                            // IRIS_26552_NIGHT_REQUEST_BUDGET_IMMUTABLE_AT_SEQUENCE_END
-                            // Keep the shutter-frozen expected budget. The bounded-drain grace may
-                            // observe a HAL drop, but IrisNightBatch still exact-validates roles.
+                            mIrisNight26540ExpectedFrames = finalFrameCount;
                             mIrisNight26540SequenceComplete = true;
                         }
                         Log.processState(TAG, "NIGHT_SEQUENCE_COMPLETE_" + finalFrameCount);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/IrisNightBatch.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/IrisNightBatch.java
index 57322eb08b73641765beaf0d84e95a894da51d4c..e9c18beec60f2e541c4a2114aa391637ad3463e2 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/IrisNightBatch.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/IrisNightBatch.java
@@ -14,10 +14,10 @@ import java.util.List;
 import java.util.Map;
 
 /**
- * IRIS_26552_NIGHT_IMMUTABLE_DYNAMIC_BATCH_OWNER
+ * IRIS_26541_NIGHT_IMMUTABLE_12_PLUS_3_BATCH_OWNER
  *
- * One shutter-frozen, fresh-capture Night batch. Frozen requested SHORT/LONG counts remain
- * authoritative through processing; actual roles must exactly match that shutter-time plan.
+ * One shutter-frozen, fresh-capture Night batch. SHORT/NORMAL frames own reference geometry;
+ * SHADOW_LONG frames retain exact per-frame Camera2 metadata and may only add shadow evidence.
  * No Motion ZSL/ring/pre-shutter RAW, Photon static IMAGE_BUFFER, or live settings own this batch.
  */
 public final class IrisNightBatch {
@@ -55,8 +55,6 @@ public final class IrisNightBatch {
             int imageFormat,
             int rotation,
             int frameBudget,
-            int requestedShortFrames,
-            int requestedLongFrames,
             int saveRaw,
             boolean superResEnabled,
             boolean aspect169,
@@ -69,16 +67,8 @@ public final class IrisNightBatch {
             throw new IllegalArgumentException("26541 NightBatch requires at least two exact RAW frames");
         if (results == null || requests == null || characteristics == null)
             throw new IllegalArgumentException("26541 NightBatch exact metadata/characteristics are required");
-        if (frameBudget < IrisNightFrameSelector.MIN_NIGHT_FRAMES
-                || frameBudget > IrisNightFrameSelector.MAX_NIGHT_FRAMES
-                || frameBudget != frames.size())
-            throw new IllegalArgumentException("26552 NightBatch invalid actual frame budget=" + frameBudget
-                    + " frames=" + frames.size());
-        if (requestedShortFrames < 2 || requestedLongFrames < 0
-                || requestedShortFrames + requestedLongFrames < IrisNightFrameSelector.MIN_NIGHT_FRAMES
-                || requestedShortFrames + requestedLongFrames > IrisNightFrameSelector.MAX_NIGHT_FRAMES)
-            throw new IllegalArgumentException("26552 NightBatch invalid requested role plan short="
-                    + requestedShortFrames + " long=" + requestedLongFrames);
+        if (frameBudget < 2 || frameBudget > IrisNightFrameSelector.TOTAL_FRAMES)
+            throw new IllegalArgumentException("26541 NightBatch invalid actual frame budget=" + frameBudget);
 
         ArrayList<ImageFrame> ordered = new ArrayList<>(frames);
         ordered.sort(java.util.Comparator.comparingLong(ImageFrame::getTimestamp));
@@ -118,10 +108,9 @@ public final class IrisNightBatch {
             requestCopy.put(frame.timestamp, request);
         }
         if (shortCount < 2 || reference == null)
-            throw new IllegalArgumentException("26552 Night requires at least two exact SHORT/reference frames; got=" + shortCount);
-        if (shortCount != requestedShortFrames || longCount != requestedLongFrames)
-            throw new IllegalArgumentException("26552 Night actual roles do not match frozen request: short="
-                    + shortCount + "/" + requestedShortFrames + " long=" + longCount + "/" + requestedLongFrames);
+            throw new IllegalArgumentException("26541 Night requires at least two exact SHORT/reference frames; got=" + shortCount);
+        if (shortCount > IrisNightFrameSelector.SHORT_FRAMES || longCount > IrisNightFrameSelector.LONG_FRAMES)
+            throw new IllegalArgumentException("26541 Night role counts exceed 12+3 request: short=" + shortCount + " long=" + longCount);
         final double referenceEnergy = reference.motionV2ExposureEnergy;
         for (ImageFrame frame : ordered) {
             if (frame.motionV2FrameRole == ImageFrame.MotionV2FrameRole.SHADOW_LONG
@@ -140,8 +129,8 @@ public final class IrisNightBatch {
         this.imageFormat = imageFormat;
         this.rotation = rotation;
         this.frameBudget = frameBudget;
-        this.requestedShortFrames = requestedShortFrames;
-        this.requestedLongFrames = requestedLongFrames;
+        this.requestedShortFrames = IrisNightFrameSelector.SHORT_FRAMES;
+        this.requestedLongFrames = IrisNightFrameSelector.LONG_FRAMES;
         this.shortFrameCount = shortCount;
         this.longFrameCount = longCount;
         this.saveRaw = saveRaw;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightExposureSelector.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightExposureSelector.java
index adaada0c528c61f30a5d35e6650205d644efcccd..f1210af477167a8de992b18a7c6370f5ea6be4d3 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightExposureSelector.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightExposureSelector.java
@@ -8,10 +8,10 @@ import android.util.Range;
 import com.particlesdevs.photoncamera.capture.CaptureController;
 import com.particlesdevs.photoncamera.util.Log;
 
-/** IRIS_26552_NIGHT_DYNAMIC_EXPOSURE_SOLE_OWNER
- * Night exposure classes are frozen once at shutter time. SHORT frames preserve the proven
- * 26540 highlight/motion-safe target and own geometry. When the frozen frame plan requests LONG
- * evidence, LONG keeps the same ISO and adds real integration time, bounded by shake/sensor limits.
+/** IRIS_26541_NIGHT_12_PLUS_3_EXPOSURE_SOLE_OWNER
+ * Two Night exposure classes are frozen once at shutter time. The 12 short frames preserve the
+ * proven 26540 highlight/motion-safe target and own geometry. The 3 long frames keep the same ISO
+ * and add real integration time for shadow SNR, adaptively bounded by shake and sensor limits.
  * No Photon IsoExpoSelector, Motion ZSL state, or pre-shutter RAW participates.
  */
 public final class IrisNightExposureSelector {
@@ -99,17 +99,6 @@ public final class IrisNightExposureSelector {
                                   boolean tripod,
                                   int shakiness,
                                   CaptureResult previewResult) {
-        return freezePlan(controller, characteristics, exposureCompensationEv, tripod, shakiness,
-                previewResult, true);
-    }
-
-    public static Plan freezePlan(CaptureController controller,
-                                  CameraCharacteristics characteristics,
-                                  float exposureCompensationEv,
-                                  boolean tripod,
-                                  int shakiness,
-                                  CaptureResult previewResult,
-                                  boolean requireLongExposure) {
         Range<Long> exposureRange = characteristics == null ? null
                 : characteristics.get(CameraCharacteristics.SENSOR_INFO_EXPOSURE_TIME_RANGE);
         Range<Integer> isoRange = characteristics == null ? null
@@ -146,16 +135,6 @@ public final class IrisNightExposureSelector {
         if (manualIso != 0.0)
             shortIso = Math.max(isoLow, Math.min(isoHigh, (int) manualIso));
 
-        if (!requireLongExposure) {
-            Plan shortOnly = new Plan(shortExposure, shortIso, shortExposure, shortIso,
-                    exposureLow, exposureHigh, isoLow, isoHigh, analogIso, tripod, safeShakiness, antibanding);
-            Log.i(TAG, "IRIS_26552_NIGHT_2_SHORT_ONLY_EXPOSURE"
-                    + " shortNs=" + shortOnly.shortExposureNs + " shortIso=" + shortOnly.shortIso
-                    + " longRequested=false freshPostShutter=true zsl=false"
-                    + " photonIsoExpoSelector=false planFrozenAtShutter=true");
-            return shortOnly;
-        }
-
         long longCap = Math.max(shortExposure, longMotionCap(tripod, safeShakiness, exposureHigh));
         long desiredLong = (long) Math.ceil(shortExposure * LONG_TARGET_MULTIPLIER);
         long longExposure = clamp(
@@ -175,7 +154,7 @@ public final class IrisNightExposureSelector {
 
         Plan plan = new Plan(shortExposure, shortIso, longExposure, longIso,
                 exposureLow, exposureHigh, isoLow, isoHigh, analogIso, tripod, safeShakiness, antibanding);
-        Log.i(TAG, "IRIS_26552_NIGHT_DYNAMIC_EXPOSURE"
+        Log.i(TAG, "IRIS_26541_NIGHT_12_PLUS_3_EXPOSURE"
                 + " shortNs=" + plan.shortExposureNs + " shortIso=" + plan.shortIso
                 + " longNs=" + plan.longExposureNs + " longIso=" + plan.longIso
                 + " longToShortEnergy=" + plan.longToShortEnergyRatio
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightFrameSelector.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightFrameSelector.java
index f08f9149a22e6e17a2f5bb2da665a15678bdedf4..764e9ed2325e59bf1ee2db2d1eb3e60ab77dffd7 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightFrameSelector.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IrisNightFrameSelector.java
@@ -1,55 +1,15 @@
 package com.particlesdevs.photoncamera.processing.parameters;
 
-/** IRIS_26552_NIGHT_DYNAMIC_2_TO_50_FRAME_OWNER
- * Dedicated Night remains a fresh post-shutter bracket, but the shutter-time frame budget is now
- * frozen from the shared frame-count preference. Night never uses Motion ZSL/pre-shutter RAWs.
- * N=2 is SHORT-only; N>=3 preserves the proven 12+3 ratio with deterministic integer rounding.
+/** IRIS_26541_NIGHT_12_PLUS_3_FRAME_OWNER
+ * Dedicated Night is a fixed fresh post-shutter bracket: twelve short/reference-cohort RAWs plus
+ * three longer shadow RAWs. It is intentionally independent of the Motion ZSL frame slider/ring.
  */
 public final class IrisNightFrameSelector {
-    public static final int MIN_NIGHT_FRAMES = 2;
-    public static final int MAX_NIGHT_FRAMES = 50;
-
-    public static final class Plan {
-        public final int totalFrames;
-        public final int shortFrames;
-        public final int longFrames;
-
-        private Plan(int totalFrames, int shortFrames, int longFrames) {
-            if (totalFrames < MIN_NIGHT_FRAMES || totalFrames > MAX_NIGHT_FRAMES)
-                throw new IllegalArgumentException("26552 Night total out of range=" + totalFrames);
-            if (shortFrames < 2 || longFrames < 0 || shortFrames + longFrames != totalFrames)
-                throw new IllegalArgumentException("26552 Night invalid role split short=" + shortFrames
-                        + " long=" + longFrames + " total=" + totalFrames);
-            if (totalFrames == 2 && longFrames != 0)
-                throw new IllegalArgumentException("26552 two-frame Night must be 2 SHORT + 0 LONG");
-            if (totalFrames >= 3 && longFrames < 1)
-                throw new IllegalArgumentException("26552 Night >=3 frames requires LONG evidence");
-            this.totalFrames = totalFrames;
-            this.shortFrames = shortFrames;
-            this.longFrames = longFrames;
-        }
-
-        public boolean isShortFrame(int index) {
-            if (index < 0 || index >= totalFrames)
-                throw new IndexOutOfBoundsException("26552 Night frame index=" + index + " total=" + totalFrames);
-            return index < shortFrames;
-        }
-    }
-
+    public static final int SHORT_FRAMES = 12;
+    public static final int LONG_FRAMES = 3;
+    public static final int TOTAL_FRAMES = SHORT_FRAMES + LONG_FRAMES;
     private IrisNightFrameSelector() {}
-
-    public static int clampNightFrames(int requestedMaximum) {
-        return Math.max(MIN_NIGHT_FRAMES, Math.min(MAX_NIGHT_FRAMES, requestedMaximum));
-    }
-
-    public static Plan freezePlan(int requestedMaximum) {
-        final int total = clampNightFrames(requestedMaximum);
-        // round(N/5) for integer N, written without floating-point ambiguity.
-        final int longFrames = total == 2 ? 0 : Math.max(1, (total + 2) / 5);
-        return new Plan(total, total - longFrames, longFrames);
-    }
-
     public static int getFrames(int requestedMaximum) {
-        return freezePlan(requestedMaximum).totalFrames;
+        return TOTAL_FRAMES;
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
index 0f8c4b6b0437efd6436e8ff0bfe457224506c940..0c9659e68aaf3eb2d0d1484bcedc6501c4a3af61 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
@@ -300,10 +300,7 @@ public class PreferenceKeys {
     }
 
     public static int getFrameCountValue() {
-        // IRIS_26552_FRAME_SLIDER_1_TO_50_RUNTIME_CLAMP
-        // Save-per-lens can retain a historical value above the new UI ceiling; clamp at read time.
-        int persisted = preferenceKeys.settingsManager.getInteger(SCOPE_GLOBAL, Key.KEY_FRAME_COUNT);
-        return Math.max(1, Math.min(50, persisted));
+        return preferenceKeys.settingsManager.getInteger(SCOPE_GLOBAL, Key.KEY_FRAME_COUNT);
     }
 
     public static float getSharpnessValue() {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
index 3619b6168f43a43e325ab2ea75e3153aacf9ee7c..a68f77ba65e750ad028a99e9534aa123b780836b 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
@@ -107,17 +107,6 @@ public class CameraUIViewImpl implements CameraUIView {
         }
         return current;
     }
-
-    /* IRIS_26552_NIGHT_SHUTTER_RING_Z_ORDER
-     * Still-mode order is shutter -> processing/capture ring -> centered frame text. This is
-     * explicit because bringToFront() on the shutter alone can hide the Motion-sized ring.
-     */
-    private void iris26552ApplyStillShutterZOrder() {
-        if (mShutterButton != null) mShutterButton.bringToFront();
-        if (mProcessingProgressBar != null) mProcessingProgressBar.bringToFront();
-        if (bottombuttons != null && bottombuttons.frameCount != null)
-            bottombuttons.frameCount.bringToFront();
-    }
     CameraUIViewImpl(CameraFragment cameraFragment) {
         this.cameraFragment = cameraFragment;
         this.topbar = cameraFragment.cameraFragmentBinding.layoutTopbar;
@@ -560,10 +549,7 @@ public class CameraUIViewImpl implements CameraUIView {
         if (mShutterButton != null) {
             mShutterButton.setClickable(true);
             mShutterButton.setEnabled(true);
-            if (cameraMode == CameraMode.VIDEO || cameraMode == CameraMode.RAWVIDEO)
-                mShutterButton.bringToFront();
-            else
-                iris26552ApplyStillShutterZOrder();
+            mShutterButton.bringToFront();
         }
 
         if (uiEventsListener != null) uiEventsListener.onCameraModeChanged(cameraMode);
@@ -657,8 +643,6 @@ public class CameraUIViewImpl implements CameraUIView {
             this.mProcessingProgressBar.setClickable(false);
             this.mProcessingProgressBar.setFocusable(false);
             this.mProcessingProgressBar.setVisibility(show ? View.VISIBLE : View.GONE);
-            if (show && ownerMode != CameraMode.VIDEO && ownerMode != CameraMode.RAWVIDEO)
-                iris26552ApplyStillShutterZOrder();
             if (!show) {
                 this.mProcessingProgressBar.setProgress(0);
                 this.mProcessingProgressBar.clearAnimation();
@@ -673,16 +657,9 @@ public class CameraUIViewImpl implements CameraUIView {
     public void incrementCaptureProgressBar(int step) {
         final long generation = iris26551ProgressUiGeneration;
         final CameraMode ownerMode = displayedMode;
-        final ProgressBar target = ownerMode == CameraMode.NIGHT
-                ? this.mProcessingProgressBar : this.mCaptureProgressBar;
-        target.post(() -> {
+        this.mCaptureProgressBar.post(() -> {
             if (!iris26551ProgressUiIsCurrent(generation, ownerMode, "capture-ring-increment")) return;
-            target.incrementProgressBy(step);
-            if (ownerMode == CameraMode.NIGHT) {
-                target.setVisibility(View.VISIBLE);
-                target.setIndeterminate(false);
-                iris26552ApplyStillShutterZOrder();
-            }
+            this.mCaptureProgressBar.incrementProgressBy(step);
         });
     }
 
@@ -704,20 +681,6 @@ public class CameraUIViewImpl implements CameraUIView {
                 cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer
                         .setVisibility(View.INVISIBLE);
             }
-            if (ownerMode == CameraMode.NIGHT && mProcessingProgressBar != null) {
-                mProcessingProgressBar.animate().cancel();
-                mProcessingProgressBar.setIndeterminate(false);
-                if (CaptureController.isProcessing) {
-                    // Sequence completion precedes processor start. Hold the same ring at full
-                    // progress instead of flashing it away; processing start turns it indeterminate.
-                    mProcessingProgressBar.setProgress(mProcessingProgressBar.getMax());
-                    mProcessingProgressBar.setVisibility(View.VISIBLE);
-                    iris26552ApplyStillShutterZOrder();
-                } else {
-                    mProcessingProgressBar.setProgress(0);
-                    mProcessingProgressBar.setVisibility(View.GONE);
-                }
-            }
         });
     }
 
@@ -728,26 +691,17 @@ public class CameraUIViewImpl implements CameraUIView {
         this.mCaptureProgressBar.post(() -> {
             if (!iris26551ProgressUiIsCurrent(generation, ownerMode, "capture-ring-opacity")) return;
             boolean visible = alpha > 0.0f;
-            if (ownerMode == CameraMode.NIGHT) {
-                this.mCaptureProgressBar.setAlpha(0.0f);
-                this.mCaptureProgressBar.setVisibility(View.INVISIBLE);
-                cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer.setVisibility(View.INVISIBLE);
-                this.mProcessingProgressBar.setIndeterminate(false);
-                this.mProcessingProgressBar.setVisibility(visible ? View.VISIBLE : View.GONE);
-                if (visible) iris26552ApplyStillShutterZOrder();
-            } else {
-                this.mCaptureProgressBar.setAlpha(alpha);
-                this.mCaptureProgressBar.setVisibility(visible ? View.VISIBLE : View.INVISIBLE);
-                if (cameraFragment.cameraFragmentBinding != null
-                        && cameraFragment.cameraFragmentBinding.layoutViewfinder != null
-                        && cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer != null) {
-                    cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer
-                            .setVisibility(visible ? View.VISIBLE : View.INVISIBLE);
-                }
+            this.mCaptureProgressBar.setAlpha(alpha);
+            this.mCaptureProgressBar.setVisibility(visible ? View.VISIBLE : View.INVISIBLE);
+
+            if (cameraFragment.cameraFragmentBinding != null
+                    && cameraFragment.cameraFragmentBinding.layoutViewfinder != null
+                    && cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer != null) {
+                cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer
+                        .setVisibility(visible ? View.VISIBLE : View.INVISIBLE);
             }
-            Log.i(TAG, "IRIS_26552_CAPTURE_RING_" + (visible ? "SHOW" : "HIDE")
-                    + " generation=" + generation + " mode=" + ownerMode
-                    + " nightUsesShutterRing=" + (ownerMode == CameraMode.NIGHT));
+            Log.i(TAG, "IRIS_26551_CAPTURE_RING_" + (visible ? "SHOW" : "HIDE")
+                    + " generation=" + generation + " mode=" + ownerMode);
         });
     }
 
@@ -758,25 +712,9 @@ public class CameraUIViewImpl implements CameraUIView {
          */
         final long generation = iris26551AdvanceProgressUiGeneration("capture-start:max=" + max);
         final CameraMode ownerMode = displayedMode;
-        final ProgressBar target = ownerMode == CameraMode.NIGHT
-                ? this.mProcessingProgressBar : this.mCaptureProgressBar;
-        target.post(() -> {
+        this.mCaptureProgressBar.post(() -> {
             if (!iris26551ProgressUiIsCurrent(generation, ownerMode, "capture-ring-max")) return;
-            target.setMax(max);
-            target.setProgress(0);
-            if (ownerMode == CameraMode.NIGHT) {
-                target.setIndeterminate(false);
-                target.setVisibility(View.VISIBLE);
-                this.mCaptureProgressBar.setVisibility(View.INVISIBLE);
-                cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer.setVisibility(View.INVISIBLE);
-                if (bottombuttons != null && bottombuttons.frameCount != null) {
-                    bottombuttons.frameCount.setText(String.valueOf(max));
-                    bottombuttons.frameCount.setVisibility(View.VISIBLE);
-                }
-                iris26552ApplyStillShutterZOrder();
-                Log.i(TAG, "IRIS_26552_NIGHT_SHUTTER_CAPTURE_RING max=" + max
-                        + " generation=" + generation + " determinate=true");
-            }
+            this.mCaptureProgressBar.setMax(max);
         });
     }
 
@@ -1033,12 +971,8 @@ public class CameraUIViewImpl implements CameraUIView {
             topbar.setEisVisible(false);
             topbar.setFpsVisible(true);
             topbar.setTimerVisible(true);
-            // IRIS_26552_NIGHT_NO_OVERSIZED_VIEWFINDER_RING
-            cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer.setVisibility(View.INVISIBLE);
-            cameraFragment.cameraFragmentBinding.layoutViewfinder.captureProgressBar.setVisibility(View.INVISIBLE);
-            if (bottombuttons != null && bottombuttons.frameCount != null)
-                bottombuttons.frameCount.setVisibility(View.VISIBLE);
-            iris26552ApplyStillShutterZOrder();
+            cameraFragment.cameraFragmentBinding.layoutViewfinder.frameTimer.setVisibility(View.VISIBLE);
+            cameraFragment.cameraFragmentBinding.layoutViewfinder.captureProgressBar.setVisibility(View.VISIBLE);
             setVideoRecordingInfoVisible(false);
             cameraFragment.cameraFragmentBinding.settingsBar.setChildVisibility(R.id.eis_entry_layout, View.GONE);
             cameraFragment.cameraFragmentBinding.settingsBar.setChildVisibility(R.id.fps_entry_layout, View.GONE);
diff --git a/app/src/main/res/xml/preferences.xml b/app/src/main/res/xml/preferences.xml
index 8c3d493f84cffd128be8ba17eaf2937d2f4dfbe8..0b41f78210117db6eda2bbfb62838f3fe2308a64 100644
--- a/app/src/main/res/xml/preferences.xml
+++ b/app/src/main/res/xml/preferences.xml
@@ -14,7 +14,7 @@
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.ManagedSwitchPreference ns0:title="@string/pref_save_per_lens_string" ns0:layout="@layout/preference_with_margin" ns0:key="@string/pref_save_per_lens_settings" ns0:icon="@drawable/ic_multi_lens" ns1:defaultValue="false" />
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_photo_key" ns0:title="@string/photo_settings">
-        <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="@string/pref_frame_count_key" ns0:layout="@layout/preference_seekbar" ns0:title="@string/frame_count" ns0:defaultValue="@string/pref_framecount_default" ns1:maxValue="50" ns1:minValue="1" ns0:icon="@drawable/ic_burst_mode_black_24dp" />
+        <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="@string/pref_frame_count_key" ns0:layout="@layout/preference_seekbar" ns0:title="@string/frame_count" ns0:defaultValue="@string/pref_framecount_default" ns1:maxValue="200" ns1:minValue="1" ns0:icon="@drawable/ic_burst_mode_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_motion_viewfinder_match_strength" ns0:layout="@layout/preference_seekbar" ns0:title="Motion viewfinder match strength" ns0:summary="Automatic viewfinder brightness response (%). 65% is the calibrated default; 100% follows the full measured EV gap. Saved separately when Save per lens settings is enabled." ns0:defaultValue="65" ns1:maxValue="100" ns1:minValue="0" ns0:icon="@drawable/ic_saturation" />
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_video_key" ns0:title="@string/video_settings">
diff --git a/app/version.properties b/app/version.properties
index d1a73f2377f3e2b5c310940ceb6d4c648fa1656a..4d44878234b0342249fc2fbef033b44f50d32c5a 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726552
-VERSION_BUILD=26552
+VERSION_NAME=0.9726551
+VERSION_BUILD=26551
