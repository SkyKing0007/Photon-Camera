diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index 66c9702a1a046da9065b7b7ba2c6cdccf990e95f..13cf70e51e0f8ee15fedef0471e12f3c32018e72 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -77,15 +77,14 @@ void main(){
     float safeMax=max(maxGainRatio,1.001);
     float ratio;
     if(motionHdrHandoff!=0){
-        /* IRIS_26629_ANDROID_UHDR_LUMINANCE_ONLY_BODY_POP
-         * The completed SDR base remains the sole detail/color/tone authority. Android Ultra HDR
-         * receives only a single-channel scalar display-luminance gain: recover the intentional
-         * 1.00/0.80 = 1.25 body headroom at full HDR display capacity, while genuine >1.25
-         * scene headroom may rise farther. No alternate HDR detail image is divided by SDR and
-         * no RGB-channel-specific gain is introduced. */
-        const float bodyLuminanceRatio=1.25;
+        /* IRIS_26614_CANONICAL_APPEARANCE_UHDR_EXTENSION
+         * The SDR base is the appearance authority. Ultra HDR is forbidden from dividing the clean
+         * RAW/HDR master by the tone-mapped SDR because that mathematically undoes SDR appearance
+         * and lets the gain map restore house/tree/curtain/ceiling structure absent from the base.
+         * Motion gain is therefore exactly unity through nominal source white. Only genuine >1
+         * source headroom adds scalar display luminance, preserving the SDR color/gamma structure. */
         float sourceGuide=max(max3(hdrPositive),luminance(hdrPositive));
-        ratio=clamp(max(sourceGuide,bodyLuminanceRatio),1.0,safeMax);
+        ratio=clamp(max(sourceGuide,1.0),1.0,safeMax);
     }else{
         float hdrTargetScale=hdrExposureScale;
         float hdr=max(luminance(hdrPositive*hdrTargetScale),0.0);
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 7685aa435f79a83eeeb53cd48645e0f388f2eaf7..a3dad7d04204714ba09f6ba0670c82944d9d142c 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -277,40 +277,6 @@ vec3 fitDisplayGamut(vec3 rgb) {
     return rgb/max(peak,1.0e-6);
 }
 
-/* IRIS_26629_LUMINANCE_LOCKED_ADAPTIVE_CHROMA_RESTORE
- * Final Iris color presentation restores only the measured dark/mid-dark chroma deficit.
- * It runs after the proven 26628 tone/gamut result so it cannot alter the tone operator itself.
- * Chroma expands only along the neutral axis at fixed Display-P3 luminance; deepest blacks,
- * near-neutrals, already-strong colors and bright pixels taper toward unity.  A per-channel
- * neutral-axis gamut limit is applied to the shared chroma scale, never to individual RGB values.
- */
-float iris26629ChromaGainLimit(float y,float c){
-    if(c>1.0e-8) return max(1.0,(1.0-y)/c);
-    if(c<(-1.0e-8)) return max(1.0,y/(-c));
-    return 1.0e6;
-}
-
-vec3 iris26629RestoreChroma(vec3 rgb){
-    rgb=clamp(rgb,vec3(0.0),vec3(1.0));
-    const vec3 displayP3Luma=vec3(0.22897456,0.69173852,0.07928691);
-    float y=clamp(dot(rgb,displayP3Luma),0.0,1.0);
-    vec3 chroma=rgb-vec3(y);
-    float relativeChroma=length(chroma)/(y+0.05);
-
-    float rise=smoothstep(0.025,0.055,y);
-    float fall=1.0-smoothstep(0.13,0.36,y);
-    float neutralGate=smoothstep(0.035,0.11,relativeChroma);
-    float strongColorTaper=1.0-0.65*smoothstep(0.38,0.72,relativeChroma);
-    float requestedGain=1.0+0.20*rise*fall*neutralGate*strongColorTaper;
-
-    float limit=1.0e6;
-    limit=min(limit,iris26629ChromaGainLimit(y,chroma.r));
-    limit=min(limit,iris26629ChromaGainLimit(y,chroma.g));
-    limit=min(limit,iris26629ChromaGainLimit(y,chroma.b));
-    float appliedGain=min(requestedGain,limit);
-    return clamp(vec3(y)+chroma*appliedGain,vec3(0.0),vec3(1.0));
-}
-
 void main() {
     ivec2 xy=ivec2(gl_FragCoord.xy);
     ivec2 sourceSize=textureSize(InputBuffer,0);
@@ -367,7 +333,6 @@ void main() {
      * Local Laplacian supplies an absolute mapped luminance target, never a second exposure gain.
      */
     linearSrgb=fitDisplayGamut(linearSrgb);
-    linearSrgb=iris26629RestoreChroma(linearSrgb);
 
     Output=clamp(
             srgbEncode(linearSrgb),
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 62239e60e5e98001c4c87b6c92ab9b4b7afb735d..b24cb9e35170802130bf3bee9f149b43060bd1da 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -474,24 +474,6 @@ inline float componentGainLimit(float y,float c){
     if(c<-1.0e-7f)return std::max(1.f,(0.f-y)/c);
     return 4.f;
 }
-/* IRIS_26629_LUMINANCE_LOCKED_ADAPTIVE_CHROMA_RESTORE_TRUE2X_CPU */
-inline float iris26629ChromaGainLimit(float y,float c){
-    if(c>1.0e-8f)return std::max(1.f,(1.f-y)/c);
-    if(c<(-1.0e-8f))return std::max(1.f,y/(-c));
-    return 1.0e6f;
-}
-inline Vec3 iris26629RestoreChroma(Vec3 rgb){
-    rgb={clampf(rgb.r,0.f,1.f),clampf(rgb.g,0.f,1.f),clampf(rgb.b,0.f,1.f)};
-    float y=clampf(luma(rgb),0.f,1.f);Vec3 c={rgb.r-y,rgb.g-y,rgb.b-y};
-    float relative=length3(c)/(y+0.05f);
-    float rise=smoothstep(0.025f,0.055f,y),fall=1.f-smoothstep(0.13f,0.36f,y);
-    float neutral=smoothstep(0.035f,0.11f,relative);
-    float strong=1.f-0.65f*smoothstep(0.38f,0.72f,relative);
-    float requested=1.f+0.20f*rise*fall*neutral*strong;
-    float limit=1.0e6f;limit=std::min(limit,iris26629ChromaGainLimit(y,c.r));limit=std::min(limit,iris26629ChromaGainLimit(y,c.g));limit=std::min(limit,iris26629ChromaGainLimit(y,c.b));
-    float applied=std::min(requested,limit);Vec3 out={y+c.r*applied,y+c.g*applied,y+c.b*applied};
-    return {clampf(out.r,0.f,1.f),clampf(out.g,0.f,1.f),clampf(out.b,0.f,1.f)};
-}
 struct SourceRegion {
     int x0=0,y0=0,w=0,h=0,fullW=0,fullH=0;
     std::vector<uint16_t> rgb;
@@ -683,8 +665,7 @@ inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float localMappedGuide){
         if(guide>1.0e-7f&&guide>start){float white=std::max(p.sceneWhite,start+0.05f);float u=std::max((guide-start)/std::max(white-start,1.0e-6f),0.f);float x=clampf(u,0.f,1.f);float shaped=std::log(1.f+6.f*x)/std::log(7.f);float mapped=start+(1.25f-start)*shaped;rgb=mul(rgb,mapped/guide);}
         rgb=mul(rgb,0.80f);
     }
-    float pk2=peak(rgb);if(pk2>1.f)rgb=mul(rgb,1.f/std::max(pk2,1.0e-6f));
-    return iris26629RestoreChroma(clampNonnegative(rgb));
+    float pk2=peak(rgb);if(pk2>1.f)rgb=mul(rgb,1.f/std::max(pk2,1.0e-6f));return clampNonnegative(rgb);
 }
 struct SrgbLut {std::array<float,16385>v{};SrgbLut(){for(size_t i=0;i<v.size();i++){float x=(float)i/(float)(v.size()-1);v[i]=x<=0.0031308f?12.92f*x:1.055f*std::pow(x,1.f/2.4f)-0.055f;}}float at(float x)const{x=clampf(x,0.f,1.f);float q=x*(float)(v.size()-1);int i=(int)q;int j=std::min(i+1,(int)v.size()-1);return v[(size_t)i]+(v[(size_t)j]-v[(size_t)i])*(q-i);}};
 inline void true2xFinalToUnrotated(float x,float y,const Params&p,float*outX,float*outY){
@@ -735,13 +716,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.25f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.25f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
@@ -864,22 +845,6 @@ float irisComponentGainLimit(float y,float c){
     if(c<(-1.0e-7))return max(1.0,(0.0-y)/c);
     return 4.0;
 }
-/* IRIS_26629_LUMINANCE_LOCKED_ADAPTIVE_CHROMA_RESTORE_TRUE2X_GPU */
-float iris26629ChromaGainLimit(float y,float c){
-    if(c>1.0e-8)return max(1.0,(1.0-y)/c);
-    if(c<(-1.0e-8))return max(1.0,y/(-c));
-    return 1.0e6;
-}
-vec3 iris26629RestoreChroma(vec3 rgb){
-    rgb=clamp(rgb,vec3(0.0),vec3(1.0));float y=clamp(irisLuma(rgb),0.0,1.0);vec3 c=rgb-vec3(y);
-    float relative=length(c)/(y+0.05);
-    float rise=smoothstep(0.025,0.055,y),fall=1.0-smoothstep(0.13,0.36,y);
-    float neutral=smoothstep(0.035,0.11,relative);
-    float strong=1.0-0.65*smoothstep(0.38,0.72,relative);
-    float requested=1.0+0.20*rise*fall*neutral*strong;
-    float limit=1.0e6;limit=min(limit,iris26629ChromaGainLimit(y,c.r));limit=min(limit,iris26629ChromaGainLimit(y,c.g));limit=min(limit,iris26629ChromaGainLimit(y,c.b));
-    return clamp(vec3(y)+c*min(requested,limit),vec3(0.0),vec3(1.0));
-}
 vec3 irisMatRows(vec3 r0,vec3 r1,vec3 r2,vec3 v){return vec3(dot(r0,v),dot(r1,v),dot(r2,v));}
 vec3 irisCameraAt(ivec2 fullP){
     fullP=clamp(fullP,ivec2(0),uTrueSize-ivec2(1));
@@ -997,7 +962,7 @@ vec3 irisHeadroom(vec3 rgb,float localMappedGuide){
         rgb*=0.80;
     }
     float pk2=irisPeak(rgb);if(pk2>1.0)rgb*=1.0/max(pk2,1.0e-6);
-    return iris26629RestoreChroma(irisClampNonnegative(rgb));
+    return irisClampNonnegative(rgb);
 }
 float irisGamma(float x){
     x=clamp(x,0.0,1.0);float q=x*16384.0;int a=int(q),b=min(a+1,16384);
@@ -1020,7 +985,7 @@ void main(){
         if(uMotionHdrHandoff!=0){
             vec3 hp=irisClampNonnegative(hdr);
             float sourceGuide=max(irisPeak(hp),irisLuma(hp));
-            ratio=clamp(max(sourceGuide,1.25),1.0,contentMax);
+            ratio=clamp(max(sourceGuide,1.0),1.0,contentMax);
         }else{
             float hdrY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
             float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index b16a98dc71d65281d9b9156fc540f5f67e9b44dc..602cd84ccbadc14a915081f1d696e7a907afa6dc 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -77,10 +77,6 @@ public final class MotionV2Render extends Node {
     private static final float IRIS_26592_TAIL_LOG_SHAPE = 3.0f;
     private static final float IRIS_26592_TANH_SCALE = 1.2020679f;
     private static final float IRIS_26592_MOTION_UHDR_MAX_RATIO = 8.0f;
-    /* IRIS_26629_ANDROID_UHDR_LUMINANCE_ONLY_BODY_POP
-     * Android UHDR changes display luminance only. The SDR base remains byte/pixel/detail authority;
-     * 1.25 is the full-HDR recovery of the existing 0.80 SDR presentation scale. */
-    static final float IRIS_26629_MOTION_UHDR_BODY_LUMINANCE_RATIO = 1.25f;
     /* IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS
      * Preserve the tested 26505 SDR primary exactly. Ultra HDR is a reversible
      * rendition relationship: the gain map should recover the wanted HDR signal
@@ -986,8 +982,8 @@ public final class MotionV2Render extends Node {
                                     + " peakCode=" + peakCode
                                     + " actualPeakContentRatio=" + actualPeakContentRatio
                                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
-                                    + " bodyGainOwner=UHDR_SCALAR_LUMINANCE_ONLY"
-                                    + " hdrGainSource=SDR_BASE_PLUS_BODY_LUMINANCE_PLUS_SOURCE_HEADROOM"
+                                    + " separateBodyGainOwner=false"
+                                    + " hdrGainSource=CANONICAL_SDR_PLUS_SOURCE_HEADROOM_ONLY"
                                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                                     + " hdrExposureScale=" + HDR_EXPOSURE_SCALE);
                 } catch (Throwable ignored) {}
@@ -1022,12 +1018,11 @@ public final class MotionV2Render extends Node {
                         + basePipeline.mParameters.motionV2ReconstructionOwner
                         + " pointDecimation=false"
                         + " postAliasSpikeRepair=false"
-                        + " sdrBaseDetailAuthority=true colorAuthority=SDR_BASE_ONLY"
+                        + " midtoneGainUnity=true sourceAtOrBelowWhiteGainUnity=true"
                         + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
-                        + " motionHdrBodyGainRatio=" + IRIS_26629_MOTION_UHDR_BODY_LUMINANCE_RATIO
-                        + " motionHdrGainIsScalarLuminanceOnly=true"
-                        + " motionHdrAdditionalHeadroomStartsAboveBodyRatio=true"
-                        + " IRIS_26629_ANDROID_UHDR_LUMINANCE_ONLY_BODY_POP=true");
+                        + " motionHdrBodyGainRatio=1.0"
+                        + " motionHdrGainStartsAboveSourceWhite=true"
+                        + " IRIS_26614_UHDR_BODY_APPEARANCE_PARITY=true");
             } finally {
                 if (gainTexture != null) {
                     try { gainTexture.close(); } catch (Throwable ignored) {}
@@ -1068,8 +1063,7 @@ public final class MotionV2Render extends Node {
                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                     + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
                     + " oneMasterRendition=true"
-                    + " uhdrBodyGainOwner=SCALAR_GAINMAP_ONLY"
-                    + " uhdrSdrDetailOneToOne=true"
+                    + " separateBodyGainOwnerRequired=false"
                     + " superResToneParity=true");
         }
 
@@ -1114,8 +1108,7 @@ public final class MotionV2Render extends Node {
                 + " hdrTargetUsesSameScale=true"
                 + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
                 + " nominalHdrBodyRecoveryRatio="
-                    + IRIS_26629_MOTION_UHDR_BODY_LUMINANCE_RATIO
-                + " uhdrLuminanceOnly=true uhdrSdrDetailOneToOne=true"
+                    + (HDR_EXPOSURE_SCALE / OUTPUT_EXPOSURE_SCALE)
                 + " IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS=true"
                 + " syntheticBitmapGainMap=false"
                 + " irisOutputZoom=" + irisOutputZoom
diff --git a/app/version.properties b/app/version.properties
index e20a68623683cb3798a26ea1244c8722f62f02c3..9b7791b0f92ea4d739e48289ac8775dc69d478ba 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726629
-VERSION_BUILD=26629
+VERSION_NAME=0.9726628
+VERSION_BUILD=26628
