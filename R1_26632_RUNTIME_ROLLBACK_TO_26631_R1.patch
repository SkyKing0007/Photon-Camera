diff --git b/app/src/main/assets/shaders/motionv2/gainmap.glsl a/app/src/main/assets/shaders/motionv2/gainmap.glsl
index 473702043d9312ee0ea0a4ca7dd34514f1e942f3..d79ce9d527bf09571618c6040908eb99a44c3ff2 100644
--- b/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ a/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -76,17 +76,27 @@ void main(){
     float safeMax=max(maxGainRatio,1.001);
     float ratio;
     if(motionHdrHandoff!=0){
-        /* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION
-         * The completed SDR remains exact appearance authority. The post-VGN master contributes
-         * only a registered scalar luminance guide. A smooth log-domain display expansion builds
-         * the HDR rendition as SDR * gain(sourceY), so local SDR tone/detail can never create an
-         * accidental unity island. Gain is exactly 1 at black, continuous everywhere, approaches
-         * maxGainRatio asymptotically, and never changes RGB chromaticity or spatial geometry. */
-        const float gainKnee=1.50;
-        float sourceY=max(luminance(hdrPositive),0.0);
-        float weight=sourceY/(sourceY+gainKnee);
-        float logGain=log2(safeMax)*weight;
-        ratio=clamp(exp2(logGain),1.0,safeMax);
+        /* IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP
+         * The gain map is the scalar luminance ratio between a registered HDR rendition intent
+         * and the exact completed SDR primary, both evaluated in linear light. The HDR intent is
+         * normalized at the unchanged source-guide 0.65 upper-tone entry, where the adaptive SDR
+         * map still equals its inherited body map. Above that point real scene radiance may exceed
+         * the compressed SDR continuously, including bright ceiling structure below source white.
+         * No fixed 1.25 floor, no nominal-white gate, no RGB gain, and no spatial smoothing. */
+        const float matchGuide=0.65;
+        float requested=max(displayGain,1.0e-6)*max(hdrExposureScale,1.0e-6);
+        float whiteAnchor=min(0.95,requested);
+        float bodyGain=requested;
+        if(requested>whiteAnchor)bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
+        float bodyRatio=max(bodyGain/max(whiteAnchor,1.0e-6)-1.0,0.0);
+        float oneMinus=1.0-matchGuide;
+        float cubic=whiteAnchor*matchGuide
+                +(bodyGain-whiteAnchor)*matchGuide*oneMinus*oneMinus;
+        float rational=bodyGain*matchGuide/(1.0+bodyRatio*matchGuide);
+        float mappedMatch=0.5*(cubic+rational);
+        float hdrIntentScale=mappedMatch/matchGuide;
+        float hdr=max(luminance(hdrPositive)*hdrIntentScale,0.0);
+        ratio=clamp((hdr+UHDR_OFFSET)/(sdr+UHDR_OFFSET),1.0,safeMax);
     }else{
         float hdrTargetScale=hdrExposureScale;
         float hdr=max(luminance(hdrPositive*hdrTargetScale),0.0);
diff --git b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
index b0c4bb5b7c60c095a89d7329b6db755090463ed9..2eca2690e51e4667da83f838313d53e6ea36f236 100644
--- b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
+++ a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
@@ -20,11 +20,7 @@ float irisGuide(vec3 rgb){
     return max(dot(rgb,IRIS_LUMA),irisMax3(rgb));
 }
 
-/* IRIS_26632_SMOOTHER_UPPER_TONE
- * 26632 preserves the same pointwise/global owner and 0.65 entry but reserves more ordered SDR
- * range through source white. No spatial mask, edge boost, or local-protection change.
- *
- * IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE
+/* IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE
  * 26622 proved that the image-formation and Local-Laplacian architecture are sound, but two
  * very different scenes still crowded real HDR separation into the last few SDR code values.
  * Preserve the exact 26621/26622 mapping through source guide 0.65. Above that point, derive one
@@ -89,8 +85,8 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
     if(adaptiveEnable<=1.0e-7 || x<=upperStart) return legacy;
 
     float pressure=iris26623HighlightPressure();
-    float targetWhite=mix(0.945,0.925,pressure);
-    float targetSlope=mix(0.360,0.300,pressure);
+    float targetWhite=mix(0.925,0.885,pressure);
+    float targetSlope=mix(0.270,0.200,pressure);
     float startValue=0.0;
     float startSlope=0.0;
     iris26623LegacyBody(upperStart,requested,startValue,startSlope);
diff --git b/app/src/main/assets/shaders/motionv2/render.glsl a/app/src/main/assets/shaders/motionv2/render.glsl
index 0244186ac88f5f977665969b2ceb6e28ef343e03..1ba810058113ea47cb7bbf1f0b8a54483dbe5067 100644
--- b/app/src/main/assets/shaders/motionv2/render.glsl
+++ a/app/src/main/assets/shaders/motionv2/render.glsl
@@ -101,11 +101,7 @@ vec3 iris26524BilinearInput(vec2 sourcePixel) {
     return mix(a,b,f.y);
 }
 
-/* IRIS_26632_SMOOTHER_UPPER_TONE
- * 26632 preserves the same pointwise/global owner and 0.65 entry but reserves more ordered SDR
- * range through source white. No spatial mask, edge boost, or local-protection change.
- *
- * IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE
+/* IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE
  * 26622 proved that the image-formation and Local-Laplacian architecture are sound, but two
  * very different scenes still crowded real HDR separation into the last few SDR code values.
  * Preserve the exact 26621/26622 mapping through source guide 0.65. Above that point, derive one
@@ -170,8 +166,8 @@ float iris26623MapMotionSdrFinalGuide(float sourceGuide){
     if(adaptiveEnable<=1.0e-7 || x<=upperStart) return legacy;
 
     float pressure=iris26623HighlightPressure();
-    float targetWhite=mix(0.945,0.925,pressure);
-    float targetSlope=mix(0.360,0.300,pressure);
+    float targetWhite=mix(0.925,0.885,pressure);
+    float targetSlope=mix(0.270,0.200,pressure);
     float startValue=0.0;
     float startSlope=0.0;
     iris26623LegacyBody(upperStart,requested,startValue,startSlope);
diff --git b/app/src/main/cpp/motionv2_jpeg444_jni.cpp a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index c6c7627341036898a8225403a0bdc32ac86d3687..9f6c238a679769c962607a0884ba140fc87ae8de 100644
--- b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -737,13 +737,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_CPU */const float gainKnee=1.50f;float sourceY=std::max(luma(clampNonnegative(hdr)),0.f);float weight=sourceY/(sourceY+gainKnee);float logGain=std::log2(contentMax)*weight;ratio=clampf(std::exp2(logGain),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP_TRUE2X_CPU */const float matchGuide=0.65f;float mappedMatch=iris26621MapMotionSdrFinalGuide(matchGuide,p.displayGain);float hdrIntentScale=mappedMatch/matchGuide;float hdrY=std::max(luma(clampNonnegative(hdr))*hdrIntentScale,0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_CPU */const float gainKnee=1.50f;float sourceY=std::max(luma(clampNonnegative(hdr)),0.f);float weight=sourceY/(sourceY+gainKnee);float logGain=std::log2(contentMax)*weight;ratio=clampf(std::exp2(logGain),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){/* IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP_TRUE2X_CPU */const float matchGuide=0.65f;float mappedMatch=iris26621MapMotionSdrFinalGuide(matchGuide,p.displayGain);float hdrIntentScale=mappedMatch/matchGuide;float hdrY=std::max(luma(clampNonnegative(hdr))*hdrIntentScale,0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
@@ -1023,12 +1023,13 @@ void main(){
         float contentMax=clamp(uGainContentMax,1.0,safeMax);
         float ratio;
         if(uMotionHdrHandoff!=0){
-            /* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION_TRUE2X_GPU */
-            const float gainKnee=1.50;
-            float sourceY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
-            float weight=sourceY/(sourceY+gainKnee);
-            float logGain=log2(contentMax)*weight;
-            ratio=clamp(exp2(logGain),1.0,contentMax);
+            /* IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP_TRUE2X_GPU */
+            const float matchGuide=0.65;
+            float mappedMatch=iris26621MapMotionSdrFinalGuide(matchGuide);
+            float hdrIntentScale=mappedMatch/matchGuide;
+            float hdrY=max(irisLuma(irisClampNonnegative(hdr))*hdrIntentScale,0.0);
+            float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
+            ratio=clamp((hdrY+off)/(sdrY+off),1.0,contentMax);
         }else{
             float hdrY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
             float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
diff --git b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index e884c061f92f9d8abda7c9a0ae763a4f1fe7c920..e439d0fb9c1bc0bac7da26b0f948c9bfb2d96df3 100644
--- b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -78,11 +78,11 @@ public final class MotionV2Render extends Node {
     private static final float IRIS_26592_TAIL_LOG_SHAPE = 3.0f;
     private static final float IRIS_26592_TANH_SCALE = 1.2020679f;
     private static final float IRIS_26592_MOTION_UHDR_MAX_RATIO = 8.0f;
-    /* IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION
-     * Motion UHDR keeps the completed SDR as exact appearance authority. The post-VGN master
-     * supplies only the spatial luminance guide for a continuous output-referred HDR expansion.
-     * No scene-linear/SDR quotient, no single match anchor, no hard threshold, and no RGB gain. */
-    private static final float IRIS_26632_HDR_GAIN_KNEE = 1.50f;
+    /* IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP
+     * Motion UHDR keeps the completed SDR as the exact primary. Its HDR rendition is a scalar
+     * luminance quotient in matched linear light, anchored at the existing 0.65 upper-tone entry.
+     * There is no fixed body boost and no nominal-white discontinuity. */
+    private static final float IRIS_26631_HDR_INTENT_MATCH_SOURCE_GUIDE = 0.65f;
     /* IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS
      * Preserve the tested 26505 SDR primary exactly. Ultra HDR is a reversible
      * rendition relationship: the gain map should recover the wanted HDR signal
@@ -113,10 +113,10 @@ public final class MotionV2Render extends Node {
     private static final float IRIS_26621_EDGE_SLOPE = 0.94f;
     public static final float IRIS_26621_SDR_WHITE_ANCHOR = 0.95f;
     public static final float IRIS_26623_UPPER_TONE_START = 0.65f;
-    public static final float IRIS_26623_SPARSE_WHITE_ANCHOR = 0.945f;
-    public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.925f;
-    public static final float IRIS_26623_SPARSE_WHITE_SLOPE = 0.360f;
-    public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.300f;
+    public static final float IRIS_26623_SPARSE_WHITE_ANCHOR = 0.925f;
+    public static final float IRIS_26623_BROAD_WHITE_ANCHOR = 0.885f;
+    public static final float IRIS_26623_SPARSE_WHITE_SLOPE = 0.270f;
+    public static final float IRIS_26623_BROAD_WHITE_SLOPE = 0.200f;
     private static final float IRIS_26626_SOURCE_PRESERVATION_MAX = 0.30f;
 
     static float iris26621MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
@@ -985,10 +985,7 @@ public final class MotionV2Render extends Node {
 
                 float actualPeakContentRatio = (float)Math.pow(
                         Math.max(maxGainRatio, 1.001f), peakCode / 255.0f);
-                /* IRIS_26632_UHDR_CAPACITY_EQUALS_ENCODING_RANGE
-                 * Capacity describes the display range over which the encoded gain map is
-                 * progressively applied; it is not the current scene's measured peak code. */
-                float fullHdrDisplayRatio = maxGainRatio;
+                float fullHdrDisplayRatio = Math.max(1.02f, actualPeakContentRatio);
                 pipeline.motionV2GainMapBitmap = gainMap;
                 pipeline.motionV2GainMapMaxRatio = maxGainRatio;
                 pipeline.motionV2GainMapFullHdrDisplayRatio = fullHdrDisplayRatio;
@@ -999,11 +996,10 @@ public final class MotionV2Render extends Node {
                                     + " peakCode=" + peakCode
                                     + " actualPeakContentRatio=" + actualPeakContentRatio
                                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
-                                    + " capacityMatchesGainMapMax=true actualPeakContentRatio=" + actualPeakContentRatio
-                                    + " bodyGainOwner=CONTINUOUS_OUTPUT_REFERRED_SCALAR_EXPANSION"
-                                    + " hdrGainSource=POST_VGN_LUMINANCE_GUIDED_SDR_DISPLAY_EXPANSION"
-                                    + " hdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
-                                    + " singleMatchAnchor=false fixedBodyGain=false nominalWhiteDiscontinuity=false"
+                                    + " bodyGainOwner=UNITY_UNTIL_HDR_INTENT_EXCEEDS_FINAL_SDR"
+                                    + " hdrGainSource=MATCHED_LINEAR_HDR_INTENT_OVER_FINAL_SDR_LUMINANCE"
+                                    + " hdrIntentMatchSourceGuide=" + IRIS_26631_HDR_INTENT_MATCH_SOURCE_GUIDE
+                                    + " fixedBodyGain=false nominalWhiteDiscontinuity=false"
                                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                                     + " hdrExposureScale=" + HDR_EXPOSURE_SCALE);
                 } catch (Throwable ignored) {}
@@ -1040,11 +1036,11 @@ public final class MotionV2Render extends Node {
                         + " postAliasSpikeRepair=false"
                         + " sdrBaseDetailAuthority=true colorAuthority=SDR_BASE_ONLY"
                         + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
-                        + " motionHdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
+                        + " motionHdrIntentMatchSourceGuide=" + IRIS_26631_HDR_INTENT_MATCH_SOURCE_GUIDE
                         + " motionHdrGainIsScalarLuminanceOnly=true"
-                        + " motionHdrGainEquation=OUTPUT_REFERRED_SDR_TIMES_SOURCE_GUIDED_SCALAR_GAIN"
-                        + " motionHdrSingleMatchAnchor=false motionHdrFixedBodyGain=false motionHdrNominalWhiteGate=false"
-                        + " IRIS_26632_OUTPUT_REFERRED_HDR_PRESENTATION=true");
+                        + " motionHdrGainEquation=LINEAR_HDR_INTENT_OVER_FINAL_LINEAR_SDR"
+                        + " motionHdrFixedBodyGain=false motionHdrNominalWhiteGate=false"
+                        + " IRIS_26631_CONTINUOUS_INTENT_QUOTIENT_GAINMAP=true");
             } finally {
                 if (gainTexture != null) {
                     try { gainTexture.close(); } catch (Throwable ignored) {}
@@ -1085,8 +1081,8 @@ public final class MotionV2Render extends Node {
                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                     + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
                     + " oneMasterRendition=true"
-                    + " uhdrBodyGainOwner=CONTINUOUS_OUTPUT_REFERRED_SCALAR_EXPANSION"
-                    + " uhdrGainOwner=POST_VGN_LUMINANCE_GUIDED_DISPLAY_EXPANSION"
+                    + " uhdrBodyGainOwner=UNITY_UNTIL_HDR_INTENT_EXCEEDS_FINAL_SDR"
+                    + " uhdrGainOwner=CONTINUOUS_LINEAR_INTENT_QUOTIENT"
                     + " superResToneParity=true");
         }
 
@@ -1130,8 +1126,8 @@ public final class MotionV2Render extends Node {
                 + " outputExposureEv=-0.321928"
                 + " hdrTargetUsesSameScale=true"
                 + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
-                + " hdrGainKnee=" + IRIS_26632_HDR_GAIN_KNEE
-                + " uhdrLuminanceOnly=true uhdrOutputReferredContinuousExpansion=true"
+                + " hdrIntentMatchSourceGuide=" + IRIS_26631_HDR_INTENT_MATCH_SOURCE_GUIDE
+                + " uhdrLuminanceOnly=true uhdrContinuousIntentQuotient=true"
                 + " IRIS_26506_SEPARATE_SDR_HDR_EXPOSURE_TARGETS=true"
                 + " syntheticBitmapGainMap=false"
                 + " irisOutputZoom=" + irisOutputZoom
diff --git b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index 885b83f6eda625f006b14a00c5eb3d615ea49b41..5491a55d783029f8538058713fe6d4aa605fab06 100644
--- b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -379,7 +379,7 @@ public class PostPipeline extends GLBasePipeline {
                                 + " hdrMasterOwner=POST_VGN_EXTENDED_LINEAR"
                                 + " maxRatio=" + motionV2GainMapMaxRatio
                                 + " fullHdrDisplayRatio=" + motionV2GainMapFullHdrDisplayRatio
-                                + " capacityMatchesGainMapMax=true");
+                                + " capacityMatchesActualGainPeak=true");
             } catch (Throwable ignored) {}
             motionV2GainMapBitmap = null;
         }
diff --git b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2UltraHdr.java a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2UltraHdr.java
index 334a6ab392e865b7201fab98870bd267d033012c..945fc865f796e1ba3927aa48cdd435f22a37e033 100644
--- b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2UltraHdr.java
+++ a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2UltraHdr.java
@@ -98,13 +98,8 @@ public final class MotionV2UltraHdr {
              * ceiling from truncating valid -2.5EV SHORT recovery (5.657x physical headroom).
              */
             float safeMax = Math.max(1.50f, Math.min(8.0f, maxRatio));
-            /* IRIS_26632_UHDR_CAPACITY_EQUALS_ENCODING_RANGE
-             * Motion advertises the same full-HDR display capacity as the gain-map encoding range.
-             * The scene's current peak gain remains content, not metadata capacity. Night retains
-             * its frozen requested-capacity policy. */
-            float fullHdrDisplayRatio = motionCapacityAuthority
-                    ? safeMax
-                    : Math.max(1.02f, Math.min(safeMax, requestedFullHdrDisplayRatio));
+            float fullHdrDisplayRatio = Math.max(
+                    1.02f, Math.min(safeMax, requestedFullHdrDisplayRatio));
             Gainmap gainmap = new Gainmap(oriented);
             gainmap.setRatioMin(MIN_RATIO, MIN_RATIO, MIN_RATIO);
             gainmap.setRatioMax(safeMax, safeMax, safeMax);
@@ -130,7 +125,7 @@ public final class MotionV2UltraHdr {
                                     + " ratioMax=" + safeMax
                                     + " minHdrTransition=" + MIN_HDR_TRANSITION
                                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
-                                    + " capacityMatchesGainMapMax=true"
+                                    + " capacityMatchesActualGainPeak=true"
                                     + " gainOwner=HEALTHY_HDR_MASTER_VS_SDR");
                 } catch (Throwable ignored) {}
             }
@@ -144,8 +139,8 @@ public final class MotionV2UltraHdr {
                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
                     + " ratioMaxIndependentFromFullDisplayRatio=" + safeMax
                     + " motionCapacityAuthority=" + motionCapacityAuthority
-                    + " capacityMatchesGainMapMax=" + motionCapacityAuthority
-                    + " IRIS_26632_UHDR_ENCODING_CAPACITY_AUTHORITY=" + motionCapacityAuthority
+                    + " capacityMatchesActualGainPeak=" + motionCapacityAuthority
+                    + " IRIS_26596_UHDR_ACTUAL_CONTENT_CAPACITY=" + motionCapacityAuthority
                     + " epsilonSdr=0.015625"
                     + " epsilonHdr=0.015625"
                     + " gamma=1.0"
diff --git b/app/version.properties a/app/version.properties
index 416ea4b2f958bc14f80dbf589b66d90420ab8c08..ef95fef2e677caa720467677c8612a10d4bdd6ae 100644
--- b/app/version.properties
+++ a/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726632
-VERSION_BUILD=26632
+VERSION_NAME=0.9726631
+VERSION_BUILD=26631
