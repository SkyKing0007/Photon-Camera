diff --git a/app/src/main/assets/shaders/preview/main_fs.glsl b/app/src/main/assets/shaders/preview/main_fs.glsl
index ad08e59a82ada33825c799fb50101ee8ef5b9e95..1e7f4b999b41a67f9d03cd44d7f153521e9a824b 100644
--- a/app/src/main/assets/shaders/preview/main_fs.glsl
+++ b/app/src/main/assets/shaders/preview/main_fs.glsl
@@ -31,19 +31,5 @@ void main() {
     vec4 dc = vec4(1.0,0.0,1.0,0.0);
     if(enablePeak)
         color = color + dc*32.0*diff*w;
-    /* IRIS_26669_STATELESS_PREVIEW_HIGHLIGHT_PRESENTATION
-     * HAL/user AE remains the sole repeating sensor-exposure owner. This is a frame-local display
-     * shoulder only: no history, histogram feedback, temporal slew, Camera2 request mutation, or
-     * delayed exposure state. It compresses bright preview luminance immediately while preserving
-     * chromaticity, so moving between scenes cannot trigger a second Iris-driven exposure step. */
-    float irisY = max(dot(color.rgb, vec3(0.299, 0.587, 0.114)), 0.0);
-    const float irisPivot = 0.38;
-    if (irisY > irisPivot) {
-        float irisT = clamp((irisY - irisPivot) / max(1.0 - irisPivot, 0.0001), 0.0, 1.0);
-        float irisCompressedT = irisT / (1.0 + 1.10 * irisT);
-        float irisOutY = irisPivot + (1.0 - irisPivot) * irisCompressedT;
-        float irisScale = irisOutY / max(irisY, 0.0001);
-        color.rgb *= irisScale;
-    }
     Output = color;
 }
\ No newline at end of file
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index 290a7b0ebc7df68adec3e8d3bdb269ceb9f2af3a..d7e0e4ed3fb3fb4228aed079671d7790ee8fe11e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -105,16 +105,15 @@ object PhotonMotionMgc1271Bridge {
                 "longExpected=$iris26593LongExpected longPresent=${longFrame != null} " +
                 "normal=${inputImages.size} total=${inputImages.size + (if (shortFrame != null) 1 else 0) + (if (longFrame != null) 1 else 0)}")
 
-            /* IRIS_26669_ISOLATED_CAPTURE_TIME_SHORT_OWNER
-             * Motion may own exactly one shutter-time HIGHLIGHT_SHORT only when the immutable
-             * capture plan requested it. It never enters inputImages/NORMAL temporal ownership;
-             * it is scheduled only as the existing Sabre one-tunnel auxiliary highlight role.
-             * This replaces the stale 26658 blanket prohibition without reviving broad SHORT
-             * temporal accumulation or SHORT color authority. Night keeps its existing contract.
+            /* IRIS_26658_GOOGLE_HDR_BRACKETING_REFERENCE_OWNER
+             * Motion follows the 2021 HDR+ bracketing topology: the timestamp-owned pre-shutter
+             * ZSL frame is the structural/reference exposure and SHADOW_LONG is auxiliary shadow
+             * evidence. Night keeps its exact prior ordering/role policy. The old post-NORMAL
+             * HIGHLIGHT_SHORT repair exposure is forbidden on the Motion active path.
              */
             if (!parameters.irisNightActive) {
-                requireParity(shortFrame == null || iris26593ShortExpected,
-                    "26669 unexpected Motion HIGHLIGHT_SHORT without capture-plan ownership")
+                requireParity(shortFrame == null,
+                    "26658 Motion HIGHLIGHT_SHORT repair survived Google-bracketing conversion")
             }
             val mgcBase = if (parameters.irisNightActive) {
                 inputImages.firstOrNull { it.motionV2FrameRole == ImageFrame.MotionV2FrameRole.NORMAL }
@@ -147,18 +146,10 @@ object PhotonMotionMgc1271Bridge {
                     orderedPhysical += frame to role
                 }
             }
-            if (!parameters.irisNightActive) {
-                shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
-                longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
-            } else {
-                /* Preserve Night's pre-26669 auxiliary ordering byte-for-byte in behavior. */
-                longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
+            longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
+            if (parameters.irisNightActive) {
                 shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
             }
-            PLog.i(TAG, "IRIS_26669_ISOLATED_SHORT_SCHEDULE " +
-                "night=${parameters.irisNightActive} expected=$iris26593ShortExpected " +
-                "present=${shortFrame != null} normalListShort=false " +
-                "scheduledAux=${shortFrame != null} oneTunnelSabre=true")
             parameters.motionV2ReferenceProtectionEv = if (parameters.irisNightActive) {
                 0.0f
             } else {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
index 37cda4b9f935b4df7d0c65f0274c6e5cce91b0aa..6aef76de5279ece5d4b07a48a0ae0380faf9c5ff 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
@@ -85,7 +85,6 @@ import com.particlesdevs.photoncamera.settings.PreferenceKeys;
 import com.particlesdevs.photoncamera.settings.SettingsManager;
 import com.particlesdevs.photoncamera.ui.camera.data.CameraLensData;
 import com.particlesdevs.photoncamera.ui.camera.viewmodel.*;
-import com.particlesdevs.photoncamera.ui.camera.views.IrisManualSliderView;
 import com.particlesdevs.photoncamera.ui.camera.views.viewfinder.GLPreview;
 import com.particlesdevs.photoncamera.ui.camera.views.viewfinder.SurfaceViewOverViewfinder;
 import com.particlesdevs.photoncamera.ui.settings.SettingsActivity;
@@ -912,15 +911,6 @@ public class CameraFragment extends Fragment implements BaseActivity.BackPressed
             Boolean flashAvailable = characteristics.get(CameraCharacteristics.FLASH_INFO_AVAILABLE);
             mCameraUIView.showFlashButton(flashAvailable != null && flashAvailable);
             manualModeConsole.init(activity, characteristics);
-            /* IRIS_26669_SINGLE_MANUAL_CONSOLE_OWNER
-             * Bind the app-owned horizontal slider to this exact Fragment console after the
-             * camera-specific Focus/Shutter/ISO/EV models exist. One console now owns both the
-             * legacy model callbacks and the visible v9 slider presentation.
-             */
-            IrisManualSliderView irisManualSlider = activity.findViewById(R.id.iris_manual_slider);
-            if (irisManualSlider != null) {
-                irisManualSlider.bindManualModeConsole(manualModeConsole);
-            }
             manualModeConsole.onResume();
         }
 
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
index 0e6100fd7bfd78d59b16e20872ed89e6d8c35fd3..3f1e68d8f05eb77ae972ee34837e9bc0c97c3881 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
@@ -321,35 +321,6 @@ public class CameraUIViewImpl implements CameraUIView {
                 }
             }
         }
-        final boolean iris26669ExactValueSliderPalette =
-                root.findViewById(R.id.iris_manual_slider) != null;
-        if (iris26669ExactValueSliderPalette) {
-            /* IRIS_26669_V9_COLLAPSED_MANUAL_OWNER
-             * 26668's XML correctly hid the legacy Auto/numeric labels, but this older styling
-             * owner ran later and forced those TextViews white again. The v9 palette owns only
-             * four icons while collapsed; selected numeric value/AUTO live exclusively inside
-             * IrisManualSliderView when a mode is open. Keep listener IDs untouched.
-             */
-            final String[] labelIds = {
-                    "focus_option_tv", "exposure_option_tv", "iso_option_tv", "ev_option_tv"
-            };
-            for (String name : labelIds) {
-                int id = root.getResources().getIdentifier(name, "id", packageName);
-                if (id == 0) continue;
-                View view = root.findViewById(id);
-                if (!(view instanceof TextView)) continue;
-                TextView label = (TextView) view;
-                label.setText("");
-                label.setTextSize(android.util.TypedValue.COMPLEX_UNIT_SP, 0.0f);
-                label.setTextColor(android.graphics.Color.TRANSPARENT);
-                label.setShadowLayer(0.0f, 0.0f, 0.0f, android.graphics.Color.TRANSPARENT);
-                label.setCompoundDrawableTintList(android.content.res.ColorStateList.valueOf(
-                        android.graphics.Color.WHITE));
-            }
-            Log.i(TAG, "IRIS_26669_V9_COLLAPSED_MANUAL_OWNER legacyText=false iconsOnly=true");
-            return;
-        }
-
         if (knobId != 0) {
             View knob = root.findViewById(knobId);
             if (knob != null) iris26648ApplyManualKnobContrast(knob);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/IrisManualSliderView.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/IrisManualSliderView.java
index a4f191f7c4d40a4b8e66a5289442f57774dc144b..a22e9a6cdd7196254ab9b1049bd91e9aba91a6ef 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/IrisManualSliderView.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/IrisManualSliderView.java
@@ -84,28 +84,12 @@ public final class IrisManualSliderView extends View implements Observer {
     @Override
     protected void onAttachedToWindow() {
         super.onAttachedToWindow();
-        /* IRIS_26669_SINGLE_MANUAL_CONSOLE_OWNER
-         * CameraFragment explicitly binds its already-created console. Never manufacture or fetch
-         * a second singleton here: that was the 26668 reason the visible slider never observed
-         * the manual model selected by the real camera UI.
-         */
-        invalidate();
-    }
-
-    public void bindManualModeConsole(ManualModeConsole owner) {
-        if (knobModel != null) knobModel.deleteObserver(this);
-        knobModel = null;
-        console = null;
-        if (!(owner instanceof ManualModeConsoleImpl)) {
-            Log.e(TAG, "IRIS_26669_MANUAL_CONSOLE_BIND rejected incompatible owner");
-            invalidate();
-            return;
+        ManualModeConsole generic = ManualModeConsoleImpl.getInstance();
+        if (generic instanceof ManualModeConsoleImpl) {
+            console = (ManualModeConsoleImpl) generic;
+            knobModel = console.getKnobModel();
+            knobModel.addObserver(this);
         }
-        console = (ManualModeConsoleImpl) owner;
-        knobModel = console.getKnobModel();
-        knobModel.addObserver(this);
-        lastSelectedIndex = -1;
-        Log.i(TAG, "IRIS_26669_MANUAL_CONSOLE_BIND sameFragmentOwner=true");
         invalidate();
     }
 
diff --git a/app/version.properties b/app/version.properties
index 6610f0f7490f80ef4c8e4fb569e099a1af62ea0c..f72e6d924d9a3710bdc48c4dc5878f77679b4247 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726669
-VERSION_BUILD=26669
+VERSION_NAME=0.9726668
+VERSION_BUILD=26668
