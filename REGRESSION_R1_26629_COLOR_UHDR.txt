PHOTON 26629 R1 PERMANENT/APPLICABLE REGRESSIONS

AUTHORITY / SCOPE
- Authority must be the successful 26628 R3 compiler-tested candidate from Actions run 34643414499, artifact 10281080928, not repository app/src.
- Authority-seeded canonical universe must remain exactly 1713 files.
- Runtime delta must equal the exact 5-path allowlist. No added runtime paths.
- app/build/** and app/.cxx/** never count as runtime source. Repository-only scaffolding absent authority never counts. Unexpected real app/src/** differences fail.

COLOR RESTORATION
- Existing 26628 R3 DNG color solve, full 3D DCP HueSat/Look support, equal-ForwardMatrix validity, common-axis negative gamut handling, and upstream fixed 0.95 restrained presentation remain byte-identical.
- Final restoration runs only after existing fitDisplayGamut() and before sRGB OETF.
- Display-P3 luminance coefficients are exactly 0.22897456 / 0.69173852 / 0.07928691.
- Restore envelope constants are exactly: rise 0.025→0.055; fall 0.13→0.36; neutral 0.035→0.11; strong-color taper 0.38→0.72 with 0.65 taper; maximum requested addition 0.20.
- Restoration is one shared neutral-axis chroma multiplier. Per-channel saturation gain is forbidden.
- Mathematical fixture must preserve linear Display-P3 luminance to <=1e-6 and keep output inside [0,1]. Requested gain must stay [1,1.20]. Bright Y>=0.36 restoration must be unity.
- true2x CPU and embedded GPU publication use the same restoration constants/order.

UHDR
- Completed SDR base remains sole spatial/detail/color/tone authority.
- Motion/true2x gain map remains single-plane scalar Android Gainmap; equal R/G/B ratio/gamma/epsilon metadata are preserved.
- Motion body full-HDR luminance floor is exactly 1.25. It is not baked into SDR pixels.
- Existing higher source headroom may exceed 1.25 within existing safe max; do not multiply existing headroom by another 1.25.
- Motion branch must not rebuild an HDR/SDR quotient that restores an alternate detail rendition.
- Night UHDR Java owner and Night gainmap branch remain unchanged.
- Motion true2x CPU cached/uncached and embedded GPU gain generation must all use the same 1.25 body floor.

INHERITED PERMANENT FAILURES
- Failed 26628 R1 C++ compiler condition `float y=luma(center)` is permanently forbidden; corrected `float lum=luma(center)` declarations remain exactly two.
- Failed 26628 R2 AuxButtons Java Button/TextView mismatch remains forbidden: TextView instanceof/cast required; standalone Button type/import/cast absent.
- Legacy Photon Initial/PostPipeline must not regain Motion/Night color ownership.
- Reconstruction/Motion merge/SHORT/CFA/pink-edge protections remain inherited from successful 26628 R3 unless intentionally replaced.

COMPILERS / FREEZE
- Modified render.glsl, gainmap.glsl, and exact embedded true2x compute shader must pass complete reserved-identifier scan and pinned real glslangValidator 16.5.0 on runtime-expanded source.
- Modified Java/C++ requires real project Kotlin/Java compile plus NDK arm64-v8a and armeabi-v7a before PRE-BUILD proof.
- Frozen compiler-tested candidate must remain byte-identical through full :app:assembleDebug and post-build invariance.
- Full-index patches must pass core.abbrev 7/12/40, fuzz=0 / --index exact preimage, exact forward and exact rollback.
