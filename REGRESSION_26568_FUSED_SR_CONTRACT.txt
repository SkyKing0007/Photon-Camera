26568 permanent fused Super Res performance/detail contract

RUNTIME AUTHORITY
- Base is the exact successful 26567 V1.4 compiled candidate from run 33350919492 / artifact 9743630741.
- 26567 V1.4 authority-seeded build/handoff implementation remains verification-mechanics authority.
- Infrastructure mechanics/order are inherited unchanged; build-specific pins/names and 26568 semantic contracts are the only intended verifier delta.

SUPER RES RGB/CHROMA OWNERSHIP
- Native Sabre Resolve + current VGN remains authoritative for RGB ratios, chroma, highlights and low-frequency structure.
- Direct-CFA true2x evidence contributes only one bounded scalar luminance/detail factor applied equally to R/G/B.
- Fused GPU output is `guideRgb * factor`; direct-CFA RGB ratios are never published.
- The scalar remains bounded to the successful 26567 +/-0.25 EV envelope.
- Weak/unsafe evidence drives factor toward exactly 1.0, preserving Sabre/VGN color.

DETAIL EVIDENCE
- Native Sabre retains the complete accepted NORMAL burst; 15-frame behavior is not reduced by this SR optimization.
- JPEG-only true2x retains up to the two strongest observations in each of four phase classes (maximum eight).
- The strongest/rank-0 observation for each phase is exactly the observation 26567 would have retained; added rank-1 evidence cannot displace it.
- DNG-requested SR bypasses the JPEG cap and retains the complete NORMAL evidence population.

PERFORMANCE / GPU LIFETIME
- Fused guide rendering happens while direct-CFA/phase evidence and Sabre/VGN guide are available, avoiding serialized 50MP guide/phase intermediates on the GPU JPEG path.
- GPU reconstruction uses full-width/even wide bands where supported; tile origins/extents preserve 2x2 phase/detail block boundaries.
- Phase-count telemetry is encoded in otherwise-unused render alpha and accumulated while packing the already-required RGBA16F readback; no second full-frame phase readback is allowed.
- GPU path skips the old second 50MP file-based scalar derivative build. CPU fallback preserves the proven 26567 file-based implementation.
- Final JPEG/JPEG-R encoder streams rendered bands directly to libjpeg and gain JPEG encoding; no full-resolution RGB8 or raw gain scratch files are allowed.

DNG HARD INVARIANT
- Dedicated DNG writers and ImageSaver are byte-identical to 26567.
- Native/vendor protected ownership remains byte-identical.
- DNG-requested true2x preserves a pristine direct-CFA/pre-VGN carrier before fused guide rendering.
- JPEG performance changes must not alter DNG color, metadata, publication or SR ownership.

COLOR / PUBLICATION HARD INVARIANT
- Proven 26567 Camera2/DNG-style JPEG characterization, linear Display-P3 working gamut, calibrated/universal appearance ownership, P3 luminance, Jin sRGB adapter and watermark/P3 publication remain unchanged.
- No second sRGB->P3 conversion or alternate color owner is introduced by SR.
- SR-on physical output remains true 2x dimensions; no silent 12MP fallback is allowed.
