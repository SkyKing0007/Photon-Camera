26570 V1 SURFACE CONTINUITY + TRUE2X PERFORMANCE CONTRACT

Runtime authority: successful 26569 V1.1 compiled candidate only.
Do not use repository app/src as runtime authority.

IQ regressions, permanent:
1. Ceiling-light pink/false-color protection remains active; 26570 must not weaken Sabre rejection globally.
2. A continuous physical surface must not develop patchy cleanup regimes, pepper dots, bands, or denoise islands merely because illumination/reflection changes.
3. Thin dark foliage/branches against bright sky retain center luma authority; opposite-side bright neighbors cannot fill them.
4. Sky chroma cannot cross a strong/one-sided foliage boundary through local median or recursive IIR state.
5. VGN must not manufacture a brighter/cyaner edge envelope than the pre-cleanup center sample.
6. Surface-continuity decision is illumination-normalized; no absolute shadowNeed branch.
7. Legitimate texture/edges remain protected; no global Sabre confidence/rejection relaxation.

True2x performance regressions, permanent:
1. Exact 26569 true2x pixel equations, Display-P3/Jin/watermark behavior, JPEG quality, base 4:4:4 sampling, 1:1 gain-map geometry, and JPEG-R/UHDR packager ownership remain unchanged.
2. Motion no-Jin band size remains 256 rows; Jin path remains 128 rows; bounded worker policy remains max 6 / max 4.
3. No full-frame RGB8 or gain-map scratch is reintroduced.
4. New throughput work is publication scheduling only: two bounded bands, next-band render overlap, concurrent independent base/gain libjpeg compressors, 32-row scanline batches.
5. Base and gain compressors never share a jpeg_compress_struct or FILE.
6. Every worker/render/encode thread is joined before a band buffer is reused or function exits.
7. Timing marker IRIS_26570_TRUE2X_ENCODER_TIMING must expose render/baseJpeg/gainJpeg/overlapPipeline/stream timing.
8. Existing Java IRIS_26569_TRUE2X_PACKAGE_TIMING remains unchanged to separate native encode from JPEG-R packaging.

Protected architecture:
- Sabre merge/rejection equations: unchanged.
- Direct-CFA/SR reconstruction and scalar detail ownership: unchanged.
- exposure/frame policy: unchanged.
- CaptureController/HdrxProcessor: unchanged.
- DNG ownership and bytes: unchanged.
- JPEG-R/UHDR packager source: unchanged.

Required active-source markers:
- IRIS_26570_SURFACE_CONTINUITY_ILLUMINATION_INDEPENDENT
- IRIS_26570_ONE_SIDED_EDGE_LUMA_AUTHORITY
- IRIS_26570_CROSS_EDGE_CHROMA_EXCLUSION
- IRIS_26570_IIR_STRONG_EDGE_STATE_RESET
- IRIS_26570_TRUE2X_RENDER_ENCODE_PIPELINE
- IRIS_26570_TRUE2X_ENCODER_TIMING
