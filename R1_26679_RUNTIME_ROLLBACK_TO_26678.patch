diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index fd9a5fa5c066852b2f6d9bc3e19bfc00b20b9a06..8c1f7ad46c0e7b755702b8dc4095d0043b4d0d64 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -1620,13 +1620,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private int mMotion26678StableDriftFrames = 0;
     private volatile boolean mMotion26678FlickerActive = false;
     private volatile int mMotion26678ActiveHarmonic = 0;
-    /* IRIS_26679_FLICKER_STATE_HYSTERESIS
-     * 26678 could enter/exit on adjacent RAWs when one evidence frame narrowly missed coherence.
-     * Require a stronger entry and four consecutive unsupported frames before exit. This changes
-     * only observer state; shader math, HAL AE, capture exposure, and stored evidence stay 26678. */
-    private static final int MOTION_26679_FLICKER_ENTER_DRIFT_FRAMES = 3;
-    private static final int MOTION_26679_FLICKER_EXIT_MISS_FRAMES = 4;
-    private int mMotion26679FlickerMissFrames = 0;
 
     /* IRIS_26608_UNIVERSAL_HDR_DYNAMIC_RANGE_TRIGGER
      * SHORT is an HDR exposure, not only a near-sensor-white rescue. The live RAW sampler keeps
@@ -1890,7 +1883,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mMotion26678StableDriftFrames = 0;
         mMotion26678FlickerActive = false;
         mMotion26678ActiveHarmonic = 0;
-        mMotion26679FlickerMissFrames = 0;
     }
 
     private void motion26678StoreEvidenceLocked(long timestampNs, int harmonic,
@@ -2098,37 +2090,11 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     mMotion26678PreviousAmplitude = 0.0f;
                 }
                 boolean wasActive = mMotion26678FlickerActive;
-                final int activeHarmonicBefore = mMotion26678ActiveHarmonic;
-                final boolean supportsActive = coherent && activeHarmonicBefore > 0
-                        && bestHarmonic == activeHarmonicBefore;
-                if (!wasActive) {
-                    nowActive = coherent
-                            && mMotion26678StableDriftFrames >= MOTION_26679_FLICKER_ENTER_DRIFT_FRAMES;
-                    if (nowActive) {
-                        mMotion26678ActiveHarmonic = bestHarmonic;
-                        mMotion26679FlickerMissFrames = 0;
-                    } else {
-                        mMotion26678ActiveHarmonic = 0;
-                        mMotion26679FlickerMissFrames = 0;
-                    }
-                } else {
-                    if (supportsActive) {
-                        mMotion26679FlickerMissFrames = 0;
-                    } else {
-                        mMotion26679FlickerMissFrames++;
-                    }
-                    nowActive = mMotion26679FlickerMissFrames
-                            < MOTION_26679_FLICKER_EXIT_MISS_FRAMES;
-                    if (!nowActive) {
-                        mMotion26678ActiveHarmonic = 0;
-                        mMotion26679FlickerMissFrames = 0;
-                    }
-                }
+                nowActive = coherent && mMotion26678StableDriftFrames >= 2;
                 mMotion26678FlickerActive = nowActive;
-                strength = nowActive && coherent
-                        && bestHarmonic == mMotion26678ActiveHarmonic
-                        ? Math.max(0.0f, Math.min(1.0f,
-                                (mMotion26678StableDriftFrames - 1) / 3.0f))
+                mMotion26678ActiveHarmonic = nowActive ? bestHarmonic : 0;
+                strength = nowActive
+                        ? Math.min(1.0f, (mMotion26678StableDriftFrames - 1) / 3.0f)
                         : 0.0f;
                 motion26678StoreEvidenceLocked(image.getTimestamp(), bestHarmonic,
                         coherentBlocks, bestA, bestB, bestAmplitude, strength);
@@ -2140,7 +2106,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                         + " harmonic=" + bestHarmonic
                         + " amplitude=" + bestAmplitude
                         + " coherentBlocks=" + coherentBlocks
-                        + " hysteresisMissFrames=" + mMotion26679FlickerMissFrames
                         + " exposureNs=" + exposureNs
                         + " iso=" + iso
                         + " halAeUnchanged=true");
@@ -3424,63 +3389,29 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         rebuildPreviewBuilder();
     }
 
-    /* IRIS_26679_SESSION_SAFE_PREVIEW_MUTATION
-     * Camera2 session teardown is asynchronous. Snapshot and validate all mutable owners before
-     * sending AF/AE requests so lifecycle overlap is a normal false return, never an exception
-     * path. Existing callers may ignore the return; TouchFocus consumes it for deferred reset. */
-    public boolean iris26679PreviewSessionReadyForControl() {
-        return mCameraDevice != null && mCaptureSession != null && mPreviewRequestBuilder != null
-                && mBackgroundHandler != null && mIris26548SessionConfigured
-                && mIris26548RepeatingSubmitted;
-    }
-
-    public boolean rebuildPreviewBuilder() {
-        if (burst) return false;
-        final CameraCaptureSession session = mCaptureSession;
-        final CaptureRequest.Builder builder = mPreviewRequestBuilder;
-        final Handler handler = mBackgroundHandler;
-        if (session == null || builder == null || handler == null) {
-            Log.d(TAG, "IRIS_26679_PREVIEW_REBUILD_SKIPPED repeating=true session="
-                    + (session != null) + " builder=" + (builder != null)
-                    + " handler=" + (handler != null));
-            return false;
-        }
+    public void rebuildPreviewBuilder() {
+        if(burst) return;
         try {
-//            session.stopRepeating();
+//            mCaptureSession.stopRepeating();
             iris26524ApplyZoomToPreviewBuilder();
-            session.setRepeatingRequest(mPreviewInputRequest = builder.build(), mCaptureCallback, handler);
-            return true;
-        } catch (IllegalStateException | IllegalArgumentException e) {
+            mCaptureSession.setRepeatingRequest(mPreviewInputRequest = mPreviewRequestBuilder.build(), mCaptureCallback, mBackgroundHandler);
+        } catch (IllegalStateException | IllegalArgumentException | NullPointerException e) {
             Logger.warnShort(TAG, "Cannot rebuildPreviewBuilder()!", e);
-            return false;
         } catch (CameraAccessException e) {
             Log.e(TAG, Log.getStackTraceString(e));
-            return false;
         }
     }
 
-    public boolean rebuildPreviewBuilderOneShot() {
-        if (burst) return false;
-        final CameraCaptureSession session = mCaptureSession;
-        final CaptureRequest.Builder builder = mPreviewRequestBuilder;
-        final Handler handler = mBackgroundHandler;
-        if (session == null || builder == null || handler == null) {
-            Log.d(TAG, "IRIS_26679_PREVIEW_REBUILD_SKIPPED oneShot=true session="
-                    + (session != null) + " builder=" + (builder != null)
-                    + " handler=" + (handler != null));
-            return false;
-        }
+    public void rebuildPreviewBuilderOneShot() {
+        if(burst) return;
         try {
-            Log.d(TAG, "rebuildPreviewBuilderOneShot: " + session + " " + builder + " " + mCaptureCallback + " " + handler);
+            Log.d(TAG, "rebuildPreviewBuilderOneShot: " + mCaptureSession + " " + mPreviewRequestBuilder + " " + mCaptureCallback + " " + mBackgroundHandler);
             iris26524ApplyZoomToPreviewBuilder();
-            session.capture(builder.build(), mCaptureCallback, handler);
-            return true;
-        } catch (IllegalStateException | IllegalArgumentException e) {
+            mCaptureSession.capture(mPreviewRequestBuilder.build(), mCaptureCallback, mBackgroundHandler);
+        } catch (IllegalStateException | IllegalArgumentException | NullPointerException e) {
             Logger.warnShort(TAG, "Cannot rebuildPreviewBuilderOneShot()!", e);
-            return false;
         } catch (CameraAccessException e) {
             Log.e(TAG, Log.getStackTraceString(e));
-            return false;
         }
     }
 
@@ -4086,27 +4017,25 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     /**
      * Lock the focus as the first step for a still image capture.
      */
-    private boolean lockFocus() {
-        if (burst) return false;
-        final CameraCaptureSession session = mCaptureSession;
-        final CaptureRequest.Builder builder = mPreviewRequestBuilder;
-        final Handler handler = mBackgroundHandler;
-        if (session == null || builder == null || handler == null) {
-            Log.w(TAG, "IRIS_26679_LOCK_FOCUS_REJECTED session=" + (session != null)
-                    + " builder=" + (builder != null) + " handler=" + (handler != null));
-            mState = STATE_PREVIEW;
-            return false;
+    private void lockFocus() {
+        if(burst) return;
+        if (mPreviewRequestBuilder == null || mCaptureSession == null) {
+            Log.w(TAG, "lockFocus(): camera not ready (builder=" + mPreviewRequestBuilder + " session=" + mCaptureSession + ")");
+            return;
         }
         startTimerLocked();
-        builder.set(CaptureRequest.CONTROL_AF_TRIGGER, CameraMetadata.CONTROL_AF_TRIGGER_START);
+        // This is how to tell the camera to lock focus.
+        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AF_TRIGGER,
+                CameraMetadata.CONTROL_AF_TRIGGER_START);
+        // Tell #mCaptureCallback to wait for the lock.
         mState = STATE_WAITING_LOCK;
         try {
-            session.setRepeatingRequest(builder.build(), mCaptureCallback, handler);
-            return true;
-        } catch (CameraAccessException | IllegalStateException | IllegalArgumentException e) {
-            Log.e(TAG, "IRIS_26679_LOCK_FOCUS_REQUEST_FAILED", e);
-            mState = STATE_PREVIEW;
-            return false;
+            mCaptureSession.setRepeatingRequest(mPreviewRequestBuilder.build(), mCaptureCallback,
+                    mBackgroundHandler);
+        } catch (CameraAccessException e) {
+            Log.e(TAG, "Failed to start camera preview because it couldn't access camera", e);
+        } catch (IllegalStateException e) {
+            Log.e(TAG, "Failed to start camera preview.", e);
         }
     }
 
@@ -4115,28 +4044,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
      * we get a response in {@link #mCaptureCallback} from {@link #lockFocus()}.
      */
     private void runPreCaptureSequence() {
-        if (burst) return;
-        final CameraCaptureSession session = mCaptureSession;
-        final CaptureRequest.Builder builder = mPreviewRequestBuilder;
-        final Handler handler = mBackgroundHandler;
-        if (session == null || builder == null || handler == null) {
-            Log.w(TAG, "IRIS_26679_PRECAPTURE_REQUEST_REJECTED session=" + (session != null)
-                    + " builder=" + (builder != null) + " handler=" + (handler != null));
-            mState = STATE_PREVIEW;
-            iris26679RejectShutter("AE_PRECAPTURE_SESSION_UNAVAILABLE", "Camera is still preparing");
-            return;
-        }
+        if(burst) return;
         try {
             // This is how to tell the camera to trigger.
-            builder.set(CaptureRequest.CONTROL_AE_PRECAPTURE_TRIGGER,
+            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_PRECAPTURE_TRIGGER,
                     CaptureRequest.CONTROL_AE_PRECAPTURE_TRIGGER_START);
             // Tell #mCaptureCallback to wait for the precapture sequence to be set.
             mState = STATE_WAITING_PRECAPTURE;
-            session.capture(builder.build(), mCaptureCallback, handler);
-        } catch (CameraAccessException | IllegalStateException | IllegalArgumentException e) {
-            Log.e(TAG, "IRIS_26679_PRECAPTURE_REQUEST_FAILED " + Log.getStackTraceString(e));
-            mState = STATE_PREVIEW;
-            iris26679RejectShutter("AE_PRECAPTURE_SESSION_UNAVAILABLE", "Camera is still preparing");
+            mCaptureSession.capture(mPreviewRequestBuilder.build(), mCaptureCallback,
+                    mBackgroundHandler);
+        } catch (CameraAccessException | IllegalStateException e) {
+            Log.e(TAG, Log.getStackTraceString(e));
         }
     }
 
@@ -4475,12 +4393,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                     + mPreviewInputRequest.get(CaptureRequest.CONTROL_AE_MODE) + " fps="
                                     + mPreviewInputRequest.get(CaptureRequest.CONTROL_AE_TARGET_FPS_RANGE));
                             unlockFocus();
-                            /* IRIS_26679_SESSION_READY_REPLAY_POINT
-                             * Session + repeating request now exist. Replay one deferred AF reset and
-                             * wake a one-deep shutter intent; both still require their own stronger
-                             * readiness checks before they can mutate/capture. */
-                            if (mTouchFocus != null) mTouchFocus.onPreviewSessionReady();
-                            scheduleMotion26598DeferredShutterReplay("session_configured");
                         }
                     } catch (Exception e) {
                         Log.e(TAG, Log.getStackTraceString(e));
@@ -4626,31 +4538,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
     }
 
-    /* IRIS_26679_SHUTTER_TERMINAL_REJECTION
-     * onTimerFinished() disables the shutter before entering this controller. Any path that
-     * terminates before onCaptureStillPictureStarted() must therefore release the UI explicitly.
-     * Deferred paths do not call this until their bounded retry becomes terminal. */
-    private void iris26679RejectShutter(String reason, String userMessage) {
-        Log.w(TAG, "IRIS_26679_SHUTTER_REJECTED reason=" + reason
-                + " generation=" + mIris26548CameraHealthGeneration
-                + " session=" + (mCaptureSession != null)
-                + " builder=" + (mPreviewRequestBuilder != null)
-                + " repeating=" + mIris26548RepeatingSubmitted
-                + " firstResult=" + mIris26548FirstCaptureResult);
-        if (userMessage != null && !userMessage.isEmpty()) showToast(userMessage);
-        try {
-            cameraEventsListener.onCaptureStillPictureRejected(reason);
-        } catch (Throwable uiError) {
-            Log.e(TAG, "IRIS_26679_SHUTTER_REJECT_UI_FAILURE " + Log.getStackTraceString(uiError));
-        }
-    }
-
-    private boolean iris26679MotionSessionReadyForShutter() {
-        return mCameraDevice != null && mCaptureSession != null && mPreviewRequestBuilder != null
-                && mBackgroundHandler != null && mIris26548SessionConfigured
-                && mIris26548RepeatingSubmitted && mIris26548FirstCaptureResult;
-    }
-
     /**
      * Initiate a still image capture.
      */
@@ -4660,76 +4547,43 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && (iris26555Mode == CameraMode.MOTION || iris26555Mode == CameraMode.NIGHT)) {
             Log.w(TAG, "IRIS_26555_RAW_MODE_UNAVAILABLE mode=" + iris26555Mode
                     + " previewAlive=true fakeYuvFallback=false");
-            iris26679RejectShutter("RAW_MODE_UNAVAILABLE",
-                    "RAW capture is not supported by this camera");
+            showToast("RAW capture is not supported by this camera");
             return;
         }
-
-        /* IRIS_26679_SCENE_INDEPENDENT_SHUTTER_ADMISSION
-         * Lighting/content never decides whether the UI can become stranded. Motion shutter
-         * intent is held one-deep while the same camera/mode regains a configured repeating
-         * session and RAW ring; terminal timeout/camera-change releases the UI explicitly. */
-        if (iris26555Mode == CameraMode.MOTION) {
-            if (!iris26679MotionSessionReadyForShutter()) {
-                latchMotion26598DeferredShutter("CAMERA_SESSION_REBUILD");
-                scheduleMotion26598DeferredShutterReplay("camera_session_rebuild");
-                Log.w(TAG, "IRIS_26679_MOTION_SHUTTER_DEFERRED sessionReady=false"
-                        + " generation=" + mIris26548CameraHealthGeneration);
-                return;
-            }
-            if (mIris26555RawStreamAvailable
-                    && (iris26555PreviewOnlyRecoveryActive() || !iris26555MotionZslReady())) {
-                latchMotion26598DeferredShutter("RAW_BUFFER_PREPARING");
-                scheduleMotion26598DeferredShutterReplay("raw_buffer_preparing");
-                Log.w(TAG, "IRIS_26679_MOTION_SHUTTER_DEFERRED rawReady=false recoveryStage="
-                        + mIris26555PreviewRecoveryStage + " firstZslRaw=" + mIris26548FirstZslRaw
-                        + " zslBuffered=" + iris26548MotionZslHasFrames());
-                return;
-            }
+        if (iris26555Mode == CameraMode.MOTION && mIris26555RawStreamAvailable
+                && (iris26555PreviewOnlyRecoveryActive() || !iris26555MotionZslReady())) {
+            Log.w(TAG, "IRIS_26555_MOTION_CAPTURE_NOT_READY recoveryStage="
+                    + mIris26555PreviewRecoveryStage + " firstZslRaw=" + mIris26548FirstZslRaw
+                    + " zslBuffered=" + iris26548MotionZslHasFrames());
+            showToast("Motion RAW buffer is still preparing");
+            return;
         }
-
         /* IRIS_26547_NIGHT_REENTRY_GUARD
          * UI state is not a sufficient serialization primitive. Reject a second Night shutter at
          * the controller boundary while the prior Night capture/reconstruction still owns RAW/GL.
          */
-        if (iris26555Mode == CameraMode.NIGHT && isProcessing) {
+        if (PhotonCamera.getSettings().selectedMode == CameraMode.NIGHT && isProcessing) {
             Log.w(TAG, "IRIS_26547_NIGHT_REENTRY_REJECTED processing=true");
-            iris26679RejectShutter("NIGHT_REENTRY_PROCESSING", null);
             return;
         }
         if (mPreviewRequestBuilder == null || mCaptureSession == null) {
-            Log.w(TAG, "takePicture(): camera not ready, rejecting shutter press safely");
-            iris26679RejectShutter("PREVIEW_SESSION_UNAVAILABLE", "Camera is still preparing");
+            Log.w(TAG, "takePicture(): camera not ready, ignoring shutter press");
             return;
         }
         if (isZslMode()) {
             captureStillPicture();
             return;
         }
-        if (mCameraAfModes.length > 1) {
-            if (!lockFocus()) {
-                iris26679RejectShutter("AF_LOCK_SESSION_UNAVAILABLE", "Camera is still preparing");
-            }
-        } else {
-            final CameraCaptureSession session = mCaptureSession;
-            final CaptureRequest.Builder builder = mPreviewRequestBuilder;
-            final Handler handler = mBackgroundHandler;
-            if (session == null || builder == null || handler == null) {
-                mState = STATE_PREVIEW;
-                iris26679RejectShutter("NON_ZSL_SESSION_UNAVAILABLE", "Camera is still preparing");
-                return;
-            }
+        if (mCameraAfModes.length > 1) lockFocus();
+        else {
             try {
                 mState = STATE_WAITING_NON_PRECAPTURE;
-                session.setRepeatingRequest(builder.build(), mCaptureCallback, handler);
+                mCaptureSession.setRepeatingRequest(mPreviewRequestBuilder.build(), mCaptureCallback,
+                        mBackgroundHandler);
             } catch (CameraAccessException e) {
                 Log.e(TAG, "Failed to start camera preview because it couldn't access camera", e);
-                mState = STATE_PREVIEW;
-                iris26679RejectShutter("NON_ZSL_CAMERA_ACCESS", "Camera is temporarily unavailable");
-            } catch (IllegalStateException | IllegalArgumentException e) {
+            } catch (IllegalStateException e) {
                 Log.e(TAG, "Failed to start camera preview.", e);
-                mState = STATE_PREVIEW;
-                iris26679RejectShutter("NON_ZSL_SESSION_CLOSED", "Camera is temporarily unavailable");
             }
         }
     }
@@ -6991,17 +6845,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             new java.util.concurrent.atomic.AtomicBoolean(false);
     private volatile long mMotion26598DeferredGeneration = -1L;
     private volatile String mMotion26598DeferredPhysicalId = "";
-    private volatile String mMotion26679DeferredCameraId = "";
     private volatile long mMotion26598DeferredRequestedMs = 0L;
-    /* IRIS_26679_SESSION_REBUILD_DEFERRED_SHUTTER
-     * Busy-batch deferrals keep the proven same-generation rule. Only a shutter explicitly
-     * deferred because the camera session/RAW ring is rebuilding may rebind to a newer health
-     * generation, and only while camera mode + physical route are unchanged. */
-    private volatile boolean mMotion26679DeferredReadinessOwner = false;
     private final ArrayDeque<Long> mMotion26598RetiredNormalTopUpTimestamps = new ArrayDeque<>();
     private static final int MOTION_26598_RETIRED_TOPUP_TIMESTAMP_LIMIT = 64;
     private static final long MOTION_26598_DEFERRED_RING_READY_WAIT_MS = 2000L;
-    private static final long MOTION_26679_DEFERRED_CAMERA_READY_WAIT_MS = 4000L;
 
     private void clearMotion26505LongTicket(
             Motion26505LongTicket ticket, String reason) {
@@ -7155,120 +7002,59 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if (first) {
             mMotion26598DeferredGeneration = mIris26548CameraHealthGeneration;
             mMotion26598DeferredPhysicalId = physicalID == null ? "" : physicalID;
-            String configuredCameraId = PhotonCamera.getSettings().mCameraID;
-            mMotion26679DeferredCameraId = configuredCameraId == null ? "" : configuredCameraId;
             mMotion26598DeferredRequestedMs = android.os.SystemClock.elapsedRealtime();
-            mMotion26679DeferredReadinessOwner = "CAMERA_SESSION_REBUILD".equals(busyReason)
-                    || "RAW_BUFFER_PREPARING".equals(busyReason);
         }
         Log.i(TAG, "IRIS_26598_DEFERRED_SHUTTER_LATCHED first=" + first
                 + " collapsed=" + (!first)
                 + " busyReason=" + busyReason
-                + " readinessOwner=" + mMotion26679DeferredReadinessOwner
                 + " generation=" + mMotion26598DeferredGeneration
                 + " physicalId=" + mMotion26598DeferredPhysicalId
-                + " cameraId=" + mMotion26679DeferredCameraId
                 + " inFlight=" + mMotion26486InFlightBatches.get()
                 + " acquiring=" + mZslCapturing);
     }
 
     private void scheduleMotion26598DeferredShutterReplay(String source) {
-        if (!mMotion26598DeferredShutter.get()) return;
-        final Handler handler = mBackgroundHandler;
-        if (handler == null) {
-            if (mMotion26598DeferredShutter.compareAndSet(true, false)) {
-                mMotion26679DeferredReadinessOwner = false;
-                iris26679RejectShutter("DEFERRED_NO_BACKGROUND_HANDLER",
-                        "Camera is temporarily unavailable");
-            }
-            return;
-        }
+        if (!mMotion26598DeferredShutter.get() || mBackgroundHandler == null) return;
         final long replayStartMs = android.os.SystemClock.elapsedRealtime();
-        handler.post(() -> replayMotion26598DeferredShutter(source, replayStartMs));
+        mBackgroundHandler.post(() -> replayMotion26598DeferredShutter(source, replayStartMs));
     }
 
     private void replayMotion26598DeferredShutter(String source, long replayStartMs) {
         if (!mMotion26598DeferredShutter.get()) return;
         if (mZslCapturing || mMotion26486InFlightBatches.get() > 0) return;
-
-        final long now = android.os.SystemClock.elapsedRealtime();
-        final long waitedSincePress = Math.max(0L, now - mMotion26598DeferredRequestedMs);
         boolean sameGeneration = mMotion26598DeferredGeneration == mIris26548CameraHealthGeneration;
-        final boolean samePhysical = Objects.equals(mMotion26598DeferredPhysicalId,
+        boolean samePhysical = Objects.equals(mMotion26598DeferredPhysicalId,
                 physicalID == null ? "" : physicalID);
-        final String configuredCameraId = PhotonCamera.getSettings().mCameraID == null
-                ? "" : PhotonCamera.getSettings().mCameraID;
-        final boolean sameCamera = Objects.equals(mMotion26679DeferredCameraId, configuredCameraId);
-        final boolean sameMode = PhotonCamera.getSettings().selectedMode == CameraMode.MOTION;
-
-        if (!sameMode || !sameCamera || !samePhysical || !mIris26555RawStreamAvailable) {
+        boolean sameMode = PhotonCamera.getSettings().selectedMode == CameraMode.MOTION;
+        if (!sameGeneration || !samePhysical || !sameMode || mCameraDevice == null
+                || mCaptureSession == null || !mIris26555RawStreamAvailable) {
             if (mMotion26598DeferredShutter.compareAndSet(true, false)) {
-                mMotion26679DeferredReadinessOwner = false;
-                Log.w(TAG, "IRIS_26679_DEFERRED_SHUTTER_CANCELED source=" + source
-                        + " samePhysical=" + samePhysical + " sameCamera=" + sameCamera
-                        + " sameMode=" + sameMode + " rawAvailable=" + mIris26555RawStreamAvailable);
-                iris26679RejectShutter("DEFERRED_CAMERA_ROUTE_CHANGED",
-                        "Queued Motion capture canceled after camera change");
-            }
-            return;
-        }
-
-        if (!sameGeneration) {
-            if (mMotion26679DeferredReadinessOwner
-                    && waitedSincePress < MOTION_26679_DEFERRED_CAMERA_READY_WAIT_MS) {
-                Log.i(TAG, "IRIS_26679_DEFERRED_SHUTTER_GENERATION_REBOUND old="
-                        + mMotion26598DeferredGeneration + " new=" + mIris26548CameraHealthGeneration
-                        + " physicalId=" + mMotion26598DeferredPhysicalId);
-                mMotion26598DeferredGeneration = mIris26548CameraHealthGeneration;
-                sameGeneration = true;
-            } else {
-                if (mMotion26598DeferredShutter.compareAndSet(true, false)) {
-                    mMotion26679DeferredReadinessOwner = false;
-                    iris26679RejectShutter("DEFERRED_GENERATION_CHANGED",
-                            "Queued Motion capture canceled after camera change");
-                }
-                return;
-            }
-        }
-
-        final boolean sessionReady = iris26679MotionSessionReadyForShutter();
-        if (!sessionReady) {
-            if (mMotion26679DeferredReadinessOwner
-                    && waitedSincePress < MOTION_26679_DEFERRED_CAMERA_READY_WAIT_MS
-                    && mBackgroundHandler != null) {
-                mBackgroundHandler.postDelayed(
-                        () -> replayMotion26598DeferredShutter(source, replayStartMs), 50L);
-            } else if (mMotion26598DeferredShutter.compareAndSet(true, false)) {
-                mMotion26679DeferredReadinessOwner = false;
-                Log.w(TAG, "IRIS_26679_DEFERRED_SHUTTER_SESSION_TIMEOUT source=" + source
-                        + " waitedSincePressMs=" + waitedSincePress);
-                iris26679RejectShutter("DEFERRED_SESSION_TIMEOUT",
-                        "Camera is still preparing");
+                Log.w(TAG, "IRIS_26598_DEFERRED_SHUTTER_CANCELED source=" + source
+                        + " sameGeneration=" + sameGeneration
+                        + " samePhysical=" + samePhysical
+                        + " sameMode=" + sameMode
+                        + " cameraDevice=" + (mCameraDevice != null)
+                        + " captureSession=" + (mCaptureSession != null));
+                showToast("Queued Motion capture canceled after camera change");
             }
             return;
         }
-
         if (iris26555PreviewOnlyRecoveryActive() || !iris26555MotionZslReady()) {
-            final long maxWait = mMotion26679DeferredReadinessOwner
-                    ? MOTION_26679_DEFERRED_CAMERA_READY_WAIT_MS
-                    : MOTION_26598_DEFERRED_RING_READY_WAIT_MS;
-            if (waitedSincePress < maxWait && mBackgroundHandler != null) {
+            long waited = android.os.SystemClock.elapsedRealtime() - replayStartMs;
+            if (waited < MOTION_26598_DEFERRED_RING_READY_WAIT_MS && mBackgroundHandler != null) {
                 mBackgroundHandler.postDelayed(
                         () -> replayMotion26598DeferredShutter(source, replayStartMs), 50L);
             } else if (mMotion26598DeferredShutter.compareAndSet(true, false)) {
-                mMotion26679DeferredReadinessOwner = false;
                 Log.w(TAG, "IRIS_26598_DEFERRED_SHUTTER_BUFFER_TIMEOUT source=" + source
-                        + " waitedSincePressMs=" + waitedSincePress);
-                iris26679RejectShutter("DEFERRED_RAW_BUFFER_TIMEOUT",
-                        "Motion RAW buffer is still preparing");
+                        + " waitedMs=" + waited);
+                showToast("Motion RAW buffer is still preparing");
             }
             return;
         }
-
         if (!mMotion26598DeferredShutter.compareAndSet(true, false)) return;
-        mMotion26679DeferredReadinessOwner = false;
         Log.i(TAG, "IRIS_26598_DEFERRED_SHUTTER_REPLAY source=" + source
-                + " waitedSincePressMs=" + waitedSincePress
+                + " waitedSincePressMs="
+                + (android.os.SystemClock.elapsedRealtime() - mMotion26598DeferredRequestedMs)
                 + " generation=" + mIris26548CameraHealthGeneration
                 + " exactNormalShortLongBudget=true");
         captureStillPicture();
@@ -7677,17 +7463,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             mMotion26676ShutterRetryScheduled = false;
             Log.w(TAG, "IRIS_26676_CAPTURE_GENERATION_GUARD timeoutMs=" + waitedMs
                     + " staleNormalCapturePrevented=true");
-            iris26679RejectShutter("HDR_PROTECTION_SETTLE_TIMEOUT",
-                    "HDR exposure is still settling");
+            showToast("HDR exposure is still settling");
             return true;
         }
-        if (!mMotion26676ShutterRetryScheduled) {
-            if (mBackgroundHandler == null) {
-                mMotion26676ShutterWaitStartMs = 0L;
-                iris26679RejectShutter("HDR_PROTECTION_NO_BACKGROUND_HANDLER",
-                        "Camera is temporarily unavailable");
-                return true;
-            }
+        if (!mMotion26676ShutterRetryScheduled && mBackgroundHandler != null) {
             mMotion26676ShutterRetryScheduled = true;
             mBackgroundHandler.postDelayed(() -> {
                 mMotion26676ShutterRetryScheduled = false;
@@ -7735,8 +7514,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     "IRIS_26593_MOTION_NOT_READY",
                     "totalTarget=" + iris26593TotalTarget
                             + " sessionReady=false ringRequirement=0");
-            latchMotion26598DeferredShutter("CAMERA_SESSION_REBUILD");
-            scheduleMotion26598DeferredShutterReplay("trigger_zsl_session_not_ready");
+            showToast("Motion capture is not ready");
             return;
         }
 
@@ -8923,8 +8701,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && (iris26555Mode == CameraMode.MOTION || iris26555Mode == CameraMode.NIGHT)) {
             Log.w(TAG, "IRIS_26555_RAW_MODE_UNAVAILABLE_INTERNAL mode=" + iris26555Mode
                     + " captureRejected=true");
-            iris26679RejectShutter("RAW_MODE_UNAVAILABLE_INTERNAL",
-                    "RAW capture is not supported by this camera");
+            showToast("RAW capture is not supported by this camera");
             return;
         }
         if (iris26555Mode == CameraMode.MOTION
@@ -8936,48 +8713,26 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if (iris26555Mode == CameraMode.MOTION && mIris26555RawStreamAvailable
                 && (iris26555PreviewOnlyRecoveryActive() || !iris26555MotionZslReady())) {
             Log.w(TAG, "IRIS_26555_MOTION_CAPTURE_NOT_READY_INTERNAL recoveryStage="
-                    + mIris26555PreviewRecoveryStage + " captureDeferred=true");
-            latchMotion26598DeferredShutter("RAW_BUFFER_PREPARING");
-            scheduleMotion26598DeferredShutterReplay("capture_still_raw_not_ready");
+                    + mIris26555PreviewRecoveryStage + " captureRejected=true");
+            showToast("Motion RAW buffer is still preparing");
             return;
         }
         if (iris26555StillCaptureSurfaceOrNull() == null) {
             Log.w(TAG, "IRIS_26555_STILL_TARGET_UNAVAILABLE mode=" + iris26555Mode
                     + " captureRejected=true previewAlive=true");
-            iris26679RejectShutter("STILL_TARGET_UNAVAILABLE",
-                    "Still capture is not supported by this camera");
-            return;
-        }
-        if (!isZslMode() && (mCaptureSession == null || mPreviewRequestBuilder == null
-                || mBackgroundHandler == null)) {
-            Log.w(TAG, "IRIS_26679_NON_ZSL_CAPTURE_ENTRY_NOT_READY session="
-                    + (mCaptureSession != null) + " builder=" + (mPreviewRequestBuilder != null)
-                    + " handler=" + (mBackgroundHandler != null));
-            mState = STATE_PREVIEW;
-            iris26679RejectShutter("NON_ZSL_CAPTURE_ENTRY_NOT_READY",
-                    "Camera is still preparing");
+            showToast("Still capture is not supported by this camera");
             return;
         }
         boolean iris26547NightRequested = false;
         boolean iris26553NightDispatchReserved = false;
-        boolean iris26679CaptureUiStarted = false;
         try {
             iris26547NightRequested = PhotonCamera.getSettings().selectedMode == CameraMode.NIGHT;
             if (iris26547NightRequested) {
-                if (!reserveIrisNight26553ShutterDispatch()) {
-                    iris26679RejectShutter("NIGHT_DISPATCH_BUSY", null);
-                    return;
-                }
+                if (!reserveIrisNight26553ShutterDispatch()) return;
                 iris26553NightDispatchReserved = true;
             }
             if (null == mCameraDevice) {
-                if (iris26555Mode == CameraMode.MOTION) {
-                    latchMotion26598DeferredShutter("CAMERA_SESSION_REBUILD");
-                    scheduleMotion26598DeferredShutterReplay("capture_still_device_not_ready");
-                } else {
-                    iris26679RejectShutter("CAMERA_DEVICE_UNAVAILABLE",
-                            "Camera is temporarily unavailable");
-                }
+                if (iris26555Mode == CameraMode.MOTION) showToast("Motion camera is not ready");
                 return;
             }
             if (isZslMode()) {
@@ -9228,7 +8983,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             final int[] maxFrameCount = {frameCount};
 
             cameraEventsListener.onCaptureStillPictureStarted("CaptureStarted!");
-            iris26679CaptureUiStarted = true;
             mMeasuredFrameCnt = 0;
 
             cameraEventsListener.onBurstPrepared(null);
@@ -9438,59 +9192,31 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 }
             };
             //mCaptureSession.setRepeatingBurst(captures, CaptureCallback, null);
-            final CameraCaptureSession iris26679SubmissionSession = mCaptureSession;
-            final Handler iris26679SubmissionHandler = mBackgroundHandler;
-            if (!isDualSession
-                    && (iris26679SubmissionSession == null || iris26679SubmissionHandler == null)) {
-                burst = false;
-                mState = STATE_PREVIEW;
-                try { PhotonCamera.getGyro().CompleteSequence(); } catch (Throwable ignored) {}
-                Log.w(TAG, "IRIS_26679_CAPTURE_SUBMISSION_NOT_READY session="
-                        + (iris26679SubmissionSession != null) + " handler="
-                        + (iris26679SubmissionHandler != null));
-                if (iris26679CaptureUiStarted) {
-                    cameraEventsListener.onProcessingError(
-                            "Camera capture session changed; please try again");
-                } else {
-                    iris26679RejectShutter("CAPTURE_SUBMISSION_NOT_READY",
-                            "Camera is temporarily unavailable");
-                }
-                return;
-            }
             burst = true;
             if (iris26533CaptureMode != CameraMode.NIGHT) Camera2ApiAutoFix.ApplyBurst();
             else Log.i(TAG, "IRIS_26540_NIGHT_BYPASS photonApplyBurst=true");
             if (isDualSession)
                 createCameraPreviewSession(true);
             else {
-                iris26679SubmissionSession.stopRepeating();
-                iris26679SubmissionSession.abortCaptures();
+            mCaptureSession.stopRepeating();
+            mCaptureSession.abortCaptures();
                 switch (iris26533CaptureMode) {
                     case UNLIMITED:
-                        iris26679SubmissionSession.setRepeatingBurst(captures, CaptureCallback, iris26679SubmissionHandler);
+                        mCaptureSession.setRepeatingBurst(captures, CaptureCallback, mBackgroundHandler);
                         break;
                     case RAWVIDEO:
-                        iris26679SubmissionSession.setRepeatingRequest(captures.get(0), CaptureCallback, iris26679SubmissionHandler);
+                        mCaptureSession.setRepeatingRequest(captures.get(0), CaptureCallback, mBackgroundHandler);
                         break;
                     case NIGHT:
                     case PHOTO:
                     case MOTION:
-                        iris26679SubmissionSession.captureBurst(captures, CaptureCallback, iris26679SubmissionHandler);
+                        mCaptureSession.captureBurst(captures, CaptureCallback, mBackgroundHandler);
                         break;
                 }
             }
         } catch (CameraAccessException e) {
-            final boolean cleaned = abortIrisNight26547CaptureState("CameraAccessException");
-            burst = false;
-            mState = STATE_PREVIEW;
-            try { PhotonCamera.getGyro().CompleteSequence(); } catch (Throwable ignored) {}
-            Log.e(TAG, "IRIS_26679_CAPTURE_SUBMISSION_CAMERA_ACCESS cleanup=" + cleaned, e);
-            if (iris26679CaptureUiStarted) {
-                cameraEventsListener.onProcessingError("Camera capture session changed; please try again");
-            } else {
-                iris26679RejectShutter("CAPTURE_SUBMISSION_CAMERA_ACCESS",
-                        "Camera is temporarily unavailable");
-            }
+            abortIrisNight26547CaptureState("CameraAccessException");
+            Log.e(TAG, Log.getStackTraceString(e));
         } catch (RuntimeException e) {
             if (iris26547NightRequested) {
                 final boolean cleaned = abortIrisNight26547CaptureState(
@@ -9510,19 +9236,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                         + e.getClass().getSimpleName());
                 return;
             }
-            if (e instanceof IllegalStateException || e instanceof IllegalArgumentException) {
-                burst = false;
-                mState = STATE_PREVIEW;
-                try { PhotonCamera.getGyro().CompleteSequence(); } catch (Throwable ignored) {}
-                Log.e(TAG, "IRIS_26679_CAPTURE_SUBMISSION_SESSION_RACE", e);
-                if (iris26679CaptureUiStarted) {
-                    cameraEventsListener.onProcessingError("Camera capture session changed; please try again");
-                } else {
-                    iris26679RejectShutter("CAPTURE_SUBMISSION_SESSION_RACE",
-                            "Camera is temporarily unavailable");
-                }
-                return;
-            }
             throw e;
         } finally {
             if (iris26553NightDispatchReserved)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureEventsListener.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureEventsListener.java
index 3e7c6b8b1e7e8fd58541a6f5718f75fbd26c5834..dc84b827ffdd1b6daae01299c024c677dec9377e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureEventsListener.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureEventsListener.java
@@ -7,11 +7,6 @@ public interface CaptureEventsListener {
 
     void onCaptureStillPictureStarted(Object o);
 
-    /* IRIS_26679_SHUTTER_TERMINAL_REJECTION
-     * A shutter press can be rejected before capture-start ownership is established.
-     * The UI must be explicitly released on every such terminal path. */
-    void onCaptureStillPictureRejected(Object o);
-
     void onBurstPrepared(Object o);
 
     void onFrameCaptureStarted(Object o);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/control/TouchFocus.java b/app/src/main/java/com/particlesdevs/photoncamera/control/TouchFocus.java
index c3bdfffc82d4d403eafe71123db2e73ac8b2f4bb..adb469d23b7a10bb2a2e72dde8e8e2e809e71418 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/control/TouchFocus.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/control/TouchFocus.java
@@ -29,10 +29,6 @@ public class TouchFocus {
     /* IRIS_26523_REAL_AF_LOCK_STATE */
     private volatile boolean focusLocked = false;
     private MeteringRectangle[] activeTouchRegion = null;
-    /* IRIS_26679_SESSION_SAFE_TOUCH_FOCUS_RESET
-     * Session teardown/reconfigure can overlap the delayed 3 s autofocus reset. Keep one pending
-     * reset intent and replay it only after CaptureController proves a usable preview session. */
-    private volatile boolean iris26679PendingAutoFocusReset = false;
 
 
     public TouchFocus(CaptureController captureController, View focusCircle, GLPreview textureView) {
@@ -75,8 +71,7 @@ public class TouchFocus {
 
     /** Re-establish the repeating preview request after a capture without cancelling user AF lock. */
     public void resumeLockedFocusAfterCapture() {
-        if (!focusLocked || CaptureController.burst
-                || !captureController.iris26679PreviewSessionReadyForControl()) return;
+        if (!focusLocked || CaptureController.burst) return;
         CaptureRequest.Builder builder = captureController.mPreviewRequestBuilder;
         if (builder == null || activeTouchRegion == null) return;
         builder.set(CaptureRequest.CONTROL_AF_REGIONS, activeTouchRegion);
@@ -86,20 +81,6 @@ public class TouchFocus {
         isTouchFocus = true;
     }
 
-    /** Replay exactly one delayed reset after a newly configured preview session is usable. */
-    public void onPreviewSessionReady() {
-        if (CaptureController.burst) return;
-        if (iris26679PendingAutoFocusReset) {
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_PENDING_RESET_REPLAY");
-            resetAutoFocus();
-            return;
-        }
-        if (focusLocked && activeTouchRegion != null) {
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_LOCK_REPLAY");
-            resumeLockedFocusAfterCapture();
-        }
-    }
-
     private void showFocusCircle(float fx, float fy) {
         focusCircleView.setX(textureView.getX() + fx - focusCircleView.getMeasuredWidth() / 2.0f);
         focusCircleView.setY(textureView.getY() + fy - focusCircleView.getMeasuredHeight() / 2.0f);
@@ -237,20 +218,13 @@ public class TouchFocus {
 
     private void triggerAutoFocus(MeteringRectangle[] rectaf, boolean lockRequested) {
         if (CaptureController.burst) return;
-        if (!captureController.iris26679PreviewSessionReadyForControl()) {
-            Log.w(TAG, "IRIS_26679_TOUCH_AF_TRIGGER_SKIPPED sessionReady=false");
-            return;
-        }
         CaptureRequest.Builder builder = captureController.mPreviewRequestBuilder;
         if (builder == null) {
             Log.w(TAG, "triggerAutoFocus(): mPreviewRequestBuilder is null");
             return;
         }
         builder.set(CaptureRequest.CONTROL_AF_TRIGGER, CaptureRequest.CONTROL_AF_TRIGGER_CANCEL);
-        if (!captureController.rebuildPreviewBuilderOneShot()) {
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_TRIGGER_ABORT cancelAccepted=false");
-            return;
-        }
+        captureController.rebuildPreviewBuilderOneShot();
         builder.set(CaptureRequest.CONTROL_AF_REGIONS, rectaf);
         builder.set(CaptureRequest.CONTROL_AE_REGIONS, rectaf);
         builder.set(CaptureRequest.CONTROL_MODE, CaptureRequest.CONTROL_MODE_AUTO);
@@ -262,16 +236,10 @@ public class TouchFocus {
                 || preferredAfMode == CaptureRequest.CONTROL_AF_MODE_MACRO;
         if (lockRequested || oneShotAfMode) {
             builder.set(CaptureRequest.CONTROL_AF_TRIGGER, CaptureRequest.CONTROL_AF_TRIGGER_START);
-            if (!captureController.rebuildPreviewBuilderOneShot()) {
-                Log.d(TAG, "IRIS_26679_TOUCH_AF_TRIGGER_ABORT startAccepted=false");
-                return;
-            }
+            captureController.rebuildPreviewBuilderOneShot();
             builder.set(CaptureRequest.CONTROL_AF_TRIGGER, CaptureRequest.CONTROL_AF_TRIGGER_IDLE);
         }
-        if (!captureController.rebuildPreviewBuilder()) {
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_TRIGGER_ABORT repeatingAccepted=false");
-            return;
-        }
+        captureController.rebuildPreviewBuilder();
         isTouchFocus = true;
         focusLocked = lockRequested;
         Log.d(TAG, "IRIS_26523_TOUCH_AF lock=" + lockRequested
@@ -281,15 +249,8 @@ public class TouchFocus {
         focusLocked = false;
         activeTouchRegion = null;
         if (CaptureController.burst) return;
-        if (!captureController.iris26679PreviewSessionReadyForControl()) {
-            iris26679PendingAutoFocusReset = true;
-            isTouchFocus = false;
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_RESET_DEFERRED sessionReady=false");
-            return;
-        }
         CaptureRequest.Builder builder = captureController.mPreviewRequestBuilder;
         if (builder == null) {
-            iris26679PendingAutoFocusReset = true;
             Log.w(TAG, "resetAutoFocus(): mPreviewRequestBuilder is null");
             isTouchFocus = false;
             return;
@@ -300,21 +261,10 @@ public class TouchFocus {
         builder.set(CaptureRequest.CONTROL_AE_REGIONS, captureController.mPreviewMeteringAE);
         builder.set(CaptureRequest.CONTROL_AF_MODE, captureController.mPreviewAFMode);
         builder.set(CaptureRequest.CONTROL_AE_MODE, captureController.mPreviewAEMode);
-        if (!captureController.rebuildPreviewBuilderOneShot()) {
-            iris26679PendingAutoFocusReset = true;
-            isTouchFocus = false;
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_RESET_DEFERRED oneShotAccepted=false");
-            return;
-        }
+        captureController.rebuildPreviewBuilderOneShot();
         builder.set(CaptureRequest.CONTROL_AF_TRIGGER, CaptureRequest.CONTROL_AF_TRIGGER_IDLE);
         builder.set(CaptureRequest.CONTROL_AE_PRECAPTURE_TRIGGER, CaptureRequest.CONTROL_AE_PRECAPTURE_TRIGGER_IDLE);
-        if (!captureController.rebuildPreviewBuilder()) {
-            iris26679PendingAutoFocusReset = true;
-            isTouchFocus = false;
-            Log.d(TAG, "IRIS_26679_TOUCH_AF_RESET_DEFERRED repeatingAccepted=false");
-            return;
-        }
-        iris26679PendingAutoFocusReset = false;
+        captureController.rebuildPreviewBuilder();
         isTouchFocus = false;
     }
 
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
index 94655b20e9f8c5bf954d1b35d6183dbaef2a829b..77d081d891f134ab5de03b0af6442715e18c5149 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraFragment.java
@@ -1114,17 +1114,6 @@ public class CameraFragment extends Fragment implements BaseActivity.BackPressed
             //textureView.post(() -> textureView.setAlpha(0.8f));
         }
 
-        @Override
-        public void onCaptureStillPictureRejected(Object o) {
-            /* IRIS_26679_SHUTTER_TERMINAL_REJECTION
-             * onTimerFinished() disables the shutter before entering CaptureController.
-             * A pre-capture terminal rejection therefore owns one explicit UI release; it must
-             * not masquerade as processing completion or mutate the active camera generation. */
-            logD("IRIS_26679_SHUTTER_UI_RELEASE reason=" + o);
-            mCameraUIView.activateShutterButton(true);
-            mCameraUIView.lockUIForBurst(false);
-        }
-
         private long prevPlayTime = 0;
         @Override
         public void onFrameCaptureStarted(Object o) {
diff --git a/app/version.properties b/app/version.properties
index 6877f52a50505f4d4dbcc12450c6c0217b8215e1..16ebd0574ccf621e3865cff7f6c28e81ebd0168f 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726679
-VERSION_BUILD=26679
+VERSION_NAME=0.9726678
+VERSION_BUILD=26678
