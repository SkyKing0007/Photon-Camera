26625 R2 permanent compiler regression

Observed authoritative failure:
- GitHub Actions run: 34549354529
- Task: :app:compileDebugKotlin
- Candidate: 26625 R1 robust SHORT fallback geometry

Exact defect class:
The frame telemetry string referenced component.fallbackTrustedCells after the block-local
`component` value had gone out of Kotlin scope. This is a compile-time ownership/scope defect,
not an imaging-algorithm defect.

Required invariant:
- Declare `shortFallbackTrustedCells26625` in frame scope before the SHORT component block.
- Assign it from `component.fallbackTrustedCells` while `component` is in scope.
- Telemetry outside that block must reference only the frame-scope scalar.
- The fallback geometry, same-CFA radiometric authorization, effective merge flow, thresholds,
  version/build, and all successful 26624 inherited presentation/runtime behavior remain unchanged.

The static regression is additive only; the real project Kotlin compiler remains mandatory.

26625 R3 permanent false-green build regression

Observed authoritative infrastructure failure:
- GitHub Actions run: 34552316575
- Artifact: 10181264679
- Uploaded artifact size: 7,797 bytes
- Successful 26624 comparison artifact: 128,979,481 bytes

Exact defect class:
The R2 repair wrapper invoked the original build script with `--local-prebuild`. That mode is
explicitly preparation-only: runtime-expanded GLSL reported `real_compiler=False`; Kotlin, Java,
NDK, full assemble, APK production, and post-build invariance were not run. The wrapper then
incorrectly printed a completion PASS, and upload-artifact succeeded because the proof directory
contained files even though the expected APK did not exist.

Permanent invariant:
- GitHub Actions must never invoke 26625 through `--local-prebuild`.
- The original 26625 real compiler/build/invariance sequence must run unchanged.
- Before artifact upload, the exact expected APK must exist and be non-empty.
- Compiler status must prove real GLSL, Kotlin, Java, both NDK ABIs, full assemble, and post-build invariance PASS.
- A proof directory by itself can never satisfy the build-product gate.
