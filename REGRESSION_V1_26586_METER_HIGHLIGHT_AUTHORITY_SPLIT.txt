26586 permanent failure regression — viewfinder/body meter must not consume final structured highlight headroom

Exact 26585 failure condition: exposureError(gain) called iris26583ToneDecision(gain), so the final P98/0.945 structured-highlight response participated in every trial exposure and could provoke compensating displayGain.

Required 26586 contract:
- fixed meter sample selection uses iris26586ViewfinderMeterToneDecision(1.0f);
- exposureError uses iris26586ViewfinderMeterToneDecision(gain);
- meter structured branch is exact successful 26584 64-bin P90 / 0.965 target;
- final structured branch remains 26585 256-bin P98 / 0.945 target;
- final highlight sceneWhite is calculated only after solvedEv -> displayGain is frozen;
- no final-highlight decision may feed back into solveBounded.
