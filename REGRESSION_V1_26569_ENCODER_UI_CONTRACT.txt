26569 V1 localized encoder + adaptive UI contract

RUNTIME AUTHORITY
- Exact successful 26568 V1.1 compiled candidate from Actions run 33358885390 / artifact 9746086429.
- Artifact ZIP SHA-256 224acb3d97ae3387c7f7a2ccfc5a61b29d98c6f89108e2d74f4d1ce7043ead12.
- Candidate TAR SHA-256 31274487b56a7770fb729345ade2ed24b86e8f6d0b09e413d40879df3784da19.

NO ARCHITECTURE REOPEN
- Sabre/VGN remains RGB/chroma/highlight owner.
- Direct-CFA true2x remains bounded scalar detail only.
- Universal adaptive color, Display-P3, Jin model-domain adapter, UHDR/JPEG-R ownership and DNG are not redesigned.
- Gain map remains 1:1; no GPU/CPU lifetime overlap or new concurrent publication ownership is introduced in this build.
- No new backup: localized encoder/UI correction is protected by exact successful authority hashes and deterministic rollback patch.

ENCODER
- Existing proven color/tone/Jin/render primitives remain byte-identical.
- Motion-only fast path is gated by !jin.enabled().
- A bounded ProfileRegion is populated exactly once from unchanged profileColor() for the current source band.
- Cached adaptive appearance and render helpers are equation-identical after substituting cached profileColor results.
- Motion skips the useless second Jin copy pass when Jin is disabled.
- Motion bands increase from 128 to 256 rows and worker cap from 4 to 6; Jin-enabled path remains 128 rows / max 4 workers.
- Base JPEG and gain JPEG settings, 4:4:4 base encoding, P3 ICC and 1:1 gain dimensions remain unchanged.
- No full-resolution RGB8 or raw-gain scratch files are allowed.
- IRIS_26569_TRUE2X_ENCODER_TIMING separates profile/render/Jin/base-JPEG/gain-JPEG/stream timing.
- IRIS_26569_TRUE2X_PACKAGE_TIMING measures the unchanged JPEG-R packaging call.

ADAPTIVE UI
- Existing preferred still/video lens-selector positions remain the starting positions.
- After actual layout, measured lens and shutter window bounds enforce a minimum 12dp vertical gap.
- correctionPx = max(0, lensBottom + minimumGapPx - shutterTop).
- final lens translation = preferredTranslationY - correctionPx.
- Already-safe layouts receive correction 0 and therefore remain at the existing preferred position.
- No Samsung/S26/Xiaomi/Pixel/Motorola/device-resolution/aspect-ratio special case is allowed.
- Root layout changes re-evaluate the measured collision, covering real display/inset/configuration geometry.
- UI XML stays byte-identical; CameraUIViewImpl is the single new collision-geometry owner.

REQUIRED DEVICE TELEMETRY
- IRIS_26569_TRUE2X_ENCODER_TIMING
- IRIS_26569_TRUE2X_PACKAGE_TIMING
- IRIS_26569_UI_COLLISION_GUARD
