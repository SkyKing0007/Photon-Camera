26498 V1.1 PREBUILD ROOT-ARCHITECTURE AUDIT
==========================================

V1 failure classification
- 26498 V1 package commit: 350d1b655afc58ae75f72e139b2f24961e17a7a3.
- V1 stopped before GLSL, javac, Gradle or APK build.
- Exact failed gate: the text guard intended to reject "Short B" also searched for literal SHORT_B.
- That pattern falsely matched the legitimate existing marker IRIS_26480_SHORT_BATCH_BOUNDARY.
- No image-source or architecture change is made in V1.1 because of this infrastructure-only false positive.

Rollback safety
- Verified pre-26498 branch: backup-26497-tested-regression-before-26498-root-architecture
  exact SHA: 00deeae2c82518511988ca6267b2e6071300cba6.
- Verified failed-V1 branch: backup-26498-v1-guard-failed-before-v1.1-short-b-gate
  exact SHA: 350d1b655afc58ae75f72e139b2f24961e17a7a3.
- dev is not modified.

V1.1 guard correction
- Removed the ambiguous Short-B substring grep.
- New structural proof parses CaptureController.java and requires:
  * exactly one definition of the active asynchronous Short-A method;
  * exactly one active call site using the per-shot Motion26486ShortTicket;
  * exactly one request tag and one physical CameraCaptureSession.capture() inside that active method;
  * the historical synchronous short method may remain as dead source but has no call site;
  * the -2.5 EV Short-A protection policy remains present.
- The structural guard was executed against the exact generated 26498 candidate and passed:
  active definitions=1, active calls=1, active request tag=1, active physical capture=1,
  legacy definitions=1, legacy mentions=1 (definition only).
- This proves one active auxiliary short-exposure owner without relying on name/sub-string guesses.

Image-quality baseline policy
- 26498 is NOT a patch-forward from 26497 image math.
- Source is reconstructed from the exact successful 26494 emitted source bundle (856 canonical files).
- 26498_source_delta_from_exact_26494.patch is BYTE-FOR-BYTE UNCHANGED from V1:
  SHA256 988691939ff9ac70a6c0ece1eae03a44c5d0f46cb4a541448c94bd10ad0106a3.
- validate_26498_root_architecture.py is BYTE-FOR-BYTE UNCHANGED from V1:
  SHA256 cf0a86765bb078c5f7c611b3d20b3a9cada1a42e5bd623ee98ac0da84cc14f83.
- Only build infrastructure changed in V1.1: build script guard/lineage verification and workflow identity/backup verification.

Evidence-backed carry-forwards
- 26496 spatially persistent small-highlight trigger.
- -2.5 EV Short A with clamp-aware Camera2 ownership.
- 26496 short failure telemetry.
- 26497 corrected interpretation of Wronski flow-expand W: interpolation cancellation preserves the base vector; it is not an invalid flow.
- 26497 bounded local short CFA correspondence refinement.

Highlight / pink root correction
- 26496/26497 post-RCD chroma-completion is absent from the active graph.
- CENSORED phases carry TrustBuf=0 inside RCD.
- Their finite brightness placeholder keeps the lattice numerically defined but cannot steer RCD directional choice or become opponent-color evidence.
- NORMAL and SHORT_VALIDATED physical sites retain measurement authority.
- Missing opponent chroma requires multiple physically trusted observations with local consensus.

True-photo border root correction
- No PPG/RCD algorithm switch is used as the final solution.
- Every RCD core band receives a 12-pixel virtual CFA halo on all four physical image edges.
- Out-of-photo virtual samples are reflected in full-photo coordinates while preserving Bayer parity for even dimensions.
- Physical output pixels therefore have complete RCD computational support rather than clamped kernels.

Colored-wall root localization
- StageTelemetry is exact texture pass-through; it does not alter image pixels.
- A sparse 32x24 probe measures low-gradient log(R/G), log(B/G), and packed-CFA green-phase split variation.
- Explicit stages are fused Bayer, post-RCD, post-display exposure, post-Camera2 color transform, and post-render.
- This is intended to locate the producer of broad false chroma instead of applying a wall-color correction by assumption.

UHDR root correction
- 26495/26496/26497 quarter-resolution gain filtering/downsampling experiments are not carried forward.
- 26498 emits one 8-bit gain sample per primary SDR pixel (1:1 full-resolution R8 gain map).
- SDR remains the full-resolution spatial primary.
- This removes the lower-resolution gain-map spatial upsampling mechanism rather than trying another blur/filter kernel.

Protected architecture
- mfsr_bayer_accumulate.glsl byte-identical to successful 26494.
- mfsr_bayer_normalize.glsl byte-identical.
- MotionV2WronskiAlignment.java byte-identical.
- render.glsl byte-identical.
- color_transform.glsl byte-identical.
- MotionV2DisplayExposure.java byte-identical.
- Historical rcd26489 shaders remain byte-identical as protected reference sources.
- No Short B image architecture is introduced.
- No ESD or sharpening is reintroduced.

Local V1.1 validation completed
- Exact 26498 V1 source patch reapplied cleanly to the exact 26494 bundle.
- Source scope remains 8 modified existing files + 8 new RCD shaders; no removals.
- 848 baseline canonical files remain byte-identical before versioning.
- Full Gate-4 protected hashes and architecture assertions pass on the exact candidate.
- New structural Short-A ownership proof passes on the exact candidate.
- Shell syntax passes.
- Workflow YAML parses.
- Python validator syntax/tests pass.
- V1 root synthetic tests remain unchanged.
- glslang and Android SDK are not available in this local runtime; GitHub Actions remains the authoritative GLSL, javac, CMake/native, and complete APK build gate.
