diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 1a0b95c9abc70d7dac6971896908693270186b5b..da03083e22d2f57c41f27316914338a60b3445d9 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -5003,6 +5003,3 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 }
-
-/* IRIS_26708_SABRE_FUSION_MATH_FROZEN
- * 26708 changes only compact provenance handoff; established 26651 SHORT fusion equations remain byte-semantic authority. */
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index ee263a46d190694fec21077632c54e4a3eca9f69..5e87e028a952fcdc62fcf0ccb2755bf32c85e735 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2771,7 +2771,6 @@ internal class GlesMgcRawSpatialStacker(
 
             var fusionDecision26651 = 0
             var shortColorValidity26651 = 0
-            var shortRadianceAuthorityMask26708: ByteArray? = null
             var resolveExtendedLinear26651 = resolveAccumulatedColor26604
             if (highlightShortScheduled26607) {
                 val shortFlow = checkNotNull(retainedShortFlow26648) {
@@ -2818,7 +2817,7 @@ internal class GlesMgcRawSpatialStacker(
                 /* IRIS_26706_SAMPLED_FUSION_DECISION_TELEMETRY
                  * One 64x48 probe restores decision/radiance observability without reviving the old
                  * full-resolution R8 readback or changing production fusion. */
-                shortRadianceAuthorityMask26708 = logSabreUniversalFusionRadiance26651(
+                logSabreUniversalFusionRadiance26651(
                     normalMean = resolveAccumulatedColor26604,
                     fusedMean = fused,
                     decision = fusionDecision26651,
@@ -3128,9 +3127,6 @@ internal class GlesMgcRawSpatialStacker(
                 highlightShortRestoreMaskGenerated = highlightShortConfidenceFieldGenerated26607,
                 highlightShortAppliedToTrue2x = highlightShortCommonGuideAppliedToTrue2x26607,
                 highlightShortExposureRatio = highlightShortExposureRatio26587,
-                shortRadianceAuthorityMask = shortRadianceAuthorityMask26708,
-                shortRadianceAuthorityWidth = if (shortRadianceAuthorityMask26708 != null) 64 else 0,
-                shortRadianceAuthorityHeight = if (shortRadianceAuthorityMask26708 != null) 48 else 0,
                 normalStackedDngRaw16 = normalStackedDngRaw16,
                 normalStackedDngFrameCount = if (exportNormalStackedDng) normalFrameCount else 0,
                 normalStackedDngNoiseProfile = normalStackedDngNoiseProfile,
@@ -4452,7 +4448,7 @@ internal class GlesMgcRawSpatialStacker(
         normalMean: Int,
         fusedMean: Int,
         decision: Int,
-    ): ByteArray {
+    ) {
         val program = sabreUniversalFusionTelemetryProgram26651
         check(program != 0)
         val probeWidth = 64
@@ -4460,7 +4456,6 @@ internal class GlesMgcRawSpatialStacker(
         val probe = createTexture(probeWidth, probeHeight, GLES30.GL_RGBA16F, GLES30.GL_NEAREST)
         val readback = ByteBuffer.allocateDirect(probeWidth * probeHeight * 4 * Short.SIZE_BYTES)
             .order(ByteOrder.nativeOrder())
-        val shortAuthority = ByteArray(probeWidth * probeHeight)
         try {
             GLES30.glUseProgram(program)
             bindTexture(program, "uNormalMean", 0, normalMean)
@@ -4518,7 +4513,6 @@ internal class GlesMgcRawSpatialStacker(
                     else -> snrRejected++
                 }
                 if (shortWeight > 0.005) {
-                    shortAuthority[index] = 1
                     shortSum += short
                     activeCount++
                 }
@@ -4537,8 +4531,6 @@ internal class GlesMgcRawSpatialStacker(
                     "confidenceSemantics=FINAL_PHYSICAL_RADIOMETRIC_WEIGHT"
             PLog.i(SABRE_TAG, "IRIS_26706_FUSION_DECISION_SAMPLE $details")
             MotionTrace.processingState("IRIS_26706_FUSION_DECISION_SAMPLE", details)
-            PLog.i(SABRE_TAG, "IRIS_26708_COMPACT_SHORT_AUTHORITY width=$probeWidth height=$probeHeight active=$activeCount")
-            return shortAuthority
         } finally {
             releaseOwnedTexture(probe, "26651 fusion radiance/weight telemetry")
         }
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt b/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
index dd0607c723244551a2b7c4ea24899273358a0f0e..62dffa65b085948841e9ade7ec26c317a4f46ff8 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/RawStackContracts.kt
@@ -64,12 +64,6 @@ data class RawStackResult(
     val highlightShortRestoreMaskGenerated: Boolean = false,
     val highlightShortAppliedToTrue2x: Boolean = false,
     val highlightShortExposureRatio: Float = 1f,
-    /* IRIS_26708_COMPACT_SHORT_RADIANCE_AUTHORITY
-     * Meter-resolution read-only provenance. Nonzero means SHORT final radiance authority touched
-     * that probe cell; it is never a second full-resolution image or a fusion input. */
-    val shortRadianceAuthorityMask: ByteArray? = null,
-    val shortRadianceAuthorityWidth: Int = 0,
-    val shortRadianceAuthorityHeight: Int = 0,
     /* IRIS_26532_STREAMED_SUPERRES_RESULT
      * High-resolution SR evidence is streamed while Spatial bands are resident. The normal
      * GPU source remains native-sized so the proven 26531 chroma/tone path never allocates
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index 0606852c120fa96f7b7af4319ee637c404f6d100..c215a5fb17d3439d609377a07d53c9c1750a4205 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -1794,11 +1794,29 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private long mMotion26680LatchGeneration = -1L;
 
     /* IRIS_26705_NORMAL_AE_SHORT_HEADROOM_OWNER
-     * HAL/user AE remains the sole NORMAL exposure owner. RAW highlight evidence is read-only and
-     * may deepen only the isolated post-shutter HIGHLIGHT_SHORT. This owner never writes
-     * CONTROL_AE_EXPOSURE_COMPENSATION, so highlight protection cannot darken or ratchet NORMAL. */
+     * The isolated HIGHLIGHT_SHORT remains independent and may still deepen capture-local
+     * headroom. 26707 adds a separate bounded NORMAL highlight-protection owner below; it is
+     * absolute relative to one settled HAL/user baseline and can never ratchet cumulatively. */
     private static final float MOTION_26705_SHORT_EXTRA_MAX_EV = 2.5f;
 
+    /* IRIS_26707_ABSOLUTE_NORMAL_HIGHLIGHT_PROTECTION
+     * Device/video evidence from 26703 showed the first useful recovery at -0.67 EV for the broad
+     * plant-wall highlight and -1.33 EV total for the compact chandelier X reflection. Keep only
+     * those absolute states (plus zero), never rebase from an already-protected request, and never
+     * bypass 26705's genuine six-frame HAL AE/AWB/energy convergence requirement. */
+    private static final float MOTION_26707_NORMAL_HIGHLIGHT_MODERATE_EV = 2.0f / 3.0f;
+    private static final float MOTION_26707_NORMAL_HIGHLIGHT_STRONG_EV = 4.0f / 3.0f;
+    private static final int MOTION_26707_HIGHLIGHT_OBSERVE_TIMEOUT_FRAMES = 12;
+    private boolean mMotion26707HighlightBaseReady = false;
+    private boolean mMotion26707HighlightDecisionMade = false;
+    private boolean mMotion26707HighlightRequestIssued = false;
+    private int mMotion26707HighlightBaseSteps = 0;
+    private int mMotion26707HighlightTargetSteps = 0;
+    private int mMotion26707HighlightObserveFrames = 0;
+    private float mMotion26707HighlightStepEv = 0.0f;
+    private float mMotion26707HighlightTargetProtectionEv = 0.0f;
+    private float mMotion26707HighlightObservedProtectionEv = 0.0f;
+
     /*
      * IRIS_26662_FRAME_MATCHED_PREVIEW_PROTECTION
      * Preview compensation must describe the exposure of the SurfaceTexture frame being drawn,
@@ -1852,21 +1870,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private volatile long mMotion26496RecentHighlightEvidenceTimestampNs = 0L;
     private final java.util.concurrent.atomic.AtomicInteger mMotion26496SparseDitherCounter =
             new java.util.concurrent.atomic.AtomicInteger(0);
-
-    /* IRIS_26708_READ_ONLY_NINE_PHASE_HIGHLIGHT_CLASSIFIER */
-    private static final float MOTION_26708_SHORT_MODERATE_EV = 2.0f / 3.0f;
-    private static final float MOTION_26708_SHORT_STRONG_EV = 4.0f / 3.0f;
-    private static final long MOTION_26708_CLASS_FRESH_NS = 333_000_000L;
-    private int mMotion26708PhaseExpected = 0, mMotion26708PhaseCount = 0;
-    private long mMotion26708ClassifierGeneration = -1L;
-    private double mMotion26708ClassifierExposureEnergy = Double.NaN;
-    private int mMotion26708HighlightSamplesSum = 0, mMotion26708CoherentCellsSum = 0;
-    private int mMotion26708PeakQuadMax = 0, mMotion26708BroadVotes = 0;
-    private int mMotion26708MixedVotes = 0, mMotion26708CompactVotes = 0;
-    private float mMotion26708P995Max = 0.0f;
-    private volatile int mMotion26708PublishedClass = 0;
-    private volatile long mMotion26708PublishedTimestampNs = 0L, mMotion26708PublishedGeneration = -1L;
-    private volatile double mMotion26708PublishedExposureEnergy = Double.NaN;
     private static final int MOTION_26496_MIN_HIGHLIGHT_SAMPLES = 2;
     private static final long MOTION_26496_HIGHLIGHT_EVIDENCE_HOLD_NS = 400_000_000L;
 
@@ -2587,9 +2590,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             mMotion26496RawCoherentHighlightCells = coherentCells;
             mMotion26496RawPeakQuadHighlightPhases = peakQuadPhases;
             mMotion26496RawDitherIndex = ditherIndex;
-            accumulateMotion26708HighlightPhase(image.getTimestamp(), ditherIndex, highlightSamples,
-                    coherentCells, peakQuadPhases, rawP995, broadDynamicConflict,
-                    mixedRangeConflict, compactDynamicConflict);
             if (highlightSamples >= MOTION_26496_MIN_HIGHLIGHT_SAMPLES
                     || coherentCells > 0) {
                 mMotion26496RecentHighlightEvidenceTimestampNs = image.getTimestamp();
@@ -2601,67 +2601,16 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
     }
 
-    private void resetMotion26708PhaseAccumulator() {
-        mMotion26708PhaseExpected = 0; mMotion26708PhaseCount = 0;
-        mMotion26708ClassifierGeneration = mIris26548CameraHealthGeneration;
-        mMotion26708ClassifierExposureEnergy = Double.NaN;
-        mMotion26708HighlightSamplesSum = 0; mMotion26708CoherentCellsSum = 0;
-        mMotion26708PeakQuadMax = 0; mMotion26708P995Max = 0.0f;
-        mMotion26708BroadVotes = 0; mMotion26708MixedVotes = 0; mMotion26708CompactVotes = 0;
-    }
-    private void clearMotion26708HighlightClassifier(@NonNull String reason) {
-        resetMotion26708PhaseAccumulator();
-        mMotion26708PublishedClass = 0; mMotion26708PublishedTimestampNs = 0L;
-        mMotion26708PublishedGeneration = -1L; mMotion26708PublishedExposureEnergy = Double.NaN;
-        Log.i(TAG, "IRIS_26708_HIGHLIGHT_CLASSIFIER_INVALIDATED reason=" + reason
-                + " generation=" + mIris26548CameraHealthGeneration);
-    }
-    private void accumulateMotion26708HighlightPhase(long timestampNs, int phase,
-            int highlightSamples, int coherentCells, int peakQuadPhases, float rawP995,
-            boolean broad, boolean mixed, boolean compact) {
-        if (!mMotion26680PreviewLatched || mZslCapturing || timestampNs <= 0L
-                || !Double.isFinite(mMotion26680LastExposureEnergy) || mMotion26680LastExposureEnergy <= 0.0) return;
-        final long generation = mIris26548CameraHealthGeneration; final double energy = mMotion26680LastExposureEnergy;
-        boolean incompatible = mMotion26708ClassifierGeneration != generation
-                || !Double.isFinite(mMotion26708ClassifierExposureEnergy)
-                || Math.abs(Math.log(energy / mMotion26708ClassifierExposureEnergy) / Math.log(2.0)) > 0.02;
-        if (mMotion26708PhaseCount == 0 || incompatible || phase != mMotion26708PhaseExpected) {
-            resetMotion26708PhaseAccumulator(); mMotion26708ClassifierGeneration = generation;
-            mMotion26708ClassifierExposureEnergy = energy; if (phase != 0) return;
-        }
-        mMotion26708HighlightSamplesSum += Math.max(0, highlightSamples);
-        mMotion26708CoherentCellsSum += Math.max(0, coherentCells);
-        mMotion26708PeakQuadMax = Math.max(mMotion26708PeakQuadMax, peakQuadPhases);
-        if (Float.isFinite(rawP995)) mMotion26708P995Max = Math.max(mMotion26708P995Max, rawP995);
-        if (broad) mMotion26708BroadVotes++; if (mixed) mMotion26708MixedVotes++; if (compact) mMotion26708CompactVotes++;
-        mMotion26708PhaseCount++; mMotion26708PhaseExpected = (phase + 1) % 9; if (mMotion26708PhaseCount != 9) return;
-        boolean any = mMotion26708HighlightSamplesSum >= MOTION_26496_MIN_HIGHLIGHT_SAMPLES
-                || mMotion26708CoherentCellsSum > 0 || mMotion26708BroadVotes > 0
-                || mMotion26708MixedVotes > 0 || mMotion26708CompactVotes > 0;
-        boolean strong = (mMotion26708CompactVotes >= 2 && mMotion26708CoherentCellsSum >= 2 && mMotion26708PeakQuadMax >= 2)
-                || (mMotion26708P995Max >= 0.90f && mMotion26708HighlightSamplesSum >= 9);
-        mMotion26708PublishedClass = !any ? 0 : (strong ? 2 : 1);
-        mMotion26708PublishedTimestampNs = timestampNs; mMotion26708PublishedGeneration = generation;
-        mMotion26708PublishedExposureEnergy = energy;
-        Log.i(TAG, "IRIS_26708_HIGHLIGHT_CLASS_PUBLISHED class=" + mMotion26708PublishedClass
-                + " completePhases=9 generation=" + generation + " exposureEnergy=" + energy
-                + " highlightSamplesSum=" + mMotion26708HighlightSamplesSum + " coherentCellsSum=" + mMotion26708CoherentCellsSum
-                + " peakQuadMax=" + mMotion26708PeakQuadMax + " p995Max=" + mMotion26708P995Max
-                + " broadVotes=" + mMotion26708BroadVotes + " mixedVotes=" + mMotion26708MixedVotes
-                + " compactVotes=" + mMotion26708CompactVotes + " previewExposureWrite=false");
-        resetMotion26708PhaseAccumulator();
-    }
-    private float motion26708FrozenShortProtectionEv(long shutterTimestampNs, long normalExposureNs, int normalIso) {
-        if (shutterTimestampNs <= 0L || normalExposureNs <= 0L || normalIso <= 0
-                || mMotion26708PublishedTimestampNs <= 0L || mMotion26708PublishedGeneration != mIris26548CameraHealthGeneration) return 0.0f;
-        long ageNs = shutterTimestampNs - mMotion26708PublishedTimestampNs;
-        if (ageNs < 0L || ageNs > MOTION_26708_CLASS_FRESH_NS) return 0.0f;
-        double normalEnergy = (double) normalExposureNs * (double) normalIso;
-        if (!Double.isFinite(mMotion26708PublishedExposureEnergy) || mMotion26708PublishedExposureEnergy <= 0.0) return 0.0f;
-        double evDelta = Math.abs(Math.log(normalEnergy / mMotion26708PublishedExposureEnergy) / Math.log(2.0));
-        if (evDelta > 0.02) return 0.0f;
-        return mMotion26708PublishedClass == 2 ? MOTION_26708_SHORT_STRONG_EV
-                : (mMotion26708PublishedClass == 1 ? MOTION_26708_SHORT_MODERATE_EV : 0.0f);
+    private void clearMotion26707AbsoluteHighlightState() {
+        mMotion26707HighlightBaseReady = false;
+        mMotion26707HighlightDecisionMade = false;
+        mMotion26707HighlightRequestIssued = false;
+        mMotion26707HighlightBaseSteps = 0;
+        mMotion26707HighlightTargetSteps = 0;
+        mMotion26707HighlightObserveFrames = 0;
+        mMotion26707HighlightStepEv = 0.0f;
+        mMotion26707HighlightTargetProtectionEv = 0.0f;
+        mMotion26707HighlightObservedProtectionEv = 0.0f;
     }
 
     /* IRIS_26680_SCENE_INDEPENDENT_STABLE_PREVIEW_OWNER
@@ -2676,9 +2625,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mMotion26680ReframeMotionFrames = 0;
         mMotion26680LastExposureEnergy = Double.NaN;
         mMotion26680LatchGeneration = mIris26548CameraHealthGeneration;
-        clearMotion26708HighlightClassifier(reason);
-        /* IRIS_26705_NORMAL_AE_SHORT_HEADROOM_OWNER
-         * No 26701 transaction survives: NORMAL compensation is never changed by HDR logic. */
+        clearMotion26707AbsoluteHighlightState();
 
         /* Retire any state belonging to the older continuously-reactive HDR reference owner. */
         mMotion26661ReferenceBaseReady = false;
@@ -2748,6 +2695,60 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         return Math.max(0.0f, Math.min(MOTION_26705_SHORT_EXTRA_MAX_EV, extraEv));
     }
 
+    /* IRIS_26707_ABSOLUTE_NORMAL_HIGHLIGHT_PROTECTION
+     * Classify only the magnitude of one scene-level NORMAL protection decision. 26703 visual
+     * evidence established the useful states: broad/mixed highlight pressure first recovers at
+     * about -0.67 EV, while a concentrated compact highlight may require -1.33 EV total. The
+     * strong class is therefore signal-defined by concentrated bright topology, never by lens,
+     * scene semantics, camera ID or the currently-applied compensation. */
+    private float motion26707AbsoluteNormalHighlightProtectionEv(long sensorTs) {
+        long rawAgeNs = mMotion26380RawSignalTimestampNs <= 0L
+                ? Long.MAX_VALUE : Math.abs(sensorTs - mMotion26380RawSignalTimestampNs);
+        if (sensorTs <= 0L || rawAgeNs > 180_000_000L
+                || mMotion26380RawSampleCount < 64
+                || !Float.isFinite(mMotion26380RawHighlightFraction)
+                || !Float.isFinite(mMotion26608RawP995)) {
+            return 0.0f;
+        }
+        long highlightEvidenceAgeNs = mMotion26496RecentHighlightEvidenceTimestampNs <= 0L
+                ? Long.MAX_VALUE
+                : Math.abs(sensorTs - mMotion26496RecentHighlightEvidenceTimestampNs);
+        long hdrAgeNs = mMotion26608RecentHdrConflictTimestampNs <= 0L
+                ? Long.MAX_VALUE
+                : Math.abs(sensorTs - mMotion26608RecentHdrConflictTimestampNs);
+        boolean spatialHighlight = mMotion26496RawHighlightSampleCount
+                >= MOTION_26496_MIN_HIGHLIGHT_SAMPLES
+                || mMotion26496RawCoherentHighlightCells > 0
+                || highlightEvidenceAgeNs <= 120_000_000L;
+        boolean hdrConflict = mMotion26608CurrentHdrConflict
+                || hdrAgeNs <= 120_000_000L;
+        boolean broadLiteralClip = mMotion26380RawHighlightFraction
+                >= MOTION_26478_HIGHLIGHT_FRACTION_TRIGGER;
+        if (!spatialHighlight && !hdrConflict && !broadLiteralClip) return 0.0f;
+
+        boolean concentratedCompact = mMotion26608CompactHdrConflict
+                && mMotion26496RawCoherentHighlightCells >= 1
+                && mMotion26496RawCoherentHighlightCells <= 4
+                && mMotion26496RawPeakQuadHighlightPhases >= 2
+                && mMotion26380RawHighlightFraction
+                    < MOTION_26478_HIGHLIGHT_FRACTION_TRIGGER;
+        boolean severeBroadClip = mMotion26380RawHighlightFraction >= 0.010f
+                && mMotion26608RawP995 >= 0.90f;
+        return (concentratedCompact || severeBroadClip)
+                ? MOTION_26707_NORMAL_HIGHLIGHT_STRONG_EV
+                : MOTION_26707_NORMAL_HIGHLIGHT_MODERATE_EV;
+    }
+
+    private float motion26707ObservedProtectionEv(
+            int observedSteps, float stepEv) {
+        if (!mMotion26707HighlightBaseReady || stepEv <= 0.0f) return 0.0f;
+        int protectionSteps = Math.max(0,
+                mMotion26707HighlightBaseSteps - observedSteps);
+        return Math.max(0.0f, Math.min(
+                MOTION_26707_NORMAL_HIGHLIGHT_STRONG_EV,
+                protectionSteps * stepEv));
+    }
+
     private void updateMotion26680StablePreviewAuthority(@NonNull TotalCaptureResult result) {
         if (!isZslMode() || mZslCapturing || burst || isProcessing
                 || mPreviewRequestBuilder == null || mCaptureSession == null
@@ -2764,16 +2765,27 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         Long exposureNs = result.get(CaptureResult.SENSOR_EXPOSURE_TIME);
         Integer iso = result.get(CaptureResult.SENSOR_SENSITIVITY);
         Long sensorTs = result.get(CaptureResult.SENSOR_TIMESTAMP);
+        Integer observedCompObj = result.get(CaptureResult.CONTROL_AE_EXPOSURE_COMPENSATION);
+        Integer builderCompObj = mPreviewRequestBuilder.get(
+                CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION);
+        int observedCompSteps = observedCompObj != null
+                ? observedCompObj : (builderCompObj != null ? builderCompObj : 0);
+        android.util.Rational aeStep = mCameraCharacteristics.get(
+                CameraCharacteristics.CONTROL_AE_COMPENSATION_STEP);
+        float aeStepEv = aeStep == null ? 0.0f : aeStep.floatValue();
         if (aeMode == null || aeMode == CaptureResult.CONTROL_AE_MODE_OFF
                 || exposureNs == null || exposureNs <= 0L || iso == null || iso <= 0
                 || sensorTs == null || sensorTs <= 0L) {
             return;
         }
 
-        /* IRIS_26705_NORMAL_REFERENCE_NEVER_PROTECTED
-         * NORMAL remains HAL/user-AE owned. Frame-matched preview protection is therefore exactly
-         * zero; SHORT headroom is capture-local and must never darken preview presentation. */
-        recordMotion26662PreviewProtection(sensorTs, 0.0f);
+        /* IRIS_26707_FRAME_MATCHED_ABSOLUTE_PROTECTION
+         * The preview and every selected ZSL NORMAL frame receive the actual absolute protection
+         * relative to the one settled baseline. This is never "last delta" metadata. */
+        mMotion26707HighlightObservedProtectionEv = motion26707ObservedProtectionEv(
+                observedCompSteps, aeStepEv);
+        recordMotion26662PreviewProtection(
+                sensorTs, mMotion26707HighlightObservedProtectionEv);
 
         int shakiness = 0;
         try {
@@ -2798,6 +2810,13 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 if (mMotion26680AwbLockApplied) {
                     mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
                 }
+                int restoredBaseSteps = observedCompSteps;
+                if (mMotion26707HighlightBaseReady) {
+                    restoredBaseSteps = mMotion26707HighlightBaseSteps;
+                    mPreviewRequestBuilder.set(
+                            CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION,
+                            restoredBaseSteps);
+                }
                 mMotion26680PreviewLatched = false;
                 mMotion26680AeLockApplied = false;
                 mMotion26680AwbLockApplied = false;
@@ -2805,13 +2824,15 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 mMotion26680StationaryFrames = 0;
                 mMotion26680ReframeMotionFrames = 0;
                 mMotion26680LastExposureEnergy = Double.NaN;
-                clearMotion26708HighlightClassifier("physical-reframe");
+                clearMotion26707AbsoluteHighlightState();
                 rebuildPreviewBuilder();
                 Log.i(TAG, "IRIS_26680_STABLE_PREVIEW_RELEASE generation="
                         + mIris26548CameraHealthGeneration
                         + " shakiness=" + shakiness
                         + " reason=physical-camera-reframe"
-                        + " subjectMotionCannotUnlock=true");
+                        + " subjectMotionCannotUnlock=true"
+                        + " iris26707BaselineRestoredSteps=" + restoredBaseSteps
+                        + " cumulativeHighlightRatchetImpossible=true");
             } catch (IllegalArgumentException | IllegalStateException e) {
                 Log.w(TAG, "IRIS_26680 stable preview release skipped: "
                         + e.getClass().getSimpleName());
@@ -2820,6 +2841,21 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
 
         if (physicalReframe) {
+            /* A reframe can happen while the one-shot protection request is still settling. Undo
+             * that scene's absolute request immediately, then let HAL AE converge from the true
+             * baseline again. This is the key anti-ratchet invariant missing from 26703. */
+            if (mMotion26707HighlightBaseReady) {
+                try {
+                    mPreviewRequestBuilder.set(
+                            CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION,
+                            mMotion26707HighlightBaseSteps);
+                    rebuildPreviewBuilder();
+                } catch (IllegalArgumentException | IllegalStateException e) {
+                    Log.w(TAG, "IRIS_26707 reframe baseline restore skipped: "
+                            + e.getClass().getSimpleName());
+                }
+                clearMotion26707AbsoluteHighlightState();
+            }
             mMotion26680StableFrames = 0;
             mMotion26680StationaryFrames = 0;
             mMotion26680LastExposureEnergy = (double) exposureNs * (double) iso;
@@ -2850,11 +2886,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
 
         /* IRIS_26705_NO_UNCONVERGED_FORCE_LATCH
-         * A stationary timer is never evidence of correct AE. Cold start may latch only after
-         * HAL AE/AWB and exposure energy remain genuinely settled for the inherited six frames. */
-        boolean iris26705BaselineReady =
+         * A stationary timer is never evidence of correct AE. Cold start and every post-protection
+         * state must show six genuinely settled AE/AWB/energy frames. */
+        boolean iris26705Settled =
                 mMotion26680StableFrames >= MOTION_26680_STABLE_CONFIRM_FRAMES;
-        if (!iris26705BaselineReady) return;
 
         Boolean aeLockAvailable = mCameraCharacteristics.get(
                 CameraCharacteristics.CONTROL_AE_LOCK_AVAILABLE);
@@ -2864,6 +2899,128 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             Log.w(TAG, "IRIS_26680_STABLE_PREVIEW_UNAVAILABLE aeLock=false");
             return;
         }
+
+        if (mMotion26707HighlightRequestIssued) {
+            mMotion26707HighlightObserveFrames++;
+            boolean targetObserved = observedCompSteps == mMotion26707HighlightTargetSteps;
+            if (!targetObserved) {
+                mMotion26680StableFrames = 0;
+                if (mMotion26707HighlightObserveFrames
+                        < MOTION_26707_HIGHLIGHT_OBSERVE_TIMEOUT_FRAMES) {
+                    return;
+                }
+                /* Bounded non-stalling fallback: accept only the actual physical compensation the
+                 * sensor reached. Never issue another darker request and never block the shutter. */
+                mMotion26707HighlightTargetSteps = observedCompSteps;
+                mMotion26707HighlightObservedProtectionEv = motion26707ObservedProtectionEv(
+                        observedCompSteps, mMotion26707HighlightStepEv);
+                mMotion26707HighlightTargetProtectionEv =
+                        mMotion26707HighlightObservedProtectionEv;
+                mMotion26707HighlightRequestIssued = false;
+                mMotion26707HighlightDecisionMade = true;
+                Log.w(TAG, "IRIS_26707_NORMAL_HIGHLIGHT_BOUNDED_TIMEOUT"
+                        + " baseSteps=" + mMotion26707HighlightBaseSteps
+                        + " observedSteps=" + observedCompSteps
+                        + " acceptedProtectionEv="
+                        + mMotion26707HighlightObservedProtectionEv
+                        + " darkerRetry=false shutterWaitAdded=false");
+                return;
+            } else if (!iris26705Settled) {
+                return;
+            } else {
+                mMotion26707HighlightObservedProtectionEv = motion26707ObservedProtectionEv(
+                        observedCompSteps, mMotion26707HighlightStepEv);
+                mMotion26707HighlightTargetProtectionEv =
+                        mMotion26707HighlightObservedProtectionEv;
+                mMotion26707HighlightRequestIssued = false;
+                mMotion26707HighlightDecisionMade = true;
+                Log.i(TAG, "IRIS_26707_NORMAL_HIGHLIGHT_OBSERVED"
+                        + " baseSteps=" + mMotion26707HighlightBaseSteps
+                        + " targetSteps=" + mMotion26707HighlightTargetSteps
+                        + " protectionEv=" + mMotion26707HighlightObservedProtectionEv
+                        + " stableFrames=" + mMotion26680StableFrames
+                        + " absoluteFromBaseline=true cumulativeRatchetImpossible=true");
+            }
+        }
+
+        if (!mMotion26707HighlightDecisionMade) {
+            if (!iris26705Settled) return;
+            android.util.Range<Integer> aeRange = mCameraCharacteristics.get(
+                    CameraCharacteristics.CONTROL_AE_COMPENSATION_RANGE);
+            if (aeRange == null || aeStep == null || aeStepEv <= 0.0f) {
+                mMotion26707HighlightDecisionMade = true;
+                mMotion26707HighlightTargetProtectionEv = 0.0f;
+            } else {
+                mMotion26707HighlightBaseReady = true;
+                mMotion26707HighlightBaseSteps = Math.max(
+                        aeRange.getLower(), Math.min(aeRange.getUpper(), observedCompSteps));
+                mMotion26707HighlightStepEv = aeStepEv;
+                float requestedProtectionEv = motion26707AbsoluteNormalHighlightProtectionEv(sensorTs);
+                int requestedProtectionSteps = Math.max(0, Math.round(
+                        requestedProtectionEv / aeStepEv));
+                int maximumProtectionSteps = Math.max(
+                        0, mMotion26707HighlightBaseSteps - aeRange.getLower());
+                requestedProtectionSteps = Math.min(
+                        requestedProtectionSteps, maximumProtectionSteps);
+                mMotion26707HighlightTargetSteps = Math.max(
+                        aeRange.getLower(), Math.min(aeRange.getUpper(),
+                                mMotion26707HighlightBaseSteps - requestedProtectionSteps));
+                mMotion26707HighlightTargetProtectionEv =
+                        requestedProtectionSteps * aeStepEv;
+
+                if (requestedProtectionSteps > 0) {
+                    try {
+                        mPreviewRequestBuilder.set(
+                                CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION,
+                                mMotion26707HighlightTargetSteps);
+                        mMotion26707HighlightRequestIssued = true;
+                        mMotion26707HighlightObserveFrames = 0;
+                        mMotion26680StableFrames = 0;
+                        mMotion26680LastExposureEnergy = Double.NaN;
+                        rebuildPreviewBuilder();
+                        Log.i(TAG, "IRIS_26707_NORMAL_HIGHLIGHT_TARGET"
+                                + " baseSteps=" + mMotion26707HighlightBaseSteps
+                                + " targetSteps=" + mMotion26707HighlightTargetSteps
+                                + " targetProtectionEv="
+                                + mMotion26707HighlightTargetProtectionEv
+                                + " rawP995=" + mMotion26608RawP995
+                                + " rawHighlightFraction="
+                                + mMotion26380RawHighlightFraction
+                                + " coherentCells="
+                                + mMotion26496RawCoherentHighlightCells
+                                + " peakQuadPhases="
+                                + mMotion26496RawPeakQuadHighlightPhases
+                                + " structuredBrightCells="
+                                + mMotion26608StructuredBrightCells
+                                + " structuredStrongBrightCells="
+                                + mMotion26608StructuredStrongBrightCells
+                                + " broad=" + mMotion26608BroadHdrConflict
+                                + " mixed=" + mMotion26608MixedHdrConflict
+                                + " compact=" + mMotion26608CompactHdrConflict
+                                + " absoluteFromBaseline=true oneShotPerSettledScene=true"
+                                + " maxProtectionEv="
+                                + MOTION_26707_NORMAL_HIGHLIGHT_STRONG_EV
+                                + " shutterWaitAdded=false");
+                    } catch (IllegalArgumentException | IllegalStateException e) {
+                        mMotion26707HighlightRequestIssued = false;
+                        mMotion26707HighlightDecisionMade = true;
+                        mMotion26707HighlightTargetProtectionEv = 0.0f;
+                        Log.w(TAG, "IRIS_26707 normal highlight request skipped: "
+                                + e.getClass().getSimpleName());
+                    }
+                    return;
+                }
+                mMotion26707HighlightDecisionMade = true;
+                mMotion26707HighlightObservedProtectionEv = 0.0f;
+                Log.i(TAG, "IRIS_26707_NORMAL_HIGHLIGHT_TARGET"
+                        + " baseSteps=" + mMotion26707HighlightBaseSteps
+                        + " targetSteps=" + mMotion26707HighlightBaseSteps
+                        + " targetProtectionEv=0.0"
+                        + " absoluteFromBaseline=true oneShotPerSettledScene=true");
+            }
+        }
+
+        if (!mMotion26707HighlightDecisionMade || !iris26705Settled) return;
         try {
             mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
             mMotion26680AeLockApplied = true;
@@ -2886,9 +3043,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     + " aeLocked=true awbLocked=" + mMotion26680AwbLockApplied
                     + " rawSceneContentCannotRewriteExposure=true"
                     + " flickerObserverOnly=true"
-                    + " iris26705NormalAeUntouched=true"
+                    + " iris26705NoUnconvergedForceLatch=true"
+                    + " iris26707AbsoluteNormalHighlightProtection=true"
+                    + " cumulativeHighlightRatchetImpossible=true"
                     + " shortHeadroomDecisionAtShutter=true"
-                    + " referenceProtectionEv=0.0"
+                    + " referenceProtectionEv="
+                    + mMotion26707HighlightObservedProtectionEv
                     + " shutterWaitAdded=false");
         } catch (IllegalArgumentException | IllegalStateException e) {
             mMotion26680PreviewLatched = false;
@@ -5835,18 +5995,85 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         Long previewTimestamp = normalReferenceResult.get(CaptureResult.SENSOR_TIMESTAMP);
         Long baseExp = normalReferenceResult.get(CaptureResult.SENSOR_EXPOSURE_TIME);
         Integer baseIso = normalReferenceResult.get(CaptureResult.SENSOR_SENSITIVITY);
-        final float iris26708ShortEv = previewTimestamp == null ? 0.0f
-                : motion26708FrozenShortProtectionEv(previewTimestamp, baseExp == null ? 0L : baseExp, baseIso == null ? 0 : baseIso);
-        final boolean sceneRequiresShort = iris26708ShortEv > 0.0f; ticket.sceneRequired = sceneRequiresShort;
-        Log.i(TAG, "IRIS_26708_FROZEN_SHORT_CLASS_DECISION completeNinePhaseOnly=true class="
-                + mMotion26708PublishedClass + " shortProtectionEv=" + iris26708ShortEv
-                + " requestShort=" + sceneRequiresShort + " normalAeUntouched=true");
-        if (!sceneRequiresShort || baseExp == null || baseExp <= 0L || baseIso == null || baseIso <= 0) return false;
-        final double iris26708ShortExposureDivisor = Math.pow(2.0, iris26708ShortEv);
+        long rawAgeNs = previewTimestamp == null || mMotion26380RawSignalTimestampNs <= 0L
+                ? Long.MAX_VALUE : Math.abs(previewTimestamp - mMotion26380RawSignalTimestampNs);
+        long highlightEvidenceAgeNs = previewTimestamp == null
+                || mMotion26496RecentHighlightEvidenceTimestampNs <= 0L
+                ? Long.MAX_VALUE
+                : Math.abs(previewTimestamp - mMotion26496RecentHighlightEvidenceTimestampNs);
+        long hdrConflictAgeNs = previewTimestamp == null
+                || mMotion26608RecentHdrConflictTimestampNs <= 0L
+                ? Long.MAX_VALUE
+                : Math.abs(previewTimestamp - mMotion26608RecentHdrConflictTimestampNs);
+        boolean legacyFractionTrigger =
+                mMotion26380RawHighlightFraction >= MOTION_26478_HIGHLIGHT_FRACTION_TRIGGER;
+        boolean currentSpatialTrigger =
+                mMotion26496RawHighlightSampleCount >= MOTION_26496_MIN_HIGHLIGHT_SAMPLES
+                        || mMotion26496RawCoherentHighlightCells > 0;
+        boolean recentSpatialTrigger =
+                highlightEvidenceAgeNs <= MOTION_26496_HIGHLIGHT_EVIDENCE_HOLD_NS;
+        boolean highlightTrigger = legacyFractionTrigger
+                || currentSpatialTrigger
+                || recentSpatialTrigger;
+        boolean currentDynamicRangeTrigger = mMotion26608CurrentHdrConflict;
+        boolean recentDynamicRangeTrigger =
+                hdrConflictAgeNs <= MOTION_26608_HDR_CONFLICT_HOLD_NS;
+        boolean universalHdrTrigger = highlightTrigger
+                || currentDynamicRangeTrigger
+                || recentDynamicRangeTrigger;
+        final boolean sceneRequiresShort =
+                rawAgeNs <= 180_000_000L && universalHdrTrigger;
+        ticket.sceneRequired = sceneRequiresShort;
+        Log.i(TAG, "IRIS_26496_SHORT_TRIGGER_DECISION"
+                + " rawAgeMs=" + (rawAgeNs == Long.MAX_VALUE ? -1L : rawAgeNs / 1_000_000L)
+                + " evidenceAgeMs=" + (highlightEvidenceAgeNs == Long.MAX_VALUE
+                        ? -1L : highlightEvidenceAgeNs / 1_000_000L)
+                + " fraction=" + mMotion26380RawHighlightFraction
+                + " highlightSamples=" + mMotion26496RawHighlightSampleCount
+                + " coherentCells=" + mMotion26496RawCoherentHighlightCells
+                + " peakQuadPhases=" + mMotion26496RawPeakQuadHighlightPhases
+                + " ditherIndex=" + mMotion26496RawDitherIndex
+                + " legacyFractionTrigger=" + legacyFractionTrigger
+                + " currentSpatialTrigger=" + currentSpatialTrigger
+                + " recentSpatialTrigger=" + recentSpatialTrigger
+                + " rawP50=" + mMotion26608RawP50
+                + " rawP90=" + mMotion26608RawP90
+                + " rawP99=" + mMotion26608RawP99
+                + " rawP995=" + mMotion26608RawP995
+                + " dynamicRangeEv=" + mMotion26608RawDynamicRangeEv
+                + " structuredBrightCells=" + mMotion26608StructuredBrightCells
+                + " structuredStrongBrightCells=" + mMotion26608StructuredStrongBrightCells
+                + " structuredPeakSecond=" + mMotion26608StructuredPeakSecond
+                + " broadHdrConflict=" + mMotion26608BroadHdrConflict
+                + " mixedHdrConflict=" + mMotion26608MixedHdrConflict
+                + " compactHdrConflict=" + mMotion26608CompactHdrConflict
+                + " hdrConflictAgeMs=" + (hdrConflictAgeNs == Long.MAX_VALUE
+                        ? -1L : hdrConflictAgeNs / 1_000_000L)
+                + " currentDynamicRangeTrigger=" + currentDynamicRangeTrigger
+                + " recentDynamicRangeTrigger=" + recentDynamicRangeTrigger
+                + " universalHdrExposureDecision=true"
+                + " requestShort=" + sceneRequiresShort);
+        if (!sceneRequiresShort
+                || baseExp == null || baseExp <= 0L || baseIso == null || baseIso <= 0) return false;
+
+        final float iris26705ExtraShortEv = motion26705ShortExtraProtectionEv(previewTimestamp);
+        final float iris26705TotalShortEv = MOTION_26480_SHORT_PROTECTION_EV
+                + iris26705ExtraShortEv;
+        final double iris26705ShortExposureDivisor = Math.pow(2.0, iris26705TotalShortEv);
         ticket.baselineEnergy = ExposureIndex.time2sec(baseExp) * baseIso;
-        Log.i(TAG, "IRIS_26708_ISOLATED_SHORT_TRANSACTION normalExposureNs=" + baseExp + " normalIso=" + baseIso
-                + " targetShortProtectionEv=" + iris26708ShortEv
-                + " repeatingPreviewMutation=false normalReferenceProtectionEv=0.0");
+        Log.i(TAG, "IRIS_26705_SHORT_ONLY_HEADROOM"
+                + " normalExposureNs=" + baseExp
+                + " normalIso=" + baseIso
+                + " baseShortEv=" + MOTION_26480_SHORT_PROTECTION_EV
+                + " extraShortEv=" + iris26705ExtraShortEv
+                + " totalShortEv=" + iris26705TotalShortEv
+                + " rawP995=" + mMotion26608RawP995
+                + " rawHighlightFraction=" + mMotion26380RawHighlightFraction
+                + " coherentCells=" + mMotion26496RawCoherentHighlightCells
+                + " broad=" + mMotion26608BroadHdrConflict
+                + " mixed=" + mMotion26608MixedHdrConflict
+                + " compact=" + mMotion26608CompactHdrConflict
+                + " normalAeUntouched=true");
         try {
             CaptureRequest.Builder b = mCameraDevice.createCaptureRequest(
                     CameraDevice.TEMPLATE_STILL_CAPTURE);
@@ -5873,7 +6100,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             }
 
             long reqExp = Math.max(1L,
-                    Math.round(baseExp / iris26708ShortExposureDivisor));
+                    Math.round(baseExp / iris26705ShortExposureDivisor));
             int reqIso = baseIso;
             android.util.Range<Long> er = mCameraCharacteristics.get(
                     CameraCharacteristics.SENSOR_INFO_EXPOSURE_TIME_RANGE);
@@ -5884,7 +6111,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             /* If the shutter hits the physical minimum before reaching the requested SHORT-only
              * headroom, spend ISO headroom next. Never increase ISO above the NORMAL reference. */
             final double iris26705TargetEnergy = ticket.baselineEnergy
-                    / iris26708ShortExposureDivisor;
+                    / iris26705ShortExposureDivisor;
             double iris26705ReqEnergy = ExposureIndex.time2sec(reqExp) * reqIso;
             if (sr != null && iris26705ReqEnergy > iris26705TargetEnergy * 1.02) {
                 int targetIso = (int) Math.round(iris26705TargetEnergy
@@ -5988,9 +6215,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                 + " requestedIso=" + requestedIso
                                 + " requestedRatio=" + requestedRatio
                                 + " requestedHeadroomEv=" + requestedHeadroomEv
-                                + " baseShortProtectionEv=" + iris26708ShortEv
-                                + " extraShortProtectionEv=" + 0.0f
-                                + " targetShortProtectionEv=" + iris26708ShortEv
+                                + " baseShortProtectionEv=" + MOTION_26480_SHORT_PROTECTION_EV
+                                + " extraShortProtectionEv=" + iris26705ExtraShortEv
+                                + " targetShortProtectionEv=" + iris26705TotalShortEv
                                 + " actualExposureNs=" + exp + " actualIso=" + iso
                                 + " ratio=" + ratio
                                 + " actualHeadroomEv=" + actualHeadroomEv
@@ -7013,10 +7240,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if(exp!=null&&exp>0L)frame.motionV2ActualExposureNs=exp;if(iso!=null&&iso>0)frame.motionV2ActualIso=iso;
         if(frame.motionV2ActualExposureNs>0L&&frame.motionV2ActualIso>0)
             frame.motionV2ExposureEnergy=ExposureIndex.time2sec(frame.motionV2ActualExposureNs)*frame.motionV2ActualIso;
-        /* IRIS_26708_NORMAL_REFERENCE_PROTECTION_ABSOLUTE_ZERO
-         * NORMAL is never intentionally underexposed by Motion HDR. SHORT exposure depth is
-         * represented by its own actual exposure/ISO metadata, not a fake NORMAL protection EV. */
-        frame.motionV2ReferenceProtectionEv = 0.0f;
+        /* IRIS_26707_ABSOLUTE_REFERENCE_PROTECTION_METADATA_TRUTH
+         * Carry the frame-exact TOTAL protection relative to the settled HAL/user baseline, never
+         * the last requested delta. Downstream FLOAT-domain canonicalization already restores this
+         * known acquisition EV exactly once before tone/UHDR. SHORT keeps its own radiometry and
+         * never masquerades as NORMAL reference protection. */
+        float exactProtectionEv = ts == null ? Float.NaN
+                : getMotion26663ReferencePreviewProtectionEv(ts);
+        frame.motionV2ReferenceProtectionEv = !shortRole && Float.isFinite(exactProtectionEv)
+                ? Math.max(0.0f, Math.min(
+                        MOTION_26707_NORMAL_HIGHLIGHT_STRONG_EV, exactProtectionEv))
+                : 0.0f;
         if(ts!=null)frame.motionV2ResultSensorTimestampNs=ts;frame.motionV2FrameNumber=result.getFrameNumber();
         if(skew!=null)frame.motionV2RollingShutterSkewNs=skew;if(focus!=null)frame.motionV2FocusDistanceDiopters=focus;
         if(lens!=null)frame.motionV2LensState=lens;
@@ -7336,7 +7570,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private static final class Motion26593CapturePlan {
         final int totalTargetFrames;
         final int normalTargetFrames;
-        volatile boolean shortExpected;
+        final boolean shortExpected;
         final boolean longExpected;
         final Motion26486ShortTicket shortTicket;
         final Motion26505LongTicket longTicket;
@@ -8050,9 +8284,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && !plan.shortTicket.slot.shadowAuxSlot.hasFrame()
                 && plan.longTicket.resultTimestampNs <= 0L;
         if (shortTerminalFailure) {
-            plan.shortExpected = false; plan.shortTicket.sceneRequired = false; plan.shortTicket.closeStaged();
-            clearMotion26490CaptureShortTicket(plan.shortTicket, "26708_terminal_short_failure_normal_only"); shortPresent = true;
-            Log.w(TAG, "IRIS_26708_SHORT_FAILURE_NORMAL_ONLY reason=terminal_failure normalReady=" + normalReadyCount + " captureFatal=false");
+            failMotion26593CapturePlan(plan, "SHORT_CAPTURE_FAILURE", "HDR highlight frame failed");
+            return;
         }
         if (longTerminalFailure) {
             failMotion26593CapturePlan(plan, "LONG_CAPTURE_FAILURE", "HDR shadow frame failed");
@@ -8097,14 +8330,14 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
 
         if (elapsed >= plan.completionTimeoutMs) {
-            if (plan.shortExpected && !shortPresent && normalReadyCount >= plan.normalTargetFrames && longPresent) {
-                plan.shortExpected = false; plan.shortTicket.sceneRequired = false; plan.shortTicket.closeStaged();
-                clearMotion26490CaptureShortTicket(plan.shortTicket, "26708_short_timeout_normal_only");
-                Log.w(TAG, "IRIS_26708_SHORT_FAILURE_NORMAL_ONLY reason=timeout normalReady=" + normalReadyCount + " captureFatal=false");
-                finalizeMotionZslCapture(plan); return;
-            }
-            String reason = plan.longExpected && !longPresent ? "LONG_CAPTURE_TIMEOUT" : "TOTAL_FRAME_TIMEOUT";
-            failMotion26593CapturePlan(plan, reason, "Motion frame capture timed out"); return;
+            String reason = plan.shortExpected && !shortPresent
+                    ? "SHORT_CAPTURE_TIMEOUT"
+                    : (plan.longExpected && !longPresent
+                            ? "LONG_CAPTURE_TIMEOUT" : "TOTAL_FRAME_TIMEOUT");
+            failMotion26593CapturePlan(plan, reason,
+                    plan.shortExpected && !shortPresent
+                            ? "HDR highlight frame timed out" : "Motion frame capture timed out");
+            return;
         }
         if (mBackgroundHandler == null) {
             failMotion26593CapturePlan(plan, "NO_BACKGROUND_HANDLER", "Motion capture unavailable");
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index 5e627d91a55401a802a935cbe3b93c099680e130..41e6ad75a1a924e4656a5dd0fe424c8f3c731c92 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -151,11 +151,9 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             }
 
             ArrayList<Float> previewLuma = collectPreviewMidtones(preview);
-            float referenceProtectionEv = 0.0f;
-            if (!iris26550Night && basePipeline.mParameters.motionV2ReferenceProtectionEv != 0.0f) {
-                throw new IllegalStateException("26708 Motion NORMAL referenceProtectionEv must be zero");
-            }
-            float referenceRestoreGain = 1.0f;
+            float referenceProtectionEv = iris26550Night ? 0.0f : clamp(
+                    basePipeline.mParameters.motionV2ReferenceProtectionEv, 0.0f, 1.50f);
+            float referenceRestoreGain = (float)Math.pow(2.0, referenceProtectionEv);
             ArrayList<RgbSample> candidate = collectCandidateSamples(
                     previousNode.WorkingTexture, preview.getWidth(), preview.getHeight(),
                     referenceRestoreGain);
@@ -438,13 +436,6 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             bytes.order(ByteOrder.nativeOrder());
             FloatBuffer fb = bytes.asFloatBuffer();
             ArrayList<RgbSample> all = new ArrayList<>();
-            ArrayList<RgbSample> bodyEligible = new ArrayList<>();
-            final byte[] shortAuthority = basePipeline.mParameters.motionV2ShortRadianceAuthorityMask;
-            final int shortAuthorityW = basePipeline.mParameters.motionV2ShortRadianceAuthorityWidth;
-            final int shortAuthorityH = basePipeline.mParameters.motionV2ShortRadianceAuthorityHeight;
-            final boolean hasShortAuthority = shortAuthority != null && shortAuthorityW > 0
-                    && shortAuthorityH > 0 && shortAuthority.length == shortAuthorityW * shortAuthorityH;
-            int excludedShortAuthoritySamples = 0;
             iris26584CandidateGuideGrid = new float[previewW * previewH];
             java.util.Arrays.fill(iris26584CandidateGuideGrid, Float.NaN);
             iris26584CandidateGridWidth = previewW;
@@ -459,17 +450,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                     b = Math.max(0.0f, b) * referenceRestoreGain;
                     float y = luma(r, g, b);
                     if (y > 1.0e-4f && y < 8.0f) {
-                        RgbSample sample = new RgbSample(r, g, b);
-                        all.add(sample);
-                        int px = pixelIndex % previewW;
-                        int py = pixelIndex / previewW;
-                        boolean shortOwned = false;
-                        if (hasShortAuthority && py < previewH) {
-                            int mx = Math.min(shortAuthorityW - 1, Math.max(0, px * shortAuthorityW / previewW));
-                            int my = Math.min(shortAuthorityH - 1, Math.max(0, py * shortAuthorityH / previewH));
-                            shortOwned = shortAuthority[my * shortAuthorityW + mx] != 0;
-                        }
-                        if (!shortOwned) bodyEligible.add(sample); else excludedShortAuthoritySamples++;
+                        all.add(new RgbSample(r, g, b));
                         if (pixelIndex < iris26584CandidateGuideGrid.length) {
                             iris26584CandidateGuideGrid[pixelIndex] = Math.max(r, Math.max(g, b));
                         }
@@ -503,18 +484,12 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                 iris26583CandidateP998Guide = iris26582CandidateGuides.get(p998);
             }
             ToneDecision iris26586MeterTone = iris26586ViewfinderMeterToneDecision(1.0f);
-            bodyEligible.sort(Comparator.comparingDouble(s ->
+            all.sort(Comparator.comparingDouble(s ->
                     presentedLuma(s, 1.0f, iris26586MeterTone.adaptiveSceneWhite)));
-            if (bodyEligible.isEmpty()) {
-                throw new IllegalStateException("26708 no NORMAL-owned body samples after SHORT authority exclusion");
-            }
-            int from = Math.max(0, Math.round((bodyEligible.size() - 1) * 0.25f));
-            int to = Math.min(bodyEligible.size(), Math.max(from + 1,
-                    Math.round((bodyEligible.size() - 1) * 0.50f) + 1));
-            Log.i(Name, "IRIS_26708_NORMAL_ONLY_BODY_SOLVER shortAuthorityExcluded="
-                    + excludedShortAuthoritySamples + " bodyEligible=" + bodyEligible.size()
-                    + " fullFusedUpperToneSamples=" + all.size() + " globalGainShortVote=false");
-            return new ArrayList<>(bodyEligible.subList(from, to));
+            int from = Math.max(0, Math.round((all.size() - 1) * 0.25f));
+            int to = Math.min(all.size(), Math.max(from + 1,
+                    Math.round((all.size() - 1) * 0.50f) + 1));
+            return new ArrayList<>(all.subList(from, to));
         } finally {
             if (probe != null) try { probe.close(); } catch (Throwable ignored) {}
             GLES30.glBindFramebuffer(GL_FRAMEBUFFER, oldFramebuffer[0]);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index c8082c6f5f5193e8d0c9a34a59767c5e07ad9b71..ea9262809420ec445b590ee26b9379b8ff82d29e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -154,9 +154,11 @@ object PhotonMotionMgc1271Bridge {
              * NORMAL reference into FLOAT-domain normalization/presentation. */
             longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
             shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
-            parameters.motionV2ReferenceProtectionEv = 0.0f
-            requireParity(parameters.irisNightActive || reference.motionV2ReferenceProtectionEv == 0.0f,
-                "26708 Motion NORMAL referenceProtectionEv must be absolute zero")
+            parameters.motionV2ReferenceProtectionEv = if (parameters.irisNightActive) {
+                0.0f
+            } else {
+                reference.motionV2ReferenceProtectionEv.coerceIn(0.0f, 1.50f)
+            }
             PLog.i(TAG, "IRIS_26674_PROTECTED_NORMAL_REFERENCE_HANDOFF " +
                 "night=${parameters.irisNightActive} referenceTimestamp=${mgcBase.timestamp} " +
                 "referenceFromOwnedZsl=${!parameters.irisNightActive} shadowLong=${longFrame != null} " +
@@ -380,17 +382,12 @@ object PhotonMotionMgc1271Bridge {
                 /* IRIS_26675_PROTECTED_NORMAL_SHORT_ADMISSION_CONTEXT
                  * Read-only downstream context. This value is already frozen from the selected
                  * 26674 NORMAL reference and cannot alter sensor/ZSL/capture exposure ownership. */
-                referenceProtectionEv = 0f,
+                referenceProtectionEv = if (parameters.irisNightActive) 0f
+                    else parameters.motionV2ReferenceProtectionEv.coerceIn(0.0f, 1.50f),
             )
             val iris26535FusionStartNs = System.nanoTime()
             val stacked = fusion.processFrames(frames)
                 ?: invalid("Iris Sabre owner returned null")
-            parameters.motionV2ShortRadianceAuthorityMask = stacked.shortRadianceAuthorityMask?.copyOf()
-            parameters.motionV2ShortRadianceAuthorityWidth = stacked.shortRadianceAuthorityWidth
-            parameters.motionV2ShortRadianceAuthorityHeight = stacked.shortRadianceAuthorityHeight
-            PLog.i(TAG, "IRIS_26708_SHORT_AUTHORITY_HANDOFF active=" +
-                (parameters.motionV2ShortRadianceAuthorityMask?.count { it.toInt() != 0 } ?: 0) +
-                " size=${parameters.motionV2ShortRadianceAuthorityWidth}x${parameters.motionV2ShortRadianceAuthorityHeight}")
             true2xLinearRgbPathForCleanup = stacked.true2xLinearRgbPath
             true2xRenderRgbPathForCleanup = stacked.true2xRenderRgbPath
             true2xPhaseSupportPathForCleanup = stacked.true2xPhaseSupportPath
@@ -679,9 +676,6 @@ object PhotonMotionMgc1271Bridge {
             parameters.motionV2MgcSourceExposureGain = 1.0f
             parameters.motionV2DisplayGain = 1.0f
             parameters.motionV2ShortHighlightRecoveryExecuted = false
-            parameters.motionV2ShortRadianceAuthorityMask = null
-            parameters.motionV2ShortRadianceAuthorityWidth = 0
-            parameters.motionV2ShortRadianceAuthorityHeight = 0
             PLog.i(TAG, "IRIS_26560_SABRE_SOURCE_DOMAIN_IDENTITY " +
                 "sourceDomainGain=1.0 displayGain=${parameters.motionV2DisplayGain} " +
                 "spatialSourceRestore=false solverAfterProfileColor=true")
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index 3b359674eace81614857bfd6af4ae9fb21b02867..951a635bad81263d828fecd045b1346e9c89f843 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -98,10 +98,6 @@ public class Parameters {
      * Diagnostic lifecycle state only. It must never alter per-pixel RCD mathematics.
      */
     public boolean motionV2ShortHighlightRecoveryExecuted = false;
-    /* IRIS_26708_COMPACT_SHORT_RADIANCE_AUTHORITY */
-    public byte[] motionV2ShortRadianceAuthorityMask = null;
-    public int motionV2ShortRadianceAuthorityWidth = 0;
-    public int motionV2ShortRadianceAuthorityHeight = 0;
     /* IRIS_26409_MOTION_V2_STATE */
     public boolean motionV2Active = false;
     /* IRIS_26545_V1_2_EXPLICIT_RECONSTRUCTION_OWNER
diff --git a/app/version.properties b/app/version.properties
index 7149cf60416e6db8ea07eed90930d779a0d84e71..e3668cb885f835ce5e220fee364ddc420ccae85b 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726708
-VERSION_BUILD=26708
+VERSION_NAME=0.9726707
+VERSION_BUILD=26707
