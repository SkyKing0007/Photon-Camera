diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
index dd10e6fa576c299b50c3cf3fcd557c84e3d6ffc2..0abd6b96d0fae49ac61b18f2134dcc88fea2b91a 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
@@ -30,6 +30,7 @@ internal class GlesIris26545SabreProcessor(
     private val exportNormalStackedDng: Boolean,
     private val vgnChromaCorrectionStrength: Float = 1f,
     private val allowShadowLong: Boolean = false,
+    private val preserveExtendedHdrThroughVgn: Boolean = false,
     private val enableSuperRes: Boolean = false,
     private val superResTempDir: File? = null,
 ) {
@@ -87,8 +88,9 @@ internal class GlesIris26545SabreProcessor(
             TAG,
             "IRIS_26587_SABRE_OWNER_ACTIVE base=${frames[baseIndex].frameNumber} " +
                 "normal=$normalCount shadowLong=$longCount highlightShort=$highlightShortCount " +
-                "allowShadowLong=$allowShadowLong excluded=${excluded.size} " +
-                "normalMergeOwner=true shortReferenceImmutable=true spatial=false bento=false output=linear-rgb " +
+                "allowShadowLong=$allowShadowLong preserveExtendedHdrThroughVgn=$preserveExtendedHdrThroughVgn " +
+                "excluded=${excluded.size} normalMergeOwner=true shortReferenceImmutable=true " +
+                "spatial=false bento=false output=linear-rgb " +
                 "superRes=$enableSuperRes srEvidence=NORMAL_ONLY shortContribution=NORMAL_ACCUMULATOR_EXCLUDED",
         )
         /* IRIS_26545_V1_2_SABRE_LOW_LEVEL_ISOLATION
@@ -117,7 +119,7 @@ internal class GlesIris26545SabreProcessor(
             processorPipeline = MgcRawProcessorPipeline.SABRE,
             exportNormalStackedDng = exportNormalStackedDng,
             vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
-            preserveExtendedHdrThroughVgn = !allowShadowLong &&
+            preserveExtendedHdrThroughVgn = preserveExtendedHdrThroughVgn &&
                 exportGpuLinearRgbSource && gpuLinearRgbStorage == GpuLinearRgbStorage.RGBA16F,
             enableSabreSuperRes = enableSuperRes,
             sabreSuperResTempDir = superResTempDir,
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
index 925aad1f7171a71cd0c09016af4e964de1d9aaaa..9d809af04b37634e346036220b6749819f3abfd7 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
@@ -22,6 +22,7 @@ internal class GlesMgcRawFusion(
     private val lensShadingWidth: Int,
     private val lensShadingHeight: Int,
     private val allowSabreShadowLong: Boolean = false,
+    private val preserveExtendedHdrThroughVgn: Boolean = false,
     private val useCurrentGlContext: Boolean,
     private val exportGpuLinearRgbSource: Boolean,
     private val gpuLinearRgbStorage: GpuLinearRgbStorage,
@@ -54,6 +55,7 @@ internal class GlesMgcRawFusion(
             exportNormalStackedDng = exportNormalStackedDng,
             vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
             allowShadowLong = allowSabreShadowLong,
+            preserveExtendedHdrThroughVgn = preserveExtendedHdrThroughVgn,
             enableSuperRes = enableSabreSuperRes,
             superResTempDir = sabreSuperResTempDir,
         ).processFrames(frames)
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 47a2a37f28d2b060b4fa560ee3ab920fcafe053e..8ef6ec0367808912638203b2081bba15e74d18e4 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2284,10 +2284,10 @@ internal class GlesMgcRawSpatialStacker(
             } else {
                 0
             }
-            /* IRIS_26547_V1_1_MOTION_DNG_26546_PRESERVATION
-             * Motion has no SHADOW_LONG frames, so keep its exact 26546 Sabre DNG support owner:
-             * the shared measured RGB coverage + measured average merge factor. Only bracketed Night
-             * allocates a second NORMAL-only coverage map so Long support can never leak into DNG.
+            /* IRIS_26658_GOOGLE_BRACKET_DNG_PRESERVATION
+             * Whenever a SHADOW_LONG exists (Motion bracket or Night), allocate a second
+             * NORMAL-only coverage map so Long support can never leak into stacked DNG statistics.
+             * Non-bracketed Motion retains the exact shared measured RGB coverage behavior.
              */
             val normalDngCoverage = if (
                 exportNormalStackedDng && (shadowLongFrameCount > 0 || highlightShortFrameCount > 0)
@@ -2543,6 +2543,56 @@ internal class GlesMgcRawSpatialStacker(
                     GLES30.GL_LINEAR,
                 )
                 renderDilation(reverseWeight, frameWeight)
+                /* IRIS_26658_LONG_FINAL_WEIGHT_PROOF
+                 * Diagnostic-only proof of post-source-clipping LONG eligibility. Reuse the exact
+                 * bracket coverage shader on an isolated R8 target; never feed this proof target
+                 * back into the production accumulator.
+                 */
+                if (frame.role == RawBurstFrameRole.SHADOW_LONG) {
+                    val longProof = createTexture(
+                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR,
+                    )
+                    clearSabreCoverage(
+                        longProof, coverageWidth, coverageHeight,
+                        "26658 LONG final-weight proof clear",
+                    )
+                    renderSabreUnifiedBracketCoverage26604(
+                        weight = frameWeight,
+                        extracted = currentExtracted,
+                        flow = flow,
+                        accumulatedCoverage = longProof,
+                        coverageWidth = coverageWidth,
+                        coverageHeight = coverageHeight,
+                        extractedWidth = extractedWidth,
+                        extractedHeight = extractedHeight,
+                        accumulatedWeightScale = accumulatedWeightScale,
+                    )
+                    val proofBytes = readR8Mask(
+                        longProof, "26658 LONG post-source-clip final weight",
+                        coverageWidth, coverageHeight,
+                    )
+                    var nonzero = 0
+                    var sum = 0L
+                    var peak = 0
+                    for (raw in proofBytes) {
+                        val value = raw.toInt() and 0xff
+                        if (value > 0) nonzero++
+                        sum += value.toLong()
+                        if (value > peak) peak = value
+                    }
+                    val cells = proofBytes.size.coerceAtLeast(1)
+                    val details26658 =
+                        "frame=$index cells=${proofBytes.size} finalWeightNonzero=$nonzero " +
+                            "nonzeroPct=${100.0 * nonzero.toDouble() / cells.toDouble()} " +
+                            "meanWeight=${sum.toDouble() / (255.0 * cells.toDouble())} " +
+                            "peakWeight=${peak / 255.0} postSourceClip=true " +
+                            "productionAccumulatorUnmodifiedByProof=true"
+                    PLog.i(SABRE_TAG, "IRIS_26658_LONG_FINAL_WEIGHT_PROOF $details26658")
+                    MotionTrace.processingState(
+                        "IRIS_26658_LONG_FINAL_WEIGHT_PROOF", details26658,
+                    )
+                    releaseOwnedTexture(longProof, "26658 LONG final-weight proof")
+                }
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     check(shortPhysicalReverseWeight26610 != 0) {
                         "26648 SHORT physical reverse weight missing"
@@ -2561,7 +2611,7 @@ internal class GlesMgcRawSpatialStacker(
                 }
                 /* IRIS_26648_NORMAL_TEMPORAL_STACK_SHORT_EXCLUDED
                  * HIGHLIGHT_SHORT does not alter accumulatedColor/weights/coverage. NORMAL and
-                 * Night SHADOW_LONG retain the successful 26646 temporal architecture; SHORT is
+                 * Motion/Night SHADOW_LONG retain the proven temporal architecture; SHORT is
                  * retained only for the later universal radiometric owner. */
                 if (frame.role != RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     if (highlightShortOneTunnel26604 || frame.role == RawBurstFrameRole.SHADOW_LONG) {
@@ -2612,10 +2662,10 @@ internal class GlesMgcRawSpatialStacker(
                     PLog.i(SABRE_TAG, "IRIS_26651_SHORT_CAPTURE_TO_FUSION $details26648")
                     MotionTrace.processingState("IRIS_26651_SHORT_CAPTURE_TO_FUSION", details26648)
                 }
-                /* IRIS_26564_NIGHT_LONG_EXCLUDED_FROM_TRUE_2X
-                 * Native Sabre still admits validated SHADOW_LONG evidence for Night SNR. True 2x
-                 * spatial reconstruction accepts NORMAL frames only, preserving the proven rule
-                 * that neither bracket exposure can become high-frequency SR evidence.
+                /* IRIS_26658_LONG_EXCLUDED_FROM_TRUE_2X
+                 * Native Sabre admits validated SHADOW_LONG evidence for Motion/Night shadow SNR.
+                 * True 2x spatial reconstruction remains NORMAL-only so bracket LONG never becomes
+                 * high-frequency SR evidence.
                  */
                 if (enableSabreSuperRes && frame.role == RawBurstFrameRole.NORMAL) {
                     val candidateEvidence = persistTrue2xEvidence(
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index 52cf09fa30ed03d3752d55b9a12d93271f961e2c..83cf899570cce3c6df387f928488cee73cc583de 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -4008,7 +4008,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 PhotonCamera.getSettings().binning);
         populateMotion26480FrameMetadata(frame, result, false);
         frame.motionV2ShortHighlightFrame = false;
-        frame.motionV2FrameRole = ImageFrame.MotionV2FrameRole.NORMAL;
+        frame.motionV2FrameRole = ImageFrame.MotionV2FrameRole.SHADOW_LONG;
         return frame;
     }
 
@@ -4031,7 +4031,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         }
         populateMotion26480FrameMetadata(staged, exactResult, false);
         staged.motionV2ShortHighlightFrame = false;
-        staged.motionV2FrameRole = ImageFrame.MotionV2FrameRole.NORMAL;
+        staged.motionV2FrameRole = ImageFrame.MotionV2FrameRole.SHADOW_LONG;
         staged.motionV2ActualExposureNs = ticket.actualExposureNs;
         staged.motionV2ActualIso = ticket.actualIso;
         staged.motionV2ExposureEnergy = ticket.actualEnergy;
@@ -4044,7 +4044,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 + " accepted=" + accepted
                 + " timestamp=" + ticket.resultTimestampNs
                 + " exactTimestampEquality=true exactMetadata=true"
-                + " normalAccumulatorAdmission=false");
+                + " commonSabreAdmission=SHADOW_LONG_DOWNSTREAM");
         return accepted;
     }
 
@@ -4080,7 +4080,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && exact != null) {
             populateMotion26480FrameMetadata(copy, exact, false);
             copy.motionV2ShortHighlightFrame = false;
-            copy.motionV2FrameRole = ImageFrame.MotionV2FrameRole.NORMAL;
+            copy.motionV2FrameRole = ImageFrame.MotionV2FrameRole.SHADOW_LONG;
             copy.motionV2ActualExposureNs = ticket.actualExposureNs;
             copy.motionV2ActualIso = ticket.actualIso;
             copy.motionV2ExposureEnergy = ticket.actualEnergy;
@@ -4091,7 +4091,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     + " accepted=" + accepted
                     + " timestamp=" + ts
                     + " exactTimestampEquality=true exactResultAlreadyKnown=true"
-                    + " normalAccumulatorAdmission=false");
+                    + " commonSabreAdmission=SHADOW_LONG_DOWNSTREAM");
             return true;
         }
 
@@ -4104,12 +4104,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         return true;
     }
 
-    /* IRIS_26505_PHYSICAL_LONG_BRACKET
-     * Bjzhou-consistency principle, adapted without replacing Wronski: request one
-     * separate longer RAW around +2.5 EV in exposure energy. Prefer shutter up to
-     * ~10 ms (or the already-longer base shutter), then ISO. Actual CaptureResult
-     * metadata, not requested metadata, decides admission. The request never gates
-     * the shutter and the frame can only enter ShadowAuxSlot.
+    /* IRIS_26658_GOOGLE_HDR_BRACKET_LONG_CAPTURE
+     * Keep the existing ZSL group as the reference/short exposure and request one
+     * separate longer RAW around +2.5 EV for shadow/SNR evidence. Spend the bracket
+     * on real integration time first, up to ~1/15 s, then ISO only for residual EV.
+     * Actual CaptureResult metadata decides admission. The request never gates the
+     * shutter and the frame can only enter ShadowAuxSlot.
      */
     private boolean applyMotion26505ExplicitLongCaptureIfUseful(
             @NonNull Motion26505LongTicket ticket,
@@ -4151,11 +4151,19 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if (er == null || sr == null) return false;
 
         double targetEnergy = ticket.baselineEnergy * MOTION_26505_LONG_TARGET_MULTIPLIER;
-        long maxLongExp = Math.min(er.getUpper(),
-                Math.max(MOTION_26505_LONG_PREFERRED_MAX_EXPOSURE_NS, baseExp));
+        long maxLongExp = Math.min(er.getUpper(), MOTION_26658_LONG_MAX_EXPOSURE_NS);
         maxLongExp = Math.max(er.getLower(), maxLongExp);
+        if (maxLongExp <= Math.round(baseExp * MOTION_26658_LONG_MIN_SHUTTER_RATIO)) {
+            Log.i(TAG, "IRIS_26658_LONG_SKIPPED_NO_PHOTON_GAIN"
+                    + " baseExposureNs=" + baseExp
+                    + " maxLongExposureNs=" + maxLongExp
+                    + " referenceRole=ZSL_REFERENCE_SHORT"
+                    + " isoOnlyLongForbidden=true");
+            return false;
+        }
         long reqExp = Math.round(baseExp * MOTION_26505_LONG_TARGET_MULTIPLIER);
         reqExp = Math.max(er.getLower(), Math.min(maxLongExp, reqExp));
+        if (!(reqExp > baseExp * MOTION_26658_LONG_MIN_SHUTTER_RATIO)) return false;
 
         int isoUpper = Math.min(sr.getUpper(),
                 Math.max(baseIso, MOTION_26505_LONG_FALLBACK_MAX_ISO));
@@ -4168,7 +4176,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
 
         final double requestedEnergy = ExposureIndex.time2sec(reqExp) * reqIso;
         final double requestedRatio = requestedEnergy / ticket.baselineEnergy;
-        if (!(requestedEnergy > ticket.baselineEnergy * MOTION_26505_LONG_MIN_ACTUAL_RATIO)) {
+        final double requestedShutterRatio = reqExp / (double) baseExp;
+        if (!(requestedEnergy > ticket.baselineEnergy * MOTION_26505_LONG_MIN_ACTUAL_RATIO)
+                || !(requestedShutterRatio > MOTION_26658_LONG_MIN_SHUTTER_RATIO)) {
             Log.w(TAG, "IRIS_26505_LONG_REQUEST_NO_PHYSICAL_GAIN"
                     + " baselineEnergy=" + ticket.baselineEnergy
                     + " targetEnergy=" + targetEnergy
@@ -4239,7 +4249,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     }
                     double energy = ExposureIndex.time2sec(exp) * iso;
                     double ratio = energy / ticket.baselineEnergy;
+                    double shutterRatio = exp / (double) baseExp;
                     boolean accepted = energy > ticket.baselineEnergy * MOTION_26505_LONG_MIN_ACTUAL_RATIO
+                            && shutterRatio > MOTION_26658_LONG_MIN_SHUTTER_RATIO
                             && ratio >= requestedRatioMin && ratio <= requestedRatioMax;
                     if (accepted) {
                         ticket.resultTimestampNs = ts;
@@ -4255,11 +4267,13 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                 + " actualExposureNs=" + exp
                                 + " actualIso=" + iso
                                 + " actualRatio=" + ratio
+                                + " requestedShutterRatio=" + requestedShutterRatio
+                                + " actualShutterRatio=" + shutterRatio
                                 + " actualDeltaEv=" + actualDeltaEv
                                 + " targetDeltaEv=" + MOTION_26505_LONG_TARGET_EV
                                 + " allowedAroundClampedRequest="
                                 + requestedRatioMin + ".." + requestedRatioMax
-                                + " normalAccumulatorAdmission=false shutterGate=false");
+                                + " normalAccumulatorAdmission=GOOGLE_BRACKET_COMMON_SABRE shutterGate=false");
                         tryDeliverMotion26505StagedLongRaw(ticket, result);
                     } else {
                         ticket.closeStaged();
@@ -4287,7 +4301,10 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     + " requestedExposureNs=" + requestedExp
                     + " requestedIso=" + requestedIso
                     + " requestedRatio=" + requestedRatio
-                    + " preferredMaxShutterNs=" + MOTION_26505_LONG_PREFERRED_MAX_EXPOSURE_NS
+                    + " requestedShutterRatio=" + requestedShutterRatio
+                    + " maxShutterNs=" + MOTION_26658_LONG_MAX_EXPOSURE_NS
+                    + " shutterFirst=true isoOnlyLongForbidden=true"
+                    + " referenceRole=ZSL_REFERENCE_SHORT"
                     + " normalRingCleared=false previewRebuilt=false shutterGate=false");
             if (mBackgroundHandler != null) {
                 mBackgroundHandler.postDelayed(() -> {
@@ -5716,8 +5733,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private static final double MOTION_26505_LONG_TARGET_EV = 2.5;
     private static final double MOTION_26505_LONG_TARGET_MULTIPLIER =
             5.656854249492381;
-    private static final long MOTION_26505_LONG_PREFERRED_MAX_EXPOSURE_NS =
-            10_000_000L;
+    private static final long MOTION_26658_LONG_MAX_EXPOSURE_NS = 66_666_667L;
+    private static final double MOTION_26658_LONG_MIN_SHUTTER_RATIO = 1.15;
     private static final int MOTION_26505_LONG_FALLBACK_MAX_ISO = 800;
     private static final double MOTION_26505_LONG_MIN_ACTUAL_RATIO = 1.15;
     private static final double MOTION_26505_LONG_REQUEST_TOLERANCE_EV = 0.40;
@@ -6541,30 +6558,25 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             clearMotion26505LongTicket(iris26505PreviousLongTicket, "new_generation");
         }
 
+        /* IRIS_26658_GOOGLE_HDR_BRACKETING_CAPTURE_OWNER
+         * Motion no longer captures a second post-NORMAL HIGHLIGHT_SHORT repair exposure.
+         * The existing equal-exposure ZSL group is the highlight-safe/reference group relative
+         * to the existing +EV SHADOW_LONG capture. Keep ShortHighlightSlot only as the immutable
+         * generation-owned container for SHADOW_LONG so MotionBatch/DNG/Night ownership remains
+         * unchanged. Capture AE/shutter/ISO policy for the ZSL group is deliberately untouched.
+         */
         final Motion26486ShortTicket iris26486ShortTicket = new Motion26486ShortTicket();
         mMotion26486CaptureShortTicket = iris26486ShortTicket;
-        final boolean iris26593ShortBudgetAllows = iris26593TotalTarget >= 2;
-        final boolean iris26593ShortSubmitted = iris26593ShortBudgetAllows
-                && applyMotion26486ExplicitShortCaptureIfNeeded(
-                        iris26486ShortTicket, iris26593NormalReference);
-        final boolean iris26480ShortHighlightRequested = iris26593ShortBudgetAllows
-                && iris26486ShortTicket.sceneRequired;
-        if (iris26480ShortHighlightRequested && !iris26593ShortSubmitted) {
-            Log.e(TAG, "IRIS_26593_REQUIRED_SHORT_NOT_SUBMITTED"
-                    + " totalTarget=" + iris26593TotalTarget
-                    + " ticketCompleted=" + iris26486ShortTicket.completed
-                    + " silentNormalOnlyForbidden=true");
-        }
-        if (!iris26480ShortHighlightRequested && !iris26486ShortTicket.requested) {
-            clearMotion26490CaptureShortTicket(iris26486ShortTicket,
-                    iris26593ShortBudgetAllows ? "not_requested" : "total_budget_requires_normal");
-        }
+        final boolean iris26593ShortBudgetAllows = false;
+        final boolean iris26593ShortSubmitted = false;
+        final boolean iris26480ShortHighlightRequested = false;
+        clearMotion26490CaptureShortTicket(iris26486ShortTicket,
+                "26658_google_hdr_bracketing_zsl_reference_group");
 
         final Motion26505LongTicket iris26505LongTicket = new Motion26505LongTicket(
                 iris26486ShortTicket.slot.shadowAuxSlot);
         mMotion26505CaptureLongTicket = iris26505LongTicket;
-        final int iris26593RemainingAfterShort = iris26593TotalTarget
-                - (iris26480ShortHighlightRequested ? 1 : 0);
+        final int iris26593RemainingAfterShort = iris26593TotalTarget;
         final boolean iris26593LongBudgetAllows = iris26593RemainingAfterShort >= 2;
         final boolean iris26505LongRequested = iris26593LongBudgetAllows
                 && applyMotion26505ExplicitLongCaptureIfUseful(iris26505LongTicket, iris26593NormalReference);
@@ -6573,8 +6585,14 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     iris26593LongBudgetAllows ? "not_requested" : "total_budget_requires_normal");
         }
 
-        final int iris26593AuxCount = (iris26480ShortHighlightRequested ? 1 : 0)
-                + (iris26505LongRequested ? 1 : 0);
+        final int iris26593AuxCount = (iris26505LongRequested ? 1 : 0);
+        Log.i(TAG, "IRIS_26658_GOOGLE_HDR_BRACKETING_CAPTURE_PLAN"
+                + " zslReferenceGroup=true"
+                + " preserveExistingZslExposurePolicy=true"
+                + " postNormalHighlightShortRepair=false"
+                + " shadowLongRequested=" + iris26505LongRequested
+                + " totalTarget=" + iris26593TotalTarget
+                + " normalTarget=" + (iris26593TotalTarget - iris26593AuxCount));
         final int iris26593NormalTarget = iris26593TotalTarget - iris26593AuxCount;
         if (iris26593NormalTarget < 1) {
             iris26598PreShutterNormals.closeAll();
@@ -7127,7 +7145,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 img.close();
                 Log.i(TAG, "IRIS_26498_V13_SHADOW_AUX_ORIGINAL_IMAGE_CONSUMED"
                         + " timestamp=" + irisV13ShadowAuxTimestamp
-                        + " copiedBeforePrefixClose=true normalAccumulatorAdmission=false");
+                        + " copiedBeforePrefixClose=true separateShadowAux=true commonSabreDownstream=true");
                 continue;
             }
             boolean iris26486TaggedShort = frameResult != null
@@ -7326,7 +7344,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             Log.i(TAG, "IRIS_26498_V13_SHADOW_AUX_BATCH_DELIVERY"
                     + " accepted=" + shadowAccepted
                     + " timestamp=" + shadowTs
-                    + " normalAccumulatorAdmission=false separateAuxSlot=true");
+                    + " separateAuxSlot=true commonSabreDownstream=true");
             irisV13ShadowAuxFrame = null;
         }
         if (iris26480ShortFrame != null) {
@@ -7573,7 +7591,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 + " shadowAuxPresent=" + iris26486ShortSlot.shadowAuxSlot.hasFrame()
                 + " superResFrozen=" + motionBatch.superResEnabled
                 + " heicFrozen=" + motionBatch.heicOutputEnabled
-                + " shortNeverNormalFusion=true shadowNeverNormalFusion=true");
+                + " shortNeverNormalFusion=true shadowAuxSeparateFromZslList=true shadowCommonSabreDownstream=true");
         mMotion26480ShortRequested = false;
         mMotion26480ShortRequestCompleted = false;
         mMotion26480ShortResultTimestampNs = 0L;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index f864177ba15eb061925feb07e8fb5e17639af8c9..b3ce8d243b0f81bd36d7013549b007ffb212b839 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -105,28 +105,55 @@ object PhotonMotionMgc1271Bridge {
                 "longExpected=$iris26593LongExpected longPresent=${longFrame != null} " +
                 "normal=${inputImages.size} total=${inputImages.size + (if (shortFrame != null) 1 else 0) + (if (longFrame != null) 1 else 0)}")
 
-            // The pinned 1.27.1 GlesMgcRawFusion contract chooses the first supplied NORMAL
-            // as its base, preserves the remaining NORMAL order, then admits valid Long and the
-            // one lowest-TET Short. HdrxProcessor has already timestamp-sorted the Normal list.
-            // Do not impose the old Wronski nearest-reference ordering here.
-            val mgcBase = inputImages.firstOrNull { it.motionV2FrameRole == ImageFrame.MotionV2FrameRole.NORMAL }
-                ?: invalid("MGC SHORT/NORMAL base is absent")
+            /* IRIS_26658_GOOGLE_HDR_BRACKETING_REFERENCE_OWNER
+             * Motion follows the 2021 HDR+ bracketing topology: the timestamp-owned pre-shutter
+             * ZSL frame is the structural/reference exposure and SHADOW_LONG is auxiliary shadow
+             * evidence. Night keeps its exact prior ordering/role policy. The old post-NORMAL
+             * HIGHLIGHT_SHORT repair exposure is forbidden on the Motion active path.
+             */
+            if (!parameters.irisNightActive) {
+                requireParity(shortFrame == null,
+                    "26658 Motion HIGHLIGHT_SHORT repair survived Google-bracketing conversion")
+            }
+            val mgcBase = if (parameters.irisNightActive) {
+                inputImages.firstOrNull { it.motionV2FrameRole == ImageFrame.MotionV2FrameRole.NORMAL }
+                    ?: invalid("MGC Night NORMAL base is absent")
+            } else {
+                reference
+            }
             val orderedPhysical = ArrayList<Pair<ImageFrame, RawBurstFrameRole>>()
-            inputImages.forEachIndexed { index, frame ->
-                val role = when (frame.motionV2FrameRole) {
-                    ImageFrame.MotionV2FrameRole.NORMAL -> RawBurstFrameRole.NORMAL
-                    ImageFrame.MotionV2FrameRole.SHADOW_LONG -> {
-                        requireParity(parameters.irisNightActive,
-                            "main-list SHADOW_LONG is Night-only index=$index")
-                        RawBurstFrameRole.SHADOW_LONG
+            if (!parameters.irisNightActive) {
+                orderedPhysical += reference to RawBurstFrameRole.NORMAL
+                inputImages.forEachIndexed { index, frame ->
+                    if (frame === reference) return@forEachIndexed
+                    when (frame.motionV2FrameRole) {
+                        ImageFrame.MotionV2FrameRole.NORMAL ->
+                            orderedPhysical += frame to RawBurstFrameRole.NORMAL
+                        ImageFrame.MotionV2FrameRole.SHADOW_LONG ->
+                            invalid("main-list SHADOW_LONG is forbidden for Motion index=$index")
+                        ImageFrame.MotionV2FrameRole.HIGHLIGHT_SHORT ->
+                            invalid("main-list HIGHLIGHT_SHORT is forbidden for Motion index=$index")
                     }
-                    ImageFrame.MotionV2FrameRole.HIGHLIGHT_SHORT ->
-                        invalid("main-list HIGHLIGHT_SHORT is forbidden; Motion Short keeps isolated slot ownership")
                 }
-                orderedPhysical += frame to role
+            } else {
+                inputImages.forEachIndexed { index, frame ->
+                    val role = when (frame.motionV2FrameRole) {
+                        ImageFrame.MotionV2FrameRole.NORMAL -> RawBurstFrameRole.NORMAL
+                        ImageFrame.MotionV2FrameRole.SHADOW_LONG -> RawBurstFrameRole.SHADOW_LONG
+                        ImageFrame.MotionV2FrameRole.HIGHLIGHT_SHORT ->
+                            invalid("main-list HIGHLIGHT_SHORT is forbidden; isolated slot ownership only index=$index")
+                    }
+                    orderedPhysical += frame to role
+                }
             }
             longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
-            shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
+            if (parameters.irisNightActive) {
+                shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
+            }
+            PLog.i(TAG, "IRIS_26658_GOOGLE_HDR_BRACKETING_REFERENCE_OWNER " +
+                "night=${parameters.irisNightActive} referenceTimestamp=${mgcBase.timestamp} " +
+                "referenceFromOwnedZsl=${!parameters.irisNightActive} shadowLong=${longFrame != null} " +
+                "highlightShortRepair=${shortFrame != null}")
 
             orderedPhysical.forEachIndexed { index, (frame, role) ->
                 validateFrame(frame, size, role, reference.motionV2ExposureEnergy, index)
@@ -138,7 +165,7 @@ object PhotonMotionMgc1271Bridge {
                         RawBurstFrameRole.SHADOW_LONG -> if (parameters.irisNightActive) {
                             "COMMON_SABRE_NIGHT_SHADOW_EVIDENCE"
                         } else {
-                            "CAPTURED_BUT_EXCLUDED_FROM_NORMAL_MOTION_SABRE"
+                            "COMMON_SABRE_MOTION_GOOGLE_BRACKET_SHADOW_EVIDENCE"
                         }
                         else -> "NORMAL"
                     }
@@ -332,7 +359,8 @@ object PhotonMotionMgc1271Bridge {
                 lensShading = lsc,
                 lensShadingWidth = lscWidth,
                 lensShadingHeight = lscHeight,
-                allowSabreShadowLong = parameters.irisNightActive,
+                allowSabreShadowLong = parameters.irisNightActive || longFrame != null,
+                preserveExtendedHdrThroughVgn = !parameters.irisNightActive,
                 useCurrentGlContext = true,
                 exportGpuLinearRgbSource = true,
                 gpuLinearRgbStorage = GpuLinearRgbStorage.RGBA16F,
@@ -407,20 +435,16 @@ object PhotonMotionMgc1271Bridge {
                 "unexpected GPU RGB storage=${gpu.storage} samples=${gpu.samplesPerPixel}")
             requireParity(gpu.textureId != 0, "zero GPU RGB texture")
             requireParity(stacked.mergedFrameCount >= 1, "mergedFrameCount=${stacked.mergedFrameCount}")
-            /* IRIS_26589_MOTION_AUX_MERGED_COUNT_PARITY
-             * Motion may capture both a Short and a Long auxiliary frame. Sabre intentionally
-             * admits Motion Short only as isolated highlight evidence and intentionally excludes
-             * Motion Long because allowSabreShadowLong is Night-only. mergedFrameCount therefore
-             * tracks NORMAL frames plus Night-only SHADOW_LONG frames; neither auxiliary Motion
-             * capture may inflate this proof count. This is the exact 26588 device-runtime failure
-             * regression: 15 NORMAL + 1 Motion Long + 1 Motion Short must expect merged=15, not 16.
+            /* IRIS_26658_GOOGLE_BRACKET_MERGED_COUNT_PARITY
+             * Motion SHADOW_LONG is now legitimate common-accumulator evidence and therefore must
+             * be included in mergedFrameCount. HIGHLIGHT_SHORT remains excluded from Motion and
+             * may not inflate this proof. Night keeps its existing NORMAL+SHADOW_LONG accounting.
              */
             val expectedMergedFrames26589 = frames.count { frame ->
-                frame.role == RawBurstFrameRole.NORMAL ||
-                    (parameters.irisNightActive && frame.role == RawBurstFrameRole.SHADOW_LONG)
+                frame.role == RawBurstFrameRole.NORMAL || frame.role == RawBurstFrameRole.SHADOW_LONG
             }
             requireParity(stacked.mergedFrameCount == expectedMergedFrames26589,
-                "26589 auxiliary role leaked into mergedFrameCount merged=${stacked.mergedFrameCount} " +
+                "26658 bracket role mismatch merged=${stacked.mergedFrameCount} " +
                     "expected=$expectedMergedFrames26589 night=${parameters.irisNightActive}")
             if (!parameters.irisNightActive && shortFrame != null) {
                 /* IRIS_26607_SEMANTIC_STATE_CONTRACT
diff --git a/app/version.properties b/app/version.properties
index 0813a9b5204b5477da933a144524c8d17126a88f..6697c3a97928f4ee40f3e52ef0ef33923c7342bf 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726653
-VERSION_BUILD=26653
+VERSION_NAME=0.9726658
+VERSION_BUILD=26658
