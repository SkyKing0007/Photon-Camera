26603 permanent regressions — one-tunnel bracket reconstruction + master-faithful SDR

1. The exact 26602 split SHORT authority is forbidden on the active processSabreFrames path: no private SHORT accumulated color/weights, no propagated SHORT protection coverage as a second owner, no separate SHORT mean, no protected post-mean fuse, no hard accumulator owner, and no late SHORT RGB compositor.
2. NORMAL + SHORT + LONG selected evidence must feed the same common Sabre rejection, dilation, common RBF accumulator, Resolve, and VGN tunnel. SHORT/LONG may have role-aware physical validity, but not a separate final reconstruction authority.
3. Source clipping in the common merge is one scalar over the exact 3x3 CFA footprint consumed by the RBF observation. A clipped sample rejects the complete observation; no per-CFA or per-R/G/B bracket ownership is allowed.
4. The bracket-aware common rejection may bypass photometric disagreement for clipped reference versus valid alternate evidence only when the existing local flow-variation geometry remains coherent at <=2 RAW pixels. The variation source is flow.z. flow.w is a permanent forbidden regression for this gate.
5. A clipped non-reference observation has zero merge weight. When a real SHORT exists, a clipped reference may retain only the sealed tiny 0.001 deterministic fallback to prevent an all-clipped numerical hole; that fallback must never dominate valid SHORT evidence.
6. The newly modified common merge GLSL may not declare `packed` or any other GLSL/GLSL ES reserved identifier. This exact failure was found by the complete 26603 runtime-expanded reserved-identifier scan before packaging and is permanent.
7. DNG and true-2x high-frequency/subpixel detail remain NORMAL-only. Bracket evidence may improve the shared RGB reconstruction but may not become a DNG or SR-detail owner.
8. Existing LONG duration robustness/source-clipping behavior and successful 26600 boundary geometry remain inherited; 26603 must not replace those proven producers with another bracket-specific tunnel.
9. Motion SDR must remain identical to the UHDR/pre-tone master through 0.98 of FINAL SDR output after the common presentation scale. The exact 26602 effective ~0.80 shoulder is forbidden.
10. The discarded provisional 0.90 final-output knee / 0.10 reserve is forbidden. The sealed Motion reserve is 0.02, beginning at final-output 0.98.
11. Above the 0.98 final-output knee, Motion SDR mapping must be continuous, strictly monotonic, bounded below SDR white, and C1 at the knee. It may compress only physical HDR excess; it may not flatten SDR-representable body/highlight structure.
12. 1x and true-2x CPU/GPU publication must share the same final-display-domain Motion SDR contract. Night retains its inherited 0.50 shoulder.
13. Plant-window / blinds / foliage / bright-light class: SDR may not regain apparent structure only through UHDR while the SDR base is needlessly flattened; recoverable colored-edge artifacts may not be suppressed by blur, chroma cleanup, hue repair, or a late RGB bracket compositor.
14. Generated app/build/** and app/.cxx/** never count as runtime-source changes; repository-only scaffolding absent from the compiled-candidate authority never counts as runtime change; strict unexpected app/src/** changes remain visible and fail.
15. Deterministic full-index forward/rollback patches must reproduce exact bytes at core.abbrev 7/12/40 with fuzz=0, and the compiler-tested candidate must remain frozen through full assemble and final export.
