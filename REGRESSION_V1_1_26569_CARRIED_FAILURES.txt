Permanent carried failures applicable to 26568

All applicable successful-26567 carried regressions remain mandatory. In particular:

26543 real GLSL reserved-identifier failure:
Prevent: complete GLSL/GLSL ES reserved-identifier scan then pinned real glslangValidator on exact runtime-expanded shaders.

26543 patch nondeterminism:
Prevent: git diff --binary --full-index --no-ext-diff at core.abbrev 7/12/40 must be byte-identical, with fuzz=0 exact forward/rollback replay.

26543 Kotlin/Java compiler failures:
Prevent: real :app:compileDebugKotlin and :app:compileDebugJavaWithJavac before patch proof/assemble.

26565 repository-wide dependency-scope and non-hermetic host-surrogate failures:
Prevent: sealed stdlib-only Python scope; no host surrogate substitutes for Android compilers/NDK.

26567 local candidate transform JNI declaration drift:
Prevent: exact final Java/C++ contracts are validated before compilers; two clean transforms from exact prior compiled candidate must be byte-identical.

26567 local stale-transform overwrite:
Prevent: sealed final-payload hashes plus deterministic two-transform replay; final transformed declarations are validated.

26567 local true2x shader anchor leakage:
Prevent: runtime-expanded exact shader ownership checks; no true2x-specific symbol may leak into earlier Sabre owners.

26567 local Java declaration-order failure:
Prevent: carried 26567 Java/color files remain byte-identical; real javac remains mandatory.

26567 reserved GLSL identifier regression:
Prevent: all declared identifiers in all exact runtime-expanded variants are scanned; real pinned Khronos glslangValidator 16.5.0 remains mandatory.

26567 local workflow py_compile self-contamination:
Prevent: sealed workflow Python syntax uses in-memory compile(); py_compile is forbidden before transient-file rejection.

26567 V1 Actions glslang bootstrap symlink failure:
run 33339787968, job 99333216734
Prevent: resolve regular `glslang` first with glslangValidator compatibility-symlink fallback; keep pinned archive SHA and fixture.

26567 V1.1 Actions Kotlin phase-stat return-type failure:
run 33340190659, job 99334293921
Prevent: runTrue2xCpu/runTrue2xGpu keep True2xPhaseStats contract; real Kotlin compile remains mandatory.

26567 V1.2 Actions post-build generated-runtime-scope false failure (permanent regression #20):
run 33340661287, job 99335599790
Prevent: app/build/** and app/.cxx/** never enter runtime-source scope. Pre/post canonical snapshots are authority-seeded, while unexpected app/src/** remains visible and fails strict allowlist equality.

26567 V1.3 Actions repository-scaffolding scope false failure (permanent regression #21):
run 33350507622, job 99362814146
Prevent: canonical snapshots start from the exact previous compiled-candidate file universe and overlay only live app/src, app/build.gradle and app/version.properties. Repository-only scaffolding absent from authority never enters candidate scope.

26568 permanent fused-SR regressions:
- exact runtime changed-file allowlist is six paths; equality may never be weakened.
- normal 15-frame native Sabre/VGN merge implementation must remain byte-identical to 26567.
- JPEG SR rank-0 evidence in each phase must equal the strongest evidence 26567 would retain; rank-1 may only add evidence.
- JPEG SR evidence is capped at two observations per phase / eight total; DNG-requested SR retains full NORMAL evidence.
- direct-CFA RGB ratios must never become final JPEG color; fused output must remain Sabre/VGN guide RGB multiplied by one bounded scalar factor equally on R/G/B.
- <=1 phase, weak signal, near-clipped highlights, chroma disagreement and low-frequency disagreement must suppress the SR scalar toward exact factor 1.0.
- GPU JPEG path must not serialize separate 50MP native-guide or phase-support intermediates.
- DNG pristine direct-CFA/pre-VGN carrier must be written before fused guide rendering and dedicated DNG/ImageSaver owners remain byte-identical.
- phase telemetry must piggyback the already-required fused RGBA16F render readback; a second full-resolution phase GPU->CPU readback must not return.
- final JPEG/JPEG-R path must not recreate full-resolution RGB8 or gain-map raw scratch files.
- final P3/Jin/watermark pixel math helpers must remain byte-identical to 26567 unless intentionally replaced in a future audited build.
- CPU fallback scalar-guided derivative behavior remains byte-identical to 26567.

26568 pre-handoff validator false-positive caught before upload:
- An older work candidate still contained a separate phase glReadPixels under IRIS26564 naming, while the first validator only rejected IRIS26568 naming.
Prevent: validate the exact runTrue2xGpu function structurally: zero direct glReadPixels/phaseRead, require one RGBA16F helper readback, require render-alpha 0..4 phase decode, and require the fused-render helper call to receive phaseHistogram.

26568 V1 Actions Kotlin helper-closure failure:
run 33357873019, job 99383368441
First compiler error: GlesMgcRawSpatialStacker.kt:9922 unresolved reference allocateTrue2xHostBuffer; subsequent unresolved true2xRegionForOutput/true2xRawScaledRegion/true2xScaledRegion/readTrue2xRawRegion/readTrue2xFileRegion and inference errors were cascades.
Root cause: the V1 transformed payload replaced the true2x region while deleting six existing 26567 helper definitions that the new wide-band path still called.
Prevent permanently: corrected candidate must contain exactly one declaration of each helper, and the complete helper block from readTrue2xFileRegion through allocateTrue2xHostBuffer must be byte-identical to exact 26567 authority. The semantic validator must run before the real Kotlin compiler.

26568 V1.1 package-freeze regression:
Prevent: the six sealed runtime payload files must be byte-identical to the independently frozen corrected candidate; deterministic transform twice must reproduce that candidate, and candidate manifests/patches are regenerated only after the helper-closure correction is frozen.


26569 localized encoder/UI regressions:
- 26568 V1.1 successful compiled candidate, not repository app/src, is runtime authority for every 26569 transform and manifest.
- the 26569 runtime changed-file allowlist is exactly four paths; equality may never be weakened.
- candidate transform must be applied twice from exact 26568 authority and reproduce the independently frozen 1708-file candidate byte-for-byte.
- the final sealed payload must be byte-identical to that independently frozen candidate; stale payload self-consistency is insufficient.
- no shader, SR reconstruction, Sabre/VGN, adaptive-color shader, DNG owner, JPEG-R packager, gain-map resolution, or UI XML bytes may change.
- original profileColor/adaptiveAppearance/tone/preRenderAt/samplePreRender/renderHeadroom/renderBase/applyJinPixel/readRegion/outputToSource functions must remain byte-identical to successful 26568.
- cached Motion renderer may replace repeated profileColor calls only with values populated by the unchanged profileColor function; normalized cached equations must equal the existing functions exactly.
- optimized encoder path is allowed only when Jin is disabled. Jin-enabled behavior retains the proven 4-worker/128-row publication path.
- Motion fast path uses bounded 256-row bands and at most 6 workers; it must not reintroduce full-resolution RGB8 or raw-gain scratch files.
- gain-map dimensions remain 1:1 with the base JPEG and motionv2_jpeg_r_encoder.cpp remains byte-identical.
- encoder stage timing marker IRIS_26569_TRUE2X_ENCODER_TIMING and JPEG-R package timing marker IRIS_26569_TRUE2X_PACKAGE_TIMING must remain in the actual fix build.
- UI correction must contain no device/model/aspect-ratio allowlist; measured shutter/lens bounds alone own collision correction.
- lens-selector correction is max(0, lensBottom + 12dp - shutterTop); a layout already satisfying the minimum gap receives exactly zero correction.
- camera_fragment.xml, layout_bottombuttons.xml and layout_main_bottombar.xml remain byte-identical to successful 26568.
- IRIS_26569_UI_COLLISION_GUARD must report the actual correction used on device without broad diagnostic logging.

26569 local canonical rollback mismatch caught before handoff sealing:
- First exact successful-V1.1 patch-verifier replay found the initially staged rollback patch differed from canonical core.abbrev=7/12/40 regeneration even though the forward patch matched.
Prevent permanently: generate and verify both packaged patches through the same successful V1.1 canonical verifier; all 7/12/40 variants must be byte-identical and fuzz/offset-free with exact 1708-file rollback before sealing.

26569 local handoff-hash transient contamination caught before sealing:
- Regenerating the handoff hash manifest after a local-prebuild replay initially included the replay work/output trees, so verification correctly failed as those transient paths were cleaned at script start.
Prevent permanently: seal only the canonical 28 payload/dependency files plus the handoff hash manifest; the hash manifest excludes itself, and local replay work/output/__pycache__ directories are never inputs to the final ZIP or handoff hash manifest.

26569 V1 Actions native C++ namespace-closure failure:
run 33401928594, job 99520075790
First compiler error: motionv2_jpeg444_jni.cpp:867 expected '}' to match namespace iris26564render opened at line 426.
Root cause: the optimized encoder payload omitted the namespace-closing brace before the JNI exports; V1 had 393 opening braces and 392 closing braces.
Prevent permanently: the semantic validator must prove lexical C++ brace balance and that iris26564render closes before writeTrue2xNative JNI export. Every build modifying native C++ must run the real Android NDK buildCMakeDebug both ABIs compiler gate before patch proof and full assemble. Static brace validation never substitutes for the real NDK compiler.
