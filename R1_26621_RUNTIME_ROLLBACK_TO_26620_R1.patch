diff --git a/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl b/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
index 5f509eff4151ab20f8c6ee4ac5ec9fcbbd7bdaf1..8a1fa9087e2467f76a425bdfae3d8bc7854e35d1 100644
--- a/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
+++ b/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
@@ -88,28 +88,22 @@ float iris26585MapHeadroomGuide(float guide) {
     return toneStart + (preScaleDisplayWhite - toneStart) * shaped;
 }
 
-float iris26621MapMotionSdrFinalGuide(float sourceGuide){
+float iris26614MapMotionSdrFinalGuide(float sourceGuide){
     const float outputExposureScale=0.80;
     float x=max(sourceGuide,0.0);
-    float requested=max(displayGain,1.0e-6)*outputExposureScale;
-    float whiteAnchor=min(0.95,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor){
-        bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
+    float requestedFinalGain=max(displayGain,1.0e-6)*outputExposureScale;
+    float whiteAnchor=min(0.95,requestedFinalGain);
+    float bodyGain=requestedFinalGain;
+    if(requestedFinalGain>whiteAnchor){
+        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
     }
-    float safeWhite=max(whiteAnchor,1.0e-6);
-    float ratio=max(bodyGain/safeWhite-1.0,0.0);
     if(x<=1.0){
         float oneMinus=1.0-x;
-        float cubic=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rational=bodyGain*x/(1.0+ratio*x);
-        return 0.5*(cubic+rational);
+        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
     }
-    float rationalSlopeAtWhite=bodyGain/((1.0+ratio)*(1.0+ratio));
-    float bodySlopeAtWhite=0.5*(whiteAnchor+rationalSlopeAtWhite);
-    float reserve=max(1.0-whiteAnchor,0.0);
+    float reserve=1.0-whiteAnchor;
     if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(bodySlopeAtWhite,1.0e-6);
+    float tailScale=reserve/max(whiteAnchor,1.0e-6);
     float excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
@@ -122,7 +116,7 @@ float iris26585PostTonePreGamutPeak(vec3 preDisplayRgb) {
     float sourceGuide=max(sourceLuma,sourcePeak);
     if(sourceGuide<=1.0e-7) return 0.0;
     if(iris26598MotionPublication!=0){
-        float mappedFinal=iris26621MapMotionSdrFinalGuide(sourceGuide);
+        float mappedFinal=iris26614MapMotionSdrFinalGuide(sourceGuide);
         return sourcePeak*(mappedFinal/sourceGuide);
     }
     vec3 postRgb=sourceRgb*max(displayGain,1.0e-6);
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_accumulate_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_accumulate_26621.glsl
deleted file mode 100644
index 182b02aa6abf9adbb17de3ea48534a78e682e5ff..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_accumulate_26621.glsl
+++ /dev/null
@@ -1,53 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D Accumulator;
-uniform sampler2D RemapFine;
-uniform sampler2D RemapCoarse;
-uniform sampler2D GuideLevel;
-uniform float referenceLog;
-uniform float referenceStep;
-uniform float referenceMin;
-uniform float referenceMax;
-uniform int firstReference;
-out float Output;
-
-/* IRIS_26621_MATCHED_PYRAMID_EXPAND
- * Exact phase-matched EXPAND companion to the centered [1 4 6 4 1]/16 REDUCE filter.
- * Even fine coordinates use [1 6 1]/8 on coarse samples; odd coordinates use [1 1]/2.
- * The separable weights sum to one at every pixel, including clamped image boundaries. */
-float irisCoarseAt(sampler2D tex,ivec2 p){
-    ivec2 sz=textureSize(tex,0);
-    return texelFetch(tex,clamp(p,ivec2(0),sz-ivec2(1)),0).r;
-}
-void irisAxis(int fineCoord,out int base,out float wm,out float wc,out float wp){
-    base=fineCoord/2;
-    if((fineCoord&1)==0){wm=0.125;wc=0.750;wp=0.125;}
-    else{wm=0.0;wc=0.5;wp=0.5;}
-}
-float irisExpand(sampler2D tex,ivec2 p){
-    int bx,by;float wxm,wxc,wxp,wym,wyc,wyp;
-    irisAxis(p.x,bx,wxm,wxc,wxp);irisAxis(p.y,by,wym,wyc,wyp);
-    float sum=0.0;
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by-1))*(wxm*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by-1))*(wxc*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by-1))*(wxp*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by  ))*(wxm*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by  ))*(wxc*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by  ))*(wxp*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by+1))*(wxm*wyp);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by+1))*(wxc*wyp);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by+1))*(wxp*wyp);
-    return sum;
-}
-void main(){
-    ivec2 p=ivec2(gl_FragCoord.xy);
-    float fine=texelFetch(RemapFine,p,0).r;
-    float coarse=irisExpand(RemapCoarse,p);
-    float lap=fine-coarse;
-    float localReference=clamp(texelFetch(GuideLevel,p,0).r,referenceMin,referenceMax);
-    float weight=max(1.0-abs(localReference-referenceLog)/max(referenceStep,1.0e-6),0.0);
-    float previous=firstReference!=0 ? 0.0 : texelFetch(Accumulator,p,0).r;
-    Output=previous+weight*lap;
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl
new file mode 100644
index 0000000000000000000000000000000000000000..d133eef73d84c34b55b3af8677800fdd181d408c
--- /dev/null
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_correction_26620.glsl
@@ -0,0 +1,102 @@
+precision highp float;
+precision mediump sampler2D;
+
+uniform sampler2D Pyramid0;
+uniform sampler2D Pyramid1;
+uniform sampler2D Pyramid2;
+uniform sampler2D Pyramid3;
+uniform sampler2D Pyramid4;
+uniform sampler2D Pyramid5;
+uniform sampler2D Pyramid6;
+uniform sampler2D Pyramid7;
+out float Output;
+
+/* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_PRESENTATION
+ * Work only in the scalar log-intensity guide. The coarsest illumination base
+ * remains the exact 26614 global-tone result. Finer Laplacian bands may move
+ * continuously toward their source contrast only when the global map compressed
+ * them, only while their amplitude is in the detail/medium-structure regime,
+ * and never beyond source contrast. This mirrors the Local-Laplacian principle
+ * alpha=1 (detail preservation) while leaving large edges to the global tone map.
+ * There is no semantic classifier, tile atlas, fine-structure amplification, or
+ * per-channel color operation. */
+const float IRIS_SIGMA_DETAIL_EV = 1.32192809489; /* log2(2.5), paper-style range scale */
+
+vec2 levelSampleAligned(sampler2D tex,vec2 level0Coord,float scale){
+    vec2 sz=vec2(textureSize(tex,0));
+    vec2 coord=level0Coord*scale;
+    vec2 uv=(coord+vec2(0.5))/max(sz,vec2(1.0));
+    vec2 lo=vec2(0.5)/max(sz,vec2(1.0));
+    return texture(tex,clamp(uv,lo,vec2(1.0)-lo)).rg;
+}
+
+float preserveCompressedBand(float mappedBand,float sourceBand,float scaleWeight){
+    float am=abs(mappedBand);
+    float as=abs(sourceBand);
+    float compression=max(as-am,0.0);
+    float denom=max(am*as,1.0e-6);
+    float signAgreement=(mappedBand*sourceBand)/denom;
+    float signGate=smoothstep(0.75,0.98,signAgreement);
+    float compressionGate=smoothstep(0.02,0.15,compression);
+    float detailGate=1.0-smoothstep(0.75*IRIS_SIGMA_DETAIL_EV,
+                                    1.25*IRIS_SIGMA_DETAIL_EV,as);
+    float blend=clamp(scaleWeight*signGate*compressionGate*detailGate,0.0,1.0);
+    return mix(mappedBand,sourceBand,blend);
+}
+
+float softLimit(float x,float limitValue){
+    float a=abs(x);
+    return x/(1.0+a/max(limitValue,1.0e-6));
+}
+
+void main(){
+    /* Every reduce pass is phase-anchored at input index 2*q. Reconstruct all
+     * coarser levels at that same level-0 sample location; using one normalized
+     * UV for unequal/ceil-sized levels would introduce a half-pixel pyramid drift. */
+    vec2 level0Coord=gl_FragCoord.xy-vec2(0.5);
+    vec2 p0=levelSampleAligned(Pyramid0,level0Coord,1.0);
+    vec2 p1=levelSampleAligned(Pyramid1,level0Coord,0.5);
+    vec2 p2=levelSampleAligned(Pyramid2,level0Coord,0.25);
+    vec2 p3=levelSampleAligned(Pyramid3,level0Coord,0.125);
+    vec2 p4=levelSampleAligned(Pyramid4,level0Coord,0.0625);
+    vec2 p5=levelSampleAligned(Pyramid5,level0Coord,0.03125);
+    vec2 p6=levelSampleAligned(Pyramid6,level0Coord,0.015625);
+    vec2 p7=levelSampleAligned(Pyramid7,level0Coord,0.0078125);
+
+    float source0=p0.r;
+    float mapped0=p0.g;
+    float outLog=p7.g;
+
+    /* Approximate source-pixel bands: 128-256, 64-128, 32-64, 16-32,
+     * 8-16, 4-8 and 2-4. The finest band stays exactly global-tone mapped;
+     * the coarsest >~256 px illumination remains the global exposure anchor. */
+    outLog+=preserveCompressedBand(p6.g-p7.g,p6.r-p7.r,0.84);
+    outLog+=preserveCompressedBand(p5.g-p6.g,p5.r-p6.r,0.78);
+    outLog+=preserveCompressedBand(p4.g-p5.g,p4.r-p5.r,0.68);
+    outLog+=preserveCompressedBand(p3.g-p4.g,p3.r-p4.r,0.54);
+    outLog+=preserveCompressedBand(p2.g-p3.g,p2.r-p3.r,0.38);
+    outLog+=preserveCompressedBand(p1.g-p2.g,p1.r-p2.r,0.18);
+    outLog+=preserveCompressedBand(p0.g-p1.g,p0.r-p1.r,0.00);
+
+    float correctionEv=outLog-mapped0;
+    /* Deep shadows remain on the exact 26614 global map. The transition is
+     * smooth in rendered luminance and introduces no black offset. */
+    float mappedGuide=exp2(mapped0);
+    float shadowPreserve=smoothstep(0.020,0.080,mappedGuide);
+    correctionEv*=shadowPreserve;
+    /* IRIS_26620_BLACK_CRUSH_PROTECTION
+     * Negative local contrast restoration is introduced more conservatively through
+     * the lower rendered body so the multiscale stage cannot deepen 26614 shadows
+     * abruptly. Positive recovery keeps the original symmetric floor gate. */
+    if(correctionEv<0.0){
+        correctionEv*=smoothstep(0.080,0.200,mappedGuide);
+    }
+    /* Symmetric smooth bound; zero stays exactly zero and no hard threshold
+     * exists in source brightness or highlight position. */
+    correctionEv=softLimit(correctionEv,0.55);
+    if(!isnan(source0) && !isinf(source0) && !isnan(correctionEv) && !isinf(correctionEv)){
+        Output=correctionEv;
+    }else{
+        Output=0.0;
+    }
+}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl
new file mode 100644
index 0000000000000000000000000000000000000000..1892f6e07d728b4791d4a170623807e3892198a2
--- /dev/null
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26620.glsl
@@ -0,0 +1,35 @@
+precision highp float;
+precision highp int;
+precision mediump sampler2D;
+
+uniform sampler2D InputPyramid;
+out vec2 Output;
+
+/* IRIS_26620_LOCAL_LAPLACIAN_GAUSSIAN_DOWNSAMPLE
+ * Centered 5x5 binomial Gaussian reduction before every 2:1 pyramid step.
+ * R/G remain source/global log-guide pairs at identical spatial phases. */
+vec2 pairAt(ivec2 p){
+    ivec2 sz=textureSize(InputPyramid,0);
+    return texelFetch(InputPyramid,clamp(p,ivec2(0),sz-ivec2(1)),0).rg;
+}
+
+float kernel5(int o){
+    int a=abs(o);
+    if(a==0)return 6.0;
+    if(a==1)return 4.0;
+    return 1.0;
+}
+
+void main(){
+    ivec2 q=ivec2(gl_FragCoord.xy);
+    ivec2 center=q*2;
+    vec2 sum=vec2(0.0);
+    for(int oy=-2;oy<=2;oy++){
+        float wy=kernel5(oy);
+        for(int ox=-2;ox<=2;ox++){
+            float wx=kernel5(ox);
+            sum+=pairAt(center+ivec2(ox,oy))*(wx*wy);
+        }
+    }
+    Output=sum*(1.0/256.0);
+}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26621.glsl
deleted file mode 100644
index e20f4cda0f7ebe55b4f4257d7a0e63e685c1eca0..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_downsample_26621.glsl
+++ /dev/null
@@ -1,30 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D InputBuffer;
-out float Output;
-
-float irisAt(ivec2 p){
-    ivec2 sz=textureSize(InputBuffer,0);
-    return texelFetch(InputBuffer,clamp(p,ivec2(0),sz-ivec2(1)),0).r;
-}
-float irisKernel5(int o){
-    int a=abs(o);
-    if(a==0)return 6.0;
-    if(a==1)return 4.0;
-    return 1.0;
-}
-void main(){
-    ivec2 q=ivec2(gl_FragCoord.xy);
-    ivec2 center=q*2;
-    float sum=0.0;
-    for(int oy=-2;oy<=2;oy++){
-        float wy=irisKernel5(oy);
-        for(int ox=-2;ox<=2;ox++){
-            float wx=irisKernel5(ox);
-            sum+=irisAt(center+ivec2(ox,oy))*(wx*wy);
-        }
-    }
-    Output=sum*(1.0/256.0);
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
deleted file mode 100644
index 24cd95c0b02c38b0962fd7030a3aa8d9a6626b95..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_global_log_26621.glsl
+++ /dev/null
@@ -1,54 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D InputBuffer;
-uniform float displayGain;
-uniform float outputExposureScale;
-out float Output;
-
-const vec3 IRIS_LUMA = vec3(0.22897456,0.69173852,0.07928691);
-const float IRIS_LOG_FLOOR = 0.000244140625; /* 2^-12 */
-
-float irisMax3(vec3 v){return max(v.r,max(v.g,v.b));}
-float irisGuide(vec3 rgb){
-    rgb=max(rgb,vec3(0.0));
-    return max(dot(rgb,IRIS_LUMA),irisMax3(rgb));
-}
-
-/* IRIS_26621_NEW_SIMPLIFIED_GLOBAL_TONE
- * motionV2DisplayGain remains the single brightness request and outputExposureScale
- * is consumed once. The old 26614 cubic and this rational map share the same black
- * slope and source-white anchor; a constant 50/50 blend keeps proven body brightness
- * while enforcing useful positive slope through the upper body. The >1 branch is C1
- * and asymptotically reserves the final SDR code range for genuine HDR headroom. */
-float iris26621GlobalTone(float sourceGuide){
-    float x=max(sourceGuide,0.0);
-    float requested=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
-    float whiteAnchor=min(0.95,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor){
-        bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
-    }
-    float safeWhite=max(whiteAnchor,1.0e-6);
-    float ratio=max(bodyGain/safeWhite-1.0,0.0);
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        float oldBody=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rationalBody=bodyGain*x/(1.0+ratio*x);
-        return 0.5*(oldBody+rationalBody);
-    }
-    float rationalSlopeAtWhite=bodyGain/((1.0+ratio)*(1.0+ratio));
-    float bodySlopeAtWhite=0.5*(whiteAnchor+rationalSlopeAtWhite);
-    float reserve=max(1.0-whiteAnchor,0.0);
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(bodySlopeAtWhite,1.0e-6);
-    float excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
-}
-
-void main(){
-    ivec2 p=ivec2(gl_FragCoord.xy);
-    float mapped=iris26621GlobalTone(irisGuide(texelFetch(InputBuffer,p,0).rgb));
-    Output=log2(max(mapped,IRIS_LOG_FLOOR));
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_reconstruct_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_reconstruct_26621.glsl
deleted file mode 100644
index 0e8f1f451eacd2e2d5b9395c45d6e3359aec5634..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_reconstruct_26621.glsl
+++ /dev/null
@@ -1,39 +0,0 @@
-precision highp float;
-precision highp int;
-precision mediump sampler2D;
-
-uniform sampler2D LocalBand;
-uniform sampler2D CoarseReconstruction;
-out float Output;
-
-/* IRIS_26621_MATCHED_PYRAMID_RECONSTRUCTION
- * Use the same phase-matched EXPAND operator used to form every Laplacian band. This keeps
- * reconstruction algebraically consistent and avoids bilinear coarse-cell geometry. */
-float irisCoarseAt(sampler2D tex,ivec2 p){
-    ivec2 sz=textureSize(tex,0);
-    return texelFetch(tex,clamp(p,ivec2(0),sz-ivec2(1)),0).r;
-}
-void irisAxis(int fineCoord,out int base,out float wm,out float wc,out float wp){
-    base=fineCoord/2;
-    if((fineCoord&1)==0){wm=0.125;wc=0.750;wp=0.125;}
-    else{wm=0.0;wc=0.5;wp=0.5;}
-}
-float irisExpand(sampler2D tex,ivec2 p){
-    int bx,by;float wxm,wxc,wxp,wym,wyc,wyp;
-    irisAxis(p.x,bx,wxm,wxc,wxp);irisAxis(p.y,by,wym,wyc,wyp);
-    float sum=0.0;
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by-1))*(wxm*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by-1))*(wxc*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by-1))*(wxp*wym);
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by  ))*(wxm*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by  ))*(wxc*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by  ))*(wxp*wyc);
-    sum+=irisCoarseAt(tex,ivec2(bx-1,by+1))*(wxm*wyp);
-    sum+=irisCoarseAt(tex,ivec2(bx  ,by+1))*(wxc*wyp);
-    sum+=irisCoarseAt(tex,ivec2(bx+1,by+1))*(wxp*wyp);
-    return sum;
-}
-void main(){
-    ivec2 p=ivec2(gl_FragCoord.xy);
-    Output=texelFetch(LocalBand,p,0).r+irisExpand(CoarseReconstruction,p);
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
deleted file mode 100644
index 1018e555bff945538de346ddb139b6fba18b32e1..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ /dev/null
@@ -1,23 +0,0 @@
-precision highp float;
-precision mediump sampler2D;
-
-uniform sampler2D GlobalMappedLog;
-uniform float referenceLog;
-uniform float sigmaEv;
-uniform float edgeSlope;
-out float Output;
-
-/* IRIS_26621_TRUE_LOCAL_LAPLACIAN_REMAP
- * Fast Local-Laplacian intensity slice. alpha=1 exactly preserves small/detail
- * log-luminance differences. Only deviations outside sigmaEv use the gentler
- * edge slope. The remap is continuous, monotonic and symmetric around referenceLog. */
-float irisRemap(float x){
-    float d=x-referenceLog;
-    float a=abs(d);
-    float mapped=a<=sigmaEv ? a : sigmaEv+edgeSlope*(a-sigmaEv);
-    return referenceLog+(d<0.0?-mapped:mapped);
-}
-void main(){
-    ivec2 p=ivec2(gl_FragCoord.xy);
-    Output=irisRemap(texelFetch(GlobalMappedLog,p,0).r);
-}
diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl
new file mode 100644
index 0000000000000000000000000000000000000000..17c33173765c4a1f1dd3e0b7b7079c03e96a0329
--- /dev/null
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_seed_26620.glsl
@@ -0,0 +1,68 @@
+precision highp float;
+precision highp int;
+precision mediump sampler2D;
+
+uniform sampler2D InputBuffer;
+uniform float displayGain;
+uniform float outputExposureScale;
+out vec2 Output;
+
+/* IRIS_26620_LOCAL_LAPLACIAN_SEED
+ * First Gaussian level at half resolution. R stores log2(source guide), G stores
+ * log2(the exact 26614 globally mapped guide). The centered 5x5 binomial kernel
+ * is a standard Gaussian-pyramid reduce filter; there is no tile or coarse-map owner. */
+const vec3 LUMA = vec3(0.22897456,0.69173852,0.07928691);
+const float LOG_FLOOR = 0.000244140625; /* 2^-12 */
+
+float lumaOf(vec3 c){return dot(c,LUMA);}
+float max3(vec3 c){return max(c.r,max(c.g,c.b));}
+float guideOf(vec3 c){c=max(c,vec3(0.0));return max(lumaOf(c),max3(c));}
+
+float iris26614MapMotionSdrFinalGuide(float sourceGuide){
+    float x=max(sourceGuide,0.0);
+    float requestedFinalGain=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
+    float whiteAnchor=min(0.95,requestedFinalGain);
+    float bodyGain=requestedFinalGain;
+    if(requestedFinalGain>whiteAnchor){
+        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
+    }
+    if(x<=1.0){
+        float oneMinus=1.0-x;
+        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
+    }
+    float reserve=1.0-whiteAnchor;
+    if(reserve<=1.0e-6)return whiteAnchor;
+    float tailScale=reserve/max(whiteAnchor,1.0e-6);
+    float excess=x-1.0;
+    return whiteAnchor+reserve*excess/(excess+tailScale);
+}
+
+vec2 logPairAt(ivec2 p){
+    ivec2 sz=textureSize(InputBuffer,0);
+    p=clamp(p,ivec2(0),sz-ivec2(1));
+    float sourceGuide=guideOf(texelFetch(InputBuffer,p,0).rgb);
+    float mappedGuide=iris26614MapMotionSdrFinalGuide(sourceGuide);
+    return vec2(log2(max(sourceGuide,LOG_FLOOR)),
+                log2(max(mappedGuide,LOG_FLOOR)));
+}
+
+float kernel5(int o){
+    int a=abs(o);
+    if(a==0)return 6.0;
+    if(a==1)return 4.0;
+    return 1.0;
+}
+
+void main(){
+    ivec2 q=ivec2(gl_FragCoord.xy);
+    ivec2 center=q*2;
+    vec2 sum=vec2(0.0);
+    for(int oy=-2;oy<=2;oy++){
+        float wy=kernel5(oy);
+        for(int ox=-2;ox<=2;ox++){
+            float wx=kernel5(ox);
+            sum+=logPairAt(center+ivec2(ox,oy))*(wx*wy);
+        }
+    }
+    Output=sum*(1.0/256.0);
+}
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index f8079b13c8f91352be19699b86b6e344fd073bf8..028a5adc874095494b706aa29d7b3374ac37865f 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -7,8 +7,9 @@ uniform float outputExposureScale;
 uniform float irisOutputZoom;
 uniform int iris26592MotionHdrHandoff;
 uniform float displayGain;
-uniform sampler2D iris26621LocalToneLog;
-uniform int iris26621LocalToneEnabled;
+uniform float iris26604MotionSdrKneeFinal;
+uniform sampler2D iris26620LocalLaplacianCorrection;
+uniform int iris26620LocalLaplacianEnabled;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
 
@@ -96,38 +97,33 @@ vec3 iris26524BilinearInput(vec2 sourcePixel) {
     return mix(a,b,f.y);
 }
 
-/* IRIS_26621_NEW_SIMPLIFIED_GLOBAL_TONE
- * Preserve 26614 black/body slope and the 0.95 source-white anchor, but blend its cubic body
- * with a rational shoulder so the upper-body derivative stays useful instead of collapsing.
- * The >1 tail is C1 at source white and asymptotically reserves the final SDR range for HDR. */
-float iris26621MapMotionSdrFinalGuide(float sourceGuide) {
+/* IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_APPEARANCE
+ * One C1 monotonic source-domain appearance map. Low source values retain the solved viewfinder
+ * body slope. Extra display gain fades continuously toward nominal source white instead of moving
+ * the highlight boundary downward as displayGain rises. Source white maps to 0.95 and >1 clean
+ * HDR occupies the final 5% asymptotically. No histogram percentile or scene-dependent knot. */
+float iris26614MapMotionSdrFinalGuide(float sourceGuide) {
     float x=max(sourceGuide,0.0);
-    float requested=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
-    float whiteAnchor=min(0.95,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor){
-        bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
+    float requestedFinalGain=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
+    float whiteAnchor=min(0.95,requestedFinalGain);
+    float bodyGain=requestedFinalGain;
+    if(requestedFinalGain>whiteAnchor){
+        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
     }
-    float safeWhite=max(whiteAnchor,1.0e-6);
-    float ratio=max(bodyGain/safeWhite-1.0,0.0);
     if(x<=1.0){
         float oneMinus=1.0-x;
-        float cubic=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rational=bodyGain*x/(1.0+ratio*x);
-        return 0.5*(cubic+rational);
+        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
     }
-    float rationalSlopeAtWhite=bodyGain/((1.0+ratio)*(1.0+ratio));
-    float bodySlopeAtWhite=0.5*(whiteAnchor+rationalSlopeAtWhite);
-    float reserve=max(1.0-whiteAnchor,0.0);
+    float reserve=1.0-whiteAnchor;
     if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(bodySlopeAtWhite,1.0e-6);
+    float tailScale=reserve/max(whiteAnchor,1.0e-6);
     float excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
 
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
-        return iris26621MapMotionSdrFinalGuide(sourceGuide);
+        return iris26614MapMotionSdrFinalGuide(sourceGuide);
     }
 
     float start=0.50;
@@ -223,34 +219,35 @@ void main() {
      * Do not apply the legacy 5x5 local-luma contrast multiplier here. Sabre remains
      * the detail authority; headroom mapping, exposure, gamut fit and zoom are unchanged.
      */
-    if(iris26592MotionHdrHandoff!=0 && iris26621LocalToneEnabled!=0){
-        vec3 sourceRgb=max(linearSrgb,vec3(0.0));
-        float sourceY=max(luminance(sourceRgb),0.0);
-        float sourcePeak=max3(sourceRgb);
-        float sourceGuide=max(sourceY,sourcePeak);
-        if(sourceGuide>1.0e-7){
-            float globalMapped=iris26621MapMotionSdrFinalGuide(sourceGuide);
-            vec2 toneSize=vec2(textureSize(iris26621LocalToneLog,0));
-            vec2 uv=(sourcePixel+vec2(0.5))/max(toneSize,vec2(1.0));
-            vec2 halfTexel=vec2(0.5)/max(toneSize,vec2(1.0));
-            float localLog=texture(iris26621LocalToneLog,clamp(uv,halfTexel,vec2(1.0)-halfTexel)).r;
-            float localMapped=exp2(clamp(localLog,-12.0,0.0));
-            /* IRIS_26621_BLACK_FLOOR_GLOBAL_AUTHORITY
-             * Exact black and deep shadows stay on the global curve; Local Laplacian fades in
-             * only after rendered body separation is safely established. */
-            float localStrength=smoothstep(0.030,0.100,globalMapped);
-            float mappedGuide=mix(globalMapped,localMapped,localStrength);
-            linearSrgb=sourceRgb*(mappedGuide/sourceGuide);
-        }else{
-            linearSrgb=sourceRgb;
+    linearSrgb=mapExtendedLinearHeadroom(linearSrgb);
+
+    /* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_FINAL_OWNER
+     * The exact 26614 global tone map above consumes motionV2DisplayGain/outputExposureScale once.
+     * A half-resolution correction field reconstructed from seven Gaussian/Laplacian scales then
+     * restores only contrast that the global map compressed. Apply one RGB scalar after global
+     * mapping so hue/channel ratios are unchanged and no second exposure owner is introduced. */
+    if(iris26620LocalLaplacianEnabled!=0){
+        vec2 correctionSize=vec2(textureSize(iris26620LocalLaplacianCorrection,0));
+        /* Pyramid0 is decimated at source index 2*q, so sample in that exact
+         * phase instead of mapping by normalized source dimensions. */
+        vec2 correctionCoord=sourcePixel*0.5;
+        vec2 uv=(correctionCoord+vec2(0.5))/max(correctionSize,vec2(1.0));
+        vec2 halfTexel=vec2(0.5)/max(correctionSize,vec2(1.0));
+        uv=clamp(uv,halfTexel,vec2(1.0)-halfTexel);
+        float correctionEv=clamp(texture(iris26620LocalLaplacianCorrection,uv).r,-0.55,0.55);
+        /* Local presentation may restore compressed contrast, but it may not
+         * create a new display-gamut clip that was absent after the 26614 map. */
+        float mappedPeak=max3(linearSrgb);
+        if(correctionEv>0.0){
+            float availableUpEv=mappedPeak>1.0e-7 ? max(log2(1.0/mappedPeak),0.0) : 0.0;
+            correctionEv=min(correctionEv,availableUpEv);
         }
-    }else{
-        linearSrgb=mapExtendedLinearHeadroom(linearSrgb);
+        linearSrgb*=exp2(correctionEv);
     }
 
-    /* IRIS_26621_FINAL_DOMAIN_SINGLE_PRESENTATION
-     * motionV2DisplayGain/outputExposureScale are consumed only by the shared global tone seed.
-     * Local Laplacian supplies an absolute mapped luminance target, never a second exposure gain.
+    /* IRIS_26604_FINAL_DOMAIN_TONE
+     * outputExposureScale and Motion brightness target are already consumed by mapFinalSdrGuide;
+     * there is no second image multiplier after the canonical tone owner.
      */
     linearSrgb=fitDisplayGamut(linearSrgb);
 
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 60ac65505f69605e693bb39d2431547e5415526b..fef09b9a3783099ed226a2752525c1c9d225fc0a 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -504,33 +504,34 @@ struct Params {
     bool mirror=false;
     bool motionHdrHandoff=false;
     float residualZoom=1.f,displayGain=1.f,exposureEv=0.f,shadows=0.f,contrast=0.f,sceneWhite=1.f;
-    const uint16_t* localToneLogMap=nullptr;
-    int localToneLogW=0,localToneLogH=0,localToneLogSourceW=0,localToneLogSourceH=0;
+    const uint16_t* localLaplacianMap=nullptr;
+    int localLaplacianW=0,localLaplacianH=0,localLaplacianSourceW=0,localLaplacianSourceH=0;
     float sensorToProfile[9]{};
     float profileToDisplay[9]{};
 };
-/* IRIS_26621_TRUE2X_SHARED_LOCAL_TONE_MAP
- * Sample the exact full-resolution R16F log2 SDR target produced by MotionV2Render.
- * This is an absolute presentation target, not a correction/gain map. */
-inline float localToneMappedGuide(const Params&p,float trueX,float trueY){
-    if(!p.motionHdrHandoff||!p.localToneLogMap||p.localToneLogW<=0||p.localToneLogH<=0||p.localToneLogSourceW<=0||p.localToneLogSourceH<=0)return -1.f;
-    float oneX=(trueX+0.5f)*(float)p.localToneLogSourceW/(float)std::max(p.trueW,1)-0.5f;
-    float oneY=(trueY+0.5f)*(float)p.localToneLogSourceH/(float)std::max(p.trueH,1)-0.5f;
-    float mx=(oneX+0.5f)*(float)p.localToneLogW/(float)std::max(p.localToneLogSourceW,1)-0.5f;
-    float my=(oneY+0.5f)*(float)p.localToneLogH/(float)std::max(p.localToneLogSourceH,1)-0.5f;
-    mx=clampf(mx,0.f,(float)std::max(0,p.localToneLogW-1));
-    my=clampf(my,0.f,(float)std::max(0,p.localToneLogH-1));
+/* IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN_MAP
+ * Sample the exact half-resolution R16F correction field produced by 1x MotionV2Render.
+ * Coordinates are scene/source aligned before crop/rotation, so 1x and true-2x share one
+ * spatial presentation decision while native SR detail remains independently reconstructed. */
+inline float localLaplacianEv(const Params&p,float trueX,float trueY){
+    if(!p.motionHdrHandoff||!p.localLaplacianMap||p.localLaplacianW<=0||p.localLaplacianH<=0||p.localLaplacianSourceW<=0||p.localLaplacianSourceH<=0)return 0.f;
+    float oneX=(trueX+0.5f)*(float)p.localLaplacianSourceW/(float)std::max(p.trueW,1)-0.5f;
+    float oneY=(trueY+0.5f)*(float)p.localLaplacianSourceH/(float)std::max(p.trueH,1)-0.5f;
+    /* Pyramid0 is generated by the exact 2:1 phase q -> source index 2*q. */
+    float mx=oneX*0.5f;
+    float my=oneY*0.5f;
+    mx=clampf(mx,0.f,(float)std::max(0,p.localLaplacianW-1));
+    my=clampf(my,0.f,(float)std::max(0,p.localLaplacianH-1));
     int x0=(int)std::floor(mx),y0=(int)std::floor(my);
-    int x1=std::min(x0+1,p.localToneLogW-1),y1=std::min(y0+1,p.localToneLogH-1);
+    int x1=std::min(x0+1,p.localLaplacianW-1),y1=std::min(y0+1,p.localLaplacianH-1);
     float fx=mx-x0,fy=my-y0;
     auto at=[&](int x,int y){
-        float v=iris26564::halfToFloat(p.localToneLogMap[(size_t)y*p.localToneLogW+x]);
-        return std::isfinite(v)?clampf(v,-12.f,0.f):-12.f;
+        float v=iris26564::halfToFloat(p.localLaplacianMap[(size_t)y*p.localLaplacianW+x]);
+        return std::isfinite(v)?clampf(v,-0.55f,0.55f):0.f;
     };
     float a=at(x0,y0)+(at(x1,y0)-at(x0,y0))*fx;
     float b=at(x0,y1)+(at(x1,y1)-at(x0,y1))*fx;
-    float logv=a+(b-a)*fy;
-    return std::exp2(clampf(logv,-12.f,0.f));
+    return a+(b-a)*fy;
 }
 inline Vec3 mat(const float*m,Vec3 v){
     return {m[0]*v.r+m[1]*v.g+m[2]*v.b,
@@ -619,38 +620,32 @@ inline bool samplePreRenderCached(const ProfileRegion&s,const Params&p,float x,f
     *out=mix3(mix3(a,b,fx),mix3(c,d,fx),fy);return true;
 }
 
-inline float iris26621MapMotionSdrFinalGuide(float sourceGuide,float displayGain){
+inline float iris26614MapMotionSdrFinalGuide(float sourceGuide,float displayGain){
     constexpr float scale=0.80f;
     float x=std::max(sourceGuide,0.f);
-    float requested=std::max(displayGain,1.0e-6f)*scale;
-    float whiteAnchor=std::min(0.95f,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor)bodyGain=std::min(requested,4.f*whiteAnchor-1.0e-4f);
-    float safeWhite=std::max(whiteAnchor,1.0e-6f);
-    float ratio=std::max(bodyGain/safeWhite-1.f,0.f);
-    if(x<=1.f){
-        float oneMinus=1.f-x;
-        float cubic=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rational=bodyGain*x/(1.f+ratio*x);
-        return 0.5f*(cubic+rational);
-    }
-    float rationalSlopeAtWhite=bodyGain/((1.f+ratio)*(1.f+ratio));
-    float bodySlopeAtWhite=0.5f*(whiteAnchor+rationalSlopeAtWhite);
-    float reserve=std::max(1.f-whiteAnchor,0.f);if(reserve<=1.0e-6f)return whiteAnchor;
-    float tailScale=reserve/std::max(bodySlopeAtWhite,1.0e-6f),excess=x-1.f;
+    float requestedFinalGain=std::max(displayGain,1.0e-6f)*scale;
+    float whiteAnchor=std::min(0.95f,requestedFinalGain);
+    float bodyGain=requestedFinalGain;
+    if(requestedFinalGain>whiteAnchor)bodyGain=std::min(requestedFinalGain,4.f*whiteAnchor-1.0e-4f);
+    if(x<=1.f){float oneMinus=1.f-x;return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;}
+    float reserve=1.f-whiteAnchor;if(reserve<=1.0e-6f)return whiteAnchor;
+    float tailScale=reserve/std::max(whiteAnchor,1.0e-6f),excess=x-1.f;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float localMappedGuide){
+inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float localCorrectionEv){
     rgb=clampNonnegative(rgb);float y=std::max(luma(rgb),0.f),pk=peak(rgb),guide=std::max(y,pk);
-    /* IRIS_26621_SINGLE_GLOBAL_PLUS_LOCAL_TONE_OWNER_TRUE2X_CPU */
+    /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_CPU */
     if(p.motionHdrHandoff){
-        float globalMapped=iris26621MapMotionSdrFinalGuide(guide,p.displayGain);
-        float mappedFinal=globalMapped;
-        if(localMappedGuide>=0.f){
-            float localStrength=smoothstep(0.030f,0.100f,globalMapped);
-            mappedFinal=globalMapped+(localMappedGuide-globalMapped)*localStrength;
-        }
+        float mappedFinal=iris26614MapMotionSdrFinalGuide(guide,p.displayGain);
         if(guide>1.0e-7f)rgb=mul(rgb,mappedFinal/guide);
+        /* IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_AFTER_GLOBAL */
+        float correctionEv=clampf(localCorrectionEv,-0.55f,0.55f);
+        if(correctionEv>0.f){
+            float mappedPeak=peak(rgb);
+            float availableUpEv=mappedPeak>1.0e-7f?std::max(std::log2(1.f/mappedPeak),0.f):0.f;
+            correctionEv=std::min(correctionEv,availableUpEv);
+        }
+        rgb=mul(rgb,std::exp2(correctionEv));
     }else{
         const float start=0.50f;
         if(guide>1.0e-7f&&guide>start){float white=std::max(p.sceneWhite,start+0.05f);float u=std::max((guide-start)/std::max(white-start,1.0e-6f),0.f);float x=clampf(u,0.f,1.f);float shaped=std::log(1.f+6.f*x)/std::log(7.f);float mapped=start+(1.25f-start)*shaped;rgb=mul(rgb,mapped/guide);}
@@ -706,13 +701,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
     crx*=tw/th;crx/=((float)w.w/(float)w.h);crx*=1.025f;crx*=15.f;cry=(cry+1.f/15.f)*15.f;if(crx<0.f||cry<0.f)return false;*u=crx;*v=cry;return true;
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;float localEv=localLaplacianEv(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localEv);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localMapped=localToneMappedGuide(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localMapped);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;float localEv=localLaplacianEv(p,sx,sy);Vec3 sdr=renderHeadroom(hdr,p,localEv);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
@@ -794,7 +789,7 @@ precision highp usampler2D;
 /* IRIS_26571_TRUE2X_PUBLICATION_COMPUTE */
 layout(local_size_x=16,local_size_y=8,local_size_z=1) in;
 layout(binding=0) uniform highp sampler2D uSource;
-uniform highp sampler2D uLocalToneLogMap;
+uniform highp sampler2D uLocalLaplacianMap;
 layout(rgba8ui,binding=0) writeonly uniform highp uimage2D uOutput;
 layout(std430,binding=1) readonly buffer IrisGammaLut { float gammaLut[]; };
 
@@ -816,9 +811,9 @@ uniform float uShadows;
 uniform float uContrast;
 uniform float uSceneWhite;
 uniform int uMotionHdrHandoff;
-uniform int uLocalToneLogEnabled;
-uniform ivec2 uLocalToneLogSize;
-uniform ivec2 uLocalToneLogSourceSize;
+uniform int uLocalLaplacianEnabled;
+uniform ivec2 uLocalLaplacianSize;
+uniform ivec2 uLocalLaplacianSourceSize;
 uniform float uGainMax;
 uniform float uGainContentMax;
 uniform vec3 uSensorRow0;
@@ -935,51 +930,42 @@ vec2 irisOutputToSource(ivec2 op){
     float sx=cx+(ux-cx)/uResidualZoom,sy=cy+(uy-cy)/uResidualZoom;
     return clamp(2.0*(vec2(sx,sy)+vec2(0.5))-vec2(0.5),vec2(0.0),vec2(uTrueSize-ivec2(1)));
 }
-/* IRIS_26621_TRUE2X_LOCAL_LAPLACIAN_GPU_SHARED_FIELD */
-float irisLocalToneMappedGuide(vec2 trueSource){
-    if(uLocalToneLogEnabled==0)return -1.0;
-    vec2 oneSource=(trueSource+vec2(0.5))*vec2(uLocalToneLogSourceSize)/max(vec2(uTrueSize),vec2(1.0))-vec2(0.5);
-    vec2 mapCoord=(oneSource+vec2(0.5))*vec2(uLocalToneLogSize)/max(vec2(uLocalToneLogSourceSize),vec2(1.0))-vec2(0.5);
-    vec2 uv=(mapCoord+vec2(0.5))/max(vec2(uLocalToneLogSize),vec2(1.0));
-    vec2 texel=vec2(0.5)/max(vec2(uLocalToneLogSize),vec2(1.0));
+/* IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_GPU_SHARED_FIELD */
+float irisLocalLaplacianEv(vec2 trueSource){
+    if(uLocalLaplacianEnabled==0)return 0.0;
+    vec2 oneSource=(trueSource+vec2(0.5))*vec2(uLocalLaplacianSourceSize)/max(vec2(uTrueSize),vec2(1.0))-vec2(0.5);
+    vec2 mapCoord=oneSource*0.5;
+    vec2 uv=(mapCoord+vec2(0.5))/max(vec2(uLocalLaplacianSize),vec2(1.0));
+    vec2 texel=vec2(0.5)/max(vec2(uLocalLaplacianSize),vec2(1.0));
     uv=clamp(uv,texel,vec2(1.0)-texel);
-    return exp2(clamp(texture(uLocalToneLogMap,uv).r,-12.0,0.0));
+    return clamp(texture(uLocalLaplacianMap,uv).r,-0.55,0.55);
 }
-float iris26621MapMotionSdrFinalGuide(float sourceGuide){
+float iris26614MapMotionSdrFinalGuide(float sourceGuide){
     const float scale=0.80;
     float x=max(sourceGuide,0.0);
-    float requested=max(uDisplayGain,1.0e-6)*scale;
-    float whiteAnchor=min(0.95,requested);
-    float bodyGain=requested;
-    if(requested>whiteAnchor)bodyGain=min(requested,4.0*whiteAnchor-1.0e-4);
-    float safeWhite=max(whiteAnchor,1.0e-6);
-    float ratio=max(bodyGain/safeWhite-1.0,0.0);
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        float cubic=whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-        float rational=bodyGain*x/(1.0+ratio*x);
-        return 0.5*(cubic+rational);
-    }
-    float rationalSlopeAtWhite=bodyGain/((1.0+ratio)*(1.0+ratio));
-    float bodySlopeAtWhite=0.5*(whiteAnchor+rationalSlopeAtWhite);
-    float reserve=max(1.0-whiteAnchor,0.0);
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(bodySlopeAtWhite,1.0e-6);
-    float excess=x-1.0;
+    float requestedFinalGain=max(uDisplayGain,1.0e-6)*scale;
+    float whiteAnchor=min(0.95,requestedFinalGain);
+    float bodyGain=requestedFinalGain;
+    if(requestedFinalGain>whiteAnchor)bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
+    if(x<=1.0){float oneMinus=1.0-x;return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;}
+    float reserve=1.0-whiteAnchor;if(reserve<=1.0e-6)return whiteAnchor;
+    float tailScale=reserve/max(whiteAnchor,1.0e-6),excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-vec3 irisHeadroom(vec3 rgb,float localMappedGuide){
+vec3 irisHeadroom(vec3 rgb,float localCorrectionEv){
     rgb=irisClampNonnegative(rgb);
     float y=max(irisLuma(rgb),0.0),pk=irisPeak(rgb),guide=max(y,pk);
-    /* IRIS_26621_SINGLE_TONE_OWNER_TRUE2X_GPU */
+    /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_GPU */
     if(uMotionHdrHandoff!=0){
-        float globalMapped=iris26621MapMotionSdrFinalGuide(guide);
-        float mappedFinal=globalMapped;
-        if(localMappedGuide>=0.0){
-            float localStrength=smoothstep(0.030,0.100,globalMapped);
-            mappedFinal=mix(globalMapped,clamp(localMappedGuide,0.0,1.0),localStrength);
-        }
+        float mappedFinal=iris26614MapMotionSdrFinalGuide(guide);
         if(guide>1.0e-7)rgb*=mappedFinal/guide;
+        float correctionEv=clamp(localCorrectionEv,-0.55,0.55);
+        if(correctionEv>0.0){
+            float mappedPeak=irisPeak(rgb);
+            float availableUpEv=mappedPeak>1.0e-7?max(log2(1.0/mappedPeak),0.0):0.0;
+            correctionEv=min(correctionEv,availableUpEv);
+        }
+        rgb*=exp2(correctionEv);
     }else{
         float start=0.50;
         if(guide>1.0e-7&&guide>start){float white=max(uSceneWhite,start+0.05);float u=max((guide-start)/max(white-start,1.0e-6),0.0);float x=clamp(u,0.0,1.0);float shaped=log(1.0+6.0*x)/log(7.0);float mapped=start+(1.25-start)*shaped;rgb*=mapped/guide;}
@@ -999,8 +985,8 @@ void main(){
     ivec2 op=ivec2(local.x,uBandTop+local.y);
     vec2 trueSource=irisOutputToSource(op);
     vec3 hdr=irisSamplePreRender(trueSource);
-    float localMapped=irisLocalToneMappedGuide(trueSource);
-    vec3 sdr=irisHeadroom(hdr,localMapped);
+    float localEv=irisLocalLaplacianEv(trueSource);
+    vec3 sdr=irisHeadroom(hdr,localEv);
     uint gainCode=0u;
     if(uGenerateGain!=0){
         const float off=0.015625;float safeMax=max(uGainMax,1.001);
@@ -1050,12 +1036,12 @@ public:
         if(maxTex<std::max(outWidth,std::max(p.trueW,p.trueH))){set(reason,"GL_MAX_TEXTURE_SIZE");return false;}
         std::string compileError;program=iris26571Program(&compileError);if(!program){set(reason,("compute:"+compileError).c_str());return false;}
         glGenTextures(1,&sourceTex);glBindTexture(GL_TEXTURE_2D,sourceTex);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);
-        if(p.motionHdrHandoff&&p.localToneLogMap&&p.localToneLogW>0&&p.localToneLogH>0){
-            glGenTextures(1,&localToneLogTex);glActiveTexture(GL_TEXTURE1);glBindTexture(GL_TEXTURE_2D,localToneLogTex);
+        if(p.motionHdrHandoff&&p.localLaplacianMap&&p.localLaplacianW>0&&p.localLaplacianH>0){
+            glGenTextures(1,&localLaplacianTex);glActiveTexture(GL_TEXTURE1);glBindTexture(GL_TEXTURE_2D,localLaplacianTex);
             glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_LINEAR);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_LINEAR);
             glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);
-            glTexStorage2D(GL_TEXTURE_2D,1,GL_R16F,p.localToneLogW,p.localToneLogH);
-            glTexSubImage2D(GL_TEXTURE_2D,0,0,0,p.localToneLogW,p.localToneLogH,GL_RED,GL_HALF_FLOAT,p.localToneLogMap);
+            glTexStorage2D(GL_TEXTURE_2D,1,GL_R16F,p.localLaplacianW,p.localLaplacianH);
+            glTexSubImage2D(GL_TEXTURE_2D,0,0,0,p.localLaplacianW,p.localLaplacianH,GL_RED,GL_HALF_FLOAT,p.localLaplacianMap);
             glActiveTexture(GL_TEXTURE0);
         }
         glGenTextures(1,&outputTex);glBindTexture(GL_TEXTURE_2D,outputTex);glTexStorage2D(GL_TEXTURE_2D,1,GL_RGBA8UI,outWidth,maxBandRows);
@@ -1063,7 +1049,7 @@ public:
         glGenBuffers(1,&lutSsbo);glBindBuffer(GL_SHADER_STORAGE_BUFFER,lutSsbo);glBufferData(GL_SHADER_STORAGE_BUFFER,(GLsizeiptr)(lut.v.size()*sizeof(float)),lut.v.data(),GL_STATIC_DRAW);
         const size_t maxBytes=(size_t)outWidth*maxBandRows*4;glGenBuffers(2,pbo);for(int i=0;i<2;i++){glBindBuffer(GL_PIXEL_PACK_BUFFER,pbo[i]);glBufferData(GL_PIXEL_PACK_BUFFER,(GLsizeiptr)maxBytes,nullptr,GL_STREAM_READ);}
         glBindBuffer(GL_PIXEL_PACK_BUFFER,0);GLenum err=glGetError();if(err!=GL_NO_ERROR){set(reason,"GL allocation");return false;}
-        glUseProgram(program);glUniform1i(loc("uSource"),0);glUniform1i(loc("uLocalToneLogMap"),1);setParams();return true;
+        glUseProgram(program);glUniform1i(loc("uSource"),0);glUniform1i(loc("uLocalLaplacianMap"),1);setParams();return true;
     }
     bool renderReadback(int fd,float gainMax,float gainContentMax,bool generateGain,int top,int h,int slot,Iris26571ReadyBand*out,Iris26571GpuTiming*timing,std::string*reason){
         if(!params||slot<0||slot>1){set(reason,"render_invalid_state");return false;}
@@ -1089,20 +1075,20 @@ public:
     }
 private:
     EGLDisplay display=EGL_NO_DISPLAY;EGLConfig config=nullptr;EGLSurface surface=EGL_NO_SURFACE;EGLContext context=EGL_NO_CONTEXT;
-    GLuint program=0,sourceTex=0,localToneLogTex=0,outputTex=0,fbo=0,lutSsbo=0,pbo[2]{0,0};GLsync fence[2]{0,0};int pendingTop[2]{0,0},pendingH[2]{0,0};int sourceAllocW=0,sourceAllocH=0,outWidth=0,outHeight=0,maxBandRows=0;bool explicitFenceSync=true;const Params*params=nullptr;
+    GLuint program=0,sourceTex=0,localLaplacianTex=0,outputTex=0,fbo=0,lutSsbo=0,pbo[2]{0,0};GLsync fence[2]{0,0};int pendingTop[2]{0,0},pendingH[2]{0,0};int sourceAllocW=0,sourceAllocH=0,outWidth=0,outHeight=0,maxBandRows=0;bool explicitFenceSync=true;const Params*params=nullptr;
     GLint loc(const char*n){return glGetUniformLocation(program,n);}
     static void set(std::string*r,const char*m){if(r)*r=m?m:"unknown";}
     static void setGl(std::string*r,const char*stage,GLenum err){if(!r)return;char b[96];std::snprintf(b,sizeof(b),"%s_gl_error_0x%04x",stage?stage:"gl",(unsigned int)err);*r=b;}
     void setParams(){
-        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);glUniform1i(loc("uLocalToneLogEnabled"),(params->motionHdrHandoff&&params->localToneLogMap&&params->localToneLogW>0&&params->localToneLogH>0&&params->localToneLogSourceW>0&&params->localToneLogSourceH>0)?1:0);glUniform2i(loc("uLocalToneLogSize"),params->localToneLogW,params->localToneLogH);glUniform2i(loc("uLocalToneLogSourceSize"),params->localToneLogSourceW,params->localToneLogSourceH);
+        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);glUniform1i(loc("uLocalLaplacianEnabled"),(params->motionHdrHandoff&&params->localLaplacianMap&&params->localLaplacianW>0&&params->localLaplacianH>0&&params->localLaplacianSourceW>0&&params->localLaplacianSourceH>0)?1:0);glUniform2i(loc("uLocalLaplacianSize"),params->localLaplacianW,params->localLaplacianH);glUniform2i(loc("uLocalLaplacianSourceSize"),params->localLaplacianSourceW,params->localLaplacianSourceH);
         glUniform3f(loc("uSensorRow0"),params->sensorToProfile[0],params->sensorToProfile[1],params->sensorToProfile[2]);glUniform3f(loc("uSensorRow1"),params->sensorToProfile[3],params->sensorToProfile[4],params->sensorToProfile[5]);glUniform3f(loc("uSensorRow2"),params->sensorToProfile[6],params->sensorToProfile[7],params->sensorToProfile[8]);
         glUniform3f(loc("uDisplayRow0"),params->profileToDisplay[0],params->profileToDisplay[1],params->profileToDisplay[2]);glUniform3f(loc("uDisplayRow1"),params->profileToDisplay[3],params->profileToDisplay[4],params->profileToDisplay[5]);glUniform3f(loc("uDisplayRow2"),params->profileToDisplay[6],params->profileToDisplay[7],params->profileToDisplay[8]);
     }
     void destroy(){
         if(display!=EGL_NO_DISPLAY&&context!=EGL_NO_CONTEXT&&surface!=EGL_NO_SURFACE)eglMakeCurrent(display,surface,surface,context);
-        for(int i=0;i<2;i++)if(fence[i]){glDeleteSync(fence[i]);fence[i]=0;}if(pbo[0]||pbo[1])glDeleteBuffers(2,pbo);if(lutSsbo)glDeleteBuffers(1,&lutSsbo);if(fbo)glDeleteFramebuffers(1,&fbo);if(outputTex)glDeleteTextures(1,&outputTex);if(localToneLogTex)glDeleteTextures(1,&localToneLogTex);if(sourceTex)glDeleteTextures(1,&sourceTex);if(program)glDeleteProgram(program);
+        for(int i=0;i<2;i++)if(fence[i]){glDeleteSync(fence[i]);fence[i]=0;}if(pbo[0]||pbo[1])glDeleteBuffers(2,pbo);if(lutSsbo)glDeleteBuffers(1,&lutSsbo);if(fbo)glDeleteFramebuffers(1,&fbo);if(outputTex)glDeleteTextures(1,&outputTex);if(localLaplacianTex)glDeleteTextures(1,&localLaplacianTex);if(sourceTex)glDeleteTextures(1,&sourceTex);if(program)glDeleteProgram(program);
         if(display!=EGL_NO_DISPLAY){eglMakeCurrent(display,EGL_NO_SURFACE,EGL_NO_SURFACE,EGL_NO_CONTEXT);if(context!=EGL_NO_CONTEXT)eglDestroyContext(display,context);if(surface!=EGL_NO_SURFACE)eglDestroySurface(display,surface);eglTerminate(display);}
-        display=EGL_NO_DISPLAY;surface=EGL_NO_SURFACE;context=EGL_NO_CONTEXT;program=sourceTex=localToneLogTex=outputTex=fbo=lutSsbo=0;pbo[0]=pbo[1]=0;
+        display=EGL_NO_DISPLAY;surface=EGL_NO_SURFACE;context=EGL_NO_CONTEXT;program=sourceTex=localLaplacianTex=outputTex=fbo=lutSsbo=0;pbo[0]=pbo[1]=0;
     }
 };
 
@@ -1183,18 +1169,18 @@ extern "C" JNIEXPORT jstring JNICALL Java_com_particlesdevs_photoncamera_process
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeTrue2xNative(
         JNIEnv*e,jclass,jobject bitmap,jstring renderPath,jint trueW,jint trueH,jint rawW,jint rawH,jint cropW,jint cropH,jint rotation,jboolean mirror,jfloat residualZoom,
         jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat sceneWhite,jboolean motionHdrHandoff,
-        jobject localToneLogBuffer,jint localToneLogW,jint localToneLogH,jint localToneLogSourceW,jint localToneLogSourceH,
+        jobject localLaplacianBuffer,jint localLaplacianW,jint localLaplacianH,jint localLaplacianSourceW,jint localLaplacianSourceH,
         jobject watermarkBitmap,
         jfloatArray jinResidual,jintArray jinReference,jint jinW,jint jinH,jstring gainPath,jfloat gainMaxRatio,jfloat gainContentMaxRatio,jstring path,jint quality){
     using namespace iris26564render;gIris26576PublicationTelemetry="backend=UNSET callStarted=true";AndroidBitmapInfo info{};if(!bitmap||!renderPath||!path||trueW<=0||trueH<=0||rawW<=0||rawH<=0||cropW<=0||cropH<=0||trueW!=rawW*2||trueH!=rawH*2||cropW>rawW||cropH>rawH||AndroidBitmap_getInfo(e,bitmap,&info)!=ANDROID_BITMAP_RESULT_SUCCESS||info.format!=ANDROID_BITMAP_FORMAT_RGBA_8888){iris26576SetPublicationTelemetry("backend=NONE validationFailed=true");return JNI_FALSE;}
     int expectedNativeW=(rotation==90||rotation==270)?cropH:rawW,expectedNativeH=(rotation==90||rotation==270)?rawW:cropH;if((int)info.width!=expectedNativeW||(int)info.height!=expectedNativeH)return JNI_FALSE;
     Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
     if(p.motionHdrHandoff){
-        if(!localToneLogBuffer||localToneLogW<=0||localToneLogH<=0||localToneLogSourceW<=0||localToneLogSourceH<=0)return JNI_FALSE;
-        void*mapAddress=e->GetDirectBufferAddress(localToneLogBuffer);jlong mapCapacity=e->GetDirectBufferCapacity(localToneLogBuffer);
-        const jlong mapBytes=(jlong)localToneLogW*(jlong)localToneLogH*2ll;
+        if(!localLaplacianBuffer||localLaplacianW<=0||localLaplacianH<=0||localLaplacianSourceW<=0||localLaplacianSourceH<=0)return JNI_FALSE;
+        void*mapAddress=e->GetDirectBufferAddress(localLaplacianBuffer);jlong mapCapacity=e->GetDirectBufferCapacity(localLaplacianBuffer);
+        const jlong mapBytes=(jlong)localLaplacianW*(jlong)localLaplacianH*2ll;
         if(!mapAddress||mapCapacity<mapBytes)return JNI_FALSE;
-        p.localToneLogMap=(const uint16_t*)mapAddress;p.localToneLogW=localToneLogW;p.localToneLogH=localToneLogH;p.localToneLogSourceW=localToneLogSourceW;p.localToneLogSourceH=localToneLogSourceH;
+        p.localLaplacianMap=(const uint16_t*)mapAddress;p.localLaplacianW=localLaplacianW;p.localLaplacianH=localLaplacianH;p.localLaplacianSourceW=localLaplacianSourceW;p.localLaplacianSourceH=localLaplacianSourceH;
     }
     if(!sensorToProfile||!profileToDisplay||e->GetArrayLength(sensorToProfile)!=9||e->GetArrayLength(profileToDisplay)!=9)return JNI_FALSE;e->GetFloatArrayRegion(sensorToProfile,0,9,p.sensorToProfile);e->GetFloatArrayRegion(profileToDisplay,0,9,p.profileToDisplay);if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.sensorToProfile)if(!std::isfinite(v))return JNI_FALSE;for(float v:p.profileToDisplay)if(!std::isfinite(v))return JNI_FALSE;if(!std::isfinite(p.displayGain)||p.displayGain<=0.f||!std::isfinite(p.exposureEv)||!std::isfinite(p.shadows)||!std::isfinite(p.contrast)||!std::isfinite(p.residualZoom)||!std::isfinite(p.sceneWhite)||p.sceneWhite<=0.f)return JNI_FALSE;
     U rp(e,renderPath),gp(e,gainPath),op(e,path);if(!rp.c||!op.c)return JNI_FALSE;const bool generateGain=gp.c!=nullptr;if(generateGain&&(!std::isfinite((float)gainMaxRatio)||gainMaxRatio<=1.f||!std::isfinite((float)gainContentMaxRatio)||gainContentMaxRatio<1.f))return JNI_FALSE;const float gainContentMax=generateGain?clampf((float)gainContentMaxRatio,1.f,(float)gainMaxRatio):1.f;int sourceFd=open(rp.c,O_RDONLY);if(sourceFd<0)return JNI_FALSE;struct stat st{};uint64_t expected=(uint64_t)trueW*(uint64_t)trueH*6ull;if(fstat(sourceFd,&st)!=0||(uint64_t)st.st_size!=expected){close(sourceFd);return JNI_FALSE;}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
index 763859ef894766b5a8e56ee918ba33b5b60a8518..48ad83b873eb489eb2f28372677e684fe678ed30 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
@@ -76,9 +76,9 @@ public final class MotionV2AdaptiveColorAppearance extends Node {
                 + " IRIS_26598_SEMANTIC_AUTHORITY=true"
                 + " toneAwareHighlightChroma=true"
                 + " displayGain=" + displayGain
-                + " iris26621SdrWhiteAnchor=" + MotionV2Render.IRIS_26621_SDR_WHITE_ANCHOR
+                + " iris26614SdrWhiteAnchor=" + MotionV2Render.IRIS_26614_SDR_WHITE_ANCHOR
                 + " iris26614DisplayDomainKneeAuthority=false"
-                + " IRIS_26621_NEW_SIMPLIFIED_TONE_PREDICTOR_PARITY=true"
+                + " IRIS_26614_SOURCE_DOMAIN_TONE_PREDICTOR_PARITY=true"
                 + " irisNight=" + basePipeline.mParameters.irisNightActive
                 + " superRes=" + basePipeline.mParameters.motionV2SuperResOutputEnabled
                 + " superResScale=" + basePipeline.mParameters.motionV2SuperResOutputScale
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 9c73c58205dcfd7c46d451b3d5ae468fa6e9bfeb..c89a661cd592bc83e779cb37c166299260c21c5f 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -34,6 +34,8 @@ public final class MotionV2Render extends Node {
      * adaptive-color safety prediction and true-2x CPU/GPU publication.
      */
     static final float IRIS_26582_TONE_START = 0.50f;
+    static final float IRIS_26604_MOTION_SDR_KNEE_FINAL =
+            IRIS_26582_TONE_START * OUTPUT_EXPOSURE_SCALE;
     static final float IRIS_26582_HIGHLIGHT_TARGET = 0.97f;
     static final float IRIS_26582_CLIP_FRACTION_START = 0.002f;
     static final float IRIS_26582_CLIP_FRACTION_FULL = 0.025f;
@@ -93,44 +95,40 @@ public final class MotionV2Render extends Node {
     private static final float HDR_EXPOSURE_SCALE = OUTPUT_EXPOSURE_SCALE;
     private static final int GAINMAP_DOWNSAMPLE = 1;
 
-    /* IRIS_26621_NEW_SIMPLIFIED_PRESENTATION_OWNER
-     * Motion keeps one global exposure request: motionV2DisplayGain * OUTPUT_EXPOSURE_SCALE.
-     * The global tone map preserves the successful 26614 black/body slope and 0.95 source-white
-     * anchor, but blends that cubic 50/50 with a rational shoulder so the complete source-body
-     * derivative cannot collapse toward zero. A true fast Local-Laplacian then operates on the
-     * globally mapped scalar log-luminance with alpha=1 (no detail amplification). */
-    private static final int IRIS_26621_LAPLACIAN_LEVELS = 7;
-    private static final int IRIS_26621_REFERENCE_COUNT = 12;
-    private static final float IRIS_26621_REFERENCE_MIN_LOG = -6.0f;
-    private static final float IRIS_26621_REFERENCE_MAX_LOG = 0.0f;
-    private static final float IRIS_26621_DETAIL_SIGMA_EV = 0.35f;
-    private static final float IRIS_26621_EDGE_SLOPE = 0.94f;
-    public static final float IRIS_26621_SDR_WHITE_ANCHOR = 0.95f;
-
-    static float iris26621MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
+    /* IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN_OWNER
+     * Eight Gaussian levels begin at half resolution. The coarsest mapped level remains the
+     * exact 26614 global appearance base; finer levels can only restore contrast that 26614
+     * compressed, never exceed source contrast. The final correction is a single RGB scalar. */
+    private static final int IRIS_26620_LAPLACIAN_LEVELS = 8;
+    private static final float IRIS_26620_LOCAL_CORRECTION_LIMIT_EV = 0.55f;
+
+    /* IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_APPEARANCE
+     * 26613 solved viewfinder brightness in the dark/body range but then applied that full gain
+     * before a display-domain 0.80 knee. Device ceiling, bathroom and curtain pairs prove that
+     * ordinary bright source structure was therefore driven into the shoulder while the UHDR
+     * master still retained the correct appearance. 26614 keeps the solved body slope at black,
+     * removes that extra gain continuously as the physical source approaches nominal white, and
+     * reserves the final 5% of SDR code space for genuine >1 extended HDR. The function is one
+     * fixed C1 monotonic source-domain map: no scene percentile, object class or moving knot. */
+    public static final float IRIS_26614_SDR_WHITE_ANCHOR = 0.95f;
+
+    static float iris26614MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
         float x = Math.max(sourceGuide, 0.0f);
         float requestedFinalGain = Math.max(brightnessTargetGain, 1.0e-6f)
                 * OUTPUT_EXPOSURE_SCALE;
-        float whiteAnchor = Math.min(IRIS_26621_SDR_WHITE_ANCHOR, requestedFinalGain);
+        float whiteAnchor = Math.min(IRIS_26614_SDR_WHITE_ANCHOR, requestedFinalGain);
         float bodyGain = requestedFinalGain;
         if (requestedFinalGain > whiteAnchor) {
             bodyGain = Math.min(requestedFinalGain, 4.0f * whiteAnchor - 1.0e-4f);
         }
-        float safeWhite = Math.max(whiteAnchor, 1.0e-6f);
-        float ratio = Math.max(bodyGain / safeWhite - 1.0f, 0.0f);
         if (x <= 1.0f) {
             float oneMinus = 1.0f - x;
-            float cubic = whiteAnchor * x
+            return whiteAnchor * x
                     + (bodyGain - whiteAnchor) * x * oneMinus * oneMinus;
-            float rational = bodyGain * x / (1.0f + ratio * x);
-            return 0.5f * (cubic + rational);
         }
-        float rationalSlopeAtWhite = bodyGain
-                / ((1.0f + ratio) * (1.0f + ratio));
-        float bodySlopeAtWhite = 0.5f * (whiteAnchor + rationalSlopeAtWhite);
-        float reserve = Math.max(1.0f - whiteAnchor, 0.0f);
+        float reserve = 1.0f - whiteAnchor;
         if (reserve <= 1.0e-6f) return whiteAnchor;
-        float tailScale = reserve / Math.max(bodySlopeAtWhite, 1.0e-6f);
+        float tailScale = reserve / Math.max(whiteAnchor, 1.0e-6f);
         float excess = x - 1.0f;
         return whiteAnchor + reserve * excess / (excess + tailScale);
     }
@@ -211,6 +209,16 @@ public final class MotionV2Render extends Node {
                 + (preScaleDisplayWhite - IRIS_26582_TONE_START) * shaped;
     }
 
+    static float iris26604MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
+        float targetFinal = Math.max(0.0f, sourceGuide)
+                * Math.max(brightnessTargetGain, 1.0e-6f) * OUTPUT_EXPOSURE_SCALE;
+        float knee = IRIS_26604_MOTION_SDR_KNEE_FINAL;
+        if (targetFinal <= knee) return targetFinal;
+        float reserve = Math.max(1.0f - knee, 1.0e-6f);
+        float excess = targetFinal - knee;
+        return knee + reserve * excess / (excess + reserve);
+    }
+
     static float iris26582AdaptiveStrength(float clippedFraction) {
         float t = iris26582Clamp((clippedFraction - IRIS_26582_CLIP_FRACTION_START)
                 / Math.max(IRIS_26582_CLIP_FRACTION_FULL
@@ -282,171 +290,95 @@ public final class MotionV2Render extends Node {
         return Math.max(lo, Math.min(hi, x));
     }
 
-    private void iris26621ClearLocalToneState() {
-        basePipeline.mParameters.motionV2LocalToneLogMap = null;
-        basePipeline.mParameters.motionV2LocalToneLogWidth = 0;
-        basePipeline.mParameters.motionV2LocalToneLogHeight = 0;
-        basePipeline.mParameters.motionV2LocalToneLogSourceWidth = 0;
-        basePipeline.mParameters.motionV2LocalToneLogSourceHeight = 0;
+    private void iris26620ClearLocalLaplacianState() {
+        basePipeline.mParameters.motionV2LocalLaplacianMap = null;
+        basePipeline.mParameters.motionV2LocalLaplacianWidth = 0;
+        basePipeline.mParameters.motionV2LocalLaplacianHeight = 0;
+        basePipeline.mParameters.motionV2LocalLaplacianSourceWidth = 0;
+        basePipeline.mParameters.motionV2LocalLaplacianSourceHeight = 0;
     }
 
-    private GLTexture iris26621BuildLocalLaplacianTone(GLTexture source) {
-        iris26621ClearLocalToneState();
+    private GLTexture iris26620BuildLocalLaplacianCorrection(GLTexture source) {
+        iris26620ClearLocalLaplacianState();
         if (!basePipeline.mParameters.motionV2Active) return null;
         if (source == null || source.mSize == null || source.mSize.x <= 0 || source.mSize.y <= 0) {
-            throw new IllegalStateException("IRIS_26621 invalid Local Laplacian source");
+            throw new IllegalStateException("IRIS_26620 invalid Local Laplacian source");
         }
 
-        final GLFormat scalar = new GLFormat(GLFormat.DataType.FLOAT_16, 1);
-        final GLTexture[] guide = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS];
-        final GLTexture[] bands = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS - 1];
-        GLTexture finalTone = null;
-        boolean keepFinal = false;
+        final GLTexture[] pyramid = new GLTexture[IRIS_26620_LAPLACIAN_LEVELS];
+        GLTexture correction = null;
+        boolean keepCorrection = false;
         try {
-            guide[0] = new GLTexture(new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-            glProg.useAssetProgram("motionv2/local_laplacian_global_log_26621");
+            Point levelSize = new Point(
+                    Math.max(1, (source.mSize.x + 1) / 2),
+                    Math.max(1, (source.mSize.y + 1) / 2));
+            final GLFormat pairFormat = new GLFormat(GLFormat.DataType.FLOAT_16, 2);
+            pyramid[0] = new GLTexture(levelSize, pairFormat, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_seed_26620");
             glProg.setTexture("InputBuffer", source);
             glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
             glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
-            glProg.drawBlocks(guide[0]);
+            glProg.drawBlocks(pyramid[0]);
 
-            for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
-                Point previous = guide[level - 1].mSize;
-                Point nextSize = new Point(
+            for (int i = 1; i < IRIS_26620_LAPLACIAN_LEVELS; i++) {
+                Point previous = pyramid[i - 1].mSize;
+                levelSize = new Point(
                         Math.max(1, (previous.x + 1) / 2),
                         Math.max(1, (previous.y + 1) / 2));
-                guide[level] = new GLTexture(nextSize, scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                glProg.useAssetProgram("motionv2/local_laplacian_downsample_26621");
-                glProg.setTexture("InputBuffer", guide[level - 1]);
-                glProg.drawBlocks(guide[level]);
-            }
-
-            final float referenceStep = (IRIS_26621_REFERENCE_MAX_LOG - IRIS_26621_REFERENCE_MIN_LOG)
-                    / (IRIS_26621_REFERENCE_COUNT - 1.0f);
-            for (int reference = 0; reference < IRIS_26621_REFERENCE_COUNT; reference++) {
-                final float referenceLog = IRIS_26621_REFERENCE_MIN_LOG + reference * referenceStep;
-                final GLTexture[] remap = new GLTexture[IRIS_26621_LAPLACIAN_LEVELS];
-                try {
-                    remap[0] = new GLTexture(new Point(source.mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                    glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
-                    glProg.setTexture("GlobalMappedLog", guide[0]);
-                    glProg.setVar("referenceLog", referenceLog);
-                    glProg.setVar("sigmaEv", IRIS_26621_DETAIL_SIGMA_EV);
-                    glProg.setVar("edgeSlope", IRIS_26621_EDGE_SLOPE);
-                    glProg.drawBlocks(remap[0]);
-
-                    for (int level = 1; level < IRIS_26621_LAPLACIAN_LEVELS; level++) {
-                        remap[level] = new GLTexture(new Point(guide[level].mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                        glProg.useAssetProgram("motionv2/local_laplacian_downsample_26621");
-                        glProg.setTexture("InputBuffer", remap[level - 1]);
-                        glProg.drawBlocks(remap[level]);
-                    }
-
-                    for (int level = 0; level < IRIS_26621_LAPLACIAN_LEVELS - 1; level++) {
-                        GLTexture next = new GLTexture(new Point(guide[level].mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                        glProg.useAssetProgram("motionv2/local_laplacian_accumulate_26621");
-                        glProg.setTexture("Accumulator", bands[level] != null ? bands[level] : remap[level]);
-                        glProg.setTexture("RemapFine", remap[level]);
-                        glProg.setTexture("RemapCoarse", remap[level + 1]);
-                        glProg.setTexture("GuideLevel", guide[level]);
-                        glProg.setVar("referenceLog", referenceLog);
-                        glProg.setVar("referenceStep", referenceStep);
-                        glProg.setVar("referenceMin", IRIS_26621_REFERENCE_MIN_LOG);
-                        glProg.setVar("referenceMax", IRIS_26621_REFERENCE_MAX_LOG);
-                        glProg.setVar("firstReference", reference == 0 ? 1 : 0);
-                        glProg.drawBlocks(next);
-                        if (bands[level] != null) {
-                            try { bands[level].close(); } catch (Throwable ignored) {}
-                        }
-                        bands[level] = next;
-                    }
-                } finally {
-                    for (GLTexture texture : remap) {
-                        if (texture != null) {
-                            try { texture.close(); } catch (Throwable ignored) {}
-                        }
-                    }
-                }
+                pyramid[i] = new GLTexture(levelSize, pairFormat, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+                glProg.useAssetProgram("motionv2/local_laplacian_downsample_26620");
+                glProg.setTexture("InputPyramid", pyramid[i - 1]);
+                glProg.drawBlocks(pyramid[i]);
             }
 
-            GLTexture reconstruction = guide[IRIS_26621_LAPLACIAN_LEVELS - 1];
-            boolean reconstructionOwned = false;
-            for (int level = IRIS_26621_LAPLACIAN_LEVELS - 2; level >= 0; level--) {
-                GLTexture next = new GLTexture(new Point(guide[level].mSize), scalar, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
-                glProg.useAssetProgram("motionv2/local_laplacian_reconstruct_26621");
-                glProg.setTexture("LocalBand", bands[level]);
-                glProg.setTexture("CoarseReconstruction", reconstruction);
-                glProg.drawBlocks(next);
-                if (reconstructionOwned) {
-                    try { reconstruction.close(); } catch (Throwable ignored) {}
-                }
-                reconstruction = next;
-                reconstructionOwned = true;
-            }
-            finalTone = reconstruction;
-
-            /* IRIS_26621_LOCAL_LAPLACIAN_PEAK_LIFETIME
-             * The remap pyramid is already gone. Once reconstruction is complete, guide/band
-             * pyramids are no longer needed; release them before the optional full-resolution
-             * true-2x readback so the direct R16F map does not overlap those transient allocations. */
-            for (int level = 0; level < bands.length; level++) {
-                if (bands[level] != null) {
-                    try { bands[level].close(); } catch (Throwable ignored) {}
-                    bands[level] = null;
-                }
-            }
-            for (int level = 0; level < guide.length; level++) {
-                if (guide[level] != null && guide[level] != finalTone) {
-                    try { guide[level].close(); } catch (Throwable ignored) {}
-                    guide[level] = null;
-                }
+            correction = new GLTexture(
+                    pyramid[0].mSize,
+                    new GLFormat(GLFormat.DataType.FLOAT_16, 1),
+                    null, GL_LINEAR, GL_CLAMP_TO_EDGE);
+            glProg.useAssetProgram("motionv2/local_laplacian_correction_26620");
+            for (int i = 0; i < IRIS_26620_LAPLACIAN_LEVELS; i++) {
+                glProg.setTexture("Pyramid" + i, pyramid[i]);
             }
+            glProg.drawBlocks(correction);
 
             if (basePipeline.mParameters.motionV2SuperResOutputEnabled) {
-                finalTone.BufferLoad();
-                ByteBuffer map = finalTone.textureBuffer(scalar, true);
+                correction.BufferLoad();
+                ByteBuffer map = correction.textureBuffer(
+                        new GLFormat(GLFormat.DataType.FLOAT_16, 1), true);
                 map.position(0);
-                basePipeline.mParameters.motionV2LocalToneLogMap = map;
-                basePipeline.mParameters.motionV2LocalToneLogWidth = finalTone.mSize.x;
-                basePipeline.mParameters.motionV2LocalToneLogHeight = finalTone.mSize.y;
-                basePipeline.mParameters.motionV2LocalToneLogSourceWidth = source.mSize.x;
-                basePipeline.mParameters.motionV2LocalToneLogSourceHeight = source.mSize.y;
+                basePipeline.mParameters.motionV2LocalLaplacianMap = map;
+                basePipeline.mParameters.motionV2LocalLaplacianWidth = correction.mSize.x;
+                basePipeline.mParameters.motionV2LocalLaplacianHeight = correction.mSize.y;
+                basePipeline.mParameters.motionV2LocalLaplacianSourceWidth = source.mSize.x;
+                basePipeline.mParameters.motionV2LocalLaplacianSourceHeight = source.mSize.y;
             }
 
-            Log.i(Name, "IRIS_26621_NEW_SIMPLIFIED_LOCAL_LAPLACIAN"
+            Log.i(Name, "IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN"
                     + " active=true"
-                    + " levels=" + IRIS_26621_LAPLACIAN_LEVELS
-                    + " references=" + IRIS_26621_REFERENCE_COUNT
+                    + " levels=" + IRIS_26620_LAPLACIAN_LEVELS
                     + " source=" + source.mSize.x + "x" + source.mSize.y
-                    + " target=" + finalTone.mSize.x + "x" + finalTone.mSize.y
-                    + " coarsest=" + guide[IRIS_26621_LAPLACIAN_LEVELS - 1].mSize.x
-                        + "x" + guide[IRIS_26621_LAPLACIAN_LEVELS - 1].mSize.y
-                    + " referenceLogRange=" + IRIS_26621_REFERENCE_MIN_LOG
-                        + ".." + IRIS_26621_REFERENCE_MAX_LOG
-                    + " sigmaEv=" + IRIS_26621_DETAIL_SIGMA_EV
-                    + " edgeSlope=" + IRIS_26621_EDGE_SLOPE
-                    + " detailAlpha=1.0"
+                    + " correction=" + correction.mSize.x + "x" + correction.mSize.y
+                    + " mapSource=" + source.mSize.x + "x" + source.mSize.y
+                    + " coarsest=" + pyramid[IRIS_26620_LAPLACIAN_LEVELS - 1].mSize.x
+                        + "x" + pyramid[IRIS_26620_LAPLACIAN_LEVELS - 1].mSize.y
                     + " globalExposureOwner=motionV2DisplayGain"
                     + " outputExposureScaleConsumedOnce=" + OUTPUT_EXPOSURE_SCALE
-                    + " absoluteFullResolutionToneMap=true"
-                    + " correctionMap=false"
+                    + " coarsestBaseOwner=IRIS_26614_GLOBAL"
+                    + " restoreOnlyCompressedBands=true"
+                    + " sourceContrastUpperBound=true"
                     + " rgbScalarOnly=true"
+                    + " correctionLimitEv=" + IRIS_26620_LOCAL_CORRECTION_LIMIT_EV
                     + " true2xSharedMap=" + basePipeline.mParameters.motionV2SuperResOutputEnabled);
-            keepFinal = true;
-            return finalTone;
+            keepCorrection = true;
+            return correction;
         } finally {
-            for (GLTexture texture : bands) {
+            for (GLTexture texture : pyramid) {
                 if (texture != null) {
                     try { texture.close(); } catch (Throwable ignored) {}
                 }
             }
-            for (GLTexture texture : guide) {
-                if (texture != null && texture != finalTone) {
-                    try { texture.close(); } catch (Throwable ignored) {}
-                }
-            }
-            if (!keepFinal && finalTone != null) {
-                try { finalTone.close(); } catch (Throwable ignored) {}
+            if (!keepCorrection && correction != null) {
+                try { correction.close(); } catch (Throwable ignored) {}
             }
         }
     }
@@ -493,25 +425,26 @@ public final class MotionV2Render extends Node {
                 + " reconstructionZoom=" + reconstructionZoom
                 + " renderResidual=" + irisOutputZoom
                 + " product=" + (reconstructionZoom * irisOutputZoom));
-        GLTexture iris26621LocalTone = iris26621BuildLocalLaplacianTone(extendedLinearHdr);
+        GLTexture iris26620Correction = iris26620BuildLocalLaplacianCorrection(extendedLinearHdr);
         try {
             glProg.useAssetProgram("motionv2/render");
             glProg.setTexture("InputBuffer", extendedLinearHdr);
             glProg.setVar("sceneWhite", sceneWhite);
             glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
             glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
+            glProg.setVar("iris26604MotionSdrKneeFinal", IRIS_26604_MOTION_SDR_KNEE_FINAL);
             glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
             glProg.setVar("irisOutputZoom", irisOutputZoom);
-            glProg.setVar("iris26621LocalToneEnabled", iris26621LocalTone != null ? 1 : 0);
-            if (iris26621LocalTone != null) {
-                glProg.setTexture("iris26621LocalToneLog", iris26621LocalTone);
+            glProg.setVar("iris26620LocalLaplacianEnabled", iris26620Correction != null ? 1 : 0);
+            if (iris26620Correction != null) {
+                glProg.setTexture("iris26620LocalLaplacianCorrection", iris26620Correction);
             }
 
             WorkingTexture = basePipeline.getMain();
             glProg.drawBlocks(WorkingTexture);
         } finally {
-            if (iris26621LocalTone != null) {
-                try { iris26621LocalTone.close(); } catch (Throwable ignored) {}
+            if (iris26620Correction != null) {
+                try { iris26620Correction.close(); } catch (Throwable ignored) {}
             }
         }
 
@@ -696,16 +629,15 @@ public final class MotionV2Render extends Node {
         glProg.closed = true;
 
         if (basePipeline.mParameters.motionV2Active) {
-            Log.i(Name, "IRIS_26621_NEW_SIMPLIFIED_SINGLE_PRESENTATION"
+            Log.i(Name, "IRIS_26604_SINGLE_TONE_OWNER"
                     + " master=extendedLinearPreTone"
                     + " brightnessTargetGain=" + basePipeline.mParameters.motionV2DisplayGain
                     + " displayGainImageMultiplier=false"
-                    + " sourceWhiteAnchor=" + IRIS_26621_SDR_WHITE_ANCHOR
-                    + " bodyBlend=cubic_rational_50_50"
-                    + " c1SourceWhite=true monotonic=true usefulSlopeFloor=true hardEndpointClamp=false"
-                    + " localToneOwner=fastLocalLaplacian"
-                    + " localToneGlobalExposureMultiplier=false"
-                    + " localToneDetailAlpha=1.0"
+                    + " motionSdrKneeFinal=" + IRIS_26604_MOTION_SDR_KNEE_FINAL
+                    + " exactBodyBrightnessThroughFinalSdr=" + IRIS_26604_MOTION_SDR_KNEE_FINAL
+                    + " c1SoftKnee=true monotonic=true hardEndpointClamp=false"
+                    + " sameColor=true sameBlack=true sameShadows=true sameMidtones=true"
+                    + " differenceOnlyInTopSdrReserveAndHdrExcess=true"
                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                     + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
                     + " oneMasterRendition=true"
@@ -759,13 +691,13 @@ public final class MotionV2Render extends Node {
                 + " syntheticBitmapGainMap=false"
                 + " irisOutputZoom=" + irisOutputZoom
                 + " nativeOutputDimensionsPreserved=true"
-                + " iris26621SdrWhiteAnchor=" + IRIS_26621_SDR_WHITE_ANCHOR
+                + " iris26614SdrWhiteAnchor=" + IRIS_26614_SDR_WHITE_ANCHOR
                 + " iris26614DisplayDomainKneeAuthority=false"
-                + " iris26614CanonicalAppearanceGainAuthority=false"
-                + " IRIS_26621_NEW_SIMPLIFIED_SDR_UHDR_SR_PARITY=true"
+                + " iris26614CanonicalAppearanceGainAuthority=true"
+                + " IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_UHDR_SR_PARITY=true"
                 + " localTone=" + basePipeline.mParameters.motionV2Active
-                + " localToneOwner=IRIS_26621_FAST_LOCAL_LAPLACIAN"
-                + " globalToneOwner=IRIS_26621_NEW_SIMPLIFIED_GLOBAL"
+                + " localToneOwner=IRIS_26620_MULTISCALE_LOCAL_LAPLACIAN"
+                + " globalToneOwner=IRIS_26614_CANONICAL_SOURCE_DOMAIN"
                 + " sharpening=false");
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index d2cb2da6f08e64dc7df15ce3820fef37e96cca7b..6c4469dc1436c4862a9628994aff2a19f96c1e63 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -804,7 +804,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                 structuredGuide, structuredStrength, floor26583White);
     }
 
-    /* IRIS_26621_SOLVER_RENDER_TONE_PARITY
+    /* IRIS_26582_SOLVER_RENDER_TONE_PARITY
      * Exact scalar tone/gamut model used by motionv2/render.glsl. The removed 26581
      * neutral-to-white and overflow-to-white solver approximations were stale and were not
      * present in the final renderer.
@@ -819,7 +819,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
         if (guide <= 1.0e-7f) return 0.0f;
         float mappedFinal;
         if (basePipeline.mParameters.motionV2Active) {
-            mappedFinal = MotionV2Render.iris26621MapMotionSdrFinalGuide(guide, gain);
+            mappedFinal = MotionV2Render.iris26614MapMotionSdrFinalGuide(guide, gain);
         } else {
             float preGuide = guide * Math.max(gain, 1.0e-6f);
             mappedFinal = MotionV2Render.iris26582MapHeadroom(preGuide, sceneWhite)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index c8b895aab16aed7938d29810deda72f139a90771..862e5807b2773bb163a4537619b2add2ebf3249a 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -125,15 +125,15 @@ public class Parameters {
     public float motionV2RenderResidualZoom = 1.0f;
     public boolean motionV2SuperResOutputEnabled = false;
     public float motionV2SuperResOutputScale = 1.0f;
-    /* IRIS_26621_TRUE2X_SHARED_LOCAL_TONE_MAP
-     * Full-resolution R16F log2 SDR target produced by the MotionV2Render Local-Laplacian owner.
-     * This is an absolute presentation target, not a coarse correction/gain field. CPU/GPU true-2x
-     * publication samples the same source-aligned field so SR cannot solve an independent tone map. */
-    public ByteBuffer motionV2LocalToneLogMap = null;
-    public int motionV2LocalToneLogWidth = 0;
-    public int motionV2LocalToneLogHeight = 0;
-    public int motionV2LocalToneLogSourceWidth = 0;
-    public int motionV2LocalToneLogSourceHeight = 0;
+    /* IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN_MAP
+     * Half-resolution R16F correction field produced by the 1x MotionV2Render multiscale owner.
+     * It is presentation-only, direct-buffer backed, and consumed synchronously by true-2x
+     * publication so SR cannot solve an independent local tone field. */
+    public ByteBuffer motionV2LocalLaplacianMap = null;
+    public int motionV2LocalLaplacianWidth = 0;
+    public int motionV2LocalLaplacianHeight = 0;
+    public int motionV2LocalLaplacianSourceWidth = 0;
+    public int motionV2LocalLaplacianSourceHeight = 0;
     public float motionV2HardwareZoom = 1.0f;
     public float motionV2ResidualSoftwareZoom = 1.0f;
     public int realWL = -1;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index 2d544c6536e5028f6f0b1c5d863834c61df93432..ce36fb537c3e35b7bead084e78a27ea5c471b2b8 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -156,24 +156,24 @@ public final class MotionV2Jpeg444Encoder {
                     ? attachedGainmap.getDisplayRatioForFullHdr() : 1.0f;
             final float publicationSceneWhite =
                     MotionV2Render.iris26598PublicationSceneWhite(parameters);
-            final ByteBuffer localToneLogMap = parameters.motionV2LocalToneLogMap;
-            final int localToneLogWidth = parameters.motionV2LocalToneLogWidth;
-            final int localToneLogHeight = parameters.motionV2LocalToneLogHeight;
-            final int localToneLogSourceWidth = parameters.motionV2LocalToneLogSourceWidth;
-            final int localToneLogSourceHeight = parameters.motionV2LocalToneLogSourceHeight;
+            final ByteBuffer localLaplacianMap = parameters.motionV2LocalLaplacianMap;
+            final int localLaplacianWidth = parameters.motionV2LocalLaplacianWidth;
+            final int localLaplacianHeight = parameters.motionV2LocalLaplacianHeight;
+            final int localLaplacianSourceWidth = parameters.motionV2LocalLaplacianSourceWidth;
+            final int localLaplacianSourceHeight = parameters.motionV2LocalLaplacianSourceHeight;
             if (parameters.motionV2Active) {
-                long expectedBytes = 2L * localToneLogWidth * localToneLogHeight;
-                if (localToneLogMap == null || !localToneLogMap.isDirect()
-                        || localToneLogWidth <= 0 || localToneLogHeight <= 0
-                        || localToneLogSourceWidth <= 0 || localToneLogSourceHeight <= 0
-                        || expectedBytes <= 0L || localToneLogMap.capacity() < expectedBytes) {
-                    Log.e(TAG, "IRIS_26621_TRUE2X_LOCAL_TONE_MAP_MISSING"
-                            + " map=" + (localToneLogMap != null)
-                            + " direct=" + (localToneLogMap != null && localToneLogMap.isDirect())
-                            + " size=" + localToneLogWidth + "x" + localToneLogHeight
-                            + " source=" + localToneLogSourceWidth + "x" + localToneLogSourceHeight
+                long expectedBytes = 2L * localLaplacianWidth * localLaplacianHeight;
+                if (localLaplacianMap == null || !localLaplacianMap.isDirect()
+                        || localLaplacianWidth <= 0 || localLaplacianHeight <= 0
+                        || localLaplacianSourceWidth <= 0 || localLaplacianSourceHeight <= 0
+                        || expectedBytes <= 0L || localLaplacianMap.capacity() < expectedBytes) {
+                    Log.e(TAG, "IRIS_26620_TRUE2X_LOCAL_LAPLACIAN_MAP_MISSING"
+                            + " map=" + (localLaplacianMap != null)
+                            + " direct=" + (localLaplacianMap != null && localLaplacianMap.isDirect())
+                            + " size=" + localLaplacianWidth + "x" + localLaplacianHeight
+                            + " source=" + localLaplacianSourceWidth + "x" + localLaplacianSourceHeight
                             + " expectedBytes=" + expectedBytes
-                            + " capacity=" + (localToneLogMap != null ? localToneLogMap.capacity() : 0));
+                            + " capacity=" + (localLaplacianMap != null ? localLaplacianMap.capacity() : 0));
                     return false;
                 }
             }
@@ -185,8 +185,8 @@ public final class MotionV2Jpeg444Encoder {
                     sensorToProPhoto, profileToDisplay,
                     parameters.motionV2DisplayGain, exposureEv, shadows, contrast,
                     publicationSceneWhite, parameters.motionV2Active,
-                    localToneLogMap, localToneLogWidth, localToneLogHeight,
-                    localToneLogSourceWidth, localToneLogSourceHeight,
+                    localLaplacianMap, localLaplacianWidth, localLaplacianHeight,
+                    localLaplacianSourceWidth, localLaplacianSourceHeight,
                     watermark,
                     useJin ? jinResidual.residual : null,
                     useJin ? jinResidual.referenceRgb : null,
@@ -203,13 +203,13 @@ public final class MotionV2Jpeg444Encoder {
                     + " gainEncodingMax=" + true2xGainEncodingMax
                     + " gainContentMax=" + true2xGainContentMax
                     + " publicationSceneWhite=" + publicationSceneWhite
-                    + " iris26621SdrWhiteAnchor=" + MotionV2Render.IRIS_26621_SDR_WHITE_ANCHOR
+                    + " iris26614SdrWhiteAnchor=" + MotionV2Render.IRIS_26614_SDR_WHITE_ANCHOR
                     + " iris26614DisplayDomainKneeAuthority=false"
-                    + " iris26614CanonicalAppearanceGainAuthority=false"
-                    + " IRIS_26621_TRUE2X_NEW_SIMPLIFIED_PRESENTATION=true"
-                    + " IRIS_26621_TRUE2X_SHARED_LOCAL_TONE=" + parameters.motionV2Active
-                    + " localToneLogSize=" + localToneLogWidth + "x" + localToneLogHeight
-                    + " localToneLogSource=" + localToneLogSourceWidth + "x" + localToneLogSourceHeight
+                    + " iris26614CanonicalAppearanceGainAuthority=true"
+                    + " IRIS_26614_TRUE2X_SOURCE_DOMAIN_PRESENTATION=true"
+                    + " IRIS_26620_TRUE2X_SHARED_LOCAL_LAPLACIAN=" + parameters.motionV2Active
+                    + " localLaplacianSize=" + localLaplacianWidth + "x" + localLaplacianHeight
+                    + " localLaplacianSource=" + localLaplacianSourceWidth + "x" + localLaplacianSourceHeight
                     + " adaptiveSceneWhite=" + parameters.motionV2ToneAdaptiveSceneWhite
                     + " publicationSceneWhiteSource="
                         + (parameters.motionV2Active ? "BASE_26598_MOTION" : "ADAPTIVE_26591_NIGHT")
@@ -291,11 +291,11 @@ public final class MotionV2Jpeg444Encoder {
             return false;
         } finally {
             if (parameters != null && parameters.motionV2Active) {
-                parameters.motionV2LocalToneLogMap = null;
-                parameters.motionV2LocalToneLogWidth = 0;
-                parameters.motionV2LocalToneLogHeight = 0;
-                parameters.motionV2LocalToneLogSourceWidth = 0;
-                parameters.motionV2LocalToneLogSourceHeight = 0;
+                parameters.motionV2LocalLaplacianMap = null;
+                parameters.motionV2LocalLaplacianWidth = 0;
+                parameters.motionV2LocalLaplacianHeight = 0;
+                parameters.motionV2LocalLaplacianSourceWidth = 0;
+                parameters.motionV2LocalLaplacianSourceHeight = 0;
             }
             if (watermark != null && !watermark.isRecycled()) watermark.recycle();
             try { if (base != null) Files.deleteIfExists(base); } catch (Throwable ignored) {}
@@ -350,8 +350,8 @@ public final class MotionV2Jpeg444Encoder {
             int rotation, boolean mirror, float renderResidualZoom,
             float[] sensorToProPhoto, float[] proPhotoToSrgb, float displayGain,
             float exposureEv, float shadows, float contrast, float sceneWhite, boolean motionHdrHandoff,
-            ByteBuffer localToneLogMap, int localToneLogWidth, int localToneLogHeight,
-            int localToneLogSourceWidth, int localToneLogSourceHeight,
+            ByteBuffer localLaplacianMap, int localLaplacianWidth, int localLaplacianHeight,
+            int localLaplacianSourceWidth, int localLaplacianSourceHeight,
             Bitmap watermark,
             float[] jinResidual, int[] jinReferenceRgb, int jinWidth, int jinHeight,
             String true2xGainmapPath, float true2xGainmapMaxRatio,
diff --git a/app/version.properties b/app/version.properties
index 7d73e7195c3715dea3e232817e3c6b2d29b6572a..aae15f2c8d43a9ec15c664d8e9bf6079fcc615f3 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726621
-VERSION_BUILD=26621
+VERSION_NAME=0.9726620
+VERSION_BUILD=26620
