PHOTON 26593 V1 WORKFLOW / PACKAGING PERMANENT REGRESSIONS

Authority seeding:
The canonical candidate must begin from the exact successful 26592 compiled-candidate app-file universe, then overlay only the exact four allowlisted 26593 runtime files. Repository app/src is never runtime authority.

Permanent runtime-scope rules:
- app/build/** and app/.cxx/** never count as runtime-source changes.
- repository-only module scaffolding absent from compiled-candidate authority never counts as a runtime change.
- unexpected files or byte changes inside actual app/src/** remain visible and fail.
- strict runtime allowlist equality is never weakened.
- the compiler-tested candidate must remain byte-identical through full assemble and final export.

Patch rules:
Forward and rollback patches are deterministic full-index patches. They must pass core.abbrev 7/12/40 with fuzz=0 and reproduce exact candidate/base bytes.

Compiler rules:
Static validators never substitute for real compilers. Actions must run the pinned real GLSL compiler over all inherited runtime-expanded variants, real project Kotlin and Java compilers, both NDK ABIs, then full :app:assembleDebug before the handoff is build-proven.

Packaging regression carried from failed 26592 V1 Actions run 33826394252:
Every literal regression asserted by the workflow must be checked against the sealed package before upload; workflow/package wording drift must fail locally rather than escape to Actions.

26593 LOCAL PACKAGING CATCH PERMANENT REGRESSION:
The first packaged authority replay referenced stale VENDOR_PROTECTED/DNG_PROTECTED manifest filenames while the sealed 26593 manifests were named VENDOR_BASE/VENDOR_CANDIDATE and DNG_BASE/DNG_CANDIDATE. The authority validator must reference the exact sealed manifest filenames, and clean-package replay must execute that validator before delivery.
