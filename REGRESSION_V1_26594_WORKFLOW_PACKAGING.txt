PHOTON 26594 V1 HANDOFF PACKAGING PERMANENT REGRESSIONS

1. COMMENT-VS-CODE FALSE POSITIVE: an early validator searched raw source text for a forbidden
   phrase and rejected the safety comment saying that phrase was not used. Validators must test
   executable ownership/identifiers or exact required source structures, not negative prose in
   comments.
2. TRANSIENT MANIFEST CONTAMINATION: failed replay work/output directories must never enter the
   handoff universe. V1_26594_HANDOFF_HASHES.sha256 is generated only from clean staging and the
   package rejects .build_*, build_*_outputs, app/build/**, app/.cxx/**, __pycache__, *.pyc and APK.
3. ROLLBACK PATCH GENERATION: git diff -R of the forward workspace is insufficient. Rollback must
   be independently regenerated with the candidate committed as authority, exact prior bytes
   overlaid, and git diff --binary --full-index. It must be byte-identical at core.abbrev 7/12/40,
   apply with fuzz=0 and restore exact prior full-app bytes.
4. NESTED-SOURCE ESCAPE REGRESSION: an early packaging generator emitted Python string escape
   sequences as literal newlines inside verify_26594_v1_shaders.py, making the sealed verifier
   syntactically invalid. The sealed Python syntax/import gate is mandatory before authority work,
   and final clean-extract replay must run that exact packaged verifier rather than a staging copy.
5. FUNCTION-DEFINITION ORDER REGRESSION: an early infrastructure verifier used the first raw text
   occurrence of build-stage names, so helper-function definitions could be mistaken for execution
   order. Ordering checks must separately inspect the top-level invocation sequence and the
   install_and_build function body that actually executes compiler/build stages.

ACTIONS INLINE-PYTHON FAILURE 26594 V1 PERMANENT REGRESSION:
The first uploaded 26594 V1 handoff reached Actions and failed in verify_scope before authority reconstruction because the expected-scope Python heredoc contained a literal newline inside print('...'), producing `SyntaxError: unterminated string literal`. `bash -n` cannot validate Python embedded in a quoted heredoc, and local-prebuild bypasses branch-only verify_scope.

Permanent invariant:
- the branch-scope heredoc must retain the successful-26593 escaped form `print('\n'.join(sorted(names)))`;
- every quoted Python heredoc embedded in the packaged build script must be extracted and compiled during verify_package before verify_scope;
- clean-extract final replay must run that embedded-heredoc syntax gate;
- this infrastructure correction must not alter any 26594 runtime payload byte, candidate manifest, runtime forward/rollback patch, shader pin, version, or build/compiler ordering.
