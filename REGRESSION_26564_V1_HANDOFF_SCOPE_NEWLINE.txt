IRIS 26564 PERMANENT REGRESSION — V1 HANDOFF-SCOPE NEWLINE PARSER

GitHub Actions failure: run 33283538711, job 99182648927.
The V1 build script parsed V1_26564_HANDOFF_HASHES.sha256 with open(file) and used
line.split('  ', 1)[1] without removing the trailing line ending. Every expected filename
therefore ended in a literal "\n", while git diff --name-only returned clean filenames.
The gate falsely reported every real handoff file as extra and every expected file as missing.

Permanent conditions for every applicable future handoff:
1. Hash-manifest filename parsing must remove line endings before comparison (splitlines or exact equivalent).
2. A self-test must reproduce a newline-terminated SHA manifest and prove parsed names are exactly clean paths.
3. The local packaged replay must execute this parser regression; it may not skip the GitHub-only scope parser silently.
4. The authoritative Actions gate must compare the exact base..HEAD changed-file set and reject any app/ runtime source write.
5. This infrastructure correction must not alter the frozen 26564 runtime candidate, transform, runtime patches, or runtime manifests.
