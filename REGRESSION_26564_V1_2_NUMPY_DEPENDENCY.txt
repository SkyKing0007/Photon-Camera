IRIS 26564 PERMANENT REGRESSION — V1.2 UNDECLARED NUMPY DEPENDENCY

GitHub Actions failure: run 33284552163, job 99185364515.
V1.2 passed sealed-package scope, exact successful 26563 artifact reconstruction, the
nested-Git transform regression, exact candidate hashes, protected-core invariance,
true-2x semantic validation 45/45, and bounded-memory validation 12/12. It then failed
before real glslang/Kotlin/Java/NDK/full assemble because
`test_26564_true2x_cpu_gpu_numeric_parity.py` imported NumPy, while the proven 26563
workflow intentionally installs only Python 3.12 and no third-party Python packages.

Permanent conditions for every applicable future handoff:
1. Do not add pip/NumPy/scipy or any third-party Python dependency to the authoritative build environment unless a future architecture intentionally replaces the proven environment contract.
2. The true-2x CPU/GPU numeric parity fixture must be Python-standard-library only.
3. The parity fixture must execute successfully under `python3 -S`, which disables site-packages.
4. Packaged Python proof tools must be statically checked so their top-level imports resolve only to Python 3.12 standard-library modules.
5. The workflow must retain the successful-26563 environment model: actions/setup-python@v5 with Python 3.12 and no pip-install step.
6. Do not "fix" this regression by installing NumPy; preserve the minimal proven Actions environment.
