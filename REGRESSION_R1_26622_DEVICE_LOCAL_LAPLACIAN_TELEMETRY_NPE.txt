REGRESSION R1 26622 — DEVICE LOCAL-LAPLACIAN TELEMETRY LIFETIME NPE

Exact observed device condition, 2026-09-10:
- Two consecutive Motion shots reproduced the same failure.
- Upstream Sabre/color/viewfinder/adaptive-color/display-exposure stages completed.
- Failure: java.lang.NullPointerException reading GLTexture.mSize in
  MotionV2Render.iris26621BuildLocalLaplacianTone(MotionV2Render.java:416).
- 26621 source releases guide[] and sets guide[level] = null, then the telemetry string
  dereferences guide[IRIS_26621_LAPLACIAN_LEVELS - 1].mSize.
- Because the exception occurs before keepFinal=true, the function's finally block closes finalTone
  and the capture aborts before JPEG publication.

Permanent applicable regression:
1. Successful 26621 authority must reproduce the post-release telemetry dereference ordering.
2. Candidate must snapshot coarsest width/height before guide release.
3. After the first guide[level] = null, candidate must contain zero accesses to
   guide[IRIS_26621_LAPLACIAN_LEVELS - 1].mSize.
4. Telemetry must use the saved primitive dimensions.
5. The fix must not bypass/disable iris26621BuildLocalLaplacianTone.
6. All 26621 global-tone and Local-Laplacian shaders/math remain byte-identical.
