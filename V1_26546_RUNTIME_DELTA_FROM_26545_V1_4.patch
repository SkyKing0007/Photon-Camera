diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
index bddf37712b321591bfce3342879f081bd51256a1..50e4a09c2a0312e4453294ff3868d24cca8545d0 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26521SpatialRgbStacker.kt
@@ -65,6 +65,7 @@ internal class GlesIris26521SpatialRgbStacker(
     /* IRIS_26532_STREAMED_SUPERRES_TEMP_OWNER */
     private val superResTempDir: File? = null,
     private val superResWriteLinearRaw: Boolean = false,
+    private val vgnChromaCorrectionStrength: Float = 1f,
 ) {
     private data class TextureLevel(
         val texture: Int,
@@ -7003,6 +7004,7 @@ internal class GlesIris26521SpatialRgbStacker(
             imageHeight = imageHeight,
             calculationWbGains = calculationWhiteBalance,
             outputScale = iirOutputScale,
+            chromaCorrectionStrength = vgnChromaCorrectionStrength,
             exportFullSizeTexture = exportGpuLinearRgbSource,
             host = object : GlesIris26529SpatialRgbChromaPostprocessor.Host {
                 override fun linkComputeProgram(source: String, name: String): Int =
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
index 8ba1d4439da0735b959856f54dd8bc1518b66a3f..c38db12b585928d62e125150ff725121f7362b92 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
@@ -29,6 +29,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
     private val imageHeight: Int,
     calculationWbGains: FloatArray,
     outputScale: Float,
+    chromaCorrectionStrength: Float = 1f,
     private val host: Host,
     private val exportFullSizeTexture: Boolean = false,
 ) {
@@ -56,6 +57,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
     ).also { gains ->
         require(gains.all { it.isFinite() && it > 0f })
     }
+    private val chromaCorrectionStrength = chromaCorrectionStrength.coerceIn(0f, 1f)
     private val coefficients = Iris26529SpatialRgbIirCoefficients.forOutputScale(
         outputScale.takeIf { it.isFinite() && it > 0f } ?: 1f,
     )
@@ -144,7 +146,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
         // then workA = directional YCCD and assembledRgb = restored-direction YCCD.
         dispatchSeed(assembledRgb, workA)
         dispatchLocal(localClampProgram, workA, workB, "local clamp")
-        dispatchLocal(localMedianProgram, workB, assembledRgb, "local median")
+        dispatchLocal(localMedianProgram, workB, assembledRgb, "local median", chromaCorrectionStrength)
         dispatchDirectional(workB, assembledRgb, workA)
         dispatchRestoreDirection(workA, workB, assembledRgb)
 
@@ -251,15 +253,25 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
         dispatch2d(seedProgram, intArrayOf(source), destination, "seed")
     }
 
-    private fun dispatchLocal(program: Int, source: Int, destination: Int, label: String) {
+    private fun dispatchLocal(
+        program: Int,
+        source: Int,
+        destination: Int,
+        label: String,
+        chromaStrength: Float? = null,
+    ) {
         GLES31.glUseProgram(program)
         setImageSize(program)
+        chromaStrength?.let {
+            GLES31.glUniform1f(host.uniformLocation(program, "uChromaStrength"), it)
+        }
         dispatch2d(program, intArrayOf(source), destination, label)
     }
 
     private fun dispatchDirectional(source: Int, destination: Int, directionalSource: Int) {
         GLES31.glUseProgram(directionalProgram)
         setImageSize(directionalProgram)
+        GLES31.glUniform1f(host.uniformLocation(directionalProgram, "uChromaStrength"), chromaCorrectionStrength)
         bindImage(0, source, GLES31.GL_READ_ONLY)
         bindImage(1, destination, GLES31.GL_READ_ONLY)
         bindImage(2, directionalSource, GLES31.GL_WRITE_ONLY)
@@ -290,6 +302,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
     private fun dispatchBlend(original: Int, smooth: Int, destination: Int) {
         GLES31.glUseProgram(blendProgram)
         setImageSize(blendProgram)
+        GLES31.glUniform1f(host.uniformLocation(blendProgram, "uChromaStrength"), chromaCorrectionStrength)
         bindImage(0, original, GLES31.GL_READ_ONLY)
         bindImage(1, smooth, GLES31.GL_READ_ONLY)
         bindImage(2, destination, GLES31.GL_WRITE_ONLY)
@@ -319,6 +332,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
             setImageSize(iirRgbProgram)
             setIirCoefficients(iirRgbProgram, pass)
             GLES31.glUniform1i(host.uniformLocation(iirRgbProgram, "uFilterLuma"), if (filterLuma) 1 else 0)
+            GLES31.glUniform1f(host.uniformLocation(iirRgbProgram, "uChromaStrength"), chromaCorrectionStrength)
             GLES31.glUniform1i(host.uniformLocation(iirRgbProgram, "uDirection"), d[0])
             GLES31.glUniform1i(host.uniformLocation(iirRgbProgram, "uAxis"), d[1])
             bindImage(0, input, GLES31.GL_READ_ONLY)
@@ -614,6 +628,7 @@ internal object Iris26529SpatialRgbChromaShaders {
         layout(local_size_x=8,local_size_y=8) in;
         layout(rgba16ui,binding=0) readonly uniform highp uimage2D uInput;
         layout(rgba16ui,binding=1) writeonly uniform highp uimage2D uOutput;
+        uniform float uChromaStrength;
         $common
         ivec3 s(ivec2 p){ uvec4 v=imageLoad(uInput,safePos(p)); return ivec3(int(v.r),signedChroma(v.g),signedChroma(v.b)); }
         void main(){
@@ -623,10 +638,13 @@ internal object Iris26529SpatialRgbChromaShaders {
             a=s(p+ivec2(-1,0));b=s(p);c=s(p+ivec2(1,0)); row=a.yz+b.yz+c.yz-min(min(a.yz,b.yz),c.yz)-max(max(a.yz,b.yz),c.yz); sum+=row; mn=min(mn,row); mx=max(mx,row); ysum+=2*a.x+4*b.x+2*c.x;
             a=s(p+ivec2(-1,1));b=s(p+ivec2(0,1));c=s(p+ivec2(1,1)); row=a.yz+b.yz+c.yz-min(min(a.yz,b.yz),c.yz)-max(max(a.yz,b.yz),c.yz); sum+=row-min(mn,row)-max(mx,row); ysum+=a.x+2*b.x+c.x;
             uvec4 center=imageLoad(uInput,p);
+            ivec2 centerChroma=ivec2(signedChroma(center.g),signedChroma(center.b));
+            float strength=clamp(uChromaStrength,0.0,1.0);
+            ivec2 corrected=strength>=0.9999?sum:(strength<=0.0001?centerChroma:ivec2(mix(vec2(centerChroma),vec2(sum),strength)));
             imageStore(uOutput,p,uvec4(
                 uint(max(ysum/16,0)),
-                unsignedChroma(sum.x),
-                unsignedChroma(sum.y),
+                unsignedChroma(corrected.x),
+                unsignedChroma(corrected.y),
                 center.a
             ));
         }
@@ -639,6 +657,7 @@ internal object Iris26529SpatialRgbChromaShaders {
         layout(rgba16ui,binding=0) readonly uniform highp uimage2D uOriginal;
         layout(rgba16ui,binding=1) readonly uniform highp uimage2D uSmooth;
         layout(rgba16ui,binding=2) writeonly uniform highp uimage2D uOutput;
+        uniform float uChromaStrength;
         $common
         vec3 smoothAt(ivec2 p){uvec4 v=imageLoad(uSmooth,safePos(p));return vec3(decodeU16(v.r),float(signedChroma(v.g)),float(signedChroma(v.b)));}
         vec4 filter3(uvec4 encoded,ivec2 p){
@@ -659,7 +678,10 @@ internal object Iris26529SpatialRgbChromaShaders {
             if(e.r!=0u&&e.a!=0u)f=filter3(e,p);
             uvec4 sm=imageLoad(uSmooth,p);ivec2 sc=ivec2(signedChroma(sm.g),signedChroma(sm.b));ivec2 fc=ivec2(f.yz);
             if(abs(sc.x)+abs(sc.y)<abs(fc.x)+abs(fc.y))fc=sc;
-            imageStore(uOutput,p,uvec4(uint(clamp(f.x,0.0,65504.0)),unsignedChroma(fc.x),unsignedChroma(fc.y),uint(clamp(f.w,0.0,65504.0))));
+            ivec2 originalChroma=ivec2(signedChroma(e.g),signedChroma(e.b));
+            float strength=clamp(uChromaStrength,0.0,1.0);
+            ivec2 corrected=strength>=0.9999?fc:(strength<=0.0001?originalChroma:ivec2(mix(vec2(originalChroma),vec2(fc),strength)));
+            imageStore(uOutput,p,uvec4(uint(clamp(f.x,0.0,65504.0)),unsignedChroma(corrected.x),unsignedChroma(corrected.y),uint(clamp(f.w,0.0,65504.0))));
         }
     """.trimIndent()
 
@@ -680,7 +702,7 @@ internal object Iris26529SpatialRgbChromaShaders {
         layout(local_size_x=1,local_size_y=1) in;
         layout(rgba16ui,binding=0) readonly uniform highp uimage2D uInput;
         layout(rgba16ui,binding=1) writeonly uniform highp uimage2D uOutput;
-        uniform vec4 uA10;uniform vec4 uB10;uniform vec4 uADyn1;uniform vec4 uBDyn1;uniform vec4 uADyn2;uniform vec4 uBDyn2;uniform int uDirection;uniform int uAxis;uniform int uFilterLuma;
+        uniform vec4 uA10;uniform vec4 uB10;uniform vec4 uADyn1;uniform vec4 uBDyn1;uniform vec4 uADyn2;uniform vec4 uBDyn2;uniform int uDirection;uniform int uAxis;uniform int uFilterLuma;uniform float uChromaStrength;
         $common
         struct State{float x0;float x1;float y0;float y1;};
         float apply(inout State s,float v,vec4 a,vec4 b,bool unsignedOut){float r=a[0]*v+a[1]*s.x0+a[2]*s.x1-b[1]*s.y0-b[2]*s.y1;s.x1=s.x0;s.y1=s.y0;s.x0=v;s.y0=unsignedOut?clamp(r,0.0,65504.0):r;return s.y0;}
@@ -695,6 +717,8 @@ internal object Iris26529SpatialRgbChromaShaders {
                 ivec2 p=pos(start+i*step,outer);uvec4 px=imageLoad(uInput,p);float currentY=decodeU16(px.r);float inCr=float(signedChroma(px.g)),inCb=float(signedChroma(px.b));
                 float y=uFilterLuma!=0?apply(ys,currentY,uA10,uB10,true):currentY;
                 float r=apply(c1,inCr,uADyn1,uBDyn1,false);float q=apply(b1,inCb,uADyn1,uBDyn1,false);r=apply(c2,r,uADyn2,uBDyn2,false);q=apply(b2,q,uADyn2,uBDyn2,false);
+                float strength=clamp(uChromaStrength,0.0,1.0);
+                if(strength<0.9999){r=strength<=0.0001?inCr:mix(inCr,r,strength);q=strength<=0.0001?inCb:mix(inCb,q,strength);}
                 imageStore(uOutput,p,uvec4(uint(clamp(y,0.0,65504.0)),unsignedChroma(int(r)),unsignedChroma(int(q)),px.a));
             }
         }
@@ -732,12 +756,13 @@ internal object Iris26529SpatialRgbChromaShaders {
         layout(rgba16ui,binding=0) readonly uniform highp uimage2D uOriginal;
         layout(rgba16ui,binding=1) readonly uniform highp uimage2D uSmooth;
         layout(rgba16ui,binding=2) writeonly uniform highp uimage2D uOutput;
+        uniform float uChromaStrength;
         $common
         float scale(float low,float v,float high){return(clamp(v,low,high)-low)/(high-low);}
         void main(){
             ivec2 p=ivec2(gl_GlobalInvocationID.xy);if(any(greaterThanEqual(p,uImageSize)))return;uvec4 o=imageLoad(uOriginal,p),s=imageLoad(uSmooth,p);
             int cr=signedChroma(o.g),cb=signedChroma(o.b),sr=signedChroma(s.g),sb=signedChroma(s.b);float errorScale=scale(100.0,decodeU16(s.a)*0.25,300.0);float smoothSat=1.0+float(abs(sr))+float(abs(sb));float normalSat=float(abs(cr))+float(abs(cb));
-            float f=errorScale*scale(0.5,normalSat/smoothSat,1.0);
+            float strength=clamp(uChromaStrength,0.0,1.0);float f=errorScale*scale(0.5,normalSat/smoothSat,1.0);if(strength<0.9999)f*=strength;
             o.g=unsignedChroma(int(mix(float(cr),float(sr),f)));o.b=unsignedChroma(int(mix(float(cb),float(sb),f)));o.a=s.a;imageStore(uOutput,p,o);
         }
     """.trimIndent()
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
index 9dcf5279d12dd66316d6b7e5878172cf9e0e2e1d..bfd4c82607fec90530684b4a72c0ee84f75a225e 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26545SabreProcessor.kt
@@ -27,6 +27,7 @@ internal class GlesIris26545SabreProcessor(
     private val exportGpuLinearRgbSource: Boolean,
     private val gpuLinearRgbStorage: GpuLinearRgbStorage,
     private val exportNormalStackedDng: Boolean,
+    private val vgnChromaCorrectionStrength: Float = 1f,
 ) {
     fun processFrames(frames: List<RawStackFrame>): RawStackResult? {
         if (frames.isEmpty()) return null
@@ -83,6 +84,7 @@ internal class GlesIris26545SabreProcessor(
             gpuLinearRgbStorage = gpuLinearRgbStorage,
             processorPipeline = MgcRawProcessorPipeline.SABRE,
             exportNormalStackedDng = exportNormalStackedDng,
+            vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
         ).processFrames(admitted)
     }
 
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
index 9145776f2d8c1003e79784441ddbd28e9504e82e..edd8e9a993c987313803b0e138b04c76cc25d1a9 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawFusion.kt
@@ -36,6 +36,7 @@ internal class GlesMgcRawFusion(
     /* IRIS_26532_STREAMED_SUPERRES_TEMP_OWNER */
     private val superResTempDir: File? = null,
     private val superResWriteLinearRaw: Boolean = false,
+    private val vgnChromaCorrectionStrength: Float = 1f,
 ) {
     private val outputScale = MultiFrameConfig.normalizeOutputScale(outputScale)
 
@@ -66,6 +67,7 @@ internal class GlesMgcRawFusion(
                 exportGpuLinearRgbSource = exportGpuLinearRgbSource,
                 gpuLinearRgbStorage = gpuLinearRgbStorage,
                 exportNormalStackedDng = exportNormalStackedDng,
+                vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
             ).processFrames(frames)
         }
 
@@ -168,6 +170,7 @@ internal class GlesMgcRawFusion(
                 exportNormalStackedDng = exportNormalStackedDng,
                 superResTempDir = superResTempDir,
                 superResWriteLinearRaw = superResWriteLinearRaw,
+                vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
             ).processFrames(scheduledFrames)
         }
         return GlesMgcRawSpatialStacker(
@@ -187,6 +190,7 @@ internal class GlesMgcRawFusion(
             useCurrentGlContext = useCurrentGlContext,
             exportGpuLinearRgbSource = exportGpuLinearRgbSource,
             gpuLinearRgbStorage = gpuLinearRgbStorage,
+            vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
         ).processFrames(scheduledFrames)
     }
 
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 871448cbc979970c0f0d77f6cd16405382d01c7a..a4a8d3e124d254213e3b948f8499d8b93dbed605 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -60,6 +60,7 @@ internal class GlesMgcRawSpatialStacker(
     // Iris has no PhotonCoreImagingTuning owner. Keep current-MGC's optional Sabre override
     // at this low-level boundary while defaulting to the exact adaptive SNR interpolation.
     private val sabreMergeGradientThreshold: Float? = null,
+    private val vgnChromaCorrectionStrength: Float = 1f,
 ) {
     private data class TextureLevel(
         val texture: Int,
@@ -3154,6 +3155,7 @@ internal class GlesMgcRawSpatialStacker(
             imageHeight = height,
             calculationWbGains = calculationWhiteBalance,
             outputScale = 1f,
+            chromaCorrectionStrength = vgnChromaCorrectionStrength,
             host = object : GlesIris26529SpatialRgbChromaPostprocessor.Host {
                 override fun linkComputeProgram(source: String, name: String): Int =
                     this@GlesMgcRawSpatialStacker.linkComputeProgram(source, name)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index 762cde240511db8e159efe79e6df42c8d5066667..b51fd6a945721d65c54daddf9605ab67f1f79003 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -6563,11 +6563,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     captureBuilder.addTarget(mImageReaderPreview.getSurface());
             } else {
                 captureBuilder.addTarget(mImageReaderRaw.getSurface());
-                final boolean iris26540AddPreviewTarget = iris26533CaptureMode == CameraMode.NIGHT
-                        ? frametime > 0.06
-                        : (frametime > 0.06 || iris26533CaptureMode == CameraMode.RAWVIDEO
+                /* IRIS_26546_NIGHT_PREVIEW_SURFACE_ISOLATION
+                 * Night RAW still requests must never drive the repeating preview surface.
+                 * The final 3 long (+2 EV) frames previously made the viewfinder brighten
+                 * and stall near 12/15 even though RAW capture itself was healthy.
+                 */
+                final boolean iris26546AddPreviewTarget = iris26533CaptureMode != CameraMode.NIGHT
+                        && (frametime > 0.06 || iris26533CaptureMode == CameraMode.RAWVIDEO
                         || iris26533CaptureMode == CameraMode.UNLIMITED || !IsoExpoSelector.HDR);
-                if (iris26540AddPreviewTarget) captureBuilder.addTarget(surface);
+                if (iris26546AddPreviewTarget) captureBuilder.addTarget(surface);
+                else if (iris26533CaptureMode == CameraMode.NIGHT)
+                    Log.i(TAG, "IRIS_26546_NIGHT_PREVIEW_SURFACE_ISOLATED stillTarget=false repeatingPreviewIndependent=true");
             }
             if (iris26533CaptureMode != CameraMode.NIGHT) Camera2ApiAutoFix.applyEnergySaving();
             else Log.i(TAG, "IRIS_26540_NIGHT_BYPASS photonApplyEnergySaving=true");
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/IrisNightRgbInput.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/IrisNightRgbInput.java
index 70ea8105362d52d2b779d3a6f79ff33f29540a89..8105ff2ff830831b7a4ef83da8104ff4e755f41e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/IrisNightRgbInput.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/IrisNightRgbInput.java
@@ -7,18 +7,19 @@ import com.particlesdevs.photoncamera.processing.opengl.GLFormat;
 import com.particlesdevs.photoncamera.processing.opengl.GLTexture;
 import com.particlesdevs.photoncamera.processing.opengl.nodes.Node;
 import com.particlesdevs.photoncamera.util.Log;
+import com.particlesdevs.photoncamera.util.Allocator;
 
 import java.nio.ByteBuffer;
 
 import static android.opengl.GLES20.GL_CLAMP_TO_EDGE;
 import static android.opengl.GLES20.GL_LINEAR;
-import static android.opengl.GLES20.GL_NEAREST;
 
 /**
  * IRIS_26538_NIGHT_RGBA16F_TWO_BUFFER_INPUT
  * Dedicated Night post input. MGC already returns full-resolution camera-linear RGBA16F, so Night
- * uploads that carrier directly and allocates only the main1/main2 half-float ping-pong pair used
- * by every active Night node. It intentionally does not allocate Motion's FLOAT32 upload texture
+ * uploads that carrier directly into main1, releases the CPU carrier, then allocates only main2.
+ * Thus Night owns exactly two full-resolution half-float GPU textures at post entry. It intentionally
+ * does not allocate Motion's FLOAT32 upload texture
  * plus main3. No Bayer/RCD/demosaic or legacy Photon Night processing exists in this route.
  */
 public final class IrisNightRgbInput extends Node {
@@ -43,18 +44,30 @@ public final class IrisNightRgbInput extends Node {
         ByteBuffer view = source.duplicate();
         view.position(0);
         GLFormat rgba16f = new GLFormat(GLFormat.DataType.FLOAT_16, 4);
-        WorkingTexture = new GLTexture(raw, rgba16f, view, GL_NEAREST, GL_CLAMP_TO_EDGE);
+        // IRIS_26546_NIGHT_TRUE_TWO_GPU_BUFFER_INPUT
+        // main1 is the upload texture itself. There is no third full-resolution upload texture.
+        basePipeline.main1 = new GLTexture(raw, rgba16f, view, GL_LINEAR, GL_CLAMP_TO_EDGE);
+        WorkingTexture = basePipeline.main1;
+
+        // glTexSubImage2D has consumed the client bytes before GLTexture construction returns.
+        // Transfer/free the ~96 MiB CPU carrier before allocating main2 to bound peak residency.
+        pipeline.irisNightRgba16f = null;
+        Allocator.free(source);
+        source = null;
+        view = null;
+        Log.critical(Name, "IRIS_26546_NIGHT_CPU_RGBA16F_RELEASED_AFTER_UPLOAD bytes=" + expected);
 
         GLFormat work = new GLFormat(GLFormat.DataType.FLOAT_16, GLDrawParams.WorkDim);
-        basePipeline.main1 = new GLTexture(raw, work, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
         basePipeline.main2 = new GLTexture(raw, work, null, GL_LINEAR, GL_CLAMP_TO_EDGE);
         basePipeline.main3 = null;
-        basePipeline.texnum = 0;
+        // main1 is current input; first downstream getMain() must return main2, preventing aliasing.
+        basePipeline.texnum = 1;
         glProg.closed = true;
 
-        Log.i(Name, "IRIS_26538_NIGHT_RGBA16F_TWO_BUFFER_INPUT"
+        Log.i(Name, "IRIS_26546_NIGHT_RGBA16F_TRUE_TWO_BUFFER_INPUT"
                 + " carrier=fullResCameraLinearRGBA16F"
-                + " main1=true main2=true main3=false"
+                + " uploadIsMain1=true main1=true main2=true main3=false texnum=1"
+                + " cpuCarrierReleasedBeforeMain2=true"
                 + " float32Expansion=false rcd=false demosaic=false photonNight=false"
                 + " bytes=" + expected);
     }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index 658543fe6ac18113ff21a9112195889d59893b59..be0e7a6ab3384b3ee995b39aecd14b650a21ed4c 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -203,11 +203,24 @@ public class PostPipeline extends GLBasePipeline {
                     + (result == null ? "null" : (result.getWidth() + "x" + result.getHeight())));
             return result;
         } finally {
+            // IRIS_26546_NIGHT_RGBA16F_POST_OWNERSHIP
+            // RunIrisNightRgb owns the carrier once entered. IrisNightRgbInput frees it immediately
+            // after successful upload; if setup fails earlier, this fallback is the sole release.
+            ByteBuffer unreleasedNightCarrier = irisNightRgba16f;
+            irisNightRgba16f = null;
+            if (unreleasedNightCarrier != null) {
+                try {
+                    Allocator.free(unreleasedNightCarrier);
+                    Log.critical("PostPipeline", "IRIS_26546_NIGHT_CPU_RGBA16F_FALLBACK_RELEASE bytes="
+                            + unreleasedNightCarrier.capacity());
+                } catch (Throwable releaseFailure) {
+                    Log.e("PostPipeline", "IRIS_26546_NIGHT_CPU_RGBA16F_FALLBACK_RELEASE_FAILED", releaseFailure);
+                }
+            }
             // IRIS_26539_NIGHT_POST_OWNER_CLOSE_BEFORE_JIN
             // The returned Bitmap owns a copied ARGB_8888 image. Night must destroy the EGL/Post
             // owner before ORT/Jin begins, even when a post node throws. Motion keeps its existing
             // caller-owned lifecycle unchanged.
-            irisNightRgba16f = null;
             Log.processState("PostPipeline", "NIGHT_POST_CLOSE_BEGIN");
             Log.critical("PostPipeline", "IRIS_26544_NIGHT_POST_CLOSE_BEGIN");
             try { close(); }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
index 33e7397717f5dbbc27a7a19e3aafe2ef5e75f03c..99f552f5ab0dc8c2d3edc669973932f339d457ee 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
@@ -23,6 +23,11 @@ public final class IrisMotionSettings {
      */
     public static final String KEY_LUMA_DENOISE = "pref_iris_luma_denoise_v2";
     public static final String KEY_CHROMA_DENOISE = "pref_iris_chroma_denoise";
+    /* IRIS_26546_VGN_CHROMA_CORRECTION_CONTROL
+     * 1.0 is exact 26545 current-MGC VGN chroma behavior. 0.0 preserves chroma through
+     * VGN chroma-changing stages while the complete VGN stage order still executes.
+     */
+    public static final String KEY_VGN_CHROMA_CORRECTION = "pref_iris_vgn_chroma_correction";
     public static final String KEY_EXPOSURE_EV = "pref_iris_exposure_ev";
     public static final String KEY_SHADOWS = "pref_iris_shadows";
     public static final String KEY_CONTRAST = "pref_iris_contrast";
@@ -50,6 +55,7 @@ public final class IrisMotionSettings {
         public final String profileDisplayName;
         public final float lumaDenoise;
         public final float chromaDenoise;
+        public final float vgnChromaCorrection;
         public final float exposureEv;
         public final float shadows;
         public final float contrast;
@@ -61,6 +67,7 @@ public final class IrisMotionSettings {
                  String profileDisplayName,
                  float lumaDenoise,
                  float chromaDenoise,
+                 float vgnChromaCorrection,
                  float exposureEv,
                  float shadows,
                  float contrast) {
@@ -71,6 +78,7 @@ public final class IrisMotionSettings {
             this.profileDisplayName = profileDisplayName == null ? "" : profileDisplayName;
             this.lumaDenoise = lumaDenoise;
             this.chromaDenoise = chromaDenoise;
+            this.vgnChromaCorrection = vgnChromaCorrection;
             this.exposureEv = exposureEv;
             this.shadows = shadows;
             this.contrast = contrast;
@@ -85,7 +93,7 @@ public final class IrisMotionSettings {
     public static Snapshot current() {
         SettingsManager sm = PhotonCamera.getSettingsManagerStatic();
         if (sm == null) {
-            return new Snapshot(Reconstruction.SPATIAL_RGB, true, false, "", "", 1.0f, 1.0f, 0.0f, 0.0f, 0.0f);
+            return new Snapshot(Reconstruction.SPATIAL_RGB, true, false, "", "", 1.0f, 1.0f, 1.0f, 0.0f, 0.0f, 0.0f);
         }
         Reconstruction reconstruction = Reconstruction.fromPersisted(
                 getString(sm, KEY_RECONSTRUCTION, Reconstruction.SPATIAL_RGB.persistedValue));
@@ -95,10 +103,11 @@ public final class IrisMotionSettings {
         String name = getString(sm, KEY_PROFILE_NAME, "");
         float luma = snap01(getFloat(sm, KEY_LUMA_DENOISE, 0.0f), 0.0f, 2.0f);
         float chroma = snap01(getFloat(sm, KEY_CHROMA_DENOISE, 1.0f), 0.0f, 2.0f);
+        float vgnChromaCorrection = snap01(getFloat(sm, KEY_VGN_CHROMA_CORRECTION, 1.0f), 0.0f, 1.0f);
         float exposure = snap01(getFloat(sm, KEY_EXPOSURE_EV, 0.0f), -1.0f, 1.0f);
         float shadows = snap01(getFloat(sm, KEY_SHADOWS, 0.0f), -1.0f, 1.0f);
         float contrast = snap01(getFloat(sm, KEY_CONTRAST, 0.0f), -1.0f, 1.0f);
-        return new Snapshot(reconstruction, nr, custom, id, name, luma, chroma, exposure, shadows, contrast);
+        return new Snapshot(reconstruction, nr, custom, id, name, luma, chroma, vgnChromaCorrection, exposure, shadows, contrast);
     }
 
     /* IRIS_26540_NIGHT_EXACT_CAMERA2_NOISE_SNAPSHOT
@@ -108,7 +117,7 @@ public final class IrisMotionSettings {
     public static Snapshot exactCamera2NightSnapshot() {
         Snapshot source = current();
         return new Snapshot(Reconstruction.SPATIAL_RGB, source.noiseReductionEnabled, false, "", "Camera2 exact",
-                0.0f, source.chromaDenoise,
+                0.0f, source.chromaDenoise, 1.0f,
                 source.exposureEv, source.shadows, source.contrast);
     }
 
@@ -120,7 +129,7 @@ public final class IrisMotionSettings {
 
     public static boolean isQuantizedSliderKey(String key) {
         return KEY_LUMA_DENOISE.equals(key) || KEY_CHROMA_DENOISE.equals(key) ||
-                KEY_EXPOSURE_EV.equals(key) || KEY_SHADOWS.equals(key) ||
+                KEY_VGN_CHROMA_CORRECTION.equals(key) || KEY_EXPOSURE_EV.equals(key) || KEY_SHADOWS.equals(key) ||
                 KEY_CONTRAST.equals(key);
     }
 
@@ -128,10 +137,12 @@ public final class IrisMotionSettings {
         if (!isQuantizedSliderKey(key)) return;
         SettingsManager sm = PhotonCamera.getSettingsManagerStatic();
         if (sm == null) return;
-        float min = (KEY_LUMA_DENOISE.equals(key) || KEY_CHROMA_DENOISE.equals(key)) ? 0.0f : -1.0f;
-        float max = (KEY_LUMA_DENOISE.equals(key) || KEY_CHROMA_DENOISE.equals(key)) ? 2.0f : 1.0f;
+        boolean denoiseSlider = KEY_LUMA_DENOISE.equals(key) || KEY_CHROMA_DENOISE.equals(key);
+        boolean vgnSlider = KEY_VGN_CHROMA_CORRECTION.equals(key);
+        float min = (denoiseSlider || vgnSlider) ? 0.0f : -1.0f;
+        float max = denoiseSlider ? 2.0f : 1.0f;
         float def = KEY_LUMA_DENOISE.equals(key) ? 0.0f :
-                (KEY_CHROMA_DENOISE.equals(key) ? 1.0f : 0.0f);
+                ((KEY_CHROMA_DENOISE.equals(key) || vgnSlider) ? 1.0f : 0.0f);
         float snapped = snap01(getFloat(sm, key, def), min, max);
         String normalized = String.format(Locale.ROOT, "%.1f", snapped);
         String current = getString(sm, key, String.format(Locale.ROOT, "%.1f", def));
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
index 8b3a64a8f051dc6de2ea5a7b7c9e861056d2fad5..3870500b66478fe1d6b3760f10ce6486ea0e613d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisNightProcessor.java
@@ -209,16 +209,17 @@ public final class IrisNightProcessor {
             Log.critical(TAG, "IRIS_26544_NIGHT_POST_RGB_BEGIN dngStageComplete=true");
             final Point crop;
             Bitmap img;
-            try {
-                img = pipeline.RunIrisNightRgb(rgb, p);
-                crop = pipeline.cropSize == null ? new Point(p.rawSize) : new Point(pipeline.cropSize);
-                Log.processState(TAG, "NIGHT_POST_RGB_COMPLETE");
-                Log.critical(TAG, "IRIS_26544_NIGHT_POST_RGB_COMPLETE bitmap="
-                        + (img == null ? "null" : (img.getWidth() + "x" + img.getHeight())));
-            } finally {
-                try { Allocator.free(rgb); } catch (Throwable ignored) {}
-                Log.critical(TAG, "IRIS_26544_NIGHT_RGBA16F_RELEASED bytes=" + expectedRgbBytes);
-            }
+            // IRIS_26546_NIGHT_POST_CARRIER_TRANSFER: PostPipeline now owns rgb before entry.
+            // It frees after GPU upload or in its early-failure fallback. Caller clears its pointer
+            // first so a native/Java setup failure cannot double-free the direct buffer.
+            ByteBuffer postOwnedRgb = rgb;
+            rgb = null;
+            img = pipeline.RunIrisNightRgb(postOwnedRgb, p);
+            crop = pipeline.cropSize == null ? new Point(p.rawSize) : new Point(pipeline.cropSize);
+            Log.processState(TAG, "NIGHT_POST_RGB_COMPLETE");
+            Log.critical(TAG, "IRIS_26546_NIGHT_POST_RGB_COMPLETE bitmap="
+                    + (img == null ? "null" : (img.getWidth() + "x" + img.getHeight()))
+                    + " cpuCarrierOwnership=postPipeline");
             Log.i(TAG, "IRIS_26540_NIGHT_RGBA16F_RELEASED_BEFORE_JIN bytes=" + expectedRgbBytes);
             if (android.os.Build.VERSION.SDK_INT >= 34 && img != null
                     && !img.isRecycled() && img.hasGainmap()) {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index 7dede246b2d18976cb723fbac68d987fa7ef42bb..55e5547dfbda9d02f08bc4713e3456eec10c6abd 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -128,6 +128,13 @@ object PhotonMotionMgc1271Bridge {
             }
             val sabreSelected = !parameters.irisNightActive &&
                 irisSettings.reconstruction == IrisMotionSettings.Reconstruction.SABRE
+            // IRIS_26546_NIGHT_VGN_POLICY_ISOLATION: Motion owns the diagnostic slider;
+            // Night remains fixed to exact 26545 current-MGC VGN behavior.
+            val vgnChromaCorrectionStrength = if (parameters.irisNightActive) {
+                1.0f
+            } else {
+                irisSettings.vgnChromaCorrection.coerceIn(0.0f, 1.0f)
+            }
             if (!parameters.irisNightActive) {
                 parameters.motionV2ReconstructionOwner = if (sabreSelected) {
                     Parameters.MOTION_V2_RECONSTRUCTION_SABRE
@@ -146,6 +153,7 @@ object PhotonMotionMgc1271Bridge {
             PLog.i(TAG, "IRIS_26545_MOTION_RECONSTRUCTION_SELECTED " +
                 "method=${if (sabreSelected) "SABRE" else "SPATIAL_RGB"} " +
                 "settings=${irisSettings.reconstruction.persistedValue} " +
+                "vgnChromaCorrection=$vgnChromaCorrectionStrength " +
                 "night=${parameters.irisNightActive} dngRequested=$produceNormalStackedDng")
 
             val noiseSelection: RawNoiseProfileSelection
@@ -340,6 +348,7 @@ object PhotonMotionMgc1271Bridge {
                 exportNormalStackedDng = produceNormalStackedDng,
                 superResTempDir = PhotonCamera.getApplicationContextStatic().cacheDir,
                 superResWriteLinearRaw = produceNormalStackedDng && !sabreSelected,
+                vgnChromaCorrectionStrength = vgnChromaCorrectionStrength,
             )
             val iris26535FusionStartNs = System.nanoTime()
             val stacked = fusion.processFrames(frames)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
index 13af9bd92b84722e3b3c21cb22c03ecde868f5db..0c9659e68aaf3eb2d0d1484bcedc6501c4a3af61 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
@@ -86,6 +86,7 @@ public class PreferenceKeys {
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_luma_denoise", "1.0");
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_luma_denoise_v2", "0.0");
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_chroma_denoise", "1.0");
+        settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_vgn_chroma_correction", "1.0");
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_exposure_ev", "0.0");
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_shadows", "0.0");
         settingsManager.setInitial(SCOPE_GLOBAL, "pref_iris_contrast", "0.0");
@@ -164,6 +165,7 @@ public class PreferenceKeys {
         map.putIfAbsent("pref_iris_luma_denoise", "1.0");
         map.putIfAbsent("pref_iris_luma_denoise_v2", "0.0");
         map.putIfAbsent("pref_iris_chroma_denoise", "1.0");
+        map.putIfAbsent("pref_iris_vgn_chroma_correction", "1.0");
         map.putIfAbsent("pref_iris_exposure_ev", "0.0");
         map.putIfAbsent("pref_iris_shadows", "0.0");
         map.putIfAbsent("pref_iris_contrast", "0.0");
diff --git a/app/src/main/res/values/default_prefs.xml b/app/src/main/res/values/default_prefs.xml
index 9adf1ba5ddc45a0deea62c5825be52eab14970ee..c5206d1778309586ef4c1f435ed8c76592afa6fd 100644
--- a/app/src/main/res/values/default_prefs.xml
+++ b/app/src/main/res/values/default_prefs.xml
@@ -15,6 +15,7 @@
     <bool name="pref_hdrx_nr_default">true</bool>
     <string name="pref_iris_luma_denoise_default" translatable="false">0.0</string>
     <string name="pref_iris_chroma_denoise_default" translatable="false">1.0</string>
+    <string name="pref_iris_vgn_chroma_correction_default" translatable="false">1.0</string>
     <string name="pref_iris_exposure_default" translatable="false">0.0</string>
     <string name="pref_iris_shadows_default" translatable="false">0.0</string>
     <string name="pref_iris_contrast_default" translatable="false">0.0</string>
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index dc7efa7bcbd457be1de0116ceeaf5da5d77ebe57..361a2b35f2f590ad1c38e75950db9c7901592306 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -47,6 +47,8 @@
     <string name="iris_luma_denoise_summary">Residual luminance denoise after reconstruction and before tone mapping. 0 = off, 1 = calibrated, 2 = strongest.</string>
     <string name="iris_chroma_denoise">Chroma Denoise</string>
     <string name="iris_chroma_denoise_summary">Residual color denoise after reconstruction and before tone mapping. 0 = off, 1 = calibrated, 2 = strongest.</string>
+    <string name="iris_vgn_chroma_correction">VGN Chroma Correction Strength</string>
+    <string name="iris_vgn_chroma_correction_summary">Motion reconstruction VGN chroma correction. 1.0 = exact 26545 behavior; lower values preserve progressively more reconstructed chroma without disabling VGN.</string>
     <string name="iris_exposure">Exposure (EV)</string>
     <string name="iris_shadows">Shadows</string>
     <string name="iris_contrast">Contrast</string>
diff --git a/app/src/main/res/xml/preferences.xml b/app/src/main/res/xml/preferences.xml
index 4bd0dc2b10b1df5cc51c047ef11f4d213b1a5d8a..0b41f78210117db6eda2bbfb62838f3fe2308a64 100644
--- a/app/src/main/res/xml/preferences.xml
+++ b/app/src/main/res/xml/preferences.xml
@@ -41,6 +41,7 @@
         <Preference ns0:key="pref_iris_import_noise_model" ns0:layout="@layout/preference_with_margin" ns0:title="@string/iris_import_noise_model" ns0:summary="@string/iris_import_noise_model_summary" ns0:icon="@drawable/ic_save" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_luma_denoise_v2" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_luma_denoise" ns0:summary="@string/iris_luma_denoise_summary" ns0:defaultValue="@string/pref_iris_luma_denoise_default" ns1:maxValue="2.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_grain_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_chroma_denoise" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_chroma_denoise" ns0:summary="@string/iris_chroma_denoise_summary" ns0:defaultValue="@string/pref_iris_chroma_denoise_default" ns1:maxValue="2.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_gradient_black_24dp" />
+        <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_vgn_chroma_correction" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_vgn_chroma_correction" ns0:summary="@string/iris_vgn_chroma_correction_summary" ns0:defaultValue="@string/pref_iris_vgn_chroma_correction_default" ns1:maxValue="1.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_gradient_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_exposure_ev" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_exposure" ns0:summary="" ns0:defaultValue="@string/pref_iris_exposure_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_saturation" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_shadows" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_shadows" ns0:summary="" ns0:defaultValue="@string/pref_iris_shadows_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_compressor" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_contrast" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_contrast" ns0:summary="" ns0:defaultValue="@string/pref_iris_contrast_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_saturation" />
diff --git a/app/version.properties b/app/version.properties
index 869a14afb7252bc60d40d536b0eab035208d37aa..8b81ebf694a0d07598fd4c6f39a45523be1eecf8 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726545
-VERSION_BUILD=26545
+VERSION_NAME=0.9726546
+VERSION_BUILD=26546
