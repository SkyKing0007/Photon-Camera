26599 V1 permanent regression contract

1. Successful 26598 V1.1 CaptureController, exact physical slider budget, pre/post-shutter NORMAL ownership, SHORT/LONG tickets, one-deep deferred shutter, DNG ownership, and Super Res detail evidence remain unchanged unless explicitly superseded in a later audited build.
2. Exact literal NORMAL sensor-code clipping continues to admit SHORT only after same-phase clipped-phase explanation and phase-scoped SHORT headroom.
3. Non-literal SHORT handoff requires physically measurable SHORT, coherent Sabre flow, exposure-normalized same-CFA SHORT>NORMAL effective loss, phase-scoped SHORT headroom, and propagated boundary radiometric trust. Brightness alone is never sufficient.
4. When exposure-normalized SHORT and NORMAL agree within the loss threshold, the effective-loss path must produce zero handoff.
5. SHORT replacement remains one whole-RGB scalar mask. No per-channel replacement, hue repair, or chroma-specific highlight owner is allowed.
6. The 26599 gate telemetry is read-only. It may never feed mask, flow, RGB, frame selection, DNG, or SR detail evidence.
7. Motion automatic presentation normalization is exactly the successful-26598 scalar through linear guide 0.18. Above 0.18, only additional positive gain may roll off, with a continuous strictly monotonic map. Gain <=1 remains exact successful-26598 scalar behavior.
8. Presentation normalization derives one scalar from max(luma, RGB peak) and scales whole RGB. Neutral RGB remains neutral and channel ratios remain invariant, preventing a new pink/green/cyan tone mechanism.
9. Night retains successful-26598 scalar presentation behavior. Manual Iris exposure/shadows/contrast remain downstream of automatic normalization.
10. 1x and true-2x Super Res use the same 0.18 anchor/knee and the same mathematical presentation map. SR changes detail/resolution, not global brightness/color/highlight rolloff. SHORT remains excluded from SR detail evidence.
11. Successful 26598 sceneWhite final Motion publication, adaptive color owner, HAL AE sensor authority, and DNG behavior remain unchanged.
12. app/build/**, app/.cxx/** and repository-only scaffolding absent compiled-candidate authority never count as runtime changes; unexpected app/src/** changes fail.
