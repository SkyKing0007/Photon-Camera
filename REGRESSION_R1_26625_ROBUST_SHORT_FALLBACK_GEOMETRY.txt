26625 R1 permanent regression — robust radiometrically validated SHORT fallback geometry

Exact successful-26624 device failure:
- Direct-sun shot had a physically valid ~-2.5 EV SHORT frame and retained HDR master headroom.
- Production SHORT local-affine residuals exploded: p50 ~11.85 RAW px, p95 ~65.88, p99 ~95.31.
- boundarySeedCells=0, componentTrustedCells=0, targetAccumulatorEligibleCells=0 and broadSupportedCells=0/1287.
- The successful 26624 component recovery therefore correctly failed closed rather than risk misregistered/colored SHORT data.

Required 26625 invariant:
1. Never loosen the successful 26611 local boundary residual envelope (0.35..0.95 RAW px). Production flow remains primary.
2. Fallback geometry may be estimated only from production-flow cells already <=0.95 RAW-pixel residual; <=0.50 cells seed deterministic RANSAC sampling.
3. The robust affine requires at least 12 core/candidate cells, >=30% consensus inliers, >=30% spatial coverage in both axes, and model p90 <=0.75 RAW px. No random/non-deterministic sampling.
4. Fallback geometry is proposal-only. It can never create component trust without the existing same-CFA 5x5 measurable-boundary radiometry (quad/phase evidence and 0.03..0.08 radiometric confidence).
5. Once that independent boundary proof passes, the exact validated geometry must reach actual SHORT weight/coverage/common Sabre merge sampling; it must not remain telemetry-only.
6. Successful 26624 literal+effective component membership, bottleneck propagation, CFA-safe neighboring-flow barrier, SHORT headroom, ordinary physical weight cap and final common-RBF 3x3 source-clipping veto remain unchanged.
7. Component propagation grants permission only. No spatial fill, smoothing, inpainting, semantic sky/sun/cloud detection, private SHORT RGB path or late RGB blend. High-frequency cloud edges, branches, grass, pine needles and ground texture remain actual aligned SHORT samples.
8. True2x NORMAL evidence and NORMAL-only DNG continue using production flow; fallback is SHORT-only.
9. Permanent proof-report regression: the human-readable runtime-authority line must print exact successful 26624 authority d0d48ab26006a868baa8525a0b81aad65939fac3 / run 34525392337 / artifact 10171452870, never the stale 26622 identifiers that survived in the 26624 report string.

Device proof after Actions:
- IRIS_26625_SHORT_FALLBACK_AFFINE must report valid/model quality.
- IRIS_26607_SHORT_SEMANTIC_FUNNEL must report fallbackTrustedCells and actual target eligibility.
- The direct-sun/cloud scene should gain boundary seeds/component support only where independent boundary radiometry succeeds; misaligned or unsupported regions must remain rejected.
