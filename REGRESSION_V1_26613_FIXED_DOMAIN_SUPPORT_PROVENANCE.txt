26613 V1 PERMANENT REGRESSION CONTRACT — FIXED-DOMAIN HDR / SABRE RGB SUPPORT PROVENANCE

DEVICE FAILURE CLASS BEING CORRECTED
1. 26612 global p99/p995/p998 three-knot presentation could create steep artificial transitions when upper source quantiles collapsed and globally altered unrelated >0.40 structure, seen as a hard ceiling boundary and grey/gamma-muted trees.
2. Bright-condition magenta/cyan/purple edge artifacts could survive 26611 because the final decisive post-VGN false-color decision still inferred CFA/support validity from reconstructed RGB appearance rather than consuming the actual common-Sabre per-channel accumulated support.
3. Chandelier/cloud/window highlight output must retain ordered radiance without an SDR plateau or an independent UHDR percentile re-interpretation.

PERMANENT CONTRACT
A. Successful 26612 exact compiled candidate is base authority; 1708 files required.
B. Runtime delta equals exact 10-file allowlist. app/build/** and app/.cxx/** never count as runtime source. Unexpected app/src/** fails.
C. Successful 26611 GlesMgcRawSabreShaders.kt must remain byte-identical. No new SHORT geometry/eligibility/photometric bypass/private compositor/late rescue is permitted.
D. No active publication owner may contain the 26612 p99/p995/p998 moving-knot authority or equivalent scene-quantile knot placement.
E. Motion fixed-domain SDR function is exact identity for every finite sourceFinal <=0.80 and continuous/monotonic with first derivative 1 at the 0.80 knee.
F. Clean extended-HDR master is direct UHDR HDR target. No second percentile boost can alter HDR ordering.
G. 1x SDR, adaptive predictor, 1x UHDR, true2x CPU and true2x GPU use matching fixed-domain semantics. Super Res cannot choose a separate rendition.
H. Exact common-Sabre accumulated R/G/B weights may be read by post-VGN cleanup only. They cannot modify temporal weights, alignment, radiance accumulation, Resolve or VGN.
I. Balanced R/G/B support yields zero physical unsupported-channel evidence.
J. A weak channel yields physical evidence only when the proposed same-surface correction increases that weak channel toward the supported neutral consensus. A correction that would decrease the weak channel yields zero physical evidence.
K. Existing real-color/topology/micro-object protection remains a multiplicative fail-closed veto. Genuine supported colored structures may not be neutralized solely because they are bright or saturated.
L. Ambiguous cases remain under the inherited conservative maximum-move cap; only decisive neutral support evidence can authorize full correction.
M. DNG/native/vendor/protected files remain invariant outside exact allowlist.
N. Every modified runtime-expanded shader passes complete reserved-identifier/static scan and pinned real glslang 16.5.0 in Actions.
O. Modified Kotlin/Java pass real project compilers; modified C++ passes both NDK ABIs; full :app:assembleDebug and exactly one APK required.
P. Deterministic full-index forward/rollback patches identical at core.abbrev 7/12/40 and exact apply/rollback required.
Q. Device visual acceptance is separate from compiler/build proof. Do not claim edge/chandelier/cloud visual success until device comparison.
