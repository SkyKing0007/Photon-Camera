diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 5c14bd972a27fa42dcf8a05336dff9c1e7b4cd89..fe418f847b0168d3fd0734a26b83bc66b19a07b6 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -6,7 +6,6 @@ uniform float sceneWhite;
 uniform float outputExposureScale;
 uniform float irisOutputZoom;
 uniform int iris26592MotionHdrHandoff;
-uniform float iris26602MotionMasterToneStart;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
 
@@ -95,13 +94,7 @@ vec3 iris26524BilinearInput(vec2 sourcePixel) {
 }
 
 float mapHeadroomLuminance(float y) {
-    /* IRIS_26602_UHDR_MASTER_SDR_PARITY
-     * Motion's UHDR target is the extended-linear pre-tone rendition at the same 0.80
-     * presentation scale. Preserve that master rendition exactly through guide=1.0, so
-     * SDR and UHDR have identical blacks/body/midtones/color/contrast. Only luminance that
-     * actually needs HDR headroom enters the shoulder. Night retains its exact 0.50 start.
-     */
-    float start=iris26592MotionHdrHandoff!=0 ? iris26602MotionMasterToneStart : 0.50;
+    const float start=0.50;
     if(y<=start) return y;
 
     float whitePoint=max(sceneWhite,start+0.05);
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index bbde744627a553c84ec5aa02bec85fb82bedc77a..a6e753a803ce2e848d52ce6f5404b69d062c5b34 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -593,14 +593,11 @@ inline bool samplePreRenderCached(const ProfileRegion&s,const Params&p,float x,f
 
 inline Vec3 renderHeadroom(Vec3 rgb,const Params&p){
     rgb=clampNonnegative(rgb);float y=std::max(luma(rgb),0.f),pk=peak(rgb),guide=std::max(y,pk);
-    /* IRIS_26602_UHDR_MASTER_SDR_PARITY_TRUE2X_CPU: Motion preserves the exact UHDR master
-     * rendition through guide=1.0; Night retains the successful 0.50 shoulder start. */
-    const float start=p.motionHdrHandoff?1.0f:0.50f;
-    if(guide>1.0e-7f&&guide>start){
-        float white=std::max(p.sceneWhite,start+0.05f);float u=std::max((guide-start)/std::max(white-start,1.0e-6f),0.f);float shaped;
+    if(guide>1.0e-7f&&guide>0.50f){
+        float white=std::max(p.sceneWhite,0.55f);float u=std::max((guide-0.50f)/std::max(white-0.50f,1.0e-6f),0.f);float shaped;
         if(p.motionHdrHandoff){/* IRIS_26597_BODY_ANCHORED_UNIVERSAL_HIGHLIGHT_RESERVE */const float a=std::tanh(1.2020679f);const float k=a/std::max(1.f-a,1.0e-6f);shaped=u<=1.f?a*std::max(u,0.f):1.f-(1.f-a)/(1.f+k*(u-1.f));}
         else {float x=clampf(u,0.f,1.f);shaped=std::log(1.f+6.f*x)/std::log(7.f);}
-        float mapped=start+(1.25f-start)*shaped;rgb=mul(rgb,mapped/guide);
+        float mapped=0.50f+(1.25f-0.50f)*shaped;rgb=mul(rgb,mapped/guide);
     }
     rgb=mul(rgb,0.80f);float pk2=peak(rgb);if(pk2>1.f)rgb=mul(rgb,1.f/std::max(pk2,1.0e-6f));return clampNonnegative(rgb);
 }
@@ -876,15 +873,13 @@ vec2 irisOutputToSource(ivec2 op){
 vec3 irisHeadroom(vec3 rgb){
     rgb=irisClampNonnegative(rgb);
     float y=max(irisLuma(rgb),0.0),pk=irisPeak(rgb),guide=max(y,pk);
-    /* IRIS_26602_UHDR_MASTER_SDR_PARITY_TRUE2X_GPU */
-    float start=uMotionHdrHandoff!=0?1.0:0.50;
-    if(guide>1.0e-7&&guide>start){
-        float white=max(uSceneWhite,start+0.05);
-        float u=max((guide-start)/max(white-start,1.0e-6),0.0);
+    if(guide>1.0e-7&&guide>0.50){
+        float white=max(uSceneWhite,0.55);
+        float u=max((guide-0.50)/max(white-0.50,1.0e-6),0.0);
         float shaped;
         if(uMotionHdrHandoff!=0){/* IRIS_26597_BODY_ANCHORED_UNIVERSAL_HIGHLIGHT_RESERVE */float a=tanh(1.2020679);float k=a/max(1.0-a,1.0e-6);shaped=u<=1.0?a*max(u,0.0):1.0-(1.0-a)/(1.0+k*(u-1.0));}
         else {float x=clamp(u,0.0,1.0);shaped=log(1.0+6.0*x)/log(7.0);}
-        float mapped=start+(1.25-start)*shaped;
+        float mapped=0.50+(1.25-0.50)*shaped;
         rgb*=mapped/guide;
     }
     rgb*=0.80;
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 80596f807f276a5bbc53bc52447233ae426ee133..e3b72fd8bf809c912a9c6bc8a7409405908294c2 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -1176,143 +1176,6 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 
-    /* IRIS_26602_SHORT_FULL_NORMAL_PROTECTION
-     * SHORT must not merely arrive before Resolve/VGN; it must inherit the same Sabre
-     * photometric/unblocker/dilated-frame-weight protection that NORMAL uses. The reference guide
-     * cannot remain measurable inside a clipped core, so the measured NORMAL-equivalent protection
-     * is seeded only on a radiometrically valid 26600 boundary and then propagated through that
-     * already-proven motion-consensus geometry. A separate NORMAL-only temporal coverage map keeps
-     * LONG support from validating SHORT.
-     */
-    val shortProtectionSeed26602 = """
-        #version 300 es
-        precision highp float;
-        uniform sampler2D uBoundaryGeometry;
-        uniform sampler2D uFrameWeight;
-        uniform sampler2D uNormalCoverage;
-        uniform ivec2 uGridSize;
-        layout(location = 0) out float oProtection;
-        void main() {
-            ivec2 p = ivec2(gl_FragCoord.xy);
-            if (any(greaterThanEqual(p, uGridSize))) { oProtection = 0.0; return; }
-            vec2 uv = (vec2(p) + vec2(0.5)) / vec2(uGridSize);
-            vec4 g = texelFetch(uBoundaryGeometry, p, 0);
-            float measured = texture(uFrameWeight, uv).r;
-            float normalSupport = texture(uNormalCoverage, uv).r;
-            bool accepted = g.z >= 0.5 && g.w >= 0.5 &&
-                measured > 0.08 && normalSupport > (0.5 / 255.0);
-            oProtection = accepted ? measured : 0.0;
-        }
-    """.trimIndent()
-
-    /* Protection never invents trust. It can only carry an already-measured NORMAL-equivalent
-     * Sabre weight through the exact 26600 coherent-flow/boundary-connected region. Confidence is
-     * non-increasing: propagation uses the weakest compatible supporting neighbour.
-     */
-    val shortProtectionPropagate26602 = """
-        #version 300 es
-        precision highp float;
-        uniform sampler2D uCurrent;
-        uniform sampler2D uBoundaryGeometry;
-        uniform ivec2 uGridSize;
-        uniform ivec2 uRawSize;
-        uniform float uConsensusPixels;
-        layout(location = 0) out float oProtection;
-        ivec2 clampGrid(ivec2 p) { return clamp(p, ivec2(0), uGridSize - ivec2(1)); }
-        vec4 geom(ivec2 p) { return texelFetch(uBoundaryGeometry, clampGrid(p), 0); }
-        float protect(ivec2 p) { return texelFetch(uCurrent, clampGrid(p), 0).r; }
-        vec2 rawFlow(vec4 g) { return g.xy * vec2(uRawSize); }
-        void main() {
-            ivec2 p = ivec2(gl_FragCoord.xy);
-            if (any(greaterThanEqual(p, uGridSize))) { oProtection = 0.0; return; }
-            float existing = protect(p);
-            if (existing > 0.0) { oProtection = existing; return; }
-            vec4 center = geom(p);
-            if (center.z < 0.5 || center.w < 0.5) { oProtection = 0.0; return; }
-            vec2 centerFlow = rawFlow(center);
-            int support = 0;
-            float weakest = 1.0;
-            for (int y = -1; y <= 1; ++y) {
-                for (int x = -1; x <= 1; ++x) {
-                    if (x == 0 && y == 0) continue;
-                    ivec2 q = clampGrid(p + ivec2(x, y));
-                    float w = protect(q);
-                    vec4 g = geom(q);
-                    if (w <= 0.0 || g.z < 0.5 || g.w < 0.5) continue;
-                    if (length(rawFlow(g) - centerFlow) > uConsensusPixels) continue;
-                    weakest = min(weakest, w);
-                    support += 1;
-                }
-            }
-            oProtection = support >= 1 ? weakest : 0.0;
-        }
-    """.trimIndent()
-
-    /* IRIS_26602_BAYER_QUAD_COHERENT_SHORT_VALIDITY
-     * All four positions of one physical Bayer quad receive one SHORT confidence. This prevents
-     * neighbouring CFA phases from independently choosing bracket ownership at a material edge.
-     * The candidate mask remains the exact 26600 sensor-loss/headroom authority; protection can
-     * only reduce it.
-     */
-    val shortProtectedMask26602 = """
-        #version 300 es
-        precision highp float;
-        uniform sampler2D uCandidateMask;
-        uniform sampler2D uProtection;
-        uniform ivec2 uOutputSize;
-        layout(location = 0) out float oMask;
-        ivec2 clampOutput(ivec2 p) { return clamp(p, ivec2(0), uOutputSize - ivec2(1)); }
-        float candidateAt(ivec2 p) { return texelFetch(uCandidateMask, clampOutput(p), 0).r; }
-        void main() {
-            ivec2 p = ivec2(gl_FragCoord.xy);
-            ivec2 q = (clampOutput(p) / 2) * 2;
-            float candidate = min(
-                min(candidateAt(q), candidateAt(q + ivec2(1, 0))),
-                min(candidateAt(q + ivec2(0, 1)), candidateAt(q + ivec2(1, 1)))
-            );
-            vec2 quadUv = (vec2(q) + vec2(1.0)) / vec2(uOutputSize);
-            float protection = texture(uProtection, quadUv).r;
-            float protectionGate = smoothstep(0.08, 0.16, protection);
-            oMask = clamp(candidate * protectionGate, 0.0, 1.0);
-        }
-    """.trimIndent()
-
-    /* IRIS_26602_PROTECTED_SHORT_COMMON_ACCUMULATOR
-     * Fuse dehomogenized NORMAL/LONG and protected SHORT evidence before the one Resolve/VGN pass.
-     * The same scalar confidence applies to R/G/B; NORMAL/LONG accumulated weights remain the
-     * temporal/noise/support authority. This removes 26601's binary accumulator replacement while
-     * retaining valid SHORT radiometry where NORMAL is censored.
-     */
-    val shortProtectedAccumulatorFuse26602 = """
-        #version 300 es
-        precision highp float;
-        uniform sampler2D uNormalColorAndRWeight;
-        uniform sampler2D uNormalWeightsGb;
-        uniform sampler2D uShortColorAndRWeight;
-        uniform sampler2D uShortWeightsGb;
-        uniform sampler2D uProtectedMask;
-        layout(location = 0) out vec4 oColorAndRWeight;
-        layout(location = 1) out vec2 oWeightsGb;
-        void main() {
-            ivec2 p = ivec2(gl_FragCoord.xy);
-            vec4 normalColor = texelFetch(uNormalColorAndRWeight, p, 0);
-            vec2 normalGb = texelFetch(uNormalWeightsGb, p, 0).rg;
-            vec4 shortColor = texelFetch(uShortColorAndRWeight, p, 0);
-            vec2 shortGb = texelFetch(uShortWeightsGb, p, 0).rg;
-            vec3 normalWeight = vec3(normalColor.a, normalGb);
-            vec3 shortWeight = vec3(shortColor.a, shortGb);
-            float minimumNormalWeight = min(normalWeight.r, min(normalWeight.g, normalWeight.b));
-            float minimumShortWeight = min(shortWeight.r, min(shortWeight.g, shortWeight.b));
-            float confidence = texelFetch(uProtectedMask, p, 0).r;
-            if (minimumNormalWeight <= 1.0e-7 || minimumShortWeight <= 1.0e-7) confidence = 0.0;
-            vec3 normalMean = normalColor.rgb / max(normalWeight, vec3(1.0e-7));
-            vec3 shortMean = shortColor.rgb / max(shortWeight, vec3(1.0e-7));
-            vec3 fusedMean = mix(normalMean, shortMean, clamp(confidence, 0.0, 1.0));
-            oColorAndRWeight = vec4(fusedMean * normalWeight, normalColor.a);
-            oWeightsGb = normalGb;
-        }
-    """.trimIndent()
-
     val shortRestoreRgba16f26587 = """
         #version 300 es
         precision highp float;
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index e96c9558cf38b17774002676ad01f17184276556..cda77f96b4262eb81c573b2a167733d451f6f935 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -559,10 +559,6 @@ internal class GlesMgcRawSpatialStacker(
     private var sabreShortRestoreMaskProbeProgram26590 = 0
     private var sabreShortMaskCountProgram26595 = 0
     private var sabreShortAccumulatorOwnershipProgram26601 = 0
-    private var sabreShortProtectionSeedProgram26602 = 0
-    private var sabreShortProtectionPropagateProgram26602 = 0
-    private var sabreShortProtectedMaskProgram26602 = 0
-    private var sabreShortProtectedAccumulatorFuseProgram26602 = 0
     /* 26587 late post-VGN RGB restore program intentionally retired from the active 26601 path. */
     private var sabreShortRestoreRgba16fProgram26587 = 0
     private var sabreRgbChromaPostprocessor: GlesIris26529SpatialRgbChromaPostprocessor? = null
@@ -2072,8 +2068,8 @@ internal class GlesMgcRawSpatialStacker(
             SABRE_TAG,
             "IRIS_26587_SABRE_ROLE_CONTRACT normal=$normalFrameCount shadowLong=$shadowLongFrameCount " +
                 "highlightShort=$highlightShortFrameCount scheduled=${frames.size} merged=$mergedFrameCount " +
-                "referenceRole=NORMAL shortCommonSabrePreResolve=true shortTemporalAccumulation=false " +
-                "shortProtection=NORMAL_SABRE_BOUNDARY_PROPAGATED shortDng=false shortSrDetail=false",
+                "referenceRole=NORMAL shortCommonSabrePreResolve=true shortTemporalSupport=false " +
+                "shortDng=false shortSrDetail=false",
         )
         if (shadowLongFrameCount > 0) {
             PLog.i(
@@ -2083,13 +2079,6 @@ internal class GlesMgcRawSpatialStacker(
                     "wholeLongObservationReject=true coverageSupportGuard=true shortHighlightAuthority=true " +
                     "sourceClippingPoint=${sabreShadowLongSourceClippingPoint()}",
             )
-            PLog.i(
-                SABRE_TAG,
-                "IRIS_26602_LONG_FULL_NORMAL_PROTECTION photometric=true unblocker=true " +
-                    "dilationFrameWeight=true sameSabreFlow=true sameCovariance=true " +
-                    "sourceClipGuard=true durationRobustness=true commonResolve=true commonVgn=true " +
-                    "dng=false srDetailEvidence=false",
-            )
         }
         baseFrameCamera2Model = frames.firstOrNull()
             ?.channelNoiseProfile
@@ -2132,9 +2121,6 @@ internal class GlesMgcRawSpatialStacker(
         var highlightShortMaskGenerated26587 = false
         var highlightShortAppliedToTrue2x26587 = false
         var highlightShortFullActivePixels26595: Int? = null
-        var highlightShortCandidatePixels26602: Int? = null
-        var highlightShortProtectionSeedCells26602 = 0
-        var highlightShortProtectionFinalCells26602 = 0
         var returned = false
         val originalThreadPriority = GlesGpuScheduler.lowerCurrentThreadPriority(SABRE_TAG)
         val processStartNs = System.nanoTime()
@@ -2227,16 +2213,6 @@ internal class GlesMgcRawSpatialStacker(
                 GLES30.GL_R8,
                 GLES30.GL_LINEAR,
             )
-            /* IRIS_26602_SHORT_NORMAL_ONLY_PROTECTION_COVERAGE
-             * SHORT must be validated by NORMAL temporal support only. LONG is protected HDR
-             * evidence but cannot bootstrap the SHORT highlight owner. This map uses the exact
-             * NORMAL Sabre dilated frame weights and never feeds DNG/SR accounting.
-             */
-            val shortNormalProtectionCoverage26602 = if (highlightShortFrameCount > 0) {
-                createTexture(coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR)
-            } else {
-                0
-            }
             /* IRIS_26545_SABRE_NORMALIZED16_DNG
              * Keep only signal+weight for the RAW sidecar. It uses the exact Sabre flow,
              * covariance and temporal rejection but never ResolveSabre, WB/color transform,
@@ -2264,14 +2240,6 @@ internal class GlesMgcRawSpatialStacker(
                 coverageWidth,
                 coverageHeight,
             )
-            if (shortNormalProtectionCoverage26602 != 0) {
-                clearSabreCoverage(
-                    shortNormalProtectionCoverage26602,
-                    coverageWidth,
-                    coverageHeight,
-                    "26602 SHORT NORMAL-only protection coverage clear",
-                )
-            }
             if (normalDngAccumulator != 0) {
                 clearSabreNormalDngAccumulator(normalDngAccumulator)
                 if (normalDngCoverage != 0) {
@@ -2466,35 +2434,53 @@ internal class GlesMgcRawSpatialStacker(
                     GLES30.GL_LINEAR,
                 )
                 renderDilation(reverseWeight, frameWeight)
-                if (
-                    shortNormalProtectionCoverage26602 != 0 &&
-                    frame.role == RawBurstFrameRole.NORMAL
-                ) {
-                    renderSabreCoverage(
-                        frameWeight,
-                        shortNormalProtectionCoverage26602,
-                        coverageWidth,
-                        coverageHeight,
-                        maxSabreAccumulatedWeight(normalFrameCount),
-                    )
-                }
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
-                    /* IRIS_26602_SHORT_FULL_NORMAL_SABRE_PROTECTION
-                     * 26601 proved that arriving before Resolve/VGN was insufficient because SHORT
-                     * discarded the already-computed NORMAL-equivalent Sabre rejection/unblocker/
-                     * dilation weight. Keep the exact 26600 boundary geometry and highlight-loss
-                     * candidate, but seed trust only where the exact SHORT frameWeight and NORMAL-
-                     * only temporal coverage both agree. Carry that measured trust into a clipped
-                     * core only through the already-proven coherent-flow boundary geometry.
+                    /* IRIS_26601_SHORT_COMMON_SABRE_PRE_RESOLVE
+                     * Alignment/rejection remains the exact exposure-normalized 26600 geometry.
+                     * Build one private SHORT homogeneous Sabre observation in NORMAL reference
+                     * exposure using the same flow/covariance/RBF estimator. It does not alter
+                     * NORMAL/LONG temporal support, noise, DNG, or SR-detail evidence. The exact
+                     * 26600 mask below is the only spatial ownership authority; accepted SHORT
+                     * evidence is switched into the common accumulator only after those accounting
+                     * owners are finalized and before the single Resolve/VGN reconstruction.
                      */
                     require(index == frames.lastIndex) {
-                        "26602 HIGHLIGHT_SHORT must be scheduled after all NORMAL/LONG evidence"
-                    }
-                    require(shortNormalProtectionCoverage26602 != 0) {
-                        "26602 HIGHLIGHT_SHORT requires NORMAL-only temporal protection coverage"
+                        "26601 HIGHLIGHT_SHORT must be scheduled after all NORMAL/LONG evidence"
                     }
                     val nativeShortCalibration = calibrationForFrame(frame, 1f, kernelTuning)
                     highlightShortExposureRatio26587 = exposureScale
+                    highlightShortAccumulatedColor26601 = createTexture(
+                        width, height, GLES30.GL_RGBA16F, GLES30.GL_NEAREST,
+                    )
+                    highlightShortAccumulatedWeightsGb26601 = createTexture(
+                        width, height, GLES30.GL_RG16F, GLES30.GL_NEAREST,
+                    )
+                    clearRgbAccumulators(
+                        highlightShortAccumulatedColor26601,
+                        highlightShortAccumulatedWeightsGb26601,
+                        width,
+                        height,
+                    )
+                    renderSabreMerge(
+                        extracted = currentExtracted,
+                        flow = flow,
+                        covariance = currentCovariance,
+                        weight = identityWeight,
+                        calibration = calibration,
+                        accumulatedColor = highlightShortAccumulatedColor26601,
+                        accumulatedWeightsGb = highlightShortAccumulatedWeightsGb26601,
+                        extractedWidth = extractedWidth,
+                        extractedHeight = extractedHeight,
+                        useFrameWeight = false,
+                        shadowLongSourceClipGuard = false,
+                    )
+                    /* IRIS_26600_BOUNDARY_CONSENSUS_SHORT_HANDOFF
+                     * 26599 device telemetry proved the old max-range flow gate rejected every
+                     * highlight candidate before SHORT evidence was sampled. Build one sparse
+                     * SHORT-only geometry field from robust Sabre-flow consensus. Literal sensor
+                     * loss needs geometry trust; non-literal loss additionally needs a connected
+                     * radiometrically-proven boundary carried through the same coherent flow.
+                     */
                     val shortBoundaryGeometry26600 = createSabreShortBoundaryGeometry26600(
                         referenceRaw = referenceRaw,
                         shortRaw = currentRaw,
@@ -2516,8 +2502,7 @@ internal class GlesMgcRawSpatialStacker(
                             "boundaryRadiometry=subpixelSameCfaPhase meanErrorMax=0.05 " +
                             "propagation=highlightConnectedMotionConsensus",
                     )
-
-                    val highlightShortCandidateMask26602 = createTexture(
+                    highlightShortMask26587 = createTexture(
                         width, height, GLES30.GL_R8, GLES30.GL_NEAREST,
                     )
                     renderSabreShortRestoreMask26595(
@@ -2528,7 +2513,7 @@ internal class GlesMgcRawSpatialStacker(
                         referenceCalibration = referenceCalibration,
                         shortCalibration = nativeShortCalibration,
                         exposureRatio = exposureScale,
-                        output = highlightShortCandidateMask26602,
+                        output = highlightShortMask26587,
                         outputGateStage26599 = 0,
                         outputWidth26599 = width,
                         outputHeight26599 = height,
@@ -2542,47 +2527,15 @@ internal class GlesMgcRawSpatialStacker(
                         shortCalibration = nativeShortCalibration,
                         exposureRatio = exposureScale,
                     )
-                    highlightShortCandidatePixels26602 =
-                        countSabreShortRestoreMaskFull26595(highlightShortCandidateMask26602)
-
-                    val shortProtection26602 = createSabreShortProtection26602(
-                        boundaryGeometry = shortBoundaryGeometry26600,
-                        frameWeight = frameWeight,
-                        normalCoverage = shortNormalProtectionCoverage26602,
-                    )
-                    highlightShortProtectionSeedCells26602 = shortProtection26602.seedCells
-                    highlightShortProtectionFinalCells26602 = shortProtection26602.finalCells
-                    highlightShortMask26587 = createTexture(
-                        width, height, GLES30.GL_R8, GLES30.GL_NEAREST,
-                    )
-                    renderSabreShortProtectedMask26602(
-                        candidateMask = highlightShortCandidateMask26602,
-                        protection = shortProtection26602.texture,
-                        output = highlightShortMask26587,
-                    )
-                    releaseOwnedTexture(
-                        highlightShortCandidateMask26602,
-                        "26602 SHORT unprotected candidate mask",
-                    )
-                    releaseOwnedTexture(
-                        shortProtection26602.texture,
-                        "26602 SHORT propagated Sabre protection",
-                    )
-
                     val shortMaskProbe26590 = probeSabreShortRestoreMask26590(highlightShortMask26587)
                     highlightShortFullActivePixels26595 =
                         countSabreShortRestoreMaskFull26595(highlightShortMask26587)
-                    val candidateText26602 = highlightShortCandidatePixels26602?.toString() ?: "UNAVAILABLE"
                     val fullActiveText26595 = highlightShortFullActivePixels26595?.toString() ?: "UNAVAILABLE"
                     val shortMaskEffectDetails26591 =
                         "probe=${shortMaskProbe26590.width}x${shortMaskProbe26590.height} " +
                             "active=${shortMaskProbe26590.activePixels}/${shortMaskProbe26590.totalPixels} " +
                             "strong=${shortMaskProbe26590.strongPixels}/${shortMaskProbe26590.totalPixels} " +
-                            "candidate=$candidateText26602/${Math.multiplyExact(width, height)} " +
-                            "protectedFullActive=$fullActiveText26595/${Math.multiplyExact(width, height)} " +
-                            "protectionSeedCells=${shortProtection26602.seedCells} " +
-                            "protectionFinalCells=${shortProtection26602.finalCells} " +
-                            "protectionPasses=${shortProtection26602.passes} " +
+                            "fullActive=$fullActiveText26595/${Math.multiplyExact(width, height)} " +
                             "gateProbe=$shortGateProbe26599 " +
                             "maxHandoffWeight=${shortMaskProbe26590.maxConfidence} " +
                             "meanHandoffWeight=${shortMaskProbe26590.meanConfidence} " +
@@ -2595,57 +2548,28 @@ internal class GlesMgcRawSpatialStacker(
                             "boundarySeed=${shortBoundaryGeometry26600.seedBoundaryCells} geometryFinal=${shortBoundaryGeometry26600.finalGeometryCells} " +
                             "boundaryFinal=${shortBoundaryGeometry26600.finalBoundaryCells} " +
                             "actualSensorClipBypass=true clipPhasesMin=1 clippedPhaseShortProof=true robustFlowGeometry=true " +
-                            "flowConsensusRawPx=2.0 sabrePhotometricGate=true unblockerGate=true " +
-                            "normalCoverageGate=true bayerQuadCoherent=true sameSabreFlow=true wholeRgb=true maskFilter=NEAREST"
-                    PLog.i(SABRE_TAG, "IRIS_26602_SHORT_PROTECTED_MASK $shortMaskEffectDetails26591")
+                            "flowConsensusRawPx=2.0 sabrePhotometricGate=false unblockerGate=false " +
+                            "normalCoverageGate=false sameSabreFlow=true wholeRgb=true maskFilter=NEAREST"
+                    PLog.i(SABRE_TAG, "IRIS_26590_SHORT_MASK_EFFECT $shortMaskEffectDetails26591")
+                    /* IRIS_26591_SHORT_MASK_EFFECT_MOTIONTRACE
+                     * Telemetry only: expose the already-computed 26590 read-only mask probe in the
+                     * capture's durable MotionTrace. No mask, flow, weight, or RGB math changes.
+                     */
                     MotionTrace.processingState(
-                        "IRIS_26602_SHORT_PROTECTED_HANDOFF",
+                        "IRIS_26592_SHORT_HANDOFF_EFFECT",
                         shortMaskEffectDetails26591,
                     )
-
-                    /* Build the private SHORT observation only after the protected final mask exists.
-                     * uUseFrameWeight=true is now mandatory. The mask is a propagated version of the
-                     * exact NORMAL Sabre frameWeight, intersected with 26600 radiometric validity and
-                     * made one scalar per Bayer quad. Identity-weight SHORT is permanently retired.
-                     */
-                    highlightShortAccumulatedColor26601 = createTexture(
-                        width, height, GLES30.GL_RGBA16F, GLES30.GL_NEAREST,
-                    )
-                    highlightShortAccumulatedWeightsGb26601 = createTexture(
-                        width, height, GLES30.GL_RG16F, GLES30.GL_NEAREST,
-                    )
-                    clearRgbAccumulators(
-                        highlightShortAccumulatedColor26601,
-                        highlightShortAccumulatedWeightsGb26601,
-                        width,
-                        height,
-                    )
-                    renderSabreMerge(
-                        extracted = currentExtracted,
-                        flow = flow,
-                        covariance = currentCovariance,
-                        weight = highlightShortMask26587,
-                        calibration = calibration,
-                        accumulatedColor = highlightShortAccumulatedColor26601,
-                        accumulatedWeightsGb = highlightShortAccumulatedWeightsGb26601,
-                        extractedWidth = extractedWidth,
-                        extractedHeight = extractedHeight,
-                        useFrameWeight = true,
-                        shadowLongSourceClipGuard = false,
-                    )
                     highlightShortAdmitted26587 = true
                     highlightShortMaskGenerated26587 = true
                     PLog.i(
                         SABRE_TAG,
-                        "IRIS_26602_SHORT_FULL_NORMAL_PROTECTION_READY exposureRatio=$exposureScale " +
-                            "normalFrames=$normalFrameCount candidate=${highlightShortCandidatePixels26602 ?: -1} " +
-                            "protectedFullActive=${highlightShortFullActivePixels26595 ?: -1} " +
-                            "protectionSeedCells=$highlightShortProtectionSeedCells26602 " +
-                            "protectionFinalCells=$highlightShortProtectionFinalCells26602 " +
-                            "sameSabreFlow=true sameSabreCovariance=true sabrePhotometricGate=true " +
-                            "unblockerGate=true dilationFrameWeight=true normalCoverageGate=true " +
-                            "bayerQuadCoherent=true boundaryGeometry26600=true hardEvidenceSwitch=false " +
-                            "dng=false srDetailEvidence=false referenceExposureReconstruction=true wholeRgb=true",
+                        "IRIS_26596_SHORT_REGION_AUXILIARY_READY exposureRatio=$exposureScale " +
+                            "normalFrames=$normalFrameCount boundaryRadiometry=subpixelSameCfaPhase " +
+                            "boundaryGeometryPasses=${shortBoundaryGeometry26600.passes} " +
+                            "actualSensorClipBypass=true fullActive=${highlightShortFullActivePixels26595 ?: -1} " +
+                            "photometricWeightAuthority=false unblockerAuthority=false robustBoundaryGeometry=true " +
+                            "normalCoverageAuthority=false commonSabrePreResolve=true temporalSupport=false " +
+                            "dng=false srDetailEvidence=false referenceExposureReconstruction=true wholeRgbMask=true",
                     )
                 } else {
                     if (frame.role == RawBurstFrameRole.SHADOW_LONG) {
@@ -2685,8 +2609,8 @@ internal class GlesMgcRawSpatialStacker(
                 }
                 /* IRIS_26564_NIGHT_LONG_EXCLUDED_FROM_TRUE_2X
                  * Native Sabre still admits validated SHADOW_LONG evidence for Night SNR. True 2x
-                 * spatial reconstruction accepts NORMAL frames only, preserving the proven rule
-                 * that neither bracket exposure can become high-frequency SR evidence.
+                 * spatial reconstruction accepts NORMAL/Short frames only, preserving the proven
+                 * rule that long-exposure blur cannot become high-frequency SR evidence.
                  */
                 if (enableSabreSuperRes && frame.role == RawBurstFrameRole.NORMAL) {
                     val candidateEvidence = persistTrue2xEvidence(
@@ -2819,65 +2743,59 @@ internal class GlesMgcRawSpatialStacker(
                 )
             }
 
-            /* IRIS_26602_SHORT_PROTECTED_SINGLE_RECONSTRUCTION_AUTHORITY
-             * NORMAL/LONG support/noise/DNG accounting above is frozen. SHORT has already consumed
-             * the exact NORMAL Sabre rejection/unblocker/dilation protection at its measurable
-             * boundary and may carry that trust inward only through 26600 coherent geometry. Fuse
-             * dehomogenized means with one Bayer-coherent RGB scalar while retaining NORMAL/LONG
-             * accumulated weights. There is no hard accumulator switch and no post-VGN compositor.
+            /* IRIS_26601_SHORT_SINGLE_RECONSTRUCTION_AUTHORITY
+             * NORMAL/LONG support/noise/DNG accounting above is now frozen. If SHORT has proven
+             * evidence, switch its exposure-normalized homogeneous Sabre observation into a new
+             * resolve accumulator at those exact 26600 mask pixels. This is the last evidence
+             * boundary: there is no post-VGN NORMAL/SHORT RGB compositing in 26601.
              */
-            var resolveAccumulatedColor26602 = accumulatedColor
-            var resolveAccumulatedWeightsGb26602 = accumulatedWeightsGb
+            var resolveAccumulatedColor26601 = accumulatedColor
+            var resolveAccumulatedWeightsGb26601 = accumulatedWeightsGb
             if (highlightShortAdmitted26587) {
                 require(
                     highlightShortAccumulatedColor26601 != 0 &&
                         highlightShortAccumulatedWeightsGb26601 != 0 &&
                         highlightShortMask26587 != 0
-                ) { "26602 protected SHORT common Sabre evidence is incomplete" }
-                resolveAccumulatedColor26602 = createTexture(
+                ) { "26601 SHORT common Sabre evidence is incomplete" }
+                resolveAccumulatedColor26601 = createTexture(
                     width, height, GLES30.GL_RGBA16F, GLES30.GL_NEAREST,
                 )
-                resolveAccumulatedWeightsGb26602 = createTexture(
+                resolveAccumulatedWeightsGb26601 = createTexture(
                     width, height, GLES30.GL_RG16F, GLES30.GL_NEAREST,
                 )
-                renderSabreShortProtectedAccumulatorFuse26602(
+                renderSabreShortAccumulatorOwnership26601(
                     normalColorAndRWeight = accumulatedColor,
                     normalWeightsGb = accumulatedWeightsGb,
                     shortColorAndRWeight = highlightShortAccumulatedColor26601,
                     shortWeightsGb = highlightShortAccumulatedWeightsGb26601,
-                    protectedMask = highlightShortMask26587,
-                    outputColorAndRWeight = resolveAccumulatedColor26602,
-                    outputWeightsGb = resolveAccumulatedWeightsGb26602,
+                    shortMask = highlightShortMask26587,
+                    outputColorAndRWeight = resolveAccumulatedColor26601,
+                    outputWeightsGb = resolveAccumulatedWeightsGb26601,
                 )
                 releaseOwnedTexture(
                     highlightShortAccumulatedColor26601,
-                    "26602 SHORT protected homogeneous color/weight evidence",
+                    "26601 SHORT homogeneous color/weight evidence",
                 )
                 releaseOwnedTexture(
                     highlightShortAccumulatedWeightsGb26601,
-                    "26602 SHORT protected homogeneous Gb weight evidence",
+                    "26601 SHORT homogeneous Gb weight evidence",
                 )
-                releaseOwnedTexture(highlightShortMask26587, "26602 SHORT protected evidence mask")
+                releaseOwnedTexture(highlightShortMask26587, "26601 SHORT evidence mask")
                 highlightShortAccumulatedColor26601 = 0
                 highlightShortAccumulatedWeightsGb26601 = 0
                 highlightShortMask26587 = 0
                 PLog.i(
                     SABRE_TAG,
-                    "IRIS_26602_SHORT_FULL_PROTECTED_COMMON_SABRE_OWNERSHIP " +
-                        "candidate=${highlightShortCandidatePixels26602 ?: -1} " +
-                        "protectedFullActive=${highlightShortFullActivePixels26595 ?: -1} " +
-                        "protectionSeedCells=$highlightShortProtectionSeedCells26602 " +
-                        "protectionFinalCells=$highlightShortProtectionFinalCells26602 " +
-                        "domain=homogeneousReferenceExposure hardEvidenceSwitch=false " +
-                        "sabrePhotometricGate=true unblockerGate=true dilationFrameWeight=true " +
-                        "normalCoverageGate=true bayerQuadCoherent=true " +
+                    "IRIS_26601_SHORT_COMMON_SABRE_OWNERSHIP " +
+                        "fullActive=${highlightShortFullActivePixels26595 ?: -1} " +
+                        "domain=homogeneousReferenceExposure hardEvidenceSwitch=true " +
                         "sharedResolve=true sharedVgn=true lateRgbBlend=false " +
                         "temporalSupportOwner=NORMAL_LONG dngOwner=NORMAL srDetailOwner=NORMAL",
                 )
             }
             renderSabreDehomogenize(
-                resolveAccumulatedColor26602,
-                resolveAccumulatedWeightsGb26602,
+                resolveAccumulatedColor26601,
+                resolveAccumulatedWeightsGb26601,
                 accumulatedCoverage,
                 alphaScale = accumulatedWeightScale / mergedFrameCount.toFloat(),
                 alphaBias = 1f / mergedFrameCount.toFloat(),
@@ -2885,7 +2803,7 @@ internal class GlesMgcRawSpatialStacker(
             // The original merge shaders write normalized, white-balanced half floats. The
             // Resolve AOT accepts those RGBA16F bit patterns directly; rawScale applies only to
             // the black/white scalar parameters below.
-            sabreAccumulatedReadback = readSabreAccumulatedRgba16f(resolveAccumulatedColor26602)
+            sabreAccumulatedReadback = readSabreAccumulatedRgba16f(resolveAccumulatedColor26601)
             sabreResolvedRgb = checkNotNull(
                 LargeDirectBuffer.allocate(
                     width.toLong() * height * 3L * Short.SIZE_BYTES,
@@ -4504,121 +4422,6 @@ internal class GlesMgcRawSpatialStacker(
         }
     }
 
-    private data class ShortProtection26602(
-        val texture: Int,
-        val seedCells: Int,
-        val finalCells: Int,
-        val passes: Int,
-    )
-
-    private fun countSabreShortProtectionCells26602(
-        texture: Int,
-        gridWidth: Int,
-        gridHeight: Int,
-        label: String,
-    ): Int = readR8Mask(
-        texture,
-        label,
-        maskWidth = gridWidth,
-        maskHeight = gridHeight,
-    ).count { (it.toInt() and 0xff) != 0 }
-
-    private fun createSabreShortProtection26602(
-        boundaryGeometry: ShortBoundaryGeometry26600,
-        frameWeight: Int,
-        normalCoverage: Int,
-    ): ShortProtection26602 {
-        require(frameWeight != 0 && normalCoverage != 0)
-        val gridWidth = boundaryGeometry.gridWidth
-        val gridHeight = boundaryGeometry.gridHeight
-        val protectionA = createTexture(gridWidth, gridHeight, GLES30.GL_R8, GLES30.GL_LINEAR)
-        val protectionB = createTexture(gridWidth, gridHeight, GLES30.GL_R8, GLES30.GL_LINEAR)
-        val seedProgram = sabreShortProtectionSeedProgram26602
-        check(seedProgram != 0) { "26602 SHORT Sabre protection seed program is unavailable" }
-        GLES30.glUseProgram(seedProgram)
-        bindTexture(seedProgram, "uBoundaryGeometry", 0, boundaryGeometry.texture)
-        bindTexture(seedProgram, "uFrameWeight", 1, frameWeight)
-        bindTexture(seedProgram, "uNormalCoverage", 2, normalCoverage)
-        uniform2i(seedProgram, "uGridSize", gridWidth, gridHeight)
-        draw(seedProgram, gridWidth, gridHeight, intArrayOf(protectionA))
-        val seedCells = countSabreShortProtectionCells26602(
-            protectionA,
-            gridWidth,
-            gridHeight,
-            "26602 SHORT Sabre protection seed",
-        )
-
-        val propagationProgram = sabreShortProtectionPropagateProgram26602
-        check(propagationProgram != 0) { "26602 SHORT Sabre protection propagation program is unavailable" }
-        val passes = Math.addExact(gridWidth, gridHeight)
-        var current = protectionA
-        repeat(passes) { pass ->
-            val output = if ((pass and 1) == 0) protectionB else protectionA
-            GLES30.glUseProgram(propagationProgram)
-            bindTexture(propagationProgram, "uCurrent", 0, current)
-            bindTexture(propagationProgram, "uBoundaryGeometry", 1, boundaryGeometry.texture)
-            uniform2i(propagationProgram, "uGridSize", gridWidth, gridHeight)
-            uniform2i(propagationProgram, "uRawSize", width, height)
-            uniform1f(propagationProgram, "uConsensusPixels", 2.0f)
-            draw(propagationProgram, gridWidth, gridHeight, intArrayOf(output))
-            current = output
-        }
-        val finalCells = countSabreShortProtectionCells26602(
-            current,
-            gridWidth,
-            gridHeight,
-            "26602 SHORT Sabre protection final",
-        )
-        val unused = if (current == protectionA) protectionB else protectionA
-        releaseOwnedTexture(unused, "26602 SHORT Sabre protection ping-pong")
-        return ShortProtection26602(
-            texture = current,
-            seedCells = seedCells,
-            finalCells = finalCells,
-            passes = passes,
-        )
-    }
-
-    private fun renderSabreShortProtectedMask26602(
-        candidateMask: Int,
-        protection: Int,
-        output: Int,
-    ) {
-        val program = sabreShortProtectedMaskProgram26602
-        check(program != 0) { "26602 SHORT protected-mask program is unavailable" }
-        GLES30.glUseProgram(program)
-        bindTexture(program, "uCandidateMask", 0, candidateMask)
-        bindTexture(program, "uProtection", 1, protection)
-        uniform2i(program, "uOutputSize", width, height)
-        draw(program, width, height, intArrayOf(output))
-    }
-
-    private fun renderSabreShortProtectedAccumulatorFuse26602(
-        normalColorAndRWeight: Int,
-        normalWeightsGb: Int,
-        shortColorAndRWeight: Int,
-        shortWeightsGb: Int,
-        protectedMask: Int,
-        outputColorAndRWeight: Int,
-        outputWeightsGb: Int,
-    ) {
-        val program = sabreShortProtectedAccumulatorFuseProgram26602
-        check(program != 0) { "26602 SHORT protected common-accumulator fuse is unavailable" }
-        GLES30.glUseProgram(program)
-        bindTexture(program, "uNormalColorAndRWeight", 0, normalColorAndRWeight)
-        bindTexture(program, "uNormalWeightsGb", 1, normalWeightsGb)
-        bindTexture(program, "uShortColorAndRWeight", 2, shortColorAndRWeight)
-        bindTexture(program, "uShortWeightsGb", 3, shortWeightsGb)
-        bindTexture(program, "uProtectedMask", 4, protectedMask)
-        draw(
-            program,
-            width,
-            height,
-            intArrayOf(outputColorAndRWeight, outputWeightsGb),
-        )
-    }
-
-    /* 26601 hard accumulator ownership is retained as dormant rollback provenance only. */
     private fun renderSabreShortAccumulatorOwnership26601(
         normalColorAndRWeight: Int,
         normalWeightsGb: Int,
@@ -4958,26 +4761,9 @@ internal class GlesMgcRawSpatialStacker(
                 PLog.w(SABRE_TAG, "26595 SHORT full-mask GPU count unavailable", error)
             }
         }
-        /* IRIS_26602_SHORT_HARD_OWNER_RETIRED
-         * 26601's binary accumulator switch is intentionally unlinked. SHORT now consumes the
-         * NORMAL-equivalent Sabre protection and fuses one protected RGB mean before Resolve/VGN.
-         */
-        sabreShortAccumulatorOwnershipProgram26601 = 0
-        sabreShortProtectionSeedProgram26602 = linkProgram(
-            GlesMgcRawSabreShaders.shortProtectionSeed26602,
-            "iris_26602_sabre_short_protection_seed",
-        )
-        sabreShortProtectionPropagateProgram26602 = linkProgram(
-            GlesMgcRawSabreShaders.shortProtectionPropagate26602,
-            "iris_26602_sabre_short_protection_propagate",
-        )
-        sabreShortProtectedMaskProgram26602 = linkProgram(
-            GlesMgcRawSabreShaders.shortProtectedMask26602,
-            "iris_26602_sabre_short_protected_mask",
-        )
-        sabreShortProtectedAccumulatorFuseProgram26602 = linkProgram(
-            GlesMgcRawSabreShaders.shortProtectedAccumulatorFuse26602,
-            "iris_26602_sabre_short_protected_accumulator_fuse",
+        sabreShortAccumulatorOwnershipProgram26601 = linkProgram(
+            GlesMgcRawSabreShaders.shortAccumulatorOwnership26601,
+            "iris_26601_sabre_short_accumulator_ownership",
         )
         /* IRIS_26601_LATE_RGB_SHORT_OWNER_RETIRED
          * Keep the old helper source for rollback provenance, but do not link it. Any accidental
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 3d13ef7486d877026dda7795f296db3a8dd36d45..07b1e190f7e5f0be774372f41e85f832b10a505e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -25,11 +25,6 @@ import static android.opengl.GLES20.GL_LINEAR;
  */
 public final class MotionV2Render extends Node {
     static final float OUTPUT_EXPOSURE_SCALE = 0.80f;
-    /* IRIS_26602_UHDR_MASTER_SDR_PARITY
-     * UHDR's pre-tone extended-linear rendition is the Motion master. Keep that exact grade
-     * through guide=1.0 in SDR; only true HDR headroom above it enters the shoulder.
-     */
-    static final float IRIS_26602_MOTION_MASTER_SDR_TONE_START = 1.00f;
     static final float IRIS_26582_TONE_START = 0.50f;
     static final float IRIS_26582_HIGHLIGHT_TARGET = 0.97f;
     static final float IRIS_26582_CLIP_FRACTION_START = 0.002f;
@@ -277,7 +272,6 @@ public final class MotionV2Render extends Node {
         glProg.setTexture("InputBuffer", extendedLinearHdr);
         glProg.setVar("sceneWhite", sceneWhite);
         glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
-        glProg.setVar("iris26602MotionMasterToneStart", IRIS_26602_MOTION_MASTER_SDR_TONE_START);
                 glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
         glProg.setVar("irisOutputZoom", irisOutputZoom);
 
@@ -461,20 +455,6 @@ public final class MotionV2Render extends Node {
 
         glProg.closed = true;
 
-        if (basePipeline.mParameters.motionV2Active) {
-            Log.i(Name, "IRIS_26602_SDR_UHDR_MASTER_PARITY"
-                    + " master=extendedLinearPreTone"
-                    + " motionToneStart=" + IRIS_26602_MOTION_MASTER_SDR_TONE_START
-                    + " bodyGuideMax=1.0"
-                    + " bodyOutputMax=" + OUTPUT_EXPOSURE_SCALE
-                    + " bodyGainUnity=true"
-                    + " sameColor=true sameBlack=true sameMidtones=true sameContrast=true"
-                    + " differenceOnlyAboveBody=HDR_HEADROOM"
-                    + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
-                    + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
-                    + " oneMasterRendition=true");
-        }
-
         Log.d(Name, "IRIS_26436_V2_RENDER"
                 + " canonicalSignalAlreadyApplied=true"
                 + " postDisplaySensorWhite=" + postDisplaySensorWhite
@@ -513,7 +493,7 @@ public final class MotionV2Render extends Node {
                                 + " toneCurve26430ExactBase=true"
                 + " outputExposureScale=" + OUTPUT_EXPOSURE_SCALE
                 + " outputExposureEv=-0.321928"
-                + " hdrTargetUsesSameScale=true"
+                + " hdrTargetUsesSameScale=false"
                 + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
                 + " nominalHdrBodyRecoveryRatio="
                     + (HDR_EXPOSURE_SCALE / OUTPUT_EXPOSURE_SCALE)
diff --git a/app/version.properties b/app/version.properties
index 9194e4970cd9416c4fe4e2124a5c25f062ad0944..d73232e51bc570a0d0ae7ea4688f78e3d75d49b8 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726602
-VERSION_BUILD=26602
+VERSION_NAME=0.9726601
+VERSION_BUILD=26601
