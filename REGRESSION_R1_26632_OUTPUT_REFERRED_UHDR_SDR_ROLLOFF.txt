26632 permanent regressions

1. The 26630 fixed 1.25x body floor / nominal-white cliff may not return.
2. The 26631 source-guide 0.65 HDR-intent match anchor may not return.
3. Motion gain must be exact 1x at black and strictly continuous/monotonic for positive post-VGN luminance; no unity plateau or isolated gain island.
4. Motion gain remains scalar luminance only, full-resolution, with no RGB gain, blur, downsample, or spatial mask.
5. 1x GLSL and true-2x CPU/GPU Motion gain equations must remain semantically identical; Night/non-Motion quotient path remains unchanged.
6. Motion HDRCapacityMax/full-HDR display ratio equals the gain-map encoding maximum, not the current scene peak; Night keeps its frozen requested-capacity policy.
7. SDR upper-tone correction stays a pointwise/global C1 continuation starting at 0.65. Local-Laplacian spatial remap/downsample/accumulate/reconstruct bytes remain protected so tree/sky edge protections cannot be weakened.
8. New SDR upper transfer must be monotonic and no more compressive than 26631 above 0.65 while remaining byte/semantic-identical at and below 0.65.
9. app/build/** and app/.cxx/** never enter runtime authority; repository-only scaffolding absent from compiled-candidate authority remains excluded; unexpected app/src/** changes fail exact allowlist equality.
