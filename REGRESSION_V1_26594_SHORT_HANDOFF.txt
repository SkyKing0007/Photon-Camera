PHOTON 26594 V1 SHORT HANDOFF PERMANENT REGRESSION

REAL DEVICE FAILURE:
Successful 26593 V1.1 proved the requested SHORT was physically captured, generation-owned,
timestamp-matched and delivered to the Sabre auxiliary path, yet the sparse final handoff probe
reported active=0/3072, strong=0/3072 and maxHandoffWeight=0.0. The 26592/26593 handoff shader
required each clipped output pixel to find at least three completely unclipped NORMAL+SHORT quads
inside its local 3x3-quad neighborhood and independently required strong NORMAL-stack coverage.
That is circular in a clipped highlight core: NORMAL is required to prove information NORMAL lost.

PERMANENT FIX CONTRACT:
- SHORT capture/ownership mechanics from successful 26593 remain byte-identical.
- Radiometric trust is seeded only where NORMAL and SHORT are both measurable, bright and locally
  consistent after exposure normalization (>=3 valid quads, mean relative error < 0.05).
- A clipped core cannot self-authorize. No valid seed means exact NORMAL.
- Seed trust may propagate only through an 8-connected bright highlight region whose every cell
  independently passes the same SHORT Sabre weight, flow-variation, unblocker and sensor-headroom
  gates. Disconnected regions and geometry holes remain NORMAL.
- Final native-resolution application rechecks local SHORT geometry/headroom and requires region
  trust. NORMAL saturation remains the only blend-opacity owner: exact NORMAL below 0.90 second-
  highest CFA signal, monotonic 0.90..0.98 transition, full trusted SHORT at >=0.98.
- Replacement remains one scalar whole-RGB mask with NEAREST sampling in RGBA16F. No per-channel
  substitution, second correspondence search, PyramidAlignment, RCD, ADRC, single-frame fallback,
  local post-tone SHORT paste or unaligned alternate is permitted.
- SHORT remains excluded from NORMAL accumulation, DNG evidence and true-2x CFA/detail evidence.
- A generated mask texture is not equivalent to visible SHORT effect; effect telemetry remains a
  read-only sparse probe and must never be used to change runtime pixels.
