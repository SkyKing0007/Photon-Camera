diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index cccdc61bef59bb268b6fb1de21c779e4bad2f9ea..7ccdb63cd4b4b6a3d7363565cdc8ac6d47a1e4a6 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2546,55 +2546,14 @@ internal class GlesMgcRawSpatialStacker(
                     GLES30.GL_LINEAR,
                 )
                 renderDilation(reverseWeight, frameWeight)
-                /* IRIS_26658_LONG_FINAL_WEIGHT_PROOF
-                 * Diagnostic-only proof of post-source-clipping LONG eligibility. Reuse the exact
-                 * bracket coverage shader on an isolated R8 target; never feed this proof target
-                 * back into the production accumulator.
+                /* IRIS_26698_LONG_FINAL_WEIGHT_PROOF_DISABLED
+                 * 26658 proved this isolated target never feeds the production accumulator.
+                 * Retain only an explicit completion boundary at the former proof location.
                  */
                 if (frame.role == RawBurstFrameRole.SHADOW_LONG) {
-                    val longProof = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR,
-                    )
-                    clearSabreCoverage(
-                        longProof, coverageWidth, coverageHeight,
-                        "26658 LONG final-weight proof clear",
-                    )
-                    renderSabreUnifiedBracketCoverage26604(
-                        weight = frameWeight,
-                        extracted = currentExtracted,
-                        flow = flow,
-                        accumulatedCoverage = longProof,
-                        coverageWidth = coverageWidth,
-                        coverageHeight = coverageHeight,
-                        extractedWidth = extractedWidth,
-                        extractedHeight = extractedHeight,
-                        accumulatedWeightScale = accumulatedWeightScale,
-                    )
-                    val proofBytes = readR8Mask(
-                        longProof, "26658 LONG post-source-clip final weight",
-                        coverageWidth, coverageHeight,
-                    )
-                    var nonzero = 0
-                    var sum = 0L
-                    var peak = 0
-                    for (raw in proofBytes) {
-                        val value = raw.toInt() and 0xff
-                        if (value > 0) nonzero++
-                        sum += value.toLong()
-                        if (value > peak) peak = value
-                    }
-                    val cells = proofBytes.size.coerceAtLeast(1)
-                    val details26658 =
-                        "frame=$index cells=${proofBytes.size} finalWeightNonzero=$nonzero " +
-                            "nonzeroPct=${100.0 * nonzero.toDouble() / cells.toDouble()} " +
-                            "meanWeight=${sum.toDouble() / (255.0 * cells.toDouble())} " +
-                            "peakWeight=${peak / 255.0} postSourceClip=true " +
-                            "productionAccumulatorUnmodifiedByProof=true"
-                    PLog.i(SABRE_TAG, "IRIS_26658_LONG_FINAL_WEIGHT_PROOF $details26658")
-                    MotionTrace.processingState(
-                        "IRIS_26658_LONG_FINAL_WEIGHT_PROOF", details26658,
-                    )
-                    releaseOwnedTexture(longProof, "26658 LONG final-weight proof")
+                    GLES30.glFinish()
+                    checkGlError("26698 LONG proof-only work disabled completion")
+                    PLog.i(SABRE_TAG, "IRIS_26698_LONG_FINAL_WEIGHT_PROOF_DISABLED productionAccumulatorUnchanged=true")
                 }
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     check(shortPhysicalReverseWeight26610 != 0) {
@@ -2853,10 +2812,12 @@ internal class GlesMgcRawSpatialStacker(
                         "inferredLossAllowed=${referenceProtectionEv < 0.25f} " +
                         "protectedNormalRequiresMeasuredSourceLoss=${referenceProtectionEv >= 0.25f}",
                 )
-                logSabreUniversalFusionDecision26651(fusionDecision26651)
-                logSabreUniversalFusionRadiance26651(
-                    resolveAccumulatedColor26604, fused, fusionDecision26651,
-                )
+                GLES30.glFinish()
+                checkGlError("26698 fusion decision telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_FUSION_DECISION_READBACK_DISABLED productionFusionUnchanged=true")
+                GLES30.glFinish()
+                checkGlError("26698 fusion radiance telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_FUSION_RADIANCE_PROBE_DISABLED productionFusionUnchanged=true")
                 renderSabreShortColorValidityAugment26651(
                     shortColorValidity = shortColorValidity26651,
                     normalWeightR = sabreRSupport26613,
@@ -2959,7 +2920,9 @@ internal class GlesMgcRawSpatialStacker(
                     output = physicalHdr26605,
                     program = sabreOutputTransformFloatProgram,
                 )
-                logSabreExtendedHdrProbe26605("FLOAT_HDR_HANDOFF_PRE_VGN", physicalHdr26605)
+                GLES30.glFinish()
+                checkGlError("26698 FLOAT_HDR_HANDOFF_PRE_VGN telemetry disabled completion")
+                PLog.i(SABRE_TAG, "IRIS_26698_HDR_PROBE_DISABLED stage=FLOAT_HDR_HANDOFF_PRE_VGN productionHdrUnchanged=true")
             }
             renderSabreOutputTransform(
                 resolvedRgbPlanes = nativeResolvedPlanes,
@@ -3040,7 +3003,9 @@ internal class GlesMgcRawSpatialStacker(
                     }
                 }
                 if (preserveExtendedHdrThroughVgn) {
-                    logSabreExtendedHdrProbe26605("POST_VGN_HDR_MASTER", exportedTexture)
+                    GLES30.glFinish()
+                    checkGlError("26698 POST_VGN_HDR_MASTER telemetry disabled completion")
+                    PLog.i(SABRE_TAG, "IRIS_26698_HDR_PROBE_DISABLED stage=POST_VGN_HDR_MASTER productionHdrUnchanged=true")
                 }
             }
             /* IRIS_26601_SHARED_NATIVE_HDR_AUTHORITY
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
index b903b172096748dd4c924f4881b1c4b25b9b2ffc..78c1bfa9dd74a52add4f2befb9dae39190007dc7 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ImageSaver.java
@@ -141,50 +141,12 @@ public class ImageSaver {
         // IRIS_26642_ANDROID_ULTRAHDR_DECODE_PROOF
         // Diagnostic-only decode happens after a completed save on a daemon thread, so it cannot
         // change gain generation, publication bytes, capture routing, or save completion latency.
-        private static final ExecutorService IRIS26642_UHDR_PROOF_EXECUTOR =
-                Executors.newSingleThreadExecutor(r -> {
-                    Thread t = new Thread(r, "Iris26642UltraHdrDecodeProof");
-                    t.setDaemon(true);
-                    return t;
-                });
-
         private static void iris26642ScheduleUltraHdrDecodeProof(Path path, String format) {
             if (path == null || android.os.Build.VERSION.SDK_INT < 34) return;
-            final String absolute = path.toAbsolutePath().toString();
-            IRIS26642_UHDR_PROOF_EXECUTOR.execute(() -> {
-                Bitmap decoded = null;
-                try {
-                    decoded = BitmapFactory.decodeFile(absolute);
-                    final boolean hasGainmap = decoded != null && !decoded.isRecycled() && decoded.hasGainmap();
-                    final ColorSpace colorSpace = decoded == null ? null : decoded.getColorSpace();
-                    if (!hasGainmap) {
-                        Log.critical(TAG, "IRIS_26642_ANDROID_ULTRAHDR_DECODE_PROOF format=" + format
-                                + " decoded=" + (decoded != null) + " hasGainmap=false"
-                                + " colorSpace=" + (colorSpace == null ? "null" : colorSpace.getName())
-                                + " path=" + absolute);
-                        return;
-                    }
-                    final Gainmap gm = decoded.getGainmap();
-                    final Bitmap contents = gm == null ? null : gm.getGainmapContents();
-                    Log.critical(TAG, "IRIS_26642_ANDROID_ULTRAHDR_DECODE_PROOF format=" + format
-                            + " decoded=true hasGainmap=" + (gm != null)
-                            + " gainmap=" + (contents == null ? "null" : contents.getWidth() + "x" + contents.getHeight())
-                            + " ratioMin=" + java.util.Arrays.toString(gm.getRatioMin())
-                            + " ratioMax=" + java.util.Arrays.toString(gm.getRatioMax())
-                            + " gamma=" + java.util.Arrays.toString(gm.getGamma())
-                            + " epsilonSdr=" + java.util.Arrays.toString(gm.getEpsilonSdr())
-                            + " epsilonHdr=" + java.util.Arrays.toString(gm.getEpsilonHdr())
-                            + " minDisplayRatio=" + gm.getMinDisplayRatioForHdrTransition()
-                            + " fullDisplayRatio=" + gm.getDisplayRatioForFullHdr()
-                            + " colorSpace=" + (colorSpace == null ? "null" : colorSpace.getName())
-                            + " path=" + absolute);
-                } catch (Throwable t) {
-                    Log.e(TAG, "IRIS_26642_ANDROID_ULTRAHDR_DECODE_PROOF_FAILED format=" + format
-                            + " path=" + absolute, t);
-                } finally {
-                    if (decoded != null && !decoded.isRecycled()) decoded.recycle();
-                }
-            });
+            // IRIS_26698_POST_SAVE_DECODE_PROOF_DISABLED
+            // The file is already successfully encoded/published; do not decode it again for proof.
+            Log.i(TAG, "IRIS_26698_POST_SAVE_DECODE_PROOF_DISABLED format=" + format
+                    + " savedBytesUnchanged=true path=" + path.toAbsolutePath());
         }
         public static boolean saveBitmapAsJPG(Path fileToSave, Bitmap img, int jpgQuality, ParseExif.ExifData exifData) {
             exifData.COMPRESSION = String.valueOf(jpgQuality);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
index d539f279eabeabed1662da7c46ec1aff70c2c05f..d51cc93373b3d00e8d38ebff0b6555fe685ea096 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/StageTelemetry.java
@@ -141,50 +141,18 @@ public class StageTelemetry extends Node {
     public void Run() {
         GLTexture source = previousNode == null ? null : previousNode.WorkingTexture;
         WorkingTexture = source;
-        // IRIS_26540_TELEMETRY_MODE_FROM_IMMUTABLE_PARAMETERS
-        // Preserve historical telemetry as Motion-only without consulting live global mode.
+        // IRIS_26698_PRODUCTION_TELEMETRY_IMAGE_WORK_DISABLED
+        // Preserve the exact pass-through node and a conservative GPU completion boundary.
         if (!basePipeline.mParameters.motionV2Active || source == null) return;
-
-        GLHistogram histogram = null;
-        try {
-            histogram = new GLHistogram(basePipeline.glint.glProcessing, HIST_SIZE);
-            histogram.Rc = true; histogram.Gc = true; histogram.Bc = true; histogram.Ac = false;
-            histogram.resize = SAMPLE_RESIZE;
-            int[][] channels = histogram.Compute(source);
-            int[] combined = new int[HIST_SIZE];
-            long total = 0L; double weighted = 0.0; long floorCount = 0L, darkCount = 0L, nearClipCount = 0L;
-            for (int i = 0; i < HIST_SIZE; i++) {
-                int c = channels[0][i] + channels[1][i] + channels[2][i];
-                combined[i] = c; total += c; weighted += (double)c * i;
-                if (i <= 2) floorCount += c; if (i <= 10) darkCount += c; if (i >= 250) nearClipCount += c;
-            }
-            float mean = total > 0L ? (float)(weighted / (double)total / (HIST_SIZE - 1.0)) : Float.NaN;
-            String chroma = chromaOriginStats(source);
-            String message = "IRIS_26498_STAGE_TELEMETRY"
-                    + " stage=" + stage
-                    + " mean=" + mean
-                    + " p01=" + percentile(combined,total,0.01)
-                    + " p10=" + percentile(combined,total,0.10)
-                    + " p50=" + percentile(combined,total,0.50)
-                    + " p90=" + percentile(combined,total,0.90)
-                    + " p95=" + percentile(combined,total,0.95)
-                    + " p99=" + percentile(combined,total,0.99)
-                    + " floorLe1pct=" + (total>0L?floorCount/(float)total:Float.NaN)
-                    + " darkLe4pct=" + (total>0L?darkCount/(float)total:Float.NaN)
-                    + " nearClipGe98pct=" + (total>0L?nearClipCount/(float)total:Float.NaN)
-                    + chroma
-                    + " sameTexturePassThrough=true";
-            Log.i("IRIS26498", message);
-            try { com.particlesdevs.photoncamera.util.MotionTrace.processingState("STAGE_TELEMETRY_26498", message); }
-            catch (Throwable ignored) {}
-        } catch (Throwable throwable) {
-            Log.e("IRIS26498", "IRIS_26498_STAGE_TELEMETRY stage=" + stage
-                    + " error=" + throwable.getClass().getSimpleName()
-                    + " sameTexturePassThrough=true");
-        } finally {
-            if (histogram != null) histogram.close();
-            WorkingTexture = source;
-            glProg.closed = true;
-        }
+        android.opengl.GLES20.glFinish();
+        String message = "IRIS_26698_STAGE_TELEMETRY_IMAGE_WORK_DISABLED"
+                + " stage=" + stage
+                + " histogramReadback=false chromaProbeReadback=false"
+                + " explicitCompletionBoundary=true sameTexturePassThrough=true";
+        Log.i("IRIS26698", message);
+        try { com.particlesdevs.photoncamera.util.MotionTrace.processingState("STAGE_TELEMETRY_26698", message); }
+        catch (Throwable ignored) {}
+        WorkingTexture = source;
+        glProg.closed = true;
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
index 895c4e24739efde3060c2027f754a94946e01f89..aa29e9aaa32894a7bb555695b973f00497e0ecf0 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/HdrxProcessor.java
@@ -973,34 +973,23 @@ private void ApplyHdrX() {
         }
 
         if (cameraMode == CameraMode.MOTION) {
-            int iris26575JpegWidth = -1;
-            int iris26575JpegHeight = -1;
-            try {
-                android.graphics.BitmapFactory.Options iris26575Bounds =
-                        new android.graphics.BitmapFactory.Options();
-                iris26575Bounds.inJustDecodeBounds = true;
-                android.graphics.BitmapFactory.decodeFile(imageFile.toString(), iris26575Bounds);
-                iris26575JpegWidth = iris26575Bounds.outWidth;
-                iris26575JpegHeight = iris26575Bounds.outHeight;
-            } catch (Throwable ignored) {}
-            Log.i(TAG, "IRIS_26575_FINAL_JPEG_DIMENSION_PROOF imageSaved=" + imageSaved
+            /* IRIS_26698_FINAL_DIMENSION_DECODE_PROOF_DISABLED
+             * Publication already owns the completed file. Do not reopen it only to prove dimensions.
+             */
+            Log.i(TAG, "IRIS_26698_FINAL_DIMENSION_DECODE_PROOF_DISABLED imageSaved=" + imageSaved
                     + " frozenSuperRes=" + mMotion26575SuperResEnabled
                     + " processingSuperRes=" + processingParameters.motionV2SuperResOutputEnabled
                     + " true2xWidth=" + iris26564True2xWidth
                     + " true2xHeight=" + iris26564True2xHeight
-                    + " jpegWidth=" + iris26575JpegWidth
-                    + " jpegHeight=" + iris26575JpegHeight
-                    + " path=" + imageFile);
-            /* IRIS_26576_SR_FINAL_DIMENSION_PERSISTENT_PROOF */
+                    + " savedFileReopen=false path=" + imageFile);
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
-                    "IRIS_26576_SR_FINAL_DIMENSIONS",
+                    "IRIS_26698_SR_FINAL_DIMENSION_PROOF_DISABLED",
                     "imageSaved=" + imageSaved
                             + " frozenSuperRes=" + mMotion26575SuperResEnabled
                             + " processingSuperRes=" + processingParameters.motionV2SuperResOutputEnabled
                             + " true2xWidth=" + iris26564True2xWidth
                             + " true2xHeight=" + iris26564True2xHeight
-                            + " jpegWidth=" + iris26575JpegWidth
-                            + " jpegHeight=" + iris26575JpegHeight);
+                            + " savedFileReopen=false");
         }
         try {
             processingEventsListener.notifyImageSavedStatus(imageSaved, imageFile);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
index df6e0779cb9fc6ae2159a5d29f04f26bc9273037..7f65d3b7ae7c683c06c7e99b3d6d1fb055e5fc56 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
@@ -230,16 +230,12 @@ public final class IrisHeicUltraHdrEncoder {
                     exif == null ? null : exif.MODEL);
             ok &= Files.isRegularFile(output) && Files.size(output) > 0L;
             boolean recognized = false;
-            if (ok) {
-                Bitmap gainContents = gm.getGainmapContents();
-                recognized = iris26646VerifyPlatformReadback(output, gm,
-                        gainContents.getWidth(), gainContents.getHeight(), "MOTION_1X",
-                        p3, gainContents);
-            }
+            // IRIS_26698_HEIC_ANDROID_READBACK_PROOF_DISABLED
+            // Encoded HEIC bytes and native publication status are already authoritative here.
             Log.i(TAG, "IRIS_26646_HEIC_ULTRAHDR_RESULT saved=" + ok
                     + " androidRecognizedUltraHdr=" + recognized
-                    + " readbackFailureNonDestructive=true p3=true existingGainmap=true"
-                    + " recomputedGainmap=false path=" + output);
+                    + " androidReadbackProofSkipped=true readbackFailureNonDestructive=true"
+                    + " p3=true existingGainmap=true recomputedGainmap=false path=" + output);
             return ok;
         } catch (Throwable t) {
             Log.e(TAG, "IRIS_26636_HEIC_ULTRAHDR_FAILED", t);
@@ -288,28 +284,14 @@ public final class IrisHeicUltraHdrEncoder {
                     exif == null ? null : exif.MAKE,
                     exif == null ? null : exif.MODEL);
             saved &= Files.isRegularFile(output) && Files.size(output) > 0L;
-            Bitmap expectedBasePreview = null;
-            Bitmap expectedGainPreview = null;
-            boolean recognized;
-            try {
-                BitmapFactory.Options preview = new BitmapFactory.Options();
-                preview.inSampleSize = 16;
-                expectedBasePreview = BitmapFactory.decodeFile(displayP3BaseJpeg.toString(), preview);
-                BitmapFactory.Options gainPreview = new BitmapFactory.Options();
-                gainPreview.inSampleSize = 16;
-                expectedGainPreview = BitmapFactory.decodeFile(true2xGainJpeg.toString(), gainPreview);
-                recognized = saved && iris26646VerifyPlatformReadback(
-                        output, metadataOwner, width / 2, height / 2, "MOTION_TRUE2X_GRID",
-                        expectedBasePreview, expectedGainPreview);
-            } finally {
-                if (expectedBasePreview != null && !expectedBasePreview.isRecycled()) expectedBasePreview.recycle();
-                if (expectedGainPreview != null && !expectedGainPreview.isRecycled()) expectedGainPreview.recycle();
-            }
+            boolean recognized = false;
+            // IRIS_26698_TRUE2X_HEIC_ANDROID_READBACK_PROOF_DISABLED
+            // Do not decode base/gain previews or the completed HEIC after successful publication.
             Log.critical(TAG, "IRIS_26646_TRUE2X_HEIC_RESULT saved=" + saved
                     + " grid=2x2 base=" + width + "x" + height
                     + " gain=" + (width / 2) + "x" + (height / 2)
                     + " androidRecognizedUltraHdr=" + recognized
-                    + " readbackFailureNonDestructive=true hardwareHevc=true");
+                    + " androidReadbackProofSkipped=true readbackFailureNonDestructive=true hardwareHevc=true");
             return saved;
         } catch (Throwable t) {
             Log.e(TAG, "IRIS_26646_TRUE2X_HEIC_FAILED", t);
diff --git a/app/version.properties b/app/version.properties
index b5ba4a40f26b2a89756b1f7683de8cf5f45b0de9..281069d0ec69ff24379d5c3634fc61685ff973b6 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726697
-VERSION_BUILD=26697
+VERSION_NAME=0.9726698
+VERSION_BUILD=26698
