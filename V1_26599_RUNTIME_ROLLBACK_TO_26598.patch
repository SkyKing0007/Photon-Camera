diff --git a/app/src/main/assets/shaders/motionv2/display_exposure.glsl b/app/src/main/assets/shaders/motionv2/display_exposure.glsl
index 1a6a870eaf8ecae82a51e6d17cfd330c79bafeb5..93682532666518310376797465ae2b0b8ace4198 100644
--- a/app/src/main/assets/shaders/motionv2/display_exposure.glsl
+++ b/app/src/main/assets/shaders/motionv2/display_exposure.glsl
@@ -3,39 +3,14 @@ precision mediump sampler2D;
 
 uniform sampler2D InputBuffer;
 uniform float displayGain;
-uniform float presentationLinearAnchor;
-uniform float presentationKnee;
-uniform int motionToneNormalization;
 out vec3 Output;
 
-/* IRIS_26599_SHARED_HDR_PRESENTATION_NORMALIZATION
- * Motion keeps the solved display gain exactly through the linear body anchor, then smoothly
- * reduces only the additional positive gain. A single scalar derived from max(luma, peak) scales
- * whole RGB, so hue and neutrals are invariant. Night and non-positive Motion gains retain the
- * exact successful-26598 scalar behavior. This node remains presentation-only: no Camera2 write.
+/* IRIS_26516_SIGNED_PRESENTATION_EV_SCALAR
+ * This shader is a pure positive linear scalar. Source restoration normally uses >=1x, while
+ * viewfinder-matched presentation may legitimately be below 1x. No Camera2 authority lives here.
  */
-float irisGuide(vec3 c) {
-    float y = dot(c, vec3(0.2126, 0.7152, 0.0722));
-    return max(y, max(c.r, max(c.g, c.b)));
-}
-float irisMappedGuide(float guide, float gain) {
-    float g = max(guide, 0.0);
-    float dg = max(gain, 1.0e-6);
-    if (motionToneNormalization == 0 || dg <= 1.0 || g <= presentationLinearAnchor)
-        return g * dg;
-    float x = g - presentationLinearAnchor;
-    float knee = max(presentationKnee, 1.0e-6);
-    return presentationLinearAnchor * dg + x
-        + (dg - 1.0) * knee * log(1.0 + x / knee);
-}
 void main() {
     ivec2 p = ivec2(gl_FragCoord.xy);
     vec3 c = max(texelFetch(InputBuffer, p, 0).rgb, vec3(0.0));
-    float guide = irisGuide(c);
-    if (guide <= 1.0e-7) {
-        Output = c * max(displayGain, 1.0e-6);
-        return;
-    }
-    float mapped = irisMappedGuide(guide, displayGain);
-    Output = c * (mapped / guide);
+    Output = c * max(displayGain, 1.0e-6);
 }
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 9cc4fad1431ccd152acda770989beda28b1b2a8b..a6e753a803ce2e848d52ce6f5404b69d062c5b34 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -504,7 +504,6 @@ struct Params {
     bool mirror=false;
     bool motionHdrHandoff=false;
     float residualZoom=1.f,displayGain=1.f,exposureEv=0.f,shadows=0.f,contrast=0.f,sceneWhite=1.f;
-    float presentationLinearAnchor=0.18f,presentationKnee=0.18f;
     float sensorToProfile[9]{};
     float profileToDisplay[9]{};
 };
@@ -532,22 +531,13 @@ inline bool adaptiveAppearance(const SourceRegion&s,const Params&p,int x,int y,V
     if(inPeak>=1.f||projectedPeak>=1.f)limit=1.f;else{limit=std::min(limit,componentGainLimit(cy,cc.r));limit=std::min(limit,componentGainLimit(cy,cc.g));limit=std::min(limit,componentGainLimit(cy,cc.b));}
     float g=clampf(std::min(requested,limit),1.f,1.22f);*out=g<=1.000001f?center:add(Vec3{cy,cy,cy},mul(cc,g));return true;
 }
-inline Vec3 iris26599PresentationNormalize(Vec3 c,const Params&p){
-    const float dg=std::max(p.displayGain,1.0e-6f);
-    if(!p.motionHdrHandoff||dg<=1.f)return mul(c,dg);
-    const float y=luma(c),g=std::max(y,peak(c));
-    if(g<=1.0e-7f||g<=p.presentationLinearAnchor)return mul(c,dg);
-    const float x=g-p.presentationLinearAnchor,k=std::max(p.presentationKnee,1.0e-6f);
-    const float mapped=p.presentationLinearAnchor*dg+x+(dg-1.f)*k*std::log(1.f+x/k);
-    return mul(c,mapped/g);
-}
 inline Vec3 tone(Vec3 rgb,const Params&p){
     rgb=mul(clampNonnegative(rgb),std::exp2(p.exposureEv));
     if(std::fabs(p.shadows)>=0.0001f){float y=luma(rgb),mask=1.f-smoothstep(0.08f,0.55f,y),target=y;if(p.shadows<0.f)target=y+(-p.shadows)*0.08f*mask*(1.f-clampf(y,0.f,1.f));else target=y*(1.f-0.75f*p.shadows*mask);rgb=y<=1.0e-7f?Vec3{std::max(target,0.f),std::max(target,0.f),std::max(target,0.f)}:mul(clampNonnegative(rgb),std::max(target,0.f)/y);}
     if(std::fabs(p.contrast)>=0.0001f){float y=luma(rgb);if(y>1.0e-7f){const float pivot=0.18f;float slope=1.f+0.25f*p.contrast;float target=pivot*std::exp2(std::log2(std::max(y/pivot,1.0e-6f))*slope);rgb=mul(clampNonnegative(rgb),std::max(target,0.f)/y);}}
     return clampNonnegative(rgb);
 }
-inline bool preRenderAt(const SourceRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearance(s,p,x,y,&c))return false;c=iris26599PresentationNormalize(c,p);*out=tone(c,p);return true;}
+inline bool preRenderAt(const SourceRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearance(s,p,x,y,&c))return false;c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
 inline bool samplePreRender(const SourceRegion&s,const Params&p,float x,float y,Vec3*out){
     x=clampf(x,0.f,(float)(p.trueW-1));y=clampf(y,0.f,(float)(p.trueH-1));
     if(p.residualZoom<=1.00001f){int ix=(int)std::lround(x),iy=(int)std::lround(y);return preRenderAt(s,p,ix,iy,out);}
@@ -592,7 +582,7 @@ inline bool adaptiveAppearanceCached(const ProfileRegion&s,const Params&p,int x,
     if(inPeak>=1.f||projectedPeak>=1.f)limit=1.f;else{limit=std::min(limit,componentGainLimit(cy,cc.r));limit=std::min(limit,componentGainLimit(cy,cc.g));limit=std::min(limit,componentGainLimit(cy,cc.b));}
     float g=clampf(std::min(requested,limit),1.f,1.22f);*out=g<=1.000001f?center:add(Vec3{cy,cy,cy},mul(cc,g));return true;
 }
-inline bool preRenderAtCached(const ProfileRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearanceCached(s,p,x,y,&c))return false;c=iris26599PresentationNormalize(c,p);*out=tone(c,p);return true;}
+inline bool preRenderAtCached(const ProfileRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearanceCached(s,p,x,y,&c))return false;c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
 inline bool samplePreRenderCached(const ProfileRegion&s,const Params&p,float x,float y,Vec3*out){
     x=clampf(x,0.f,(float)(p.trueW-1));y=clampf(y,0.f,(float)(p.trueH-1));
     if(p.residualZoom<=1.00001f){int ix=(int)std::lround(x),iy=(int)std::lround(y);return preRenderAtCached(s,p,ix,iy,out);}
@@ -763,8 +753,6 @@ uniform int uBandH;
 uniform int uGenerateGain;
 uniform float uResidualZoom;
 uniform float uDisplayGain;
-uniform float uPresentationLinearAnchor;
-uniform float uPresentationKnee;
 uniform float uExposureEv;
 uniform float uShadows;
 uniform float uContrast;
@@ -837,15 +825,6 @@ vec3 irisAdaptive(ivec2 p){
     float g=clamp(min(requested,limitV),1.0,1.22);
     return g<=1.000001?center:vec3(cy)+cc*g;
 }
-vec3 irisPresentationNormalize(vec3 c){
-    float dg=max(uDisplayGain,1.0e-6);
-    if(uMotionHdrHandoff==0||dg<=1.0)return c*dg;
-    float y=irisLuma(c),g=max(y,irisPeak(c));
-    if(g<=1.0e-7||g<=uPresentationLinearAnchor)return c*dg;
-    float x=g-uPresentationLinearAnchor,k=max(uPresentationKnee,1.0e-6);
-    float mapped=uPresentationLinearAnchor*dg+x+(dg-1.0)*k*log(1.0+x/k);
-    return c*(mapped/g);
-}
 vec3 irisTone(vec3 rgb){
     rgb=irisClampNonnegative(rgb)*exp2(uExposureEv);
     if(abs(uShadows)>=0.0001){
@@ -865,7 +844,7 @@ vec3 irisTone(vec3 rgb){
     }
     return irisClampNonnegative(rgb);
 }
-vec3 irisPreRenderAt(ivec2 p){return irisTone(irisPresentationNormalize(irisAdaptive(p)));}
+vec3 irisPreRenderAt(ivec2 p){return irisTone(irisAdaptive(p)*max(uDisplayGain,1.0e-6));}
 vec3 irisSamplePreRender(vec2 p){
     p=clamp(p,vec2(0.0),vec2(uTrueSize-ivec2(1)));
     if(uResidualZoom<=1.00001)return irisPreRenderAt(ivec2(floor(p+vec2(0.5))));
@@ -996,7 +975,7 @@ private:
     static void set(std::string*r,const char*m){if(r)*r=m?m:"unknown";}
     static void setGl(std::string*r,const char*stage,GLenum err){if(!r)return;char b[96];std::snprintf(b,sizeof(b),"%s_gl_error_0x%04x",stage?stage:"gl",(unsigned int)err);*r=b;}
     void setParams(){
-        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uPresentationLinearAnchor"),params->presentationLinearAnchor);glUniform1f(loc("uPresentationKnee"),params->presentationKnee);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);
+        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);
         glUniform3f(loc("uSensorRow0"),params->sensorToProfile[0],params->sensorToProfile[1],params->sensorToProfile[2]);glUniform3f(loc("uSensorRow1"),params->sensorToProfile[3],params->sensorToProfile[4],params->sensorToProfile[5]);glUniform3f(loc("uSensorRow2"),params->sensorToProfile[6],params->sensorToProfile[7],params->sensorToProfile[8]);
         glUniform3f(loc("uDisplayRow0"),params->profileToDisplay[0],params->profileToDisplay[1],params->profileToDisplay[2]);glUniform3f(loc("uDisplayRow1"),params->profileToDisplay[3],params->profileToDisplay[4],params->profileToDisplay[5]);glUniform3f(loc("uDisplayRow2"),params->profileToDisplay[6],params->profileToDisplay[7],params->profileToDisplay[8]);
     }
@@ -1084,12 +1063,12 @@ extern "C" JNIEXPORT jstring JNICALL Java_com_particlesdevs_photoncamera_process
 
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeTrue2xNative(
         JNIEnv*e,jclass,jobject bitmap,jstring renderPath,jint trueW,jint trueH,jint rawW,jint rawH,jint cropW,jint cropH,jint rotation,jboolean mirror,jfloat residualZoom,
-        jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat sceneWhite,jboolean motionHdrHandoff,jfloat presentationLinearAnchor,jfloat presentationKnee,jobject watermarkBitmap,
+        jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat sceneWhite,jboolean motionHdrHandoff,jobject watermarkBitmap,
         jfloatArray jinResidual,jintArray jinReference,jint jinW,jint jinH,jstring gainPath,jfloat gainMaxRatio,jfloat gainContentMaxRatio,jstring path,jint quality){
     using namespace iris26564render;gIris26576PublicationTelemetry="backend=UNSET callStarted=true";AndroidBitmapInfo info{};if(!bitmap||!renderPath||!path||trueW<=0||trueH<=0||rawW<=0||rawH<=0||cropW<=0||cropH<=0||trueW!=rawW*2||trueH!=rawH*2||cropW>rawW||cropH>rawH||AndroidBitmap_getInfo(e,bitmap,&info)!=ANDROID_BITMAP_RESULT_SUCCESS||info.format!=ANDROID_BITMAP_FORMAT_RGBA_8888){iris26576SetPublicationTelemetry("backend=NONE validationFailed=true");return JNI_FALSE;}
     int expectedNativeW=(rotation==90||rotation==270)?cropH:rawW,expectedNativeH=(rotation==90||rotation==270)?rawW:cropH;if((int)info.width!=expectedNativeW||(int)info.height!=expectedNativeH)return JNI_FALSE;
-    Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.presentationLinearAnchor=presentationLinearAnchor;p.presentationKnee=presentationKnee;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
-    if(!sensorToProfile||!profileToDisplay||e->GetArrayLength(sensorToProfile)!=9||e->GetArrayLength(profileToDisplay)!=9)return JNI_FALSE;e->GetFloatArrayRegion(sensorToProfile,0,9,p.sensorToProfile);e->GetFloatArrayRegion(profileToDisplay,0,9,p.profileToDisplay);if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.sensorToProfile)if(!std::isfinite(v))return JNI_FALSE;for(float v:p.profileToDisplay)if(!std::isfinite(v))return JNI_FALSE;if(!std::isfinite(p.displayGain)||p.displayGain<=0.f||!std::isfinite(p.exposureEv)||!std::isfinite(p.shadows)||!std::isfinite(p.contrast)||!std::isfinite(p.residualZoom)||!std::isfinite(p.sceneWhite)||p.sceneWhite<=0.f||!std::isfinite(p.presentationLinearAnchor)||p.presentationLinearAnchor<=0.f||!std::isfinite(p.presentationKnee)||p.presentationKnee<=0.f)return JNI_FALSE;
+    Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
+    if(!sensorToProfile||!profileToDisplay||e->GetArrayLength(sensorToProfile)!=9||e->GetArrayLength(profileToDisplay)!=9)return JNI_FALSE;e->GetFloatArrayRegion(sensorToProfile,0,9,p.sensorToProfile);e->GetFloatArrayRegion(profileToDisplay,0,9,p.profileToDisplay);if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.sensorToProfile)if(!std::isfinite(v))return JNI_FALSE;for(float v:p.profileToDisplay)if(!std::isfinite(v))return JNI_FALSE;if(!std::isfinite(p.displayGain)||p.displayGain<=0.f||!std::isfinite(p.exposureEv)||!std::isfinite(p.shadows)||!std::isfinite(p.contrast)||!std::isfinite(p.residualZoom)||!std::isfinite(p.sceneWhite)||p.sceneWhite<=0.f)return JNI_FALSE;
     U rp(e,renderPath),gp(e,gainPath),op(e,path);if(!rp.c||!op.c)return JNI_FALSE;const bool generateGain=gp.c!=nullptr;if(generateGain&&(!std::isfinite((float)gainMaxRatio)||gainMaxRatio<=1.f||!std::isfinite((float)gainContentMaxRatio)||gainContentMaxRatio<1.f))return JNI_FALSE;const float gainContentMax=generateGain?clampf((float)gainContentMaxRatio,1.f,(float)gainMaxRatio):1.f;int sourceFd=open(rp.c,O_RDONLY);if(sourceFd<0)return JNI_FALSE;struct stat st{};uint64_t expected=(uint64_t)trueW*(uint64_t)trueH*6ull;if(fstat(sourceFd,&st)!=0||(uint64_t)st.st_size!=expected){close(sourceFd);return JNI_FALSE;}
     Watermark water{};if(watermarkBitmap){AndroidBitmapInfo wi{};if(AndroidBitmap_getInfo(e,watermarkBitmap,&wi)!=ANDROID_BITMAP_RESULT_SUCCESS||wi.format!=ANDROID_BITMAP_FORMAT_RGBA_8888||wi.width==0||wi.height==0){close(sourceFd);return JNI_FALSE;}void*wp=nullptr;if(AndroidBitmap_lockPixels(e,watermarkBitmap,&wp)!=ANDROID_BITMAP_RESULT_SUCCESS||!wp){close(sourceFd);return JNI_FALSE;}water.w=(int)wi.width;water.h=(int)wi.height;water.rgba.resize((size_t)water.w*water.h*4);for(int y=0;y<water.h;y++)std::memcpy(water.rgba.data()+(size_t)y*water.w*4,(const uint8_t*)wp+(size_t)y*wi.stride,(size_t)water.w*4);AndroidBitmap_unlockPixels(e,watermarkBitmap);}
     Jin jin{};if(jinResidual||jinReference||jinW||jinH){if(!jinResidual||!jinReference||jinW<=1||jinH<=1||e->GetArrayLength(jinResidual)!=(jsize)((jlong)jinW*jinH*3)||e->GetArrayLength(jinReference)!=(jsize)((jlong)jinW*jinH)){close(sourceFd);return JNI_FALSE;}jin.w=jinW;jin.h=jinH;jin.residual.resize((size_t)jinW*jinH*3);jin.guide.resize((size_t)jinW*jinH);e->GetFloatArrayRegion(jinResidual,0,(jsize)jin.residual.size(),jin.residual.data());e->GetIntArrayRegion(jinReference,0,(jsize)jin.guide.size(),jin.guide.data());if(e->ExceptionCheck()){close(sourceFd);return JNI_FALSE;}for(float v:jin.residual)if(!std::isfinite(v)){close(sourceFd);return JNI_FALSE;}const auto&p3=displayP3Lut();jin.guideP3.resize((size_t)jinW*jinH*3);for(int i=0;i<jinW*jinH;i++){jint argb=jin.guide[(size_t)i];float sr=(float)((argb>>16)&255)/255.f,sg=(float)((argb>>8)&255)/255.f,sb=(float)(argb&255)/255.f;auto baseP3=p3.convertEncoded(sr,sg,sb);auto outP3=p3.convertEncoded(clampf(sr+jin.residual[(size_t)i*3],0.f,1.f),clampf(sg+jin.residual[(size_t)i*3+1],0.f,1.f),clampf(sb+jin.residual[(size_t)i*3+2],0.f,1.f));for(int c=0;c<3;c++){jin.guideP3[(size_t)i*3+c]=baseP3[(size_t)c];jin.residual[(size_t)i*3+c]=outP3[(size_t)c]-baseP3[(size_t)c];}}}
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index df085a6807006ddcb0c7134b247d19ebb8c99a15..87837f4e03db4995e8559fa66dbff6f0c94ff40d 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -478,17 +478,13 @@ internal object GlesMgcRawSabreShaders {
             }
             return second;
         }
-        /* IRIS_26599_PHASE_SCOPED_REGION_MEASURABILITY
-         * Region connectivity needs the SHORT quad to remain physically measurable, not a full
-         * 10% reserve in every unrelated CFA phase. Boundary seeds below still demand the strict
-         * per-phase 0.90 reserve for every phase actually used by radiometric proof.
-         */
-        bool shortQuadPhysicallyMeasurable(ivec2 referenceQuad, vec2 flowRawPixels) {
+        bool shortQuadHasHeadroom(ivec2 referenceQuad, vec2 flowRawPixels) {
             for (int phase = 0; phase < 4; ++phase) {
                 ivec2 rp = referenceQuad + phaseOffset(phase);
                 vec2 shortCenter = vec2(rp) + vec2(0.5) + flowRawPixels;
                 if (samePhasePeak(samePhaseSupport(
-                        uShortRaw, shortCenter, phase, uShortBlackByPhase)) >= 0.995) return false;
+                        uShortRaw, shortCenter, phase, uShortBlackByPhase)) >=
+                        uShortHeadroomThreshold) return false;
             }
             return true;
         }
@@ -502,7 +498,7 @@ internal object GlesMgcRawSabreShaders {
             ivec2 referenceQuad = (referenceP / 2) * 2;
 
             bool geometryTrusted = flow.w <= uFlowVariationPixelsThreshold &&
-                shortQuadPhysicallyMeasurable(referenceQuad, flowRawPixels);
+                shortQuadHasHeadroom(referenceQuad, flowRawPixels);
             float referenceSecond = secondHighestReference(referenceP);
             bool regionCandidate = geometryTrusted && referenceSecond >= uRegionFloor;
             oRegion = regionCandidate ? 1.0 : 0.0;
@@ -566,18 +562,11 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 
-    /* IRIS_26599_EFFECTIVE_SENSOR_LOSS_SHORT_MASK
-     * Preserve the exact 26596/26597 literal sensor-code clipping owner, but add a physically
-     * grounded non-literal loss owner. RAW is linear: before NORMAL loses information, an aligned
-     * SHORT sample scaled by the exact exposure ratio must agree with the same NORMAL CFA phase.
-     * A material SHORT>NORMAL deficit therefore proves effective NORMAL highlight loss even when
-     * the NORMAL code is a few counts below literal white. Whole-RGB replacement remains one
-     * scalar mask, and the non-literal route still requires propagated boundary radiometry,
-     * coherent Sabre flow, physically measurable SHORT, and phase-scoped SHORT headroom.
-     *
-     * oGateStage is telemetry only and never feeds reconstruction. It stores terminal/cumulative
-     * progress as stage/7: 1 bright candidate, 2 flow, 3 measurable SHORT, 4 loss detected,
-     * 5 SHORT headroom/evidence, 6 region/direct radiometric trust, 7 non-zero final handoff.
+    /* IRIS_26596_PHASE_COMPLETE_SENSOR_LOSS_SHORT_MASK
+     * Exact sensor-code clipping in even one CFA phase is real information loss. SHORT replaces
+     * whole RGB only when the frozen Sabre flow is coherent, every SHORT CFA phase needed by
+     * reconstruction retains headroom, and every actually clipped NORMAL phase is explained by
+     * exposure-normalized same-phase SHORT. Near-clip/non-clipped ownership remains frozen 26595.
      */
     val shortRestoreMask26596 = """
         #version 300 es
@@ -589,7 +578,6 @@ internal object GlesMgcRawSabreShaders {
         uniform sampler2D uFlow;
         uniform sampler2D uRegionTrust;
         uniform ivec2 uRawSize;
-        uniform ivec2 uOutputSize;
         uniform vec4 uFlowScaleOffset;
         uniform vec4 uReferenceBlackByPhase;
         uniform vec4 uShortBlackByPhase;
@@ -598,14 +586,7 @@ internal object GlesMgcRawSabreShaders {
         uniform float uReferenceNearClipThreshold;
         uniform float uShortHeadroomThreshold;
         uniform float uFlowVariationPixelsThreshold;
-        uniform float uEffectiveSignalFloor;
-        uniform float uEffectiveLossStart;
-        uniform float uEffectiveLossFull;
-        uniform float uSevereSingleReferenceFloor;
-        uniform float uSevereSingleLossStart;
-        uniform float uSevereSingleLossFull;
         layout(location = 0) out float oMask;
-        layout(location = 1) out float oGateStage;
 
         ivec2 clampRaw(ivec2 p) { return clamp(p, ivec2(0), uRawSize - ivec2(1)); }
         int phaseAt(ivec2 p) { return (p.x & 1) + 2 * (p.y & 1); }
@@ -663,14 +644,16 @@ internal object GlesMgcRawSabreShaders {
                 count += rawAt(uReferenceRaw, q + phaseOffset(phase)) >= clipCode ? 1 : 0;
             return count;
         }
-        vec4 referenceQuadNormalized(ivec2 referenceQuad) {
-            vec4 v = vec4(0.0);
-            for (int phase = 0; phase < 4; ++phase)
-                v[phase] = normalizedAt(
-                    uReferenceRaw, referenceQuad + phaseOffset(phase), uReferenceBlackByPhase);
-            return v;
+        float referenceSecondHighest(ivec2 p) {
+            ivec2 q = (clampRaw(p) / 2) * 2;
+            float first = 0.0; float second = 0.0;
+            for (int phase = 0; phase < 4; ++phase) {
+                float signal = normalizedAt(uReferenceRaw, q + phaseOffset(phase), uReferenceBlackByPhase);
+                if (signal >= first) { second = first; first = signal; }
+                else if (signal > second) second = signal;
+            }
+            return second;
         }
-        float highestVec4(vec4 v) { return max(max(v.x, v.y), max(v.z, v.w)); }
         float secondHighestVec4(vec4 v) {
             float first = 0.0; float second = 0.0;
             for (int phase = 0; phase < 4; ++phase) {
@@ -690,6 +673,12 @@ internal object GlesMgcRawSabreShaders {
                 vec4 support = samePhaseSupport(uShortRaw, shortCenter, phase, uShortBlackByPhase);
                 float supportPeak = samePhasePeak(support);
                 shortSupportPeak[phase] = supportPeak;
+                /* IRIS_26597_PHASE_SCOPED_SHORT_HEADROOM
+                 * Whole-RGB SHORT still requires every phase to remain physically measurable,
+                 * but do not demand a full 10% reserve from an unrelated phase. The strict 0.90
+                 * reserve is checked below only for phases whose NORMAL sensor code is actually
+                 * clipped. A SHORT phase at/near physical sensor white still fails closed.
+                 */
                 if (supportPeak >= 0.995) return false;
                 shortNormalized[phase] = bilinearSamePhase(support, shortCenter, phase) * uExposureRatio;
             }
@@ -708,102 +697,36 @@ internal object GlesMgcRawSabreShaders {
             }
             return clipped >= 1;
         }
-        vec4 effectiveLossByPhase(vec4 referenceNormalized, vec4 shortNormalized) {
-            vec4 outLoss = vec4(0.0);
-            for (int phase = 0; phase < 4; ++phase) {
-                float predicted = max(shortNormalized[phase], 0.0);
-                float reference = max(referenceNormalized[phase], 0.0);
-                float relativeLoss = max(predicted - reference, 0.0) / max(predicted, 0.05);
-                float signalGate = smoothstep(uEffectiveSignalFloor, 0.92, predicted);
-                outLoss[phase] = signalGate *
-                    smoothstep(uEffectiveLossStart, uEffectiveLossFull, relativeLoss);
-            }
-            return outLoss;
-        }
-        vec4 phaseScopedHeadroom(vec4 evidence, vec4 shortSupportPeak) {
-            vec4 result = vec4(0.0);
-            for (int phase = 0; phase < 4; ++phase)
-                result[phase] = shortSupportPeak[phase] < uShortHeadroomThreshold
-                    ? evidence[phase] : 0.0;
-            return result;
+        bool allShortPhasesHaveStrongHeadroom(vec4 shortSupportPeak) {
+            return max(max(shortSupportPeak.x, shortSupportPeak.y),
+                       max(shortSupportPeak.z, shortSupportPeak.w)) < uShortHeadroomThreshold;
         }
-        float severeSingleLossWeight(vec4 referenceNormalized, vec4 shortNormalized,
-                vec4 shortSupportPeak) {
-            float best = 0.0;
-            for (int phase = 0; phase < 4; ++phase) {
-                float predicted = max(shortNormalized[phase], 0.0);
-                float reference = max(referenceNormalized[phase], 0.0);
-                float relativeLoss = max(predicted - reference, 0.0) / max(predicted, 0.05);
-                float referenceGate = smoothstep(
-                    uSevereSingleReferenceFloor, uReferenceNearClipThreshold, reference);
-                float signalGate = smoothstep(uEffectiveSignalFloor, 0.92, predicted);
-                float lossGate = smoothstep(
-                    uSevereSingleLossStart, uSevereSingleLossFull, relativeLoss);
-                float headroom = shortSupportPeak[phase] < uShortHeadroomThreshold ? 1.0 : 0.0;
-                best = max(best, referenceGate * signalGate * lossGate * headroom);
-            }
-            return best;
-        }
-        void stage(int value) { oGateStage = float(value) / 7.0; }
         void main() {
-            oMask = 0.0;
-            stage(0);
-            vec2 referenceUv = gl_FragCoord.xy / vec2(uOutputSize);
-            ivec2 referenceP = clampRaw(ivec2(floor(referenceUv * vec2(uRawSize))));
-            ivec2 referenceQuad = (clampRaw(referenceP) / 2) * 2;
-            vec4 referenceNormalized = referenceQuadNormalized(referenceQuad);
-            float referenceSecond = secondHighestVec4(referenceNormalized);
-            bool actualSensorLoss = sensorClippedPhaseCount(referenceP) >= 1;
-            if (!actualSensorLoss && referenceSecond < 0.70) return;
-            stage(1);
-
+            vec2 referenceUv = gl_FragCoord.xy / vec2(uRawSize);
             vec2 flowUv = referenceUv * uFlowScaleOffset.xy + uFlowScaleOffset.zw;
             vec4 flow = texture(uFlow, flowUv);
-            if (flow.w > uFlowVariationPixelsThreshold) return;
-            stage(2);
+            if (flow.w > uFlowVariationPixelsThreshold) { oMask = 0.0; return; }
             vec2 flowRawPixels = flow.xy * vec2(uRawSize);
+            ivec2 referenceP = ivec2(gl_FragCoord.xy);
+            ivec2 referenceQuad = (clampRaw(referenceP) / 2) * 2;
             vec4 shortNormalized = vec4(0.0);
             vec4 shortSupportPeak = vec4(0.0);
-            if (!shortQuadEvidence(referenceQuad, flowRawPixels, shortNormalized, shortSupportPeak)) return;
-            stage(3);
-
+            if (!shortQuadEvidence(referenceQuad, flowRawPixels, shortNormalized, shortSupportPeak)) { oMask = 0.0; return; }
+            const float handoffStart = 0.90;
+            bool actualSensorLoss = sensorClippedPhaseCount(referenceP) >= 1;
             if (actualSensorLoss) {
-                stage(4);
-                if (!everyClippedReferencePhaseExplained(
-                        referenceQuad, shortNormalized, shortSupportPeak)) return;
-                stage(5);
-                stage(6); /* Literal sensor loss + same-phase SHORT proof is direct radiometry. */
-                oMask = 1.0;
-                stage(7);
+                oMask = everyClippedReferencePhaseExplained(
+                    referenceQuad, shortNormalized, shortSupportPeak) ? 1.0 : 0.0;
                 return;
             }
-
-            vec4 rawLossEvidence = effectiveLossByPhase(referenceNormalized, shortNormalized);
-            float twoPhaseRawLoss = secondHighestVec4(rawLossEvidence);
-            float severeRawLoss = 0.0;
-            for (int phase = 0; phase < 4; ++phase) {
-                float predicted = max(shortNormalized[phase], 0.0);
-                float reference = max(referenceNormalized[phase], 0.0);
-                float relativeLoss = max(predicted - reference, 0.0) / max(predicted, 0.05);
-                severeRawLoss = max(severeRawLoss,
-                    smoothstep(uSevereSingleReferenceFloor, uReferenceNearClipThreshold, reference) *
-                    smoothstep(uEffectiveSignalFloor, 0.92, predicted) *
-                    smoothstep(uSevereSingleLossStart, uSevereSingleLossFull, relativeLoss));
-            }
-            if (max(twoPhaseRawLoss, severeRawLoss) <= 0.0) return;
-            stage(4);
-
-            vec4 headroomLossEvidence = phaseScopedHeadroom(rawLossEvidence, shortSupportPeak);
-            float twoPhaseHandoff = secondHighestVec4(headroomLossEvidence);
-            float severeSingleHandoff = severeSingleLossWeight(
-                referenceNormalized, shortNormalized, shortSupportPeak);
-            float effectiveHandoff = max(twoPhaseHandoff, severeSingleHandoff);
-            if (effectiveHandoff <= 0.0) return;
-            stage(5);
-            if (texture(uRegionTrust, referenceUv).r < 0.5) return;
-            stage(6);
-            oMask = clamp(effectiveHandoff, 0.0, 1.0);
-            if (oMask > 0.0) stage(7);
+            /* Preserve the exact 26596 fail-closed near-clip path: without an actual clipped
+             * NORMAL phase, every SHORT phase must still carry the full 0.90 reserve. */
+            if (!allShortPhasesHaveStrongHeadroom(shortSupportPeak)) { oMask = 0.0; return; }
+            float referenceSecond = referenceSecondHighest(referenceP);
+            float shortSecondNormalized = secondHighestVec4(shortNormalized);
+            if (referenceSecond < handoffStart || shortSecondNormalized < handoffStart) { oMask = 0.0; return; }
+            if (texture(uRegionTrust, referenceUv).r < 0.5) { oMask = 0.0; return; }
+            oMask = smoothstep(handoffStart, uReferenceNearClipThreshold, referenceSecond);
         }
     """.trimIndent()
 
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 75f820af68a181339750789d83f9e0aed89d6ae7..1aab2bb8d2175b4dafcfcbb676d2eaea3a1b7f03 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2505,18 +2505,6 @@ internal class GlesMgcRawSpatialStacker(
                         shortCalibration = nativeShortCalibration,
                         exposureRatio = exposureScale,
                         output = highlightShortMask26587,
-                        outputGateStage26599 = 0,
-                        outputWidth26599 = width,
-                        outputHeight26599 = height,
-                    )
-                    val shortGateProbe26599 = probeSabreShortGateStages26599(
-                        referenceRaw = referenceRaw,
-                        shortRaw = currentRaw,
-                        flow = flow,
-                        regionTrust = shortRegionTrust26594,
-                        referenceCalibration = referenceCalibration,
-                        shortCalibration = nativeShortCalibration,
-                        exposureRatio = exposureScale,
                     )
                     val shortMaskProbe26590 = probeSabreShortRestoreMask26590(highlightShortMask26587)
                     highlightShortFullActivePixels26595 =
@@ -2527,13 +2515,10 @@ internal class GlesMgcRawSpatialStacker(
                             "active=${shortMaskProbe26590.activePixels}/${shortMaskProbe26590.totalPixels} " +
                             "strong=${shortMaskProbe26590.strongPixels}/${shortMaskProbe26590.totalPixels} " +
                             "fullActive=$fullActiveText26595/${Math.multiplyExact(width, height)} " +
-                            "gateProbe=$shortGateProbe26599 " +
                             "maxHandoffWeight=${shortMaskProbe26590.maxConfidence} " +
                             "meanHandoffWeight=${shortMaskProbe26590.meanConfidence} " +
-                            "effectiveSignalFloor=0.72 effectiveLoss=0.05..0.12 " +
-                            "severeSingleReference=0.94 severeSingleLoss=0.12..0.22 " +
-                            "shortHeadroomThreshold=0.90 boundaryRadiometry=subpixelSameCfaPhase " +
-                            "regionFloor=0.70 boundaryCeiling=0.90 " +
+                            "handoffStart=0.90 fullShortAt=0.98 shortHeadroomThreshold=0.90 " +
+                            "boundaryRadiometry=subpixelSameCfaPhase regionFloor=0.70 boundaryCeiling=0.90 " +
                             "regionPropagation=8connected passes=$SHORT_REGION_PROPAGATION_PASSES_26594 " +
                             "actualSensorClipBypass=true clipPhasesMin=1 clippedPhaseShortProof=true pureFlowGeometry=true " +
                             "flowVariationRawPxMax=2.0 sabrePhotometricGate=false unblockerGate=false " +
@@ -4108,9 +4093,6 @@ internal class GlesMgcRawSpatialStacker(
         shortCalibration: FrameCalibration,
         exposureRatio: Float,
         output: Int,
-        outputGateStage26599: Int,
-        outputWidth26599: Int,
-        outputHeight26599: Int,
     ) {
         val program = sabreShortRestoreMaskProgram26595
         check(program != 0)
@@ -4120,7 +4102,6 @@ internal class GlesMgcRawSpatialStacker(
         bindTexture(program, "uFlow", 2, flow.texture)
         bindTexture(program, "uRegionTrust", 3, regionTrust)
         uniform2i(program, "uRawSize", width, height)
-        uniform2i(program, "uOutputSize", outputWidth26599, outputHeight26599)
         uniformSabreFlowScaleOffset(program, flow)
         uniform4fv(program, "uReferenceBlackByPhase", positionalBlackLevels26587(referenceCalibration))
         uniform4fv(program, "uShortBlackByPhase", positionalBlackLevels26587(shortCalibration))
@@ -4129,64 +4110,7 @@ internal class GlesMgcRawSpatialStacker(
         uniform1f(program, "uReferenceNearClipThreshold", 0.98f)
         uniform1f(program, "uShortHeadroomThreshold", 0.90f)
         uniform1f(program, "uFlowVariationPixelsThreshold", 2.0f)
-        uniform1f(program, "uEffectiveSignalFloor", 0.72f)
-        uniform1f(program, "uEffectiveLossStart", 0.05f)
-        uniform1f(program, "uEffectiveLossFull", 0.12f)
-        uniform1f(program, "uSevereSingleReferenceFloor", 0.94f)
-        uniform1f(program, "uSevereSingleLossStart", 0.12f)
-        uniform1f(program, "uSevereSingleLossFull", 0.22f)
-        val targets = if (outputGateStage26599 != 0)
-            intArrayOf(output, outputGateStage26599) else intArrayOf(output)
-        draw(program, outputWidth26599, outputHeight26599, targets)
-    }
-
-    /* IRIS_26599_SHORT_GATE_PROBE
-     * Re-evaluate the exact same mask shader at 64x48 sample points and read only 3 KiB. This
-     * avoids a native-resolution diagnostic allocation/compute pass while exposing which gate is
-     * dominant. The full-resolution final-active count below remains the exact handoff proof.
-     */
-    private fun probeSabreShortGateStages26599(
-        referenceRaw: Int,
-        shortRaw: Int,
-        flow: SabreConvertedAlignment,
-        regionTrust: Int,
-        referenceCalibration: FrameCalibration,
-        shortCalibration: FrameCalibration,
-        exposureRatio: Float,
-    ): String {
-        val probeWidth = 64
-        val probeHeight = 48
-        val maskProbe = createTexture(probeWidth, probeHeight, GLES30.GL_R8, GLES30.GL_NEAREST)
-        val stageProbe = createTexture(probeWidth, probeHeight, GLES30.GL_R8, GLES30.GL_NEAREST)
-        try {
-            renderSabreShortRestoreMask26595(
-                referenceRaw = referenceRaw, shortRaw = shortRaw, flow = flow,
-                regionTrust = regionTrust, referenceCalibration = referenceCalibration,
-                shortCalibration = shortCalibration, exposureRatio = exposureRatio,
-                output = maskProbe, outputGateStage26599 = stageProbe,
-                outputWidth26599 = probeWidth, outputHeight26599 = probeHeight,
-            )
-            val bytes = readR8Mask(
-                stageProbe, "26599 SHORT gate-stage probe",
-                maskWidth = probeWidth, maskHeight = probeHeight,
-            )
-            val terminal = IntArray(8)
-            bytes.forEach { raw ->
-                val encoded = raw.toInt() and 0xff
-                val stage = ((encoded * 7 + 127) / 255).coerceIn(0, 7)
-                terminal[stage]++
-            }
-            val cumulative = IntArray(8)
-            var running = 0
-            for (stage in 7 downTo 0) { running += terminal[stage]; cumulative[stage] = running }
-            return "${probeWidth}x$probeHeight candidate=${cumulative[1]} flow=${cumulative[2]} " +
-                "shortMeasurable=${cumulative[3]} loss=${cumulative[4]} " +
-                "shortHeadroom=${cumulative[5]} radiometry=${cumulative[6]} " +
-                "final=${cumulative[7]} total=${bytes.size}"
-        } finally {
-            releaseOwnedTexture(maskProbe, "26599 SHORT gate mask probe")
-            releaseOwnedTexture(stageProbe, "26599 SHORT gate stage probe")
-        }
+        draw(program, width, height, intArrayOf(output))
     }
 
     private data class ShortMaskProbe26590(
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2DisplayExposure.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2DisplayExposure.java
index 08f0bb44574fb657b523f0b1b4ca65b310f8eb91..10c49b1a32fec0d2f5ac59453ed1a01a0926c093 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2DisplayExposure.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2DisplayExposure.java
@@ -10,22 +10,6 @@ import com.particlesdevs.photoncamera.util.Log;
  * MotionV2MgcSourceExposure instead. Sabre has no such source-domain pass.
  */
 public final class MotionV2DisplayExposure extends Node {
-    public static final float PRESENTATION_LINEAR_ANCHOR_26599 = 0.18f;
-    public static final float PRESENTATION_KNEE_26599 = 0.18f;
-
-    /** Shared scalar guide map mirrored by 1x GLSL and true-2x CPU/GPU publication. */
-    public static float iris26599MapPresentationGuide(float guide, float displayGain,
-                                                       boolean motionToneNormalization) {
-        final float g = Math.max(guide, 0.0f);
-        final float gain = Math.max(displayGain, 1.0e-6f);
-        if (!motionToneNormalization || gain <= 1.0f || g <= PRESENTATION_LINEAR_ANCHOR_26599) {
-            return g * gain;
-        }
-        final float x = g - PRESENTATION_LINEAR_ANCHOR_26599;
-        return PRESENTATION_LINEAR_ANCHOR_26599 * gain + x
-                + (gain - 1.0f) * PRESENTATION_KNEE_26599
-                * (float)Math.log(1.0f + x / PRESENTATION_KNEE_26599);
-    }
     public MotionV2DisplayExposure() {
         super("", "MotionV2DisplayExposure");
     }
@@ -38,8 +22,6 @@ public final class MotionV2DisplayExposure extends Node {
             throw new IllegalStateException("MotionV2DisplayExposure used outside Motion V2");
         }
         float displayGain = basePipeline.mParameters.motionV2DisplayGain;
-        final boolean motionToneNormalization = basePipeline.mParameters.motionV2Active
-                && !basePipeline.mParameters.irisNightActive;
         if (!Float.isFinite(displayGain) || displayGain <= 0.0f) {
             throw new IllegalStateException("Invalid viewfinder presentation gain: " + displayGain);
         }
@@ -55,9 +37,6 @@ public final class MotionV2DisplayExposure extends Node {
         glProg.useAssetProgram("motionv2/display_exposure");
         glProg.setTexture("InputBuffer", previousNode.WorkingTexture);
         glProg.setVar("displayGain", displayGain);
-        glProg.setVar("presentationLinearAnchor", PRESENTATION_LINEAR_ANCHOR_26599);
-        glProg.setVar("presentationKnee", PRESENTATION_KNEE_26599);
-        glProg.setVar("motionToneNormalization", motionToneNormalization ? 1 : 0);
         WorkingTexture = basePipeline.getMain();
         glProg.drawBlocks(WorkingTexture);
         glProg.closed = true;
@@ -70,10 +49,6 @@ public final class MotionV2DisplayExposure extends Node {
                 + " extremeLift=" + (displayGain >= 4.0f)
                 + " headroomOwner=MotionV2RenderCommonRgbScalar"
                 + " duplicateHeadroomLimiter=false"
-                + " motionToneNormalization=" + motionToneNormalization
-                + " presentationAnchor=" + PRESENTATION_LINEAR_ANCHOR_26599
-                + " presentationKnee=" + PRESENTATION_KNEE_26599
-                + " wholeRgbScalar=true perChannelTone=false"
                 + " camera2Write=false");
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index 77a64d0f9d578746e369cdf27309db21c242cbeb..816f1220841d0d595ba80551cc01d64d66b8ece5 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -154,13 +154,10 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             }
 
             targetLog = medianLogLuma(previewLuma);
-            final boolean motionToneNormalization26599 = !iris26550Night;
-            error0 = exposureError(candidate, 0.0f, targetLog, motionToneNormalization26599);
-            errorMinus = exposureError(candidate, -0.5f, targetLog, motionToneNormalization26599);
-            errorPlus = exposureError(candidate, 0.5f, targetLog, motionToneNormalization26599);
-            rawSolvedEv = solveBounded(
-                    candidate, targetLog, error0, errorMinus, errorPlus,
-                    motionToneNormalization26599);
+            error0 = exposureError(candidate, 0.0f, targetLog);
+            errorMinus = exposureError(candidate, -0.5f, targetLog);
+            errorPlus = exposureError(candidate, 0.5f, targetLog);
+            rawSolvedEv = solveBounded(candidate, targetLog, error0, errorMinus, errorPlus);
             if (iris26550Night) {
                 float targetLinearLuma = (float)Math.pow(2.0, targetLog);
                 float daylightness = smoothstep(NIGHT_DARK_LUMA, NIGHT_FULL_MATCH_LUMA, targetLinearLuma);
@@ -419,7 +416,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             }
             ToneDecision iris26586MeterTone = iris26586ViewfinderMeterToneDecision(1.0f);
             all.sort(Comparator.comparingDouble(s ->
-                    presentedLuma(s, 1.0f, iris26586MeterTone.adaptiveSceneWhite, false)));
+                    presentedLuma(s, 1.0f, iris26586MeterTone.adaptiveSceneWhite)));
             int from = Math.max(0, Math.round((all.size() - 1) * 0.25f));
             int to = Math.min(all.size(), Math.max(from + 1,
                     Math.round((all.size() - 1) * 0.50f) + 1));
@@ -447,33 +444,29 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
         return medianSorted(logs);
     }
 
-    private float exposureError(ArrayList<RgbSample> samples, float ev, float targetLog,
-                                boolean motionToneNormalization26599) {
+    private float exposureError(ArrayList<RgbSample> samples, float ev, float targetLog) {
         float gain = (float)Math.pow(2.0, ev);
         ToneDecision tone = iris26586ViewfinderMeterToneDecision(gain);
         ArrayList<Float> logs = new ArrayList<>(samples.size());
         for (RgbSample sample : samples) {
-            logs.add(log2(Math.max(presentedLuma(
-                    sample, gain, tone.adaptiveSceneWhite, motionToneNormalization26599), 1.0e-5f)));
+            logs.add(log2(Math.max(presentedLuma(sample, gain, tone.adaptiveSceneWhite), 1.0e-5f)));
         }
         Collections.sort(logs);
         return medianSorted(logs) - targetLog;
     }
 
     private float solveBounded(ArrayList<RgbSample> samples, float targetLog,
-                                      float e0, float eMinus, float ePlus,
-                                      boolean motionToneNormalization26599) {
+                                      float e0, float eMinus, float ePlus) {
         float slope = ePlus - eMinus;
         float guess = Math.abs(slope) > 1.0e-4f
                 ? clamp(-e0 / slope, MIN_EV, MAX_EV)
                 : 0.0f;
         float bestEv = guess;
-        float bestErr = Math.abs(exposureError(
-                samples, guess, targetLog, motionToneNormalization26599));
+        float bestErr = Math.abs(exposureError(samples, guess, targetLog));
 
         float lo = MIN_EV, hi = MAX_EV;
-        float flo = exposureError(samples, lo, targetLog, motionToneNormalization26599);
-        float fhi = exposureError(samples, hi, targetLog, motionToneNormalization26599);
+        float flo = exposureError(samples, lo, targetLog);
+        float fhi = exposureError(samples, hi, targetLog);
         if (Math.signum(flo) == Math.signum(fhi)) {
             if (Math.abs(flo) < bestErr) { bestErr = Math.abs(flo); bestEv = lo; }
             if (Math.abs(fhi) < bestErr) bestEv = hi;
@@ -487,7 +480,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                     : 0.5f * (lo + hi);
             float guard = 0.12f * (hi - lo);
             x = clamp(x, lo + guard, hi - guard);
-            float fx = exposureError(samples, x, targetLog, motionToneNormalization26599);
+            float fx = exposureError(samples, x, targetLog);
             if (Math.abs(fx) < bestErr) { bestErr = Math.abs(fx); bestEv = x; }
             if (Math.abs(fx) < 0.01f) break;
             if (Math.signum(fx) == Math.signum(flo)) { lo = x; flo = fx; }
@@ -816,22 +809,10 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
      * neutral-to-white and overflow-to-white solver approximations were stale and were not
      * present in the final renderer.
      */
-    private static float presentedLuma(RgbSample sample, float gain, float sceneWhite,
-                                       boolean motionToneNormalization26599) {
-        float r = Math.max(0.0f, sample.r);
-        float g = Math.max(0.0f, sample.g);
-        float b = Math.max(0.0f, sample.b);
-        float sourceY = luma(r, g, b);
-        float sourcePeak = Math.max(r, Math.max(g, b));
-        float sourceGuide = Math.max(sourceY, sourcePeak);
-        float presentationScale = gain;
-        if (sourceGuide > 1.0e-7f) {
-            presentationScale = MotionV2DisplayExposure.iris26599MapPresentationGuide(
-                    sourceGuide, gain, motionToneNormalization26599) / sourceGuide;
-        }
-        r *= presentationScale;
-        g *= presentationScale;
-        b *= presentationScale;
+    private static float presentedLuma(RgbSample sample, float gain, float sceneWhite) {
+        float r = Math.max(0.0f, sample.r * gain);
+        float g = Math.max(0.0f, sample.g * gain);
+        float b = Math.max(0.0f, sample.b * gain);
         float y = luma(r, g, b);
         float peak = Math.max(r, Math.max(g, b));
         float guide = Math.max(y, peak);
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index e28cee1e466b68a1f67108f20c09a37686dc9758..4d388227d631ebc25da6e79ef3c6070d8f5ad586 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -1,7 +1,5 @@
 package com.particlesdevs.photoncamera.processing.ultrahdr;
 
-import com.particlesdevs.photoncamera.processing.opengl.postpipeline.MotionV2DisplayExposure;
-
 import android.graphics.Bitmap;
 import android.graphics.Canvas;
 import android.graphics.ColorSpace;
@@ -165,8 +163,6 @@ public final class MotionV2Jpeg444Encoder {
                     sensorToProPhoto, profileToDisplay,
                     parameters.motionV2DisplayGain, exposureEv, shadows, contrast,
                     publicationSceneWhite, parameters.motionV2Active,
-                    MotionV2DisplayExposure.PRESENTATION_LINEAR_ANCHOR_26599,
-                    MotionV2DisplayExposure.PRESENTATION_KNEE_26599,
                     watermark,
                     useJin ? jinResidual.residual : null,
                     useJin ? jinResidual.referenceRgb : null,
@@ -316,7 +312,6 @@ public final class MotionV2Jpeg444Encoder {
             int rotation, boolean mirror, float renderResidualZoom,
             float[] sensorToProPhoto, float[] proPhotoToSrgb, float displayGain,
             float exposureEv, float shadows, float contrast, float sceneWhite, boolean motionHdrHandoff,
-            float presentationLinearAnchor, float presentationKnee,
             Bitmap watermark,
             float[] jinResidual, int[] jinReferenceRgb, int jinWidth, int jinHeight,
             String true2xGainmapPath, float true2xGainmapMaxRatio,
diff --git a/app/version.properties b/app/version.properties
index fd5b18dbe5eec080011697be859c29568669dfd1..81e76c2021cc3a514072d84f553099bf8def3226 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726599
-VERSION_BUILD=26599
+VERSION_NAME=0.9726598
+VERSION_BUILD=26598
