diff --git a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
index 8d3e55dbe75a4e2334dfa516c01df29f9b32bfe0..cecffc7cb37591d375a8bc1969c3560d75576f1b 100644
--- a/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
+++ b/app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
@@ -64,6 +64,37 @@ float irisExpand(sampler2D tex,ivec2 p){
     return sum;
 }
 
+/* IRIS_26644_VISUAL_HIGHLIGHT_SOURCE_STRUCTURE_OWNER
+ * 26635 intentionally reduced small residuals in bright fields, but that also attenuated real
+ * pavement/fabric/foliage microstructure when its residual amplitude happened to be small after
+ * reconstruction. Detect only balanced structure physically present in the pre-tone linear source.
+ * Required opposite samples must exist; image borders fail closed instead of repeating edge texels. */
+float iris26644SourceStructure(ivec2 p){
+    ivec2 sz=textureSize(SourceLinear,0);
+    float center=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,p,0).rgb),IRIS_26626_LOG_FLOOR));
+    float evidence=0.0;
+    const int R0=2;
+    const int R1=6;
+    const int R2=16;
+    for(int k=0;k<3;k++){
+        int r=k==0?R0:(k==1?R1:R2);
+        if(p.x-r<0||p.x+r>=sz.x||p.y-r<0||p.y+r>=sz.y) continue;
+        float l=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,p-ivec2(r,0),0).rgb),IRIS_26626_LOG_FLOOR));
+        float rr=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,p+ivec2(r,0),0).rgb),IRIS_26626_LOG_FLOOR));
+        float u=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,p-ivec2(0,r),0).rgb),IRIS_26626_LOG_FLOOR));
+        float d=log2(max(iris26626SourceGuide(texelFetch(SourceLinear,p+ivec2(0,r),0).rgb),IRIS_26626_LOG_FLOOR));
+        float ax0=abs(center-l),ax1=abs(center-rr);
+        float ay0=abs(center-u),ay1=abs(center-d);
+        float axMax=max(ax0,ax1),ayMax=max(ay0,ay1);
+        float balanceX=axMax>0.002?min(ax0,ax1)/axMax:0.0;
+        float balanceY=ayMax>0.002?min(ay0,ay1)/ayMax:0.0;
+        float curvatureX=abs(center-0.5*(l+rr));
+        float curvatureY=abs(center-0.5*(u+d));
+        evidence=max(evidence,max(balanceX*curvatureX,balanceY*curvatureY));
+    }
+    return smoothstep(0.0035,0.020,evidence);
+}
+
 void main(){
     ivec2 p=ivec2(gl_FragCoord.xy);
 
@@ -121,8 +152,13 @@ void main(){
         float bodyScale=exp2(-0.25*bodyStrength*bodyBump);
         float shadowBase=baseOut*bodyScale;
         float structureGate=smoothstep(0.012,0.045,abs(residual));
+        float sourceStructureGate=iris26644SourceStructure(p);
         float smallResidualWeight=mix(1.0,0.78,upperGate);
-        float residualWeight=mix(smallResidualWeight,1.0,structureGate);
+        /* IRIS_26644_VISUAL_HIGHLIGHT_STRUCTURE_PRESERVATION
+         * Keep 26635 suppression only for genuinely smooth bright-field residuals. If the
+         * pre-tone source independently proves real local structure, preserve that residual. */
+        float residualWeight=mix(
+                smallResidualWeight,1.0,max(structureGate,sourceStructureGate));
         float outLinear=clamp(shadowBase+residualWeight*residual,IRIS_26626_LOG_FLOOR,1.0);
         Output=log2(outLinear);
         return;
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 12b59d47b6f04364f83eda344236401d3fcdbfc4..2d77de72a9879ce1180a3dbf2d739c47995a6621 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -718,21 +718,31 @@ internal object GlesMgcRawSabreShaders {
             ivec2 off = phaseOffset(phase);
             return max((uRawSize - off + ivec2(1)) / 2, ivec2(1));
         }
-        ivec2 phaseSamplePixel(int phase, ivec2 gridP) {
-            ivec2 off = phaseOffset(phase);
-            ivec2 size = phaseGridSize(phase);
-            return 2 * clamp(gridP, ivec2(0), size - ivec2(1)) + off;
-        }
-        vec4 samePhaseRawSupport(highp usampler2D t, vec2 rawCenter, int phase) {
+        ivec2 phaseSamplePixelUnchecked(int phase, ivec2 gridP) {
+            return 2 * gridP + phaseOffset(phase);
+        }
+        /* IRIS_26644_SHORT_SOURCE_SUPPORT_FAIL_CLOSED
+         * A SHORT sample owns radiance only when the complete same-CFA bilinear footprint exists
+         * in the physical SHORT image. Never clamp or duplicate an edge texel to manufacture
+         * support: the caller receives false and the complete SHORT observation is rejected. */
+        bool samePhaseRawSupport(
+            highp usampler2D t, vec2 rawCenter, int phase, out vec4 values
+        ) {
             vec2 origin = vec2(phaseOffset(phase)) + vec2(0.5);
             vec2 g = (rawCenter - origin) * 0.5;
             ivec2 g0 = ivec2(floor(g));
-            return vec4(
-                rawAt(t, phaseSamplePixel(phase, g0)),
-                rawAt(t, phaseSamplePixel(phase, g0 + ivec2(1, 0))),
-                rawAt(t, phaseSamplePixel(phase, g0 + ivec2(0, 1))),
-                rawAt(t, phaseSamplePixel(phase, g0 + ivec2(1, 1)))
+            ivec2 size = phaseGridSize(phase);
+            if (g0.x < 0 || g0.y < 0 || g0.x + 1 >= size.x || g0.y + 1 >= size.y) {
+                values = vec4(0.0);
+                return false;
+            }
+            values = vec4(
+                float(texelFetch(t, phaseSamplePixelUnchecked(phase, g0), 0).r),
+                float(texelFetch(t, phaseSamplePixelUnchecked(phase, g0 + ivec2(1, 0)), 0).r),
+                float(texelFetch(t, phaseSamplePixelUnchecked(phase, g0 + ivec2(0, 1)), 0).r),
+                float(texelFetch(t, phaseSamplePixelUnchecked(phase, g0 + ivec2(1, 1)), 0).r)
             );
+            return true;
         }
         float bilinearRaw(vec4 values, vec2 rawCenter, int phase) {
             vec2 origin = vec2(phaseOffset(phase)) + vec2(0.5);
@@ -792,7 +802,8 @@ internal object GlesMgcRawSabreShaders {
             for (int phase = 0; phase < 4; ++phase) {
                 ivec2 rp = referenceQuad + phaseOffset(phase);
                 vec2 shortCenter = vec2(rp) + vec2(0.5) + flowRawPixels;
-                vec4 supportRaw = samePhaseRawSupport(uShortRaw, shortCenter, phase);
+                vec4 supportRaw = vec4(0.0);
+                if (!samePhaseRawSupport(uShortRaw, shortCenter, phase, supportRaw)) return false;
                 phaseSourceConfidence[phase] = sourcePhaseConfidence(peak4(supportRaw));
                 float shortRawCode = bilinearRaw(supportRaw, shortCenter, phase);
                 scaledShort[phase] = normalizedRaw(
@@ -1227,16 +1238,12 @@ internal object GlesMgcRawSabreShaders {
         layout(location = 1) out float oRescueOnlyWeight;
         layout(location = 2) out float oLossCandidate;
 
-        vec2 mirrorUvs(vec2 sampleUv) {
-            if (sampleUv.x <= uFrameBorderPadded.x) sampleUv.x =
-                2.0 * uFrameBorderPadded.x - sampleUv.x;
-            if (sampleUv.y <= uFrameBorderPadded.y) sampleUv.y =
-                2.0 * uFrameBorderPadded.y - sampleUv.y;
-            if (sampleUv.x > uFrameBorderPadded.z) sampleUv.x =
-                2.0 * uFrameBorderPadded.z - sampleUv.x;
-            if (sampleUv.y > uFrameBorderPadded.w) sampleUv.y =
-                2.0 * uFrameBorderPadded.w - sampleUv.y;
-            return sampleUv;
+        /* IRIS_26644_SHORT_WARP_DOMAIN_FAIL_CLOSED
+         * HIGHLIGHT_SHORT is a second physical exposure, not a texture that may be mirrored into
+         * missing geometry. The complete warped sample must stay inside the padded physical source. */
+        bool shortWarpUvValid(vec2 sampleUv) {
+            return sampleUv.x > uFrameBorderPadded.x && sampleUv.y > uFrameBorderPadded.y &&
+                sampleUv.x < uFrameBorderPadded.z && sampleUv.y < uFrameBorderPadded.w;
         }
         float secondHighest4(vec4 v) {
             float first = -1.0;
@@ -1255,16 +1262,26 @@ internal object GlesMgcRawSabreShaders {
         ivec2 clampExtracted(ivec2 p) {
             return clamp(p, ivec2(0), uExtractedSize - ivec2(1));
         }
-        void shortPhaseSupport(vec2 uv, out vec4 interpolatedRaw, out vec4 peakRaw) {
+        /* IRIS_26644_SHORT_EXTRACTED_BILINEAR_FAIL_CLOSED
+         * Never synthesize a complete SHORT footprint by clamping missing bilinear taps to an
+         * image edge. This is the final per-pixel guard before SHORT may enter the common Sabre
+         * accumulator and permanently prevents repeated/grey clipped-border recovery. */
+        bool shortPhaseSupport(vec2 uv, out vec4 interpolatedRaw, out vec4 peakRaw) {
             vec2 position = uv * vec2(uExtractedSize) - vec2(0.5);
             ivec2 p0 = ivec2(floor(position));
+            if (p0.x < 0 || p0.y < 0 || p0.x + 1 >= uExtractedSize.x || p0.y + 1 >= uExtractedSize.y) {
+                interpolatedRaw = vec4(0.0);
+                peakRaw = vec4(uSourceClippingPoint);
+                return false;
+            }
             vec2 f = fract(position);
-            vec4 q00 = texelFetch(uShortExtractedBayer, clampExtracted(p0), 0);
-            vec4 q10 = texelFetch(uShortExtractedBayer, clampExtracted(p0 + ivec2(1, 0)), 0);
-            vec4 q01 = texelFetch(uShortExtractedBayer, clampExtracted(p0 + ivec2(0, 1)), 0);
-            vec4 q11 = texelFetch(uShortExtractedBayer, clampExtracted(p0 + ivec2(1, 1)), 0);
+            vec4 q00 = texelFetch(uShortExtractedBayer, p0, 0);
+            vec4 q10 = texelFetch(uShortExtractedBayer, p0 + ivec2(1, 0), 0);
+            vec4 q01 = texelFetch(uShortExtractedBayer, p0 + ivec2(0, 1), 0);
+            vec4 q11 = texelFetch(uShortExtractedBayer, p0 + ivec2(1, 1), 0);
             interpolatedRaw = mix(mix(q00, q10, f.x), mix(q01, q11, f.x), f.y);
             peakRaw = max(max(q00, q10), max(q01, q11));
+            return true;
         }
         float wholeShortHeadroom(vec4 peakRaw) {
             float peak = max(max(peakRaw.x, peakRaw.y), max(peakRaw.z, peakRaw.w));
@@ -1320,7 +1337,7 @@ internal object GlesMgcRawSabreShaders {
             vec2 referenceUv = gl_FragCoord.xy / vec2(textureSize(uOrdinaryWeight, 0));
             vec2 flowUv = referenceUv * uFlowScaleOffset.xy + uFlowScaleOffset.zw;
             vec4 flow = texture(uFlow, flowUv);
-            vec2 warpedUv = mirrorUvs(referenceUv + flow.xy);
+            vec2 warpedUv = referenceUv + flow.xy;
             float ordinaryWeight = texture(uOrdinaryWeight, referenceUv).r;
             float physicalWeight = texture(uPhysicalWeight, referenceUv).r;
 
@@ -1329,7 +1346,14 @@ internal object GlesMgcRawSabreShaders {
             vec4 referenceRaw = texelFetch(uReferenceExtractedBayer, referenceP, 0);
             vec4 shortRaw = vec4(0.0);
             vec4 shortPeakRaw = vec4(0.0);
-            shortPhaseSupport(warpedUv, shortRaw, shortPeakRaw);
+            bool shortSupportValid = shortWarpUvValid(warpedUv) &&
+                shortPhaseSupport(warpedUv, shortRaw, shortPeakRaw);
+            if (!shortSupportValid) {
+                oWeight = 0.0;
+                oRescueOnlyWeight = 0.0;
+                oLossCandidate = 0.0;
+                return;
+            }
             vec4 referenceNormalized = normalizedQuad(referenceRaw, uReferenceBlackByPhase);
             vec4 scaledShort = normalizedQuad(shortRaw, uShortBlackByPhase) * uExposureRatio;
 
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index ef0fb4ae0320a848721a2524804165d1119cfbf9..05d590eb1765c0282899bf8cb842accdd3971df4 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -734,6 +734,10 @@ public final class MotionV2Render extends Node {
             glProg.useAssetProgram("motionv2/local_laplacian_remap_26621");
             glProg.setTexture("CurrentToneLog", finalTone);
             glProg.setTexture("SourceGuideLog", guide[IRIS_26635_HIGHLIGHT_BASE_LEVEL]);
+            /* IRIS_26644_VISUAL_HIGHLIGHT_SOURCE_STRUCTURE_BINDING
+             * Preserve a small bright residual only when the original pre-tone linear source
+             * independently proves balanced local structure. */
+            glProg.setTexture("SourceLinear", source);
             glProg.setVar("iris26639ShadowBodyStrength",
                     basePipeline.mParameters.motionV2ShadowBodyStrength);
             glProg.setVar("iris26639ShadowBodyEnd",
@@ -807,6 +811,8 @@ public final class MotionV2Render extends Node {
                     + " iris26626PreservationMax=" + IRIS_26626_SOURCE_PRESERVATION_MAX
                     + " iris26626LowerBodyGate=" + IRIS_26623_UPPER_TONE_START + "..0.72"
                     + " iris26626Baseline=EXACT_26625_RECONSTRUCTION"
+                    + " iris26644VisualHighlightSourceStructure=true"
+                    + " iris26644SmoothBrightResidualSuppressionRetained=true"
                     + " iris26639ShadowBodyStrength=" + basePipeline.mParameters.motionV2ShadowBodyStrength
                     + " iris26639ShadowBodyEnd=" + basePipeline.mParameters.motionV2ShadowBodyEnd
                     + " iris26639ShadowMaxCorrectionEv=0.25"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
index 5f7c1aabc712f65d227b925b4924085b007d07ae..d0d5778cb6db54a7ae58683dc15f31d7908f8526 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
@@ -186,10 +186,14 @@ public final class IrisHardwareHevcEncoder {
                     MediaCodecInfo.CodecCapabilities.COLOR_FormatYUV420Flexible);
             format.setInteger(MediaFormat.KEY_I_FRAME_INTERVAL, 0);
             format.setInteger(MediaFormat.KEY_FRAME_RATE, 1);
+            /* IRIS_26644_CPU_I420_HEVC_FULL_RANGE_OWNER
+             * Our CPU-I420 path does not inherit HeicCompositeStream's full-range P010 dataspace
+             * or MPEG4Writer's per-sample range metadata. libheif supplies full-range YUV bytes
+             * and the HEIF NCLX items are full-range, so explicitly request FULL from MediaCodec. */
+            format.setInteger(MediaFormat.KEY_COLOR_RANGE, MediaFormat.COLOR_RANGE_FULL);
             /* IRIS_26643_AOSP_ANDROID16_CODEC_CONFIGURATION_OWNER
-             * HeicCompositeStream configures HEVC without color-aspect keys, then supplies the
-             * base/gain color aspects to MPEG4Writer as item metadata. Do the same here: keep
-             * vendor HEVC coding independent and make the HEIF NCLX items the publication authority. */
+             * Primaries/transfer remain HEIF-NCLX publication authority; only the missing range
+             * transport adaptation is explicit for this CPU-I420 direct-NAL path. */
             MediaCodecInfo.EncoderCapabilities enc = caps.getEncoderCapabilities();
             int q = Math.max(1, Math.min(100, quality));
             if (enc.isBitrateModeSupported(MediaCodecInfo.EncoderCapabilities.BITRATE_MODE_CQ)) {
@@ -253,6 +257,16 @@ public final class IrisHardwareHevcEncoder {
                     Log.i(TAG, "IRIS_26641_HW_HEVC_COLOR_ASPECTS gainMap=" + gainMap
                             + " standard=" + outStandard + " transfer=" + outTransfer
                             + " range=" + outRange);
+                    /* IRIS_26644_HEVC_RANGE_RUNTIME_PROOF
+                     * Never publish full-range NCLX around a hardware stream that explicitly
+                     * reports anything except FULL. Fail the HEIC save instead of silently
+                     * producing the 26643 limited/full contradiction. */
+                    if (outRange != MediaFormat.COLOR_RANGE_FULL) {
+                        throw new IllegalStateException("Hardware HEVC did not honor full-range I420 contract: range="
+                                + outRange + " gainMap=" + gainMap);
+                    }
+                    Log.i(TAG, "IRIS_26644_HEVC_RANGE_PROOF gainMap=" + gainMap
+                            + " requested=FULL output=FULL");
                     for (int i = 0; i < 4; i++) {
                         ByteBuffer csd = of.getByteBuffer("csd-" + i);
                         if (csd == null) continue;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
index 13d2f9e1d84b439e83fde4bd96d8163b4c027624..66c642d383b7efbae2dcc62d5ff3466a753e2875 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
@@ -247,6 +247,7 @@ public class CameraUIViewImpl implements CameraUIView {
                 bottombuttons.flipCameraButton,
                 bottombuttons.shutterButton
         );
+        iris26644StyleManualPalette(root);
         View approvedManualHandle = root.findViewById(R.id.approved_manual_handle);
         TextView approvedManualChevron = root.findViewById(R.id.approved_manual_chevron);
         if (approvedManualHandle != null && approvedManualChevron != null) {
@@ -275,6 +276,36 @@ public class CameraUIViewImpl implements CameraUIView {
         return 1.0f;
     }
 
+
+    /* IRIS_26644_MANUAL_PALETTE_VISUAL_OWNER
+     * Preserve circularbarlib behavior but normalize the inflated camera palette: no translucent
+     * rectangle and the exact app AuxButtonText lens-label typography. Resource-name lookup keeps
+     * repository-only library source outside the compiled-candidate runtime domain. */
+    private void iris26644StyleManualPalette(View root) {
+        if (root == null) return;
+        final String packageName = root.getContext().getPackageName();
+        final int containerId = root.getResources().getIdentifier(
+                "buttons_container", "id", packageName);
+        if (containerId != 0) {
+            View container = root.findViewById(containerId);
+            if (container != null) container.setBackgroundResource(android.R.color.transparent);
+        }
+        View manualMode = root.findViewById(R.id.manual_mode);
+        if (manualMode != null) manualMode.setBackgroundResource(android.R.color.transparent);
+        final String[] labelIds = {
+                "focus_option_tv", "exposure_option_tv", "iso_option_tv", "ev_option_tv"
+        };
+        for (String name : labelIds) {
+            int id = root.getResources().getIdentifier(name, "id", packageName);
+            if (id == 0) continue;
+            View view = root.findViewById(id);
+            if (!(view instanceof TextView)) continue;
+            TextView label = (TextView) view;
+            label.setTextAppearance(R.style.AuxButtonText);
+            label.setShadowLayer(0.0f, 0.0f, 0.0f, 0);
+        }
+    }
+
     private void installPressAnimation(View... views) {
         for (View view : views) {
             if (view == null) continue;
diff --git a/app/version.properties b/app/version.properties
index 21b6886c392329a7aef6557d483823f9aeadf6fb..e33707b45efa5b6084993f50cbda944e7c8a9923 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726643
-VERSION_BUILD=26643
+VERSION_NAME=0.9726644
+VERSION_BUILD=26644
