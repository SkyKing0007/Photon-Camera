diff --git a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_apply_26618.glsl b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_apply_26618.glsl
deleted file mode 100644
index 472fa531eb302ca1434727a452bac19b7e01c771..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_apply_26618.glsl
+++ /dev/null
@@ -1,22 +0,0 @@
-precision highp float;
-precision mediump sampler2D;
-
-uniform sampler2D InputBuffer;
-uniform sampler2D LtmGainBuffer;
-out vec3 Output;
-
-/* IRIS_26618_GUIDED_BASE_DETAIL_LTM_APPLY
- * One common scalar preserves RGB chromaticity.  Genuine extended-HDR samples are
- * faded back to unity so the local operator cannot erase physically reconstructed
- * >1 headroom that 26614 UHDR publication is responsible for carrying.
- */
-void main() {
-    ivec2 imageSize = textureSize(InputBuffer, 0);
-    vec2 uv = gl_FragCoord.xy / vec2(max(imageSize, ivec2(1)));
-    vec3 rgb = max(texture(InputBuffer, uv).rgb, vec3(0.0));
-    float localGain = clamp(texture(LtmGainBuffer, uv).r, 0.50, 1.0);
-    float sourcePeak = max(rgb.r, max(rgb.g, rgb.b));
-    float preserveHdr = smoothstep(0.95, 1.05, sourcePeak);
-    float appliedGain = mix(localGain, 1.0, preserveHdr);
-    Output = rgb * appliedGain;
-}
diff --git a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_base_26619.glsl b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_base_26619.glsl
new file mode 100644
index 0000000000000000000000000000000000000000..efede68b0dc15a7273b187aa6205c5c167e0abad
--- /dev/null
+++ b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_base_26619.glsl
@@ -0,0 +1,44 @@
+precision highp float;
+precision mediump sampler2D;
+
+uniform sampler2D InputBuffer;
+uniform sampler2D CoeffBuffer;
+uniform ivec2 ltmGridSize;
+out vec4 Output;
+
+/*
+ * IRIS_26619_GUIDED_BASE_DETAIL_LTM_BASE
+ *
+ * Reconstruct only the edge-aware broad log-guide base.  This pass deliberately
+ * emits no exposure gain and modifies no image pixel.  MotionV2Render is the sole
+ * consumer/appearance owner; the exact same base samples are exported to true-2x.
+ */
+const vec3 IRIS_LUMA = vec3(0.22897456, 0.69173852, 0.07928691);
+
+float irisLogGuide(vec2 uv) {
+    vec3 rgb = max(texture(InputBuffer, clamp(uv, vec2(0.0), vec2(1.0))).rgb, vec3(0.0));
+    float y = max(dot(rgb, IRIS_LUMA), 0.0);
+    float peak = max(rgb.r, max(rgb.g, rgb.b));
+    return log2(max(max(y, peak), 0.000244140625));
+}
+
+void main() {
+    vec2 gridSize = vec2(max(ltmGridSize, ivec2(1)));
+    vec2 uv = gl_FragCoord.xy / gridSize;
+    ivec2 coeffSize = textureSize(CoeffBuffer, 0);
+    vec2 coeffStep = 1.0 / vec2(max(coeffSize, ivec2(1)));
+
+    vec2 meanAb = vec2(0.0);
+    for (int j = -1; j <= 1; ++j) {
+        for (int i = -1; i <= 1; ++i) {
+            meanAb += texture(CoeffBuffer,
+                    clamp(uv + vec2(float(i), float(j)) * coeffStep,
+                          vec2(0.0), vec2(1.0))).rg;
+        }
+    }
+    meanAb *= 1.0 / 9.0;
+
+    float logGuide = irisLogGuide(uv);
+    float guidedBaseLog = meanAb.x * logGuide + meanAb.y;
+    Output = vec4(guidedBaseLog, 0.0, 0.0, 1.0);
+}
diff --git a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26618.glsl b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26619.glsl
similarity index 62%
rename from app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26618.glsl
rename to app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26619.glsl
index 5b3f66484db7b117d83205a58393b62e6f7b4129..23e1b8c716cb3ae8f373ef72cc7dabb420e1be2e 100644
--- a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26618.glsl
+++ b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_coeff_26619.glsl
@@ -6,21 +6,23 @@ uniform ivec2 ltmGridSize;
 out vec4 Output;
 
 /*
- * IRIS_26618_GUIDED_BASE_DETAIL_LTM_COEFFICIENTS
+ * IRIS_26619_GUIDED_BASE_DETAIL_LTM_COEFFICIENTS
  *
- * Self-guided filter coefficients in log2 luminance.  This is a luminance-only
- * appearance decomposition: no RGB component is mixed with a neighbour and no
- * chroma quantity is modified.  A 7x7 support on the deliberately low-resolution
+ * Self-guided filter coefficients in log2 final-tone guide.  The guide is the exact
+ * max(luma,maxRGB) scalar consumed by MotionV2Render, so the spatial decomposition
+ * cannot disagree with final highlight ownership.  No RGB component is mixed with
+ * a neighbour and no chroma quantity is modified.  A 7x7 support on the deliberately low-resolution
  * coefficient grid captures broad illumination while the guided variance term
  * keeps material/geometry edges out of that broad base.
  */
 const vec3 IRIS_LUMA = vec3(0.22897456, 0.69173852, 0.07928691);
 const float IRIS_GUIDED_EPSILON_STOPS2 = 0.0625; // (0.25 stop)^2
 
-float irisLogLuma(vec2 uv) {
+float irisLogGuide(vec2 uv) {
     vec3 rgb = max(texture(InputBuffer, clamp(uv, vec2(0.0), vec2(1.0))).rgb, vec3(0.0));
-    float y = max(dot(rgb, IRIS_LUMA), 0.000244140625); // 2^-12
-    return log2(y);
+    float y = max(dot(rgb, IRIS_LUMA), 0.0);
+    float peak = max(rgb.r, max(rgb.g, rgb.b));
+    return log2(max(max(y, peak), 0.000244140625)); // 2^-12
 }
 
 void main() {
@@ -32,7 +34,7 @@ void main() {
     float sum2 = 0.0;
     for (int j = -3; j <= 3; ++j) {
         for (int i = -3; i <= 3; ++i) {
-            float v = irisLogLuma(uv + vec2(float(i), float(j)) * stepUv);
+            float v = irisLogGuide(uv + vec2(float(i), float(j)) * stepUv);
             sum += v;
             sum2 += v * v;
         }
diff --git a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_gain_26618.glsl b/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_gain_26618.glsl
deleted file mode 100644
index 623e69b2b1bf649645e36b2cfae4df1a4cb50d17..0000000000000000000000000000000000000000
--- a/app/src/main/assets/shaders/motionv2/guided_base_detail_ltm_gain_26618.glsl
+++ /dev/null
@@ -1,64 +0,0 @@
-precision highp float;
-precision mediump sampler2D;
-
-uniform sampler2D InputBuffer;
-uniform sampler2D CoeffBuffer;
-uniform float displayGain;
-uniform ivec2 ltmGridSize;
-out vec4 Output;
-
-/*
- * IRIS_26618_GUIDED_BASE_DETAIL_LTM_GAIN
- *
- * Reconstruct the low-frequency guided log-luminance base from smoothed (a,b)
- * coefficients, then allocate only the already-requested 26614 presentation lift.
- * Dark broad regions retain the full global target; broad regions that would be
- * driven near display white receive a bounded pre-tone attenuation.  Because the
- * scalar depends on the guided base rather than the local detail residual, the
- * original log-luminance detail is recombined unchanged.
- */
-const vec3 IRIS_LUMA = vec3(0.22897456, 0.69173852, 0.07928691);
-const float IRIS_OUTPUT_EXPOSURE_SCALE = 0.80;
-
-float irisLogLuma(vec2 uv) {
-    vec3 rgb = max(texture(InputBuffer, clamp(uv, vec2(0.0), vec2(1.0))).rgb, vec3(0.0));
-    return log2(max(dot(rgb, IRIS_LUMA), 0.000244140625));
-}
-
-void main() {
-    vec2 gridSize = vec2(max(ltmGridSize, ivec2(1)));
-    vec2 uv = gl_FragCoord.xy / gridSize;
-    ivec2 coeffSize = textureSize(CoeffBuffer, 0);
-    vec2 coeffStep = 1.0 / vec2(max(coeffSize, ivec2(1)));
-
-    vec2 meanAb = vec2(0.0);
-    for (int j = -1; j <= 1; ++j) {
-        for (int i = -1; i <= 1; ++i) {
-            meanAb += texture(CoeffBuffer,
-                    clamp(uv + vec2(float(i), float(j)) * coeffStep,
-                          vec2(0.0), vec2(1.0))).rg;
-        }
-    }
-    meanAb *= 1.0 / 9.0;
-
-    float logY = irisLogLuma(uv);
-    float guidedBaseLog = meanAb.x * logY + meanAb.y;
-    float guidedBase = exp2(guidedBaseLog);
-
-    float requestedFinalGain = max(displayGain * IRIS_OUTPUT_EXPOSURE_SCALE, 1.0e-6);
-    float requestedEv = max(log2(requestedFinalGain), 0.0);
-    float projectedBase = guidedBase * requestedFinalGain;
-
-    /* Universal, non-semantic allocation.  The operator is deliberately inactive
-     * for small/no presentation lifts, then smoothly reserves up to half of the
-     * requested EV in broad regions whose projected base approaches nominal white. */
-    float requestPressure = smoothstep(0.35, 1.50, requestedEv);
-    float brightBasePressure = smoothstep(0.42, 0.95, projectedBase);
-    float protectEv = min(1.0, 0.50 * requestedEv)
-            * requestPressure * brightBasePressure;
-    float gain = exp2(-protectEv);
-
-    /* Bound the scalar so this stage cannot become a replacement global tone curve. */
-    gain = clamp(gain, 0.50, 1.0);
-    Output = vec4(gain, guidedBaseLog, requestedEv, 1.0);
-}
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index b9578215a51538ebcfcbdf5ca01f8631135b1053..61f5ba4bfd60e676f9b75762d0e5bd71b8ad7936 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -2,6 +2,8 @@ precision highp float;
 precision mediump sampler2D;
 
 uniform sampler2D InputBuffer;
+uniform sampler2D GuidedBaseBuffer;
+uniform int iris26619GuidedBaseEnabled;
 uniform float sceneWhite;
 uniform float outputExposureScale;
 uniform float irisOutputZoom;
@@ -119,6 +121,47 @@ float iris26614MapMotionSdrFinalGuide(float sourceGuide) {
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
 
+/* IRIS_26619_MONOTONIC_GUIDED_BASE_MAP
+ * The viewfinder target remains the low-guide slope.  Broad source white is reserved below final
+ * white so measured local detail and compact highlights have display range above the illumination
+ * field.  The rational branch is strictly increasing; the >1 tail matches its derivative at
+ * source white, making the complete broad-base map C1 and strictly monotonic.
+ */
+const float IRIS_26619_BROAD_BASE_WHITE = 0.86;
+const float IRIS_26619_DETAIL_LIMIT_STOPS = 2.0;
+
+float iris26619MapMotionBroadBase(float sourceBase) {
+    float x=max(sourceBase,0.0);
+    float requestedFinalGain=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
+    float globalMapped=iris26614MapMotionSdrFinalGuide(x);
+    /* Fade the spatial allocation in as a GLOBAL capture-level condition.  This is not a
+     * source-luminance threshold, so it cannot draw a ring across a smooth spatial gradient. */
+    float spatialStrength=smoothstep(1.05,1.80,requestedFinalGain);
+    if(spatialStrength<=0.0)return globalMapped;
+    float broadWhite=min(IRIS_26619_BROAD_BASE_WHITE,requestedFinalGain);
+    float bodyGain=min(requestedFinalGain,4.0*broadWhite-1.0e-4);
+    float spatialMapped;
+    if(bodyGain<=broadWhite+1.0e-6) {
+        spatialMapped=bodyGain*x;
+    } else if(x<=1.0) {
+        float c=bodyGain/max(broadWhite,1.0e-6)-1.0;
+        spatialMapped=bodyGain*x/(1.0+c*x);
+    } else {
+        float leftSlope=broadWhite*broadWhite/bodyGain;
+        float reserve=max(1.0-broadWhite,1.0e-6);
+        float tailScale=reserve/max(leftSlope,1.0e-6);
+        float excess=x-1.0;
+        spatialMapped=broadWhite+reserve*excess/(excess+tailScale);
+    }
+    return mix(globalMapped,spatialMapped,spatialStrength);
+}
+
+float iris26619GuidedBaseLogAt(vec2 sourcePixel, ivec2 sourceSize) {
+    vec2 denom=max(vec2(sourceSize),vec2(1.0));
+    vec2 uv=(sourcePixel+vec2(0.5))/denom;
+    return texture(GuidedBaseBuffer,clamp(uv,vec2(0.0),vec2(1.0))).r;
+}
+
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
         return iris26614MapMotionSdrFinalGuide(sourceGuide);
@@ -137,34 +180,33 @@ float mapFinalSdrGuide(float sourceGuide) {
     return mappedPre*outputExposureScale;
 }
 
-vec3 mapExtendedLinearHeadroom(vec3 rgb) {
+vec3 mapExtendedLinearHeadroom(vec3 rgb, vec2 sourcePixel, ivec2 sourceSize) {
     rgb=max(rgb,vec3(0.0));
     float y=max(luminance(rgb),0.0);
     float peak=max3(rgb);
     float guide=max(y,peak);
     if(guide<=1.0e-7) return rgb;
 
-    /* IRIS_26480_MAX_RGB_HIGHLIGHT_TONE_GUIDE_V2
-     * Saturated channels participate in the shoulder decision directly.
-     * Uniform scaling preserves hue/channel ratios.
-     */
-    /* IRIS_26491_EXTENDED_LINEAR_CHROMA_PRESERVING_HIGHLIGHT_COMPRESSION
-     * Scene exposure arrived in extended-linear RGB from MotionV2DisplayExposure.
-     * Compression is one scalar derived from the max-RGB/luma guide, so values
-     * keep channel ratios until the final display-gamut fit. No per-channel 1.0
-     * clamp is introduced here.
-     */
     float mappedGuide=mapFinalSdrGuide(guide);
-    vec3 mapped=rgb*(mappedGuide/guide);
-
-    /* IRIS_26503_UPSTREAM_EXHAUSTION_OWNS_WHITE
-     * 26502 already knows whether physical Bayer/Short-A evidence is genuinely
-     * exhausted.  Do not create a second visible white-convergence decision from
-     * render headroom position.  Uniform scalar compression preserves the hue of
-     * recoverable warm walls/lamps; truly exhausted upstream-neutral pixels remain
-     * neutral because equal channels stay equal under the same scalar.
-     */
-    return mapped;
+    if(iris26592MotionHdrHandoff!=0 && iris26619GuidedBaseEnabled!=0) {
+        /* IRIS_26619_TRUE_BASE_DETAIL_RECOMBINATION
+         * 26618 multiplied linear RGB by a rapidly varying pre-tone gain and then passed that
+         * result through the nonlinear 26614 map.  The composition became non-monotonic and its
+         * separate 0.95->1.05 release created the observed smooth-gradient contour failure.
+         *
+         * 26619 instead maps only the edge-aware broad tone-guide base with a C1 monotonic curve,
+         * then restores the measured local log-guide residual.  Detail strength is exactly 1.0; the +/-2-stop guard is only a pathological
+         * halo/numerical bound and never increases the measured residual.
+         */
+        float baseLog=iris26619GuidedBaseLogAt(sourcePixel,sourceSize);
+        float baseGuide=exp2(baseLog);
+        float detailStops=clamp(log2(max(guide,0.000244140625))-baseLog,
+                                -IRIS_26619_DETAIL_LIMIT_STOPS,
+                                 IRIS_26619_DETAIL_LIMIT_STOPS);
+        float mappedBase=iris26619MapMotionBroadBase(baseGuide);
+        mappedGuide=max(mappedBase*exp2(detailStops),0.0);
+    }
+    return rgb*(mappedGuide/guide);
 }
 
 /*
@@ -215,7 +257,7 @@ void main() {
      * Do not apply the legacy 5x5 local-luma contrast multiplier here. Sabre remains
      * the detail authority; headroom mapping, exposure, gamut fit and zoom are unchanged.
      */
-    linearSrgb=mapExtendedLinearHeadroom(linearSrgb);
+    linearSrgb=mapExtendedLinearHeadroom(linearSrgb,vec2(sourceXY),sourceSize);
 
     /* IRIS_26604_FINAL_DOMAIN_TONE
      * outputExposureScale and Motion brightness target are already consumed by mapFinalSdrGuide;
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 1217543185e633a194f485b3f8c4b9297984797c..ddf58d9069bd1efd5bd76b5ade1d0a080621c9bc 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -507,21 +507,21 @@ struct Params {
     float sensorToProfile[9]{};
     float profileToDisplay[9]{};
     int guidedLtmW=0,guidedLtmH=0;
-    std::vector<float> guidedLtmGain;
-    bool guidedLtmEnabled()const{return guidedLtmW>1&&guidedLtmH>1&&guidedLtmGain.size()==(size_t)guidedLtmW*guidedLtmH;}
+    std::vector<float> guidedBaseLog;
+    bool guidedLtmEnabled()const{return guidedLtmW>1&&guidedLtmH>1&&guidedBaseLog.size()==(size_t)guidedLtmW*guidedLtmH;}
 };
-inline float iris26618GuidedLtmGainAt(const Params&p,float trueX,float trueY){
-    if(!p.motionHdrHandoff||!p.guidedLtmEnabled())return 1.f;
+inline float iris26619GuidedBaseLogAt(const Params&p,float trueX,float trueY){
+    if(!p.motionHdrHandoff||!p.guidedLtmEnabled())return 0.f;
     float nx=p.trueW>1?clampf(trueX/(float)(p.trueW-1),0.f,1.f):0.f;
     float ny=p.trueH>1?clampf(trueY/(float)(p.trueH-1),0.f,1.f):0.f;
     float mx=nx*(float)(p.guidedLtmW-1),my=ny*(float)(p.guidedLtmH-1);
     int x0=(int)std::floor(mx),y0=(int)std::floor(my);
     int x1=std::min(x0+1,p.guidedLtmW-1),y1=std::min(y0+1,p.guidedLtmH-1);
     float fx=mx-x0,fy=my-y0;
-    auto at=[&](int x,int y){return p.guidedLtmGain[(size_t)y*p.guidedLtmW+x];};
+    auto at=[&](int x,int y){return p.guidedBaseLog[(size_t)y*p.guidedLtmW+x];};
     float a=at(x0,y0)+(at(x1,y0)-at(x0,y0))*fx;
     float b=at(x0,y1)+(at(x1,y1)-at(x0,y1))*fx;
-    return clampf(a+(b-a)*fy,0.50f,1.f);
+    return a+(b-a)*fy;
 }
 inline Vec3 mat(const float*m,Vec3 v){
     return {m[0]*v.r+m[1]*v.g+m[2]*v.b,
@@ -556,7 +556,7 @@ inline Vec3 tone(Vec3 rgb,const Params&p){
     if(virtualGain!=1.f)rgb=mul(rgb,1.f/virtualGain);
     return clampNonnegative(rgb);
 }
-inline bool preRenderAt(const SourceRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearance(s,p,x,y,&c))return false;if(p.motionHdrHandoff){float g=iris26618GuidedLtmGainAt(p,(float)x,(float)y);float preserve=smoothstep(0.95f,1.05f,peak(c));g=g+(1.f-g)*preserve;c=mul(c,g);}else c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
+inline bool preRenderAt(const SourceRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearance(s,p,x,y,&c))return false;if(!p.motionHdrHandoff)c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
 inline bool samplePreRender(const SourceRegion&s,const Params&p,float x,float y,Vec3*out){
     x=clampf(x,0.f,(float)(p.trueW-1));y=clampf(y,0.f,(float)(p.trueH-1));
     if(p.residualZoom<=1.00001f){int ix=(int)std::lround(x),iy=(int)std::lround(y);return preRenderAt(s,p,ix,iy,out);}
@@ -601,7 +601,7 @@ inline bool adaptiveAppearanceCached(const ProfileRegion&s,const Params&p,int x,
     if(inPeak>=1.f||projectedPeak>=1.f)limit=1.f;else{limit=std::min(limit,componentGainLimit(cy,cc.r));limit=std::min(limit,componentGainLimit(cy,cc.g));limit=std::min(limit,componentGainLimit(cy,cc.b));}
     float g=clampf(std::min(requested,limit),1.f,1.22f);*out=g<=1.000001f?center:add(Vec3{cy,cy,cy},mul(cc,g));return true;
 }
-inline bool preRenderAtCached(const ProfileRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearanceCached(s,p,x,y,&c))return false;if(p.motionHdrHandoff){float g=iris26618GuidedLtmGainAt(p,(float)x,(float)y);float preserve=smoothstep(0.95f,1.05f,peak(c));g=g+(1.f-g)*preserve;c=mul(c,g);}else c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
+inline bool preRenderAtCached(const ProfileRegion&s,const Params&p,int x,int y,Vec3*out){Vec3 c;if(!adaptiveAppearanceCached(s,p,x,y,&c))return false;if(!p.motionHdrHandoff)c=mul(c,std::max(p.displayGain,1.0e-6f));*out=tone(c,p);return true;}
 inline bool samplePreRenderCached(const ProfileRegion&s,const Params&p,float x,float y,Vec3*out){
     x=clampf(x,0.f,(float)(p.trueW-1));y=clampf(y,0.f,(float)(p.trueH-1));
     if(p.residualZoom<=1.00001f){int ix=(int)std::lround(x),iy=(int)std::lround(y);return preRenderAtCached(s,p,ix,iy,out);}
@@ -622,11 +622,30 @@ inline float iris26614MapMotionSdrFinalGuide(float sourceGuide,float displayGain
     float tailScale=reserve/std::max(whiteAnchor,1.0e-6f),excess=x-1.f;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-inline Vec3 renderHeadroom(Vec3 rgb,const Params&p){
+inline float iris26619MapMotionBroadBase(float sourceBase,float displayGain){
+    constexpr float scale=0.80f,broadAnchor=0.86f;
+    float x=std::max(sourceBase,0.f),requested=std::max(displayGain,1.0e-6f)*scale;
+    float globalMapped=iris26614MapMotionSdrFinalGuide(x,displayGain);
+    float spatialStrength=smooth01((requested-1.05f)/(1.80f-1.05f));
+    if(spatialStrength<=0.f)return globalMapped;
+    float broadWhite=std::min(broadAnchor,requested);
+    float bodyGain=std::min(requested,4.f*broadWhite-1.0e-4f),spatialMapped=0.f;
+    if(bodyGain<=broadWhite+1.0e-6f)spatialMapped=bodyGain*x;
+    else if(x<=1.f){float c=bodyGain/std::max(broadWhite,1.0e-6f)-1.f;spatialMapped=bodyGain*x/(1.f+c*x);}
+    else{float leftSlope=broadWhite*broadWhite/bodyGain,reserve=std::max(1.f-broadWhite,1.0e-6f);float tailScale=reserve/std::max(leftSlope,1.0e-6f),excess=x-1.f;spatialMapped=broadWhite+reserve*excess/(excess+tailScale);}
+    return globalMapped+(spatialMapped-globalMapped)*spatialStrength;
+}
+inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float trueX,float trueY){
     rgb=clampNonnegative(rgb);float y=std::max(luma(rgb),0.f),pk=peak(rgb),guide=std::max(y,pk);
-    /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_CPU */
+    /* IRIS_26619_TRUE2X_GUIDED_BASE_DETAIL_CPU */
     if(p.motionHdrHandoff){
         float mappedFinal=iris26614MapMotionSdrFinalGuide(guide,p.displayGain);
+        if(p.guidedLtmEnabled()&&guide>1.0e-7f){
+            float baseLog=iris26619GuidedBaseLogAt(p,trueX,trueY);
+            float baseGuide=std::exp2(baseLog);
+            float detailStops=clampf(std::log2(std::max(guide,0.000244140625f))-baseLog,-2.f,2.f);
+            mappedFinal=iris26619MapMotionBroadBase(baseGuide,p.displayGain)*std::exp2(detailStops);
+        }
         if(guide>1.0e-7f)rgb=mul(rgb,mappedFinal/guide);
     }else{
         const float start=0.50f;
@@ -683,13 +702,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
     crx*=tw/th;crx/=((float)w.w/(float)w.h);crx*=1.025f;crx*=15.f;cry=(cry+1.f/15.f)*15.f;if(crx<0.f||cry<0.f)return false;*u=crx;*v=cry;return true;
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p,sx,sy);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
-    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
+    float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p,sx,sy);
     if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
@@ -773,8 +792,8 @@ layout(local_size_x=16,local_size_y=8,local_size_z=1) in;
 layout(binding=0) uniform highp sampler2D uSource;
 layout(rgba8ui,binding=0) writeonly uniform highp uimage2D uOutput;
 layout(std430,binding=1) readonly buffer IrisGammaLut { float gammaLut[]; };
-/* IRIS_26618_TRUE2X_SHARED_GUIDED_LTM_GPU */
-layout(std430,binding=2) readonly buffer IrisGuidedLtmGain { float guidedLtmGain[]; };
+/* IRIS_26619_TRUE2X_SHARED_GUIDED_BASE_DETAIL_GPU */
+layout(std430,binding=2) readonly buffer IrisGuidedBaseLog { float guidedBaseLog[]; };
 
 uniform ivec2 uSrcOrigin;
 uniform ivec2 uSrcSize;
@@ -864,8 +883,8 @@ vec3 irisAdaptive(ivec2 p){
     float g=clamp(min(requested,limitV),1.0,1.22);
     return g<=1.000001?center:vec3(cy)+cc*g;
 }
-float iris26618GuidedLtmGainAt(vec2 truePos){
-    if(uGuidedLtmEnabled==0||uGuidedLtmSize.x<=1||uGuidedLtmSize.y<=1)return 1.0;
+float iris26619GuidedBaseLogAt(vec2 truePos){
+    if(uGuidedLtmEnabled==0||uGuidedLtmSize.x<=1||uGuidedLtmSize.y<=1)return 0.0;
     vec2 denom=max(vec2(uTrueSize-ivec2(1)),vec2(1.0));
     vec2 n=clamp(truePos/denom,vec2(0.0),vec2(1.0));
     vec2 mp=n*vec2(uGuidedLtmSize-ivec2(1));
@@ -876,9 +895,9 @@ float iris26618GuidedLtmGainAt(vec2 truePos){
     int i10=p0.y*uGuidedLtmSize.x+p1.x;
     int i01=p1.y*uGuidedLtmSize.x+p0.x;
     int i11=p1.y*uGuidedLtmSize.x+p1.x;
-    float a=mix(guidedLtmGain[i00],guidedLtmGain[i10],f.x);
-    float b=mix(guidedLtmGain[i01],guidedLtmGain[i11],f.x);
-    return clamp(mix(a,b,f.y),0.50,1.0);
+    float a=mix(guidedBaseLog[i00],guidedBaseLog[i10],f.x);
+    float b=mix(guidedBaseLog[i01],guidedBaseLog[i11],f.x);
+    return mix(a,b,f.y);
 }
 vec3 irisTone(vec3 rgb){
     bool manualActive=abs(uExposureEv)>=0.0001||abs(uShadows)>=0.0001||abs(uContrast)>=0.0001;
@@ -902,7 +921,7 @@ vec3 irisTone(vec3 rgb){
     if(virtualGain!=1.0)rgb/=virtualGain;
     return irisClampNonnegative(rgb);
 }
-vec3 irisPreRenderAt(ivec2 p){vec3 c=irisAdaptive(p);if(uMotionHdrHandoff!=0){float g=iris26618GuidedLtmGainAt(vec2(p));float preserve=smoothstep(0.95,1.05,irisPeak(c));g=mix(g,1.0,preserve);c*=g;}else c*=max(uDisplayGain,1.0e-6);return irisTone(c);}
+vec3 irisPreRenderAt(ivec2 p){vec3 c=irisAdaptive(p);if(uMotionHdrHandoff==0)c*=max(uDisplayGain,1.0e-6);return irisTone(c);}
 vec3 irisSamplePreRender(vec2 p){
     p=clamp(p,vec2(0.0),vec2(uTrueSize-ivec2(1)));
     if(uResidualZoom<=1.00001)return irisPreRenderAt(ivec2(floor(p+vec2(0.5))));
@@ -940,12 +959,31 @@ float iris26614MapMotionSdrFinalGuide(float sourceGuide){
     float tailScale=reserve/max(whiteAnchor,1.0e-6),excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
-vec3 irisHeadroom(vec3 rgb){
+float iris26619MapMotionBroadBase(float sourceBase){
+    const float scale=0.80,broadAnchor=0.86;
+    float x=max(sourceBase,0.0),requested=max(uDisplayGain,1.0e-6)*scale;
+    float globalMapped=iris26614MapMotionSdrFinalGuide(x);
+    float spatialStrength=smoothstep(1.05,1.80,requested);
+    if(spatialStrength<=0.0)return globalMapped;
+    float broadWhite=min(broadAnchor,requested);
+    float bodyGain=min(requested,4.0*broadWhite-1.0e-4),spatialMapped=0.0;
+    if(bodyGain<=broadWhite+1.0e-6)spatialMapped=bodyGain*x;
+    else if(x<=1.0){float c=bodyGain/max(broadWhite,1.0e-6)-1.0;spatialMapped=bodyGain*x/(1.0+c*x);}
+    else{float leftSlope=broadWhite*broadWhite/bodyGain,reserve=max(1.0-broadWhite,1.0e-6);float tailScale=reserve/max(leftSlope,1.0e-6),excess=x-1.0;spatialMapped=broadWhite+reserve*excess/(excess+tailScale);}
+    return mix(globalMapped,spatialMapped,spatialStrength);
+}
+vec3 irisHeadroom(vec3 rgb,vec2 truePos){
     rgb=irisClampNonnegative(rgb);
     float y=max(irisLuma(rgb),0.0),pk=irisPeak(rgb),guide=max(y,pk);
     /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_GPU */
     if(uMotionHdrHandoff!=0){
         float mappedFinal=iris26614MapMotionSdrFinalGuide(guide);
+        if(uGuidedLtmEnabled!=0&&guide>1.0e-7){
+            float baseLog=iris26619GuidedBaseLogAt(truePos);
+            float baseGuide=exp2(baseLog);
+            float detailStops=clamp(log2(max(guide,0.000244140625))-baseLog,-2.0,2.0);
+            mappedFinal=iris26619MapMotionBroadBase(baseGuide)*exp2(detailStops);
+        }
         if(guide>1.0e-7)rgb*=mappedFinal/guide;
     }else{
         float start=0.50;
@@ -964,8 +1002,9 @@ void main(){
     ivec2 local=ivec2(gl_GlobalInvocationID.xy);
     if(local.x>=uOutputSize.x||local.y>=uBandH)return;
     ivec2 op=ivec2(local.x,uBandTop+local.y);
-    vec3 hdr=irisSamplePreRender(irisOutputToSource(op));
-    vec3 sdr=irisHeadroom(hdr);
+    vec2 truePos=irisOutputToSource(op);
+    vec3 hdr=irisSamplePreRender(truePos);
+    vec3 sdr=irisHeadroom(hdr,truePos);
     uint gainCode=0u;
     if(uGenerateGain!=0){
         const float off=0.015625;float safeMax=max(uGainMax,1.001);
@@ -1019,8 +1058,8 @@ public:
         glGenFramebuffers(1,&fbo);glBindFramebuffer(GL_FRAMEBUFFER,fbo);glFramebufferTexture2D(GL_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_TEXTURE_2D,outputTex,0);if(glCheckFramebufferStatus(GL_FRAMEBUFFER)!=GL_FRAMEBUFFER_COMPLETE){set(reason,"RGBA8UI framebuffer");return false;}
         glGenBuffers(1,&lutSsbo);glBindBuffer(GL_SHADER_STORAGE_BUFFER,lutSsbo);glBufferData(GL_SHADER_STORAGE_BUFFER,(GLsizeiptr)(lut.v.size()*sizeof(float)),lut.v.data(),GL_STATIC_DRAW);
         glGenBuffers(1,&guidedLtmSsbo);glBindBuffer(GL_SHADER_STORAGE_BUFFER,guidedLtmSsbo);
-        const float unityLtm=1.f;const float*ltmData=p.guidedLtmEnabled()?p.guidedLtmGain.data():&unityLtm;
-        size_t ltmCount=p.guidedLtmEnabled()?p.guidedLtmGain.size():1u;
+        const float zeroBaseLog=0.f;const float*ltmData=p.guidedLtmEnabled()?p.guidedBaseLog.data():&zeroBaseLog;
+        size_t ltmCount=p.guidedLtmEnabled()?p.guidedBaseLog.size():1u;
         glBufferData(GL_SHADER_STORAGE_BUFFER,(GLsizeiptr)(ltmCount*sizeof(float)),ltmData,GL_STATIC_DRAW);
         const size_t maxBytes=(size_t)outWidth*maxBandRows*4;glGenBuffers(2,pbo);for(int i=0;i<2;i++){glBindBuffer(GL_PIXEL_PACK_BUFFER,pbo[i]);glBufferData(GL_PIXEL_PACK_BUFFER,(GLsizeiptr)maxBytes,nullptr,GL_STREAM_READ);}
         glBindBuffer(GL_PIXEL_PACK_BUFFER,0);GLenum err=glGetError();if(err!=GL_NO_ERROR){set(reason,"GL allocation");return false;}
@@ -1144,14 +1183,14 @@ extern "C" JNIEXPORT jstring JNICALL Java_com_particlesdevs_photoncamera_process
 extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_processing_ultrahdr_MotionV2Jpeg444Encoder_writeTrue2xNative(
         JNIEnv*e,jclass,jobject bitmap,jstring renderPath,jint trueW,jint trueH,jint rawW,jint rawH,jint cropW,jint cropH,jint rotation,jboolean mirror,jfloat residualZoom,
         jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat sceneWhite,jboolean motionHdrHandoff,
-        jfloatArray guidedLtmGain,jint guidedLtmW,jint guidedLtmH,
+        jfloatArray guidedBaseLog,jint guidedLtmW,jint guidedLtmH,
         jobject watermarkBitmap,
         jfloatArray jinResidual,jintArray jinReference,jint jinW,jint jinH,jstring gainPath,jfloat gainMaxRatio,jfloat gainContentMaxRatio,jstring path,jint quality){
     using namespace iris26564render;gIris26576PublicationTelemetry="backend=UNSET callStarted=true";AndroidBitmapInfo info{};if(!bitmap||!renderPath||!path||trueW<=0||trueH<=0||rawW<=0||rawH<=0||cropW<=0||cropH<=0||trueW!=rawW*2||trueH!=rawH*2||cropW>rawW||cropH>rawH||AndroidBitmap_getInfo(e,bitmap,&info)!=ANDROID_BITMAP_RESULT_SUCCESS||info.format!=ANDROID_BITMAP_FORMAT_RGBA_8888){iris26576SetPublicationTelemetry("backend=NONE validationFailed=true");return JNI_FALSE;}
     int expectedNativeW=(rotation==90||rotation==270)?cropH:rawW,expectedNativeH=(rotation==90||rotation==270)?rawW:cropH;if((int)info.width!=expectedNativeW||(int)info.height!=expectedNativeH)return JNI_FALSE;
     Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
     if(!sensorToProfile||!profileToDisplay||e->GetArrayLength(sensorToProfile)!=9||e->GetArrayLength(profileToDisplay)!=9)return JNI_FALSE;e->GetFloatArrayRegion(sensorToProfile,0,9,p.sensorToProfile);e->GetFloatArrayRegion(profileToDisplay,0,9,p.profileToDisplay);if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.sensorToProfile)if(!std::isfinite(v))return JNI_FALSE;for(float v:p.profileToDisplay)if(!std::isfinite(v))return JNI_FALSE;if(!std::isfinite(p.displayGain)||p.displayGain<=0.f||!std::isfinite(p.exposureEv)||!std::isfinite(p.shadows)||!std::isfinite(p.contrast)||!std::isfinite(p.residualZoom)||!std::isfinite(p.sceneWhite)||p.sceneWhite<=0.f)return JNI_FALSE;
-    if(guidedLtmGain||guidedLtmW||guidedLtmH){if(!guidedLtmGain||guidedLtmW<=1||guidedLtmH<=1||e->GetArrayLength(guidedLtmGain)!=(jsize)((jlong)guidedLtmW*guidedLtmH))return JNI_FALSE;p.guidedLtmW=guidedLtmW;p.guidedLtmH=guidedLtmH;p.guidedLtmGain.resize((size_t)guidedLtmW*guidedLtmH);e->GetFloatArrayRegion(guidedLtmGain,0,(jsize)p.guidedLtmGain.size(),p.guidedLtmGain.data());if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.guidedLtmGain)if(!std::isfinite(v)||v<0.499f||v>1.001f)return JNI_FALSE;}
+    if(guidedBaseLog||guidedLtmW||guidedLtmH){if(!guidedBaseLog||guidedLtmW<=1||guidedLtmH<=1||e->GetArrayLength(guidedBaseLog)!=(jsize)((jlong)guidedLtmW*guidedLtmH))return JNI_FALSE;p.guidedLtmW=guidedLtmW;p.guidedLtmH=guidedLtmH;p.guidedBaseLog.resize((size_t)guidedLtmW*guidedLtmH);e->GetFloatArrayRegion(guidedBaseLog,0,(jsize)p.guidedBaseLog.size(),p.guidedBaseLog.data());if(e->ExceptionCheck())return JNI_FALSE;for(float v:p.guidedBaseLog)if(!std::isfinite(v)||v<(-16.f)||v>8.f)return JNI_FALSE;}
     U rp(e,renderPath),gp(e,gainPath),op(e,path);if(!rp.c||!op.c)return JNI_FALSE;const bool generateGain=gp.c!=nullptr;if(generateGain&&(!std::isfinite((float)gainMaxRatio)||gainMaxRatio<=1.f||!std::isfinite((float)gainContentMaxRatio)||gainContentMaxRatio<1.f))return JNI_FALSE;const float gainContentMax=generateGain?clampf((float)gainContentMaxRatio,1.f,(float)gainMaxRatio):1.f;int sourceFd=open(rp.c,O_RDONLY);if(sourceFd<0)return JNI_FALSE;struct stat st{};uint64_t expected=(uint64_t)trueW*(uint64_t)trueH*6ull;if(fstat(sourceFd,&st)!=0||(uint64_t)st.st_size!=expected){close(sourceFd);return JNI_FALSE;}
     Watermark water{};if(watermarkBitmap){AndroidBitmapInfo wi{};if(AndroidBitmap_getInfo(e,watermarkBitmap,&wi)!=ANDROID_BITMAP_RESULT_SUCCESS||wi.format!=ANDROID_BITMAP_FORMAT_RGBA_8888||wi.width==0||wi.height==0){close(sourceFd);return JNI_FALSE;}void*wp=nullptr;if(AndroidBitmap_lockPixels(e,watermarkBitmap,&wp)!=ANDROID_BITMAP_RESULT_SUCCESS||!wp){close(sourceFd);return JNI_FALSE;}water.w=(int)wi.width;water.h=(int)wi.height;water.rgba.resize((size_t)water.w*water.h*4);for(int y=0;y<water.h;y++)std::memcpy(water.rgba.data()+(size_t)y*water.w*4,(const uint8_t*)wp+(size_t)y*wi.stride,(size_t)water.w*4);AndroidBitmap_unlockPixels(e,watermarkBitmap);}
     Jin jin{};if(jinResidual||jinReference||jinW||jinH){if(!jinResidual||!jinReference||jinW<=1||jinH<=1||e->GetArrayLength(jinResidual)!=(jsize)((jlong)jinW*jinH*3)||e->GetArrayLength(jinReference)!=(jsize)((jlong)jinW*jinH)){close(sourceFd);return JNI_FALSE;}jin.w=jinW;jin.h=jinH;jin.residual.resize((size_t)jinW*jinH*3);jin.guide.resize((size_t)jinW*jinH);e->GetFloatArrayRegion(jinResidual,0,(jsize)jin.residual.size(),jin.residual.data());e->GetIntArrayRegion(jinReference,0,(jsize)jin.guide.size(),jin.guide.data());if(e->ExceptionCheck()){close(sourceFd);return JNI_FALSE;}for(float v:jin.residual)if(!std::isfinite(v)){close(sourceFd);return JNI_FALSE;}const auto&p3=displayP3Lut();jin.guideP3.resize((size_t)jinW*jinH*3);for(int i=0;i<jinW*jinH;i++){jint argb=jin.guide[(size_t)i];float sr=(float)((argb>>16)&255)/255.f,sg=(float)((argb>>8)&255)/255.f,sb=(float)(argb&255)/255.f;auto baseP3=p3.convertEncoded(sr,sg,sb);auto outP3=p3.convertEncoded(clampf(sr+jin.residual[(size_t)i*3],0.f,1.f),clampf(sg+jin.residual[(size_t)i*3+1],0.f,1.f),clampf(sb+jin.residual[(size_t)i*3+2],0.f,1.f));for(int c=0;c<3;c++){jin.guideP3[(size_t)i*3+c]=baseP3[(size_t)c];jin.residual[(size_t)i*3+c]=outP3[(size_t)c]-baseP3[(size_t)c];}}}
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2GuidedBaseDetailLtm.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2GuidedBaseDetailLtm.java
index e2610db3de473a883c1b36f813e5abb9613cdff8..cd48c17f2416281969e469309d02fea1538f133f 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2GuidedBaseDetailLtm.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2GuidedBaseDetailLtm.java
@@ -12,17 +12,12 @@ import java.nio.ByteOrder;
 import java.nio.FloatBuffer;
 
 /**
- * IRIS_26618_GUIDED_BASE_DETAIL_LTM_OWNER
+ * IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION
  *
- * Motion-only spatial exposure-allocation augmentation.  26614's viewfinder matcher still owns
- * the desired global brightness target and MotionV2Render remains the only final tone/output
- * curve.  This node only decomposes common extended-linear Display-P3 luminance into a guided
- * broad base plus untouched local detail, then emits a bounded common-RGB scalar that prevents a
- * large global brightness target from flattening already-bright broad illumination.
- *
- * The low-resolution scalar field is also copied into Parameters so the direct true-2x renderer
- * consumes the identical field.  It is read-only presentation state: Sabre, SHORT, CFA validity,
- * DNG, capture, denoise, alignment and temporal weights never consume it.
+ * Motion-only edge-aware broad-guide decomposition.  Unlike 26618, this node does not multiply
+ * or otherwise alter RGB.  It publishes one low-resolution guided log-guide base to the final
+ * MotionV2Render owner and to true-2x publication.  The final renderer then maps only the broad
+ * base and recombines the measured local log-detail residual with one common RGB scalar.
  */
 public final class MotionV2GuidedBaseDetailLtm extends Node {
     private static final int MAP_LONG_EDGE = 256;
@@ -37,7 +32,7 @@ public final class MotionV2GuidedBaseDetailLtm extends Node {
 
     private static Point mapSize(Point source) {
         if (source == null || source.x <= 0 || source.y <= 0) {
-            throw new IllegalArgumentException("Invalid 26618 LTM source size");
+            throw new IllegalArgumentException("Invalid 26619 LTM source size");
         }
         final float scale = (float) MAP_LONG_EDGE / (float) Math.max(source.x, source.y);
         final int w = Math.max(MAP_MIN_EDGE, Math.round(source.x * scale));
@@ -45,31 +40,36 @@ public final class MotionV2GuidedBaseDetailLtm extends Node {
         return new Point(w, h);
     }
 
-    private void clearState() {
-        basePipeline.mParameters.motionV2GuidedLtmApplied = false;
-        basePipeline.mParameters.motionV2GuidedLtmWidth = 0;
-        basePipeline.mParameters.motionV2GuidedLtmHeight = 0;
-        basePipeline.mParameters.motionV2GuidedLtmGainMap = null;
+    private void clearState(PostPipeline pipeline) {
+        basePipeline.mParameters.motionV2GuidedBaseDetailApplied = false;
+        basePipeline.mParameters.motionV2GuidedBaseDetailWidth = 0;
+        basePipeline.mParameters.motionV2GuidedBaseDetailHeight = 0;
+        basePipeline.mParameters.motionV2GuidedBaseLogMap = null;
+        if (pipeline.motionV2GuidedBaseLogTexture != null) {
+            try { pipeline.motionV2GuidedBaseLogTexture.close(); } catch (Throwable ignored) {}
+            pipeline.motionV2GuidedBaseLogTexture = null;
+        }
     }
 
     @Override
     public void Run() {
         if (!basePipeline.mParameters.motionV2Active || basePipeline.mParameters.irisNightActive) {
-            throw new IllegalStateException("26618 guided LTM is Motion-only");
+            throw new IllegalStateException("26619 guided LTM is Motion-only");
         }
-        clearState();
+        final PostPipeline pipeline = (PostPipeline) basePipeline;
+        clearState(pipeline);
         final float displayGain = basePipeline.mParameters.motionV2DisplayGain;
         if (!Float.isFinite(displayGain) || displayGain <= 0.0f) {
-            throw new IllegalStateException("Invalid 26618 LTM displayGain=" + displayGain);
+            throw new IllegalStateException("Invalid 26619 LTM displayGain=" + displayGain);
         }
-        final float requestedFinalGain = displayGain * 0.80f;
+        final float requestedFinalGain = displayGain * MotionV2Render.OUTPUT_EXPOSURE_SCALE;
         if (requestedFinalGain <= ACTIVE_FINAL_GAIN) {
             WorkingTexture = previousNode.WorkingTexture;
             glProg.closed = true;
-            Log.i(Name, "IRIS_26618_GUIDED_BASE_DETAIL_LTM"
+            Log.i(Name, "IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION"
                     + " active=false requestedFinalGain=" + requestedFinalGain
                     + " reason=SMALL_OR_NO_GLOBAL_LIFT"
-                    + " finalToneOwner=MotionV2Render true2xSharedField=false");
+                    + " imageMutation=false finalToneOwner=MotionV2Render");
             return;
         }
 
@@ -77,72 +77,69 @@ public final class MotionV2GuidedBaseDetailLtm extends Node {
         final Point low = mapSize(sourceSize);
         final GLFormat rgba16f = new GLFormat(GLFormat.DataType.FLOAT_16, 4);
         GLTexture coefficients = null;
-        GLTexture gainMap = null;
+        GLTexture baseLog = null;
+        boolean transferred = false;
         try {
             coefficients = new GLTexture(low, rgba16f);
-            gainMap = new GLTexture(low, rgba16f);
+            baseLog = new GLTexture(low, rgba16f);
 
-            glProg.useAssetProgram("motionv2/guided_base_detail_ltm_coeff_26618");
+            glProg.useAssetProgram("motionv2/guided_base_detail_ltm_coeff_26619");
             glProg.setTexture("InputBuffer", previousNode.WorkingTexture);
             glProg.setVar("ltmGridSize", low);
             glProg.drawBlocks(coefficients);
 
-            glProg.useAssetProgram("motionv2/guided_base_detail_ltm_gain_26618");
+            glProg.useAssetProgram("motionv2/guided_base_detail_ltm_base_26619");
             glProg.setTexture("InputBuffer", previousNode.WorkingTexture);
             glProg.setTexture("CoeffBuffer", coefficients);
-            glProg.setVar("displayGain", displayGain);
             glProg.setVar("ltmGridSize", low);
-            glProg.drawBlocks(gainMap);
+            glProg.drawBlocks(baseLog);
 
-            gainMap.BufferLoad();
-            ByteBuffer bytes = gainMap.textureBuffer(
+            baseLog.BufferLoad();
+            ByteBuffer bytes = baseLog.textureBuffer(
                     new GLFormat(GLFormat.DataType.FLOAT_32, 4), true);
             bytes.order(ByteOrder.nativeOrder());
             FloatBuffer floats = bytes.asFloatBuffer();
             final int count = low.x * low.y;
-            final float[] sharedGain = new float[count];
+            final float[] sharedBaseLog = new float[count];
             float min = Float.POSITIVE_INFINITY;
             float max = Float.NEGATIVE_INFINITY;
             double sum = 0.0;
             for (int i = 0; i < count; ++i) {
-                final float g = floats.get(i * 4);
-                if (!Float.isFinite(g) || g < 0.499f || g > 1.001f) {
+                final float v = floats.get(i * 4);
+                if (!Float.isFinite(v) || v < -16.0f || v > 8.0f) {
                     throw new IllegalStateException(
-                            "Invalid 26618 shared LTM gain at " + i + ": " + g);
+                            "Invalid 26619 guided base log at " + i + ": " + v);
                 }
-                sharedGain[i] = g;
-                min = Math.min(min, g);
-                max = Math.max(max, g);
-                sum += g;
+                sharedBaseLog[i] = v;
+                min = Math.min(min, v);
+                max = Math.max(max, v);
+                sum += v;
             }
 
-            glProg.useAssetProgram("motionv2/guided_base_detail_ltm_apply_26618");
-            glProg.setTexture("InputBuffer", previousNode.WorkingTexture);
-            glProg.setTexture("LtmGainBuffer", gainMap);
-            WorkingTexture = basePipeline.getMain();
-            glProg.drawBlocks(WorkingTexture);
-            glProg.closed = true;
+            basePipeline.mParameters.motionV2GuidedBaseLogMap = sharedBaseLog;
+            basePipeline.mParameters.motionV2GuidedBaseDetailWidth = low.x;
+            basePipeline.mParameters.motionV2GuidedBaseDetailHeight = low.y;
+            basePipeline.mParameters.motionV2GuidedBaseDetailApplied = true;
+            pipeline.motionV2GuidedBaseLogTexture = baseLog;
+            transferred = true;
 
-            basePipeline.mParameters.motionV2GuidedLtmGainMap = sharedGain;
-            basePipeline.mParameters.motionV2GuidedLtmWidth = low.x;
-            basePipeline.mParameters.motionV2GuidedLtmHeight = low.y;
-            basePipeline.mParameters.motionV2GuidedLtmApplied = true;
-
-            Log.i(Name, "IRIS_26618_GUIDED_BASE_DETAIL_LTM"
-                    + " active=true owner=GUIDED_LOG_LUMINANCE_BASE"
-                    + " detailResidual=UNCHANGED"
-                    + " rgbGain=COMMON_SCALAR"
+            WorkingTexture = previousNode.WorkingTexture;
+            glProg.closed = true;
+            Log.i(Name, "IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION"
+                    + " active=true owner=GUIDED_LOG_TONE_GUIDE_BASE"
+                    + " imageMutation=false preRenderRgbGain=false"
+                    + " detailResidual=RECOMBINED_IN_FINAL_RENDER"
                     + " map=" + low.x + "x" + low.y
-                    + " gainMin=" + min + " gainMean=" + (sum / count) + " gainMax=" + max
+                    + " baseLogMin=" + min + " baseLogMean=" + (sum / count)
+                    + " baseLogMax=" + max
                     + " displayGain=" + displayGain
                     + " requestedFinalGain=" + requestedFinalGain
-                    + " hdrAboveOnePreserved=true"
                     + " finalToneOwner=MotionV2Render"
                     + " true2xSharedField=true"
                     + " sabreShortCfaDngUnchanged=true");
         } finally {
             if (coefficients != null) coefficients.close();
-            if (gainMap != null) gainMap.close();
+            if (baseLog != null && !transferred) baseLog.close();
         }
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 28611650a88ad5dc878cdad4988bf65e25264700..5574ecc2f734f1a2ac0f9fa5acf9ae01aacc2f36 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -104,6 +104,7 @@ public final class MotionV2Render extends Node {
      * reserves the final 5% of SDR code space for genuine >1 extended HDR. The function is one
      * fixed C1 monotonic source-domain map: no scene percentile, object class or moving knot. */
     public static final float IRIS_26614_SDR_WHITE_ANCHOR = 0.95f;
+    public static final float IRIS_26619_BROAD_BASE_WHITE_ANCHOR = 0.86f;
 
     static float iris26614MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
         float x = Math.max(sourceGuide, 0.0f);
@@ -325,20 +326,38 @@ public final class MotionV2Render extends Node {
                 + " reconstructionZoom=" + reconstructionZoom
                 + " renderResidual=" + irisOutputZoom
                 + " product=" + (reconstructionZoom * irisOutputZoom));
-                glProg.useAssetProgram("motionv2/render");
+        final PostPipeline pipeline = (PostPipeline) basePipeline;
+        final GLTexture guidedBaseTexture = pipeline.motionV2GuidedBaseLogTexture;
+        final boolean guidedBaseEnabled = basePipeline.mParameters.motionV2Active
+                && basePipeline.mParameters.motionV2GuidedBaseDetailApplied
+                && guidedBaseTexture != null;
+        if (basePipeline.mParameters.motionV2GuidedBaseDetailApplied != guidedBaseEnabled) {
+            throw new IllegalStateException("26619 guided-base texture/state mismatch applied="
+                    + basePipeline.mParameters.motionV2GuidedBaseDetailApplied
+                    + " texture=" + (guidedBaseTexture != null));
+        }
+
+        glProg.useAssetProgram("motionv2/render");
         glProg.setTexture("InputBuffer", extendedLinearHdr);
+        glProg.setTexture("GuidedBaseBuffer", guidedBaseEnabled ? guidedBaseTexture : extendedLinearHdr);
+        glProg.setVar("iris26619GuidedBaseEnabled", guidedBaseEnabled ? 1 : 0);
         glProg.setVar("sceneWhite", sceneWhite);
         glProg.setVar("iris26592MotionHdrHandoff", basePipeline.mParameters.motionV2Active ? 1 : 0);
         glProg.setVar("displayGain", basePipeline.mParameters.motionV2DisplayGain);
         glProg.setVar("iris26604MotionSdrKneeFinal", IRIS_26604_MOTION_SDR_KNEE_FINAL);
-        
-                glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
+        glProg.setVar("outputExposureScale", OUTPUT_EXPOSURE_SCALE);
         glProg.setVar("irisOutputZoom", irisOutputZoom);
 
         WorkingTexture = basePipeline.getMain();
-        glProg.drawBlocks(WorkingTexture);
+        try {
+            glProg.drawBlocks(WorkingTexture);
+        } finally {
+            if (guidedBaseTexture != null) {
+                guidedBaseTexture.close();
+                pipeline.motionV2GuidedBaseLogTexture = null;
+            }
+        }
 
-        PostPipeline pipeline = (PostPipeline) basePipeline;
         pipeline.motionV2GainMapBitmap = null;
         pipeline.motionV2GainMapMaxRatio = 1.0f;
         pipeline.motionV2GainMapFullHdrDisplayRatio = 1.0f;
@@ -585,7 +604,11 @@ public final class MotionV2Render extends Node {
                 + " iris26614DisplayDomainKneeAuthority=false"
                 + " iris26614CanonicalAppearanceGainAuthority=true"
                 + " IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_UHDR_SR_PARITY=true"
-                + " localTone=false"
+                + " iris26619GuidedBaseEnabled=" + guidedBaseEnabled
+                + " iris26619BroadBaseWhiteAnchor=" + IRIS_26619_BROAD_BASE_WHITE_ANCHOR
+                + " iris26619PreToneRgbGain=false"
+                + " iris26619NearWhiteRelease=false"
+                + " localTone=IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION"
                 + " sharpening=false");
     }
 }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
index 367f7e4992f8bb53381acac4cacda2a2f4a034e9..760d4d8bf947ac8fdec88e6f78128b77cf65a7e0 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/PostPipeline.java
@@ -44,6 +44,8 @@ public class PostPipeline extends GLBasePipeline {
     public ByteBuffer irisMotionDirectRgbAuxiliary;
     public GLTexture irisMotionDirectRgbTexture;
     public GLTexture motionV2HighlightProvenanceTexture;
+    /* IRIS_26619_GUIDED_BASE_TEXTURE_LIFETIME: transient low-resolution render input only. */
+    public GLTexture motionV2GuidedBaseLogTexture;
 
     /* IRIS_26432_TRUE_V2_GAINMAP_HANDOFF */
     public Bitmap motionV2GainMapBitmap;
@@ -551,13 +553,6 @@ public class PostPipeline extends GLBasePipeline {
              */
             add(new MotionV2AdaptiveColorAppearance());
             add(new StageTelemetry("IRIS_26563_MOTION_ADAPTIVE_COLOR_APPEARANCE"));
-            /* IRIS_26618_GUIDED_BASE_DETAIL_LTM_OWNER
-             * Spatially allocate the already-solved presentation target on luminance base only.
-             * MotionV2DisplayExposure remains pass-through and MotionV2Render remains the single
-             * final tone/output curve; Night is intentionally unchanged.
-             */
-            add(new MotionV2GuidedBaseDetailLtm());
-            add(new StageTelemetry("IRIS_26618_GUIDED_BASE_DETAIL_LTM"));
             add(new MotionV2DisplayExposure());
             add(new StageTelemetry("V2_POST_VIEWFINDER_PRESENTATION_EXPOSURE"));
 
@@ -572,6 +567,14 @@ public class PostPipeline extends GLBasePipeline {
                 add(new StageTelemetry("IRIS_26514_LINEAR_PRESENTATION_CONTROLS"));
             }
 
+            /* IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION
+             * Decompose the exact pre-render signal after optional manual controls.  This node
+             * never modifies RGB; MotionV2Render alone maps the broad base and recombines the
+             * measured local log-detail residual.  Night remains unchanged.
+             */
+            add(new MotionV2GuidedBaseDetailLtm());
+            add(new StageTelemetry("IRIS_26619_GUIDED_BASE_DETAIL_FINAL_COMPOSITION"));
+
             /* IRIS_26560_SABRE_PRIMARY_RECONSTRUCTION_DENOISE
              * No legacy Photon residual spatial-denoise stage is reintroduced after Sabre.
              */
@@ -581,7 +584,7 @@ public class PostPipeline extends GLBasePipeline {
             com.particlesdevs.photoncamera.util.MotionTrace.processingState(
                     "IRIS_26410_MOTION_V2_POST_GRAPH",
                     "owner=" + motionReconstructionOwnerName(mParameters.motionV2ReconstructionOwner)
-                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,OwnerSpecificPreColor,MotionV2ColorTransform,MotionV2ViewfinderExposureMatcher,MotionV2AdaptiveColorAppearance,MotionV2GuidedBaseDetailLtm,MotionV2DisplayExposure,IrisManualOptional,MotionV2Render,RotateWatermark"
+                            + " nodes=MotionV2CfaInput,DirectRGB-or-CFAFallback,OwnerSpecificPreColor,MotionV2ColorTransform,MotionV2ViewfinderExposureMatcher,MotionV2AdaptiveColorAppearance,MotionV2DisplayExposure,IrisManualOptional,MotionV2GuidedBaseDetailLtm,MotionV2Render,RotateWatermark"
                             + " spatialHighlightNode=false"
                             + " directMultiframeRgb=" + directRgbCarrier
                             + " strictSabreAuthority26560=true"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index 49d38dde098aa49ec058a974e2a8e7c275b8aec0..51c7bb5b2c6f342f70c392594b44ae0e98e2e0d9 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -76,14 +76,15 @@ public class Parameters {
     public float motionV2ToneBaseSceneWhite = 1.0f;
     public float motionV2ToneAdaptiveSceneWhite = 1.0f;
     public float motionV2ToneAdaptiveStrength = 0.0f;
-    /* IRIS_26618_GUIDED_BASE_DETAIL_LTM_STATE
-     * Read-only presentation state produced after adaptive color and consumed only by the
-     * matching true-2x publication path. The 1x path has already applied the same field on GPU.
+    /* IRIS_26619_GUIDED_BASE_DETAIL_LTM_STATE
+     * Read-only edge-aware broad log-guide field.  It is computed from the exact pre-render
+     * appearance input and consumed by both 1x MotionV2Render and true-2x publication.  It is
+     * never a pre-tone gain map and never participates in RAW/CFA/Sabre/SHORT/DNG ownership.
      */
-    public boolean motionV2GuidedLtmApplied = false;
-    public int motionV2GuidedLtmWidth = 0;
-    public int motionV2GuidedLtmHeight = 0;
-    public float[] motionV2GuidedLtmGainMap = null;
+    public boolean motionV2GuidedBaseDetailApplied = false;
+    public int motionV2GuidedBaseDetailWidth = 0;
+    public int motionV2GuidedBaseDetailHeight = 0;
+    public float[] motionV2GuidedBaseLogMap = null;
     /* IRIS_26515_MGC_SOURCE_EXPOSURE_GAIN
      * MGC BaselineExposure restoration is source-domain metadata, not scene/display exposure.
      * It is consumed once by MotionV2DisplayExposure after MGC full-resolution denoise.
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index ad70e025a63fb597a58cc2a33bb65c53bacf7ea0..41182e257f4e890430429dbcff12c845f25f9ef6 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -155,15 +155,15 @@ public final class MotionV2Jpeg444Encoder {
                     ? attachedGainmap.getDisplayRatioForFullHdr() : 1.0f;
             final float publicationSceneWhite =
                     MotionV2Render.iris26598PublicationSceneWhite(parameters);
-            final boolean guidedLtm = parameters.motionV2GuidedLtmApplied;
-            final float[] guidedLtmGain = guidedLtm ? parameters.motionV2GuidedLtmGainMap : null;
-            final int guidedLtmWidth = guidedLtm ? parameters.motionV2GuidedLtmWidth : 0;
-            final int guidedLtmHeight = guidedLtm ? parameters.motionV2GuidedLtmHeight : 0;
-            if (guidedLtm && (guidedLtmGain == null || guidedLtmWidth <= 1 || guidedLtmHeight <= 1
-                    || guidedLtmGain.length != guidedLtmWidth * guidedLtmHeight)) {
-                Log.e(TAG, "IRIS_26618_TRUE2X_LTM_STATE_INVALID applied=true"
+            final boolean guidedLtm = parameters.motionV2GuidedBaseDetailApplied;
+            final float[] guidedBaseLog = guidedLtm ? parameters.motionV2GuidedBaseLogMap : null;
+            final int guidedLtmWidth = guidedLtm ? parameters.motionV2GuidedBaseDetailWidth : 0;
+            final int guidedLtmHeight = guidedLtm ? parameters.motionV2GuidedBaseDetailHeight : 0;
+            if (guidedLtm && (guidedBaseLog == null || guidedLtmWidth <= 1 || guidedLtmHeight <= 1
+                    || guidedBaseLog.length != guidedLtmWidth * guidedLtmHeight)) {
+                Log.e(TAG, "IRIS_26619_TRUE2X_LTM_STATE_INVALID applied=true"
                         + " map=" + guidedLtmWidth + "x" + guidedLtmHeight
-                        + " values=" + (guidedLtmGain == null ? -1 : guidedLtmGain.length));
+                        + " values=" + (guidedBaseLog == null ? -1 : guidedBaseLog.length));
                 return false;
             }
             final long renderStartNs = System.nanoTime();
@@ -174,7 +174,7 @@ public final class MotionV2Jpeg444Encoder {
                     sensorToProPhoto, profileToDisplay,
                     parameters.motionV2DisplayGain, exposureEv, shadows, contrast,
                     publicationSceneWhite, parameters.motionV2Active,
-                    guidedLtmGain, guidedLtmWidth, guidedLtmHeight,
+                    guidedBaseLog, guidedLtmWidth, guidedLtmHeight,
                     watermark,
                     useJin ? jinResidual.residual : null,
                     useJin ? jinResidual.referenceRgb : null,
@@ -200,7 +200,7 @@ public final class MotionV2Jpeg444Encoder {
                         + (parameters.motionV2Active ? "BASE_26598_MOTION" : "ADAPTIVE_26591_NIGHT")
                     + " IRIS_26598_TRUE2X_TONE_PARITY=true"
                     + " IRIS_26596_TRUE2X_CONTENT_CAP=true"
-                    + " IRIS_26618_TRUE2X_SHARED_GUIDED_LTM=" + guidedLtm
+                    + " IRIS_26619_TRUE2X_SHARED_GUIDED_BASE_DETAIL=" + guidedLtm
                     + " guidedLtmMap=" + guidedLtmWidth + "x" + guidedLtmHeight);
             /* IRIS_26576_NATIVE_PUBLICATION_BACKEND_PERSISTENT_PROOF
              * Native 26571 remains GPU-first with the exact 26570 CPU fallback. Retrieve only the
@@ -330,7 +330,7 @@ public final class MotionV2Jpeg444Encoder {
             int rotation, boolean mirror, float renderResidualZoom,
             float[] sensorToProPhoto, float[] proPhotoToSrgb, float displayGain,
             float exposureEv, float shadows, float contrast, float sceneWhite, boolean motionHdrHandoff,
-            float[] guidedLtmGain, int guidedLtmWidth, int guidedLtmHeight,
+            float[] guidedBaseLog, int guidedLtmWidth, int guidedLtmHeight,
             Bitmap watermark,
             float[] jinResidual, int[] jinReferenceRgb, int jinWidth, int jinHeight,
             String true2xGainmapPath, float true2xGainmapMaxRatio,
diff --git a/app/version.properties b/app/version.properties
index 491cadd63c987362438982f924c83fca7432726e..b783be34550e31fa323160417a97b80c759778e7 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726618
-VERSION_BUILD=26618
+VERSION_NAME=0.9726619
+VERSION_BUILD=26619
