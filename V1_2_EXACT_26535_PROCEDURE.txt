Photon 26536 V1.2 — exact successful-26535 procedure audit

Scope
- Infrastructure-only correction.
- Runtime target remains 0.9726536 / 26536.
- The six 26536 app/src runtime files remain byte-authoritative from the original V1 handoff.
- No backup branch is created.
- No dev checkout/switch and no push command are introduced.

Direct comparison to successful 26535 procedure
GATE 0 — PRESERVED
- exact experimental-clean-photon-rebuild branch check
- exact prior-success handoff-head ancestry
- complete handoff SHA verification
- base manifest-file SHA verification
- forward and rollback patch SHA verification
- inherited safety-file presence and pinned bjzhou native commit

GATE 1 — PRESERVED
- repository app/src is not runtime authority
- exact successful prior artifact is selected by pinned head
- candidate TAR SHA and candidate manifest-file SHA are pinned
- all 964 base files are verified
- version 0.9726535 / build 26535 verified
- protected runtime anchors verified

GATE 2 — PRESERVED WITH USER-REQUESTED NO-BACKUP CHANGE
- exact six-file transform validation
- exact binary git diff and reverse diff regeneration
- byte-identical expected forward/rollback patches
- fuzz=0 forward application
- exact rollback to successful 26535
- backup branch creation removed; rollback patch remains the recovery proof

GATE 3 — PRESERVED
- changed syntax preflight
- false-color GLSL preflight
- inherited v16/shader/embedded shader checks
- Kotlin API, native DNG/JPEG, Java/XML, DNG SubIFD checks
- architecture contract validation
- PRE-BUILD SAFETY PROOF PASSED

GATE 4 — PRESERVED
- version increment to 0.9726536 / 26536
- exact validation after increment
- audited pre-Gradle runtime manifest
- pinned bjzhou native source restoration and SHA manifest
- exact cpp/deps precondition
- real Kotlin + Java compile and assembleDebug in the same guarded block

GATE 5 — PRESERVED, WITH THE V1 BUG FIXED
- exact cpp/deps post-build file-set proof remains first
- exact pre/post audited runtime equality remains
- exact pinned bjzhou native dependency manifest remains
- only then, architecture validation receives POSTCHECK: a separate copy with the already-proven build-time native additions removed
- Java/XML, native JPEG and false-color shader post-build checks still run on real ROOT

GATE 6 — PRESERVED
- exactly one Gradle APK required
- final APK copied once
- APK SHA generated
- deterministic 964-file next candidate manifest and TAR generated
- scope/provenance proof emitted

Pre-execution workflow hardening
- Verifies the exact original V1 build-script SHA before applying V1.2.
- Mirrors the successful 26535 explicit anchor checks rather than trusting hash equality alone.
- After applying V1.2, rechecks the preserved anchors and Gate 5 ordering.
- Requires repository working-tree changes to be exactly the build script and its handoff-hash bookkeeping.
- Requires git diff --quiet -- app/src before the guarded build starts.

Known V1 failure resolved
- V1 compiled Kotlin/Java, native code and assembleDebug successfully.
- It failed only because the six-file validator recursively saw intentionally restored third_party_26507 and generated cpp/deps files.
- V1.2 does not weaken those native checks; it proves them first and removes them only from the separate architecture-validator view.
