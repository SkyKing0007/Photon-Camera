diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index 2f7fe0cccfc3a062a34827075a593ba6d1a39475..3aa2888c2142151c9b2c7a3b4e1432e42108fbbf 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -17,6 +17,7 @@ uniform int iris26653HighlightCompressionEnabled;
 uniform float iris26653HighlightKnee;
 uniform float iris26653AdaptiveWhitePoint;
 uniform float iris26630MotionSaturation;
+uniform float iris26704BodyToneStrength;
 /* IRIS_26524_FULLSIZE_MOTION_ZOOM_RENDER */
 out vec3 Output;
 
@@ -301,6 +302,21 @@ float iris26660ObjectColorGamma(float mappedGuide){
     return mix(gammaOwned,y,release);
 }
 
+/* IRIS_26704_RANGE_SEPARATED_BODY_TONE
+ * Global pointwise toe lift only. True/near black remains protected, dark body/midtones can rise,
+ * and mapped guide >=0.35 is exact identity so the existing recovered highlight owner is untouched.
+ * One scalar is applied to RGB by the caller; no local/spatial/chroma synthesis occurs. */
+float iris26704BodyToneGuide(float mappedGuide){
+    float y=max(mappedGuide,0.0);
+    float strength=clamp(iris26704BodyToneStrength,0.0,1.0);
+    const float anchorGuide=0.35;
+    if(strength<=1.0e-7 || y<=0.0 || y>=anchorGuide)return y;
+    float gamma=1.0-0.28*strength;
+    float toe=anchorGuide*pow(clamp(y/anchorGuide,0.0,1.0),gamma);
+    float blackGate=smoothstep(0.004,0.018,y);
+    return mix(y,toe,blackGate);
+}
+
 float mapFinalSdrGuide(float sourceGuide) {
     if(iris26592MotionHdrHandoff!=0) {
         return iris26653MapMotionSdrFinalGuide(sourceGuide);
@@ -337,6 +353,7 @@ vec3 mapExtendedLinearHeadroom(vec3 rgb) {
      * clamp is introduced here.
      */
     float mappedGuide=mapFinalSdrGuide(guide);
+    mappedGuide=iris26704BodyToneGuide(mappedGuide);
     mappedGuide=iris26660ObjectColorGamma(mappedGuide);
     vec3 mapped=rgb*(mappedGuide/guide);
 
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 86166b30a66478c58fe1e74b31145ee9c264adb3..aca72835351009a4645f6cf1059defcc5e86fb95 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -522,7 +522,7 @@ struct Params {
     bool mirror=false;
     bool motionHdrHandoff=false;
     bool acr3Enabled=false;
-    float residualZoom=1.f,displayGain=1.f,exposureEv=0.f,shadows=0.f,contrast=0.f,saturation=1.f,sceneWhite=1.f;
+    float residualZoom=1.f,displayGain=1.f,exposureEv=0.f,shadows=0.f,contrast=0.f,saturation=1.f,sceneWhite=1.f,bodyToneStrength=0.f;
     const uint16_t* localToneLogMap=nullptr;
     int localToneLogW=0,localToneLogH=0,localToneLogSourceW=0,localToneLogSourceH=0;
     float sensorToProfile[9]{};
@@ -708,6 +708,17 @@ inline float iris26621MapMotionSdrFinalGuide(float sourceGuide,float displayGain
     float tailScale=reserve/std::max(bodySlopeAtWhite,1.0e-6f),excess=x-1.f;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
+/* IRIS_26704_RANGE_SEPARATED_BODY_TONE_TRUE2X_CPU
+ * Exact CPU mirror of the 1x Motion global pointwise body toe. It is identity at mapped guide
+ * >=0.35 so recovered highlights remain under the inherited upper-tone owner. */
+inline float iris26704BodyToneGuide(float mappedGuide,float strength){
+    float y=std::max(mappedGuide,0.f),s=clampf(strength,0.f,1.f);constexpr float anchorGuide=0.35f;
+    if(s<=1.0e-7f||y<=0.f||y>=anchorGuide)return y;
+    float gamma=1.f-0.28f*s;
+    float toe=anchorGuide*std::pow(clampf(y/anchorGuide,0.f,1.f),gamma);
+    float blackGate=smoothstep(0.004f,0.018f,y);
+    return y+(toe-y)*blackGate;
+}
 inline float iris26638ShadowFloorGuide(float guide){
     constexpr float floorEnd=0.050f,deepScale=0.72f;float x=std::max(guide,0.f);if(x>=floorEnd)return x;
     float t=clampf(x/floorEnd,0.f,1.f),smoothFactor=t*t*(3.f-2.f*t);return x*(deepScale+(1.f-deepScale)*smoothFactor);
@@ -721,6 +732,7 @@ inline Vec3 renderHeadroom(Vec3 rgb,const Params&p,float localMappedGuide){
     /* IRIS_26621_SINGLE_GLOBAL_PLUS_LOCAL_TONE_OWNER_TRUE2X_CPU */
     if(p.motionHdrHandoff){
         float globalMapped=iris26621MapMotionSdrFinalGuide(guide,p.displayGain);
+        globalMapped=iris26704BodyToneGuide(globalMapped,p.bodyToneStrength);
         float mappedFinal=globalMapped;
         if(localMappedGuide>=0.f){
             float localStrength=smoothstep(0.030f,0.100f,globalMapped);
@@ -888,8 +900,7 @@ struct Iris26571BandQueue {
     }
 };
 
-static const char*kIris26571PublicationCompute=R"GLSL(
-#version 310 es
+static const char*kIris26571PublicationCompute=R"GLSL(#version 310 es
 precision highp float;
 precision highp int;
 precision highp usampler2D;
@@ -919,6 +930,7 @@ uniform float uShadows;
 uniform float uContrast;
 uniform float uSaturation;
 uniform float uSceneWhite;
+uniform float uBodyToneStrength;
 uniform int uMotionHdrHandoff;
 uniform int uAcr3Enabled;
 uniform int uLocalToneLogEnabled;
@@ -1056,6 +1068,15 @@ float iris26621MapMotionSdrFinalGuide(float sourceGuide){
     float excess=x-1.0;
     return whiteAnchor+reserve*excess/(excess+tailScale);
 }
+/* IRIS_26704_RANGE_SEPARATED_BODY_TONE_TRUE2X_GPU */
+float iris26704BodyToneGuide(float mappedGuide){
+    float y=max(mappedGuide,0.0),strength=clamp(uBodyToneStrength,0.0,1.0);const float anchorGuide=0.35;
+    if(strength<=1.0e-7||y<=0.0||y>=anchorGuide)return y;
+    float gamma=1.0-0.28*strength;
+    float toe=anchorGuide*pow(clamp(y/anchorGuide,0.0,1.0),gamma);
+    float blackGate=smoothstep(0.004,0.018,y);
+    return mix(y,toe,blackGate);
+}
 float iris26638ShadowFloorGuide(float guide){
     const float floorEnd=0.050,deepScale=0.72;float x=max(guide,0.0);if(x>=floorEnd)return x;float t=clamp(x/floorEnd,0.0,1.0),smoothFactor=t*t*(3.0-2.0*t);return x*mix(deepScale,1.0,smoothFactor);
 }
@@ -1068,6 +1089,7 @@ vec3 irisHeadroom(vec3 rgb,float localMappedGuide){
     /* IRIS_26621_SINGLE_TONE_OWNER_TRUE2X_GPU */
     if(uMotionHdrHandoff!=0){
         float globalMapped=iris26621MapMotionSdrFinalGuide(guide);
+        globalMapped=iris26704BodyToneGuide(globalMapped);
         float mappedFinal=globalMapped;
         if(localMappedGuide>=0.0){
             float localStrength=smoothstep(0.030,0.100,globalMapped);
@@ -1193,7 +1215,7 @@ private:
     static void set(std::string*r,const char*m){if(r)*r=m?m:"unknown";}
     static void setGl(std::string*r,const char*stage,GLenum err){if(!r)return;char b[96];std::snprintf(b,sizeof(b),"%s_gl_error_0x%04x",stage?stage:"gl",(unsigned int)err);*r=b;}
     void setParams(){
-        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSaturation"),params->saturation);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);glUniform1i(loc("uAcr3Enabled"),params->acr3Enabled?1:0);glUniform1i(loc("uLocalToneLogEnabled"),(params->motionHdrHandoff&&params->localToneLogMap&&params->localToneLogW>0&&params->localToneLogH>0&&params->localToneLogSourceW>0&&params->localToneLogSourceH>0)?1:0);glUniform2i(loc("uLocalToneLogSize"),params->localToneLogW,params->localToneLogH);glUniform2i(loc("uLocalToneLogSourceSize"),params->localToneLogSourceW,params->localToneLogSourceH);
+        glUseProgram(program);glUniform2i(loc("uTrueSize"),params->trueW,params->trueH);glUniform2i(loc("uOutputSize"),outWidth,outHeight);glUniform2i(loc("uRawSize"),params->rawW,params->rawH);glUniform2i(loc("uCropSize"),params->cropW,params->cropH);glUniform1i(loc("uRotation"),params->rotation);glUniform1i(loc("uMirror"),params->mirror?1:0);glUniform1f(loc("uResidualZoom"),params->residualZoom);glUniform1f(loc("uDisplayGain"),params->displayGain);glUniform1f(loc("uExposureEv"),params->exposureEv);glUniform1f(loc("uShadows"),params->shadows);glUniform1f(loc("uContrast"),params->contrast);glUniform1f(loc("uSaturation"),params->saturation);glUniform1f(loc("uSceneWhite"),params->sceneWhite);glUniform1f(loc("uBodyToneStrength"),params->bodyToneStrength);glUniform1i(loc("uMotionHdrHandoff"),params->motionHdrHandoff?1:0);glUniform1i(loc("uAcr3Enabled"),params->acr3Enabled?1:0);glUniform1i(loc("uLocalToneLogEnabled"),(params->motionHdrHandoff&&params->localToneLogMap&&params->localToneLogW>0&&params->localToneLogH>0&&params->localToneLogSourceW>0&&params->localToneLogSourceH>0)?1:0);glUniform2i(loc("uLocalToneLogSize"),params->localToneLogW,params->localToneLogH);glUniform2i(loc("uLocalToneLogSourceSize"),params->localToneLogSourceW,params->localToneLogSourceH);
         glUniform3f(loc("uSensorRow0"),params->sensorToProfile[0],params->sensorToProfile[1],params->sensorToProfile[2]);glUniform3f(loc("uSensorRow1"),params->sensorToProfile[3],params->sensorToProfile[4],params->sensorToProfile[5]);glUniform3f(loc("uSensorRow2"),params->sensorToProfile[6],params->sensorToProfile[7],params->sensorToProfile[8]);
         glUniform3f(loc("uDisplayRow0"),params->profileToDisplay[0],params->profileToDisplay[1],params->profileToDisplay[2]);glUniform3f(loc("uDisplayRow1"),params->profileToDisplay[3],params->profileToDisplay[4],params->profileToDisplay[5]);glUniform3f(loc("uDisplayRow2"),params->profileToDisplay[6],params->profileToDisplay[7],params->profileToDisplay[8]);
     }
@@ -1284,13 +1306,13 @@ extern "C" JNIEXPORT jboolean JNICALL Java_com_particlesdevs_photoncamera_proces
         jfloatArray sensorToProfile,jfloatArray profileToDisplay,jfloatArray acr3Curve,jboolean acr3Enabled,
         jfloatArray dcpHueSatMap,jint dcpHueDivisions,jint dcpSatDivisions,jint dcpValueDivisions,
         jfloatArray dcpLookMap,jint lookHueDivisions,jint lookSatDivisions,jint lookValueDivisions,
-        jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat saturation,jfloat sceneWhite,jboolean motionHdrHandoff,jboolean localToneEnabled,
+        jfloat displayGain,jfloat exposureEv,jfloat shadows,jfloat contrast,jfloat saturation,jfloat sceneWhite,jfloat bodyToneStrength,jboolean motionHdrHandoff,jboolean localToneEnabled,
         jobject localToneLogBuffer,jint localToneLogW,jint localToneLogH,jint localToneLogSourceW,jint localToneLogSourceH,
         jobject watermarkBitmap,
         jfloatArray jinResidual,jintArray jinReference,jint jinW,jint jinH,jstring gainPath,jfloat gainMaxRatio,jfloat gainContentMaxRatio,jstring path,jint quality){
     using namespace iris26564render;gIris26576PublicationTelemetry="backend=UNSET callStarted=true";AndroidBitmapInfo info{};if(!bitmap||!renderPath||!path||trueW<=0||trueH<=0||rawW<=0||rawH<=0||cropW<=0||cropH<=0||trueW!=rawW*2||trueH!=rawH*2||cropW>rawW||cropH>rawH||AndroidBitmap_getInfo(e,bitmap,&info)!=ANDROID_BITMAP_RESULT_SUCCESS||info.format!=ANDROID_BITMAP_FORMAT_RGBA_8888){iris26576SetPublicationTelemetry("backend=NONE validationFailed=true");return JNI_FALSE;}
     int expectedNativeW=(rotation==90||rotation==270)?cropH:rawW,expectedNativeH=(rotation==90||rotation==270)?rawW:cropH;if((int)info.width!=expectedNativeW||(int)info.height!=expectedNativeH)return JNI_FALSE;
-    Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.saturation=clampf((float)saturation,0.f,2.f);p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.acr3Enabled=acr3Enabled==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
+    Params p{};p.trueW=trueW;p.trueH=trueH;p.rawW=rawW;p.rawH=rawH;p.cropW=cropW;p.cropH=cropH;p.rotation=rotation;p.mirror=mirror==JNI_TRUE;p.residualZoom=std::max(1.f,(float)residualZoom);p.displayGain=displayGain;p.exposureEv=exposureEv;p.shadows=shadows;p.contrast=contrast;p.saturation=clampf((float)saturation,0.f,2.f);p.bodyToneStrength=clampf((float)bodyToneStrength,0.f,1.f);p.motionHdrHandoff=motionHdrHandoff==JNI_TRUE;p.acr3Enabled=acr3Enabled==JNI_TRUE;p.sceneWhite=p.motionHdrHandoff?std::max(1.f,(float)sceneWhite):std::max(1.f,std::min(6.f,0.90f*std::max(1.f,p.displayGain)));
     const bool iris26702LocalToneEnabled=localToneEnabled==JNI_TRUE;
     if(p.motionHdrHandoff&&iris26702LocalToneEnabled){
         if(!localToneLogBuffer||localToneLogW<=0||localToneLogH<=0||localToneLogSourceW<=0||localToneLogSourceH<=0)return JNI_FALSE;
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 5984ebde8dd4d831ca7313bd9cf230af56be7c94..5316cc3faf36777f63ed2ce5828d86f15670d6c2 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -3159,6 +3159,14 @@ internal object GlesMgcRawSabreShaders {
             float fineStructurePhysicalSupport = smoothstep(0.20, 0.70, physicalNormalLoss);
             float inferredRadiometricLoss = effectiveRadiometricLoss *
                 mix(1.0, fineStructurePhysicalSupport, fineStructure);
+            /* IRIS_26704_FAIL_CLOSED_INFERRED_SHORT_RADIANCE
+             * A brighter warped SHORT sample does not prove highlight loss when NORMAL itself is
+             * a dark foreground structure against a bright background. Require two-channel NORMAL
+             * highlight context before non-physical inferred loss can contribute. Literal/measured
+             * CFA loss remains independent below, so genuine clipped highlight recovery survives. */
+            float normalRadiometricGuide = secondHighest3(max(normalMean.rgb, vec3(0.0)));
+            float inferredHighlightContext = smoothstep(0.22, 0.50, normalRadiometricGuide);
+            inferredRadiometricLoss *= inferredHighlightContext;
             /* IRIS_26675_PROTECTED_NORMAL_SHORT_ADMISSION
              * Once 26674 has deliberately protected the NORMAL reference, a brighter normalized
              * SHORT sample is no longer sufficient evidence of NORMAL radiometric loss: amplifying
@@ -3172,12 +3180,14 @@ internal object GlesMgcRawSabreShaders {
             float edgeTrust = edgeCorrespondence(referenceUv, sampleUv);
             float shortConfidence = physicalWeight * alignmentConfidence * edgeTrust *
                 shortSourceConfidence * shortSnrConfidence;
-            /* Low NORMAL temporal/SNR confidence may only soften a proven radiometric-loss
-             * transition; it can never create SHORT authority by itself. */
+            /* IRIS_26704_LOW_NORMAL_CONFIDENCE_NEVER_BOOSTS_SHORT
+             * Low local NORMAL support may attenuate an otherwise-proven SHORT rescue, but it must
+             * never increase SHORT authority. Keep the inherited conservative 0.82 floor to avoid
+             * weakening genuine physical highlight recovery while correcting the directionality. */
             float transitionSupport = mix(0.82, 1.0,
                 min(normalTemporalConfidence, normalSnrConfidence));
             float radianceAuthority = clamp(
-                shortConfidence * measuredNormalLoss / max(transitionSupport, 1.0e-3),
+                shortConfidence * measuredNormalLoss * transitionSupport,
                 0.0, 1.0);
 
             float normalY = luma(max(normalMean.rgb, vec3(0.0)));
@@ -3214,16 +3224,12 @@ internal object GlesMgcRawSabreShaders {
             vec3 consensusWithShort = mix(
                 consensusCarrier, shortChromaCarrier, 0.35 * shortConsensusAgreement);
 
-            /* Visual wash-out is an image-domain condition, not merely a sensor-code condition.
-             * When trustworthy same-surface neighbors carry coherent color but the center has
-             * collapsed toward low chroma, let that measured consensus make recovery eligible.
-             * This also covers tiny colored structure without object labels or scene semantics. */
-            vec3 normalChromaticity = chromaticityAtLuma(normalMean.rgb);
-            float consensusColorMagnitude = length(consensusChromaticity - vec3(1.0));
-            float normalColorMagnitude = length(normalChromaticity - vec3(1.0));
-            float visualColorDeficit = smoothstep(0.06, 0.20,
-                consensusColorMagnitude - normalColorMagnitude) * consensusConfidence;
-            float colorRecoveryNeed = max(allColorSupportLost, visualColorDeficit);
+            /* IRIS_26704_VALID_NORMAL_CHROMA_IMMUTABLE
+             * Neighborhood color is reconstruction evidence only after measured CFA validity proves
+             * that all three NORMAL color supports are physically lost. A more colorful neighbor
+             * must never independently declare a valid neutral/different-colored center "washed
+             * out"; this protects sign lettering, foliage/sky boundaries and tiny sky holes. */
+            float colorRecoveryNeed = allColorSupportLost;
             float colorEligibility = clamp(radianceAuthority * colorRecoveryNeed, 0.0, 1.0);
 
             /* No trustworthy local color means no invented camera-domain neutral. Preserve the
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 41f15869846f6c2bb6bda1f8e4427c79c0838491..93be7f066d4958d67328f1bd73aa78640011cec4 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -187,6 +187,34 @@ public final class MotionV2Render extends Node {
         return t * t * (3.0f - 2.0f * t);
     }
 
+    /* IRIS_26704_RANGE_SEPARATED_BODY_TONE
+     * Pointwise global toe lift for scenes with a large dark body plus a substantial bright region.
+     * It never samples neighbors, never changes hue, and is exact identity from mapped guide 0.35
+     * upward so recovered window/highlight structure remains owned by the existing upper-tone path. */
+    public static float iris26704BodyToneStrength(Parameters p) {
+        if (p == null || !p.motionV2Active) return 0.0f;
+        final float p50 = p.motionV2ToneP50Guide;
+        final float p95 = p.motionV2ToneP95Guide;
+        final float p99 = p.motionV2ToneP99Guide;
+        if (!Float.isFinite(p50) || !Float.isFinite(p95) || !Float.isFinite(p99)) return 0.0f;
+        final float darkBody = 1.0f - iris26623Smoothstep(0.025f, 0.070f, p50);
+        final float darkMass = 1.0f - iris26623Smoothstep(0.080f, 0.180f, p95);
+        final float brightArea = iris26623Smoothstep(0.120f, 0.220f, p99);
+        return iris26582Clamp(darkBody * darkMass * brightArea, 0.0f, 1.0f);
+    }
+
+    static float iris26704ApplyBodyToneGuide(float mappedGuide, float strength) {
+        final float y = Math.max(mappedGuide, 0.0f);
+        final float s = iris26582Clamp(strength, 0.0f, 1.0f);
+        final float anchorGuide = 0.35f;
+        if (s <= 1.0e-7f || y <= 0.0f || y >= anchorGuide) return y;
+        final float gamma = 1.0f - 0.28f * s;
+        final float toe = anchorGuide * (float)Math.pow(
+                iris26582Clamp(y / anchorGuide, 0.0f, 1.0f), gamma);
+        final float blackGate = iris26623Smoothstep(0.004f, 0.018f, y);
+        return y + (toe - y) * blackGate;
+    }
+
     /* IRIS_26623_SCENE_ADAPTIVE_UPPER_TONE_REFERENCE
      * Java reference for validation/telemetry. The viewfinder meter deliberately keeps the exact
      * 26621 body solver; final Motion presentation uses this adaptive upper-tone equation only
@@ -518,6 +546,8 @@ public final class MotionV2Render extends Node {
     }
 
     private void iris26653SetFinalToneUniforms() {
+        glProg.setVar("iris26704BodyToneStrength",
+                iris26704BodyToneStrength(basePipeline.mParameters));
         iris26623SetAdaptiveUpperToneUniforms();
         PostPipeline p = (PostPipeline) basePipeline;
         glProg.setVar("iris26653HighlightCompressionEnabled",
@@ -1459,6 +1489,19 @@ public final class MotionV2Render extends Node {
                     + " bracketMerge26658Frozen=true"
                     + " old26659BandRemoved=true");
 
+            final float iris26704BodyToneStrength =
+                    iris26704BodyToneStrength(basePipeline.mParameters);
+            Log.i(Name, "IRIS_26704_RANGE_SEPARATED_BODY_TONE"
+                    + " strength=" + iris26704BodyToneStrength
+                    + " p50Guide=" + basePipeline.mParameters.motionV2ToneP50Guide
+                    + " p95Guide=" + basePipeline.mParameters.motionV2ToneP95Guide
+                    + " p99Guide=" + basePipeline.mParameters.motionV2ToneP99Guide
+                    + " blackIdentityThrough=0.004"
+                    + " fullToeBy=0.018"
+                    + " mappedIdentityFrom=0.35"
+                    + " maxGammaLift=0.72"
+                    + " scalarRgb=true spatialSampling=false highlightOwnerUnchanged=true");
+
             Log.i(Name, "IRIS_26653_SINGLE_FINAL_HIGHLIGHT_TONE"
                     + " enabled=" + iris26653Enabled
                     + " owner=MotionV2RenderAfterBrightnessSolve"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
index 7decb5a6ecb61870b4ab7ec92b772341963199ef..4b94a5310848ca84b4c1665534fd31e5d0154e88 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/IrisMotionSettings.java
@@ -38,8 +38,10 @@ public final class IrisMotionSettings {
     private IrisMotionSettings() {}
 
     public static boolean isLocalLaplacianToneEnabled() {
-        SettingsManager sm = PhotonCamera.getSettingsManagerStatic();
-        return sm == null || getBoolean(sm, KEY_LOCAL_LAPLACIAN_TONE, true);
+        /* IRIS_26704_LOCAL_LAPLACIAN_PERMANENT_OFF
+         * The A/B implementation remains available in source for rollback, but the active Iris
+         * runtime ignores historical persisted TRUE values and always uses global tone. */
+        return false;
     }
 
     public static final class Snapshot {
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
index 568bb38942362d21f02f61da5958b8cd05fa03fb..951a635bad81263d828fecd045b1346e9c89f843 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/render/Parameters.java
@@ -148,7 +148,7 @@ public class Parameters {
     public int motionV2LocalToneLogSourceHeight = 0;
     /* IRIS_26702_LOCAL_LAPLACIAN_PRESENTATION_SWITCH
      * Frozen at Motion shutter; TRUE is exact 26701 local-tone ownership, FALSE is global-tone-only. */
-    public boolean motionV2LocalLaplacianToneEnabled = true;
+    public boolean motionV2LocalLaplacianToneEnabled = false;
     public float motionV2HardwareZoom = 1.0f;
     public float motionV2ResidualSoftwareZoom = 1.0f;
     public int realWL = -1;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index 5778a652d70683cdbb07999d1801480fb5623e02..5bf357f613a5de94bab9a1561dafbc1522cabe24 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -254,7 +254,8 @@ public final class MotionV2Jpeg444Encoder {
                     parameters.irisJpegLookMapSize[0], parameters.irisJpegLookMapSize[1],
                     parameters.irisJpegLookMapSize[2],
                     parameters.motionV2DisplayGain, exposureEv, shadows, contrast, saturation,
-                    publicationSceneWhite, parameters.motionV2Active, localToneRequired,
+                    publicationSceneWhite, MotionV2Render.iris26704BodyToneStrength(parameters),
+                    parameters.motionV2Active, localToneRequired,
                     localToneLogMap, localToneLogWidth, localToneLogHeight,
                     localToneLogSourceWidth, localToneLogSourceHeight,
                     watermark,
@@ -510,7 +511,7 @@ public final class MotionV2Jpeg444Encoder {
             float[] dcpHueSatMap, int dcpHueDivisions, int dcpSatDivisions, int dcpValueDivisions,
             float[] dcpLookMap, int lookHueDivisions, int lookSatDivisions, int lookValueDivisions,
             float displayGain, float exposureEv, float shadows, float contrast, float saturation, float sceneWhite,
-            boolean motionHdrHandoff, boolean localToneEnabled,
+            float bodyToneStrength, boolean motionHdrHandoff, boolean localToneEnabled,
             ByteBuffer localToneLogMap, int localToneLogWidth, int localToneLogHeight,
             int localToneLogSourceWidth, int localToneLogSourceHeight,
             Bitmap watermark,
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
index 5a5fbef92dfad284958729f28c6d1bb83cd9a6b8..0e58ebcdc820b78e24a751535211806ff76f2105 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/settings/PreferenceKeys.java
@@ -79,7 +79,8 @@ public class PreferenceKeys {
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_BRACKETING_MODE, 0); // Default to disable bracketing
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_IRIS_SUPER_RES, false);
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_IRIS_HEIC_OUTPUT, false);
-        settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_HIGHLIGHT_COMPRESSION, true);
+        // IRIS_26704_HIGHLIGHT_COMPRESSION_PERMANENT_OFF: retired test UI; keep key only for rollback compatibility.
+        settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_HIGHLIGHT_COMPRESSION, false);
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_PHOTO_PREFIX_NAME, "IMG_");
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_VIDEO_RESOLUTION, resources.getString(R.string.pref_video_resolution_default));
         settingsManager.setInitial(SCOPE_GLOBAL, Key.KEY_RAWVIDEO_DOWNSCALE_4X, false);
@@ -304,7 +305,10 @@ public class PreferenceKeys {
     }
 
     public static boolean isHighlightCompressionOn() {
-        return preferenceKeys.settingsManager.getBoolean(SCOPE_GLOBAL, Key.KEY_HIGHLIGHT_COMPRESSION);
+        /* IRIS_26704_HIGHLIGHT_COMPRESSION_PERMANENT_OFF
+         * Ignore any historical persisted TRUE value. The implementation remains compiled for
+         * rollback, but Iris no longer exposes or activates the experimental presentation path. */
+        return false;
     }
 
     public static void setIrisHeicOutput(boolean value) {
diff --git a/app/src/main/res/values/default_prefs.xml b/app/src/main/res/values/default_prefs.xml
index f1469ac1e85cf5e8311dbb054877a891dab222bf..b8a858d3401fa8a1282d072a3a8180a1ab519ea6 100644
--- a/app/src/main/res/values/default_prefs.xml
+++ b/app/src/main/res/values/default_prefs.xml
@@ -13,7 +13,7 @@
     <bool name="pref_system_nr_default">false</bool>
     <bool name="pref_disable_align_default">false</bool>
     <bool name="pref_hdrx_nr_default">true</bool>
-    <bool name="pref_highlight_compression_default">true</bool>
+    <bool name="pref_highlight_compression_default">false</bool>
     <string name="pref_iris_luma_denoise_default" translatable="false">0.0</string>
     <string name="pref_iris_chroma_denoise_default" translatable="false">1.0</string>
     <string name="pref_iris_saturation_default" translatable="false">1.0</string>
diff --git a/app/src/main/res/xml/preferences.xml b/app/src/main/res/xml/preferences.xml
index 47f2483e1c32ff26bad212c858e1b447a78a7b72..0aa47896ff2532a2edce6bc0f12567bef93efdc2 100644
--- a/app/src/main/res/xml/preferences.xml
+++ b/app/src/main/res/xml/preferences.xml
@@ -14,7 +14,6 @@
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_photo_key" ns0:title="@string/photo_settings">
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="@string/pref_frame_count_key" ns0:layout="@layout/preference_seekbar" ns0:title="@string/frame_count" ns0:defaultValue="@string/pref_framecount_default" ns1:maxValue="50" ns1:minValue="1" ns0:icon="@drawable/ic_burst_mode_black_24dp" />
-        <com.particlesdevs.photoncamera.ui.settings.custompreferences.ManagedSwitchPreference ns0:key="@string/pref_highlight_compression_key" ns0:defaultValue="@bool/pref_highlight_compression_default" ns0:layout="@layout/preference_with_margin" ns0:title="@string/highlight_compression" ns0:summary="@string/highlight_compression_summary" ns0:icon="@drawable/ic_tune_black_24dp" />
     </PreferenceCategory>
     <PreferenceCategory ns0:layout="@layout/preference_category_layout" ns0:key="@string/pref_category_video_key" ns0:title="@string/video_settings">
         <ListPreference ns0:layout="@layout/preference_with_margin" ns0:key="@string/pref_video_resolution_key" ns0:title="@string/video_resolution" ns1:useSimpleSummaryProvider="true" ns0:icon="@drawable/ic_saturation" ns0:entries="@array/video_resolution_entries" ns0:entryValues="@array/video_resolution_entryvalues" ns0:defaultValue="@string/pref_video_resolution_default" />
@@ -38,7 +37,6 @@
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_luma_denoise_v2" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_luma_denoise" ns0:summary="@string/iris_luma_denoise_summary" ns0:defaultValue="@string/pref_iris_luma_denoise_default" ns1:maxValue="2.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_grain_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_chroma_denoise" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_chroma_denoise" ns0:summary="@string/iris_chroma_denoise_summary" ns0:defaultValue="@string/pref_iris_chroma_denoise_default" ns1:maxValue="2.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_gradient_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_saturation" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_saturation" ns0:summary="@string/iris_saturation_summary" ns0:defaultValue="@string/pref_iris_saturation_default" ns1:maxValue="2.0" ns1:minValue="0.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_saturation" />
-        <com.particlesdevs.photoncamera.ui.settings.custompreferences.ManagedSwitchPreference ns0:key="pref_iris_local_laplacian_tone" ns0:defaultValue="true" ns0:layout="@layout/preference_with_margin" ns0:title="@string/iris_local_laplacian_tone" ns0:summary="@string/iris_local_laplacian_tone_summary" ns0:icon="@drawable/ic_tune_black_24dp" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_exposure_ev" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_exposure" ns0:summary="" ns0:defaultValue="@string/pref_iris_exposure_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_saturation" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_shadows" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_shadows" ns0:summary="" ns0:defaultValue="@string/pref_iris_shadows_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_compressor" />
         <com.particlesdevs.photoncamera.ui.settings.custompreferences.UniversalSeekBarPreference ns0:key="pref_iris_contrast" ns0:layout="@layout/preference_seekbar" ns0:title="@string/iris_contrast" ns0:summary="" ns0:defaultValue="@string/pref_iris_contrast_default" ns1:maxValue="1.0" ns1:minValue="-1.0" ns1:isFloat="true" ns1:stepPerUnit="10" ns0:icon="@drawable/ic_saturation" />
diff --git a/app/version.properties b/app/version.properties
index c16052c9f04caf997c1d3723c3c6000a132d6bcc..05fdfeffa79a53669759b39d7205e4e6baaa7621 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726703
-VERSION_BUILD=26703
+VERSION_NAME=0.9726704
+VERSION_BUILD=26704
