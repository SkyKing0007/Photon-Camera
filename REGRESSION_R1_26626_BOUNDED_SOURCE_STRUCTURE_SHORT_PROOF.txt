PHOTON 26626 R1 PERMANENT REGRESSIONS — BOUNDED SOURCE STRUCTURE + SHORT PROOF

AUTHORITY
Runtime base: successful 26625 R3 compiled candidate, commit 50764653ecd67e62b303ab0bf27325dd82ac4239, Actions run 34552875046, artifact 10181524325.
Verification mechanics: successful 26624 R1 direct compiler/build sequence, commit d0d48ab26006a868baa8525a0b81aad65939fac3, Actions run 34525392337.

PERMANENT REGRESSION 1 — DO NOT MOVE TO A SIMPLE REMAP-LEVEL SOURCE BLEND
During 26626 implementation, an early remap-level formulation looked compatible with the prior synthetic design but replayed against the implemented equations could produce small tone reversals at high display gain.
Invariant: successful 26625 must be fully reconstructed as the canonical baseline before the bounded source-domain preservation difference is applied at the final full-resolution Local-Laplacian map.
Invariant: do not claim simulation parity from formula intent alone; implemented equations must replay the monotonic-ramp conditions.

PERMANENT REGRESSION 2 — DO NOT USE A SINGLE COARSE/ONE-SIDED EDGE TEST
A coarse-distance coherence approximation could miss thin dark branches/stripes because two distant samples could agree while a narrow material boundary sat between them.
Invariant: the preservation path must require balanced opposite-side source structure / curvature support at Local-Laplacian scales and fail closed on flat bright areas beside thin branches and on one-sided material steps.
Invariant: unsupported structure falls back to exact successful-26625 appearance; no semantic ceiling/tree/sky detector is allowed.

PERMANENT REGRESSION 3 — PRESERVE SOURCE, NEVER INVENT CONTRAST
The correction may recover only a bounded fraction of structure already present in source-domain luminance.
Invariant: maximum preservation strength is IRIS_26626_SOURCE_PRESERVATION_MAX=0.30 multiplied by the existing adaptive-enable and existing 26623 highlight pressure.
Invariant: per-pixel structural allowance is capped by measured source structure; no sharpening/local-contrast multiplier may exceed source structure.
Invariant: lower-body source guide <=0.65 remains the successful-26625 baseline; preservation ramps in only at 0.65..0.72.
Invariant: preservation fades out at source guide 0.98..1.05; true >1 HDR headroom remains owned by existing UHDR/headroom behavior.

PERMANENT REGRESSION 4 — SUPER RES MUST NOT GAIN A SECOND TONE OWNER
Invariant: Super Res ON and OFF consume the same final Motion V2 Local-Laplacian appearance map.
Invariant: true2x/SR reconstruction, material-edge/tree-sky halo protection and publication bytes are protected unless a future intentionally-scoped SR change replaces them.
Invariant: enabling Super Res may add spatial detail/resolution only; it must not select a separate 26626 tone/highlight path.

PERMANENT REGRESSION 5 — SHORT PROOF MUST REMAIN READ-ONLY
Invariant: all 45 embedded Sabre shaders inherited from successful 26625 remain byte-identical.
Invariant: new 26626 diagnostic shaders cannot authorize trust, change flow, weights, clipping, merge, Resolve or VGN.
Invariant: diagnostics distinguish component membership/untrusted/barrier-frontier evidence and measure whether the unchanged final SHORT merge changes the normalized common accumulator input.
Invariant: accumulator-input change is NOT equivalent to resolveEffective or outputPreserved. Logs must not overclaim either state.

PERMANENT REGRESSION 6 — BUILD 26625 FALSE-GREEN MUST NOT RECUR
26625 R2 Actions run 34552316575 incorrectly invoked the build script with --local-prebuild and uploaded a 7,797-byte proof-only artifact with no APK.
Invariant: GitHub Actions must invoke build_26626_r1_bounded_source_structure_short_proof.sh directly with NO --local-prebuild argument.
Invariant: the build script itself must reach real pinned GLSL, real Kotlin/Java, both NDK ABIs, patch proof, PRE-BUILD, full assemble, exactly-one-APK and post-build invariance before returning success in Actions.
Invariant: no R2/R3 wrapper, git replace, git replace --graft or carrier projection is permitted for 26626.

PERMANENT REGRESSION 7 — SUCCESSFUL 26624 MECHANICS ARE THE DIRECT VERIFICATION TEMPLATE
Invariant: before Actions source writes, diff-audit the 26626 build/workflow/transform against exact successful 26624 mechanics.
Invariant order: real GLSL -> authority-seeded live candidate -> Kotlin/Java -> both NDK ABIs -> deterministic patch proof -> PRE-BUILD -> :app:assembleDebug -> exactly one APK -> post-build candidate/protected/DNG/native/vendor invariance -> deterministic candidate export.
Invariant: candidate-first transform isolation remains enabled with GIT_CEILING_DIRECTORIES so repository/container/generated files cannot contaminate the canonical compiled candidate.

PERMANENT REGRESSION 8 — RUNTIME SOURCE SCOPE
Exact runtime allowlist is five files only:
app/src/main/assets/shaders/motionv2/local_laplacian_remap_26621.glsl
app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
app/version.properties
Unexpected app/src/** differences fail. app/build/** and app/.cxx/** never count as runtime source changes. Repository-only scaffolding absent from compiled-candidate authority never counts as runtime change.

STATUS
These regressions are packaged proof. Before GitHub Actions succeeds, real compiler/build status remains NOT RUN locally / PREPARED-UPLOAD-READY only.
