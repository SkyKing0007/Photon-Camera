name: Build 26477 Strict Wronski Authority V3

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26477_strict_wronski_authority_v3.sh
      - transform_26477_strict_wronski_authority_v3.py
      - .github/workflows/build-26477-strict-wronski-authority-v3.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26477 V3 strict Wronski authority
    runs-on: ubuntu-latest
    timeout-minutes: 50

    steps:
      - name: Checkout exact experimental branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard V3 infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "316a28f078bad14951be3608317c74d1fc923901" || {
            echo "26477 V3 infrastructure commit must be direct child of failed V2 infrastructure commit"
            git log -5 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26477 V3 infrastructure commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26477 V3 infrastructure lineage PASS"

      - name: Run guarded strict-authority V3 build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26477_strict_wronski_authority_v3.sh
          ./build_26477_strict_wronski_authority_v3.sh

      - name: Upload 26477 V3 audit patches and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26477-strict-wronski-authority-v3
          if-no-files-found: error
          path: |
            build_26477_outputs/**
            IrisCamera-0.9726477-26477-strict-wronski-authority-debug.apk
