name: Build 26472 Wronski Reference SDR-Authoritative UHDR V4

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26472_wronski_reference_sdr_authoritative_uhdr_v4.sh
      - .github/workflows/build-26472-wronski-reference-sdr-authoritative-uhdr-v4.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26472 SDR-authoritative V4 build
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard V4 corrective infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "e8386bf8b9a4a1cd7839d0f612fd2f3e20000717" || {
            echo "26472 V4 corrective commit must be directly based on V3 infrastructure commit"
            git log -4 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26472 V4 corrective commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26472 V4 corrective workflow lineage gate PASS"

      - name: Run guarded 26472 SDR-authoritative V4 operation
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26472_wronski_reference_sdr_authoritative_uhdr_v4.sh
          ./build_26472_wronski_reference_sdr_authoritative_uhdr_v4.sh

      - name: Upload 26472 V4 audit and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26472-wronski-reference-sdr-authoritative-uhdr-v4
          if-no-files-found: error
          path: |
            build_26472_outputs/**
            IrisCamera-0.9726472-26472-wronski-reference-sdr-authoritative-uhdr-debug.apk
