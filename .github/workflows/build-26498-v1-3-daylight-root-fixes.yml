name: Build 26498 V1.3 Daylight Root Fixes + Shadow Aux

on:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - transform_26498_v1_3_daylight_root_fixes.py
      - validate_26498_v1_3.py
      - build_26498_v1_3_daylight_root_fixes.sh
      - .github/workflows/build-26498-v1-3-daylight-root-fixes.yml
      - 26498_V1_3_PACKAGE_HASHES.sha256
      - 26498_V1_3_HANDOFF_README.txt
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout guarded experimental branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require experimental-clean-photon-rebuild
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = "experimental-clean-photon-rebuild"
          test "${GITHUB_REF_NAME}" != "dev"

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install GLSL validator
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y glslang-tools
          glslangValidator --version

      - name: Verify V1.3 handoff identities
        shell: bash
        run: |
          set -euo pipefail
          echo 'a1336e124e6777b91597af30ccb146724b1204a254cf10ce2bdd911f631fb4a9  transform_26498_v1_3_daylight_root_fixes.py' | sha256sum -c -
          echo 'a65fb429b19d32650c66c50da46846aa379b8556fe09411a5ddb2093202543d7  validate_26498_v1_3.py' | sha256sum -c -
          echo '41197e3a66d9a991b89be292e07f1d3449ff263aeb825a43673604c5913bf001  build_26498_v1_3_daylight_root_fixes.sh' | sha256sum -c -
          python -m py_compile transform_26498_v1_3_daylight_root_fixes.py validate_26498_v1_3.py
          bash -n build_26498_v1_3_daylight_root_fixes.sh

      - name: Strict guarded source reconstruction and build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x build_26498_v1_3_daylight_root_fixes.sh
          ./build_26498_v1_3_daylight_root_fixes.sh

      - name: Upload 26498 V1.3 APK
        uses: actions/upload-artifact@v4
        with:
          name: IrisCamera-0.9726498-26498-v1.3-daylight-root-fixes-shadow-aux
          path: IrisCamera-0.9726498-26498-v1.3-daylight-root-fixes-shadow-aux-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Upload strict build proof
        uses: actions/upload-artifact@v4
        with:
          name: 26498-v1.3-build-proof
          path: build_26498_v1_3_outputs/
          if-no-files-found: error
          retention-days: 14
