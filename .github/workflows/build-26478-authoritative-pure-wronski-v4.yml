name: Build 26478 Authoritative Pure Wronski V4

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Guarded authoritative 26478 V4
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      - name: Checkout exact experimental branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard and syntax-check 26478 V4 infrastructure
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "62fed09aa61ac963f951fd39b1a62be870d75654" || {
            echo "26478 V4 commit must be direct child of exact 62fed09 correction HEAD"
            git log -6 --oneline
            exit 1
          }
          test "$(git rev-parse HEAD~2)" = "a5fa756f8b5cf1101e807b776728d08225f3e53e" || {
            echo "26478 V4 chain missing exact first 26478 infrastructure commit"
            git log -6 --oneline
            exit 1
          }
          test "$(git rev-parse HEAD~3)" = "be24c16cb091e753852b900384dbde6f8d32744e" || {
            echo "26478 V4 chain must descend exactly from successful 26477 V3"
            git log -6 --oneline
            exit 1
          }

          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26478 V4 infrastructure commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }

          expected_current="$(printf '%s\n' \
            '.github/workflows/build-26478-authoritative-pure-wronski-v4.yml' \
            'build_26478_authoritative_pure_wronski_v4.sh' \
            'transform_26478_pure_wronski_highlight_safe_v1.py' | sort)"
          actual_current="$(git diff --name-only HEAD^ HEAD | sort)"
          test "$actual_current" = "$expected_current" || {
            echo "26478 V4 commit must contain exactly the core transform plus V4 wrapper/workflow"
            echo "ACTUAL:"
            printf '%s\n' "$actual_current"
            echo "EXPECTED:"
            printf '%s\n' "$expected_current"
            exit 1
          }

          # Across all three 26478 infrastructure-only commits, app source must remain untouched.
          test "$(git diff --name-only be24c16cb091e753852b900384dbde6f8d32744e HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26478 infrastructure chain changed app source directly"
            git diff --name-only be24c16cb091e753852b900384dbde6f8d32744e HEAD -- app/src/main app/version.properties
            exit 1
          }

          expected_combined="$(printf '%s\n' \
            '.github/workflows/build-26478-authoritative-pure-wronski-v3.yml' \
            'build_26478_authoritative_pure_wronski_v3.sh' \
            'transform_26478_speaker_support_diagnostic_v1.py' \
            '.github/workflows/build-26478-authoritative-pure-wronski-v4.yml' \
            'build_26478_authoritative_pure_wronski_v4.sh' \
            'transform_26478_pure_wronski_highlight_safe_v1.py' | sort)"
          actual_combined="$(git diff --name-only be24c16cb091e753852b900384dbde6f8d32744e HEAD | sort)"
          test "$actual_combined" = "$expected_combined" || {
            echo "final 26478 infrastructure chain is not exactly the six intended infrastructure files"
            echo "ACTUAL:"
            printf '%s\n' "$actual_combined"
            echo "EXPECTED:"
            printf '%s\n' "$expected_combined"
            exit 1
          }

          bash -n ./build_26478_authoritative_pure_wronski_v4.sh
          python3 -m py_compile ./transform_26478_pure_wronski_highlight_safe_v1.py
          python3 -m py_compile ./transform_26478_speaker_support_diagnostic_v1.py
          echo "26478 V4 exact infrastructure lineage + syntax PASS"

      - name: Run guarded 26478 build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26478_authoritative_pure_wronski_v4.sh
          ./build_26478_authoritative_pure_wronski_v4.sh

      - name: Upload 26478 audit patches and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26478-authoritative-pure-wronski-v4
          if-no-files-found: error
          path: |
            build_26478_outputs/**
            IrisCamera-0.9726478-26478-pure-wronski-highlight-safe-speaker-diag-debug.apk
