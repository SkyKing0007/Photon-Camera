name: Build 26478 Authoritative Pure Wronski V7

on:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26478_authoritative_pure_wronski_v7.sh
      - .github/workflows/build-26478-authoritative-pure-wronski-v7.yml

permissions:
  contents: write

jobs:
  build:
    name: Guarded authoritative 26478 V7
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      - name: Checkout exact experimental branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard exact V7 infrastructure scope
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "c8beb8882370d66b85c5e3dbe13c69096aef5202"
          test "$(git rev-parse HEAD~2)" = "fbf480d37c5213f72697170ae356493570ab3cc4"
          test "$(git rev-parse HEAD~3)" = "87de69aff3d927be9c031c2fce476dffe88e4d71"
          test "$(git rev-parse HEAD~4)" = "62fed09aa61ac963f951fd39b1a62be870d75654"
          test "$(git rev-parse HEAD~5)" = "a5fa756f8b5cf1101e807b776728d08225f3e53e"
          test "$(git rev-parse HEAD~6)" = "be24c16cb091e753852b900384dbde6f8d32744e"

          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26478 V7 infrastructure commit changed app source directly"
            exit 1
          }

          expected_current="$(printf '%s\n' \
            '.github/workflows/build-26478-authoritative-pure-wronski-v7.yml' \
            'build_26478_authoritative_pure_wronski_v7.sh' | sort)"
          actual_current="$(git diff --name-only HEAD^ HEAD | sort)"
          test "$actual_current" = "$expected_current" || {
            echo "26478 V7 commit must contain exactly the two V7 infrastructure files"
            echo "ACTUAL:"
            printf '%s\n' "$actual_current"
            echo "EXPECTED:"
            printf '%s\n' "$expected_current"
            exit 1
          }

          test "$(git diff --name-only be24c16cb091e753852b900384dbde6f8d32744e HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26478 infrastructure chain changed committed app source"
            exit 1
          }

          test -f ./transform_26478_pure_wronski_highlight_safe_v1.py
          test -f ./transform_26478_speaker_support_diagnostic_v2.py
          bash -n ./build_26478_authoritative_pure_wronski_v7.sh
          python3 -m py_compile ./transform_26478_pure_wronski_highlight_safe_v1.py
          python3 -m py_compile ./transform_26478_speaker_support_diagnostic_v2.py
          echo "26478 V7 infrastructure scope + syntax PASS"

      - name: Create exact pre-V7 backup branch
        shell: bash
        run: |
          set -euo pipefail
          BACKUP="backup-26478-v6-before-26478-v7-scoped-diffcheck"
          TARGET="c8beb8882370d66b85c5e3dbe13c69096aef5202"
          remote="$(git ls-remote origin "refs/heads/$BACKUP" | awk '{print $1}')"
          if [ -n "$remote" ]; then
            test "$remote" = "$TARGET" || {
              echo "Existing V7 backup branch points to wrong commit: $remote"
              exit 1
            }
          else
            git push origin "$TARGET:refs/heads/$BACKUP"
          fi
          remote="$(git ls-remote origin "refs/heads/$BACKUP" | awk '{print $1}')"
          test "$remote" = "$TARGET"
          echo "V7 backup branch exact pre-modification state PASS"

      - name: Run guarded 26478 V7 build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26478_authoritative_pure_wronski_v7.sh
          ./build_26478_authoritative_pure_wronski_v7.sh

      - name: Upload 26478 V7 audit patches and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26478-authoritative-pure-wronski-v7
          if-no-files-found: error
          path: |
            build_26478_outputs/**
            IrisCamera-0.9726478-26478-pure-wronski-highlight-safe-speaker-diag-debug.apk
