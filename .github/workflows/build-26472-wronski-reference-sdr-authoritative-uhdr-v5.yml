name: Build 26472 Wronski Reference SDR-Authoritative UHDR V5

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26472_wronski_reference_sdr_authoritative_uhdr_v5.sh
      - .github/workflows/build-26472-wronski-reference-sdr-authoritative-uhdr-v5.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26472 SDR-authoritative V5 build
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard V5 corrective infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "7dd8bcf37461d966bfd320e10d41ef5727772cf9" || {
            echo "26472 V5 corrective commit must be directly based on V4 infrastructure commit"
            git log -4 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26472 V5 corrective commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26472 V5 corrective workflow lineage gate PASS"

      - name: Run guarded 26472 SDR-authoritative V5 operation
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26472_wronski_reference_sdr_authoritative_uhdr_v5.sh
          ./build_26472_wronski_reference_sdr_authoritative_uhdr_v5.sh

      - name: Upload 26472 V5 audit and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26472-wronski-reference-sdr-authoritative-uhdr-v5
          if-no-files-found: error
          path: |
            build_26472_outputs/**
            IrisCamera-0.9726472-26472-wronski-reference-sdr-authoritative-uhdr-debug.apk
