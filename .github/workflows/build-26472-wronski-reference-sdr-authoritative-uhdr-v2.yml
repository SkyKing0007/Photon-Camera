name: Build 26472 Wronski Reference SDR-Authoritative UHDR V2

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26472_wronski_reference_sdr_authoritative_uhdr_v2.sh
      - .github/workflows/build-26472-wronski-reference-sdr-authoritative-uhdr-v2.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26472 SDR-authoritative build
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard corrective infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "0b7e406cd44a8792596fecb93deace0740459622" || {
            echo "26472 V2 corrective commit must be directly based on first 26472 infrastructure commit"
            git log -4 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26472 V2 corrective commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26472 V2 corrective workflow lineage gate PASS"

      - name: Run guarded 26472 SDR-authoritative operation
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26472_wronski_reference_sdr_authoritative_uhdr_v2.sh
          ./build_26472_wronski_reference_sdr_authoritative_uhdr_v2.sh

      - name: Upload 26472 audit and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26472-wronski-reference-sdr-authoritative-uhdr
          if-no-files-found: error
          path: |
            build_26472_outputs/**
            IrisCamera-0.9726472-26472-wronski-reference-sdr-authoritative-uhdr-debug.apk
