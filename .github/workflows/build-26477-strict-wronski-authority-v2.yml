name: Build 26477 Strict Wronski Authority V2

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26477_strict_wronski_authority_v2.sh
      - transform_26477_strict_wronski_authority_v2.py
      - .github/workflows/build-26477-strict-wronski-authority-v2.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26477 V2 strict Wronski authority
    runs-on: ubuntu-latest
    timeout-minutes: 50

    steps:
      - name: Checkout exact experimental branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard V2 infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "739a6afc4bfeafc5f3d6a4cb731b7c0b203c1938" || {
            echo "26477 V2 infrastructure commit must be direct child of failed V1 infrastructure commit"
            git log -5 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26477 V2 infrastructure commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26477 V2 infrastructure lineage PASS"

      - name: Run guarded strict-authority V2 build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26477_strict_wronski_authority_v2.sh
          ./build_26477_strict_wronski_authority_v2.sh

      - name: Upload 26477 V2 audit patches and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26477-strict-wronski-authority-v2
          if-no-files-found: error
          path: |
            build_26477_outputs/**
            IrisCamera-0.9726477-26477-strict-wronski-authority-debug.apk
