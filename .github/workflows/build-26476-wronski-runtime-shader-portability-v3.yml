name: Build 26476 Wronski Runtime Shader Portability V3

on:
  workflow_dispatch:
  push:
    branches:
      - experimental-clean-photon-rebuild
    paths:
      - build_26476_wronski_runtime_shader_portability_v3.sh
      - .github/workflows/build-26476-wronski-runtime-shader-portability-v3.yml

permissions:
  contents: read

jobs:
  build:
    name: Guarded 26476 Wronski runtime shader portability V3
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: experimental-clean-photon-rebuild
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Guard 26476 V3 infrastructure lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "experimental-clean-photon-rebuild"
          test "$(git rev-parse HEAD^)" = "cd3aecad42726c3efe42de616762b1a778441b5e" || {
            echo "26476 V3 infrastructure commit must be directly based on failed 26476 V2 infrastructure commit"
            git log -4 --oneline
            exit 1
          }
          test "$(git diff --name-only HEAD^ HEAD -- app/src/main app/version.properties | wc -l)" -eq 0 || {
            echo "26476 V3 infrastructure commit changed app source directly"
            git diff --name-only HEAD^ HEAD
            exit 1
          }
          echo "26476 V3 workflow lineage gate PASS"

      - name: Run guarded 26476 V3 build
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./build_26476_wronski_runtime_shader_portability_v3.sh
          ./build_26476_wronski_runtime_shader_portability_v3.sh

      - name: Upload 26476 V3 audit and APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photon-26476-wronski-runtime-shader-portability-v3
          if-no-files-found: error
          path: |
            build_26476_outputs/**
            IrisCamera-0.9726476-26476-wronski-runtime-shader-portability-debug.apk
