diff --git a/app/src/main/assets/shaders/preview/main_fs.glsl b/app/src/main/assets/shaders/preview/main_fs.glsl
index 5468b55256930573b310636e1e7a5c66e0ca9816..42ce21ae4108c40297ad6de0b27bb5b188593076 100644
--- a/app/src/main/assets/shaders/preview/main_fs.glsl
+++ b/app/src/main/assets/shaders/preview/main_fs.glsl
@@ -40,8 +40,12 @@ void main() {
      * 26661 full linear multiplier. The map is monotone, maps display white to display white, and
      * rescales RGB uniformly, so it cannot create the blown preview highlights seen in 26661.
      */
-    float iris26662Gain = max(iris26662ReferencePreviewGain, 1.0);
-    if (iris26662Gain > 1.0001) {
+    /* IRIS_26709_SIGNED_FRAME_EXACT_PREVIEW_PRESENTATION
+     * The physical adaptive NORMAL epoch may be darker or brighter than HAL. Apply the same
+     * white-anchored rational map for either direction so only Iris' hidden sensor offset is
+     * cancelled; ordinary HAL scene adaptation remains visible exactly as in 26708. */
+    float iris26662Gain = clamp(iris26662ReferencePreviewGain, 0.3535534, 2.8284271);
+    if (abs(iris26662Gain - 1.0) > 0.0001) {
         vec3 encoded = clamp(color.rgb, vec3(0.0), vec3(1.0));
         vec3 lowLinear = encoded / 12.92;
         vec3 highLinear = pow((encoded + 0.055) / 1.055, vec3(2.4));
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
index 0606852c120fa96f7b7af4319ee637c404f6d100..65b353a1e4f20d64eedb9f86ce48f0cb2c922b5e 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java
@@ -1812,8 +1812,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         synchronized (mMotion26662PreviewResultLock) {
             int slot = mMotion26662PreviewResultWriteIndex % MOTION_26662_PREVIEW_RESULT_HISTORY;
             mMotion26662PreviewResultTimestampNs[slot] = sensorTimestampNs;
-            mMotion26662PreviewResultProtectionEv[slot] = Math.max(0.0f,
-                    Math.min(MOTION_26661_MAX_PROTECTION_EV, protectionEv));
+            mMotion26662PreviewResultProtectionEv[slot] = Math.max(-MOTION_26709_MAX_RELATIVE_EV,
+                    Math.min(MOTION_26709_MAX_RELATIVE_EV, protectionEv));
             mMotion26662PreviewResultWriteIndex++;
         }
     }
@@ -1867,6 +1867,41 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private volatile int mMotion26708PublishedClass = 0;
     private volatile long mMotion26708PublishedTimestampNs = 0L, mMotion26708PublishedGeneration = -1L;
     private volatile double mMotion26708PublishedExposureEnergy = Double.NaN;
+
+    /* IRIS_26709_ADAPTIVE_EQUAL_EXPOSURE_REFERENCE_OWNER
+     * Keep 26708's HAL/user appearance baseline and physical-reframe state machine, but let a
+     * complete same-exposure nine-phase RAW cycle choose one signed AE-compensation offset around
+     * that immutable baseline. The controller never rebases on its own adjusted exposure. The
+     * offset may be negative for highlight headroom or positive when HAL left useful RAW headroom.
+     * Once the target is solved it is locked for the rolling NORMAL epoch until a physical reframe. */
+    private static final float MOTION_26709_REFERENCE_SIGNAL_TARGET = 0.92f;
+    private static final float MOTION_26709_CLIP_SIGNAL = 0.985f;
+    private static final float MOTION_26709_MAX_RELATIVE_EV = 1.50f;
+    private static final float MOTION_26709_CLIPPED_PROBE_MIN_EV = 0.55f;
+    private static final float MOTION_26709_CLIPPED_PROBE_MAX_EV = 1.10f;
+    private static final float MOTION_26709_SOLVE_DEADBAND_EV = 0.08f;
+    private static final float MOTION_26709_MAX_SOLVE_STEP_EV = 1.00f;
+    private static final long MOTION_26709_METER_FRESH_NS = 450_000_000L;
+    private boolean mMotion26709HalBaselineReady = false;
+    private boolean mMotion26709ReferenceSettled = false;
+    private boolean mMotion26709ReleasePending = false;
+    private int mMotion26709HalBaseCompSteps = 0;
+    private int mMotion26709TargetCompSteps = 0;
+    private float mMotion26709AeStepEv = 0.0f;
+    private double mMotion26709HalBaselineEnergy = Double.NaN;
+    private long mMotion26709HalBaselineExposureNs = 0L;
+    private int mMotion26709HalBaselineIso = 0;
+    private long mMotion26709LastConsumedMeterTimestampNs = 0L;
+    private volatile float mMotion26709PublishedP995 = Float.NaN;
+    private volatile int mMotion26709PublishedHighlightSamples = 0;
+    private volatile int mMotion26709PublishedCoherentCells = 0;
+    private volatile int mMotion26709PublishedBroadVotes = 0;
+    private volatile int mMotion26709PublishedMixedVotes = 0;
+    private volatile int mMotion26709PublishedCompactVotes = 0;
+    private volatile float mMotion26709PublishedFloorFraction = Float.NaN;
+    private volatile float mMotion26709PublishedShadowFraction = Float.NaN;
+    private volatile float mMotion26709PublishedMeanSignal = Float.NaN;
+    private volatile float mMotion26709FrozenCaptureProtectionEv = 0.0f;
     private static final int MOTION_26496_MIN_HIGHLIGHT_SAMPLES = 2;
     private static final long MOTION_26496_HIGHLIGHT_EVIDENCE_HOLD_NS = 400_000_000L;
 
@@ -2613,13 +2648,22 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         resetMotion26708PhaseAccumulator();
         mMotion26708PublishedClass = 0; mMotion26708PublishedTimestampNs = 0L;
         mMotion26708PublishedGeneration = -1L; mMotion26708PublishedExposureEnergy = Double.NaN;
+        mMotion26709PublishedP995 = Float.NaN;
+        mMotion26709PublishedHighlightSamples = 0;
+        mMotion26709PublishedCoherentCells = 0;
+        mMotion26709PublishedBroadVotes = 0;
+        mMotion26709PublishedMixedVotes = 0;
+        mMotion26709PublishedCompactVotes = 0;
+        mMotion26709PublishedFloorFraction = Float.NaN;
+        mMotion26709PublishedShadowFraction = Float.NaN;
+        mMotion26709PublishedMeanSignal = Float.NaN;
         Log.i(TAG, "IRIS_26708_HIGHLIGHT_CLASSIFIER_INVALIDATED reason=" + reason
                 + " generation=" + mIris26548CameraHealthGeneration);
     }
     private void accumulateMotion26708HighlightPhase(long timestampNs, int phase,
             int highlightSamples, int coherentCells, int peakQuadPhases, float rawP995,
             boolean broad, boolean mixed, boolean compact) {
-        if (!mMotion26680PreviewLatched || mZslCapturing || timestampNs <= 0L
+        if (!mMotion26709HalBaselineReady || mZslCapturing || timestampNs <= 0L
                 || !Double.isFinite(mMotion26680LastExposureEnergy) || mMotion26680LastExposureEnergy <= 0.0) return;
         final long generation = mIris26548CameraHealthGeneration; final double energy = mMotion26680LastExposureEnergy;
         boolean incompatible = mMotion26708ClassifierGeneration != generation
@@ -2643,12 +2687,22 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mMotion26708PublishedClass = !any ? 0 : (strong ? 2 : 1);
         mMotion26708PublishedTimestampNs = timestampNs; mMotion26708PublishedGeneration = generation;
         mMotion26708PublishedExposureEnergy = energy;
-        Log.i(TAG, "IRIS_26708_HIGHLIGHT_CLASS_PUBLISHED class=" + mMotion26708PublishedClass
+        mMotion26709PublishedP995 = mMotion26708P995Max;
+        mMotion26709PublishedHighlightSamples = mMotion26708HighlightSamplesSum;
+        mMotion26709PublishedCoherentCells = mMotion26708CoherentCellsSum;
+        mMotion26709PublishedBroadVotes = mMotion26708BroadVotes;
+        mMotion26709PublishedMixedVotes = mMotion26708MixedVotes;
+        mMotion26709PublishedCompactVotes = mMotion26708CompactVotes;
+        mMotion26709PublishedFloorFraction = mMotion26380RawFloorFraction;
+        mMotion26709PublishedShadowFraction = mMotion26380RawShadowFraction;
+        mMotion26709PublishedMeanSignal = mMotion26380RawMeanSignal;
+        Log.i(TAG, "IRIS_26709_REFERENCE_METER_PUBLISHED class=" + mMotion26708PublishedClass
                 + " completePhases=9 generation=" + generation + " exposureEnergy=" + energy
                 + " highlightSamplesSum=" + mMotion26708HighlightSamplesSum + " coherentCellsSum=" + mMotion26708CoherentCellsSum
                 + " peakQuadMax=" + mMotion26708PeakQuadMax + " p995Max=" + mMotion26708P995Max
                 + " broadVotes=" + mMotion26708BroadVotes + " mixedVotes=" + mMotion26708MixedVotes
-                + " compactVotes=" + mMotion26708CompactVotes + " previewExposureWrite=false");
+                + " compactVotes=" + mMotion26708CompactVotes
+                + " signedAdaptiveReference=true previewPresentationFrameMatched=true");
         resetMotion26708PhaseAccumulator();
     }
     private float motion26708FrozenShortProtectionEv(long shutterTimestampNs, long normalExposureNs, int normalIso) {
@@ -2677,6 +2731,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         mMotion26680LastExposureEnergy = Double.NaN;
         mMotion26680LatchGeneration = mIris26548CameraHealthGeneration;
         clearMotion26708HighlightClassifier(reason);
+        mMotion26709HalBaselineReady = false;
+        mMotion26709ReferenceSettled = false;
+        mMotion26709ReleasePending = false;
+        mMotion26709HalBaseCompSteps = 0;
+        mMotion26709TargetCompSteps = 0;
+        mMotion26709AeStepEv = 0.0f;
+        mMotion26709HalBaselineEnergy = Double.NaN;
+        mMotion26709HalBaselineExposureNs = 0L;
+        mMotion26709HalBaselineIso = 0;
+        mMotion26709LastConsumedMeterTimestampNs = 0L;
+        mMotion26709FrozenCaptureProtectionEv = 0.0f;
         /* IRIS_26705_NORMAL_AE_SHORT_HEADROOM_OWNER
          * No 26701 transaction survives: NORMAL compensation is never changed by HDR logic. */
 
@@ -2748,6 +2813,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         return Math.max(0.0f, Math.min(MOTION_26705_SHORT_EXTRA_MAX_EV, extraEv));
     }
 
+    /* IRIS_26709_ADAPTIVE_EQUAL_EXPOSURE_REFERENCE_OWNER
+     * Preserve 26708's user-visible viewfinder behavior while changing only the physical NORMAL
+     * exposure behind it. HAL first converges at the user's baseline. A complete same-exposure RAW
+     * meter cycle may then move AE compensation in either direction around that immutable baseline.
+     * Each actual result carries the inverse signed presentation EV to the timestamp-matched preview.
+     * Once the RAW target is solved, AE/AWB are locked exactly like 26708 until physical reframe. */
     private void updateMotion26680StablePreviewAuthority(@NonNull TotalCaptureResult result) {
         if (!isZslMode() || mZslCapturing || burst || isProcessing
                 || mPreviewRequestBuilder == null || mCaptureSession == null
@@ -2769,11 +2840,8 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 || sensorTs == null || sensorTs <= 0L) {
             return;
         }
-
-        /* IRIS_26705_NORMAL_REFERENCE_NEVER_PROTECTED
-         * NORMAL remains HAL/user-AE owned. Frame-matched preview protection is therefore exactly
-         * zero; SHORT headroom is capture-local and must never darken preview presentation. */
-        recordMotion26662PreviewProtection(sensorTs, 0.0f);
+        final double exposureEnergy = (double) exposureNs * (double) iso;
+        mMotion26680LastExposureEnergy = exposureEnergy;
 
         int shakiness = 0;
         try {
@@ -2783,37 +2851,248 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         } catch (Exception ignored) {}
         boolean physicalReframe = shakiness > MOTION_26680_REFRAME_GYRO_THRESHOLD;
 
+        android.util.Range<Integer> aeRange = mCameraCharacteristics.get(
+                CameraCharacteristics.CONTROL_AE_COMPENSATION_RANGE);
+        android.util.Rational aeStep = mCameraCharacteristics.get(
+                CameraCharacteristics.CONTROL_AE_COMPENSATION_STEP);
+        final boolean adaptiveAeAvailable = aeRange != null && aeStep != null
+                && aeStep.floatValue() > 0.0f;
+        Integer resultCompObj = result.get(CaptureResult.CONTROL_AE_EXPOSURE_COMPENSATION);
+        Integer builderCompObj = mPreviewRequestBuilder.get(
+                CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION);
+        int resultCompSteps = resultCompObj != null ? resultCompObj
+                : (builderCompObj != null ? builderCompObj : 0);
+
+        if (mMotion26709ReleasePending && resultCompSteps == mMotion26709HalBaseCompSteps) {
+            recordMotion26662PreviewProtection(sensorTs, 0.0f);
+        } else if (mMotion26709HalBaselineReady && Double.isFinite(mMotion26709HalBaselineEnergy)
+                && mMotion26709HalBaselineEnergy > 0.0 && exposureEnergy > 0.0) {
+            float physicalOffsetEv = (float)(Math.log(
+                    exposureEnergy / mMotion26709HalBaselineEnergy) / Math.log(2.0));
+            recordMotion26662PreviewProtection(sensorTs, -physicalOffsetEv);
+        } else {
+            recordMotion26662PreviewProtection(sensorTs, 0.0f);
+        }
+
+        /* Keep frame-exact inverse presentation active until Camera2 proves the restored user/HAL
+         * compensation has reached the sensor after a physical reframe. This prevents a one-frame
+         * flash between the old adaptive epoch and the new 26708-style HAL convergence epoch. */
+        if (mMotion26709ReleasePending) {
+            if (resultCompSteps != mMotion26709HalBaseCompSteps) return;
+            mMotion26709ReleasePending = false;
+            mMotion26709HalBaselineReady = false;
+            mMotion26709ReferenceSettled = false;
+            mMotion26709AeStepEv = 0.0f;
+            mMotion26709HalBaselineEnergy = Double.NaN;
+            mMotion26709HalBaselineExposureNs = 0L;
+            mMotion26709HalBaselineIso = 0;
+            mMotion26709LastConsumedMeterTimestampNs = 0L;
+            mMotion26680StableFrames = 0;
+            mMotion26680StationaryFrames = 0;
+            Log.i(TAG, "IRIS_26709_REFERENCE_RELEASE_CONFIRMED compSteps=" + resultCompSteps
+                    + " frameMatchedPreviewCompensation=0 newHalConvergence=true");
+            return;
+        }
+
         if (mMotion26680PreviewLatched) {
             if (physicalReframe) {
                 mMotion26680ReframeMotionFrames++;
             } else {
                 mMotion26680ReframeMotionFrames = 0;
             }
-            if (mMotion26680ReframeMotionFrames < MOTION_26680_REFRAME_CONFIRM_FRAMES) return;
+            if (mMotion26680ReframeMotionFrames >= MOTION_26680_REFRAME_CONFIRM_FRAMES) {
+                try {
+                    if (mMotion26680AeLockApplied) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
+                    }
+                    if (mMotion26680AwbLockApplied) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
+                    }
+                    if (mMotion26709HalBaselineReady && adaptiveAeAvailable) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION,
+                                Math.max(aeRange.getLower(), Math.min(aeRange.getUpper(),
+                                        mMotion26709HalBaseCompSteps)));
+                    }
+                    mMotion26680PreviewLatched = false;
+                    mMotion26680AeLockApplied = false;
+                    mMotion26680AwbLockApplied = false;
+                    mMotion26680StableFrames = 0;
+                    mMotion26680StationaryFrames = 0;
+                    mMotion26680ReframeMotionFrames = 0;
+                    mMotion26709ReferenceSettled = false;
+                    mMotion26709ReleasePending = mMotion26709HalBaselineReady;
+                    mMotion26709LastConsumedMeterTimestampNs = 0L;
+                    clearMotion26708HighlightClassifier("physical-reframe");
+                    rebuildPreviewBuilder();
+                    Log.i(TAG, "IRIS_26709_REFERENCE_EPOCH_RELEASE generation="
+                            + mIris26548CameraHealthGeneration
+                            + " shakiness=" + shakiness
+                            + " restoreUserHalCompSteps=" + mMotion26709HalBaseCompSteps
+                            + " subjectMotionCannotUnlock=true viewfinder26708Behavior=true");
+                } catch (IllegalArgumentException | IllegalStateException e) {
+                    Log.w(TAG, "IRIS_26709 adaptive reference release skipped: "
+                            + e.getClass().getSimpleName());
+                }
+                return;
+            }
 
-            try {
-                if (mMotion26680AeLockApplied) {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
+            if (!adaptiveAeAvailable || mMotion26709AeStepEv <= 0.0f) {
+                if (!mMotion26709ReferenceSettled) {
+                    try {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
+                        mMotion26680AeLockApplied = true;
+                        mMotion26709ReferenceSettled = true;
+                        rebuildPreviewBuilder();
+                        Log.i(TAG, "IRIS_26709_REFERENCE_SETTLED adaptiveAeUnavailable=true"
+                                + " fallback=HAL_BASELINE viewfinder26708Behavior=true");
+                    } catch (IllegalArgumentException | IllegalStateException e) {
+                        Log.w(TAG, "IRIS_26709 HAL fallback latch skipped: "
+                                + e.getClass().getSimpleName());
+                    }
                 }
-                if (mMotion26680AwbLockApplied) {
-                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
+                return;
+            }
+
+            if (builderCompObj != null && builderCompObj != mMotion26709TargetCompSteps) {
+                /* A user/UI EV write supersedes Iris. Re-enter 26708-style HAL convergence and
+                 * establish a fresh immutable user baseline instead of treating our prior target
+                 * as the new baseline. */
+                try {
+                    if (mMotion26680AwbLockApplied) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, false);
+                    }
+                    if (mMotion26680AeLockApplied) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
+                    }
+                    mMotion26680PreviewLatched = false;
+                    mMotion26680AwbLockApplied = false;
+                    mMotion26680AeLockApplied = false;
+                    mMotion26680StableFrames = 0;
+                    mMotion26680StationaryFrames = 0;
+                    mMotion26709HalBaselineReady = false;
+                    mMotion26709ReferenceSettled = false;
+                    mMotion26709LastConsumedMeterTimestampNs = 0L;
+                    clearMotion26708HighlightClassifier("user-ev-change");
+                    Log.i(TAG, "IRIS_26709_USER_EV_REBASE requestedSteps=" + builderCompObj
+                            + " previousAdaptiveTarget=" + mMotion26709TargetCompSteps
+                            + " userAuthority=true");
+                } catch (IllegalArgumentException | IllegalStateException ignored) {}
+                return;
+            }
+            if (mMotion26709ReferenceSettled) return;
+
+            if (resultCompSteps != mMotion26709TargetCompSteps) return;
+            if (mMotion26708PublishedTimestampNs <= mMotion26709LastConsumedMeterTimestampNs
+                    || mMotion26708PublishedTimestampNs <= 0L
+                    || mMotion26708PublishedGeneration != mIris26548CameraHealthGeneration) return;
+            long meterAgeNs = Math.abs(sensorTs - mMotion26708PublishedTimestampNs);
+            if (meterAgeNs > MOTION_26709_METER_FRESH_NS
+                    || !Float.isFinite(mMotion26709PublishedP995)
+                    || mMotion26709PublishedP995 <= 0.0f) return;
+            if (Double.isFinite(mMotion26708PublishedExposureEnergy)
+                    && mMotion26708PublishedExposureEnergy > 0.0) {
+                double meterEvMismatch = Math.abs(Math.log(
+                        exposureEnergy / mMotion26708PublishedExposureEnergy) / Math.log(2.0));
+                if (meterEvMismatch > 0.08) return;
+            }
+
+            mMotion26709LastConsumedMeterTimestampNs = mMotion26708PublishedTimestampNs;
+            final float currentOffsetEv = (resultCompSteps - mMotion26709HalBaseCompSteps)
+                    * mMotion26709AeStepEv;
+            final boolean broadOrMixedHdr = mMotion26709PublishedBroadVotes > 0
+                    || mMotion26709PublishedMixedVotes > 0;
+            final boolean meaningfulHdr = broadOrMixedHdr
+                    || (mMotion26709PublishedCompactVotes >= 2
+                        && mMotion26709PublishedCoherentCells >= 2);
+            final boolean meaningfulClip = mMotion26709PublishedP995 >= MOTION_26709_CLIP_SIGNAL
+                    && (mMotion26709PublishedHighlightSamples >= MOTION_26496_MIN_HIGHLIGHT_SAMPLES
+                        || mMotion26709PublishedCoherentCells > 0);
+            /* Never snap an already-established adaptive epoch back to HAL merely because
+             * the protection move successfully removed the classifier trigger. Continue from the
+             * current absolute target and converge by measured RAW signal; this is the anti-pump
+             * invariant for the clipped -> measurable transition. */
+            float desiredOffsetEv = currentOffsetEv;
+            float correctionEv = 0.0f;
+            if (meaningfulHdr || meaningfulClip || Math.abs(currentOffsetEv) > MOTION_26709_SOLVE_DEADBAND_EV) {
+                if (meaningfulClip) {
+                    float clipPressure = motion26368Clamp01(
+                            (mMotion26709PublishedHighlightSamples - 2.0f) / 34.0f);
+                    float probeDownEv = MOTION_26709_CLIPPED_PROBE_MIN_EV
+                            + (MOTION_26709_CLIPPED_PROBE_MAX_EV
+                                - MOTION_26709_CLIPPED_PROBE_MIN_EV) * clipPressure;
+                    correctionEv = -probeDownEv;
+                } else {
+                    correctionEv = (float)(Math.log(
+                            MOTION_26709_REFERENCE_SIGNAL_TARGET
+                                    / Math.max(0.02f, mMotion26709PublishedP995))
+                            / Math.log(2.0));
+                    correctionEv = Math.max(-MOTION_26709_MAX_SOLVE_STEP_EV,
+                            Math.min(MOTION_26709_MAX_SOLVE_STEP_EV, correctionEv));
+                    if (correctionEv > 0.0f && !broadOrMixedHdr) {
+                        correctionEv = 0.0f;
+                    }
+                    if (Math.abs(correctionEv) < MOTION_26709_SOLVE_DEADBAND_EV) {
+                        correctionEv = 0.0f;
+                    }
                 }
-                mMotion26680PreviewLatched = false;
-                mMotion26680AeLockApplied = false;
-                mMotion26680AwbLockApplied = false;
-                mMotion26680StableFrames = 0;
-                mMotion26680StationaryFrames = 0;
-                mMotion26680ReframeMotionFrames = 0;
-                mMotion26680LastExposureEnergy = Double.NaN;
-                clearMotion26708HighlightClassifier("physical-reframe");
+                desiredOffsetEv = currentOffsetEv + correctionEv;
+            }
+            desiredOffsetEv = Math.max(-MOTION_26709_MAX_RELATIVE_EV,
+                    Math.min(MOTION_26709_MAX_RELATIVE_EV, desiredOffsetEv));
+
+            int desiredSteps = mMotion26709HalBaseCompSteps
+                    + Math.round(desiredOffsetEv / mMotion26709AeStepEv);
+            desiredSteps = Math.max(aeRange.getLower(), Math.min(aeRange.getUpper(), desiredSteps));
+            final float quantizedOffsetEv = (desiredSteps - mMotion26709HalBaseCompSteps)
+                    * mMotion26709AeStepEv;
+
+            if (desiredSteps != mMotion26709TargetCompSteps) {
+                try {
+                    if (mMotion26680AeLockApplied) {
+                        mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false);
+                        mMotion26680AeLockApplied = false;
+                    }
+                    mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_EXPOSURE_COMPENSATION,
+                            desiredSteps);
+                    mMotion26709TargetCompSteps = desiredSteps;
+                    rebuildPreviewBuilder();
+                    Log.i(TAG, "IRIS_26709_ADAPTIVE_REFERENCE_TARGET"
+                            + " halBaseSteps=" + mMotion26709HalBaseCompSteps
+                            + " currentSteps=" + resultCompSteps
+                            + " targetSteps=" + desiredSteps
+                            + " stepEv=" + mMotion26709AeStepEv
+                            + " currentOffsetEv=" + currentOffsetEv
+                            + " correctionEv=" + correctionEv
+                            + " targetOffsetEv=" + quantizedOffsetEv
+                            + " p995=" + mMotion26709PublishedP995
+                            + " meaningfulHdr=" + meaningfulHdr
+                            + " broadOrMixedHdr=" + broadOrMixedHdr
+                            + " meaningfulClip=" + meaningfulClip
+                            + " signedBidirectional=true previousAdjustedExposureNeverRebased=true"
+                            + " previewFrameMatchedInverse=true");
+                } catch (IllegalArgumentException | IllegalStateException e) {
+                    Log.w(TAG, "IRIS_26709 adaptive reference target skipped: "
+                            + e.getClass().getSimpleName());
+                }
+                return;
+            }
+
+            try {
+                mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
+                mMotion26680AeLockApplied = true;
+                mMotion26709ReferenceSettled = true;
                 rebuildPreviewBuilder();
-                Log.i(TAG, "IRIS_26680_STABLE_PREVIEW_RELEASE generation="
-                        + mIris26548CameraHealthGeneration
-                        + " shakiness=" + shakiness
-                        + " reason=physical-camera-reframe"
-                        + " subjectMotionCannotUnlock=true");
+                Log.i(TAG, "IRIS_26709_REFERENCE_SETTLED"
+                        + " exposureNs=" + exposureNs + " iso=" + iso
+                        + " halBaselineExposureNs=" + mMotion26709HalBaselineExposureNs
+                        + " halBaselineIso=" + mMotion26709HalBaselineIso
+                        + " p995=" + mMotion26709PublishedP995
+                        + " targetSignal=" + MOTION_26709_REFERENCE_SIGNAL_TARGET
+                        + " targetOffsetEv=" + currentOffsetEv
+                        + " aeLocked=true awbLocked=" + mMotion26680AwbLockApplied
+                        + " equalExposureNormalEpoch=true viewfinder26708Behavior=true");
             } catch (IllegalArgumentException | IllegalStateException e) {
-                Log.w(TAG, "IRIS_26680 stable preview release skipped: "
+                Log.w(TAG, "IRIS_26709 adaptive reference latch skipped: "
                         + e.getClass().getSimpleName());
             }
             return;
@@ -2822,7 +3101,6 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if (physicalReframe) {
             mMotion26680StableFrames = 0;
             mMotion26680StationaryFrames = 0;
-            mMotion26680LastExposureEnergy = (double) exposureNs * (double) iso;
             return;
         }
 
@@ -2834,68 +3112,66 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         boolean awbSettled = awbState == null
                 || awbState == CaptureResult.CONTROL_AWB_STATE_CONVERGED
                 || awbState == CaptureResult.CONTROL_AWB_STATE_LOCKED;
-        double exposureEnergy = (double) exposureNs * (double) iso;
         boolean energyStable = true;
-        if (Double.isFinite(mMotion26680LastExposureEnergy)
-                && mMotion26680LastExposureEnergy > 0.0 && exposureEnergy > 0.0) {
-            double evDelta = Math.abs(Math.log(exposureEnergy / mMotion26680LastExposureEnergy)
+        if (Double.isFinite(mMotion26709HalBaselineEnergy)
+                && mMotion26709HalBaselineEnergy > 0.0 && exposureEnergy > 0.0) {
+            double evDelta = Math.abs(Math.log(exposureEnergy / mMotion26709HalBaselineEnergy)
                     / Math.log(2.0));
             energyStable = evDelta <= MOTION_26680_STABLE_ENERGY_EV;
         }
-        mMotion26680LastExposureEnergy = exposureEnergy;
+        mMotion26709HalBaselineEnergy = exposureEnergy;
         if (aeSettled && awbSettled && energyStable) {
             mMotion26680StableFrames++;
         } else {
             mMotion26680StableFrames = 0;
         }
 
-        /* IRIS_26705_NO_UNCONVERGED_FORCE_LATCH
-         * A stationary timer is never evidence of correct AE. Cold start may latch only after
-         * HAL AE/AWB and exposure energy remain genuinely settled for the inherited six frames. */
-        boolean iris26705BaselineReady =
-                mMotion26680StableFrames >= MOTION_26680_STABLE_CONFIRM_FRAMES;
-        if (!iris26705BaselineReady) return;
+        if (mMotion26680StableFrames < MOTION_26680_STABLE_CONFIRM_FRAMES) return;
 
         Boolean aeLockAvailable = mCameraCharacteristics.get(
                 CameraCharacteristics.CONTROL_AE_LOCK_AVAILABLE);
         Boolean awbLockAvailable = mCameraCharacteristics.get(
                 CameraCharacteristics.CONTROL_AWB_LOCK_AVAILABLE);
         if (!Boolean.TRUE.equals(aeLockAvailable)) {
-            Log.w(TAG, "IRIS_26680_STABLE_PREVIEW_UNAVAILABLE aeLock=false");
+            Log.w(TAG, "IRIS_26709_REFERENCE_UNAVAILABLE aeLock=false fallback=HAL");
             return;
         }
         try {
-            mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, true);
-            mMotion26680AeLockApplied = true;
+            mMotion26709HalBaselineReady = true;
+            mMotion26709ReferenceSettled = false;
+            mMotion26709ReleasePending = false;
+            mMotion26709HalBaseCompSteps = resultCompSteps;
+            mMotion26709TargetCompSteps = resultCompSteps;
+            mMotion26709AeStepEv = adaptiveAeAvailable ? aeStep.floatValue() : 0.0f;
+            mMotion26709HalBaselineEnergy = exposureEnergy;
+            mMotion26709HalBaselineExposureNs = exposureNs;
+            mMotion26709HalBaselineIso = iso;
+            mMotion26709LastConsumedMeterTimestampNs = 0L;
+            clearMotion26708HighlightClassifier("26709-baseline-start");
             if (Boolean.TRUE.equals(awbLockAvailable)) {
                 mPreviewRequestBuilder.set(CaptureRequest.CONTROL_AWB_LOCK, true);
                 mMotion26680AwbLockApplied = true;
             }
             mMotion26680PreviewLatched = true;
+            mMotion26680AeLockApplied = false;
             mMotion26680ReframeMotionFrames = 0;
             rebuildPreviewBuilder();
-            Log.i(TAG, "IRIS_26680_STABLE_PREVIEW_LATCH generation="
+            Log.i(TAG, "IRIS_26709_HAL_APPEARANCE_BASELINE generation="
                     + mIris26548CameraHealthGeneration
-                    + " exposureNs=" + exposureNs
-                    + " iso=" + iso
-                    + " aeState=" + aeState
-                    + " awbState=" + awbState
+                    + " exposureNs=" + exposureNs + " iso=" + iso
+                    + " compSteps=" + resultCompSteps
+                    + " aeStepEv=" + mMotion26709AeStepEv
                     + " stableFrames=" + mMotion26680StableFrames
                     + " stationaryFrames=" + mMotion26680StationaryFrames
                     + " shakiness=" + shakiness
-                    + " aeLocked=true awbLocked=" + mMotion26680AwbLockApplied
-                    + " rawSceneContentCannotRewriteExposure=true"
-                    + " flickerObserverOnly=true"
-                    + " iris26705NormalAeUntouched=true"
-                    + " shortHeadroomDecisionAtShutter=true"
-                    + " referenceProtectionEv=0.0"
-                    + " shutterWaitAdded=false");
+                    + " aeLocked=false awbLocked=" + mMotion26680AwbLockApplied
+                    + " rawSolverBehindViewfinder=true viewfinder26708Behavior=true");
         } catch (IllegalArgumentException | IllegalStateException e) {
             mMotion26680PreviewLatched = false;
             mMotion26680AeLockApplied = false;
             mMotion26680AwbLockApplied = false;
-            Log.w(TAG, "IRIS_26680 stable preview latch skipped: "
-                    + e.getClass().getSimpleName());
+            mMotion26709HalBaselineReady = false;
+            Log.w(TAG, "IRIS_26709 baseline start skipped: " + e.getClass().getSimpleName());
         }
     }
 
@@ -5636,18 +5912,46 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 CameraCharacteristics.SENSOR_INFO_SENSITIVITY_RANGE);
         if (er == null || sr == null) return false;
 
-        double targetEnergy = ticket.baselineEnergy * MOTION_26505_LONG_TARGET_MULTIPLIER;
+        float longFloor = Float.isFinite(mMotion26709PublishedFloorFraction)
+                ? mMotion26709PublishedFloorFraction
+                : (Float.isFinite(mMotion26380RawFloorFraction) ? mMotion26380RawFloorFraction : 0.10f);
+        float longShadow = Float.isFinite(mMotion26709PublishedShadowFraction)
+                ? mMotion26709PublishedShadowFraction
+                : (Float.isFinite(mMotion26380RawShadowFraction) ? mMotion26380RawShadowFraction : 0.24f);
+        float longMean = Float.isFinite(mMotion26709PublishedMeanSignal)
+                ? mMotion26709PublishedMeanSignal
+                : (Float.isFinite(mMotion26380RawMeanSignal) ? mMotion26380RawMeanSignal : 0.18f);
+        float floorNeed = motion26368Clamp01((longFloor - 0.10f) / (0.58f - 0.10f));
+        float shadowNeed = motion26368Clamp01((longShadow - 0.24f) / (0.78f - 0.24f));
+        float meanNeed = 1.0f - motion26368Clamp01((longMean - 0.035f) / (0.18f - 0.035f));
+        float rawNeed = Math.max(floorNeed, 0.72f * shadowNeed) * (0.40f + 0.60f * meanNeed);
+        long shutterCeilingNs = iris26381MotionOpportunityCeilingNs(normalReferenceResult);
+        float shutterOpportunity = motion26368Clamp01(
+                (shutterCeilingNs - baseExp) / (float)Math.max(1L, shutterCeilingNs));
+        double halBaselineEnergySecIso = Double.isFinite(mMotion26709HalBaselineEnergy)
+                && mMotion26709HalBaselineEnergy > 0.0
+                ? mMotion26709HalBaselineEnergy / 1_000_000_000.0 : ticket.baselineEnergy;
+        double baselineFloorEv = Math.max(0.0, Math.log(
+                Math.max(ticket.baselineEnergy, halBaselineEnergySecIso) / ticket.baselineEnergy)
+                / Math.log(2.0));
+        double adaptiveLongEv = MOTION_26709_LONG_MIN_EV
+                + (MOTION_26709_LONG_MAX_EV - MOTION_26709_LONG_MIN_EV)
+                    * rawNeed * (0.45 + 0.55 * shutterOpportunity);
+        adaptiveLongEv = Math.max(baselineFloorEv, adaptiveLongEv);
+        adaptiveLongEv = Math.max(MOTION_26709_LONG_MIN_EV,
+                Math.min(MOTION_26709_LONG_MAX_EV, adaptiveLongEv));
+        double targetEnergy = ticket.baselineEnergy * Math.pow(2.0, adaptiveLongEv);
         long maxLongExp = Math.min(er.getUpper(), MOTION_26658_LONG_MAX_EXPOSURE_NS);
         maxLongExp = Math.max(er.getLower(), maxLongExp);
         if (maxLongExp <= Math.round(baseExp * MOTION_26658_LONG_MIN_SHUTTER_RATIO)) {
             Log.i(TAG, "IRIS_26658_LONG_SKIPPED_NO_PHOTON_GAIN"
                     + " baseExposureNs=" + baseExp
                     + " maxLongExposureNs=" + maxLongExp
-                    + " referenceRole=ZSL_REFERENCE_SHORT"
+                    + " referenceRole=ADAPTIVE_EQUAL_EXPOSURE_NORMAL"
                     + " isoOnlyLongForbidden=true");
             return false;
         }
-        long reqExp = Math.round(baseExp * MOTION_26505_LONG_TARGET_MULTIPLIER);
+        long reqExp = Math.round(baseExp * Math.pow(2.0, adaptiveLongEv));
         reqExp = Math.max(er.getLower(), Math.min(maxLongExp, reqExp));
         if (!(reqExp > baseExp * MOTION_26658_LONG_MIN_SHUTTER_RATIO)) return false;
 
@@ -5660,6 +5964,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 targetEnergy / Math.max(reqIso, 1) * 1_000_000_000.0);
         reqExp = Math.max(er.getLower(), Math.min(maxLongExp, recomputedExp));
 
+        // IRIS_26709_R1_JAVAC_EFFECTIVELY_FINAL_LONG_EV -- freeze the completed
+        // adaptive LONG solution before the asynchronous CaptureCallback captures it.
+        final double adaptiveLongEvFinal = adaptiveLongEv;
         final double requestedEnergy = ExposureIndex.time2sec(reqExp) * reqIso;
         final double requestedRatio = requestedEnergy / ticket.baselineEnergy;
         final double requestedShutterRatio = reqExp / (double) baseExp;
@@ -5756,7 +6063,7 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                                 + " requestedShutterRatio=" + requestedShutterRatio
                                 + " actualShutterRatio=" + shutterRatio
                                 + " actualDeltaEv=" + actualDeltaEv
-                                + " targetDeltaEv=" + MOTION_26505_LONG_TARGET_EV
+                                + " targetDeltaEv=" + adaptiveLongEvFinal
                                 + " allowedAroundClampedRequest="
                                 + requestedRatioMin + ".." + requestedRatioMax
                                 + " normalAccumulatorAdmission=GOOGLE_BRACKET_COMMON_SABRE shutterGate=false");
@@ -5783,14 +6090,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     + " submitted=true rawOnlyTarget=true"
                     + " baseExposureNs=" + baseExp
                     + " baseIso=" + baseIso
-                    + " targetEv=" + MOTION_26505_LONG_TARGET_EV
+                    + " targetEv=" + adaptiveLongEv
+                    + " rawNeed=" + rawNeed
+                    + " shutterOpportunity=" + shutterOpportunity
+                    + " halBaselineFloorEv=" + baselineFloorEv
                     + " requestedExposureNs=" + requestedExp
                     + " requestedIso=" + requestedIso
                     + " requestedRatio=" + requestedRatio
                     + " requestedShutterRatio=" + requestedShutterRatio
                     + " maxShutterNs=" + MOTION_26658_LONG_MAX_EXPOSURE_NS
                     + " shutterFirst=true isoOnlyLongForbidden=true"
-                    + " referenceRole=ZSL_REFERENCE_SHORT"
+                    + " referenceRole=ADAPTIVE_EQUAL_EXPOSURE_NORMAL"
                     + " normalRingCleared=false previewRebuilt=false shutterGate=false");
             if (mBackgroundHandler != null) {
                 mBackgroundHandler.postDelayed(() -> {
@@ -7013,10 +7323,14 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if(exp!=null&&exp>0L)frame.motionV2ActualExposureNs=exp;if(iso!=null&&iso>0)frame.motionV2ActualIso=iso;
         if(frame.motionV2ActualExposureNs>0L&&frame.motionV2ActualIso>0)
             frame.motionV2ExposureEnergy=ExposureIndex.time2sec(frame.motionV2ActualExposureNs)*frame.motionV2ActualIso;
-        /* IRIS_26708_NORMAL_REFERENCE_PROTECTION_ABSOLUTE_ZERO
-         * NORMAL is never intentionally underexposed by Motion HDR. SHORT exposure depth is
-         * represented by its own actual exposure/ISO metadata, not a fake NORMAL protection EV. */
-        frame.motionV2ReferenceProtectionEv = 0.0f;
+        /* IRIS_26709_ADAPTIVE_NORMAL_REFERENCE_PROTECTION
+         * The selected NORMAL epoch may be physically below the frozen HAL appearance baseline.
+         * Carry only that measured positive underexposure amount into float-domain canonical
+         * restoration. A brighter-than-HAL reference carries zero here and is handled by the
+         * existing viewfinder presentation solve; SHORT never owns this value. */
+        frame.motionV2ReferenceProtectionEv = shortRole ? 0.0f
+                : Math.max(0.0f, Math.min(MOTION_26709_MAX_RELATIVE_EV,
+                        mMotion26709FrozenCaptureProtectionEv));
         if(ts!=null)frame.motionV2ResultSensorTimestampNs=ts;frame.motionV2FrameNumber=result.getFrameNumber();
         if(skew!=null)frame.motionV2RollingShutterSkewNs=skew;if(focus!=null)frame.motionV2FocusDistanceDiopters=focus;
         if(lens!=null)frame.motionV2LensState=lens;
@@ -7180,15 +7494,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     }
     private volatile Motion26486ShortTicket mMotion26486CaptureShortTicket = null;
 
-    /* IRIS_26505_PHYSICAL_LONG_BRACKET_OWNER
-     * One intentional +EV RAW observation shares the already-existing isolated
-     * ShadowAuxSlot owned by this Motion generation. It never enters the equal-
-     * exposure Wronski frame list. Camera2 onCaptureStarted/result/Image timestamps
-     * are the only identity authority; there is no nearest-frame borrowing.
-     */
-    private static final double MOTION_26505_LONG_TARGET_EV = 2.5;
-    private static final double MOTION_26505_LONG_TARGET_MULTIPLIER =
-            5.656854249492381;
+    /* IRIS_26709_ADAPTIVE_LONG_BRACKET_OWNER
+     * One intentional +EV RAW observation shares the already-existing isolated ShadowAuxSlot.
+     * Its EV is solved per scene from the frozen reference/HAL baseline plus RAW starvation and
+     * motion opportunity; it never enters the equal-exposure NORMAL temporal accumulator. */
+    private static final double MOTION_26709_LONG_MIN_EV = 0.50;
+    private static final double MOTION_26709_LONG_MAX_EV = 2.50;
     private static final long MOTION_26658_LONG_MAX_EXPOSURE_NS = 66_666_667L;
     private static final double MOTION_26658_LONG_MIN_SHUTTER_RATIO = 1.15;
     private static final int MOTION_26505_LONG_FALLBACK_MAX_ISO = 800;
@@ -8125,11 +8436,12 @@ private long mMotionUnifiedLastUpdateMs = 0L;
     private volatile boolean mMotion26676ShutterRetryScheduled = false;
     private volatile long mMotion26676ShutterWaitStartMs = 0L;
 
-    /* IRIS_26705_NO_NORMAL_HDR_PROTECTION_WAIT
-     * No HDR owner writes NORMAL AE compensation anymore, so there is no protected NORMAL
-     * generation to wait for. SHORT is capture-local and never gates shutter acknowledgement. */
+    /* IRIS_26709_ADAPTIVE_REFERENCE_SHUTTER_GENERATION_GUARD
+     * Shutter may proceed immediately once the signed adaptive NORMAL epoch is settled. If the
+     * user presses during a transition, wait only for a result belonging to the new absolute
+     * target; the existing exact-exposure post-shutter NORMAL top-up fills any missing ring slots. */
     private boolean motion26676ProtectionIncreasePendingForShutter() {
-        return false;
+        return isZslMode() && (!mMotion26709HalBaselineReady || !mMotion26709ReferenceSettled);
     }
 
     private boolean motion26676DeferShutterUntilProtectedGeneration() {
@@ -8143,11 +8455,9 @@ private long mMotionUnifiedLastUpdateMs = 0L;
         if (waitedMs >= MOTION_26676_SHUTTER_MAX_WAIT_MS) {
             mMotion26676ShutterWaitStartMs = 0L;
             mMotion26676ShutterRetryScheduled = false;
-            Log.w(TAG, "IRIS_26676_CAPTURE_GENERATION_GUARD timeoutMs=" + waitedMs
-                    + " staleNormalCapturePrevented=true");
-            iris26679RejectShutter("HDR_PROTECTION_SETTLE_TIMEOUT",
-                    "HDR exposure is still settling");
-            return true;
+            Log.w(TAG, "IRIS_26709_CAPTURE_GENERATION_GUARD timeoutMs=" + waitedMs
+                    + " settleTimeout=true fallback=currentPhysicalEpoch topUpStillExact=true");
+            return false;
         }
         if (!mMotion26676ShutterRetryScheduled) {
             if (mBackgroundHandler == null) {
@@ -8194,6 +8504,17 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                 && iris26593ReferenceTs != null
                 && iris26593ReferenceExp != null && iris26593ReferenceExp > 0L
                 && iris26593ReferenceIso != null && iris26593ReferenceIso > 0;
+        if (iris26593SessionReady && mMotion26709HalBaselineReady
+                && Double.isFinite(mMotion26709HalBaselineEnergy)
+                && mMotion26709HalBaselineEnergy > 0.0) {
+            double refEnergy = (double) iris26593ReferenceExp * (double) iris26593ReferenceIso;
+            double protect = refEnergy > 0.0
+                    ? Math.log(mMotion26709HalBaselineEnergy / refEnergy) / Math.log(2.0) : 0.0;
+            mMotion26709FrozenCaptureProtectionEv = (float)Math.max(0.0,
+                    Math.min(MOTION_26709_MAX_RELATIVE_EV, protect));
+        } else {
+            mMotion26709FrozenCaptureProtectionEv = 0.0f;
+        }
         if (!iris26593SessionReady) {
             Log.w(TAG, "IRIS_26593_MOTION_NOT_READY sessionReady=false"
                     + " cameraDevice=" + (mCameraDevice != null)
@@ -8236,31 +8557,21 @@ private long mMotionUnifiedLastUpdateMs = 0L;
             clearMotion26505LongTicket(iris26505PreviousLongTicket, "new_generation");
         }
 
-        /* IRIS_26670_ISOLATED_POST_SHUTTER_HDR_TRANSACTION
-         * Keep successful-26660 live HAL/user AE and NORMAL-ring ownership exactly. A fresh RAW
-         * highlight trigger may request one post-shutter RAW-only HIGHLIGHT_SHORT, and the exact
-         * successful-26660 +2.5 EV SHADOW_LONG policy remains unchanged. Both are capture-local
-         * radiometric evidence only; neither may become the next live preview/NORMAL authority.
-         */
+        /* IRIS_26709_ADAPTIVE_REFERENCE_PLUS_LONG_CAPTURE_PLAN
+         * NORMAL itself is now the highlight-safe equal-exposure reference burst. The old darker
+         * HIGHLIGHT_SHORT transaction is therefore retired. One RAW-only SHADOW_LONG remains the
+         * auxiliary body/shadow exposure and is chosen adaptively from the frozen reference/HAL
+         * baseline plus current RAW starvation and motion budget. */
         final Motion26486ShortTicket iris26486ShortTicket = new Motion26486ShortTicket();
         mMotion26486CaptureShortTicket = iris26486ShortTicket;
-        final boolean iris26593ShortBudgetAllows = iris26593TotalTarget >= 2;
-        final boolean iris26593ShortSubmitted = iris26593ShortBudgetAllows
-                && applyMotion26486ExplicitShortCaptureIfNeeded(
-                        iris26486ShortTicket, iris26593NormalReference);
-        final boolean iris26480ShortHighlightRequested = iris26593ShortSubmitted;
-        if (!iris26593ShortSubmitted) {
-            clearMotion26490CaptureShortTicket(iris26486ShortTicket,
-                    iris26593ShortBudgetAllows ? "26670_short_not_required_or_unavailable"
-                            : "26670_total_budget_requires_normal");
-        }
+        final boolean iris26593ShortSubmitted = false;
+        final boolean iris26480ShortHighlightRequested = false;
+        clearMotion26490CaptureShortTicket(iris26486ShortTicket, "26709_short_retired_reference_owns_highlights");
 
         final Motion26505LongTicket iris26505LongTicket = new Motion26505LongTicket(
                 iris26486ShortTicket.slot.shadowAuxSlot);
         mMotion26505CaptureLongTicket = iris26505LongTicket;
-        final int iris26593RemainingAfterShort = iris26593TotalTarget
-                - (iris26593ShortSubmitted ? 1 : 0);
-        final boolean iris26593LongBudgetAllows = iris26593RemainingAfterShort >= 2;
+        final boolean iris26593LongBudgetAllows = iris26593TotalTarget >= 2;
         final boolean iris26505LongRequested = iris26593LongBudgetAllows
                 && applyMotion26505ExplicitLongCaptureIfUseful(
                         iris26505LongTicket, iris26593NormalReference);
@@ -8269,19 +8580,16 @@ private long mMotionUnifiedLastUpdateMs = 0L;
                     iris26593LongBudgetAllows ? "not_requested" : "total_budget_requires_normal");
         }
 
-        final int iris26593AuxCount = (iris26593ShortSubmitted ? 1 : 0)
-                + (iris26505LongRequested ? 1 : 0);
-        Log.i(TAG, "IRIS_26670_ISOLATED_HDR_CAPTURE_PLAN"
-                + " livePreviewOwner=SUCCESSFUL_26660_HAL_USER_AE"
-                + " highlightShortRequested=" + iris26480ShortHighlightRequested
-                + " highlightShortSubmitted=" + iris26593ShortSubmitted
-                + " shortTemporalAccumulator=false"
+        final int iris26593AuxCount = iris26505LongRequested ? 1 : 0;
+        Log.i(TAG, "IRIS_26709_ADAPTIVE_REFERENCE_CAPTURE_PLAN"
+                + " liveViewfinderOwner=26708_STABLE_HAL_APPEARANCE"
+                + " adaptiveReferenceSettled=" + mMotion26709ReferenceSettled
+                + " referenceProtectionEv=" + mMotion26709FrozenCaptureProtectionEv
+                + " highlightShortRequested=false highlightShortRetired=true"
                 + " shadowLongRequested=" + iris26505LongRequested
-                + " shadowLongTargetEv=" + MOTION_26505_LONG_TARGET_EV
-                + " adaptiveLong=false"
-                + " totalTarget=" + iris26593TotalTarget
+                + " adaptiveLong=true totalTarget=" + iris26593TotalTarget
                 + " normalTarget=" + (iris26593TotalTarget - iris26593AuxCount)
-                + " previewRestoreRequired=" + (iris26593AuxCount > 0));
+                + " equalExposureNormalReference=true previewPhysicalOffsetHidden=true");
         final int iris26593NormalTarget = iris26593TotalTarget - iris26593AuxCount;
         if (iris26593NormalTarget < 1) {
             iris26598PreShutterNormals.closeAll();
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index 5e627d91a55401a802a935cbe3b93c099680e130..6c15a414a7f9863a92b8d37521eb85117b87750d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -151,11 +151,9 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
             }
 
             ArrayList<Float> previewLuma = collectPreviewMidtones(preview);
-            float referenceProtectionEv = 0.0f;
-            if (!iris26550Night && basePipeline.mParameters.motionV2ReferenceProtectionEv != 0.0f) {
-                throw new IllegalStateException("26708 Motion NORMAL referenceProtectionEv must be zero");
-            }
-            float referenceRestoreGain = 1.0f;
+            float referenceProtectionEv = iris26550Night ? 0.0f : clamp(
+                    basePipeline.mParameters.motionV2ReferenceProtectionEv, 0.0f, 1.50f);
+            float referenceRestoreGain = (float)Math.pow(2.0, referenceProtectionEv);
             ArrayList<RgbSample> candidate = collectCandidateSamples(
                     previousNode.WorkingTexture, preview.getWidth(), preview.getHeight(),
                     referenceRestoreGain);
@@ -211,7 +209,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                 matchStrengthPercent = MOTION_FIXED_MATCH_STRENGTH_PERCENT;
                 float matchStrength = matchStrengthPercent / 100.0f;
                 solvedEv = clamp(rawSolvedEv * matchStrength, MIN_EV, MAX_EV);
-                Log.i(Name, "IRIS_26662_CANONICAL_REFERENCE_PRESENTATION_SOLVE"
+                Log.i(Name, "IRIS_26709_CANONICAL_REFERENCE_PRESENTATION_SOLVE"
                         + " referenceProtectionEv=" + referenceProtectionEv
                         + " referenceRestoreGain=" + referenceRestoreGain
                         + " candidateMeterCanonicalized=true"
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
index c8082c6f5f5193e8d0c9a34a59767c5e07ad9b71..82473058261be5a82dcf7c4a833e6eaed2a18ab1 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/processor/PhotonMotionMgc1271Bridge.kt
@@ -154,10 +154,12 @@ object PhotonMotionMgc1271Bridge {
              * NORMAL reference into FLOAT-domain normalization/presentation. */
             longFrame?.let { orderedPhysical += it to RawBurstFrameRole.SHADOW_LONG }
             shortFrame?.let { orderedPhysical += it to RawBurstFrameRole.HIGHLIGHT_SHORT }
-            parameters.motionV2ReferenceProtectionEv = 0.0f
-            requireParity(parameters.irisNightActive || reference.motionV2ReferenceProtectionEv == 0.0f,
-                "26708 Motion NORMAL referenceProtectionEv must be absolute zero")
-            PLog.i(TAG, "IRIS_26674_PROTECTED_NORMAL_REFERENCE_HANDOFF " +
+            parameters.motionV2ReferenceProtectionEv = if (parameters.irisNightActive) {
+                0.0f
+            } else {
+                reference.motionV2ReferenceProtectionEv.coerceIn(0.0f, 1.50f)
+            }
+            PLog.i(TAG, "IRIS_26709_ADAPTIVE_NORMAL_REFERENCE_HANDOFF " +
                 "night=${parameters.irisNightActive} referenceTimestamp=${mgcBase.timestamp} " +
                 "referenceFromOwnedZsl=${!parameters.irisNightActive} shadowLong=${longFrame != null} " +
                 "highlightShort=${shortFrame != null} shortFinal=${shortFrame == null || orderedPhysical.last().second == RawBurstFrameRole.HIGHLIGHT_SHORT} " +
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
index 670bd5a9a7406fb501ec177ca4921bd428c580d6..f6f6a44dd1c7cb6833aefb7768c72a11d48963ed 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/views/viewfinder/MainRenderer.java
@@ -80,7 +80,7 @@ public class MainRenderer implements GLSurfaceView.Renderer, SurfaceTexture.OnFr
         GLES20.glUniform1i(enablePeak, PhotonCamera.getSettings().focusPeak);
         GLES20.glUniform1i(mirror, mMirrorPreview ? 1 : 0);
         GLES20.glUniform1f(irisSoftwareZoom, mSoftwareZoom);
-        /* IRIS_26667_FRAME_EXACT_PREVIEW_PRESENTATION
+        /* IRIS_26709_SIGNED_FRAME_EXACT_PREVIEW_PRESENTATION
          * Preserve 26662 timestamp matching and 26663 hold-on-miss, with no additional display
          * slew. Compensation changes only when an exact Camera2 result for the displayed frame
          * proves the protection EV. */
@@ -89,11 +89,11 @@ public class MainRenderer implements GLSurfaceView.Renderer, SurfaceTexture.OnFr
             float exactEv = iris26663Controller.getMotion26663ReferencePreviewProtectionEv(
                     iris26553FrameTimestamp);
             if (Float.isFinite(exactEv)) {
-                mIris26663LastConfirmedProtectionEv = Math.max(0.0f, exactEv);
+                mIris26663LastConfirmedProtectionEv = Math.max(-1.50f, Math.min(1.50f, exactEv));
             }
         }
         float iris26667PreviewGain = (float) Math.pow(2.0,
-                Math.max(0.0f, mIris26663LastConfirmedProtectionEv));
+                Math.max(-1.50f, Math.min(1.50f, mIris26663LastConfirmedProtectionEv)));
         GLES20.glUniform1f(iris26662ReferencePreviewGain, iris26667PreviewGain);
         mIris26678PreviewFlicker[0] = 0.0f;
         mIris26678PreviewFlicker[1] = 0.0f;
diff --git a/app/version.properties b/app/version.properties
index 7149cf60416e6db8ea07eed90930d779a0d84e71..51ca699a44f22b30ef78cf51a712bf56855eed3a 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726708
-VERSION_BUILD=26708
+VERSION_NAME=0.9726709
+VERSION_BUILD=26709
