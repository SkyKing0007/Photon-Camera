diff --git a/app/src/main/cpp/iris_heic_jni.cpp b/app/src/main/cpp/iris_heic_jni.cpp
index 9d60d1a1f763ea48d83ae19b765b14d17ba9ff20..b568dcf0dcc412eb83c7b321fd26d2e26ce24506 100644
--- a/app/src/main/cpp/iris_heic_jni.cpp
+++ b/app/src/main/cpp/iris_heic_jni.cpp
@@ -723,10 +723,10 @@ Java_com_particlesdevs_photoncamera_processing_ultrahdr_IrisHeicUltraHdrEncoder_
         if (!baseNclx) break;
         // IRIS_26643_AOSP_ANDROID16_HEIC_ULTRAHDR_BASE_AUTHORITY
         // Match HeicCompositeStream/MPEG4Writer: Display-P3 primaries, sRGB transfer,
-        // unspecified matrix, full range, and a single NCLX colr authority (no parallel ICC).
+        // BT.601 matrix, full range, and a single NCLX colr authority (no parallel ICC).
         if (heif_nclx_color_profile_set_color_primaries(baseNclx, heif_color_primaries_SMPTE_EG_432_1).code != heif_error_Ok ||
             heif_nclx_color_profile_set_transfer_characteristics(baseNclx, heif_transfer_characteristic_IEC_61966_2_1).code != heif_error_Ok ||
-            heif_nclx_color_profile_set_matrix_coefficients(baseNclx, heif_matrix_coefficients_unspecified).code != heif_error_Ok) break;
+            heif_nclx_color_profile_set_matrix_coefficients(baseNclx, heif_matrix_coefficients_ITU_R_BT_601_6).code != heif_error_Ok) break;
         baseNclx->full_range_flag = true;
         if (heif_image_set_nclx_color_profile(baseImage, baseNclx).code != heif_error_Ok) break;
         options->save_two_colr_boxes_when_ICC_and_nclx_available = 0;
@@ -784,10 +784,10 @@ Java_com_particlesdevs_photoncamera_processing_ultrahdr_IrisHeicUltraHdrEncoder_
         alternateNclx = heif_nclx_color_profile_alloc();
         if (!alternateNclx) break;
         // Android 16 MPEG4Writer publishes the tmap metadata item using the SDR base's
-        // Display-P3/sRGB/unspecified-matrix/full-range color aspects and 8-bit PIXI.
+        // Display-P3/sRGB/BT.601-matrix/full-range color aspects and 8-bit PIXI.
         if (heif_nclx_color_profile_set_color_primaries(alternateNclx, heif_color_primaries_SMPTE_EG_432_1).code != heif_error_Ok ||
             heif_nclx_color_profile_set_transfer_characteristics(alternateNclx, heif_transfer_characteristic_IEC_61966_2_1).code != heif_error_Ok ||
-            heif_nclx_color_profile_set_matrix_coefficients(alternateNclx, heif_matrix_coefficients_unspecified).code != heif_error_Ok) break;
+            heif_nclx_color_profile_set_matrix_coefficients(alternateNclx, heif_matrix_coefficients_ITU_R_BT_601_6).code != heif_error_Ok) break;
         alternateNclx->full_range_flag = true;
 
         // Keep the established Iris gain-map publication quality authority.
@@ -800,7 +800,7 @@ Java_com_particlesdevs_photoncamera_processing_ultrahdr_IrisHeicUltraHdrEncoder_
         heif_writer writer{1, iris26636WriteCallback};
         if (heif_context_write(ctx, &writer, &memory).code != heif_error_Ok || memory.bytes.empty()) break;
         ok = iris26636WriteFile(path, memory.bytes);
-        if (ok) IRIS26636_LOGI("IRIS_26643_HEIC_AOSP_ANDROID16_ISO21496 hardwareHevc=true opaqueRgb=true baseP3SrgbMatrixUnspecified=true baseIcc=false tmapP3Srgb8=true gainmapUnspecified=true gainmapVisible=true ipmaEssential=true gainmapReused=true bytes=%zu", memory.bytes.size());
+        if (ok) IRIS26636_LOGI("IRIS_26643_HEIC_AOSP_ANDROID16_ISO21496 hardwareHevc=true opaqueRgb=true baseP3SrgbBt601Matrix=true baseIcc=false tmapP3Srgb8=true gainmapUnspecified=true gainmapVisible=true ipmaEssential=true gainmapReused=true bytes=%zu", memory.bytes.size());
     } while (false);
 
     gIris26636Env = nullptr;
@@ -877,7 +877,7 @@ Java_com_particlesdevs_photoncamera_processing_ultrahdr_IrisHeicUltraHdrEncoder_
         if (!baseNclx) break;
         if (heif_nclx_color_profile_set_color_primaries(baseNclx, heif_color_primaries_SMPTE_EG_432_1).code != heif_error_Ok ||
             heif_nclx_color_profile_set_transfer_characteristics(baseNclx, heif_transfer_characteristic_IEC_61966_2_1).code != heif_error_Ok ||
-            heif_nclx_color_profile_set_matrix_coefficients(baseNclx, heif_matrix_coefficients_unspecified).code != heif_error_Ok) break;
+            heif_nclx_color_profile_set_matrix_coefficients(baseNclx, heif_matrix_coefficients_ITU_R_BT_601_6).code != heif_error_Ok) break;
         baseNclx->full_range_flag = true;
         options->save_two_colr_boxes_when_ICC_and_nclx_available = 0;
         options->output_nclx_profile = baseNclx;
@@ -973,7 +973,7 @@ Java_com_particlesdevs_photoncamera_processing_ultrahdr_IrisHeicUltraHdrEncoder_
         if (!alternateNclx) break;
         if (heif_nclx_color_profile_set_color_primaries(alternateNclx, heif_color_primaries_SMPTE_EG_432_1).code != heif_error_Ok ||
             heif_nclx_color_profile_set_transfer_characteristics(alternateNclx, heif_transfer_characteristic_IEC_61966_2_1).code != heif_error_Ok ||
-            heif_nclx_color_profile_set_matrix_coefficients(alternateNclx, heif_matrix_coefficients_unspecified).code != heif_error_Ok) break;
+            heif_nclx_color_profile_set_matrix_coefficients(alternateNclx, heif_matrix_coefficients_ITU_R_BT_601_6).code != heif_error_Ok) break;
         alternateNclx->full_range_flag = true;
 
         if (heif_encoder_set_lossy_quality(encoder, 95).code != heif_error_Ok) break;
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 6eebcc76127b120f2e934d448ad46e07a710cbf7..abfa8664cbfe8b6c98e6e1c1e5cfe84874a8cb9e 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -2738,6 +2738,367 @@ internal object GlesMgcRawSabreShaders {
         }
     """.trimIndent()
 
+
+    /* IRIS_26648_UNIVERSAL_NORMAL_SHORT_RADIOMETRIC_FUSION_OWNER
+     * NORMAL temporal reconstruction is dehomogenized first. HIGHLIGHT_SHORT never enters the
+     * temporal accumulator. This owner reconstructs the aligned/exposure-normalized SHORT with the
+     * exact Sabre RBF, evaluates physical alignment/source-headroom/SNR plus measured NORMAL CFA
+     * validity, and selects one scalar RGB radiometric authority. No scene semantics, gradients,
+     * spatial fill, mask dilation, border extrapolation, or per-channel exposure switching. */
+    val universalNormalShortFusion26648 = """
+        #version 300 es
+        precision highp float;
+        precision highp int;
+        uniform sampler2D uNormalMean;
+        uniform sampler2D uNormalWeightR;
+        uniform sampler2D uNormalWeightsGb;
+        uniform sampler2D uNormalValidWeights;
+        uniform sampler2D uShortExtractedBayer;
+        uniform sampler2D uShortFlow;
+        uniform sampler2D uShortCovariance;
+        uniform sampler2D uShortPhysicalReverseWeight;
+        uniform vec4 uFlowScaleOffset;
+        uniform ivec2 uExtractedSize;
+        uniform ivec2 uOutputSize;
+        uniform vec4 uFrameBorderPadded;
+        uniform int uCfaPattern;
+        uniform float uSourceClippingPoint;
+        uniform float uValidityWeightScale;
+        uniform vec4 uGains;
+        uniform vec4 uBlackLevelsTimesGains;
+        uniform vec4 uCovRangeRg;
+        uniform vec2 uCovRangeB;
+        uniform vec3 uShortShotNoiseRgb;
+        uniform vec3 uShortReadNoiseRgb;
+        uniform vec3 uNormalShotNoiseRgb;
+        uniform vec3 uNormalReadNoiseRgb;
+        uniform float uNormalFrameCount;
+        layout(location = 0) out vec4 oFusedExtendedLinear;
+        layout(location = 1) out float oDecision;
+
+        bool shortWarpUvValid(vec2 sampleUv) {
+            return sampleUv.x > uFrameBorderPadded.x && sampleUv.y > uFrameBorderPadded.y &&
+                sampleUv.x < uFrameBorderPadded.z && sampleUv.y < uFrameBorderPadded.w;
+        }
+        float minimum3(vec3 v) { return min(v.r, min(v.g, v.b)); }
+        float secondHighest3(vec3 v) {
+            return max(min(v.r, v.g), max(min(v.r, v.b), min(v.g, v.b)));
+        }
+        float luma(vec3 v) { return dot(v, vec3(0.25, 0.50, 0.25)); }
+        float kernelWeight(vec2 pixelOffset, vec3 covariance) {
+            float kernelDistance =
+                pixelOffset.x * pixelOffset.x * covariance.x +
+                pixelOffset.y * pixelOffset.y * covariance.y +
+                pixelOffset.x * pixelOffset.y * covariance.z * 2.0;
+            return exp2(-0.5 * kernelDistance) + 0.00005;
+        }
+
+        vec3 unpackCovariance(vec3 packedCovariance) {
+            return vec3(
+                packedCovariance.x * uCovRangeRg.y + uCovRangeRg.x,
+                packedCovariance.y * uCovRangeRg.w + uCovRangeRg.z,
+                packedCovariance.z * uCovRangeB.y + uCovRangeB.x
+            );
+        }
+
+        mat3 get3x3FromExtractedBayer(ivec2 bayerPosition) {
+            mat3 values = mat3(0.0);
+            int type = (bayerPosition.y % 2) * 2 + (bayerPosition.x % 2);
+            vec2 texturePosition = vec2(bayerPosition / 2);
+            if (type == 0) texturePosition += vec2(-1.0, -1.0);
+            else if (type == 1) texturePosition += vec2(0.0, -1.0);
+            else if (type == 2) texturePosition += vec2(-1.0, 0.0);
+            texturePosition += vec2(0.5);
+            vec2 reciprocalSize = 1.0 / vec2(uExtractedSize);
+            vec4 bayer0 = texture(uShortExtractedBayer, texturePosition * reciprocalSize);
+            vec4 bayer1 = texture(
+                uShortExtractedBayer,
+                (texturePosition + vec2(1.0, 0.0)) * reciprocalSize
+            );
+            vec4 bayer2 = texture(
+                uShortExtractedBayer,
+                (texturePosition + vec2(0.0, 1.0)) * reciprocalSize
+            );
+            vec4 bayer3 = texture(
+                uShortExtractedBayer,
+                (texturePosition + vec2(1.0, 1.0)) * reciprocalSize
+            );
+            if (type == 0) {
+                values[0][0] = bayer0.w; values[1][0] = bayer1.z; values[2][0] = bayer1.w;
+                values[0][1] = bayer2.y; values[1][1] = bayer3.x; values[2][1] = bayer3.y;
+                values[0][2] = bayer2.w; values[1][2] = bayer3.z; values[2][2] = bayer3.w;
+            } else if (type == 1) {
+                values[0][0] = bayer0.z; values[1][0] = bayer0.w; values[2][0] = bayer1.z;
+                values[0][1] = bayer2.x; values[1][1] = bayer2.y; values[2][1] = bayer3.x;
+                values[0][2] = bayer2.z; values[1][2] = bayer2.w; values[2][2] = bayer3.z;
+            } else if (type == 2) {
+                values[0][0] = bayer0.y; values[1][0] = bayer1.x; values[2][0] = bayer1.y;
+                values[0][1] = bayer0.w; values[1][1] = bayer1.z; values[2][1] = bayer1.w;
+                values[0][2] = bayer2.y; values[1][2] = bayer3.x; values[2][2] = bayer3.y;
+            } else {
+                values[0][0] = bayer0.x; values[1][0] = bayer0.y; values[2][0] = bayer1.x;
+                values[0][1] = bayer0.z; values[1][1] = bayer0.w; values[2][1] = bayer1.z;
+                values[0][2] = bayer2.x; values[1][2] = bayer2.y; values[2][2] = bayer3.x;
+            }
+            return values;
+        }
+
+        vec4 swizzleForType(vec4 value, int type) {
+            if (type == 0) return value.rgba;
+            if (type == 1) return value.grab;
+            if (type == 2) return value.barg;
+            return value.abgr;
+        }
+
+        void sampleNeighborhoodRbf(
+            vec2 sampleUv,
+            vec3 covariance,
+            out vec3 accumulatedIntensities,
+            out vec3 accumulatedWeights,
+            out float sourceNeighborhoodConfidence,
+            out vec3 validAccumulatedWeights
+        ) {
+            accumulatedIntensities = vec3(0.0);
+            accumulatedWeights = vec3(0.0);
+            validAccumulatedWeights = vec3(0.0);
+            sourceNeighborhoodConfidence = 1.0;
+            vec2 coordinateScaled = sampleUv * (vec2(uExtractedSize) * 2.0);
+            ivec2 position = ivec2(coordinateScaled);
+            mat3 bayerValue = get3x3FromExtractedBayer(position);
+            mat3 sourceValidity = mat3(1.0);
+            float headroomStart=max(1.0,uSourceClippingPoint*0.9925);
+            for (int sx=0;sx<3;++sx) {
+                for (int sy=0;sy<3;++sy) {
+                    /* IRIS_26648_SAMPLE_LEVEL_SHORT_VALIDITY
+                     * Saturated SHORT CFA samples receive zero radiometric authority individually.
+                     * One bad sample must not veto other valid same-color evidence in the 3x3 RBF. */
+                    sourceValidity[sx][sy]=1.0-smoothstep(
+                        headroomStart,max(headroomStart+1.0e-4,uSourceClippingPoint),
+                        bayerValue[sx][sy]);
+                }
+            }
+            mat3 weights = mat3(0.0);
+            vec2 subpixelOffset = floor(coordinateScaled) + 0.5 - coordinateScaled;
+            for (int i = -1; i <= 1; ++i) {
+                for (int j = -1; j <= 1; ++j) {
+                    weights[i + 1][j + 1] = kernelWeight(
+                        subpixelOffset + vec2(ivec2(i, j)),
+                        covariance
+                    );
+                }
+            }
+            mat3 validWeights = mat3(0.0);
+            for (int sx=0;sx<3;++sx) {
+                for (int sy=0;sy<3;++sy) {
+                    validWeights[sx][sy]=weights[sx][sy]*sourceValidity[sx][sy];
+                }
+            }
+            ivec2 bayerOffset = ivec2(0);
+            if (uCfaPattern == 0) bayerOffset = ivec2(1, 1);
+            else if (uCfaPattern == 1) bayerOffset = ivec2(0, 1);
+            else if (uCfaPattern == 2) bayerOffset = ivec2(1, 0);
+            int type = (((position.y + bayerOffset.y) & 1) << 1) +
+                ((position.x + bayerOffset.x) & 1);
+            vec4 cornerWeights = vec4(
+                weights[0][0], weights[0][2], weights[2][0], weights[2][2]
+            );
+            vec2 upDownWeights = vec2(weights[1][0], weights[1][2]);
+            vec2 leftRightWeights = vec2(weights[0][1], weights[2][1]);
+            vec4 validCornerWeights = vec4(
+                validWeights[0][0], validWeights[0][2], validWeights[2][0], validWeights[2][2]
+            );
+            vec2 validUpDownWeights = vec2(validWeights[1][0], validWeights[1][2]);
+            vec2 validLeftRightWeights = vec2(validWeights[0][1], validWeights[2][1]);
+            vec4 value1 = vec4(
+                bayerValue[0][0], bayerValue[0][2], bayerValue[2][0], bayerValue[2][2]
+            );
+            vec2 value2 = vec2(bayerValue[1][0], bayerValue[1][2]);
+            vec2 value3 = vec2(bayerValue[0][1], bayerValue[2][1]);
+            vec4 reorderedGains = swizzleForType(uGains, type);
+            vec4 reorderedBlack = swizzleForType(uBlackLevelsTimesGains, type);
+            vec4 intensities = vec4(
+                dot(value1 * reorderedGains.r + reorderedBlack.r, validCornerWeights),
+                dot(value2 * reorderedGains.g + reorderedBlack.g, validUpDownWeights),
+                dot(value3 * reorderedGains.b + reorderedBlack.b, validLeftRightWeights),
+                (bayerValue[1][1] * reorderedGains.a + reorderedBlack.a) * validWeights[1][1]
+            );
+            vec4 reorderedWeights = vec4(
+                dot(cornerWeights, vec4(1.0)),
+                dot(upDownWeights, vec2(1.0)),
+                dot(leftRightWeights, vec2(1.0)),
+                weights[1][1]
+            );
+            vec4 validReorderedWeights = vec4(
+                dot(validCornerWeights, vec4(1.0)),
+                dot(validUpDownWeights, vec2(1.0)),
+                dot(validLeftRightWeights, vec2(1.0)),
+                validWeights[1][1]
+            );
+            intensities = swizzleForType(intensities, type);
+            reorderedWeights = swizzleForType(reorderedWeights, type);
+            validReorderedWeights = swizzleForType(validReorderedWeights, type);
+            accumulatedIntensities = vec3(
+                intensities.r,
+                intensities.g + intensities.b,
+                intensities.a
+            );
+            vec3 fullAccumulatedWeights = vec3(
+                reorderedWeights.r,
+                reorderedWeights.g + reorderedWeights.b,
+                reorderedWeights.a
+            );
+            validAccumulatedWeights = vec3(
+                validReorderedWeights.r,
+                validReorderedWeights.g + validReorderedWeights.b,
+                validReorderedWeights.a
+            );
+            /* RGB is reconstructed only from physically valid SHORT samples. Confidence is the
+             * weakest color's retained RBF support fraction, not the weakest individual sample. */
+            accumulatedWeights = validAccumulatedWeights;
+            sourceNeighborhoodConfidence = minimum3(clamp(
+                validAccumulatedWeights / max(fullAccumulatedWeights, vec3(1.0e-6)),
+                vec3(0.0), vec3(1.0)));
+        }
+
+        void main() {
+            vec2 referenceUv = gl_FragCoord.xy / vec2(uOutputSize);
+            vec4 normalMean = texture(uNormalMean, referenceUv);
+            vec3 totalWeight = vec3(
+                max(texture(uNormalWeightR, referenceUv).r, 0.0),
+                max(texture(uNormalWeightsGb, referenceUv).r, 0.0),
+                max(texture(uNormalWeightsGb, referenceUv).g, 0.0));
+            vec3 validWeight = max(texture(uNormalValidWeights, referenceUv).rgb, vec3(0.0)) *
+                max(uValidityWeightScale, 1.0);
+            vec3 normalValidity = clamp(validWeight / max(totalWeight, vec3(1.0e-6)), 0.0, 1.0);
+            float normalSourceConfidence = minimum3(normalValidity);
+            float normalTemporalConfidence = clamp(normalMean.a, 0.0, 1.0);
+            vec3 normalVariance = max(
+                uNormalShotNoiseRgb * max(normalMean.rgb, vec3(0.0)) + uNormalReadNoiseRgb,
+                vec3(1.0e-8)) / max(1.0, uNormalFrameCount * max(normalTemporalConfidence, 0.10));
+            float normalSnr = luma(max(normalMean.rgb, vec3(0.0))) /
+                sqrt(max(luma(normalVariance), 1.0e-8));
+            float normalSnrConfidence = smoothstep(3.0, 8.0, normalSnr);
+
+            vec2 flowUv = referenceUv * uFlowScaleOffset.xy + uFlowScaleOffset.zw;
+            vec4 flow = texture(uShortFlow, flowUv);
+            vec2 sampleUv = referenceUv + flow.xy;
+            /* Use the exact undilated ordinary Sabre physical blocker. Rejection dilation is deliberately
+             * not an HDR fusion mask: it could widen exposure transitions and create edge halos. */
+            ivec2 physicalCell = clamp(ivec2(referenceUv * vec2(uExtractedSize)),
+                ivec2(0), uExtractedSize - ivec2(1));
+            float physicalWeight = 1.0 - clamp(
+                texelFetch(uShortPhysicalReverseWeight, physicalCell, 0).r, 0.0, 1.0);
+            /* IRIS_26648_SHORT_ALIGNMENT_RESIDUAL_AUTHORITY
+             * flow.w is the existing local affine residual in RAW pixels. It reduces SHORT
+             * authority continuously; coherent local alignment remains untouched, while poor
+             * geometry cannot create color/brightness authority across an edge. */
+            float alignmentConfidence = 1.0 - smoothstep(0.50, 2.00, max(flow.w, 0.0));
+            if (physicalWeight <= 0.001 || alignmentConfidence <= 0.001 ||
+                !shortWarpUvValid(sampleUv)) {
+                oFusedExtendedLinear = normalMean;
+                oDecision = 204.0 / 255.0; // physical geometry/support rejection
+                return;
+            }
+            vec3 covariance = unpackCovariance(texture(uShortCovariance, sampleUv).xyz);
+            vec3 shortColorSum = vec3(0.0);
+            vec3 shortWeights = vec3(0.0);
+            vec3 shortValidWeights = vec3(0.0);
+            float shortSourceConfidence = 1.0;
+            sampleNeighborhoodRbf(sampleUv, covariance, shortColorSum, shortWeights,
+                shortSourceConfidence, shortValidWeights);
+            if (shortSourceConfidence <= 0.001 || minimum3(shortWeights) <= 1.0e-6) {
+                oFusedExtendedLinear = normalMean;
+                oDecision = 224.0 / 255.0; // SHORT source clipped/incomplete
+                return;
+            }
+            vec3 shortRgb = shortColorSum / max(shortWeights, vec3(1.0e-6));
+            vec3 shortVariance = max(
+                uShortShotNoiseRgb * max(shortRgb, vec3(0.0)) + uShortReadNoiseRgb,
+                vec3(1.0e-8));
+            float shortSnr = luma(max(shortRgb, vec3(0.0))) /
+                sqrt(max(luma(shortVariance), 1.0e-8));
+            float shortSnrConfidence = smoothstep(3.0, 8.0, shortSnr);
+            if (shortSnrConfidence <= 0.001) {
+                oFusedExtendedLinear = normalMean;
+                oDecision = 240.0 / 255.0; // SHORT signal/SNR rejection
+                return;
+            }
+
+            /* Radiometric loss is measured in the common exposure-normalized extended-linear
+             * domain. Two coherent RGB channels establish effective censorship before literal
+             * saturation; source CFA validity independently catches one-channel physical loss. */
+            vec3 relativeLoss = max(shortRgb - normalMean.rgb, vec3(0.0)) /
+                max(shortRgb, vec3(0.05));
+            vec3 radiometricLossByChannel = smoothstep(vec3(0.05), vec3(0.12), relativeLoss) *
+                smoothstep(vec3(0.55), vec3(0.90), max(shortRgb, vec3(0.0)));
+            float effectiveRadiometricLoss = secondHighest3(radiometricLossByChannel);
+            float measuredNormalLoss = max(1.0 - normalSourceConfidence, effectiveRadiometricLoss);
+            float shortConfidence = physicalWeight * alignmentConfidence *
+                shortSourceConfidence * shortSnrConfidence;
+            /* Low NORMAL temporal/SNR confidence may only soften a proven radiometric-loss
+             * transition; it can never create SHORT authority by itself. */
+            float transitionSupport = mix(0.82, 1.0,
+                min(normalTemporalConfidence, normalSnrConfidence));
+            float authority = clamp(shortConfidence * measuredNormalLoss / max(transitionSupport, 1.0e-3),
+                0.0, 1.0);
+            vec3 fused = mix(normalMean.rgb, shortRgb, authority);
+            oFusedExtendedLinear = vec4(fused, normalMean.a); // SHORT never inflates temporal support
+            oDecision = authority * (191.0 / 255.0); // 0..191 are valid authority; >191 are rejections
+        }
+    """.trimIndent()
+
+    /* IRIS_26648_FUSION_RADIANCE_WEIGHT_TELEMETRY
+     * Tiny diagnostic only: recover SHORT radiance algebraically from the actual NORMAL/fused
+     * production values and final scalar authority. It never feeds production reconstruction. */
+    val universalFusionTelemetry26648 = """
+        #version 300 es
+        precision highp float;
+        uniform sampler2D uNormalMean;
+        uniform sampler2D uFusedMean;
+        uniform sampler2D uDecision;
+        uniform ivec2 uProbeSize;
+        layout(location = 0) out vec4 oTelemetry;
+        float luma(vec3 v) { return dot(v, vec3(0.25, 0.50, 0.25)); }
+        void main() {
+            vec2 uv = gl_FragCoord.xy / vec2(uProbeSize);
+            float encoded = texture(uDecision, uv).r;
+            float authority = encoded <= (191.5 / 255.0)
+                ? clamp(encoded / (191.0 / 255.0), 0.0, 1.0) : 0.0;
+            float normalRadiance = luma(max(texture(uNormalMean, uv).rgb, vec3(0.0)));
+            float fusedRadiance = luma(max(texture(uFusedMean, uv).rgb, vec3(0.0)));
+            float shortRadiance = authority > 0.005
+                ? max((fusedRadiance - (1.0 - authority) * normalRadiance) / authority, 0.0)
+                : normalRadiance;
+            oTelemetry = vec4(normalRadiance, shortRadiance, authority, fusedRadiance);
+        }
+    """.trimIndent()
+
+    /* IRIS_26648_SHORT_COLOR_VALIDITY_NOT_TEMPORAL_SUPPORT
+     * The same whole-RGB radiometric authority may restore color-validity provenance so the
+     * downstream chroma owner cannot reject real SHORT evidence. It adds no temporal weight and
+     * changes neither NORMAL support nor denoise effective-frame accounting. */
+    val universalShortValidityAugment26648 = """
+        #version 300 es
+        precision highp float;
+        uniform sampler2D uDecision;
+        uniform sampler2D uNormalWeightR;
+        uniform sampler2D uNormalWeightsGb;
+        uniform float uValidityWeightScale;
+        layout(location = 0) out vec4 oValidityAdd;
+        void main() {
+            vec2 uv = gl_FragCoord.xy / vec2(textureSize(uNormalWeightR, 0));
+            float encoded = texture(uDecision, uv).r;
+            float authority = encoded <= (191.5 / 255.0)
+                ? clamp(encoded / (191.0 / 255.0), 0.0, 1.0) : 0.0;
+            vec3 totalWeight = vec3(
+                max(texture(uNormalWeightR, uv).r, 0.0),
+                max(texture(uNormalWeightsGb, uv).r, 0.0),
+                max(texture(uNormalWeightsGb, uv).g, 0.0));
+            oValidityAdd = vec4(totalWeight * authority /
+                max(uValidityWeightScale, 1.0e-6), 0.0);
+        }
+    """.trimIndent()
+
     /* IRIS_26558_SABRE_SHADOW_LONG_SOURCE_CLIP_GUARD
      * Night-only Sabre merge. The proven Motion/NORMAL merge shader above remains byte-for-byte
      * unchanged. For SHADOW_LONG only, reject the whole Long observation at an output pixel if
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 4b9058974196957358421ecf21bf4228e83fe2ab..548cd4874a8fa33b6062a144de16d1bd63d58280 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -589,6 +589,9 @@ internal class GlesMgcRawSpatialStacker(
     private var sabreShortAccumulatorProxyProgram26626 = 0
     private var sabreShortAccumulatorDeltaProgram26626 = 0
     private var sabreShortRescueWeightProgram26607 = 0
+    private var sabreUniversalRadiometricFusionProgram26648 = 0
+    private var sabreUniversalShortValidityAugmentProgram26648 = 0
+    private var sabreUniversalFusionTelemetryProgram26648 = 0
     private var sabreRgbChromaPostprocessor: GlesIris26529SpatialRgbChromaPostprocessor? = null
     private var sabreConvertAlignmentSparseProgram = 0
     private var currentMergeCovariance = 0
@@ -2435,6 +2438,14 @@ internal class GlesMgcRawSpatialStacker(
             }
             GlesGpuScheduler.yieldToUiRenderer()
 
+            /* IRIS_26648_UNIVERSAL_SHORT_RETAINED_EVIDENCE
+             * SHORT is final by contract. Retain only its aligned RBF inputs and physical support
+             * until NORMAL has been fully accumulated/dehomogenized; SHORT never enters temporal
+             * coverage, temporal noise support, DNG, or true-2x high-frequency evidence. */
+            var retainedShortFlow26648: SabreConvertedAlignment? = null
+            var retainedShortPhysicalReverseWeight26648 = 0
+            var retainedShortCalibration26648: FrameCalibration? = null
+
             for (index in 1 until frames.size) {
                 val transientTextureStart = textures.size
                 val frameStartNs = System.nanoTime()
@@ -2473,22 +2484,6 @@ internal class GlesMgcRawSpatialStacker(
                     referenceAlignmentProducts,
                 )
                 val flow = createSabreConvertedAlignment(alignment)
-                val shortFallbackAffine26625 = if (
-                    frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT
-                ) {
-                    analyzeSabreShortFallbackAffine26625(flow, index)
-                } else {
-                    null
-                }
-                var mergeFlow = flow
-                var shortFallbackTrustedCells26625 = 0
-                var shortComponentMembershipCells26626 = 0
-                var shortUntrustedComponentCells26626 = 0
-                var shortHardBarrierFrontierCells26626 = 0
-                var shortOpenFrontierCells26626 = 0
-                var shortContributionMask26626 = 0
-                var shortAccumulatorBefore26626 = 0
-                var shortAccumulatorContribution26626: ShortAccumulatorContribution26626? = null
                 val unblockerWidth = ceilDiv(width, UNBLOCKER_FULLRES_TILE_SIZE * 2)
                 val unblockerHeight = ceilDiv(height, UNBLOCKER_FULLRES_TILE_SIZE * 2)
                 val unblocker = createTexture(
@@ -2547,291 +2542,74 @@ internal class GlesMgcRawSpatialStacker(
                     GLES30.GL_R8,
                     GLES30.GL_LINEAR,
                 )
-                var shortRescueOnlyWeight26607 = 0
-                var shortLossCandidateWeight26607 = 0
-                var shortLossStats26623: ShortEffectiveCoverageStats26606? = null
-                var shortPhysicalWeight26610 = 0
                 renderDilation(reverseWeight, frameWeight)
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     check(shortPhysicalReverseWeight26610 != 0) {
-                        "26610 SHORT physical reverse weight missing"
+                        "26648 SHORT physical reverse weight missing"
                     }
-                    shortPhysicalWeight26610 = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR,
-                    )
-                    /* Exact same dilation implementation as ordinary NORMAL/Sabre weight. */
-                    renderDilation(shortPhysicalReverseWeight26610, shortPhysicalWeight26610)
-                    releaseOwnedTexture(
-                        shortPhysicalReverseWeight26610,
-                        "26610 SHORT exact ordinary-rejection physical reverse weight",
-                    )
-                    val component = createSabreShortComponentTrust26607(
-                        referenceRaw = referenceRaw,
-                        shortRaw = currentRaw,
-                        flow = flow,
-                        referenceCalibration = referenceCalibration,
-                        shortCalibration = calibration,
-                        exposureRatio = exposureScale,
-                        fallbackAffine = checkNotNull(shortFallbackAffine26625),
-                    )
-                    mergeFlow = component.effectiveFlow
-                    shortFallbackTrustedCells26625 = component.fallbackTrustedCells
-                    shortComponentMembershipCells26626 = component.componentMembershipCells26626
-                    shortUntrustedComponentCells26626 = component.untrustedComponentCells26626
-                    shortHardBarrierFrontierCells26626 = component.hardBarrierFrontierCells26626
-                    shortOpenFrontierCells26626 = component.openFrontierCells26626
-                    highlightShortBoundarySeedCells26607 = component.seedCells
-                    highlightShortBoundaryFinalCells26607 = component.finalCells
-                    val rescuedWeight = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR,
-                    )
-                    shortRescueOnlyWeight26607 = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_LINEAR,
-                    )
-                    shortLossCandidateWeight26607 = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_NEAREST,
-                    )
-                    renderSabreShortRescueWeight26607(
-                        ordinaryWeight = frameWeight,
-                        physicalWeight = shortPhysicalWeight26610,
-                        referenceExtracted = referenceExtracted,
-                        shortExtracted = currentExtracted,
-                        flow = mergeFlow,
-                        componentTrust = component.texture,
-                        referenceCalibration = referenceCalibration,
-                        shortCalibration = calibration,
-                        exposureRatio = exposureScale,
-                        output = rescuedWeight,
-                        rescueOnlyOutput = shortRescueOnlyWeight26607,
-                        lossCandidateOutput = shortLossCandidateWeight26607,
-                        coverageWidth = coverageWidth,
-                        coverageHeight = coverageHeight,
-                        extractedWidth = extractedWidth,
-                        extractedHeight = extractedHeight,
-                    )
-                    val lossStats = readSabreShortEffectiveCoverage26606(
-                        shortLossCandidateWeight26607, coverageWidth, coverageHeight,
-                        "IRIS_26607_SHORT_NORMAL_LOSS_CANDIDATES",
-                        retainBytes = true,
-                    )
-                    highlightShortLossCandidateCells26607 = lossStats.nonzeroCells
-                    shortLossStats26623 = lossStats
-                    val componentWeightStats = readSabreShortEffectiveCoverage26606(
-                        shortRescueOnlyWeight26607, coverageWidth, coverageHeight,
-                        "IRIS_26607_SHORT_COMPONENT_WEIGHT",
-                    )
-                    highlightShortComponentWeightedCells26607 = componentWeightStats.nonzeroCells
-                    releaseOwnedTexture(
-                        shortLossCandidateWeight26607, "26607 SHORT loss-candidate proof",
-                    )
-                    shortLossCandidateWeight26607 = 0
-                    releaseOwnedTexture(frameWeight, "26607 ordinary SHORT post-dilation weight")
-                    releaseOwnedTexture(
-                        shortPhysicalWeight26610,
-                        "26610 SHORT shared physical post-dilation weight",
-                    )
-                    shortPhysicalWeight26610 = 0
-                    releaseOwnedTexture(component.texture, "26607 SHORT component trust")
-                    frameWeight = rescuedWeight
+                    /* IRIS_26648_NO_HDR_MASK_DILATION
+                     * Retain the exact physical blocker emitted by ordinary Sabre rejection.
+                     * Do not dilate/blur/propagate it into an exposure-fusion mask. */
+                    releaseOwnedTexture(frameWeight, "26648 retired ordinary SHORT photometric weight")
+                    frameWeight = 0
+                    retainedShortFlow26648 = flow
+                    retainedShortPhysicalReverseWeight26648 = shortPhysicalReverseWeight26610
+                    retainedShortCalibration26648 = calibration
+                    highlightShortScheduled26607 = true
+                    highlightShortConfidenceFieldGenerated26607 = true
+                    highlightShortExposureRatio26587 = exposureScale
                 }
-                /* IRIS_26606_CONTINUOUS_ONE_TUNNEL_NORMAL_SHORT_LONG
-                 * No role owns a private reconstruction. NORMAL, HIGHLIGHT_SHORT and SHADOW_LONG
-                 * use the same Sabre frameWeight, same source-clipped support, same homogeneous RBF
-                 * accumulator, same Resolve and same VGN. Source clipping is evaluated on the exact
-                 * 3x3 CFA footprint consumed by the RBF and is one scalar for the complete RGB
-                 * observation. SHORT has no propagated mask, private mean or post-merge fuse.
-                 */
-                if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
-                    val effectiveCoverage = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_NEAREST,
-                    )
-                    clearSabreSingleChannelTexture26606(
-                        effectiveCoverage, coverageWidth, coverageHeight,
-                        "26606 SHORT effective source-clipped coverage clear",
-                    )
-                    renderSabreUnifiedBracketCoverage26604(
-                        weight = frameWeight,
-                        extracted = currentExtracted,
-                        flow = mergeFlow,
-                        accumulatedCoverage = effectiveCoverage,
-                        coverageWidth = coverageWidth,
-                        coverageHeight = coverageHeight,
-                        extractedWidth = extractedWidth,
-                        extractedHeight = extractedHeight,
-                        accumulatedWeightScale = 1f,
-                    )
-                    val effectiveStats = readSabreShortEffectiveCoverage26606(
-                        effectiveCoverage, coverageWidth, coverageHeight,
-                        "IRIS_26607_SHORT_TOTAL_ACCUMULATOR_ELIGIBLE_COVERAGE",
-                    )
-                    highlightShortTotalAccumulatorEligibleCells26607 = effectiveStats.nonzeroCells
-                    highlightShortAnyAccumulatorEligibleEvidence26607 =
-                        if (effectiveStats.nonzeroCells > 0) 1 else 0
-
-                    check(shortRescueOnlyWeight26607 != 0) {
-                        "26607 SHORT target-rescue weight missing at exact post-source-clip eligibility proof"
+                /* IRIS_26648_NORMAL_TEMPORAL_STACK_SHORT_EXCLUDED
+                 * HIGHLIGHT_SHORT does not alter accumulatedColor/weights/coverage. NORMAL and
+                 * Night SHADOW_LONG retain the successful 26646 temporal architecture; SHORT is
+                 * retained only for the later universal radiometric owner. */
+                if (frame.role != RawBurstFrameRole.HIGHLIGHT_SHORT) {
+                    if (highlightShortOneTunnel26604 || frame.role == RawBurstFrameRole.SHADOW_LONG) {
+                        renderSabreUnifiedBracketCoverage26604(
+                            weight = frameWeight,
+                            extracted = currentExtracted,
+                            flow = flow,
+                            accumulatedCoverage = accumulatedCoverage,
+                            coverageWidth = coverageWidth,
+                            coverageHeight = coverageHeight,
+                            extractedWidth = extractedWidth,
+                            extractedHeight = extractedHeight,
+                            accumulatedWeightScale = accumulatedWeightScale,
+                        )
+                    } else {
+                        renderSabreCoverage(
+                            frameWeight, accumulatedCoverage, coverageWidth, coverageHeight,
+                            accumulatedWeightScale,
+                        )
                     }
-                    val rescueEffectiveCoverage = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_NEAREST,
-                    )
-                    clearSabreSingleChannelTexture26606(
-                        rescueEffectiveCoverage, coverageWidth, coverageHeight,
-                        "26606 SHORT clipped-rescue effective coverage clear",
-                    )
-                    renderSabreUnifiedBracketCoverage26604(
-                        weight = shortRescueOnlyWeight26607,
-                        extracted = currentExtracted,
-                        flow = mergeFlow,
-                        accumulatedCoverage = rescueEffectiveCoverage,
-                        coverageWidth = coverageWidth,
-                        coverageHeight = coverageHeight,
-                        extractedWidth = extractedWidth,
-                        extractedHeight = extractedHeight,
-                        accumulatedWeightScale = 1f,
-                    )
-                    val rescueStats = readSabreShortEffectiveCoverage26606(
-                        rescueEffectiveCoverage, coverageWidth, coverageHeight,
-                        "IRIS_26607_SHORT_TARGET_ACCUMULATOR_ELIGIBLE_COVERAGE",
-                        retainBytes = true,
-                    )
-                    logSabreShortBroadLossSupport26623(
-                        shortLossStats26623?.retainedBytes, rescueStats.retainedBytes,
-                        coverageWidth, coverageHeight,
-                    )
-                    logSabreShortLossComponents26638(
-                        shortLossStats26623?.retainedBytes, rescueStats.retainedBytes,
-                        coverageWidth, coverageHeight,
-                    )
-                    highlightShortTargetAccumulatorEligibleCells26607 = rescueStats.nonzeroCells
-                    highlightShortTargetEligibleCellsAbi26607 = rescueStats.nonzeroCells
-                    /* IRIS_26626_SHORT_ACCUMULATOR_PROOF_SNAPSHOT
-                     * Copy only the small target-rescue mask and normalized common accumulator proxy.
-                     * Existing rescue/effective coverage release timing remains unchanged below. */
-                    shortContributionMask26626 = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_R8, GLES30.GL_NEAREST,
-                    )
-                    copySabreR8Mask26626(
-                        rescueEffectiveCoverage, shortContributionMask26626,
-                        coverageWidth, coverageHeight,
-                    )
-                    shortAccumulatorBefore26626 = createTexture(
-                        coverageWidth, coverageHeight, GLES30.GL_RGBA16F, GLES30.GL_NEAREST,
-                    )
-                    renderSabreShortAccumulatorProxy26626(
-                        accumulatedColor, accumulatedWeightsGb, shortAccumulatorBefore26626,
-                        coverageWidth, coverageHeight,
-                    )
-                    renderSabreCoverage(
-                        effectiveCoverage, accumulatedCoverage, coverageWidth, coverageHeight,
-                        accumulatedWeightScale,
-                    )
-                    releaseOwnedTexture(effectiveCoverage, "26606 SHORT effective coverage")
-                    shortLossStats26623 = null
-                    releaseOwnedTexture(
-                        rescueEffectiveCoverage, "26606 SHORT clipped-rescue effective coverage",
-                    )
-                    releaseOwnedTexture(
-                        shortRescueOnlyWeight26607, "26607 SHORT target-rescue weight",
-                    )
-                    shortRescueOnlyWeight26607 = 0
-                } else if (highlightShortOneTunnel26604 ||
-                    frame.role == RawBurstFrameRole.SHADOW_LONG) {
-                    renderSabreUnifiedBracketCoverage26604(
-                        weight = frameWeight,
+                    renderSabreMerge(
                         extracted = currentExtracted,
                         flow = flow,
-                        accumulatedCoverage = accumulatedCoverage,
-                        coverageWidth = coverageWidth,
-                        coverageHeight = coverageHeight,
+                        covariance = currentCovariance,
+                        weight = frameWeight,
+                        calibration = calibration,
+                        accumulatedColor = accumulatedColor,
+                        accumulatedWeightsGb = accumulatedWeightsGb,
                         extractedWidth = extractedWidth,
                         extractedHeight = extractedHeight,
-                        accumulatedWeightScale = accumulatedWeightScale,
+                        useFrameWeight = true,
+                        sourceClipGuard = frame.role == RawBurstFrameRole.SHADOW_LONG,
+                        sourceClippedWeight = 0f,
+                        accumulatedValidity26614 = accumulatedValidity26614,
+                        validityWeightScale26614 = validityWeightScale26614,
                     )
                 } else {
-                    renderSabreCoverage(
-                        frameWeight, accumulatedCoverage, coverageWidth, coverageHeight,
-                        accumulatedWeightScale,
-                    )
-                }
-                renderSabreMerge(
-                    extracted = currentExtracted,
-                    flow = mergeFlow,
-                    covariance = currentCovariance,
-                    weight = frameWeight,
-                    calibration = calibration,
-                    accumulatedColor = accumulatedColor,
-                    accumulatedWeightsGb = accumulatedWeightsGb,
-                    extractedWidth = extractedWidth,
-                    extractedHeight = extractedHeight,
-                    useFrameWeight = true,
-                    sourceClipGuard = highlightShortOneTunnel26604 || frame.role == RawBurstFrameRole.SHADOW_LONG,
-                    sourceClippedWeight = 0f,
-                    accumulatedValidity26614 = accumulatedValidity26614,
-                    validityWeightScale26614 = validityWeightScale26614,
-                )
-                if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT &&
-                    shortContributionMask26626 != 0 && shortAccumulatorBefore26626 != 0) {
-                    shortAccumulatorContribution26626 =
-                        measureSabreShortAccumulatorContribution26626(
-                            shortAccumulatorBefore26626, shortContributionMask26626,
-                            accumulatedColor, accumulatedWeightsGb,
-                            coverageWidth, coverageHeight,
-                        )
-                    releaseOwnedTexture(
-                        shortAccumulatorBefore26626, "26626 SHORT pre-merge accumulator proxy",
-                    )
-                    shortAccumulatorBefore26626 = 0
-                    releaseOwnedTexture(
-                        shortContributionMask26626, "26626 SHORT target contribution mask",
-                    )
-                    shortContributionMask26626 = 0
-                }
-                if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     require(index == frames.lastIndex) {
-                        "26604 HIGHLIGHT_SHORT must remain the final scheduled auxiliary frame"
+                        "26648 HIGHLIGHT_SHORT must remain the final scheduled auxiliary frame"
                     }
-                    highlightShortExposureRatio26587 = exposureScale
-                    highlightShortScheduled26607 = true
-                    highlightShortConfidenceFieldGenerated26607 = true
-                    val details26607 =
+                    val details26648 =
                         "exposureRatio=$exposureScale exposureTimeNs=${frame.exposureTimeNs} " +
-                            "iso=${frame.sensitivityIso} sensorWhite=$sensorWhiteLevel " +
-                            "black=${calibration.blackLevels.joinToString(prefix="[",postfix="]")} " +
-                            "captured=true scheduled=true commonAccumulatorPath=true " +
-                            "privateShortAccumulator=false lateRgbBlend=false normalRejectionMeasurableOnly=true " +
-                            "targetOwner=UNIVERSAL_SENSOR_LOSS literalPlusEffective=true " +
-                            "effectiveLoss=twoPhase5to12pct_or_severeNearSaturation " +
-                            "absoluteOwner=SAME_CFA_MEASURABLE_BOUNDARY_RADIOMETRY probes=5x5 " +
-                            "boundaryError=0.03..0.08 boundaryResidualRawPx=0.35..0.95 " +
-                            "componentFlowBarrierRawPx=0.50..1.25 " +
-                            "boundaryLocalResidualRequired=true " +
-                            "fallbackGeometry=ROBUST_GLOBAL_AFFINE_FROM_STRICT_FLOW_INLIERS " +
-                            "fallbackGeometryCannotAuthorizeAlone=true " +
-                            "fallbackActualSampling=true " +
-                            "fallbackTrustedCells=$shortFallbackTrustedCells26625 " +
-                            "componentMembershipCells=$shortComponentMembershipCells26626 " +
-                            "untrustedComponentCells=$shortUntrustedComponentCells26626 " +
-                            "hardBarrierFrontierCells=$shortHardBarrierFrontierCells26626 " +
-                            "openFrontierCells=$shortOpenFrontierCells26626 " +
-                            "literalCoreSelfSeed=false effectiveCoreSelfSeed=false " +
-                            "componentMembership=LITERAL_PLUS_BOUNDARY_PROVEN_EFFECTIVE_LOSS " +
-                            "effectiveLossPhotometricBypass=BOUNDARY_PROVEN_COMPONENT_ONLY_ADDITIVE " +
-                            "shortInteriorSamples=ACTUAL_ALIGNED_SAME_CFA_SHORT noSpatialFill=true " +
-                            "shortHeadroomOwner=SAME_AS_COMMON_RBF_FINAL_0P75_PERCENT " +
-                            "boundarySeedCells=$highlightShortBoundarySeedCells26607 " +
-                            "componentTrustedCells=$highlightShortBoundaryFinalCells26607 " +
-                            "normalLossCandidateCells=$highlightShortLossCandidateCells26607 " +
-                            "componentWeightedTargetCells=$highlightShortComponentWeightedCells26607 " +
-                            "targetAccumulatorEligibleCells=$highlightShortTargetAccumulatorEligibleCells26607 " +
-                            "totalShortAccumulatorEligibleCells=$highlightShortTotalAccumulatorEligibleCells26607 " +
-                            "commonAccumulatorTargetProbeCells=${shortAccumulatorContribution26626?.targetCells ?: 0} " +
-                            "commonAccumulatorChangedProbeCells=${shortAccumulatorContribution26626?.changedCells ?: 0} " +
-                            "commonAccumulatorInputChanged=${(shortAccumulatorContribution26626?.changedCells ?: 0) > 0} " +
-                            "resolveEffective=NOT_DIRECTLY_MEASURED outputPreserved=DEVICE_IMAGE_REQUIRED " +
-                            "sourceClip3x3Continuous=true bayerQuadWholeRgb=true " +
-                            "dng=false srDetailEvidence=false"
-                    PLog.i(SABRE_TAG, "IRIS_26607_SHORT_SEMANTIC_FUNNEL $details26607")
-                    MotionTrace.processingState("IRIS_26607_SHORT_SEMANTIC_FUNNEL", details26607)
+                            "iso=${frame.sensitivityIso} captured=true scheduled=true " +
+                            "temporalAccumulator=false temporalSupport=false dng=false srDetail=false " +
+                            "owner=UNIVERSAL_RADIOMETRIC_FUSION sceneSemantic=false gradientGate=false " +
+                            "componentPropagation=false borderExtrapolation=false wholeRgbAuthority=true"
+                    PLog.i(SABRE_TAG, "IRIS_26648_SHORT_CAPTURE_TO_FUSION $details26648")
+                    MotionTrace.processingState("IRIS_26648_SHORT_CAPTURE_TO_FUSION", details26648)
                 }
                 /* IRIS_26564_NIGHT_LONG_EXCLUDED_FROM_TRUE_2X
                  * Native Sabre still admits validated SHADOW_LONG evidence for Night SNR. True 2x
@@ -2883,7 +2661,15 @@ internal class GlesMgcRawSpatialStacker(
                         "role=${frame.role} exposureScale=$exposureScale " +
                         "durationRobustness=$durationRobustness submit=${elapsedMs(frameStartNs)}ms",
                 )
-                releaseTexturesFrom(transientTextureStart)
+                if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
+                    val retainedFlow = checkNotNull(retainedShortFlow26648)
+                    releaseTexturesFromExcept(
+                        transientTextureStart,
+                        intArrayOf(retainedFlow.texture, retainedShortPhysicalReverseWeight26648),
+                    )
+                } else {
+                    releaseTexturesFrom(transientTextureStart)
+                }
                 GlesGpuScheduler.yieldToUiRenderer()
             }
 
@@ -2956,39 +2742,79 @@ internal class GlesMgcRawSpatialStacker(
                 )
             }
 
-            /* IRIS_26604_ONE_TUNNEL_RESOLVE_AUTHORITY
-             * The compiler-tested common accumulatedColor/accumulatedWeightsGb are the only RGB
-             * authority entering Resolve/VGN. There is no SHORT mean, handoff mask, accumulator
-             * switch, protected fuse, or post-VGN compositor on the active path.
-             */
+            /* IRIS_26648_ONE_EXTENDED_LINEAR_HDR_MASTER
+             * Dehomogenize the NORMAL temporal result first. A physically validated, aligned and
+             * exposure-normalized SHORT is then fused once in extended-linear space with one RGB
+             * authority. This is the sole HDR exposure owner consumed by Resolve/VGN and true-2x. */
             val resolveAccumulatedColor26604 = accumulatedColor
             val resolveAccumulatedWeightsGb26604 = accumulatedWeightsGb
-            if (highlightShortScheduled26607) {
-                PLog.i(
-                    SABRE_TAG,
-                    "IRIS_26604_SINGLE_COMMON_ACCUMULATOR_OWNERSHIP " +
-                        "domain=homogeneousReferenceExposure sharedRbf=true sharedResolve=true sharedVgn=true " +
-                        "privateShortMean=false propagatedShortMask=false lateRgbBlend=false " +
-                        "temporalSupportOwner=${if (shadowLongFrameCount > 0) "NORMAL_SHORT_LONG" else "NORMAL_SHORT"} " +
-                            "longOwner=${if (shadowLongFrameCount > 0) "NIGHT_COMMON_SABRE" else "EXCLUDED"} " +
-                            "dngOwner=NORMAL srDetailOwner=NORMAL",
-                )
-            }
-            val effectiveEvidenceCount26607 =
-                mergedFrameCount + highlightShortAnyAccumulatorEligibleEvidence26607
-            require(effectiveEvidenceCount26607 >= 1)
             val sabreRSupport26613 = renderSabreDehomogenize(
                 resolveAccumulatedColor26604,
                 resolveAccumulatedWeightsGb26604,
                 accumulatedCoverage,
-                alphaScale = accumulatedWeightScale / effectiveEvidenceCount26607.toFloat(),
-                alphaBias = 1f / effectiveEvidenceCount26607.toFloat(),
-            )
-            check(sabreRSupport26613 != 0) { "26613 Sabre R support provenance missing" }
-            // The original merge shaders write normalized, white-balanced half floats. The
-            // Resolve AOT accepts those RGBA16F bit patterns directly; rawScale applies only to
-            // the black/white scalar parameters below.
-            sabreAccumulatedReadback = readSabreAccumulatedRgba16f(resolveAccumulatedColor26604)
+                alphaScale = accumulatedWeightScale / mergedFrameCount.toFloat().coerceAtLeast(1f),
+                alphaBias = 1f / mergedFrameCount.toFloat().coerceAtLeast(1f),
+            )
+            check(sabreRSupport26613 != 0) { "26648 Sabre R support provenance missing" }
+
+            var fusionDecision26648 = 0
+            var resolveExtendedLinear26648 = resolveAccumulatedColor26604
+            if (highlightShortScheduled26607) {
+                val shortFlow = checkNotNull(retainedShortFlow26648) {
+                    "26648 scheduled SHORT lost alignment owner"
+                }
+                val shortCalibration = checkNotNull(retainedShortCalibration26648) {
+                    "26648 scheduled SHORT lost radiometric calibration"
+                }
+                check(retainedShortPhysicalReverseWeight26648 != 0) {
+                    "26648 scheduled SHORT lost physical support"
+                }
+                val fused = createTexture(width, height, GLES30.GL_RGBA16F, GLES30.GL_NEAREST)
+                fusionDecision26648 = createTexture(width, height, GLES30.GL_R8, GLES30.GL_NEAREST)
+                renderSabreUniversalRadiometricFusion26648(
+                    normalMean = resolveAccumulatedColor26604,
+                    normalWeightR = sabreRSupport26613,
+                    normalWeightsGb = resolveAccumulatedWeightsGb26604,
+                    normalValidWeights = accumulatedValidity26614,
+                    shortExtracted = currentExtracted,
+                    shortFlow = shortFlow,
+                    shortCovariance = currentCovariance,
+                    shortPhysicalReverseWeight = retainedShortPhysicalReverseWeight26648,
+                    shortCalibration = shortCalibration,
+                    normalCalibration = referenceCalibration,
+                    normalFrameCount = mergedFrameCount,
+                    fusedOutput = fused,
+                    decisionOutput = fusionDecision26648,
+                    extractedWidth = extractedWidth,
+                    extractedHeight = extractedHeight,
+                    validityWeightScale = validityWeightScale26614,
+                )
+                logSabreUniversalFusionDecision26648(fusionDecision26648)
+                logSabreUniversalFusionRadiance26648(
+                    resolveAccumulatedColor26604, fused, fusionDecision26648,
+                )
+                renderSabreUniversalShortValidityAugment26648(
+                    decision = fusionDecision26648,
+                    normalWeightR = sabreRSupport26613,
+                    normalWeightsGb = resolveAccumulatedWeightsGb26604,
+                    accumulatedValidity = accumulatedValidity26614,
+                    validityWeightScale = validityWeightScale26614,
+                )
+                resolveExtendedLinear26648 = fused
+                releaseOwnedTexture(retainedShortPhysicalReverseWeight26648, "26648 retained SHORT physical reverse blocker")
+                releaseOwnedTexture(shortFlow.texture, "26648 retained SHORT alignment")
+                retainedShortPhysicalReverseWeight26648 = 0
+                retainedShortFlow26648 = null
+            }
+            // Resolve consumes exactly the single fused extended-linear master.
+            sabreAccumulatedReadback = readSabreAccumulatedRgba16f(resolveExtendedLinear26648)
+            if (resolveExtendedLinear26648 != resolveAccumulatedColor26604) {
+                releaseOwnedTexture(resolveExtendedLinear26648, "26648 fusedExtendedLinear HDR master after Resolve input readback")
+            }
+            if (fusionDecision26648 != 0) {
+                releaseOwnedTexture(fusionDecision26648, "26648 universal fusion decision telemetry")
+                fusionDecision26648 = 0
+            }
             sabreResolvedRgb = checkNotNull(
                 LargeDirectBuffer.allocate(
                     width.toLong() * height * 3L * Short.SIZE_BYTES,
@@ -3155,6 +2981,15 @@ internal class GlesMgcRawSpatialStacker(
              * is no independently reconstructed SHORT RGB texture and no post-VGN exposure blend.
              */
             val nativeHdrAuthority26601 = exportedTexture
+            if (highlightShortScheduled26607) {
+                val details26648 =
+                    "master=fusedExtendedLinear resolveInputExact=true " +
+                        "postVgnProbe=IRIS_26605_HDR_DOMAIN_STATS postVgnMaster=true " +
+                        "true2xUnifiedGuide=${enableSabreSuperRes} srSpecificHdrOwner=false " +
+                        "jpegHeicCommonHdrSource=true"
+                PLog.i(SABRE_TAG, "IRIS_26648_FUSION_OUTPUT_PRESERVED $details26648")
+                MotionTrace.processingState("IRIS_26648_FUSION_OUTPUT_PRESERVED", details26648)
+            }
             if (enableSabreSuperRes) {
                 val reconstructionStartNs=System.nanoTime()
                 val reconstructionEvidence=true2xFastPhaseSlots?.filterNotNull()?:true2xEvidence
@@ -3204,19 +3039,18 @@ internal class GlesMgcRawSpatialStacker(
              * evidence remained NORMAL-only. Nothing is composited or republished after SR.
              */
             if (highlightShortScheduled26607) {
-                PLog.i(
-                    SABRE_TAG,
-                    "IRIS_26607_SHORT_SEMANTIC_RESULT exposureRatio=$highlightShortExposureRatio26587 " +
-                        "normalLossCandidateCells=$highlightShortLossCandidateCells26607 " +
-                        "componentWeightedTargetCells=$highlightShortComponentWeightedCells26607 " +
-                        "targetAccumulatorEligibleCells=$highlightShortTargetAccumulatorEligibleCells26607 " +
-                        "totalShortAccumulatorEligibleCells=$highlightShortTotalAccumulatorEligibleCells26607 " +
-                        "boundarySeedCells=$highlightShortBoundarySeedCells26607 " +
-                        "componentTrustedCells=$highlightShortBoundaryFinalCells26607 " +
-                        "resolveEffective=NOT_DIRECTLY_MEASURED outputPreserved=DEVICE_IMAGE_REQUIRED " +
-                        "domain=COMMON_SABRE_PRE_RESOLVE_VGN wholeRgbEvidence=true lateRgbBlend=false " +
-                        "true2xGuide=${enableSabreSuperRes} true2xDetailEvidenceShort=false",
-                )
+                val fusionSummary26648 =
+                    "exposureRatio=$highlightShortExposureRatio26587 " +
+                        "owner=UNIVERSAL_NORMAL_SHORT_RADIOMETRIC_FUSION " +
+                        "shortInTemporalAccumulator=false wholeRgbAuthority=true gradientGate=false " +
+                        "spatialFill=false dilation=false borderExtrapolation=false " +
+                        "fusionDecisionMeasured=IRIS_26648_UNIVERSAL_RADIOMETRIC_FUSION " +
+                        "radianceWeightsMeasured=IRIS_26648_FUSION_RADIANCE_WEIGHTS " +
+                        "postVgnMeasured=IRIS_26605_HDR_DOMAIN_STATS " +
+                        "true2xGuide=${enableSabreSuperRes} true2xDetailEvidenceShort=false " +
+                        "uhdrSource=COMMON_POST_VGN_HDR_MASTER"
+                PLog.i(SABRE_TAG, "IRIS_26648_FUSION_SEMANTIC_RESULT $fusionSummary26648")
+                MotionTrace.processingState("IRIS_26648_FUSION_SEMANTIC_RESULT", fusionSummary26648)
             }
             checkGlError("MGC Sabre Resolve/VGN color noise")
             returned = true
@@ -4507,6 +4341,192 @@ internal class GlesMgcRawSpatialStacker(
     private fun sabreShadowLongSourceClippingPoint(): Float =
         sensorWhiteLevel.coerceAtLeast(1f) - 0.5f
 
+    private fun renderSabreUniversalRadiometricFusion26648(
+        normalMean: Int,
+        normalWeightR: Int,
+        normalWeightsGb: Int,
+        normalValidWeights: Int,
+        shortExtracted: Int,
+        shortFlow: SabreConvertedAlignment,
+        shortCovariance: Int,
+        shortPhysicalReverseWeight: Int,
+        shortCalibration: FrameCalibration,
+        normalCalibration: FrameCalibration,
+        normalFrameCount: Int,
+        fusedOutput: Int,
+        decisionOutput: Int,
+        extractedWidth: Int,
+        extractedHeight: Int,
+        validityWeightScale: Float,
+    ) {
+        val program = sabreUniversalRadiometricFusionProgram26648
+        check(program != 0)
+        GLES30.glUseProgram(program)
+        bindTexture(program, "uNormalMean", 0, normalMean)
+        bindTexture(program, "uNormalWeightR", 1, normalWeightR)
+        bindTexture(program, "uNormalWeightsGb", 2, normalWeightsGb)
+        bindTexture(program, "uNormalValidWeights", 3, normalValidWeights)
+        bindTexture(program, "uShortExtractedBayer", 4, shortExtracted)
+        bindTexture(program, "uShortFlow", 5, shortFlow.texture)
+        bindTexture(program, "uShortCovariance", 6, shortCovariance)
+        bindTexture(program, "uShortPhysicalReverseWeight", 7, shortPhysicalReverseWeight)
+        uniform2i(program, "uExtractedSize", extractedWidth, extractedHeight)
+        uniform2i(program, "uOutputSize", width, height)
+        uniformSabreFlowScaleOffset(program, shortFlow)
+        uniform4f(
+            program, "uFrameBorderPadded",
+            SABRE_SAMPLE_BORDER_PIXELS / width,
+            SABRE_SAMPLE_BORDER_PIXELS / height,
+            1f - SABRE_SAMPLE_BORDER_PIXELS / width,
+            1f - SABRE_SAMPLE_BORDER_PIXELS / height,
+        )
+        uniform1i(program, "uCfaPattern", cfaPattern)
+        uniform1f(program, "uSourceClippingPoint", sabreShadowLongSourceClippingPoint())
+        uniform1f(program, "uValidityWeightScale", validityWeightScale.coerceAtLeast(1f))
+        uniform4fv(program, "uGains", shortCalibration.gains)
+        uniform4fv(program, "uBlackLevelsTimesGains", shortCalibration.blackTerms)
+        uniform4f(program, "uCovRangeRg", COV_MIN_R, COV_MAX_R - COV_MIN_R, COV_MIN_G, COV_MAX_G - COV_MIN_G)
+        uniform2f(program, "uCovRangeB", COV_MIN_B, COV_MAX_B - COV_MIN_B)
+        uniform3f(program, "uShortShotNoiseRgb",
+            shortCalibration.cameraRgbShotNoise[0], shortCalibration.cameraRgbShotNoise[1], shortCalibration.cameraRgbShotNoise[2])
+        uniform3f(program, "uShortReadNoiseRgb",
+            shortCalibration.cameraRgbReadNoise[0], shortCalibration.cameraRgbReadNoise[1], shortCalibration.cameraRgbReadNoise[2])
+        uniform3f(program, "uNormalShotNoiseRgb",
+            normalCalibration.cameraRgbShotNoise[0], normalCalibration.cameraRgbShotNoise[1], normalCalibration.cameraRgbShotNoise[2])
+        uniform3f(program, "uNormalReadNoiseRgb",
+            normalCalibration.cameraRgbReadNoise[0], normalCalibration.cameraRgbReadNoise[1], normalCalibration.cameraRgbReadNoise[2])
+        uniform1f(program, "uNormalFrameCount", normalFrameCount.toFloat().coerceAtLeast(1f))
+        draw(program, width, height, intArrayOf(fusedOutput, decisionOutput))
+    }
+
+    private fun logSabreUniversalFusionRadiance26648(
+        normalMean: Int,
+        fusedMean: Int,
+        decision: Int,
+    ) {
+        val program = sabreUniversalFusionTelemetryProgram26648
+        check(program != 0)
+        val probeWidth = 64
+        val probeHeight = 48
+        val probe = createTexture(probeWidth, probeHeight, GLES30.GL_RGBA16F, GLES30.GL_NEAREST)
+        val readback = ByteBuffer.allocateDirect(probeWidth * probeHeight * 4 * Short.SIZE_BYTES)
+            .order(ByteOrder.nativeOrder())
+        try {
+            GLES30.glUseProgram(program)
+            bindTexture(program, "uNormalMean", 0, normalMean)
+            bindTexture(program, "uFusedMean", 1, fusedMean)
+            bindTexture(program, "uDecision", 2, decision)
+            uniform2i(program, "uProbeSize", probeWidth, probeHeight)
+            draw(program, probeWidth, probeHeight, intArrayOf(probe))
+            GlesGpuScheduler.memoryBarrier()
+            bindRenderTargets(intArrayOf(probe), "26648 fusion radiance/weight telemetry")
+            GLES30.glBindBuffer(GLES30.GL_PIXEL_PACK_BUFFER, 0)
+            GLES30.glPixelStorei(GLES30.GL_PACK_ALIGNMENT, 1)
+            GLES30.glReadPixels(
+                0, 0, probeWidth, probeHeight,
+                GLES30.GL_RGBA, GLES30.GL_HALF_FLOAT, readback,
+            )
+            GLES30.glBindFramebuffer(GLES30.GL_FRAMEBUFFER, 0)
+            checkGlError("26648 fusion radiance/weight telemetry readback")
+            val half = readback.asShortBuffer()
+            var normalSum = 0.0
+            var shortSum = 0.0
+            var fusedSum = 0.0
+            var shortWeightSum = 0.0
+            var allCount = 0
+            var activeCount = 0
+            for (index in 0 until probeWidth * probeHeight) {
+                val normal = Half.toFloat(half.get(index * 4)).toDouble()
+                val short = Half.toFloat(half.get(index * 4 + 1)).toDouble()
+                val shortWeight = Half.toFloat(half.get(index * 4 + 2)).toDouble()
+                val fused = Half.toFloat(half.get(index * 4 + 3)).toDouble()
+                if (!normal.isFinite() || !short.isFinite() || !shortWeight.isFinite() || !fused.isFinite()) continue
+                normalSum += normal
+                fusedSum += fused
+                shortWeightSum += shortWeight.coerceIn(0.0, 1.0)
+                allCount++
+                if (shortWeight > 0.005) {
+                    shortSum += short
+                    activeCount++
+                }
+            }
+            val meanShortWeight = if (allCount > 0) shortWeightSum / allCount else Double.NaN
+            val details =
+                "samples=$allCount activeShortSamples=$activeCount " +
+                    "normalRadianceMean=${if (allCount > 0) normalSum / allCount else Double.NaN} " +
+                    "normalizedShortRadianceMean=${if (activeCount > 0) shortSum / activeCount else Double.NaN} " +
+                    "fusedRadianceMean=${if (allCount > 0) fusedSum / allCount else Double.NaN} " +
+                    "normalFinalWeightMean=${if (meanShortWeight.isFinite()) 1.0 - meanShortWeight else Double.NaN} " +
+                    "shortFinalWeightMean=$meanShortWeight " +
+                    "confidenceSemantics=FINAL_PHYSICAL_RADIOMETRIC_WEIGHT"
+            PLog.i(SABRE_TAG, "IRIS_26648_FUSION_RADIANCE_WEIGHTS $details")
+            MotionTrace.processingState("IRIS_26648_FUSION_RADIANCE_WEIGHTS", details)
+        } finally {
+            releaseOwnedTexture(probe, "26648 fusion radiance/weight telemetry")
+        }
+    }
+
+    private fun renderSabreUniversalShortValidityAugment26648(
+        decision: Int,
+        normalWeightR: Int,
+        normalWeightsGb: Int,
+        accumulatedValidity: Int,
+        validityWeightScale: Float,
+    ) {
+        val program = sabreUniversalShortValidityAugmentProgram26648
+        check(program != 0)
+        GLES30.glUseProgram(program)
+        bindTexture(program, "uDecision", 0, decision)
+        bindTexture(program, "uNormalWeightR", 1, normalWeightR)
+        bindTexture(program, "uNormalWeightsGb", 2, normalWeightsGb)
+        uniform1f(program, "uValidityWeightScale", validityWeightScale.coerceAtLeast(1f))
+        GLES30.glEnable(GLES30.GL_BLEND)
+        try {
+            GLES30.glBlendEquation(GLES30.GL_FUNC_ADD)
+            GLES30.glBlendFunc(GLES30.GL_ONE, GLES30.GL_ONE)
+            draw(program, width, height, intArrayOf(accumulatedValidity), preserveBlend = true)
+        } finally {
+            GLES30.glDisable(GLES30.GL_BLEND)
+        }
+    }
+
+    private fun logSabreUniversalFusionDecision26648(decision: Int) {
+        val bytes = readR8Mask(decision, "26648 universal fusion decision", width, height)
+        var normalOwned = 0L
+        var blended = 0L
+        var shortOwned = 0L
+        var geometryRejected = 0L
+        var sourceRejected = 0L
+        var snrRejected = 0L
+        var authoritySum = 0.0
+        var authoritySamples = 0L
+        for (raw in bytes) {
+            val v = raw.toInt() and 0xff
+            when {
+                v <= 191 -> {
+                    val authority = v / 191.0
+                    authoritySum += authority
+                    authoritySamples++
+                    when {
+                        v <= 2 -> normalOwned++
+                        v >= 189 -> shortOwned++
+                        else -> blended++
+                    }
+                }
+                v < 216 -> geometryRejected++
+                v < 233 -> sourceRejected++
+                else -> snrRejected++
+            }
+        }
+        val details =
+            "normalOwned=$normalOwned blended=$blended shortOwned=$shortOwned " +
+                "geometryRejected=$geometryRejected sourceRejected=$sourceRejected snrRejected=$snrRejected " +
+                "meanShortAuthority=${if (authoritySamples > 0) authoritySum / authoritySamples else 0.0} " +
+                "wholeRgb=true sceneSemantic=false gradientGate=false spatialFill=false"
+        PLog.i(SABRE_TAG, "IRIS_26648_UNIVERSAL_RADIOMETRIC_FUSION $details")
+        MotionTrace.processingState("IRIS_26648_UNIVERSAL_RADIOMETRIC_FUSION", details)
+    }
+
     private fun renderSabreMerge(
         extracted: Int,
         flow: SabreConvertedAlignment,
@@ -6004,33 +6024,28 @@ internal class GlesMgcRawSpatialStacker(
         sabreShortBoundaryAnchorProgram26606 = 0
         sabreShortBoundaryPropagateProgram26606 = 0
         sabreShortRescueWeightProgram26606 = 0
-        sabreShortComponentAnchorProgram26607 = linkProgram(
-            GlesMgcRawSabreShaders.shortComponentAnchor26607,
-            "iris_26607_short_component_anchor",
-        )
-        sabreShortComponentPropagateProgram26607 = linkProgram(
-            GlesMgcRawSabreShaders.shortComponentPropagate26607,
-            "iris_26607_short_component_propagate",
-        )
-        sabreShortComponentEffectiveFlowProgram26625 = linkProgram(
-            GlesMgcRawSabreShaders.shortComponentEffectiveFlow26625,
-            "iris_26625_short_component_effective_flow",
-        )
-        sabreShortComponentDiagnosticsProgram26626 = linkProgram(
-            GlesMgcRawSabreShaders.shortComponentDiagnostics26626,
-            "iris_26626_short_component_diagnostics",
-        )
-        sabreShortAccumulatorProxyProgram26626 = linkProgram(
-            GlesMgcRawSabreShaders.shortAccumulatorProxy26626,
-            "iris_26626_short_accumulator_proxy",
-        )
-        sabreShortAccumulatorDeltaProgram26626 = linkProgram(
-            GlesMgcRawSabreShaders.shortAccumulatorDelta26626,
-            "iris_26626_short_accumulator_delta",
-        )
-        sabreShortRescueWeightProgram26607 = linkProgram(
-            GlesMgcRawSabreShaders.shortRescueWeight26607,
-            "iris_26607_short_rescue_weight",
+        /* IRIS_26648_RETIRES_GRADIENT_COMPONENT_SHORT_AUTHORITY
+         * Preserve old source for regression provenance, but do not link any 26607/26625/26626
+         * component/gradient/additive-SHORT owner. The only Motion HDR owner is the universal
+         * radiometric fusion below. */
+        sabreShortComponentAnchorProgram26607 = 0
+        sabreShortComponentPropagateProgram26607 = 0
+        sabreShortComponentEffectiveFlowProgram26625 = 0
+        sabreShortComponentDiagnosticsProgram26626 = 0
+        sabreShortAccumulatorProxyProgram26626 = 0
+        sabreShortAccumulatorDeltaProgram26626 = 0
+        sabreShortRescueWeightProgram26607 = 0
+        sabreUniversalRadiometricFusionProgram26648 = linkProgram(
+            GlesMgcRawSabreShaders.universalNormalShortFusion26648,
+            "iris_26648_universal_normal_short_radiometric_fusion",
+        )
+        sabreUniversalShortValidityAugmentProgram26648 = linkProgram(
+            GlesMgcRawSabreShaders.universalShortValidityAugment26648,
+            "iris_26648_short_color_validity_augment",
+        )
+        sabreUniversalFusionTelemetryProgram26648 = linkProgram(
+            GlesMgcRawSabreShaders.universalFusionTelemetry26648,
+            "iris_26648_fusion_radiance_weight_telemetry",
         )
         /* IRIS_26564_TRUE_2X_NO_FAKE_SR_OWNER
          * Do not eagerly link the 26561/26562 native-RGB-upscale/detail shaders. The true-2x GPU
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
index d0d5778cb6db54a7ae58683dc15f31d7908f8526..dad7fee90883e9dea13dee9115322d5fbca7112c 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHardwareHevcEncoder.java
@@ -28,6 +28,10 @@ public final class IrisHardwareHevcEncoder {
     private static final String MIME_HEVC = MediaFormat.MIMETYPE_VIDEO_HEVC;
     private static final long IO_TIMEOUT_US = 5_000_000L;
     private static final double MAX_COMPRESS_RATIO = 0.25;
+    /* Android ColorUtils stable platform values: Display-P3 standard + sRGB transfer. The public
+     * SDK does not expose these symbolic constants, but MediaCodec accepts the platform values. */
+    private static final int IRIS_COLOR_STANDARD_DISPLAY_P3 = 10;
+    private static final int IRIS_COLOR_TRANSFER_SRGB = 2;
 
     private IrisHardwareHevcEncoder() {}
 
@@ -190,10 +194,14 @@ public final class IrisHardwareHevcEncoder {
              * Our CPU-I420 path does not inherit HeicCompositeStream's full-range P010 dataspace
              * or MPEG4Writer's per-sample range metadata. libheif supplies full-range YUV bytes
              * and the HEIF NCLX items are full-range, so explicitly request FULL from MediaCodec. */
+            /* IRIS_26648_HEVC_DISPLAY_P3_SRGB_FULL_BT601_CONTRACT
+             * Match the HEIF NCLX authority end-to-end. Android's Display-P3 standard maps to
+             * SMPTE-EG-432 primaries with BT.601 matrix and sRGB transfer in the codec VUI. */
+            if (!gainMap) {
+                format.setInteger(MediaFormat.KEY_COLOR_STANDARD, IRIS_COLOR_STANDARD_DISPLAY_P3);
+                format.setInteger(MediaFormat.KEY_COLOR_TRANSFER, IRIS_COLOR_TRANSFER_SRGB);
+            }
             format.setInteger(MediaFormat.KEY_COLOR_RANGE, MediaFormat.COLOR_RANGE_FULL);
-            /* IRIS_26643_AOSP_ANDROID16_CODEC_CONFIGURATION_OWNER
-             * Primaries/transfer remain HEIF-NCLX publication authority; only the missing range
-             * transport adaptation is explicit for this CPU-I420 direct-NAL path. */
             MediaCodecInfo.EncoderCapabilities enc = caps.getEncoderCapabilities();
             int q = Math.max(1, Math.min(100, quality));
             if (enc.isBitrateModeSupported(MediaCodecInfo.EncoderCapabilities.BITRATE_MODE_CQ)) {
@@ -261,12 +269,17 @@ public final class IrisHardwareHevcEncoder {
                      * Never publish full-range NCLX around a hardware stream that explicitly
                      * reports anything except FULL. Fail the HEIC save instead of silently
                      * producing the 26643 limited/full contradiction. */
-                    if (outRange != MediaFormat.COLOR_RANGE_FULL) {
-                        throw new IllegalStateException("Hardware HEVC did not honor full-range I420 contract: range="
-                                + outRange + " gainMap=" + gainMap);
+                    boolean colorContract = outRange == MediaFormat.COLOR_RANGE_FULL &&
+                            (gainMap || (outStandard == IRIS_COLOR_STANDARD_DISPLAY_P3 &&
+                                    outTransfer == IRIS_COLOR_TRANSFER_SRGB));
+                    if (!colorContract) {
+                        throw new IllegalStateException("hardware HEVC color contract mismatch gainMap="
+                                + gainMap + " standard=" + outStandard + " transfer=" + outTransfer
+                                + " range=" + outRange);
                     }
-                    Log.i(TAG, "IRIS_26644_HEVC_RANGE_PROOF gainMap=" + gainMap
-                            + " requested=FULL output=FULL");
+                    Log.critical(TAG, "IRIS_26648_HEVC_COLOR_CONTRACT gainMap=" + gainMap
+                            + " standard=" + outStandard + " transfer=" + outTransfer + " range=" + outRange
+                            + " baseMatrix=BT601_BY_ANDROID_DISPLAY_P3_MAPPING");
                     for (int i = 0; i < 4; i++) {
                         ByteBuffer csd = of.getByteBuffer("csd-" + i);
                         if (csd == null) continue;
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
index fa2be2b9ea8e13c559f65a38c398cf2e15c23fa5..df6e0779cb9fc6ae2159a5d29f04f26bc9273037 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/IrisHeicUltraHdrEncoder.java
@@ -41,9 +41,85 @@ public final class IrisHeicUltraHdrEncoder {
         return true;
     }
 
+    private static final class Iris26648HdrStats {
+        final double meanSdr;
+        final double meanHdr;
+        final double peakHdr;
+        final int samples;
+        Iris26648HdrStats(double meanSdr, double meanHdr, double peakHdr, int samples) {
+            this.meanSdr = meanSdr; this.meanHdr = meanHdr; this.peakHdr = peakHdr; this.samples = samples;
+        }
+        double expansion() { return meanHdr / Math.max(meanSdr, 1.0e-8); }
+    }
+
+    private static double iris26648SrgbToLinear(int code) {
+        double x = Math.max(0.0, Math.min(1.0, code / 255.0));
+        return x <= 0.04045 ? x / 12.92 : Math.pow((x + 0.055) / 1.055, 2.4);
+    }
+
+    private static double iris26648ApplyGain(double sdr, int gainCode, int channel, Gainmap metadata) {
+        float[] rmin = metadata.getRatioMin();
+        float[] rmax = metadata.getRatioMax();
+        float[] gamma = metadata.getGamma();
+        float[] es = metadata.getEpsilonSdr();
+        float[] eh = metadata.getEpsilonHdr();
+        int c = Math.max(0, Math.min(2, channel));
+        double encoded = Math.max(0.0, Math.min(1.0, gainCode / 255.0));
+        double shaped = Math.pow(encoded, Math.max(1.0e-6, gamma[c]));
+        double lo = Math.log(Math.max(rmin[c], 1.0e-6));
+        double hi = Math.log(Math.max(rmax[c], 1.0e-6));
+        double gain = Math.exp(lo + shaped * (hi - lo));
+        return Math.max(0.0, (sdr + es[c]) * gain - eh[c]);
+    }
+
+    /* IRIS_26648_NUMERICAL_ANDROID_GAINMAP_RECONSTRUCTION
+     * Reconstruct a bounded grid of actual HDR samples using the Android Gainmap metadata equation.
+     * This proves brightness expansion from decoded base+gain pixels rather than stopping at
+     * hasGainmap=true. Display-P3 linear luma coefficients are used only for aggregate comparison. */
+    private static Iris26648HdrStats iris26648HdrStats(Bitmap base, Bitmap gain, Gainmap metadata) {
+        if (base == null || gain == null || metadata == null || base.isRecycled() || gain.isRecycled()) return null;
+        int nx = Math.min(32, Math.max(4, base.getWidth()));
+        int ny = Math.min(32, Math.max(4, base.getHeight()));
+        double sdrSum = 0.0, hdrSum = 0.0, peak = 0.0;
+        int count = 0;
+        for (int iy = 0; iy < ny; ++iy) {
+            int by = Math.min(base.getHeight() - 1, (int) (((iy + 0.5) * base.getHeight()) / ny));
+            int gy = Math.min(gain.getHeight() - 1, (int) (((iy + 0.5) * gain.getHeight()) / ny));
+            for (int ix = 0; ix < nx; ++ix) {
+                int bx = Math.min(base.getWidth() - 1, (int) (((ix + 0.5) * base.getWidth()) / nx));
+                int gx = Math.min(gain.getWidth() - 1, (int) (((ix + 0.5) * gain.getWidth()) / nx));
+                int bp = base.getPixel(bx, by);
+                int gp = gain.getPixel(gx, gy);
+                double sr = iris26648SrgbToLinear((bp >>> 16) & 0xff);
+                double sg = iris26648SrgbToLinear((bp >>> 8) & 0xff);
+                double sb = iris26648SrgbToLinear(bp & 0xff);
+                double hr = iris26648ApplyGain(sr, (gp >>> 16) & 0xff, 0, metadata);
+                double hg = iris26648ApplyGain(sg, (gp >>> 8) & 0xff, 1, metadata);
+                double hb = iris26648ApplyGain(sb, gp & 0xff, 2, metadata);
+                double sdrY = 0.2289746 * sr + 0.6917385 * sg + 0.0792869 * sb;
+                double hdrY = 0.2289746 * hr + 0.6917385 * hg + 0.0792869 * hb;
+                sdrSum += sdrY; hdrSum += hdrY; peak = Math.max(peak, hdrY); count++;
+            }
+        }
+        return count == 0 ? null : new Iris26648HdrStats(sdrSum / count, hdrSum / count, peak, count);
+    }
+
+    private static boolean iris26648HdrStatsAgree(Iris26648HdrStats expected, Iris26648HdrStats actual) {
+        if (expected == null || actual == null || expected.samples == 0 || actual.samples == 0) return false;
+        double meanScale = Math.max(0.02, expected.meanHdr);
+        double peakScale = Math.max(0.05, expected.peakHdr);
+        double expectedExpansion = expected.expansion();
+        double actualExpansion = actual.expansion();
+        boolean expectedNeedsExpansion = expectedExpansion > 1.005;
+        boolean expansionConsistent = !expectedNeedsExpansion || actualExpansion > 1.005;
+        return Math.abs(actual.meanHdr - expected.meanHdr) <= 0.15 * meanScale &&
+                Math.abs(actual.peakHdr - expected.peakHdr) <= 0.25 * peakScale &&
+                expansionConsistent;
+    }
+
     private static boolean iris26646VerifyPlatformReadback(
             Path output, Gainmap expected, int expectedGainWidth, int expectedGainHeight,
-            String publication) {
+            String publication, Bitmap expectedBasePreview, Bitmap expectedGainPreview) {
         Bitmap decoded = null;
         try {
             decoded = BitmapFactory.decodeFile(output.toAbsolutePath().toString());
@@ -65,8 +141,32 @@ public final class IrisHeicUltraHdrEncoder {
                     expected.getMinDisplayRatioForHdrTransition(), actual.getMinDisplayRatioForHdrTransition());
             boolean full = actual != null && iris26646NearlyEqual(
                     expected.getDisplayRatioForFullHdr(), actual.getDisplayRatioForFullHdr());
-            boolean metadata = ratioMin && ratioMax && gamma && epsilonSdr && epsilonHdr && transition && full;
-            boolean recognized = decodedOk && hasGainmap && p3 && geometry && metadata;
+            boolean direction = actual != null && expected.getGainmapDirection() == Gainmap.GAINMAP_DIRECTION_SDR_TO_HDR
+                    && actual.getGainmapDirection() == Gainmap.GAINMAP_DIRECTION_SDR_TO_HDR;
+            ColorSpace actualAlternative = actual == null ? null : actual.getAlternativeImagePrimaries();
+            boolean alternativePrimaries = actual != null && (actualAlternative == null ||
+                    actualAlternative.equals(ColorSpace.get(ColorSpace.Named.DISPLAY_P3)));
+            boolean metadata = ratioMin && ratioMax && gamma && epsilonSdr && epsilonHdr && transition && full
+                    && direction && alternativePrimaries;
+            Iris26648HdrStats decodedHdr = actual == null || actualContents == null ? null
+                    : iris26648HdrStats(decoded, actualContents, actual);
+            Iris26648HdrStats expectedHdr = expectedBasePreview == null || expectedGainPreview == null ? null
+                    : iris26648HdrStats(expectedBasePreview, expectedGainPreview, expected);
+            boolean numericExpansion = decodedHdr != null && decodedHdr.expansion() > 1.005;
+            boolean expectedNumericProof = expectedHdr != null;
+            boolean expectedNeedsExpansion = expectedHdr != null && expectedHdr.expansion() > 1.005;
+            boolean numericAgreement = expectedNumericProof && iris26648HdrStatsAgree(expectedHdr, decodedHdr);
+            boolean recognized = decodedOk && hasGainmap && p3 && geometry && metadata && numericAgreement;
+            Log.critical(TAG, "IRIS_26648_HEIC_NUMERICAL_HDR publication=" + publication
+                    + " numericExpansion=" + numericExpansion + " expectedNumericProof=" + expectedNumericProof
+                    + " expectedNeedsExpansion=" + expectedNeedsExpansion + " numericAgreement=" + numericAgreement
+                    + " decodedMeanSdr=" + (decodedHdr == null ? Double.NaN : decodedHdr.meanSdr)
+                    + " decodedMeanHdr=" + (decodedHdr == null ? Double.NaN : decodedHdr.meanHdr)
+                    + " decodedPeakHdr=" + (decodedHdr == null ? Double.NaN : decodedHdr.peakHdr)
+                    + " decodedExpansion=" + (decodedHdr == null ? Double.NaN : decodedHdr.expansion())
+                    + " expectedMeanHdr=" + (expectedHdr == null ? Double.NaN : expectedHdr.meanHdr)
+                    + " expectedPeakHdr=" + (expectedHdr == null ? Double.NaN : expectedHdr.peakHdr)
+                    + " presentationProof=NUMERIC_ANDROID_RECONSTRUCTION externalViewerPresentation=SEPARATE");
             Log.critical(TAG, "IRIS_26646_HEIC_ANDROID_READBACK publication=" + publication
                     + " decoded=" + decodedOk + " hasGainmap=" + hasGainmap + " p3=" + p3
                     + " colorSpace=" + (cs == null ? "null" : cs.getName())
@@ -76,6 +176,7 @@ public final class IrisHeicUltraHdrEncoder {
                     + " ratioMin=" + ratioMin + " ratioMax=" + ratioMax + " gamma=" + gamma
                     + " epsilonSdr=" + epsilonSdr + " epsilonHdr=" + epsilonHdr
                     + " transition=" + transition + " full=" + full
+                    + " directionSdrToHdr=" + direction + " alternativePrimaries=" + alternativePrimaries
                     + " expectedRatioMax=" + java.util.Arrays.toString(expected.getRatioMax())
                     + " actualRatioMax=" + (actual == null ? "null" : java.util.Arrays.toString(actual.getRatioMax()))
                     + " expectedFull=" + expected.getDisplayRatioForFullHdr()
@@ -132,7 +233,8 @@ public final class IrisHeicUltraHdrEncoder {
             if (ok) {
                 Bitmap gainContents = gm.getGainmapContents();
                 recognized = iris26646VerifyPlatformReadback(output, gm,
-                        gainContents.getWidth(), gainContents.getHeight(), "MOTION_1X");
+                        gainContents.getWidth(), gainContents.getHeight(), "MOTION_1X",
+                        p3, gainContents);
             }
             Log.i(TAG, "IRIS_26646_HEIC_ULTRAHDR_RESULT saved=" + ok
                     + " androidRecognizedUltraHdr=" + recognized
@@ -186,8 +288,23 @@ public final class IrisHeicUltraHdrEncoder {
                     exif == null ? null : exif.MAKE,
                     exif == null ? null : exif.MODEL);
             saved &= Files.isRegularFile(output) && Files.size(output) > 0L;
-            boolean recognized = saved && iris26646VerifyPlatformReadback(
-                    output, metadataOwner, width / 2, height / 2, "MOTION_TRUE2X_GRID");
+            Bitmap expectedBasePreview = null;
+            Bitmap expectedGainPreview = null;
+            boolean recognized;
+            try {
+                BitmapFactory.Options preview = new BitmapFactory.Options();
+                preview.inSampleSize = 16;
+                expectedBasePreview = BitmapFactory.decodeFile(displayP3BaseJpeg.toString(), preview);
+                BitmapFactory.Options gainPreview = new BitmapFactory.Options();
+                gainPreview.inSampleSize = 16;
+                expectedGainPreview = BitmapFactory.decodeFile(true2xGainJpeg.toString(), gainPreview);
+                recognized = saved && iris26646VerifyPlatformReadback(
+                        output, metadataOwner, width / 2, height / 2, "MOTION_TRUE2X_GRID",
+                        expectedBasePreview, expectedGainPreview);
+            } finally {
+                if (expectedBasePreview != null && !expectedBasePreview.isRecycled()) expectedBasePreview.recycle();
+                if (expectedGainPreview != null && !expectedGainPreview.isRecycled()) expectedGainPreview.recycle();
+            }
             Log.critical(TAG, "IRIS_26646_TRUE2X_HEIC_RESULT saved=" + saved
                     + " grid=2x2 base=" + width + "x" + height
                     + " gain=" + (width / 2) + "x" + (height / 2)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
index 5d6e44f79c5c4505789b9e96cf3d1af2e4420ce1..3f1e68d8f05eb77ae972ee34837e9bc0c97c3881 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/ui/camera/CameraUIViewImpl.java
@@ -10,6 +10,8 @@ import com.particlesdevs.photoncamera.util.Log;
 import android.view.MotionEvent;
 import android.view.View;
 import android.widget.ImageButton;
+import android.widget.ImageView;
+import android.view.ViewGroup;
 import android.widget.ProgressBar;
 
 import androidx.constraintlayout.widget.ConstraintLayout;
@@ -249,9 +251,9 @@ public class CameraUIViewImpl implements CameraUIView {
         );
         iris26644StyleManualPalette(root);
         View approvedManualHandle = root.findViewById(R.id.approved_manual_handle);
-        TextView approvedManualChevron = root.findViewById(R.id.approved_manual_chevron);
+        ImageView approvedManualChevron = root.findViewById(R.id.approved_manual_chevron);
         if (approvedManualHandle != null && approvedManualChevron != null) {
-            approvedManualChevron.setText("\u2304");
+            approvedManualChevron.setImageDrawable(iris26648CreateManualChevronDrawable(approvedManualChevron));
             approvedManualHandle.setOnClickListener(v -> {
                 boolean opening = !cameraFragment.getManualModeConsole().isPanelVisible();
                 approvedManualChevron.animate()
@@ -259,6 +261,11 @@ public class CameraUIViewImpl implements CameraUIView {
                         .setDuration(220L)
                         .start();
                 cameraFragment.toggleManualControls();
+                if (opening) {
+                    /* The knob creates/replaces its rotating value drawables dynamically. Re-run
+                     * contrast styling after the panel has completed the opening transaction. */
+                    root.post(() -> iris26644StyleManualPalette(root));
+                }
             });
         }
 
@@ -314,6 +321,10 @@ public class CameraUIViewImpl implements CameraUIView {
                 }
             }
         }
+        if (knobId != 0) {
+            View knob = root.findViewById(knobId);
+            if (knob != null) iris26648ApplyManualKnobContrast(knob);
+        }
         final String[] labelIds = {
                 "focus_option_tv", "exposure_option_tv", "iso_option_tv", "ev_option_tv"
         };
@@ -324,7 +335,239 @@ public class CameraUIViewImpl implements CameraUIView {
             if (!(view instanceof TextView)) continue;
             TextView label = (TextView) view;
             label.setTextAppearance(R.style.AuxButtonText);
-            label.setShadowLayer(0.0f, 0.0f, 0.0f, 0);
+            label.setTextColor(android.graphics.Color.WHITE);
+            iris26648ApplyContrastSafeWhite(label);
+        }
+        /* Includes the dynamic value text (for example Auto) and bundled manual icons without
+         * modifying repository-only circularbarlib source. */
+        iris26648ApplyContrastSafeWhite(manualMode);
+    }
+
+    /* IRIS_26648_CONTRAST_SAFE_KNOB_TEXT
+     * The rotating Auto/numeric labels are private ShadowTextDrawable instances owned by the
+     * inflated circularbarlib KnobView, not child TextViews. Keep that repository-only module
+     * byte-untouched and style only its already-inflated runtime drawables through the same
+     * reflection boundary used by the proven 26645 background-neutralization owner. */
+    private void iris26648ApplyManualKnobContrast(View knob) {
+        if (knob == null) return;
+        final float density = knob.getResources().getDisplayMetrics().density;
+        try {
+            java.lang.reflect.Field itemsField = knob.getClass().getDeclaredField("m_KnobItems");
+            itemsField.setAccessible(true);
+            Object rawItems = itemsField.get(knob);
+            if (!(rawItems instanceof java.util.List)) return;
+            for (Object item : (java.util.List<?>) rawItems) {
+                if (item == null) continue;
+                java.lang.reflect.Field drawableField = item.getClass().getDeclaredField("drawable");
+                drawableField.setAccessible(true);
+                Object rawDrawable = drawableField.get(item);
+                if (!(rawDrawable instanceof android.graphics.drawable.Drawable)) continue;
+                android.graphics.drawable.Drawable drawable = (android.graphics.drawable.Drawable) rawDrawable;
+                if (!drawable.getClass().getName().endsWith(".ShadowTextDrawable")) continue;
+
+                java.lang.reflect.Method setTextColor = drawable.getClass()
+                        .getMethod("setTextColor", int.class);
+                java.lang.reflect.Method setShadow = drawable.getClass()
+                        .getMethod("setShadow", float.class, float.class, float.class, int.class);
+                setTextColor.invoke(drawable, android.graphics.Color.WHITE);
+                setShadow.invoke(drawable, 1.55f * density, 0.0f, 0.0f, 0xF0000000);
+
+                /* circularbarlib's ShadowTextDrawable intentionally does not expose stroke
+                 * enablement. Configure its already-existing renderer fields directly; this keeps
+                 * the module bytes frozen while giving Auto/numeric values the same dark edge. */
+                java.lang.reflect.Field rendererField = drawable.getClass().getDeclaredField("m_Renderer");
+                rendererField.setAccessible(true);
+                Object renderer = rendererField.get(drawable);
+                if (renderer != null) {
+                    java.lang.reflect.Field hasStrokeField = renderer.getClass().getDeclaredField("m_HasStroke");
+                    hasStrokeField.setAccessible(true);
+                    hasStrokeField.setBoolean(renderer, true);
+                    java.lang.reflect.Field strokePaintField = renderer.getClass().getDeclaredField("m_StrokePaint");
+                    strokePaintField.setAccessible(true);
+                    Object strokePaintObject = strokePaintField.get(renderer);
+                    if (strokePaintObject instanceof android.graphics.Paint) {
+                        android.graphics.Paint strokePaint = (android.graphics.Paint) strokePaintObject;
+                        strokePaint.setStyle(android.graphics.Paint.Style.STROKE);
+                        strokePaint.setColor(0xE6000000);
+                        strokePaint.setStrokeWidth(0.90f * density);
+                        strokePaint.setStrokeJoin(android.graphics.Paint.Join.ROUND);
+                    }
+                }
+            }
+            knob.invalidate();
+        } catch (ReflectiveOperationException reflectionFailure) {
+            Log.e(TAG, "IRIS_26648_MANUAL_KNOB_CONTRAST_FAILED", reflectionFailure);
+        }
+    }
+
+    /* IRIS_26648_CONTRAST_SAFE_WHITE_CHEVRON
+     * Draw one real chevron icon directly rather than using a font glyph. The outer dark stroke
+     * is contrast protection only; the visible foreground remains pure white with no backdrop. */
+    private android.graphics.drawable.Drawable iris26648CreateManualChevronDrawable(View host) {
+        final float density = host.getResources().getDisplayMetrics().density;
+        return new android.graphics.drawable.Drawable() {
+            private final android.graphics.Paint paint = new android.graphics.Paint(android.graphics.Paint.ANTI_ALIAS_FLAG);
+            private int drawableAlpha = 255;
+            private android.graphics.ColorFilter colorFilter;
+
+            @Override
+            public void draw(android.graphics.Canvas canvas) {
+                android.graphics.Rect bounds = getBounds();
+                float width = bounds.width();
+                float height = bounds.height();
+                float left = bounds.left + width * 0.22f;
+                float centerX = bounds.left + width * 0.50f;
+                float right = bounds.left + width * 0.78f;
+                float top = bounds.top + height * 0.36f;
+                float bottom = bounds.top + height * 0.66f;
+                android.graphics.Path path = new android.graphics.Path();
+                path.moveTo(left, top);
+                path.lineTo(centerX, bottom);
+                path.lineTo(right, top);
+
+                paint.setStyle(android.graphics.Paint.Style.STROKE);
+                paint.setStrokeCap(android.graphics.Paint.Cap.ROUND);
+                paint.setStrokeJoin(android.graphics.Paint.Join.ROUND);
+                paint.setColorFilter(colorFilter);
+                paint.setAlpha(drawableAlpha);
+
+                paint.setColor(0xE6000000);
+                paint.setStrokeWidth(3.75f * density);
+                canvas.drawPath(path, paint);
+
+                paint.setColor(android.graphics.Color.WHITE);
+                paint.setStrokeWidth(1.85f * density);
+                canvas.drawPath(path, paint);
+            }
+
+            @Override
+            public void setAlpha(int alpha) {
+                drawableAlpha = alpha;
+                invalidateSelf();
+            }
+
+            @Override
+            public void setColorFilter(android.graphics.ColorFilter filter) {
+                colorFilter = filter;
+                invalidateSelf();
+            }
+
+            @Override
+            public int getOpacity() {
+                return android.graphics.PixelFormat.TRANSLUCENT;
+            }
+        };
+    }
+
+    /* IRIS_26648_CONTRAST_SAFE_WHITE_ICON_EDGE
+     * Transparent drawable wrapper: eight sub-pixel dark edge draws followed by one pure-white
+     * center draw. This is an icon outline, not a pill/scrim/background. */
+    private static final class Iris26648OutlinedWhiteDrawable extends android.graphics.drawable.Drawable {
+        private final android.graphics.drawable.Drawable source;
+        private final float edgePx;
+        private final int edgeInset;
+        private int drawableAlpha = 255;
+
+        Iris26648OutlinedWhiteDrawable(android.graphics.drawable.Drawable source, float edgePx) {
+            this.source = source;
+            this.edgePx = Math.max(0.5f, edgePx);
+            this.edgeInset = Math.max(1, (int) Math.ceil(this.edgePx));
+        }
+
+        @Override
+        public void draw(android.graphics.Canvas canvas) {
+            android.graphics.Rect b = getBounds();
+            android.graphics.Rect inner = new android.graphics.Rect(
+                    b.left + edgeInset, b.top + edgeInset,
+                    b.right - edgeInset, b.bottom - edgeInset);
+            if (inner.width() <= 0 || inner.height() <= 0) inner.set(b);
+            source.setBounds(inner);
+            source.setAlpha(drawableAlpha);
+            source.setColorFilter(new android.graphics.PorterDuffColorFilter(
+                    0xF0000000, android.graphics.PorterDuff.Mode.SRC_IN));
+            final float[] offsets = new float[]{-edgePx, 0f, edgePx};
+            for (float dx : offsets) {
+                for (float dy : offsets) {
+                    if (dx == 0f && dy == 0f) continue;
+                    int save = canvas.save();
+                    canvas.translate(dx, dy);
+                    source.draw(canvas);
+                    canvas.restoreToCount(save);
+                }
+            }
+            source.setColorFilter(new android.graphics.PorterDuffColorFilter(
+                    android.graphics.Color.WHITE, android.graphics.PorterDuff.Mode.SRC_IN));
+            source.draw(canvas);
+        }
+
+        @Override public void setAlpha(int alpha) { drawableAlpha = alpha; invalidateSelf(); }
+        @Override public void setColorFilter(android.graphics.ColorFilter filter) { /* fixed white */ }
+        @Override public int getOpacity() { return android.graphics.PixelFormat.TRANSLUCENT; }
+        @Override public int getIntrinsicWidth() {
+            int width = source.getIntrinsicWidth();
+            return width < 0 ? width : width + 2 * edgeInset;
+        }
+        @Override public int getIntrinsicHeight() {
+            int height = source.getIntrinsicHeight();
+            return height < 0 ? height : height + 2 * edgeInset;
+        }
+        @Override public boolean isStateful() { return source.isStateful(); }
+        @Override protected boolean onStateChange(int[] state) {
+            boolean changed = source.setState(state);
+            if (changed) invalidateSelf();
+            return changed;
+        }
+        @Override protected boolean onLevelChange(int level) {
+            boolean changed = source.setLevel(level);
+            if (changed) invalidateSelf();
+            return changed;
+        }
+        @Override protected void onBoundsChange(android.graphics.Rect bounds) { /* draw() applies inset bounds */ }
+    }
+
+    /* IRIS_26648_CONTRAST_SAFE_WHITE_MANUAL_OVERLAY
+     * No chip/pill/scrim/background. Keep the manual foreground pure white and use only a tight
+     * dark edge/shadow so Auto/ISO/EV/focus remain legible over sun, bulbs and white walls. */
+    private void iris26648ApplyContrastSafeWhite(View view) {
+        if (view == null) return;
+        final float density = view.getResources().getDisplayMetrics().density;
+        if (view instanceof TextView) {
+            TextView text = (TextView) view;
+            text.setTextColor(android.graphics.Color.WHITE);
+            /* A centered soft-black shadow behaves as a thin dark edge around every glyph while
+             * leaving the actual text fill pure white and adding no backing surface. */
+            text.setShadowLayer(1.55f * density, 0.0f, 0.0f, 0xF0000000);
+            /* The four manual buttons use drawableBottom compound icons, not child ImageViews.
+             * Clear their XML tint and wrap the actual drawables so the icon receives the same
+             * dark edge + white center treatment as its label. */
+            android.graphics.drawable.Drawable[] compound = text.getCompoundDrawablesRelative();
+            boolean replacedCompound = false;
+            for (int i = 0; i < compound.length; ++i) {
+                android.graphics.drawable.Drawable drawable = compound[i];
+                if (drawable != null && !(drawable instanceof Iris26648OutlinedWhiteDrawable)) {
+                    compound[i] = new Iris26648OutlinedWhiteDrawable(drawable.mutate(), 1.05f * density);
+                    replacedCompound = true;
+                }
+            }
+            if (replacedCompound) {
+                text.setCompoundDrawableTintList(null);
+                text.setCompoundDrawablesRelativeWithIntrinsicBounds(
+                        compound[0], compound[1], compound[2], compound[3]);
+            }
+        } else if (view instanceof ImageView) {
+            ImageView image = (ImageView) view;
+            if (image.getId() != R.id.approved_manual_chevron && image.getDrawable() != null &&
+                    !(image.getDrawable() instanceof Iris26648OutlinedWhiteDrawable)) {
+                image.clearColorFilter();
+                image.setImageDrawable(new Iris26648OutlinedWhiteDrawable(
+                        image.getDrawable().mutate(), 1.05f * density));
+            }
+        }
+        if (view instanceof ViewGroup) {
+            ViewGroup group = (ViewGroup) view;
+            for (int i = 0; i < group.getChildCount(); ++i) {
+                iris26648ApplyContrastSafeWhite(group.getChildAt(i));
+            }
         }
     }
 
diff --git a/app/src/main/res/layout/layout_manual_toggle_panel.xml b/app/src/main/res/layout/layout_manual_toggle_panel.xml
index 252eb25a621ad60b44786703e7d26bbb4a8f11da..220ad04947b449192341ae0af1c5512e84f491fd 100644
--- a/app/src/main/res/layout/layout_manual_toggle_panel.xml
+++ b/app/src/main/res/layout/layout_manual_toggle_panel.xml
@@ -16,14 +16,10 @@
         android:clickable="true"
         android:focusable="true">
 
-        <TextView
+        <ImageView
             android:id="@+id/approved_manual_chevron"
-            android:layout_width="wrap_content"
-            android:layout_height="wrap_content"
-            android:text="&#8964;"
-            android:textColor="#FFFFFFFF"
-            android:textSize="13sp"
-            android:textStyle="bold"
-            android:includeFontPadding="false" />
+            android:layout_width="18dp"
+            android:layout_height="18dp"
+            android:contentDescription="@null" />
     </LinearLayout>
 </FrameLayout>
\ No newline at end of file
diff --git a/app/version.properties b/app/version.properties
index 079ee13b1d0604266761376798aeb56f71dbf704..a06ef3066ec0e2be3dfc6bf68962387e0b9ef98e 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726646
-VERSION_BUILD=26646
+VERSION_NAME=0.9726648
+VERSION_BUILD=26648
