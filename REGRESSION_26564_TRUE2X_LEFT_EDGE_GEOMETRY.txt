IRIS 26564 PERMANENT REGRESSION — TRUE2X LEFT EDGE GEOMETRY

Failure found during candidate audit:
The no-residual-zoom native true-2x renderer forced source x==0 to x==1 even though
SourceRegion::cameraAt already clamps neighbors correctly and readRegion includes the required halo.
This duplicated/shifted the first true-2x source column.

Permanent conditions:
1. residualZoom==1 maps each final true-2x sample to its actual mapped source coordinate.
2. x==0 must remain x==0; boundary neighbor access is handled by profileColor/cameraAt clamping.
3. No artificial one-pixel source shift is allowed at any image edge.
