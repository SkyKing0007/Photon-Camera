26586 permanent packaging regression — workflow/build serialization must remain valid before Actions execution

Exact failures carried forward:
- initial 26586 workflow was rejected by GitHub YAML parsing because the inherited escaped-newline build-order assertion was serialized as literal unindented lines;
- the first correction then failed before candidate reconstruction because it pinned the wrong Git blob for the successful 26585 build-script implementation.

Required contract:
- workflow uses the exact successful-26585 single-line escaped build-order assertion form;
- workflow does not invent or require a prior build-script blob assertion that successful 26585 did not use;
- build_26586_v1_viewfinder_highlight_authority_split.sh must pass bash -n;
- all six Python verifier/transform files must compile with Python stdlib only;
- 26586 must be one clean handoff commit directly on successful 26585 commit 7201c93c1993a1a42c89744acce2f661adc5d09d;
- no __pycache__ or .pyc may be packaged.
