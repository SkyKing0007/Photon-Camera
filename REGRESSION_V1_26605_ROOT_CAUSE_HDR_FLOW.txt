26605 permanent regressions — root-cause HDR carrier + RAW-pixel flow correction

1. Successful 26604 one-tunnel NORMAL+SHORT ownership, continuous bracket confidence, one canonical Motion tone owner, NORMAL reference geometry, DNG NORMAL-only and true-2x high-frequency detail NORMAL-only remain mandatory.
2. The physical HDR authority may not be clamped to [0,1] before the final healthy extended-linear master is formed. outputTransformUint16 remains a normalized VGN proxy only; outputTransformFloat is the physical pre-VGN carrier on normal Motion.
3. VGN remains normalized/subordinate. The post-VGN HDR master must equal physical + (vgnNormalized - clamp(physical,0,1)) per channel, preserving VGN's below-white result and physical excess above white without a second tone owner.
4. Normal Motion extended-HDR preservation must fail closed unless caller-owned RGBA16F export is active. Night LONG/legacy behavior stays on the inherited path unless separately audited.
5. The active SHORT clipped-reference 0.50..2.00 variation gate must consume flow.w in RAW pixels. flow.z remains available to normalized-domain consumers and may not be globally substituted.
6. Flow conversion must continue publishing vec4(uvFlow, localFlowVariation, localFlowVariationRawPixels). Telemetry must identify flow.w as RAW_PIXELS and report p50/p95/p99/max plus threshold counts.
7. Normal Motion LONG remains captured-but-excluded from common Sabre in this build; Night LONG remains common Sabre shadow evidence. mergedFrameCount/ownership telemetry must not claim normal LONG support.
8. The corrected post-VGN exported HDR master is the native guide supplied to Super Res. SHORT may improve the shared guide but may not become a true-2x high-frequency/subpixel detail owner.
9. DNG ownership remains NORMAL-only. Native/vendor/DNG/protected invariance remains exact.
10. UHDR SDR/gain-map math remains the successful 26604 canonical tone path. bodyGainUnity may not return as a required owner or stale ownership claim; gain derives from healthy HDR master versus SDR.
11. The fix may not reintroduce private SHORT accumulated color, protection mask, late RGB compositor, hue/chroma repair, blur, saturation suppression, or post-tone detail paste.
12. HDR boundary telemetry must expose Resolve pre-normalization, float pre-VGN handoff and post-VGN master statistics, including >1 counts/fractions, so clamp ownership is directly falsifiable on-device.
13. Generated app/build/** and app/.cxx/** never count as runtime-source changes; repository-only scaffolding absent from compiled-candidate authority never counts as runtime change; unexpected actual app/src/** changes remain visible and fail.
14. Deterministic full-index forward/rollback patches must reproduce exact candidate/base bytes at core.abbrev 7/12/40 with fuzz=0; compiler-tested candidate must remain frozen through assemble and final export.
