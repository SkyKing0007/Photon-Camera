26597 V1 PERMANENT RUNTIME REGRESSIONS

1. 26596 real-device visible-overexposure regression
   - SHORT was genuinely active, but bright road/ground/foliage structure still washed toward white after presentation.
   - A build may not be accepted merely because SHORT active > 0 or UHDR attached=true.
   - Overall/body exposure must remain unchanged while recoverable bright-value ordering and separation survive final publication.

2. Body-exposure invariance
   - At and below the existing Motion highlight start, 26597 publication must equal 26596 exactly.
   - Viewfinder exposure matcher/display exposure/PostPipeline exposure owners are protected and byte-identical.

3. Universal highlight ordering
   - Recoverable upper-tail values must remain strictly monotonic.
   - The new output must not be brighter than the 26596 curve inside the recoverable 0..sceneWhite interval.
   - The exact 26596 scene-white output anchor is retained.
   - Values beyond scene white remain strictly ordered and may asymptotically approach white for true speculars.
   - No scene/object category threshold is allowed.

4. 1x / true2x parity
   - Motion 1x GLSL, true2x CPU publication and true2x GPU publication must implement the same body-anchored highlight reserve.
   - true2x detail/CFA evidence remains NORMAL-owned as proven by 26595/26596.

5. SHORT actual-loss phase-scoped headroom
   - A genuinely clipped NORMAL CFA phase explained by SHORT must not be rejected solely because an unrelated SHORT phase is bright but still physically measurable.
   - The actually clipped phase still requires strict SHORT reserve and exposure-normalized explanation.
   - Any SHORT phase at/near physical sensor white fails whole-RGB restore.
   - The non-actual-sensor-loss near-clip path retains the all-phase 0.90 reserve.
   - Existing flow/coherence/fail-closed rules remain.

6. Carried invariants
   - DNG unchanged.
   - 26596 UHDR actual-content capacity ownership unchanged.
   - capture/bridge/Night/color/global exposure ownership unchanged.
   - full SHORT contribution counter remains.
   - generated app/build/** and app/.cxx/** never count as runtime source changes.
   - repository-only scaffolding absent from compiled authority never counts as runtime changes.
   - unexpected app/src/** changes remain visible and fail.
