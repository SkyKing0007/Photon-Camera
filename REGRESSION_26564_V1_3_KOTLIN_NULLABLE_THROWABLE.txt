IRIS 26564 PERMANENT REGRESSION — V1.3 KOTLIN NULLABLE THROWABLE

GitHub Actions failure: run 33285297620, job 99187338833.
V1.3 passed sealed scope, exact successful 26563 authority reconstruction, all prior
infrastructure regressions, exact candidate transform, 45/45 semantic gates, 12/12
bounded-memory gates, stdlib CPU/GPU parity, exact runtime-expanded GLSL extraction,
reserved-identifier scan, and pinned real glslangValidator 15.1.0. It then reached the
real project Kotlin compiler and failed at GlesMgcRawSpatialStacker.kt because
`gpuAttempt.exceptionOrNull()` has type Throwable? while PLog.e requires Throwable.

Permanent conditions for every applicable future handoff:
1. After the successful GPU result returns, capture the failed Result exception once into a non-null Throwable before passing it to PLog.e.
2. The fallback log message and throwable argument must use that same non-null value; do not call exceptionOrNull() again at the PLog.e argument.
3. This regression must be replayed by the packaged semantic validator before real compilation.
4. Real Kotlin compilation remains mandatory; static/parser checks do not substitute for it.
5. The correction must not alter true-2x reconstruction math, GPU-to-CPU fallback policy, color, DNG/UHDR, frame policy, or version/build.
