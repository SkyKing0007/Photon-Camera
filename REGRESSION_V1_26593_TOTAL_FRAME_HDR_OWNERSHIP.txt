PHOTON 26593 V1 TOTAL-FRAME HDR OWNERSHIP REGRESSION

CHANDELIER DEVICE FAILURE 26592 PERMANENT REGRESSION:
On the 2026-09-03 chandelier capture, shutter telemetry reported requestShort=true / iris26480ShortHighlightRequested=true, but the Motion processing trace later showed bridgeTotalScheduledFrames=15 and inputFrames=15 for a 15-frame request, with no IRIS_26592_SHORT_HANDOFF_EFFECT record. A required SHORT therefore disappeared before Sabre/HDR reconstruction and the capture silently continued NORMAL-only.

Permanent invariant: requestShort=true must never reach Sabre as NORMAL-only. If the shutter-frozen scene requires SHORT, its exact generation-owned ticket must remain owned by the capture plan through RAW/result delivery, immutable batch freeze, bridge admission, and Sabre scheduling. The only allowed terminal states are SHORT_PRESENT or an explicit SHORT_CAPTURE_FAILURE / SHORT_CAPTURE_TIMEOUT before reconstruction begins.

Slider contract:
- user frame slider N is the exact total physical RAW-frame budget, 1..30;
- each required SHORT/LONG reserves one slot at shutter time;
- normalTarget = N - requiredAuxCount and at least one NORMAL must remain;
- example N=15 + SHORT => 14 NORMAL + 1 SHORT = 15 total;
- example N=15 + SHORT + LONG => 13 NORMAL + 1 SHORT + 1 LONG = 15 total;
- no hidden 16th/17th frame may be added beyond the slider total.

Quick-open contract:
If the rolling ZSL ring is not yet full when the shutter is pressed, every eligible shutter-frozen NORMAL already present is retained and only the missing NORMAL count is captured post-shutter. Those top-up NORMALs use the shutter-frozen NORMAL exposure/ISO on manual-sensor devices, or capture-local AE lock with metadata-equality admission where manual control is unavailable. The preview repeating request is never mutated. No frame is duplicated to fake the requested count.

Lifetime contract:
- the shutter-created SHORT/LONG ticket objects are immutable members of the exact capture generation;
- finalization must never reacquire auxiliary ownership from a mutable global pointer;
- successful callback ingress may clear its global ingress pointer without destroying the generation-owned slot;
- the old 80/120 ms processing-side auxiliary seal is forbidden;
- capture completion is bounded by the generation-owned 1.4..3.5 second completion policy, not by an auxiliary deadline shorter than legitimate callback delivery;
- SHORT and LONG completion state are independently represented and neither silently consumes the other's ownership.

Reconstruction contract:
- NORMAL frames remain the body/denoise/detail authority;
- SHORT/LONG remain separate Sabre auxiliaries and never enter the NORMAL accumulator;
- DNG and true-2x CFA/detail evidence remain NORMAL-only;
- the existing successful 26592 radiometric SHORT handoff, same-Sabre geometry/rejection/unblocker gates, whole-RGB NEAREST mask, unclamped Motion tail and 8x UltraHDR range remain byte-identical.

ACTIONS JAVA COMPILER FAILURE 33833745581 PERMANENT REGRESSION:
GitHub Actions run 33833745581, job 100901909409 reached the real project Java compiler and failed in CaptureController.java because 26593 V1 directly assigned mPreviewCaptureResult to a TotalCaptureResult variable. Successful 26592 declares mPreviewCaptureResult as CaptureResult, so javac correctly reported: "incompatible types: CaptureResult cannot be converted to TotalCaptureResult".

Permanent compiler invariant:
- successful-26592 declaration `public static CaptureResult mPreviewCaptureResult;` is type authority;
- 26593 may retain the actual TotalCaptureResult callback object only through an explicit `instanceof TotalCaptureResult` checked cast, failing closed as capture-not-ready if that invariant is ever false;
- the exact direct assignment `final TotalCaptureResult iris26593NormalReference = mPreviewCaptureResult;` is forbidden;
- this compiler correction must not change slider-total accounting, shutter-frozen exposure values, SHORT/LONG ownership, Sabre scheduling, viewfinder metering, render, UltraHDR, Night, DNG, or SR behavior.
