26602 V1 — Full-Protected Bracket HDR + UHDR-Master SDR Parity

Upload/replace every file from this ZIP in the root of experimental-clean-photon-rebuild, preserving paths. Commit once and push. Do not upload or commit an APK. GitHub Actions is the authoritative compiler/build proof.

Runtime authority: successful 26601 V1 commit 5bafd5eeed4e253eb6bdcaa22588c1239fdea4fa / run 33971966223 / artifact 9971238245.
Verification mechanics: exact successful 26601 procedure with successful 26593 compiler/NDK/full-assemble order.
Backup: none, per user instruction; exact deterministic forward/rollback patches are sealed.

Runtime change: six files only. SHORT now consumes the same Sabre photometric/unblocker/dilated frame-weight protection as NORMAL at measurable boundaries, can propagate only that measured trust through the frozen 26600 2px coherent geometry, uses Bayer-quad coherent whole-RGB confidence, and no longer uses the 26601 identity-weight/hard accumulator switch. LONG's already-complete NORMAL protection implementation is preserved. DNG and true-2x high-frequency detail remain NORMAL-only.

Motion SDR/UHDR: UHDR pre-tone extended-linear rendition is the master grade. Motion SDR preserves that grade through guide=1.0 before the common 0.80 presentation scale; only true HDR headroom enters the shoulder. True-2x CPU/GPU publication uses the same rule. Night retains its prior 0.50 shoulder.

Before Actions success this handoff is PREPARED/UPLOAD-READY only; real pinned GLSL, Kotlin, Java, NDK and full assemble are not locally claimed.
