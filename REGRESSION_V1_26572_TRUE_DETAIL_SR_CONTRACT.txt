26572 V1 TRUE-DETAIL SUPER RESOLUTION CONTRACT

Runtime authority:
- exact successful 26571 V1 compiled candidate
- commit c3a1f82ca93de02403fda4bc85cff9462bc4aca4
- Actions run 33458186716 / job 99702528713
- artifact 9782274031 photon-26571-v1-gpu-publication
- artifact ZIP SHA-256 1e374c00ef96c93c145eda963bc0f3f26660a26911c2c5cb6c03337ed8997698
- deterministic candidate tar SHA-256 3c0009fe41150a447d3486e36d9bdbe846678e560d51cb007fc36e242dc6e557
- backup branch backup-26571-pre-true-detail-sr points exactly to the same commit.

Problem being corrected:
- 26567-26571 physically publish ~50MP but final RGB is primarily native-resolution Sabre/VGN guide upsampling multiplied by a conservative direct-CFA scalar residual.
- 26571 phase*signal*highlight*chroma*agreement multiplication plus +/-0.25 EV residual can collapse much of the 2x output toward the 12MP guide.
- 26572 must add reproducible high-resolution spatial information without returning independent 2x RGB/chroma authority that previously caused pink/green false color.

IRIS_26572_TRUE_DETAIL_LUMA_OWNER:
- Sabre Resolve + VGN remains sole RGB/chroma/highlight and low-frequency identity owner.
- direct-CFA true2x contributes only luminance structure.
- each exact 2x2 output cell computes direct-CFA luminance deviations around that cell's mean; the residual is zero-DC before the final per-pixel safety bound.
- no broad-neighborhood convolution, unsharp mask, or sharpening kernel is introduced.
- accepted direct-CFA microstructure is bounded to 0.42 relative amplitude before the final output factor bound of 0.68..1.47.
- output RGB is always trusted guide RGB multiplied by one scalar, so its per-pixel chromaticity cannot be replaced by direct-CFA chroma.

Evidence/safety:
- block phase support: 4=>1.00, 3=>0.85, 2=>0.50, <2=>0.
- signal safety is based on trusted 2x2 guide luminance.
- highlight safety remains strong: blocks reaching the existing 0.72..0.92 highlight protection transition progressively lose direct detail and at the top return exact guide.
- chroma disagreement is measured on 2x2 block means, avoiding per-pixel CFA color alias rejection while still blocking low-frequency material/color disagreement.
- low-frequency direct/guide luminance disagreement retains the existing agreement safety threshold.
- confidence uses phase evidence times the weakest safety condition rather than multiplying all moderate safety gates into near-zero.
- if confidence is zero, factor=1 and RGB is the exact successful 26571 Sabre/VGN guide.

Preserved successful 26571 behavior:
- GlesIris26529SpatialRgbChromaPostprocessor.kt is byte-identical: clean sky, no-clump surface continuity, one-sided luma halo protection, coherent edge color, cross-edge chroma exclusion, and pink/magenta highlight safety are unchanged.
- motionv2_jpeg444_jni.cpp from IRIS_26571_TRUE2X_GPU_PUBLICATION onward is byte-identical: bounded GPU band, two PBOs/fences, concurrent CPU JPEG, exact CPU publication fallback, Display-P3, 4:4:4 base, 1:1 gain map, requested quality and gain q95 are unchanged.
- CMakeLists.txt is byte-identical to successful 26571.
- DNG PRE_VGN direct-CFA boundary remains unchanged.
- capture/frame/exposure/alignment/Sabre/VGN/UHDR owners are unchanged.

Telemetry:
- GPU render readback alpha packs phaseCount plus exact binary16 quarter/half-step activity class without adding a second full-resolution readback.
- IRIS_26572_TRUE_DETAIL_ACTIVITY reports GPU fallback/active/strong percentages.
- CPU fallback reports accepted/strong/highlight-rejected/disagreement-rejected/phase-rejected percentages.
- IRIS_26572_TRUE_DETAIL_RENDER_FUSED and IRIS_26568_TRUE2X_READY explicitly report highResLumaOwner=DIRECT_CFA and directChromaOwner=false.
- all existing IRIS_26571_TRUE2X_GPU_PUBLICATION / GPU_FALLBACK / CPU_FALLBACK performance telemetry remains byte-identical.

Permanent synthetic regressions:
- flat direct evidence => exact guide.
- accepted full-phase 2x microstructure must carry at least 35% more modulation contrast than the 26571 residual fixture.
- direct 2x2 residual sum must be zero before safety scaling.
- accepted unclipped fixture preserves guide block mean.
- <2 block phases => exact guide.
- 2-phase support may contribute but remains bounded.
- protected highlight => exact guide.
- strong block chroma/material disagreement => exact guide.
- accepted output chromaticity equals trusted guide chromaticity.
- no factor can exceed 0.68..1.47.
- renderReadback must NOT require a non-null Iris26572ReadyBand output; deferred two-PBO output is intentional and must remain reachable exactly as in successful 26571 publication mechanics.
- true-detail must share the four 2x2 direct/phase fetches per fragment and use one native Sabre/VGN block anchor for block safety/DC scaling; it must not reintroduce four full bilinear guide evaluations for every output pixel and erase the 26571 speed work.
