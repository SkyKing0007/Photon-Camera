diff --git a/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl b/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
index 8a1fa9087e2467f76a425bdfae3d8bc7854e35d1..b623ff40b1308fdb260e1c6bd5fc0e6d4ce5a5e2 100644
--- a/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
+++ b/app/src/main/assets/shaders/motionv2/adaptive_color_appearance_26563.glsl
@@ -88,24 +88,12 @@ float iris26585MapHeadroomGuide(float guide) {
     return toneStart + (preScaleDisplayWhite - toneStart) * shaped;
 }
 
-float iris26614MapMotionSdrFinalGuide(float sourceGuide){
-    const float outputExposureScale=0.80;
-    float x=max(sourceGuide,0.0);
-    float requestedFinalGain=max(displayGain,1.0e-6)*outputExposureScale;
-    float whiteAnchor=min(0.95,requestedFinalGain);
-    float bodyGain=requestedFinalGain;
-    if(requestedFinalGain>whiteAnchor){
-        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
-    }
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-    }
-    float reserve=1.0-whiteAnchor;
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(whiteAnchor,1.0e-6);
-    float excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
+float iris26613MapSdrSourceFinal(float sourceFinal){
+    const float knee=0.80;
+    const float reserve=1.0-knee;
+    if(sourceFinal<=knee)return sourceFinal;
+    float excess=sourceFinal-knee;
+    return knee+reserve*excess/(excess+reserve);
 }
 
 float iris26585PostTonePreGamutPeak(vec3 preDisplayRgb) {
@@ -116,7 +104,9 @@ float iris26585PostTonePreGamutPeak(vec3 preDisplayRgb) {
     float sourceGuide=max(sourceLuma,sourcePeak);
     if(sourceGuide<=1.0e-7) return 0.0;
     if(iris26598MotionPublication!=0){
-        float mappedFinal=iris26614MapMotionSdrFinalGuide(sourceGuide);
+        const float kneeFinal=0.40;
+        float targetFinal=sourceGuide*max(displayGain,1.0e-6)*outputExposureScale;
+        float mappedFinal=iris26613MapSdrSourceFinal(targetFinal);
         return sourcePeak*(mappedFinal/sourceGuide);
     }
     vec3 postRgb=sourceRgb*max(displayGain,1.0e-6);
diff --git a/app/src/main/assets/shaders/motionv2/gainmap.glsl b/app/src/main/assets/shaders/motionv2/gainmap.glsl
index 13cf70e51e0f8ee15fedef0471e12f3c32018e72..aa6df3ec8789014cfcccdccbfc22b668c5703fe5 100644
--- a/app/src/main/assets/shaders/motionv2/gainmap.glsl
+++ b/app/src/main/assets/shaders/motionv2/gainmap.glsl
@@ -72,23 +72,15 @@ void main(){
         vec2 zoomedSource=center+(sourcePixel-center)/zoom;
         hdrRgb=iris26524BilinearHdr(zoomedSource);
     }
+    float hdrTargetScale=hdrExposureScale*(motionHdrHandoff!=0?max(displayGain,1.0e-6):1.0);
     vec3 hdrPositive=max(hdrRgb,vec3(0.0));
+    float hdrBase=max(luminance(hdrPositive*hdrTargetScale),0.0);
+    /* IRIS_26613_CLEAN_HDR_MASTER_GAIN_AUTHORITY
+     * The common clean extended-HDR master is already scene-radiance authority. UHDR derives only
+     * its reversible HDR/SDR relationship here; no second percentile-driven HDR boost exists. */
+    float hdr=hdrBase;
     float sdr=max(luminance(srgbDecode(iris26550BilinearSdr(sourcePixel))),0.0);
     float safeMax=max(maxGainRatio,1.001);
-    float ratio;
-    if(motionHdrHandoff!=0){
-        /* IRIS_26614_CANONICAL_APPEARANCE_UHDR_EXTENSION
-         * The SDR base is the appearance authority. Ultra HDR is forbidden from dividing the clean
-         * RAW/HDR master by the tone-mapped SDR because that mathematically undoes SDR appearance
-         * and lets the gain map restore house/tree/curtain/ceiling structure absent from the base.
-         * Motion gain is therefore exactly unity through nominal source white. Only genuine >1
-         * source headroom adds scalar display luminance, preserving the SDR color/gamma structure. */
-        float sourceGuide=max(max3(hdrPositive),luminance(hdrPositive));
-        ratio=clamp(max(sourceGuide,1.0),1.0,safeMax);
-    }else{
-        float hdrTargetScale=hdrExposureScale;
-        float hdr=max(luminance(hdrPositive*hdrTargetScale),0.0);
-        ratio=clamp((hdr+UHDR_OFFSET)/(sdr+UHDR_OFFSET),1.0,safeMax);
-    }
+    float ratio=clamp((hdr+UHDR_OFFSET)/(sdr+UHDR_OFFSET),1.0,safeMax);
     Output=clamp(log2(ratio)/max(log2(safeMax),1.0e-6),0.0,1.0);
 }
diff --git a/app/src/main/assets/shaders/motionv2/render.glsl b/app/src/main/assets/shaders/motionv2/render.glsl
index b9578215a51538ebcfcbdf5ca01f8631135b1053..ffcf6b48476cacd7409441ef88baf73d5cbefede 100644
--- a/app/src/main/assets/shaders/motionv2/render.glsl
+++ b/app/src/main/assets/shaders/motionv2/render.glsl
@@ -95,33 +95,30 @@ vec3 iris26524BilinearInput(vec2 sourcePixel) {
     return mix(a,b,f.y);
 }
 
-/* IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_APPEARANCE
- * One C1 monotonic source-domain appearance map. Low source values retain the solved viewfinder
- * body slope. Extra display gain fades continuously toward nominal source white instead of moving
- * the highlight boundary downward as displayGain rises. Source white maps to 0.95 and >1 clean
- * HDR occupies the final 5% asymptotically. No histogram percentile or scene-dependent knot. */
-float iris26614MapMotionSdrFinalGuide(float sourceGuide) {
-    float x=max(sourceGuide,0.0);
-    float requestedFinalGain=max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
-    float whiteAnchor=min(0.95,requestedFinalGain);
-    float bodyGain=requestedFinalGain;
-    if(requestedFinalGain>whiteAnchor){
-        bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
-    }
-    if(x<=1.0){
-        float oneMinus=1.0-x;
-        return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;
-    }
-    float reserve=1.0-whiteAnchor;
-    if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(whiteAnchor,1.0e-6);
-    float excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
+/* IRIS_26613_FIXED_DOMAIN_UPPER_HIGHLIGHT_SHOULDER
+ * Device 26612 proved that scene-quantile knots can collapse together and create artificial
+ * slope changes while also remapping unrelated body structure. Motion now has one fixed-domain,
+ * C1-continuous scalar shoulder. Values through 0.80 final-linear are exact identity; only the
+ * upper display range is compressed. No histogram percentile participates in publication. */
+float iris26613MapSdrSourceFinal(float sourceFinal) {
+    const float knee = 0.80;
+    const float reserve = 1.0 - knee;
+    if (sourceFinal <= knee) return sourceFinal;
+    float excess = sourceFinal - knee;
+    return knee + reserve * excess / (excess + reserve);
 }
 
 float mapFinalSdrGuide(float sourceGuide) {
+    /* IRIS_26604_SINGLE_TONE_OWNER
+     * Motion never receives a pre-tone RGB*displayGain texture. The solved gain is consumed here
+     * only as a brightness target. Values through the proven final-domain 0.40 body anchor are
+     * mathematically identical to 26603 body brightness; only the highlight tail is compressed.
+     * Night retains its exact prior scalar math, folded into this final-domain return value.
+     */
     if(iris26592MotionHdrHandoff!=0) {
-        return iris26614MapMotionSdrFinalGuide(sourceGuide);
+        float targetFinal=sourceGuide*max(displayGain,1.0e-6)*max(outputExposureScale,1.0e-6);
+        /* IRIS_26613_FIXED_DOMAIN_SDR_OWNER */
+        return iris26613MapSdrSourceFinal(targetFinal);
     }
 
     float start=0.50;
diff --git a/app/src/main/cpp/motionv2_jpeg444_jni.cpp b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
index 1c4812c0f6f278177a1dc88c5b780747737e1dce..c27c10595bcc5aa40e1a1d23c622d8cfd34bb0a5 100644
--- a/app/src/main/cpp/motionv2_jpeg444_jni.cpp
+++ b/app/src/main/cpp/motionv2_jpeg444_jni.cpp
@@ -594,23 +594,20 @@ inline bool samplePreRenderCached(const ProfileRegion&s,const Params&p,float x,f
     *out=mix3(mix3(a,b,fx),mix3(c,d,fx),fy);return true;
 }
 
-inline float iris26614MapMotionSdrFinalGuide(float sourceGuide,float displayGain){
-    constexpr float scale=0.80f;
-    float x=std::max(sourceGuide,0.f);
-    float requestedFinalGain=std::max(displayGain,1.0e-6f)*scale;
-    float whiteAnchor=std::min(0.95f,requestedFinalGain);
-    float bodyGain=requestedFinalGain;
-    if(requestedFinalGain>whiteAnchor)bodyGain=std::min(requestedFinalGain,4.f*whiteAnchor-1.0e-4f);
-    if(x<=1.f){float oneMinus=1.f-x;return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;}
-    float reserve=1.f-whiteAnchor;if(reserve<=1.0e-6f)return whiteAnchor;
-    float tailScale=reserve/std::max(whiteAnchor,1.0e-6f),excess=x-1.f;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
+inline float iris26613MapSdrSourceFinal(float sourceFinal){
+    constexpr float knee=0.80f,reserve=0.20f;
+    if(sourceFinal<=knee)return sourceFinal;
+    float excess=sourceFinal-knee;
+    return knee+reserve*excess/(excess+reserve);
 }
 inline Vec3 renderHeadroom(Vec3 rgb,const Params&p){
     rgb=clampNonnegative(rgb);float y=std::max(luma(rgb),0.f),pk=peak(rgb),guide=std::max(y,pk);
     /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_CPU */
     if(p.motionHdrHandoff){
-        float mappedFinal=iris26614MapMotionSdrFinalGuide(guide,p.displayGain);
+        constexpr float scale=0.80f;
+        float targetFinal=guide*std::max(p.displayGain,1.0e-6f)*scale;
+        float mappedFinal=targetFinal;
+        mappedFinal=iris26613MapSdrSourceFinal(targetFinal);
         if(guide>1.0e-7f)rgb=mul(rgb,mappedFinal/guide);
     }else{
         const float start=0.50f;
@@ -668,13 +665,13 @@ inline bool watermarkUv(const Params&p,const Watermark&w,int ox,int oy,float*u,f
 }
 inline bool renderBase(const SourceRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRender(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float hdrScale=p.motionHdrHandoff?std::max(p.displayGain,1.0e-6f)*0.80f:1.f;float hdrBase=std::max(luma(clampNonnegative(hdr))*hdrScale,0.f),hdrY=hdrBase,sdrY=std::max(luma(clampNonnegative(sdr)),0.f);float ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
 inline bool renderBaseCached(const ProfileRegion&s,const Params&p,const Watermark&w,const SrgbLut&lut,int ox,int oy,float gainMax,float gainContentMax,uint8_t*out,uint8_t*gainOut){
     float sx,sy;outputToSource(p,ox,oy,&sx,&sy);Vec3 hdr;if(!samplePreRenderCached(s,p,sx,sy,&hdr))return false;Vec3 sdr=renderHeadroom(hdr,p);
-    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float ratio;if(p.motionHdrHandoff){Vec3 hp=clampNonnegative(hdr);float sourceGuide=std::max(peak(hp),luma(hp));ratio=clampf(std::max(sourceGuide,1.f),1.f,contentMax);}else{float hdrY=std::max(luma(clampNonnegative(hdr)),0.f),sdrY=std::max(luma(clampNonnegative(sdr)),0.f);ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);}float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
+    if(gainOut){const float off=0.015625f,safeMax=std::max(gainMax,1.001f),contentMax=clampf(gainContentMax,1.f,safeMax);float hdrScale=p.motionHdrHandoff?std::max(p.displayGain,1.0e-6f)*0.80f:1.f;float hdrBase=std::max(luma(clampNonnegative(hdr))*hdrScale,0.f),hdrY=hdrBase,sdrY=std::max(luma(clampNonnegative(sdr)),0.f);float ratio=clampf((hdrY+off)/(sdrY+off),1.f,contentMax);float code=clampf(std::log2(ratio)/std::max(std::log2(safeMax),1.0e-6f),0.f,1.f);*gainOut=(uint8_t)std::lround(code*255.f);}
     float rr=lut.at(sdr.r),gg=lut.at(sdr.g),bb=lut.at(sdr.b);float wu,wv;if(watermarkUv(p,w,ox,oy,&wu,&wv)){float a=sampleWatermarkAlpha(w,wu,wv);if(a>0.f){Vec3 wc=sampleWatermark(w,wu,wv);rr=rr+(wc.r-rr)*a;gg=gg+(wc.g-gg)*a;bb=bb+(wc.b-bb)*a;}}
     out[0]=(uint8_t)std::lround(clampf(rr,0.f,1.f)*255.f);out[1]=(uint8_t)std::lround(clampf(gg,0.f,1.f)*255.f);out[2]=(uint8_t)std::lround(clampf(bb,0.f,1.f)*255.f);return true;
 }
@@ -892,24 +889,21 @@ vec2 irisOutputToSource(ivec2 op){
     float sx=cx+(ux-cx)/uResidualZoom,sy=cy+(uy-cy)/uResidualZoom;
     return clamp(2.0*(vec2(sx,sy)+vec2(0.5))-vec2(0.5),vec2(0.0),vec2(uTrueSize-ivec2(1)));
 }
-float iris26614MapMotionSdrFinalGuide(float sourceGuide){
-    const float scale=0.80;
-    float x=max(sourceGuide,0.0);
-    float requestedFinalGain=max(uDisplayGain,1.0e-6)*scale;
-    float whiteAnchor=min(0.95,requestedFinalGain);
-    float bodyGain=requestedFinalGain;
-    if(requestedFinalGain>whiteAnchor)bodyGain=min(requestedFinalGain,4.0*whiteAnchor-1.0e-4);
-    if(x<=1.0){float oneMinus=1.0-x;return whiteAnchor*x+(bodyGain-whiteAnchor)*x*oneMinus*oneMinus;}
-    float reserve=1.0-whiteAnchor;if(reserve<=1.0e-6)return whiteAnchor;
-    float tailScale=reserve/max(whiteAnchor,1.0e-6),excess=x-1.0;
-    return whiteAnchor+reserve*excess/(excess+tailScale);
+float iris26613MapSdrSourceFinal(float sourceFinal){
+    const float knee=0.80,reserve=0.20;
+    if(sourceFinal<=knee)return sourceFinal;
+    float excess=sourceFinal-knee;
+    return knee+reserve*excess/(excess+reserve);
 }
 vec3 irisHeadroom(vec3 rgb){
     rgb=irisClampNonnegative(rgb);
     float y=max(irisLuma(rgb),0.0),pk=irisPeak(rgb),guide=max(y,pk);
     /* IRIS_26604_SINGLE_TONE_OWNER_TRUE2X_GPU */
     if(uMotionHdrHandoff!=0){
-        float mappedFinal=iris26614MapMotionSdrFinalGuide(guide);
+        const float scale=0.80,knee=0.40,reserve=0.60;
+        float targetFinal=guide*max(uDisplayGain,1.0e-6)*scale;
+        float mappedFinal=targetFinal;
+        mappedFinal=iris26613MapSdrSourceFinal(targetFinal);
         if(guide>1.0e-7)rgb*=mappedFinal/guide;
     }else{
         float start=0.50;
@@ -934,16 +928,11 @@ void main(){
     if(uGenerateGain!=0){
         const float off=0.015625;float safeMax=max(uGainMax,1.001);
         float contentMax=clamp(uGainContentMax,1.0,safeMax);
-        float ratio;
-        if(uMotionHdrHandoff!=0){
-            vec3 hp=irisClampNonnegative(hdr);
-            float sourceGuide=max(irisPeak(hp),irisLuma(hp));
-            ratio=clamp(max(sourceGuide,1.0),1.0,contentMax);
-        }else{
-            float hdrY=max(irisLuma(irisClampNonnegative(hdr)),0.0);
-            float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
-            ratio=clamp((hdrY+off)/(sdrY+off),1.0,contentMax);
-        }
+        float hdrScale=uMotionHdrHandoff!=0?max(uDisplayGain,1.0e-6)*0.80:1.0;
+        float hdrBase=max(irisLuma(irisClampNonnegative(hdr))*hdrScale,0.0);
+        float hdrY=hdrBase;
+        float sdrY=max(irisLuma(irisClampNonnegative(sdr)),0.0);
+        float ratio=clamp((hdrY+off)/(sdrY+off),1.0,contentMax);
         float code=clamp(log2(ratio)/max(log2(safeMax),1.0e-6),0.0,1.0);
         gainCode=irisByte(code);
     }
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
index a9931385fdbf875ba5506aed57dc9e1d8c6c3833..832aaf32e04091bcd24204e5d393d563c312334f 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesIris26529SpatialRgbChromaPostprocessor.kt
@@ -139,8 +139,6 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
         onFinalSubmitted: (() -> Unit)? = null,
         sabreSupportR: Int = 0,
         sabreSupportGb: Int = 0,
-        sabreValidWeights: Int = 0,
-        sabreValidityWeightScale: Float = 1f,
     ): Result {
         check(assembledRgb != 0)
         check(writtenBands == bands.size) {
@@ -192,10 +190,7 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
          * longer pulled toward a weaker opposite-side consensus. Clean sky remains pass-through;
          * highlight false-color suppression remains authoritative.
          */
-        dispatchUniversalAdaptiveColor(
-            assembledRgb, workA, sabreSupportR, sabreSupportGb, sabreValidWeights,
-            sabreValidityWeightScale,
-        )
+        dispatchUniversalAdaptiveColor(assembledRgb, workA, sabreSupportR, sabreSupportGb)
         val completedVgn = assembledRgb
         assembledRgb = workA
         workA = completedVgn
@@ -369,14 +364,11 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
         destination: Int,
         sabreSupportR: Int,
         sabreSupportGb: Int,
-        sabreValidWeights: Int,
-        sabreValidityWeightScale: Float,
     ) {
         check(universalAdaptiveColorProgram != 0)
-        val sabreSupportValid = sabreSupportR != 0 && sabreSupportGb != 0 && sabreValidWeights != 0
-        check((sabreSupportR == 0) == (sabreSupportGb == 0) &&
-            (sabreSupportR == 0) == (sabreValidWeights == 0)) {
-            "Sabre validity provenance must be provided as total R + total GB + valid RGB"
+        val sabreSupportValid = sabreSupportR != 0 && sabreSupportGb != 0
+        check((sabreSupportR == 0) == (sabreSupportGb == 0)) {
+            "Sabre RGB support provenance must be provided as an R + GB pair"
         }
         GLES31.glUseProgram(universalAdaptiveColorProgram)
         setImageSize(universalAdaptiveColorProgram)
@@ -386,10 +378,6 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
             host.uniformLocation(universalAdaptiveColorProgram, "uSabreSupportValid"),
             if (sabreSupportValid) 1 else 0,
         )
-        GLES31.glUniform1f(
-            host.uniformLocation(universalAdaptiveColorProgram, "uSabreValidityWeightScale"),
-            sabreValidityWeightScale.coerceAtLeast(1f),
-        )
         if (sabreSupportValid) {
             GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 2)
             GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, sabreSupportR)
@@ -397,22 +385,13 @@ internal class GlesIris26529SpatialRgbChromaPostprocessor(
             GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 3)
             GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, sabreSupportGb)
             GLES31.glUniform1i(host.uniformLocation(universalAdaptiveColorProgram, "uSabreWeightsGb"), 3)
-            GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 4)
-            GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, sabreValidWeights)
-            GLES31.glUniform1i(host.uniformLocation(universalAdaptiveColorProgram, "uSabreValidWeights"), 4)
         }
         trackedDispatch(
-            "IRIS 26614 RAW-CFA-validity-owned universal adaptive color",
-            if (sabreSupportValid) {
-                intArrayOf(source, sabreSupportR, sabreSupportGb, sabreValidWeights)
-            } else {
-                intArrayOf(source)
-            },
+            "IRIS 26613 Sabre-support-aware universal adaptive color",
+            if (sabreSupportValid) intArrayOf(source, sabreSupportR, sabreSupportGb) else intArrayOf(source),
             intArrayOf(destination),
         )
         if (sabreSupportValid) {
-            GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 4)
-            GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, 0)
             GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 3)
             GLES30.glBindTexture(GLES30.GL_TEXTURE_2D, 0)
             GLES30.glActiveTexture(GLES30.GL_TEXTURE0 + 2)
@@ -664,15 +643,12 @@ internal object Iris26529SpatialRgbChromaShaders {
         layout(rgba16ui, binding = 0) readonly uniform highp uimage2D uSource;
         layout(rgba16ui, binding = 1) writeonly uniform highp uimage2D uDestination;
         uniform ivec2 uImageSize;
-        /* IRIS_26614_RAW_CFA_CHANNEL_VALIDITY_PROVENANCE
-         * Total R/G/B reconstruction weights plus the additive subset backed by source CFA samples
-         * that retained real sensor-code headroom. The ratio is measurement validity, not a color
-         * classifier. Valid measured color is protected; invalid color cannot protect itself merely
-         * because the reconstructed RGB artifact forms a coherent contour. */
+        /* IRIS_26613_SABRE_CHANNEL_SUPPORT_PROVENANCE
+         * Exact accumulated R/G/B weights from the common Sabre accumulator, sampled read-only.
+         * These weights can authorize a decisive neutral false-color correction but never alter
+         * temporal contribution, geometry, Resolve, VGN, or genuine supported color. */
         uniform highp sampler2D uSabreWeightR;
         uniform highp sampler2D uSabreWeightsGb;
-        uniform highp sampler2D uSabreValidWeights;
-        uniform highp float uSabreValidityWeightScale;
         uniform int uSabreSupportValid;
 
         vec3 loadRgb(ivec2 p) {
@@ -680,21 +656,6 @@ internal object Iris26529SpatialRgbChromaShaders {
             return vec3(packedValue.rgb) / 65535.0;
         }
 
-        vec3 loadSabreChannelValidity(ivec2 p) {
-            if (uSabreSupportValid == 0) return vec3(1.0);
-            ivec2 q = clamp(p, ivec2(0), uImageSize - ivec2(1));
-            vec3 totalWeight = vec3(
-                max(texelFetch(uSabreWeightR, q, 0).r, 0.0),
-                max(texelFetch(uSabreWeightsGb, q, 0).r, 0.0),
-                max(texelFetch(uSabreWeightsGb, q, 0).g, 0.0));
-            vec3 validWeight = max(texelFetch(uSabreValidWeights, q, 0).rgb, vec3(0.0)) *
-                max(uSabreValidityWeightScale, 1.0);
-            return clamp(validWeight / max(totalWeight, vec3(1.0e-6)), vec3(0.0), vec3(1.0));
-        }
-
-        float minimum3(vec3 v) { return min(v.r, min(v.g, v.b)); }
-        float maximum3(vec3 v) { return max(v.r, max(v.g, v.b)); }
-
         float luminanceOf(vec3 rgb) {
             return dot(rgb, vec3(0.25, 0.50, 0.25));
         }
@@ -712,18 +673,9 @@ internal object Iris26529SpatialRgbChromaShaders {
             vec3 centerChroma = center - vec3(centerLuma);
             vec3 centerNormalizedChroma = centerChroma / centerScale;
             float centerNormalizedMagnitude = length(centerNormalizedChroma);
-            vec3 centerChannelValidity = loadSabreChannelValidity(p);
-            float centerMeasuredValidity = minimum3(centerChannelValidity);
-            float measuredColorProtection = uSabreSupportValid != 0
-                ? smoothstep(0.985, 0.9995, centerMeasuredValidity) : 0.0;
-            float centerValidConsensusWeight = uSabreSupportValid != 0
-                ? smoothstep(0.90, 0.985, centerMeasuredValidity) : 0.0;
 
             vec3 normalizedChromaSum = centerNormalizedChroma;
             float weightSum = 1.0;
-            vec3 validNormalizedChromaSum = centerNormalizedChroma * centerValidConsensusWeight;
-            float validWeightSum = centerValidConsensusWeight;
-            float validNeighborSupport = 0.0;
             float neighborSupport = 0.0;
             float neutralNeighborSupport = 0.0;
             float maximumRelativeLumaDelta = 0.0;
@@ -749,15 +701,6 @@ internal object Iris26529SpatialRgbChromaShaders {
                     float neighborNormalizedMagnitude = length(neighborNormalizedChroma);
                     neutralNeighborSupport += sameSurfaceWeight *
                         (1.0 - smoothstep(0.035, 0.100, neighborNormalizedMagnitude));
-                    if (uSabreSupportValid != 0) {
-                        vec3 neighborChannelValidity = loadSabreChannelValidity(p + ivec2(x, y));
-                        float neighborMeasuredValidity = minimum3(neighborChannelValidity);
-                        float measuredNeighborWeight = smoothstep(0.90, 0.985, neighborMeasuredValidity);
-                        float validSameSurfaceWeight = sameSurfaceWeight * measuredNeighborWeight;
-                        validNormalizedChromaSum += neighborNormalizedChroma * validSameSurfaceWeight;
-                        validWeightSum += validSameSurfaceWeight;
-                        validNeighborSupport += validSameSurfaceWeight;
-                    }
                 }
             }
 
@@ -767,16 +710,6 @@ internal object Iris26529SpatialRgbChromaShaders {
             float chromaAgreement = hueAgreement(centerNormalizedChroma, consensusNormalizedChroma);
             float surfaceSupport = smoothstep(2.5, 5.75, neighborSupport);
             float neutralSurfaceSupport = smoothstep(2.50, 4.00, neutralNeighborSupport);
-            vec3 validConsensusNormalizedChroma = validWeightSum > 1.0e-6
-                ? validNormalizedChromaSum / validWeightSum : centerNormalizedChroma;
-            float validConsensusSupport = uSabreSupportValid != 0
-                ? clamp(validNeighborSupport / 3.50, 0.0, 1.0) : 0.0;
-            float physicalChromaDisagreement =
-                length(centerNormalizedChroma - validConsensusNormalizedChroma);
-            /* Physical provenance has no hard "artifact must be this strong" dead zone. Once a
-             * channel is known to be radiometrically invalid, disagreement with valid same-surface
-             * color is continuous evidence rather than another scene-tuned classification gate. */
-            float physicalOutlierEvidence = clamp(physicalChromaDisagreement / 0.180, 0.0, 1.0);
             float legacyEdgeProtection = smoothstep(0.45, 0.90, maximumRelativeLumaDelta);
 
             /* IRIS_26571_SAME_SIDE_MATERIAL_BOUNDARY: preserve the proven 26571 material-boundary ownership. */
@@ -968,21 +901,35 @@ internal object Iris26529SpatialRgbChromaShaders {
             float realColorConfidence = clamp(max(max(max(supportedMaterialBoundary,
                 coherentCenterProtection), topologyProtection), microObjectProtection), 0.0, 1.0);
 
-            /* IRIS_26614_PHYSICAL_COLOR_VALIDITY_AUTHORITY
-             * 26613 used total contribution imbalance, then allowed reconstructed-RGB topology to
-             * veto it. That let a coherent magenta rail classify itself as real color. 26614 uses
-             * the ratio of valid source-CFA weight to total reconstruction weight instead. The
-             * correction direction is measured from neighboring pixels whose own R/G/B validity
-             * is high and whose luminance places them on the same local surface. */
-            vec3 channelInvalidity = uSabreSupportValid != 0
-                ? vec3(1.0) - centerChannelValidity : vec3(0.0);
-            vec3 physicalTargetChroma = validConsensusNormalizedChroma * centerScale;
-            vec3 physicalDesiredDelta = physicalTargetChroma - centerChroma;
-            float physicalDesiredLength = length(physicalDesiredDelta);
-            float invalidChannelDirection = physicalDesiredLength > 1.0e-7
-                ? clamp(length(physicalDesiredDelta * channelInvalidity) / physicalDesiredLength,
-                    0.0, 1.0) : 0.0;
-            float physicalValidityEvidence = invalidChannelDirection * validConsensusSupport;
+            vec3 sabreChannelSupport = vec3(1.0);
+            float sabreSupportEvidence = 0.0;
+            float sabreWeakChannelDirection = 0.0;
+            if (uSabreSupportValid != 0) {
+                sabreChannelSupport = vec3(
+                    max(texelFetch(uSabreWeightR, p, 0).r, 0.0),
+                    max(texelFetch(uSabreWeightsGb, p, 0).r, 0.0),
+                    max(texelFetch(uSabreWeightsGb, p, 0).g, 0.0));
+                float supportMax = max(sabreChannelSupport.r,
+                    max(sabreChannelSupport.g, sabreChannelSupport.b));
+                float supportMin = min(sabreChannelSupport.r,
+                    min(sabreChannelSupport.g, sabreChannelSupport.b));
+                float supportImbalance = supportMax > 1.0e-7
+                    ? clamp((supportMax - supportMin) / supportMax, 0.0, 1.0) : 0.0;
+                int weakChannel = sabreChannelSupport.r <= sabreChannelSupport.g &&
+                        sabreChannelSupport.r <= sabreChannelSupport.b ? 0 :
+                    (sabreChannelSupport.g <= sabreChannelSupport.b ? 1 : 2);
+                vec3 supportTargetChroma = consensusNormalizedChroma * centerScale;
+                vec3 supportDesiredDelta = supportTargetChroma - centerChroma;
+                float supportDesiredLength = length(supportDesiredDelta);
+                float weakIncrease = weakChannel == 0 ? supportDesiredDelta.r :
+                    (weakChannel == 1 ? supportDesiredDelta.g : supportDesiredDelta.b);
+                sabreWeakChannelDirection = supportDesiredLength > 1.0e-7
+                    ? clamp(weakIncrease / supportDesiredLength, 0.0, 1.0) : 0.0;
+                /* Continuous physical evidence: the more one channel lacks actual reconstruction
+                 * support and the more the neutral correction specifically restores that channel,
+                 * the stronger the evidence. No scene/object threshold participates. */
+                sabreSupportEvidence = supportImbalance * sabreWeakChannelDirection;
+            }
 
             /* Fail-closed false-color proof.
              * 1) The center must disagree materially with same-surface consensus.
@@ -999,71 +946,55 @@ internal object Iris26529SpatialRgbChromaShaders {
             float neutralConsensusEvidence = 1.0 - smoothstep(0.070, 0.160, consensusMagnitude);
             float isolatedEvidence = smoothstep(1.35, 2.75, centerToConsensusMagnitude);
             float phaseConfidence = phaseLikeEvidence * mix(0.70, 1.0, isolatedEvidence);
-            float legacyFalseColorBase = centerOutlierEvidence * neutralConsensusEvidence *
-                surfaceSupport * neutralSurfaceSupport * (1.0 - realColorConfidence) *
-                (1.0 - measuredColorProtection);
-            float legacyFalseColorScore = legacyFalseColorBase * phaseConfidence;
-
-            /* Physical validity does not require a neutral target. A real red/blue/purple material
-             * with one clipped channel is repaired toward physically valid same-surface color, not
-             * toward grey. Conversely a neutral window/ceiling edge naturally has a neutral valid
-             * consensus. RGB topology is deliberately not a veto on this path. */
-            float physicalFalseColorScore = physicalOutlierEvidence * physicalValidityEvidence;
-
-            vec3 legacyTargetChroma = consensusNormalizedChroma * centerScale;
+            float falseColorBase = centerOutlierEvidence * neutralConsensusEvidence * surfaceSupport *
+                neutralSurfaceSupport * (1.0 - realColorConfidence);
+            float legacyFalseColorScore = falseColorBase * phaseConfidence;
+            float physicalFalseColorScore = falseColorBase * sabreSupportEvidence;
+            float falseColorScore = max(legacyFalseColorScore, physicalFalseColorScore);
+            float falseColorGate = smoothstep(0.72, 0.90, falseColorScore);
+
+            vec3 targetChroma = consensusNormalizedChroma * centerScale;
             float centerMagnitude = length(centerChroma);
-            float legacyTargetMagnitude = length(legacyTargetChroma);
-            if (legacyTargetMagnitude > centerMagnitude && legacyTargetMagnitude > 1.0e-7) {
-                legacyTargetChroma *= centerMagnitude / legacyTargetMagnitude;
-                legacyTargetMagnitude = centerMagnitude;
+            float targetMagnitude = length(targetChroma);
+            if (targetMagnitude > centerMagnitude && targetMagnitude > 1.0e-7) {
+                targetChroma *= centerMagnitude / targetMagnitude;
             }
 
-            float targetAgreement = hueAgreement(centerChroma, legacyTargetChroma);
-            float targetNeutral = 1.0 - smoothstep(0.010, 0.045, legacyTargetMagnitude);
+            /* IRIS_26611_DECISIVE_NEUTRAL_CFA_FALSE_COLOR_OWNER
+             * Ambiguous and genuinely colored pixels retain the proven fail-closed 26578 cap. But
+             * when every independent gate already says "neutral same-surface Bayer-phase outlier"
+             * with overwhelming confidence, the old min(0.05,40%) cap is itself a known failure:
+             * device 26610 can carry a strong 1-2 px opponent fringe through VGN. In that narrow
+             * decisive case the observed neutral consensus is allowed to become authoritative in a
+             * single pass. Real-color topology remains a multiplicative veto through falseColorScore. */
+            float targetAgreement = hueAgreement(centerChroma, targetChroma);
+            float targetNeutral = 1.0 - smoothstep(0.010, 0.045, targetMagnitude);
             float supportedTarget = max(targetNeutral, smoothstep(0.82, 0.94, targetAgreement));
-            float legacyCorrection = smoothstep(0.72, 0.90, legacyFalseColorScore) * supportedTarget;
-            vec3 legacyDesiredDelta = legacyTargetChroma - centerChroma;
-            float legacyDesiredLength = length(legacyDesiredDelta);
+            float correction = falseColorGate * supportedTarget;
+            vec3 desiredDelta = targetChroma - centerChroma;
+            float desiredLength = length(desiredDelta);
             float legacyDecisiveNeutralCfaProof =
                 smoothstep(0.92, 0.985, legacyFalseColorScore) *
                 smoothstep(0.88, 0.985, phaseLikeEvidence) *
                 smoothstep(0.90, 0.995, neutralSurfaceSupport) *
                 smoothstep(0.90, 0.995, targetNeutral) *
-                (1.0 - smoothstep(0.02, 0.12, realColorConfidence)) *
-                (1.0 - measuredColorProtection);
+                (1.0 - smoothstep(0.02, 0.12, realColorConfidence));
+            float sabreDecisiveNeutralCfaProof =
+                smoothstep(0.72, 0.94, physicalFalseColorScore) *
+                smoothstep(0.20, 0.55, sabreSupportEvidence) *
+                smoothstep(0.90, 0.995, neutralSurfaceSupport) *
+                smoothstep(0.90, 0.995, targetNeutral) *
+                (1.0 - smoothstep(0.02, 0.12, realColorConfidence));
+            float decisiveNeutralCfaProof = uSabreSupportValid != 0
+                ? sabreDecisiveNeutralCfaProof : legacyDecisiveNeutralCfaProof;
             float legacyMaximumMove = min(0.050, 0.40 * centerMagnitude);
-            float maximumMove = mix(legacyMaximumMove, legacyDesiredLength,
-                legacyDecisiveNeutralCfaProof);
-            float boundedScale = legacyDesiredLength > 1.0e-7
-                ? min(1.0, maximumMove / legacyDesiredLength) : 0.0;
-            vec3 correctedChroma = centerChroma +
-                legacyDesiredDelta * boundedScale * legacyCorrection;
+            float maximumMove = mix(legacyMaximumMove, desiredLength, decisiveNeutralCfaProof);
+            float boundedScale = desiredLength > 1.0e-7 ? min(1.0, maximumMove / desiredLength) : 0.0;
+            vec3 correctedChroma = centerChroma + desiredDelta * boundedScale * correction;
             if (length(correctedChroma) > centerMagnitude && length(correctedChroma) > 1.0e-7) {
                 correctedChroma *= centerMagnitude / length(correctedChroma);
             }
 
-            /* The physical path may increase or decrease chroma because its target is measured
-             * neighboring color with high per-channel CFA validity. Only physically invalid RGB
-             * components are replaced. The candidate is then uniformly rescaled back to the center
-             * luminance, so valid measured channels are never independently repainted. This is what
-             * protects true colored objects: no neutral assumption and no post-RGB coherence veto. */
-            float physicalAuthority = uSabreSupportValid != 0
-                ? clamp(physicalFalseColorScore, 0.0, 1.0) *
-                    (1.0 - measuredColorProtection)
-                : 0.0;
-            vec3 physicalTargetRgb=max(vec3(centerLuma)+physicalTargetChroma,vec3(0.0));
-            /* Per-channel invalidity is the only component replacement mask. Do not multiply it by
-             * physicalAuthority here and again below: that double attenuation was explicitly
-             * rejected because it can turn strong physical evidence into another visible no-op. */
-            vec3 componentRepair=clamp(channelInvalidity,vec3(0.0),vec3(1.0));
-            vec3 physicalCandidateRgb=mix(center,physicalTargetRgb,componentRepair);
-            float physicalCandidateLuma=luminanceOf(physicalCandidateRgb);
-            if(centerLuma>1.0e-7&&physicalCandidateLuma>1.0e-7){
-                physicalCandidateRgb*=centerLuma/physicalCandidateLuma;
-            }
-            vec3 physicalCandidateChroma=physicalCandidateRgb-vec3(centerLuma);
-            correctedChroma=mix(correctedChroma,physicalCandidateChroma,physicalAuthority);
-
             /* IRIS_26581_GAP_BACKGROUND_CHROMA_RESTORE
              * When the strict paired-edge proof succeeds, the far samples are the actual supported
              * background owner. Restore only toward their observed chroma; never invent a hue or use
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
index 0fd7533d02b6d51fde58e484b8891bbe45d43273..504961829a43f265c1c6d883943414982e393696 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSabreShaders.kt
@@ -2101,18 +2101,12 @@ internal object GlesMgcRawSabreShaders {
         uniform int uSourceClipGuard;
         uniform float uSourceClippingPoint;
         uniform float uSourceClippedWeight;
-        uniform float uValidityWeightScale;
         uniform vec4 uGains;
         uniform vec4 uBlackLevelsTimesGains;
         uniform vec4 uCovRangeRg;
         uniform vec2 uCovRangeB;
         layout(location = 0) out vec4 oColorAndRWeight;
         layout(location = 1) out vec2 oWeightsGb;
-        /* IRIS_26614_RAW_CFA_CHANNEL_VALIDITY_SIDECAR
-         * Third MRT is read-only provenance for the later color owner. It accumulates kernel/frame
-         * weight multiplied by the actual source-code headroom confidence of each CFA channel.
-         * It never feeds temporal weights, color accumulation, Resolve, VGN, DNG or SR detail. */
-        layout(location = 2) out vec4 oValidWeights;
 
         vec2 mirrorUvs(vec2 sampleUv) {
             if (sampleUv.x <= uFrameBorderPadded.x) {
@@ -2200,24 +2194,20 @@ internal object GlesMgcRawSabreShaders {
             vec3 covariance,
             out vec3 accumulatedIntensities,
             out vec3 accumulatedWeights,
-            out float sourceNeighborhoodConfidence,
-            out vec3 validAccumulatedWeights
+            out float sourceNeighborhoodConfidence
         ) {
             accumulatedIntensities = vec3(0.0);
             accumulatedWeights = vec3(0.0);
-            validAccumulatedWeights = vec3(0.0);
             sourceNeighborhoodConfidence = 1.0;
             vec2 coordinateScaled = sampleUv * (vec2(uExtractedSize) * 2.0);
             ivec2 position = ivec2(coordinateScaled);
             mat3 bayerValue = get3x3FromExtractedBayer(position);
-            mat3 sourceValidity = mat3(1.0);
             float headroomStart=max(1.0,uSourceClippingPoint*0.9925);
             for (int sx=0;sx<3;++sx) {
                 for (int sy=0;sy<3;++sy) {
                     float sampleConfidence=1.0-smoothstep(
                         headroomStart,max(headroomStart+1.0e-4,uSourceClippingPoint),
                         bayerValue[sx][sy]);
-                    sourceValidity[sx][sy]=sampleConfidence;
                     sourceNeighborhoodConfidence=min(
                         sourceNeighborhoodConfidence,sampleConfidence);
                 }
@@ -2232,12 +2222,6 @@ internal object GlesMgcRawSabreShaders {
                     );
                 }
             }
-            mat3 validWeights = mat3(0.0);
-            for (int sx=0;sx<3;++sx) {
-                for (int sy=0;sy<3;++sy) {
-                    validWeights[sx][sy]=weights[sx][sy]*sourceValidity[sx][sy];
-                }
-            }
             ivec2 bayerOffset = ivec2(0);
             if (uCfaPattern == 0) bayerOffset = ivec2(1, 1);
             else if (uCfaPattern == 1) bayerOffset = ivec2(0, 1);
@@ -2249,11 +2233,6 @@ internal object GlesMgcRawSabreShaders {
             );
             vec2 upDownWeights = vec2(weights[1][0], weights[1][2]);
             vec2 leftRightWeights = vec2(weights[0][1], weights[2][1]);
-            vec4 validCornerWeights = vec4(
-                validWeights[0][0], validWeights[0][2], validWeights[2][0], validWeights[2][2]
-            );
-            vec2 validUpDownWeights = vec2(validWeights[1][0], validWeights[1][2]);
-            vec2 validLeftRightWeights = vec2(validWeights[0][1], validWeights[2][1]);
             vec4 value1 = vec4(
                 bayerValue[0][0], bayerValue[0][2], bayerValue[2][0], bayerValue[2][2]
             );
@@ -2273,15 +2252,8 @@ internal object GlesMgcRawSabreShaders {
                 dot(leftRightWeights, vec2(1.0)),
                 weights[1][1]
             );
-            vec4 validReorderedWeights = vec4(
-                dot(validCornerWeights, vec4(1.0)),
-                dot(validUpDownWeights, vec2(1.0)),
-                dot(validLeftRightWeights, vec2(1.0)),
-                validWeights[1][1]
-            );
             intensities = swizzleForType(intensities, type);
             reorderedWeights = swizzleForType(reorderedWeights, type);
-            validReorderedWeights = swizzleForType(validReorderedWeights, type);
             accumulatedIntensities = vec3(
                 intensities.r,
                 intensities.g + intensities.b,
@@ -2292,11 +2264,6 @@ internal object GlesMgcRawSabreShaders {
                 reorderedWeights.g + reorderedWeights.b,
                 reorderedWeights.a
             );
-            validAccumulatedWeights = vec3(
-                validReorderedWeights.r,
-                validReorderedWeights.g + validReorderedWeights.b,
-                validReorderedWeights.a
-            );
         }
 
         void main() {
@@ -2309,15 +2276,13 @@ internal object GlesMgcRawSabreShaders {
             vec3 covariance = unpackCovariance(texture(uCovariance, sampleUv).xyz);
             vec3 accumulatedColor = vec3(0.0);
             vec3 accumulatedWeight = vec3(0.0);
-            vec3 validAccumulatedWeight = vec3(0.0);
             float sourceNeighborhoodConfidence = 1.0;
             sampleNeighborhoodRbf(
                 sampleUv,
                 covariance,
                 accumulatedColor,
                 accumulatedWeight,
-                sourceNeighborhoodConfidence,
-                validAccumulatedWeight
+                sourceNeighborhoodConfidence
             );
             float frameWeight = uUseFrameWeight != 0
                 ? texture(uRejection, referenceUv).r
@@ -2329,11 +2294,8 @@ internal object GlesMgcRawSabreShaders {
             }
             accumulatedColor *= frameWeight;
             accumulatedWeight *= frameWeight;
-            validAccumulatedWeight *= frameWeight;
             oColorAndRWeight = vec4(accumulatedColor, accumulatedWeight.r);
             oWeightsGb = accumulatedWeight.gb;
-            oValidWeights = vec4(
-                validAccumulatedWeight / max(uValidityWeightScale, 1.0e-6), 0.0);
         }
     """.trimIndent()
 
diff --git a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
index 809eb0a31e9e0f3b11eaed7fc09cf9885b84e1a3..075315e5b20a9cb1edcd401eea78b2b6711be115 100644
--- a/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
+++ b/app/src/main/java/com/hinnka/mycamera/processor/GlesMgcRawSpatialStacker.kt
@@ -2237,15 +2237,6 @@ internal class GlesMgcRawSpatialStacker(
                 GLES30.GL_RG16F,
                 GLES30.GL_NEAREST,
             )
-            /* IRIS_26614_RAW_CFA_CHANNEL_VALIDITY_OWNER
-             * Additive per-channel valid-measurement weight. Existing color and total-weight
-             * accumulators are byte-for-byte equation owners and this sidecar never feeds them. */
-            val accumulatedValidity26614 = createTexture(
-                width,
-                height,
-                GLES30.GL_RGB10_A2,
-                GLES30.GL_NEAREST,
-            )
             /* IRIS_26564_TRUE_2X_RETIRES_NATIVE_UPSCALE_DETAIL_OWNER
              * Keep the old carrier functions dormant for rollback provenance, but allocate no
              * full-frame 2x luma/detail surface. True 2x is direct multiframe CFA reconstruction.
@@ -2285,7 +2276,6 @@ internal class GlesMgcRawSpatialStacker(
                 accumulatedCoverage,
                 coverageWidth,
                 coverageHeight,
-                accumulatedValidity26614,
             )
             if (normalDngAccumulator != 0) {
                 clearSabreNormalDngAccumulator(normalDngAccumulator)
@@ -2368,11 +2358,6 @@ internal class GlesMgcRawSpatialStacker(
             val highlightShortOneTunnel26604 = highlightShortFrameCount > 0
             val scheduledEvidenceCount26606 = mergedFrameCount + highlightShortFrameCount
             val accumulatedWeightScale = maxSabreAccumulatedWeight(scheduledEvidenceCount26606)
-            /* IRIS_26614_PACKED_VALIDITY_WEIGHT_SCALE
-             * RGB10_A2 stores the validity numerator as normalized fixed point. One 3x3 CFA
-             * neighborhood contributes at most five unit-kernel samples to any single channel;
-             * reserve six units per scheduled evidence frame so additive blending cannot saturate. */
-            val validityWeightScale26614 = 6f * scheduledEvidenceCount26606.coerceAtLeast(1).toFloat()
             renderSabreMerge(
                 extracted = referenceExtracted,
                 flow = zeroFlow,
@@ -2386,8 +2371,6 @@ internal class GlesMgcRawSpatialStacker(
                 useFrameWeight = false,
                 sourceClipGuard = highlightShortOneTunnel26604,
                 sourceClippedWeight = if (highlightShortOneTunnel26604) 0.001f else 1f,
-                accumulatedValidity26614 = accumulatedValidity26614,
-                validityWeightScale26614 = validityWeightScale26614,
             )
             if (enableSabreSuperRes) {
                 val referenceEvidence = persistTrue2xEvidence(
@@ -2702,8 +2685,6 @@ internal class GlesMgcRawSpatialStacker(
                     useFrameWeight = true,
                     sourceClipGuard = highlightShortOneTunnel26604 || frame.role == RawBurstFrameRole.SHADOW_LONG,
                     sourceClippedWeight = 0f,
-                    accumulatedValidity26614 = accumulatedValidity26614,
-                    validityWeightScale26614 = validityWeightScale26614,
                 )
                 if (frame.role == RawBurstFrameRole.HIGHLIGHT_SHORT) {
                     require(index == frames.lastIndex) {
@@ -2999,17 +2980,13 @@ internal class GlesMgcRawSpatialStacker(
                 },
                 sabreSupportR = sabreRSupport26613,
                 sabreSupportGb = resolveAccumulatedWeightsGb26604,
-                sabreValidWeights = accumulatedValidity26614,
-                sabreValidityWeightScale = validityWeightScale26614,
             )
             releaseOwnedTexture(sabreRSupport26613, "26613 Sabre R support provenance")
-            releaseOwnedTexture(accumulatedValidity26614, "26614 Sabre channel validity provenance")
             PLog.i(
                 SABRE_TAG,
-                "IRIS_26614_RAW_CFA_CHANNEL_VALIDITY_PROVENANCE " +
-                    "source=EXACT_CONSUMED_CFA_HEADROOM_WEIGHT_OVER_TOTAL_WEIGHT readOnly=true " +
-                    "changesTemporalWeights=false changesColorAccumulator=false " +
-                    "changesResolve=false changesVgn=false trueColorMeasuredValidityOwner=true",
+                "IRIS_26613_SABRE_RGB_SUPPORT_PROVENANCE " +
+                    "source=COMMON_ACCUMULATED_R_G_B_WEIGHTS readOnly=true " +
+                    "changesTemporalWeights=false changesResolve=false changesVgn=false",
             )
             var postprocessedUi = chromaResult.exportedTextureId
             if (exportGpuLinearRgbSource) {
@@ -4005,8 +3982,6 @@ internal class GlesMgcRawSpatialStacker(
         useFrameWeight: Boolean,
         sourceClipGuard: Boolean,
         sourceClippedWeight: Float,
-        accumulatedValidity26614: Int = 0,
-        validityWeightScale26614: Float = 1f,
     ) {
         val program = sabreMergeProgram
         GLES30.glUseProgram(program)
@@ -4030,7 +4005,6 @@ internal class GlesMgcRawSpatialStacker(
         uniform1i(program, "uSourceClipGuard", if (sourceClipGuard) 1 else 0)
         uniform1f(program, "uSourceClippingPoint", sabreShadowLongSourceClippingPoint())
         uniform1f(program, "uSourceClippedWeight", sourceClippedWeight.coerceIn(0f, 1f))
-        uniform1f(program, "uValidityWeightScale", validityWeightScale26614.coerceAtLeast(1f))
         uniform4fv(program, "uGains", calibration.gains)
         uniform4fv(program, "uBlackLevelsTimesGains", calibration.blackTerms)
         uniform4f(
@@ -4050,11 +4024,7 @@ internal class GlesMgcRawSpatialStacker(
                 program,
                 width,
                 height,
-                if (accumulatedValidity26614 != 0) {
-                    intArrayOf(accumulatedColor, accumulatedWeightsGb, accumulatedValidity26614)
-                } else {
-                    intArrayOf(accumulatedColor, accumulatedWeightsGb)
-                },
+                intArrayOf(accumulatedColor, accumulatedWeightsGb),
                 preserveBlend = true,
             )
         } finally {
@@ -4308,17 +4278,8 @@ internal class GlesMgcRawSpatialStacker(
         accumulatedCoverage: Int,
         coverageWidth: Int,
         coverageHeight: Int,
-        accumulatedValidity26614: Int = 0,
     ) {
         clearRgbAccumulators(accumulatedColor, accumulatedWeightsGb, width, height)
-        if (accumulatedValidity26614 != 0) {
-            bindRenderTargets(intArrayOf(accumulatedValidity26614), "26614 Sabre channel validity clear")
-            GLES30.glViewport(0, 0, width, height)
-            GLES30.glClearBufferfv(
-                GLES30.GL_COLOR, 0, floatArrayOf(0f, 0f, 0f, 0f), 0,
-            )
-            GLES30.glBindFramebuffer(GLES30.GL_FRAMEBUFFER, 0)
-        }
         clearSabreCoverage(
             accumulatedCoverage,
             coverageWidth,
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
index 48ad83b873eb489eb2f28372677e684fe678ed30..8d7310713e49e38f27dcf60a4040b4dff408ca2d 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2AdaptiveColorAppearance.java
@@ -76,9 +76,9 @@ public final class MotionV2AdaptiveColorAppearance extends Node {
                 + " IRIS_26598_SEMANTIC_AUTHORITY=true"
                 + " toneAwareHighlightChroma=true"
                 + " displayGain=" + displayGain
-                + " iris26614SdrWhiteAnchor=" + MotionV2Render.IRIS_26614_SDR_WHITE_ANCHOR
-                + " iris26614DisplayDomainKneeAuthority=false"
-                + " IRIS_26614_SOURCE_DOMAIN_TONE_PREDICTOR_PARITY=true"
+                + " iris26613SdrKneeFinal=" + MotionV2Render.IRIS_26613_SDR_KNEE_FINAL
+                + " iris26613QuantileToneAuthority=false"
+                + " IRIS_26613_FIXED_DOMAIN_TONE_PREDICTOR_PARITY=true"
                 + " irisNight=" + basePipeline.mParameters.irisNightActive
                 + " superRes=" + basePipeline.mParameters.motionV2SuperResOutputEnabled
                 + " superResScale=" + basePipeline.mParameters.motionV2SuperResOutputScale
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
index 28611650a88ad5dc878cdad4988bf65e25264700..56d4a76088f71d787b14f36221f60978095fe7af 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2Render.java
@@ -95,38 +95,26 @@ public final class MotionV2Render extends Node {
     private static final float HDR_EXPOSURE_SCALE = OUTPUT_EXPOSURE_SCALE;
     private static final int GAINMAP_DOWNSAMPLE = 1;
 
-    /* IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_APPEARANCE
-     * 26613 solved viewfinder brightness in the dark/body range but then applied that full gain
-     * before a display-domain 0.80 knee. Device ceiling, bathroom and curtain pairs prove that
-     * ordinary bright source structure was therefore driven into the shoulder while the UHDR
-     * master still retained the correct appearance. 26614 keeps the solved body slope at black,
-     * removes that extra gain continuously as the physical source approaches nominal white, and
-     * reserves the final 5% of SDR code space for genuine >1 extended HDR. The function is one
-     * fixed C1 monotonic source-domain map: no scene percentile, object class or moving knot. */
-    public static final float IRIS_26614_SDR_WHITE_ANCHOR = 0.95f;
-
-    static float iris26614MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
-        float x = Math.max(sourceGuide, 0.0f);
-        float requestedFinalGain = Math.max(brightnessTargetGain, 1.0e-6f)
+    /* IRIS_26613_FIXED_DOMAIN_UPPER_HIGHLIGHT_PRESENTATION
+     * 26612's moving p99/p995/p998 knots are retired as rendition authorities. Motion publication
+     * is exact identity through 0.80 final-linear and uses one C1 rational shoulder above it.
+     * Scene statistics remain diagnostic inputs elsewhere only; they cannot move this curve. */
+    public static final float IRIS_26613_SDR_KNEE_FINAL = 0.80f;
+
+    static float iris26613MapMotionSdrSourceFinal(float sourceFinal) {
+        if (sourceFinal <= IRIS_26613_SDR_KNEE_FINAL) return sourceFinal;
+        float reserve = 1.0f - IRIS_26613_SDR_KNEE_FINAL;
+        float excess = sourceFinal - IRIS_26613_SDR_KNEE_FINAL;
+        return IRIS_26613_SDR_KNEE_FINAL + reserve * excess / (excess + reserve);
+    }
+
+    static float iris26613MapMotionSdrFinalGuide(float sourceGuide, float brightnessTargetGain) {
+        float sourceFinal = sourceGuide * Math.max(brightnessTargetGain, 1.0e-6f)
                 * OUTPUT_EXPOSURE_SCALE;
-        float whiteAnchor = Math.min(IRIS_26614_SDR_WHITE_ANCHOR, requestedFinalGain);
-        float bodyGain = requestedFinalGain;
-        if (requestedFinalGain > whiteAnchor) {
-            bodyGain = Math.min(requestedFinalGain, 4.0f * whiteAnchor - 1.0e-4f);
-        }
-        if (x <= 1.0f) {
-            float oneMinus = 1.0f - x;
-            return whiteAnchor * x
-                    + (bodyGain - whiteAnchor) * x * oneMinus * oneMinus;
-        }
-        float reserve = 1.0f - whiteAnchor;
-        if (reserve <= 1.0e-6f) return whiteAnchor;
-        float tailScale = reserve / Math.max(whiteAnchor, 1.0e-6f);
-        float excess = x - 1.0f;
-        return whiteAnchor + reserve * excess / (excess + tailScale);
+        return iris26613MapMotionSdrSourceFinal(sourceFinal);
     }
 
-    static float iris26614MapHdrTargetLuma(float hdrBase) {
+    static float iris26613MapHdrTargetLuma(float hdrBase) {
         return hdrBase;
     }
 
@@ -469,7 +457,7 @@ public final class MotionV2Render extends Node {
                                     + " actualPeakContentRatio=" + actualPeakContentRatio
                                     + " fullHdrDisplayRatio=" + fullHdrDisplayRatio
                                     + " separateBodyGainOwner=false"
-                                    + " hdrGainSource=CANONICAL_SDR_PLUS_SOURCE_HEADROOM_ONLY"
+                                    + " hdrGainSource=HEALTHY_HDR_MASTER_VS_SDR"
                                     + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
                                     + " hdrExposureScale=" + HDR_EXPOSURE_SCALE);
                 } catch (Throwable ignored) {}
@@ -504,11 +492,12 @@ public final class MotionV2Render extends Node {
                         + basePipeline.mParameters.motionV2ReconstructionOwner
                         + " pointDecimation=false"
                         + " postAliasSpikeRepair=false"
-                        + " midtoneGainUnity=true sourceAtOrBelowWhiteGainUnity=true"
+                        + " midtoneGainUnity=true"
                         + " sdrExposureScale=" + OUTPUT_EXPOSURE_SCALE
-                        + " motionHdrBodyGainRatio=1.0"
-                        + " motionHdrGainStartsAboveSourceWhite=true"
-                        + " IRIS_26614_UHDR_BODY_APPEARANCE_PARITY=true");
+                        + " hdrTargetExposureScale=" + HDR_EXPOSURE_SCALE
+                        + " nominalBodyRecoveryRatio="
+                            + (HDR_EXPOSURE_SCALE / OUTPUT_EXPOSURE_SCALE)
+                        + " IRIS_26506_HDR_BODY_RECOVERY_GAINMAP=true");
             } finally {
                 if (gainTexture != null) {
                     try { gainTexture.close(); } catch (Throwable ignored) {}
@@ -581,10 +570,10 @@ public final class MotionV2Render extends Node {
                 + " syntheticBitmapGainMap=false"
                 + " irisOutputZoom=" + irisOutputZoom
                 + " nativeOutputDimensionsPreserved=true"
-                + " iris26614SdrWhiteAnchor=" + IRIS_26614_SDR_WHITE_ANCHOR
-                + " iris26614DisplayDomainKneeAuthority=false"
-                + " iris26614CanonicalAppearanceGainAuthority=true"
-                + " IRIS_26614_CANONICAL_SOURCE_DOMAIN_SDR_UHDR_SR_PARITY=true"
+                + " iris26613SdrKneeFinal=" + IRIS_26613_SDR_KNEE_FINAL
+                + " iris26613QuantileToneAuthority=false"
+                + " iris26613CleanHdrMasterGainAuthority=true"
+                + " IRIS_26613_FIXED_DOMAIN_SDR_UHDR_SR_PARITY=true"
                 + " localTone=false"
                 + " sharpening=false");
     }
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
index 6c4469dc1436c4862a9628994aff2a19f96c1e63..69c654a75ac332052d8820f34e3fce0d6242e16a 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/opengl/postpipeline/MotionV2ViewfinderExposureMatcher.java
@@ -264,7 +264,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
                     + " solvedEv=" + solvedEv
                     + " cameraId=" + cameraIdForLog()
                     + " brightnessTargetGain=" + gain
-                    + " meterToneAuthority=IRIS_26614_CANONICAL_SOURCE_DOMAIN finalHighlightAuthority=IRIS_26614_CANONICAL_SOURCE_DOMAIN"
+                    + " meterToneAuthority=IRIS_26604_CANONICAL_FOR_MOTION finalHighlightAuthority=IRIS_26604_CANONICAL_FOR_MOTION"
                     + " irisNight=" + iris26550Night
                     + " candidateP99Guide=" + iris26550CandidateP99Guide
                     + " fixedCandidateSampleSet=true"
@@ -819,7 +819,7 @@ public final class MotionV2ViewfinderExposureMatcher extends Node {
         if (guide <= 1.0e-7f) return 0.0f;
         float mappedFinal;
         if (basePipeline.mParameters.motionV2Active) {
-            mappedFinal = MotionV2Render.iris26614MapMotionSdrFinalGuide(guide, gain);
+            mappedFinal = MotionV2Render.iris26604MapMotionSdrFinalGuide(guide, gain);
         } else {
             float preGuide = guide * Math.max(gain, 1.0e-6f);
             mappedFinal = MotionV2Render.iris26582MapHeadroom(preGuide, sceneWhite)
diff --git a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
index e4c71385582dbae3b14a46cad3f8010f2e1b475a..aaccaec3762a73d1a353fc6e553b701fdaf2392c 100644
--- a/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
+++ b/app/src/main/java/com/particlesdevs/photoncamera/processing/ultrahdr/MotionV2Jpeg444Encoder.java
@@ -179,10 +179,10 @@ public final class MotionV2Jpeg444Encoder {
                     + " gainEncodingMax=" + true2xGainEncodingMax
                     + " gainContentMax=" + true2xGainContentMax
                     + " publicationSceneWhite=" + publicationSceneWhite
-                    + " iris26614SdrWhiteAnchor=" + MotionV2Render.IRIS_26614_SDR_WHITE_ANCHOR
-                    + " iris26614DisplayDomainKneeAuthority=false"
-                    + " iris26614CanonicalAppearanceGainAuthority=true"
-                    + " IRIS_26614_TRUE2X_SOURCE_DOMAIN_PRESENTATION=true"
+                    + " iris26613SdrKneeFinal=" + MotionV2Render.IRIS_26613_SDR_KNEE_FINAL
+                    + " iris26613QuantileToneAuthority=false"
+                    + " iris26613CleanHdrMasterGainAuthority=true"
+                    + " IRIS_26613_TRUE2X_FIXED_DOMAIN_PRESENTATION=true"
                     + " adaptiveSceneWhite=" + parameters.motionV2ToneAdaptiveSceneWhite
                     + " publicationSceneWhiteSource="
                         + (parameters.motionV2Active ? "BASE_26598_MOTION" : "ADAPTIVE_26591_NIGHT")
diff --git a/app/version.properties b/app/version.properties
index 1529e77152446c36d7bbc901c2ec2c0602fc9fdf..5c73e00ad08692a0137df5b570c5ecb5cb12a6ba 100644
--- a/app/version.properties
+++ b/app/version.properties
@@ -1,4 +1,4 @@
 VERSION_MAJOR=0
 VERSION_MINOR=9726440
-VERSION_NAME=0.9726614
-VERSION_BUILD=26614
+VERSION_NAME=0.9726613
+VERSION_BUILD=26613
