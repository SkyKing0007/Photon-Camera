IRIS 26565 PERMANENT REGRESSION — V1 REPOSITORY-WIDE DEPENDENCY SCOPE

GitHub Actions failure: run 33322029512, job 99285563475.
Failed handoff commit: c775e067b5e73e3297dcd00f5cd0423da8cfa93f.

Observed failure:
The sealed V1 workflow hash/preflight step passed, then the actual packaged build script
failed before downloading the successful 26564 authority with:
  ERROR: NumPy import regression

Root cause:
The dependency regression used repository-wide shell globs:
  "$ROOT"/*.py
and the adjacent package-manager regression also used:
  "$ROOT"/*.py "$ROOT"/*.sh
In clean-package replay ROOT contained only the handoff package, while GitHub Actions ROOT
was the full historical repository. Unrelated historical files therefore changed the result.
That violated the strict requirement that local packaged pre-build replay use the same
comparison semantics and effective inputs as GitHub Actions.

Permanent conditions for this and every applicable future handoff:
1. Dependency/package-manager gates must enumerate the exact sealed handoff scripts they own.
2. Unrelated repository files must never participate in a sealed handoff dependency decision.
3. A permanent fixture must place `import numpy` and `pip install numpy` in unrelated decoy
   repository files and prove the sealed gate still passes.
4. The same fixture must prove those exact patterns are detected when placed in a file that
   is part of the sealed dependency inventory.
5. The corrected package must be replayed from a repository-shaped polluted root using the
   exact successful 26564 V1.4 artifact before handoff.
6. Runtime candidate hashes, forward/rollback patches, DNG, protected color/Night/GLSL,
   version/build, and processing architecture must remain byte-identical to 26565 V1.
